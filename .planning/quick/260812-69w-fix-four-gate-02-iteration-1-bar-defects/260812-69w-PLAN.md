---
phase: quick-260812-69w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickshell/.config/quickshell/modules/bar/BarTooltip.qml
  - quickshell/.config/quickshell/modules/bar/BarTooltipHost.qml
  - quickshell/.config/quickshell/modules/bar/qmldir
  - quickshell/.config/quickshell/modules/bar/IdleInhibitorCapsule.qml
  - quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml
  - quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml
  - quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml
  - quickshell/.config/quickshell/modules/bar/MediaPopout.qml
  - quickshell/.config/quickshell/modules/bar/AudioPopout.qml
  - quickshell/.config/quickshell/modules/bar/SectionPopout.qml
  - quickshell/.config/quickshell/modules/bar/BarCapsule.qml
  - hypr/.config/hypr/scripts/quickshell-doctor
autonomous: true
requirements: [GATE-02, GATE-03, QBAR-09, QBAR-12]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "F2 — a bar glyph's hover label renders on a surface whose top edge sits below the bar's bottom edge in horizontal orientation (and left of the bar's left edge in vertical), never overlapping the glyph it describes. Proven by a measured layer-surface rectangle, not by a look."
    - "F1 — the clock/time pill's rendered vertical centre sits within 1px of its sibling capsules' content centres, and the five capsules that already looked right did not move by even 1px as a result of the fix. Both halves measured, before and after."
    - "F3 — toggling the idle bulb changes the whole capsule pill to the accent fill, the same 'on' language the active workspace pill already uses, so the state reads at bar scale independently of whether the FILL variable axis works on this host."
    - "F4 — the media popout's transport row is horizontally centred in the popout body: its measured x equals (bodyColumn.width - transportRow.width) / 2."
    - "No temporary probe survives into any commit — the probe-tag grep returns nothing at every commit point."
    - "The bar's reservation is still [0,48,0,0] and quickshell is still pid 528309 at the end of every task — no restart, no hyprctl reload, no hyprctl eval."
  artifacts:
    - "quickshell/.config/quickshell/modules/bar/BarTooltip.qml — the escaping tooltip surface (Branch B only)"
    - "quickshell/.config/quickshell/modules/bar/BarTooltipHost.qml — the dwell-timer + LazyLoader host each site mounts (Branch B only)"
    - "quickshell/.config/quickshell/modules/bar/qmldir — one registration line per new type, added in the same commit that creates it"
    - "hypr/.config/hypr/scripts/quickshell-doctor — one added QSD_BAR_SURFACE_ROWS row for the new namespace (Branch B only)"
  key_links:
    - "A QtQuick.Controls ToolTip is a Popup and is clamped to its own window. Bar.qml is a PanelWindow whose horizontal implicitHeight is Design.barHeight (42). That geometry — not a missing property — is the likely reason the tooltip lands on the glyph, and Task 1 measures it before Task 2 picks a fix shape."
    - "A new bar-family layer namespace must resolve to EXACTLY ONE row in quickshell-doctor's _qsd_assert_bar_surface_registry_live. quickshell-bardrawer- is the precedent for why the prefix must not be quickshell-bar-."
    - "Tooltip placement must reuse SectionPopout.qml's own anchoring expression pair (_horizontalTopMargin/_horizontalClampedLeft, _verticalRightMargin/_verticalClampedTop) rather than growing a second, contradictory anchoring rule (18-UI-SPEC.md § Anchoring per orientation)."
    - "BarCapsule.qml has six consumers. Any edit to its shared contentGrid must be shown by measurement not to move the five capsules the operator did not complain about."
    - "Every colour under modules/bar/ routes through BarRoles.* (D-20) and no capsule fill may sit below the ^quickshell-.* family layer rule's ignore_alpha floor of 0.5."
---

<objective>
Fix the four defects the operator reported during GATE-02 iteration 1, using the method this
file family has repeatedly proven is the only one that works here: measure first, edit second.

Purpose: `## Deletion Authorisation` in `18-GATE-02-RECORD.md` stays `RETIRE-02 BLOCKED` until
these four are closed and all fifteen rows are re-observed. This plan closes the four; the
re-observation belongs to plan 18-19, not here.

Output: fixes for F1 (clock pill alignment), F2 (tooltips over their own glyph), F3 (idle bulb
feedback) and F4 (media transport centring), each landing with the measurement that proves it
or an explicit statement that it is claimed by construction and still needs the operator's eyes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/18-qml-bar-retirement-machinery/18-GATE-02-RECORD.md
@.planning/phases/18-qml-bar-retirement-machinery/18-UI-SPEC.md
@quickshell/.config/quickshell/modules/bar/BarDrawer.qml
@quickshell/.config/quickshell/modules/bar/SectionPopout.qml
</context>

<operating_rules>
These bind every task in this plan. Violating one voids the work regardless of the result.

1. **`hyprctl reload` and `hyprctl eval` are PROHIBITED**, phase-wide. `hyprctl dispatch 'hl.dsp.*'`
   is the same thing in another spelling and is prohibited too. `hyprctl monitors -j`,
   `hyprctl layers -j` and `hyprctl clients -j` are read-only and allowed.
2. **Do NOT restart quickshell.** It hot-reloads on file change. The reload path is
   `touch quickshell/.config/quickshell/shell.qml`, then read the live instance log. A QBAR-11
   soak window is anchored on **pid 528309**; `pgrep -x quickshell` must return 528309 at the
   start and end of every task. If a restart becomes unavoidable, stop, and say so at the TOP of
   the SUMMARY so the orchestrator re-anchors the soak window.
3. **The live log is not in journalctl.** It is the newest `/run/user/1000/quickshell/by-id/*/log.log`
   — resolve it with `ls -dt /run/user/1000/quickshell/by-id/*/log.log | head -1`. A reload is
   confirmed by a `Configuration Loaded` line whose timestamp is later than the edit.
4. **Do NOT change `Bar.qml`'s reservation or `exclusiveZone`.** `hyprctl monitors -j | jq '.[0].reserved'`
   must still return `[0,48,0,0]` at the end of every task.
5. **Do NOT edit `18-GATE-02-RECORD.md`.** It belongs to plan 18-19 and the gate re-run.
6. Do not regress the horizontal drawer path (GATE-02 round 4 tuned it against operator feedback).
   Do not run `git stash`. Do not touch `waybar/`. Do not run plan 18-20.
7. **The executor cannot move the pointer on this host.** `movecursor` is rejected by Hyprland
   0.56.2's Lua dispatch form and `wtype` is keyboard-only. So no verify step in this plan may
   depend on a hover or a click being performed. Every verify is structural or
   instrumented-numeric. Where a fix cannot be proven that way, the SUMMARY says so in the words
   required by Task 3.
8. **One probe tag for the whole plan.** Every temporary instrumentation line carries the literal
   tag in a trailing comment so a single grep finds all of it. No probe may survive into a
   commit. <!-- planner-discipline-allow: Q69PROBE -->
</operating_rules>

<tasks>

<task type="tracer">
  <name>Task 1: Measure all four defects before touching a single fix</name>
  <files>quickshell/.config/quickshell/modules/bar/BarCapsule.qml, quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml, quickshell/.config/quickshell/modules/bar/IdleInhibitorCapsule.qml, quickshell/.config/quickshell/modules/bar/MediaPopout.qml, quickshell/.config/quickshell/modules/bar/AudioPopout.qml</files>
  <precondition>`pgrep -x quickshell` returns 528309 and `git status --porcelain -- quickshell/` is empty. If either is false, halt and report — the probe teardown in this task reverts by path and would discard unrelated work.</precondition>
  <read_first>
    quickshell/.config/quickshell/modules/bar/BarCapsule.qml (contentGrid, lines 112-121),
    quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml (the clock cell: contentHost,
    clockFillPill, clockTriggerGrid, clockCellRightPad — lines 95-200),
    quickshell/.config/quickshell/modules/bar/IdleInhibitorCapsule.qml (idleCell, idleGlyph,
    idleMouseArea, the hardcoded fillAxisAvailable at line 44).
  </read_first>
  <action>
This task writes NO fix. It adds temporary instrumentation, reads real numbers back out of the
live instance log, records them, and removes the instrumentation again. Every added line carries
the trailing tag comment `Q69PROBE` so one grep finds all of it.

Nothing here may be skipped on the grounds that the source reads unambiguously. This file family
has disproven four confident code-reading hypotheses — three during Phase 18.1's spacing work and
one on 2026-08-12 on the hover path, where `MA.entered` fired 6/5/4 times on the working sections
and zero on the broken ones, replacing a plausible source-read story with a measured one.

**Probe A — F1, the alignment numbers.** Add a one-shot non-repeating `Timer` to `BarCapsule.qml`
(interval about 1500ms, `running: true`, `repeat: false`) that logs, for the capsule it is running
inside: `capsuleId`, the capsule's own `x`, `y`, `width`, `height`, its `mapToItem(null, 0, 0)`
scene position, and `contentGrid`'s `x`, `y`, `width`, `height`. Then iterate `contentGrid.children`
and log per child its index, `x`, `y`, `width`, `height`, `implicitHeight`, and its own
`mapToItem(null, 0, 0)` scene y. Because the probe sits on the shared chrome it fires once per
capsule, which delivers the intra-capsule comparison and the capsule-to-capsule comparison in one
pass — that second half is exactly the "does the fix shift a capsule that currently looks right"
question, captured as a baseline before anything moves.

Add a second block to `ClockActionsCapsule.qml`, same tag, same one-shot timer shape, logging the
clock cell's internals specifically: `contentHost`'s `height` and its `childrenRect` `x`/`y`/`width`/`height`,
`clockTriggerGrid`'s `x`/`y`/`height`, `clockFillPill`'s `x`/`y`/`height`, and each of those three
mapped through `mapToItem(null, 0, 0)`.

There are two live candidate causes and this probe exists to discriminate between them, not to
confirm either. **Candidate 1:** `Grid` defaults to `AlignTop` for its items and no
`verticalItemAlignment` is set anywhere in `BarCapsule.qml`, so a child shorter than the tallest in
the row rides high. **Candidate 2:** `clockFillPill` is `anchors.centerIn: clockTriggerGrid` and is
`barCapsulePadding * 2` taller than that Grid, so it extends above the Grid's own top edge; if
`contentHost` sizes off `childrenRect`, that overhang lands at a negative `childrenRect.y` and the
cell's content is offset inside its allocated slot. Candidate 1 predicts every capsule's children
share a top edge and differ at the bottom. Candidate 2 predicts a negative `contentHost.childrenRect.y`
and a clock cell whose scene-y differs from the neighbouring ActionCells while those cells agree
with each other. Read which prediction the numbers match. If they match neither, say so and record
the numbers rather than forcing a story onto them.

**Probe B — F2, whether a repositioned tooltip can even be rendered.** The load-bearing unknown is
not whether the six sites set a position (they demonstrably do not) but whether setting one would
help: a `QtQuick.Controls` `ToolTip` is a `Popup`, `Popup` is clamped to its own window, and
`Bar.qml` is a `PanelWindow` whose horizontal `implicitHeight` is `Design.barHeight` (42). If that
clamp is real, setting `y` produces a tooltip that is invisible instead of misplaced, which is a
worse defect than the one being fixed.

In `IdleInhibitorCapsule.qml`, add a one-shot timer that calls `idleMouseArea.ToolTip.show("probe", 6000)`,
and a second one-shot timer roughly 400ms later that logs: `idleMouseArea.ToolTip.toolTip.x`, `.y`,
`.width`, `.height`, `.visible`, and `.parent`; `idleCell`'s `mapToItem(null, 0, 0)` and its width
and height; and the bar window's own `width` and `height` (reachable from the capsule's window
handle). Then, in a third one-shot timer, assign `idleMouseArea.ToolTip.toolTip.y = 60` and log the
resolved `.y` back. The decisive readings are two: does the tooltip rectangle fall inside
`[0, 42]` on the bar window's vertical axis, and does an explicitly assigned y of 60 survive or get
clamped back? Record both numbers verbatim.

Add the same shape of probe to `AudioPopout.qml`'s `audioMuteMouseArea` — but that site only exists
while the audio popout is mounted, so precede it with a one-shot timer calling
`PopoutController.open("audio")` and follow the logging with `PopoutController.close()`. Log the
tooltip rectangle against the popout window's own `width`/`height`. The two popout-hosted tooltip
sites live in a window several hundred pixels tall and may already have room; this measurement is
what decides whether they need converting at all, rather than a guess in either direction.

**Probe C — F3, whether the FILL variable axis actually does anything on this host.**
`IdleInhibitorCapsule.qml` line 44 declares `readonly property bool fillAxisAvailable: true` as a
hardcoded literal — that is a claim about the font build, not a measurement of it, so logging the
property alone proves nothing. Log it anyway (the plan asks for it), then prove or disprove it.
Add two off-screen `Text` items with identical `text: "lightbulb"`, identical
`font.family: Design.symbolFontFamily` and identical `font.pixelSize: Design.barGlyphSize`,
differing only in `font.variableAxes` — one at FILL 0, one at FILL 1 — and a one-shot timer that
calls `grabToImage` on each and `saveToFile` into the scratch directory. Then compare the two files
with `cmp -s`. Identical bytes means the axis is inert on this host and the bulb's entire feedback
today is a small glyph changing colour, which is exactly what the operator reported. Differing bytes
means the axis works and the feedback is simply too small to read at bar scale. Either verdict
changes nothing about Task 3's fix, but it must be recorded, because the SUMMARY has to state which
of the two the operator was actually looking at.

Alongside it, run the host-level check: `fc-list -f '%{file}\t%{family}\t%{variable}\n' | grep -i "material symbols rounded"`
and `fc-match "Material Symbols Rounded" file`. Record the resolved file name — a variable build
carries its axis list in the filename — and note that fontconfig's own `%{variable}` column is
inconsistent across the entries for this family on this host, which is precisely why the render
diff above is the deciding evidence and the fontconfig output is only corroboration.

**Probe D — F4, the transport row's actual x.** In `MediaPopout.qml`, add a one-shot timer logging
`transportRow`'s `x`, `width`, `visible`, `mediaRow.width`, and `bodyColumn.width` reached through
`transportRow.parent`. Because the media popout only exists while summoned, precede it with a
one-shot `PopoutController.open("media")` and follow it with `PopoutController.close()`. Expected
baseline if the source read is right: `transportRow.x` is 0 while its siblings span the full
`bodyColumn.width`. Record the real number either way — this is the before-value Task 2's fix is
measured against.

**Running the probes.** Save the files, then `touch quickshell/.config/quickshell/shell.qml`, wait
a few seconds, resolve the newest log with `ls -dt /run/user/1000/quickshell/by-id/*/log.log | head -1`,
and read the probe lines out of it. Confirm a `Configuration Loaded` line later than the edit, and
confirm `pgrep -x quickshell` still returns 528309 — a changed pid means quickshell restarted and
the soak window is gone, which must be reported immediately rather than worked around.

**Teardown, inside this task.** Revert every probe with `git checkout --` on exactly the five files
listed above (they carry no other uncommitted change — the precondition asserted that), `touch`
shell.qml again so the live instance returns to the committed source, confirm the pid is still
528309, and confirm the probe-tag grep over the quickshell tree returns nothing. Nothing is
committed by this task. The measurements live in the task record and are carried forward to Tasks
2 and 3 in the SUMMARY.
  </action>
  <verify>
    <automated>test "$(pgrep -x quickshell)" = "528309" && hyprctl monitors -j | jq -e '.[0].reserved == [0,48,0,0]' >/dev/null && ! grep -rn "Q69PROBE" /home/aorus/dotfiles/quickshell/ && git -C /home/aorus/dotfiles status --porcelain -- quickshell/ | grep -q . && echo "FAIL: quickshell tree dirty after teardown" && exit 1 || echo OK</automated>
    <human-check>None. Every reading in this task is a number from the log, not a look at the screen.</human-check>
  </verify>
  <done>
All four measurement sets are recorded as real numbers copied out of the live log: (A) per-capsule
and per-child geometry for every capsule plus the clock cell's internals, with a stated verdict on
which of the two named candidate causes the numbers match, or an explicit "neither"; (B) the
tooltip rectangle against the 42px bar window and the survive-or-clamp result of the assigned y,
plus the same pair for the audio popout site; (C) the FILL-axis render-diff verdict and the
resolved font file; (D) `transportRow.x` and `bodyColumn.width`. The probe-tag grep returns
nothing, the quickshell tree is clean, the pid is still 528309 and the reserved array is still
`[0,48,0,0]`. No fix has been written.
  </done>
</task>

<task type="auto">
  <name>Task 2: F2 — one tooltip placement rule for the whole bar — and F4 — centre the transport row</name>
  <files>quickshell/.config/quickshell/modules/bar/BarTooltip.qml, quickshell/.config/quickshell/modules/bar/BarTooltipHost.qml, quickshell/.config/quickshell/modules/bar/qmldir, quickshell/.config/quickshell/modules/bar/IdleInhibitorCapsule.qml, quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml, quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml, quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml, quickshell/.config/quickshell/modules/bar/AudioPopout.qml, quickshell/.config/quickshell/modules/bar/SectionPopout.qml, quickshell/.config/quickshell/modules/bar/MediaPopout.qml, hypr/.config/hypr/scripts/quickshell-doctor</files>
  <precondition>Task 1's Probe B and Probe D readings are in hand. This task's first decision — which of the two F2 branches to take — is made from those numbers and from nothing else.</precondition>
  <read_first>
    quickshell/.config/quickshell/modules/bar/BarDrawer.qml in full — it is the template for a
    LazyLoader-gated escaping surface and its header records every reason each posture line is
    what it is; quickshell/.config/quickshell/modules/bar/SectionPopout.qml lines 120-180 (the
    anchoring expression pair for both orientations); modules/bar/qmldir (the registration rule);
    hypr/.config/hypr/scripts/quickshell-doctor lines 256-310 (the bar-surface registry and why
    the namespace prefix is load-bearing).
  </read_first>
  <action>
**F4 first — it is one line and it unblocks a measurement the rest of this task reuses.**
In `MediaPopout.qml`, `transportRow` (about line 168) is the only child of the popout body with
neither `width: parent.width` nor a centring anchor; `mediaRow` (:66), the progress line (:145) and
`multiPlayerText` (:263) all set `width: parent.width`. Its parent is `SectionPopout.qml`'s
`bodyColumn`, a `Column` with a fixed `width` of `Design.popoutMaxWidth - Design.spacingMd * 2`.
Add `anchors.horizontalCenter: parent.horizontalCenter` to `transportRow`. Use that anchor
specifically and NOT `anchors.centerIn` and NOT `width: parent.width`: a `Column` positions its
children on the vertical axis only, so a horizontal-centre anchor is legal on a Column child while
`centerIn` and any vertical anchor are not and would draw the "Cannot specify ... anchors for items
inside Column" warning and silently break the positioner. `width: parent.width` would stretch the
Row without centring its three buttons, since a Row lays its children out from its own x=0.

**F2 — branch on Task 1's Probe B numbers.**

*Branch A applies only if* Probe B showed the tooltip rectangle can sit outside the 42px bar window
AND an explicitly assigned y of 60 survived unclamped. Then the fix is local to each site: replace
the attached-property usage with a declared `ToolTip` instance per site carrying an explicit
placement, below the item in horizontal orientation and to its left in vertical, reading
orientation from `BarEntryModel.isVertical` exactly as `BarCapsule.qml` already does. Keep
`Design.tooltipDelayMs` unchanged. Skip the rest of this action's Branch B material.

*Branch B applies if the tooltip was clamped inside the bar window* — the expected outcome, since a
`Popup` cannot leave its window. A property nobody set is not the whole story here: the container
cannot hold the fix, so the fix is a container that can. Build it once, as two new files under
`modules/bar/`.

`BarTooltip.qml` — a `PanelWindow` whose layer posture is copied from `BarDrawer.qml` line for
line, for the reasons that file's own header states: `exclusiveZone: 0` written as a bare literal
(GATE-03's forward closure greps for that literal form on every `noreserve` row),
`exclusionMode: ExclusionMode.Ignore`, `WlrLayershell.layer: WlrLayer.Overlay`,
`WlrLayershell.keyboardFocus: WlrKeyboardFocus.None`, `focusable: false`, `color: "transparent"`.
Its namespace is `"quickshell-bartip-" + tipId`. That prefix choice is load-bearing and must not be
tidied: `quickshell-bartip-` still begins with `quickshell-bar` so the doctor's broad bar-family
candidate net still captures it, and with `quickshell-` so the family-wide blur and ignore_alpha
Hyprland layer rules still apply, while resolving to exactly one registry row —
a `quickshell-bar-…` namespace would match both `SectionPopout`'s pattern row and its own and be
counted unmatched. Its public contract is a `text` string, a `vertical` bool and a `triggerCentre`
real. Its geometry is content-sized: implicit width from the label plus `Design.spacingSm` per side,
implicit height from the label plus `Design.spacingXs` per side. Its anchoring is the SAME
expression pair `SectionPopout.qml` already declares, transcribed rather than re-derived — the
horizontal pair off its `_horizontalTopMargin` / `_horizontalClampedLeft` and the vertical pair off
its `_verticalRightMargin` / `_verticalClampedTop` — so this surface obeys 18-UI-SPEC's one
anchoring rule (below the bar in horizontal, leftward in vertical, clamped inward from the screen
edge) instead of growing a second contradictory one. Carry a comment saying to diff that block
against `SectionPopout.qml` whenever either changes, the same instruction `BarDrawer.qml` carries.
Its chrome is one `Rectangle` at `Design.barCapsuleRadius` filled `BarRoles.capsule` and one `Text`
in `BarRoles.capsuleFg` at `Design.barBodySize`. Every colour routes through `BarRoles.*` — this
file is new and is deliberately NOT added to quickshell-doctor's colour-role exempt array — and
`BarRoles.capsule` is 0.85 alpha, comfortably above the family layer rule's ignore_alpha floor of
0.5, which no fill in this file may drop below. No `HoverHandler`, no `MouseArea`, no `Timer`, no
second positioner: this surface displays a string and nothing else.

`BarTooltipHost.qml` — a plain `Item` with `width: 0` and `height: 0` that each site declares once
as a sibling of its hover target. It carries `property Item anchorItem`, `property string text`,
`property bool active`, and `property string tipId`. Inside it: one non-repeating `Timer` at
`Design.tooltipDelayMs` running while `active` is true, and one `LazyLoader` whose `active` is the
timer having fired and `active` still being true, mounting one `BarTooltip`. On the timer firing,
compute the anchor once and assign it — `anchorItem.mapToItem(null, anchorItem.width / 2, anchorItem.height / 2)`,
taking x in horizontal orientation and y in vertical — and pass it as `triggerCentre`. Publish it
ONCE at that moment rather than as a live binding: scene mapping does not re-evaluate when an
ancestor moves, which is the reason `SectionPopout` and `BarDrawer` both publish their anchors the
same way. Read orientation from `BarEntryModel.isVertical`. When `active` goes false the loader
deactivates and the surface is destroyed, so nothing exists while nothing is hovered — the
`LazyLoader` lifecycle `HotZone.qml` established under D-18-24.

Register both types in `modules/bar/qmldir` as ordinary instance types, not singletons, in the SAME
commit that creates them — that manifest's header records that an unregistered type becomes
unresolvable with NO load error. Add exactly one row to `QSD_BAR_SURFACE_ROWS` in
`hypr/.config/hypr/scripts/quickshell-doctor`, in the same shape as the `BarDrawer` row:
file `bar/BarTooltip.qml`, prefix `quickshell-bartip-`, match kind `pattern`, layer `3`,
`noreserve`, `transient`. `BarTooltipHost.qml` declares no namespace and gets no registry row.

Convert the four BAR-WINDOW sites to the host: `IdleInhibitorCapsule.qml:97`,
`ClockActionsCapsule.qml:367`, `MediaConnectivityCapsule.qml:552`, `LauncherCapsule.qml:246`. At
each, delete the three attached `ToolTip.visible` / `ToolTip.text` / `ToolTip.delay` lines and
declare one `BarTooltipHost` whose `anchorItem` is the cell the `MouseArea` fills, whose `text` is
the same string the deleted `ToolTip.text` carried (including
`ClockActionsCapsule.qml`'s existing empty-label suppression, which becomes an `active` term rather
than a separate condition), whose `active` is the same `containsMouse` expression, and whose
`tipId` is a short stable per-site string.

The two POPOUT-WINDOW sites — `AudioPopout.qml:104` and `SectionPopout.qml:408` — are decided by
Probe B's second reading, not by symmetry. Their tooltips live inside a popout window several
hundred pixels tall, which may already have room above the item. If Probe B showed that site's
tooltip clamped over its own glyph, fix it in place with a declared `ToolTip` carrying an explicit
placement below the item, inside its own window — the same orientation convention, a different
mechanism, because a bar-anchored layer surface would compute its margins from the wrong window's
coordinate space. State that reasoning in a comment at both sites so the difference does not read
later as an oversight. If Probe B showed those tooltips render clear of their glyph, change neither
site, and record in the SUMMARY that F2 was four sites and not six, with the measurement that says
so.

**Prove the fix, without a pointer.** Re-add one temporary probe tagged `Q69PROBE`: force one
`BarTooltipHost`'s `active` term true unconditionally so the surface mounts and stays mounted.
Save, `touch shell.qml`, then read `hyprctl layers -j` and record the `quickshell-bartip-…`
surface's x, y, w and h. The two numbers that prove F2: the surface's y must be at or below 48+4
(the bar's own 48px reservation plus the `Design.spacingXs` gap), i.e. entirely below the bar
rather than over it, and the surface's horizontal centre must be within 1px of the glyph's scene
centre as logged by the same probe. Then, with the same reload, re-read Probe D's F4 numbers by
force-opening the media popout once more and logging `transportRow.x` against
`(bodyColumn.width - transportRow.width) / 2`. Also grep the live log for
`Cannot specify` — the Column-anchor warning — and confirm it is absent.

Remove every probe, `touch` shell.qml a final time, confirm the probe-tag grep returns nothing,
confirm the pid is still 528309, run `hypr/.config/hypr/scripts/quickshell-doctor` and confirm it
reports no failure that was not already failing before this task's edits (capture that baseline
before editing if it has not been captured), then commit.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && ! grep -rn "Q69PROBE" quickshell/ hypr/ && grep -c "BarTooltip 1.0 BarTooltip.qml" quickshell/.config/quickshell/modules/bar/qmldir && grep -c "quickshell-bartip-" hypr/.config/hypr/scripts/quickshell-doctor && grep -v '^ *//' quickshell/.config/quickshell/modules/bar/MediaPopout.qml | grep -c "anchors.horizontalCenter: parent.horizontalCenter" && test "$(pgrep -x quickshell)" = "528309" && hyprctl monitors -j | jq -e '.[0].reserved == [0,48,0,0]' >/dev/null && ! grep -n "Cannot specify" "$(ls -dt /run/user/1000/quickshell/by-id/*/log.log | head -1)" && bash hypr/.config/hypr/scripts/quickshell-doctor >/tmp/qsd-after.txt 2>&1; echo "doctor exit $?"; echo OK</automated>
    <human-check>None in this task. Placement is proven by the recorded `hyprctl layers -j` rectangle, and centring by the recorded `transportRow.x`.</human-check>
  </verify>
  <done>
The four bar tooltip sites no longer set an attached `ToolTip` and instead mount a
`BarTooltipHost`; the two popout sites are either converted or explicitly left alone with the
measurement that justifies it. `transportRow` carries `anchors.horizontalCenter` and its measured x
equals `(bodyColumn.width - transportRow.width) / 2`. A forced-mount reading of `hyprctl layers -j`
recorded the tooltip surface's rectangle sitting entirely below the bar and centred on its glyph to
within 1px. `qmldir` and the doctor registry each gained exactly one row, in this commit.
quickshell-doctor reports no new failure. No probe survives, the pid is 528309, the reserved array
is `[0,48,0,0]`, and the work is committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: F1 — fix the alignment the numbers show — and F3 — make the bulb's state read at bar scale</name>
  <files>quickshell/.config/quickshell/modules/bar/BarCapsule.qml, quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml, quickshell/.config/quickshell/modules/bar/IdleInhibitorCapsule.qml</files>
  <precondition>Task 1's Probe A and Probe C readings are in hand, including the per-capsule baseline scene-y values for all six capsules. Without the baseline the "did a capsule that looked right move" half of F1 cannot be answered and this task must not proceed.</precondition>
  <read_first>
    quickshell/.config/quickshell/modules/bar/BarCapsule.qml lines 106-147 (contentColour,
    contentGrid, the implicit sizing and the `visible` contract);
    quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml around lines 620-640 (the active
    accent pill — the existing "on" language F3 must match);
    quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml around line 669 (gamingCell's
    `active ? BarRoles.accent : contentColour` tint idiom).
  </read_first>
  <action>
**F1 — fix what Probe A's numbers showed, and nothing else.**

If the numbers matched Candidate 1 (children sharing a top edge and differing at the bottom, i.e.
the `Grid` `AlignTop` default), set `verticalItemAlignment: Grid.AlignVCenter` on `contentGrid` in
`BarCapsule.qml` for the horizontal case and `horizontalItemAlignment: Grid.AlignHCenter` for the
vertical case, expressed through the same `vertical` ternary the surrounding `rows`/`columns`
bindings already use. Set it once on the shared chrome rather than patching one capsule.

If the numbers matched Candidate 2 (a negative `contentHost.childrenRect.y` from `clockFillPill`
overhanging `clockTriggerGrid`'s top edge), the fix belongs in `ClockActionsCapsule.qml` alone and
`BarCapsule.qml` must not be touched at all: give the clock cell an explicit height and vertical
placement that accounts for the pill's own overhang, in the same derived-from-tokens style the
surrounding block already uses, rather than letting `childrenRect` absorb a negative origin.

If the numbers matched neither, do not guess. Record what they showed, add whatever second probe
discriminates the remaining possibilities, and measure again before editing.

`BarCapsule.qml` has six consumers, and an unmeasured change to a shared file is exactly where this
kind of fix does collateral damage. So whichever branch is taken, re-add Probe A verbatim after the
edit, reload, and diff the after-values against Task 1's baseline for every one of the six
capsules. The acceptance bar is two-sided: the clock cell's content centre must now agree with its
siblings' to within 1px, AND every capsule whose children were already co-aligned must show a
delta of exactly 0 against its baseline. If any already-correct capsule moved, the fix is wrong
even if the clock now looks right — revert it and take the other branch.

**F3 — give the bulb an "on" signal that reads at bar scale.**

Record Probe C's verdict first, because it determines what the operator was actually looking at:
either the FILL axis is inert and the only feedback was a small glyph changing colour, or the axis
works and that feedback was simply too small to read. Note in the code comment that
`fillAxisAvailable` at line 44 is a hardcoded literal claim about the font build and record what
the render diff measured it to actually be — but do NOT change that property's value as part of
this fix; it is read by the glyph's `font.variableAxes` expression and flipping it is a separate,
unrequested change.

The fix: `IdleInhibitorCapsule` is already `surfaced: true`, so it draws a real pill. When
`idleInhibited`, fill that pill with `BarRoles.accent` and render the glyph in `BarRoles.onAccent`;
at rest keep the existing `BarRoles.capsule` / `capsuleHover` fill and `contentColour` glyph. That
is the identical language the active workspace pill already speaks (an accent pill with on-accent
content) and that the gaming toggle's `active ? BarRoles.accent : contentColour` tint already
speaks — no new visual vocabulary is introduced, which the operator's instruction explicitly
forbids. Implement the fill by overriding the `color` binding in `IdleInhibitorCapsule.qml` rather
than by adding a property to `BarCapsule.qml`: a derived-type binding replaces the base binding
cleanly, the base's `Behavior on color` still animates it under the motion gate, and the other five
consumers of the shared chrome are untouched — which is worth more here than vocabulary symmetry
with `ActionCell`'s `fillActive`/`fillColour` pair. Keep the hover branch alive in the rest state so
the capsule does not lose its hover response. `BarRoles.accent` is fully opaque, far above the
family layer rule's ignore_alpha floor of 0.5, so the blur behind the bar is unaffected.

Do NOT change the glyph's ligature name to signal state. `notifications` / `notifications_active`
is the repo's precedent for glyph-name state signalling, but GATE-02 row A.3 names a nonexistent
Material Symbols ligature rendering as its own name in plain text as a specific failure mode, and
there is no ligature-presence check available here to verify a candidate name against the installed
font. The accent pill delivers the signal without that risk.

**Teardown and commit.** Remove every probe added by this task, `touch shell.qml`, confirm the
probe-tag grep returns nothing, confirm the pid is still 528309 and the reserved array still reads
`[0,48,0,0]`, run quickshell-doctor and confirm no new failure, then commit.

**The SUMMARY's required statement.** Because no executor on this host can move the pointer, the
SUMMARY must carry a short section that sorts all four defects into exactly two lists and hedges
neither: **proven by measurement** (each with the number that proves it — the tooltip surface
rectangle, the transport row's x, the before/after capsule geometry, the FILL-axis render diff) and
**claimed by construction, still needs the operator's eyes on the GATE-02 re-run** (anything whose
final acceptance is an aesthetic judgment — notably whether the accent-filled bulb reads as "on" to
the operator, and whether the tooltip's below-the-bar placement is legible in practice rather than
merely geometrically correct). If any restart of quickshell happened at any point, say so at the
top of the SUMMARY so the orchestrator re-anchors the QBAR-11 soak window.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && ! grep -rn "Q69PROBE" quickshell/ hypr/ && test "$(pgrep -x quickshell)" = "528309" && hyprctl monitors -j | jq -e '.[0].reserved == [0,48,0,0]' >/dev/null && grep -v '^ *//' quickshell/.config/quickshell/modules/bar/IdleInhibitorCapsule.qml | grep -c "BarRoles.accent" && grep -n "Configuration Loaded" "$(ls -dt /run/user/1000/quickshell/by-id/*/log.log | head -1)" | tail -1 && ! grep -niE "error|warning: .*qml" "$(ls -dt /run/user/1000/quickshell/by-id/*/log.log | head -1)" | tail -5 | grep -q . ; bash hypr/.config/hypr/scripts/quickshell-doctor >/tmp/qsd-final.txt 2>&1; echo "doctor exit $?"; echo OK</automated>
    <human-check>Deferred to plan 18-19's GATE-02 re-run by design: whether the accent-filled bulb reads as "on" and whether the tooltip is legible below the bar are aesthetic judgments this host cannot automate. The SUMMARY must name both explicitly as unproven.</human-check>
  </verify>
  <done>
F1's fix landed in whichever single file Probe A's numbers pointed at, and a post-fix re-measure
shows the clock cell's content centre agreeing with its siblings' to within 1px while every
already-correct capsule shows a delta of exactly 0 against its Task 1 baseline. F3's bulb fills
`BarRoles.accent` with `BarRoles.onAccent` content when inhibited and is unchanged at rest, with no
ligature-name change and no edit to `fillAxisAvailable`. No probe survives, quickshell is still pid
528309, the reserved array is `[0,48,0,0]`, quickshell-doctor reports no new failure, the work is
committed, and the SUMMARY sorts all four defects into proven-by-measurement and
claimed-by-construction with no defect left unsorted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wayland layer-shell surface → compositor | A new `quickshell-bartip-*` layer surface claims screen real estate and inherits the `^quickshell-.*` family blur and ignore_alpha rules |
| QML source → live shell process | Hot reload executes edited source in the running shell with the user's own privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69W-01 | Denial of Service | `BarTooltip` layer surface | medium | mitigate | `exclusiveZone: 0` as a bare literal and `exclusionMode: ExclusionMode.Ignore`, verified by the reserved-array assertion `[0,48,0,0]` in every task's automated gate — a tooltip surface must never claim screen space away from windows |
| T-69W-02 | Tampering | quickshell-doctor bar-surface registry | medium | mitigate | The new namespace resolves to exactly one registry row via the `quickshell-bartip-` prefix; the doctor run in Tasks 2 and 3 fails if it does not |
| T-69W-03 | Information Disclosure | Probe grabToImage output | low | accept | Probe PNGs are two glyph renders written to the session scratch directory and are not committed; the probe-tag grep gate ensures no probe code ships |
| T-69W-04 | Denial of Service | Live shell process | high | mitigate | No restart, no `hyprctl reload`, no `hyprctl eval`; every task's automated gate asserts pid 528309 is unchanged, protecting the QBAR-11 soak window |
| T-69W-05 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — no dependency is added, removed or upgraded |
</threat_model>

<verification>
- `pgrep -x quickshell` returns `528309` at the end of every task.
- `hyprctl monitors -j | jq '.[0].reserved'` returns `[0,48,0,0]` at the end of every task.
- `grep -rn "Q69PROBE" quickshell/ hypr/` returns nothing at every commit point.
- `hypr/.config/hypr/scripts/quickshell-doctor` reports no failure that was not already failing
  before this plan's first edit (baseline captured in Task 2 before editing).
- The live log's newest `Configuration Loaded` line is later than the last source edit, and the log
  carries no `Cannot specify` positioner-anchor warning.
- `git status --porcelain -- quickshell/ hypr/` is empty after the final commit.
</verification>

<success_criteria>
- F2: a measured `quickshell-bartip-*` layer rectangle sits entirely below the bar and is centred on
  its glyph to within 1px — or, in Branch A, a measured unclamped tooltip y does the same.
- F1: post-fix capsule geometry shows the clock cell centred with its siblings to within 1px and
  zero movement in every capsule that was already correct.
- F3: the idle bulb's inhibited state fills the capsule `BarRoles.accent` with `BarRoles.onAccent`
  content, matching the active workspace pill's existing language.
- F4: `transportRow.x` measures `(bodyColumn.width - transportRow.width) / 2`.
- Every fix is sorted in the SUMMARY into proven-by-measurement or claimed-by-construction, with
  the number or the reason attached.
- `18-GATE-02-RECORD.md` is untouched; the four rows stay `Open` until plan 18-19 re-observes them.
</success_criteria>

<output>
Create `.planning/quick/260812-69w-fix-four-gate-02-iteration-1-bar-defects/260812-69w-SUMMARY.md`
when done. It must contain, at minimum: the four measurement sets from Task 1 as literal numbers;
which F1 candidate cause the numbers matched; whether F2 was four sites or six and the measurement
that decided it; the FILL-axis render-diff verdict; and the two-list sort of all four defects into
proven-by-measurement and claimed-by-construction-still-needs-the-operator. If quickshell restarted
at any point, that goes at the very top.
</output>
