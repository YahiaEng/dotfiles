---
phase: quick-260726-l0y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [QS-03]

must_haves:
  truths:
    - "11-VERIFICATION.md frontmatter carries exactly one `overrides` entry for QS-03, attributed to YahiaEng at 2026-07-26T12:07:57Z, citing D-10"
    - "`overrides_applied` reads 1, and the verifier's three `gaps` entries and two `behavior_unverified_items` entries are still present and byte-identical"
    - "QS-03 has exactly one owner in the REQUIREMENTS.md traceability table, and that owner is Phase 12"
    - "Phase 11's ROADMAP Requirements line marks QS-03 deferred with a pointer to Phase 12 rather than dropping it"
    - "Phase 12 carries a testable success criterion for QS-03, so the requirement can actually close there"
    - "The requirement-coverage arithmetic in REQUIREMENTS.md still totals 38 with zero duplicates"
    - "No source file, script, or QML is modified"
  artifacts:
    - ".planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md (override + dated addendum)"
    - ".planning/ROADMAP.md (Phase 11 deferral note, Phase 12 ownership + criterion 6, placement rationale)"
    - ".planning/REQUIREMENTS.md (checklist note, traceability row, counts, placement note)"
  key_links:
    - "11-VERIFICATION.md `overrides[0].reason` -> ROADMAP Phase 12 Requirements line -> REQUIREMENTS.md QS-03 traceability row (one consistent ownership story across all three files)"
    - "ROADMAP Phase 12 criterion 6 -> `quickshell-doctor`'s existing per-screen surface-creation check (the mechanical gate that closes QS-03)"
---

<objective>
Record the user's two locked decisions about QS-03 as durable planning artifacts: (1) formally accept the QS-03 gap for Phase 11 via an `overrides` entry in `11-VERIFICATION.md`, and (2) hand QS-03 ownership to Phase 12 so the requirement has a named owner instead of being silently dropped.

Purpose: The Phase 11 verifier's specific complaint was not that QS-03 failed — the phase disclosed that honestly — but that the failure had *no recorded override* and *no later phase claiming ownership*. This plan closes both halves of that complaint.

Output: Three edited planning artifacts. Zero code changes.

**Tracer-first is not applicable here.** There is no architecture to prove end-to-end — this is pure planning-artifact bookkeeping across three markdown files. Tasks are ordered by dependency (the override must exist before other artifacts point at it), and Task 3 carries the cross-file consistency gate that acts as this plan's end-to-end proof.
</objective>

<decisions>
Three judgment calls the constraints asked the planner to make and state:

**D-A — Phase 12 DOES get a new success criterion for QS-03. Decided: yes, add criterion 6.**
Without it, Phase 12 inherits an orphaned requirement with nothing testable attached, and Phase 12's verifier will raise the exact same complaint the Phase 11 verifier just raised ("requirement present, no criterion, no proof path"). Moving the ID without moving a testable definition of done would be bookkeeping theater. The criterion is written against the gate that already exists — `quickshell-doctor`'s per-screen surface-creation check, currently the sole FAIL in a 13-passed/1-failed run — so closure is mechanically provable, and it names the 11-04 revert hazard so the next implementer does not re-walk it blind.

**D-B — Phase 12 is the right owner, and the rationale is recorded, not assumed.**
Phase 12 is the first phase that needs a *permanent, non-summoned* QML surface: its criterion 1 re-colours a live Quickshell surface on theme switch. A permanent shell root is exactly the thing that has to fan out over `Quickshell.screens`. So QS-03 is not being parked in an unrelated phase for convenience — it is landing in the first phase that structurally has to solve it anyway. Phases 14 and 16 both build on that root, so it closes before them either way.

**D-C — `status:` and `score:` in 11-VERIFICATION.md are deliberately NOT changed.**
Two of the three gaps (CR-01, and CR-02/CR-03) are genuinely fixed and committed, and the third is now overridden — so the file arguably "should" read `passed`. It stays `gaps_found` at `6/9` anyway. That report is a dated point-in-time artifact signed by the verifier at 2026-07-26T11:49:32Z; flipping its verdict without re-running verification would fabricate a re-verification that never happened. The correct way to flip it is `/gsd-verify-phase 11`, which is out of scope for this task. The dated addendum in Task 1 records the delta instead, and the verify gate in Task 1 asserts these two fields are untouched.
</decisions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md
@.planning/phases/11-quickshell-viability-gate/11-REVIEW-FIX.md
</context>

<scope_fence>
**Touch ONLY these three files:**
- `.planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`

**Do NOT:**
- Modify any source file, script, `.qml`, `.conf`, or `.sh` — in particular do not touch `quickshell/.config/quickshell/shell.qml` or `hypr/.config/hypr/scripts/quickshell-doctor`
- Attempt to actually fix QS-03. This task records an acceptance and moves an owner. It does not write a `Variants` fan-out.
- Rewrite, reword, reorder, or delete any of the verifier's existing findings prose, `gaps` entries, `behavior_unverified_items` entries, tables, or the `_Verified:_` / `_Verifier:_` footer in `11-VERIFICATION.md`. Additive edits only, plus the single `overrides_applied` scalar bump.
- Edit `.planning/STATE.md` — the surrounding quick-task workflow owns that.

Use `Edit` with scoped replacements on all three files. Never `Write` a whole planning file.
</scope_fence>

<reference_content>

### Block A — the `overrides:` YAML to insert into 11-VERIFICATION.md frontmatter (verbatim)

```yaml
overrides:
  - must_have: "Quickshell surfaces render correctly across all connected monitors and survive monitor hotplug (QS-03 / ROADMAP criterion 2)"
    reason: "D-10: only QS-02 carries STOP authority this phase, so QS-03's per-screen-mounting gap is a disclosed, non-blocking finding rather than a phase-goal blocker. A Variants-based fix was written in 11-04 and reverted after it introduced two independent live-daemon reliability regressions on quickshell 0.3.0 — an intermittent config-load race and a post-hotplug visibility break — and shipping that against the always-on autostart daemon was judged worse than the gap it closed. This host has exactly one physical monitor, so the defect has no present user-visible impact. The requirement is not dropped: ownership moves to Phase 12 (Unified Design-Token Pipeline), the first phase that needs a permanent non-summoned QML surface and therefore has to solve per-screen fan-out anyway — see the Phase 12 Requirements line and success criterion 6 in ROADMAP.md, and the QS-03 row in REQUIREMENTS.md traceability."
    accepted_by: "YahiaEng"
    accepted_at: "2026-07-26T12:07:57Z"
```

Quoting matters: `accepted_at` MUST stay double-quoted or YAML will parse it into a datetime object instead of the string the gate asserts. The `reason` string contains no double-quote characters — keep it that way.

### Block B — the dated addendum to append at the very end of 11-VERIFICATION.md (verbatim)

```markdown

## Addendum — 2026-07-26 (post-verification)

This section was appended after the verification pass above. Everything above it is
unmodified and remains accurate as of its own `2026-07-26T11:49:32Z` timestamp.

**Gap 1 (QS-03, per-screen mounting) — ACCEPTED, not fixed.** Recorded as a formal override
in this file's frontmatter (`overrides[0]`), accepted by YahiaEng at 2026-07-26T12:07:57Z
under D-10. Ownership of QS-03 moves from Phase 11 to **Phase 12 — Unified Design-Token
Pipeline**, which now carries it in its ROADMAP `Requirements` line and in a dedicated,
mechanically-testable success criterion 6. REQUIREMENTS.md's traceability row for QS-03 was
repointed to Phase 12 in the same change. This directly answers the verifier's objection
that the gap "is not deferred to any later phase in the traceability table, so it currently
has no owner" — it has one now.

**Gaps 2 and 3 (CR-01 / CR-02 / CR-03) — FIXED and committed.** All three were resolved
after this report was written; see `11-REVIEW-FIX.md` (`status: all_fixed`, 5/5 in-scope
findings, `2026-07-26T12:02:52Z`):

| Finding | Fix | Commit |
|---|---|---|
| CR-01 | `Probe.qml` hardcoded home path replaced with `Quickshell.env("HOME")` | `74ec933` |
| CR-02 | Headless-remove step's probe summon armed via `PROBE_SUMMONED_FOR_HEADLESS_TEST` + trap cleanup | `1945a81` |
| CR-03 | Reserved-space manifest summon loop armed via new `RESERVED_CHECK_SUMMONED` + `_qsd_cleanup` branch | `3ec4661` |
| WR-01 | Volume-probe regex extraction validated before arithmetic | `2529894` |
| WR-02 | MPRIS-writer check narrowed from bare substring to actual API surface | `9978851` |

**Frontmatter `status` and `score` are deliberately unchanged** at `gaps_found` / `6/9`.
This report is a signed point-in-time artifact; flipping its verdict without re-running
verification would assert a re-verification that never happened. Re-run
`/gsd-verify-phase 11` to produce an authoritative updated verdict.
```

### Block C — Phase 12 success criterion 6 for ROADMAP.md (verbatim)

```markdown
  6. *(Carried forward from Phase 11 as QS-03, accepted there via a recorded override — see 11-VERIFICATION.md frontmatter `overrides`.)* The permanent Quickshell shell root mounts a surface on **every** entry in `Quickshell.screens`, including an output hotplugged after shell startup, and `quickshell-doctor`'s per-screen surface-creation check reports PASS with the script exiting 0 — it currently reports 13 passed / 1 failed, exit 1, on exactly this one check, so this criterion is proven by that number going green, not by inspection. The 11-04 `Variants`-based attempt was reverted for two independent live-daemon reliability regressions on quickshell 0.3.0 (an intermittent config-load race and a post-hotplug visibility break), so the replacement must be re-proven against the always-on autostart daemon across a real session restart, not only in a foreground test run.
```
</reference_content>

<tasks>

<task type="auto">
  <name>Task 1: Record the QS-03 acceptance override and the post-verification addendum in 11-VERIFICATION.md</name>
  <files>.planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md</files>
  <precondition>The file's frontmatter still reads `overrides_applied: 0` and has no `overrides:` key — if an override is already recorded, halt and report rather than adding a second one.</precondition>
  <action>
Three additive edits, no rewrites.

(1) Bump the scalar on line 7 from zero to one: `overrides_applied: 0` becomes `overrides_applied: 1`. This is the only existing character in the file that changes.

(2) Append the `overrides:` mapping from reference Block A as the **last top-level key in the frontmatter**, inserted immediately after the final line of the `behavior_unverified_items:` list (the `why_human:` line ending in `Findings and Caveats` plus its closing quote) and immediately before the closing `---` fence. Appending at the tail rather than splicing next to `overrides_applied` is deliberate: it guarantees zero disturbance to the two existing multi-line block lists (`gaps:` with 3 entries, `behavior_unverified_items:` with 2 entries), which the verify gate re-counts. Copy Block A byte-for-byte including its two-space list indentation and four-space key indentation.

(3) Append the addendum from reference Block B to the very end of the file, **after** the existing `_Verified:_` / `_Verifier:_` footer lines. Placing it after the signature keeps the verifier's report body and its signature intact as one visually unbroken unit, which is what the constraint to preserve the dated artifact is protecting.

Leave `status: gaps_found`, `score: 6/9 must-haves verified`, `behavior_unverified: 2`, every `gaps:` entry, every `behavior_unverified_items:` entry, all report body prose, all tables, and the Gaps Summary section exactly as they are — including the verifier's own suggested override snippet at the tail of Gaps Summary, which stays as the record of what was suggested.
  </action>
  <verify>
    <automated>python3 -c "
import re,yaml
t=open('.planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md').read()
d=yaml.safe_load(re.match(r'^---\n(.*?)\n---\n', t, re.S).group(1))
assert d['overrides_applied'] == 1, d['overrides_applied']
assert len(d['overrides']) == 1, len(d['overrides'])
o = d['overrides'][0]
assert o['accepted_by'] == 'YahiaEng', o['accepted_by']
assert o['accepted_at'] == '2026-07-26T12:07:57Z', repr(o['accepted_at'])
assert 'QS-03' in o['must_have']
assert 'D-10' in o['reason'] and 'Phase 12' in o['reason'] and 'Variants' in o['reason']
assert len(d['gaps']) == 3 and len(d['behavior_unverified_items']) == 2
assert d['status'] == 'gaps_found' and d['score'] == '6/9 must-haves verified' and d['behavior_unverified'] == 2
assert 'Addendum' in t and '74ec933' in t and '1945a81' in t and '3ec4661' in t
assert t.index('## Addendum') > t.index('_Verifier: Claude (gsd-verifier)_')
print('OK: override recorded, verifier blocks and verdict intact, addendum after footer')
"</automated>
  </verify>
  <done>Frontmatter parses as valid YAML with `overrides_applied: 1` and one `overrides` entry attributed to YahiaEng at the exact timestamp, citing D-10 and naming Phase 12; the 3 gaps and 2 behavior-unverified entries are still there; `status`/`score` unchanged; the addendum sits after the verifier's signature and names all three fix commits.</done>
</task>

<task type="auto">
  <name>Task 2: Move QS-03 ownership from Phase 11 to Phase 12 in ROADMAP.md, with a testable criterion</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Five scoped edits. Line numbers below are pre-edit positions from the current file — anchor on the quoted text, not the number.

(1) **Phase 11 Requirements line (~line 79).** Mark QS-03 deferred rather than dropping it. `**Requirements**: QS-01, QS-02, QS-03, QS-04, QS-05, QS-06, MAINT-01` becomes `**Requirements**: QS-01, QS-02, QS-03 *(deferred — accepted for Phase 11 via a recorded override, ownership reassigned to Phase 12; see 11-VERIFICATION.md)*, QS-04, QS-05, QS-06, MAINT-01`.

(2) **Phase 11 success criterion 2 (~line 83).** The criterion currently bundles multi-monitor render, hotplug survival, suspend/resume, hot reload, and FileView propagation into one sentence, and reads as fully met — it is not. Append an italic amendment clause to the end of that criterion, following the same annotation convention criteria 4 and 5 already use in this phase. The clause must state: the multi-monitor render sub-clause was not met because a single-`LazyLoader` shell root does not fan out over `Quickshell.screens`; it was accepted for Phase 11 via a recorded override under D-10 (pointing at 11-VERIFICATION.md frontmatter `overrides`) and reassigned to Phase 12 as QS-03; and hotplug survival, suspend/resume, hot reload and `FileView`/`JsonAdapter` propagation all did pass. Do not delete or reword the existing criterion text — append only.

(3) **Phase 12 Requirements line (~line 115).** Append the carried-forward ID: the line ends `..., TOKEN-05, TOKEN-06` and becomes `..., TOKEN-05, TOKEN-06, QS-03 *(carried forward from Phase 11 — see 11-VERIFICATION.md)*`.

(4) **Phase 12 success criterion 6 (new).** Insert reference Block C verbatim as a new numbered item immediately after Phase 12's existing criterion 5 (the italic `*(Stretch — blocks nothing.)*` spring-physics item) and before the blank line preceding `**Open questions owned**`. Match the existing two-space indent that criteria 1-5 use. Per planner decision D-A, this criterion is what makes QS-03 actually closable in Phase 12 instead of an orphaned ID.

(5) **Phase 12 Owns line (~line 125).** Append the carried-forward scope so Phase 12's own section is self-consistent: the line ending `...and the generator safeguards against malformed emitted curves.` gains `Also owns the carried-forward QS-03 per-screen surface fan-out.`

(6) **Carried-in placement section (~lines 65-72).** Append one bullet after the `**MAINT-03 → Phase 13.**` bullet, in the same shape as its siblings, headed `**QS-03 → Phase 12 (carried forward from Phase 11, 2026-07-26).**` It must record: what the defect is (shell root mounts one `LazyLoader` per `GlobalShortcut`, not one surface per `Quickshell.screens` entry, so an output hotplugged after startup gets nothing); that the `Variants` fix was written in 11-04 and reverted for two live-daemon reliability regressions; that Phase 11 accepted the gap under D-10 with a recorded override; and the placement rationale from planner decision D-B — Phase 12 is the first phase needing a permanent, non-summoned QML surface (its criterion 1 re-colours a live surface on theme switch), so it has to solve per-screen fan-out anyway, and Phases 14 and 16 both build on that root so it closes before them.
  </action>
  <verify>
    <automated>python3 -c "
t = open('.planning/ROADMAP.md').read()
p11 = t.split('### Phase 11:')[1].split('### Phase 12:')[0]
p12 = t.split('### Phase 12:')[1].split('### Phase 13:')[0]
place = t.split('#### Carried-in maintenance placement')[1].split('## Phase Details')[0]
r11 = [l for l in p11.splitlines() if l.startswith('**Requirements**')][0]
r12 = [l for l in p12.splitlines() if l.startswith('**Requirements**')][0]
assert 'QS-03' in r11 and 'deferred' in r11.lower() and 'Phase 12' in r11, r11
assert 'QS-03' in r12 and 'TOKEN-06' in r12, r12
sc11 = p11.split('**Success Criteria**')[1].split('**Open questions owned**')[0]
c2 = [l for l in sc11.splitlines() if l.strip().startswith('2.')][0]
assert 'override' in c2 and 'Phase 12' in c2 and 'Quickshell.screens' in c2, c2
sc12 = p12.split('**Success Criteria**')[1].split('**Open questions owned**')[0]
c6 = [l for l in sc12.splitlines() if l.strip().startswith('6.')]
assert len(c6) == 1, c6
assert 'QS-03' in c6[0] and 'quickshell-doctor' in c6[0] and 'Quickshell.screens' in c6[0] and 'exit 0' in c6[0], c6[0]
owns12 = [l for l in p12.splitlines() if l.startswith('**Owns**')][0]
assert 'QS-03' in owns12, owns12
assert 'QS-03' in place and 'Phase 12' in place and 'D-10' in place, place[-400:]
print('OK: Phase 11 defers QS-03, Phase 12 owns it with a testable criterion 6, placement rationale recorded')
"</automated>
  </verify>
  <done>Phase 11's Requirements line and criterion 2 both disclose the QS-03 deferral and point at Phase 12; Phase 12's Requirements line, Owns line, and a new criterion 6 all claim it; criterion 6 is provable via `quickshell-doctor` exiting 0; the placement section explains why Phase 12 is the owner.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the QS-03 traceability row to Phase 12 in REQUIREMENTS.md and run the cross-file consistency gate</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Five scoped edits, then the plan's end-to-end gate.

(1) **QS-03 checklist entry (~line 18).** Keep it unchecked — the requirement is not satisfied, only reassigned. Append a note so the checklist itself shows the new owner: the line becomes `- [ ] **QS-03**: Quickshell surfaces render correctly across all connected monitors and survive monitor hotplug *(carried forward to Phase 12 — accepted for Phase 11 via a recorded override, see 11-VERIFICATION.md)*`.

(2) **Traceability row (~line 129).** Replace `| QS-03 | Phase 11 — Quickshell Viability Gate | Pending |` with `| QS-03 | Phase 12 — Unified Design-Token Pipeline *(carried forward from Phase 11; accepted there via a recorded override)* | Deferred |`. Exactly one row for QS-03 must exist afterward — the coverage block below asserts zero duplicates, and this task must keep that true.

(3) **Per-phase counts (~line 173).** QS-03 moving changes the split but not the total. `Phase 11: 7 · Phase 12: 6` becomes `Phase 11: 6 · Phase 12: 7`; leave phases 13-17 and the `= 38` total untouched.

(4) **Placement notes (~lines 175-180).** Append a bullet mirroring the ROADMAP one from Task 2, headed `**QS-03 → Phase 12 (carried forward from Phase 11, 2026-07-26).**` Keep it to the essentials this file cares about: the gap was accepted for Phase 11 under D-10 via a recorded override in 11-VERIFICATION.md rather than dropped; Phase 12 is the first phase needing a permanent non-summoned QML surface so it has to solve per-screen fan-out anyway; Phase 12 carries it as success criterion 6; and the total stays 38 with no duplicate because the row moved rather than being copied.

(5) **Last-updated line (~line 185).** Append the change to the existing trailer so the file's own history line stays honest: `; QS-03 ownership moved Phase 11 -> Phase 12 after the Phase 11 acceptance override (2026-07-26)`.

Then run the cross-file gate in `<verify>`. It is the end-to-end proof for this plan: it asserts the same ownership story holds in all three artifacts simultaneously, and that nothing outside `.planning/` was touched.
  </action>
  <verify>
    <automated>python3 -c "
req = open('.planning/REQUIREMENTS.md').read()
rows = [l for l in req.splitlines() if l.startswith('| QS-03 ')]
assert len(rows) == 1, rows
assert 'Phase 12' in rows[0] and 'Phase 11' in rows[0] and 'Deferred' in rows[0], rows[0]
chk = [l for l in req.splitlines() if '**QS-03**' in l and l.startswith('- [')][0]
assert chk.startswith('- [ ]') and 'Phase 12' in chk, chk
assert 'Phase 11: 6 · Phase 12: 7' in req
assert '= 38' in req and 'Unmapped: 0' in req and 'Duplicates (a requirement in more than one phase): 0' in req
notes = req.split('**Placement notes:**')[1]
assert 'QS-03' in notes and 'Phase 12' in notes and 'D-10' in notes
p11rows = [l for l in req.splitlines() if l.startswith('| QS-0') or l.startswith('| MAINT-01 ')]
assert sum(1 for l in p11rows if 'Phase 11' in l.split('|')[2]) == 6, [l for l in p11rows]
rm = open('.planning/ROADMAP.md').read()
ver = open('.planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md').read()
assert 'QS-03' in rm.split('### Phase 12:')[1].split('### Phase 13:')[0]
assert 'YahiaEng' in ver and '2026-07-26T12:07:57Z' in ver
print('OK: QS-03 owned solely by Phase 12 across all three artifacts; coverage still 38/38, 0 duplicates')
" && test "$(git status --porcelain -- ':!.planning' | wc -l)" -eq 0 && echo 'OK: no file outside .planning was modified'</automated>
  </verify>
  <done>REQUIREMENTS.md shows QS-03 unchecked, owned by Phase 12, Deferred; Phase 11 retains exactly 6 traceability rows and Phase 12 has 7; total is still 38 with 0 unmapped and 0 duplicates; the cross-file gate confirms ROADMAP and 11-VERIFICATION.md tell the same story; git confirms nothing outside `.planning/` changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier's signed report → post-hoc editor | This task edits an artifact that is evidence. An additive-only discipline is the boundary control: everything the verifier wrote stays byte-identical, and the one scalar that changes (`overrides_applied`) is a counter the override schema owns. |
| planning artifacts → future phase executors | Phase 12's executor will act on the Requirements line and criterion 6 written here. A vague or untestable criterion silently becomes an unclosable requirement three phases later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11Q-01 | Tampering | `11-VERIFICATION.md` verifier findings | high | mitigate | Additive-only edit rule in `<scope_fence>`; Task 1's gate re-counts `gaps` (3) and `behavior_unverified_items` (2) and asserts `status`/`score`/`behavior_unverified` are unchanged, so any accidental rewrite fails the task. |
| T-11Q-02 | Repudiation | The override entry | high | mitigate | `accepted_by` and `accepted_at` are recorded verbatim from the user's explicit decision and asserted exactly by the Task 1 gate; the `reason` cites D-10 so the authority for the acceptance is traceable, not implied. |
| T-11Q-03 | Information disclosure | QS-03 defect visibility | medium | mitigate | The failure is disclosed in four places rather than buried: the override, the addendum, Phase 11's criterion-2 amendment, and the `Deferred` traceability status. Silent removal of the ID is explicitly forbidden by the scope fence. |
| T-11Q-04 | Denial of service | Requirement coverage integrity | medium | mitigate | Task 3's gate asserts exactly one QS-03 row, the 6/7 per-phase split, and the unchanged `= 38` / `Unmapped: 0` / `Duplicates: 0` invariants — a copied-instead-of-moved row fails the gate. |
| T-11Q-05 | Elevation of privilege | Verification verdict | medium | accept | A bookkeeping task could flip `status: gaps_found` to `passed` and manufacture an unearned pass. Accepted by refusing to change it (planner decision D-C) and asserting immutability in Task 1's gate; only `/gsd-verify-phase 11` may change the verdict. |
| T-11Q-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this task and no dependency is added — the supply-chain surface is empty. No legitimacy audit required. |
</threat_model>

<verification>
1. `11-VERIFICATION.md` frontmatter parses as YAML with exactly one `overrides` entry (YahiaEng / 2026-07-26T12:07:57Z / cites D-10 / names Phase 12) and `overrides_applied: 1`.
2. The verifier's 3 `gaps`, 2 `behavior_unverified_items`, `status: gaps_found`, and `score: 6/9` are all still present and unmodified.
3. The dated addendum sits after the `_Verifier:_` footer and cites commits 74ec933, 1945a81, 3ec4661.
4. ROADMAP Phase 11 defers QS-03 with a pointer; ROADMAP Phase 12 owns it in its Requirements line, Owns line, and a new testable criterion 6.
5. REQUIREMENTS.md has exactly one QS-03 row, pointing at Phase 12 with status `Deferred`; coverage arithmetic still reads 38 total / 0 unmapped / 0 duplicates with a 6/7 Phase 11/12 split.
6. `git status --porcelain -- ':!.planning'` is empty — no source file, script, or QML touched.
</verification>

<success_criteria>
QS-03 is no longer an unowned failure. Phase 11's acceptance of it is formally recorded and attributable, Phase 12 owns it with a criterion that a `quickshell-doctor` exit code can close, and all three planning artifacts tell the identical story with the requirement-coverage math still balancing at 38.
</success_criteria>

<output>
Create `.planning/quick/260726-l0y-record-the-qs-03-acceptance-override-in-/260726-l0y-SUMMARY.md` when done.
</output>
