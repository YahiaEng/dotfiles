---
quick_id: 260827-ar3
date: 2026-08-27
status: planned
reversibility: one-way
supersedes: "Task 8 of quick task 260827-833"
---

# 260827-ar3 — Retire hyprlock

## Objective

Complete Task 8 of quick task 260827-833: remove hyprlock from the repo and the
host. The in-process Quickshell lock screen shipped and is operator-verified;
all three consumers were repointed in `ee25f4b9`, so hyprlock is dormant. This
lands config-then-package in ONE commit (the `eww.scss` / WINDOWS #1 precedent)
and finishes with `retirement-check hyprlock` reporting `failed_classes=0`.

## Measured ground truth (this session)

**PAM is independent — the removal cannot break authentication.**
`LockPam.qml:101` is the only `PamContext` in the tree and uses
`config: "passwd"`. `/etc/pam.d/passwd` is owned by **shadow 4.20.0.arch1-1**,
not hyprlock, and `/usr/bin/quickshell`'s default `configDirectory` is
`/etc/pam.d`. `/etc/pam.d/hyprlock` IS owned by hyprlock 0.9.6-2 and goes with
`-Rns`, but nothing reads it. Verified independently of the code comment that
asserts it.

**The brief said seven touchpoints, the SUMMARY said ten. 25 files carry the
string.** Enumerated with a tree-wide grep, then classified against
`retirement-check`'s own 16 scan classes rather than guessed.

**Only ONE class skips whole-line comments.** `grep_hits(...,
skip_comment_lines=True)` is passed by `scan_cross_package_refs` and no other
scanner. So comment prose is fatal in `keybinds`, `contract-json`,
`matugen-templates`, `test-fixtures` and `install-stow-lists`, and harmless
everywhere cross-package-refs reaches. `repo-prose` and `planning-archive` are
report-domain (walker carries 163 prose refs at `failed_classes=0`).

**No live invocation of hyprlock exists.** The only non-comment references are
the six code lines listed in Task 1. The `~/.local/state/theme/hyprlock.conf`
producer/consumer pair is exactly the two files being deleted.

## Tasks

### Task 1 — Remove every blocking reference, then the package (ONE commit)

**Deletions**
- `hypr/.config/hypr/hyprlock.conf` (own-tree)
- `matugen/.config/matugen/templates/hyprlock-colors.conf`

**Code edits (blocking classes)**
- `matugen/.config/matugen/config.toml` — drop `[templates.hyprlock]` (123-126) and the comment at 161
- `matugen/.config/matugen/templates/hyprland-colors.lua:43` — reword comment
- `theme-engine/.config/theme-engine/contract.json` — drop the `hyprlock.conf` entry (22 → 21 files)
- `hypr/.config/hypr/config/permissions.lua:129` — delete the screencopy rule; retire its 115-124 rationale block
- `hypr/.config/hypr/config/keybinds.lua:56,368` — reword comments
- `hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-quickshell-windowrules.lua:7,10` — swap the non-quickshell example namespace off hyprlock
- `install.sh:62` — drop the package; `install.sh:159` — reword comment
- `stow.sh:158,654` — reword comments

**Registry**
- ADD `hyprlock|retired|hypr/.config/hypr/hyprlock.conf|260827-833` to `REGISTRY_RAW`.
  (There is no row to remove — `retirement-check hyprlock` currently errors
  `unknown surface`.)

**Prose that is now FALSE (correctness, not tidying)**
- `README.md:27,50,76` — lock screen is Quickshell, not Hyprlock
- `PowerActions.qml:23-24,105`, `Prefs.qml:332-333`, `AppearancePage.qml:374-375`,
  `LockContinuity.qml:13` — all say the retirement is "HELD"
- `LockPam.qml` — the `/etc/pam.d/hyprlock` note moves to past tense
- `theme-engine/.config/theme-engine/lib/wallpaper.sh:233` — cites `hyprlock.conf:50`, a deleted file

**Left deliberately untouched**
- `docs/superpowers/specs/2026-07-28-hyprland-lua-config-migration-design.md` —
  point-in-time design record, same treatment as `v3.0-MILESTONE-AUDIT.md`
- `.claude/settings.local.json` — untracked local permission allowlist
- Accurate historical comments in `hypridle.conf`, `dynamic-cursors.lua`,
  `hypr-lua-harness`, `wallpaper-picker.sh`, `LockClock.qml`, `PowerMenu.qml`

**Package**: `sudo pacman -Rns hyprlock` in the same commit boundary.

**verify**: `retirement-check hyprlock` → `failed_classes=0`; `theme-parity`,
`theme-doctor`, `hypr-equivalence-check` stay green; quickshell config still
loads (`~/.cache/quickshell.log`).

## Must-haves

- `retirement-check hyprlock` reports `status=retired failed_classes=0`
- `pacman -Q hyprlock` fails
- Lock screen still authenticates via `passwd` PAM stack (unchanged file)
- One commit contains both the config removal and the package removal
