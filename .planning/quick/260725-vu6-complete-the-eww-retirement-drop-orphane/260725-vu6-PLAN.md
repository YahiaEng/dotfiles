---
phase: quick-260725-vu6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/contract.sh
  - theme-engine/.config/theme-engine/lib/reload.sh
  - eww/.config/eww/eww.scss
  - eww/.config/eww/eww.yuck
  - eww/.config/eww/assets/blank.png
  - stow.sh
  - install.sh
  - hypr/.config/hypr/config/windowrules.conf
  - hypr/.config/hypr/config/autostart.conf
  - hypr/.config/hypr/scripts/media-status.sh
  - matugen/.config/matugen/config.toml
  - waybar/.config/waybar/config-athena.jsonc
  - waybar/.config/waybar/style-athena.css
  - waybar/.config/waybar/config-vertical.jsonc
  - ags/.config/ags/style.scss
autonomous: true
requirements: [QUICK-EWW-RETIRE]

must_haves:
  truths:
    - "theme-doctor no longer reports the ~/.local/state/theme/eww.scss existence FAILURE; the only remaining failure is the pre-existing, out-of-scope git-clean check."
    - "theme-parity exits with 0 failures across all 22 theme targets (the 22 orphaned-target failures are gone)."
    - "A fresh clone + install.sh + stow.sh no longer installs the retired AUR toolkit, no longer stows a package for it, and no longer pre-creates its config dir."
    - "This host has no ~/.config/eww directory and no new broken symlinks anywhere under ~/.config."
    - "Hyprland's config parses clean with no layerrules left targeting a namespace no client claims."
    - "Every remaining lowercase reference outside .planning/, .git/, docs/superpowers/ and ags @girs typings is a runtime ~/.cache/ path literal that is still live and explicitly reported."
  artifacts:
    - "theme-engine/.config/theme-engine/contract.json — files[] one entry shorter, valid JSON"
    - "theme-engine/.config/theme-engine/lib/contract.sh — both scss-kv dispatcher branches removed"
    - "theme-engine/.config/theme-engine/lib/reload.sh — dead reload branch removed"
    - "eww/ — deleted from the repo and from git's index"
  key_links:
    - "contract.json files[] drives contract_files() in lib/contract.sh, which drives BOTH theme-doctor's existence loop and theme-parity's 3-layer loop — removing the entry is what turns 1 doctor check and 22 parity targets green."
    - "contract.json's format tag dispatches to case \"$fmt\" in contract_extract_names() AND contract_extract_values(); the catch-all is LOUD by design, so a format value and its two branches must be removed together, never one without the other."
    - "stow -D must run while eww/ still exists in the repo — stow computes link ownership from the package tree, so deleting the directory first strands the host symlinks."
---

<!-- planner-discipline-allow: eww -->
<!-- planner-discipline-allow: eww.scss -->
<!-- planner-discipline-allow: eww.yuck -->
<!-- planner-discipline-allow: eww-media-popup -->
<!-- planner-discipline-allow: scss-kv -->
<!-- Rationale: every negative grep in this plan is path-scoped and explicitly excludes .planning/, where this plan file lives. The plan's own prose can never satisfy or invalidate its own gates. -->

<objective>
Finish the toolkit retirement that Phase 10 (10-06) left half-done. The matugen template that rendered `~/.local/state/theme/eww.scss` was deleted, but the contract entry that declares it was not — so `theme-doctor`, `theme-parity` and `theme-stress-test`, this project's three core regression gates, are red for a pure bookkeeping reason. Four more pieces of scaffolding survive alongside it: inert layerrules, a stow package, an AUR dependency, and a dead reload branch.

Purpose: every gate result in this repo is untrustworthy until these go green. STATE.md lists this as the first thing v3.0 must close.
Output: 1 doctor failure cleared, 22 parity failures cleared, the retired toolkit gone from the repo, from `install.sh`/`stow.sh`, and from this host's `~/.config`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@theme-engine/.config/theme-engine/contract.json
@theme-engine/.config/theme-engine/lib/contract.sh
@theme-engine/.config/theme-engine/lib/reload.sh
@stow.sh
</context>

<constraints>

**Read this before touching anything.**

1. **This runs directly on the main checkout — no worktree isolation.** `~/.config/theme-engine` and `~/.config/eww` are stow symlinks into this checkout, so an isolated worktree would edit files the live host does not read. Every edit here is live.

2. **The working tree is dirty and MUST stay that way.** `hypr/.config/hypr/config/monitors.conf` is modified, many files under `wallpapers/Pictures/Wallpapers/` are added/deleted, `.planning/HANDOFF.json` is deleted, and an untracked `csv` file exists. **None of that is in scope.** Never `git add -A`, never `git add .`, never `git checkout`/`git restore`/`git stash` anything. Every `git add` in this plan uses explicit pathspecs, and every `git rm` names one path.

3. **`theme-doctor`'s `git status --porcelain is empty` failure is EXPECTED to remain.** It is caused by constraint 2's pre-existing dirt. Do not "fix" it. The final report must say plainly: 1 failure remains and it is out of scope — never claim a fully green doctor run.

4. **Do not commit `.planning/` artifacts** (SUMMARY.md, PLAN.md, STATE.md). The orchestrator commits docs separately. Do not touch ROADMAP.md.

5. **Baselines, captured before any change:**
   - `theme-doctor` → 135 passed, 2 failed (the orphaned state-file check + the out-of-scope git-clean check)
   - `theme-parity` → 1542 passed, 22 failed (22 targets × the one orphaned check)
   - Broken symlinks under `~/.config` (max depth 3) → exactly 2 pre-existing: `~/.config/hyprland.conf.bak` and `~/.config/zen/a7er8vyx.Default (release)/lock`. Neither is related to this task; both must still be there at the end, and no third may appear.

6. **Tooling is confirmed present on this host:** `jq`, `shellcheck`, `sass`, `stow`, `hyprctl`.

</constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Re-verify no live consumer, then cut the retired target out of the theme pipeline</name>
  <files>theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/contract.sh, theme-engine/.config/theme-engine/lib/reload.sh</files>
  <precondition>`jq` is on PATH and `theme-engine/.config/theme-engine/contract.json` parses (`jq . theme-engine/.config/theme-engine/contract.json`) — the whole theme pipeline reads this file through `jq`, and a pre-broken contract would make every gate in this plan meaningless.</precondition>

  <action>
This is the thin end-to-end slice: it touches the declaration (contract.json), the parser (contract.sh) and the reload fan-out (reload.sh) — the full depth of the theme pipeline — and it is what turns the failing gates green. Do the consumer sweep FIRST, in this task, before deleting anything.

**A. Consumer re-verification sweep (mandatory, do not skip).** PROJECT.md records the "Consumer-check before retiring a toolkit" decision as `⚠ Revisit` precisely because the Phase 10 check missed the layerrules. Do it properly. From the repo root run a repo-wide case-insensitive sweep excluding `.git`, `.planning`, `docs/superpowers` and `ags/.config/ags/@girs` (a 145k-line vendored GTK typings tree whose `previewWidget`/`FocusNewWindows` identifiers contain the letters as an incidental mixed-case substring — confirm this for yourself rather than taking it on faith). Classify EVERY hit into exactly one of: **live consumer** (must not be deleted blind), **runtime cache-path literal**, **stale comment**, or **incidental substring**. Cover matugen templates and `matugen/config.toml`, `hypr/.config/hypr/config/autostart.conf`, every keybind file, all `waybar/.config/waybar/config-*.jsonc` and `style-*.css`, every script under `hypr/.config/hypr/scripts/`, and `windowrules.conf`. Record the classification table in the SUMMARY — it is a required output of this task, not optional colour.

Expected classification (verify each, do not assume): `waybar/.config/waybar/{config-athena.jsonc,style-athena.css,config-vertical.jsonc}` and `ags/.config/ags/style.scss` are **stale comments only** — no code path invokes the retired binary from any of them; `hypr/.config/hypr/scripts/media-players.sh` (`SELECTED_FILE`) and `media-art-resolve.sh` (`CACHE_DIR`) hold **live `~/.cache/` path literals** whose names merely carry the retired toolkit's name — these are real runtime state paths still read and written by the AGS media stack via `ags/.config/ags/lib/media.ts`, so they stay exactly as they are in this task (renaming them is a runtime-state change outside this task's scope, and would silently reset the user's persisted player selection). Confirm the retired binary is invoked nowhere: `grep -rn` for it as a bare command word across `hypr/`, `waybar/`, `ags/`, `matugen/`, `install.sh`, `stow.sh` must return only the lines this plan removes.

**B. `contract.json` — drop the orphaned declaration.** Remove the last element of `files[]`, the one naming the SCSS state file with the `scss-kv` format. **It is the last array element**, so the preceding entry (`satty.toml`) must lose its trailing comma or the file stops being valid JSON. `files[]` goes 18 → 17 entries. Do not touch `state_metadata_files` or `presence_only_files` — neither mentions this target.

**C. `lib/contract.sh` — remove both now-dead dispatcher branches.** `grep -n 'scss-kv' theme-engine/.config/theme-engine/contract.json` after step B must return nothing: that entry was the format's ONLY consumer, so both branches are now unreachable code. Delete the `scss-kv)` branch in `contract_extract_names()` (the `grep -oP` name extractor, around lines 88-98) and the `scss-kv)` branch in `contract_extract_values()` (the `sed -nE` pair extractor, around lines 190-197), each including its full leading comment block and its `;;` terminator. Leave the neighbouring `env-kv)` and `toml)` branches and the LOUD `*)` catch-all completely untouched — that catch-all is load-bearing by design and is what would make a future re-added format fail noisily instead of silently. **Gate this deletion on the jq check in `<verify>`**: if any other `files[]` entry still declared this format, the branches would stay.

**D. `lib/reload.sh` — remove the dead reload branch.** Delete the comment block and guarded `if` at roughly lines 120-135 (the `command -v` + `pgrep -x` double guard wrapping the `reload` subcommand). Leave exactly one blank line between the swayosd block's closing `fi` (line 118) and the `# ── AGS media applet (MEDIA-03, 10-05)` comment that follows, matching the spacing used between every other block in that function. Change nothing else in `theme_engine_reload()`.

Do not touch `stow.sh`, `install.sh`, `windowrules.conf` or the `eww/` directory in this task — those are Task 2.
  </action>

  <verify>
    <automated>jq . theme-engine/.config/theme-engine/contract.json >/dev/null && echo "contract.json: valid JSON"</automated>
    <automated>test "$(jq '.files | length' theme-engine/.config/theme-engine/contract.json)" = "17" && echo "contract.json: files[] == 17 (was 18)"</automated>
    <automated>jq -e '[.files[].format] | index("scss-kv") == null' theme-engine/.config/theme-engine/contract.json >/dev/null && echo "no contract entry declares that format"</automated>
    <automated>test "$(grep -c 'scss-kv)' theme-engine/.config/theme-engine/lib/contract.sh)" = "0" && echo "both dispatcher branches gone"</automated>
    <automated>grep -c 'unknown format' theme-engine/.config/theme-engine/lib/contract.sh | grep -qx 2 && echo "both LOUD catch-alls still present"</automated>
    <automated>bash -n theme-engine/.config/theme-engine/lib/contract.sh && bash -n theme-engine/.config/theme-engine/lib/reload.sh && echo "shell syntax OK"</automated>
    <automated>shellcheck -S error theme-engine/.config/theme-engine/lib/contract.sh theme-engine/.config/theme-engine/lib/reload.sh && echo "shellcheck clean"</automated>
    <automated>./theme-engine/.config/theme-engine/theme-parity 2>&1 | tail -5</automated>
    <automated>./theme-engine/.config/theme-engine/theme-doctor 2>&1 | tail -8</automated>
  </verify>

  <done>
`files[]` holds 17 entries and parses as JSON; no entry declares the retired format; both dispatcher branches and the dead reload branch are gone while both LOUD catch-alls survive; `bash -n` and `shellcheck -S error` are clean on both edited scripts. `theme-parity` reports **0 failed** (was 22), with its passed count unchanged at 1542. `theme-doctor` reports **1 failed** (was 2), and that one remaining failure is the out-of-scope `git status --porcelain is empty` check. Note the passed count does **not** rise: the checks are generated per contract entry, so dropping the entry removes that check rather than turning it into a pass — expect 135 passed / 1 failed, and treat any drop below 135 passed as a regression to investigate. The consumer classification table is written down.

Commit with explicit pathspecs only:
`git add theme-engine/.config/theme-engine/contract.json theme-engine/.config/theme-engine/lib/contract.sh theme-engine/.config/theme-engine/lib/reload.sh`
then commit as `fix(theme-engine): drop orphaned contract entry, dead format parser and dead reload branch`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the stow package, the AUR dependency, the inert layerrules — and unstow this host</name>
  <files>eww/.config/eww/eww.scss, eww/.config/eww/eww.yuck, eww/.config/eww/assets/blank.png, stow.sh, install.sh, hypr/.config/hypr/config/windowrules.conf</files>
  <precondition>`~/.config/eww` currently exists as a real directory containing three stow-owned symlinks (`eww.scss`, `eww.yuck`, `assets`) pointing into this checkout, and the `eww/` package directory still exists in the repo — `stow -D` needs the package tree present to compute what to unlink.</precondition>

  <action>
**Order matters. Unstow the host BEFORE deleting the repo directory** — stow derives link ownership from the package tree, so deleting first would strand three dangling symlinks in `~/.config/eww` with no clean way to have stow remove them.

**A. Unstow this host.** From the repo root, dry-run first: `stow -n -v -D eww --target="$HOME"` and read what it proposes to unlink — it must name only the three entries under `~/.config/eww` and nothing else. Then run it for real without `-n`. `~/.config/eww` will be left as an empty real directory (it was pre-created by `stow.sh`, so stow does not remove it); confirm it is empty and `rmdir` it. If stow proposes to touch anything outside `~/.config/eww`, **stop and report** rather than proceeding.

**B. Delete the package from the repo.** `git rm -r eww` — one explicit pathspec, removing `eww.scss`, `eww.yuck` and `assets/blank.png`. The `.yuck` file is already gutted (its own header states it is intentionally left with no active window or widget definitions), so nothing live is lost.

**C. `stow.sh` — two removals.** Remove the package name from the `PACKAGES` array (line 22, between `elephant` and `fastfetch`). Remove the whole `BAR-04/D-19` comment block and its `mkdir -p "$HOME/.config/eww"` (lines 72-82) plus the now-doubled blank line, leaving the preceding `THM-01/D-08` gtk-3.0/gtk-4.0 pre-create block and the following `for pkg in ...` loop separated by exactly one blank line. Leave the fish and gtk pre-creates alone — they serve packages that still exist.

**D. `install.sh` — drop the AUR dependency.** Remove the `Media center (BAR-04 / D-18 / D-36 ...)` comment block and the package name beneath it (lines 269-276) plus one adjacent blank line, from the `AUR_PKGS` array. Keep the `AGS media applet (MEDIA-04)` block and `aylurs-gtk-shell` that follow it fully intact — that is the live replacement toolkit. Verify the array still closes with `)` and that no other array member was disturbed.

**E. `hypr/.config/hypr/config/windowrules.conf` — three edits.** Remove the four-line comment block plus the `layerrule = blur on, ...` rule that targets the retired popup's namespace (lines 255-259). Remove the matching `layerrule = ignore_alpha 0.5, ...` line for the same namespace (line 272) — and only that line: the long comment block immediately below it (lines 273-285) belongs to the `layerrule = ignore_alpha 0.25, match:namespace ags-media` at line 286, so it must survive untouched. Then fix the dangling cross-reference: the AGS media applet comment (around lines 260-264) says its blur rule mirrors "the eww-media-popup rule above", which will point at nothing — reword that sentence so it refers to the surrounding namespace-scoped blur rules generally, keeping the rest of the comment's meaning (namespace `ags-media`, set by Astal.Window in 10-02, paired with the translucent backgrounds in `ags/style.scss`) intact. The `wleave` animation rule at line 254 and every other rule in the file stay exactly as they are.
  </action>

  <verify>
    <automated>test ! -e "$HOME/.config/eww" && echo "host config dir gone"</automated>
    <automated>test ! -e eww && echo "repo package dir gone"</automated>
    <automated>test "$(find "$HOME/.config" -maxdepth 3 -xtype l 2>/dev/null | wc -l)" = "2" && find "$HOME/.config" -maxdepth 3 -xtype l</automated>
    <automated>bash -n stow.sh && bash -n install.sh && shellcheck -S error stow.sh install.sh && echo "stow.sh + install.sh syntax and shellcheck clean"</automated>
    <automated>grep -vE '^[[:space:]]*#' stow.sh | grep -c 'eww' | grep -qx 0 && echo "stow.sh: no non-comment reference remains"</automated>
    <automated>grep -vE '^[[:space:]]*#' install.sh | grep -c 'eww' | grep -qx 0 && echo "install.sh: no non-comment reference remains"</automated>
    <automated>grep -c 'eww' stow.sh install.sh hypr/.config/hypr/config/windowrules.conf</automated>
    <automated>grep -c 'layerrule' hypr/.config/hypr/config/windowrules.conf</automated>
    <automated>hyprctl reload >/dev/null 2>&1; hyprctl configerrors 2>&1 | head -5</automated>
    <automated>./theme-engine/.config/theme-engine/theme-parity 2>&1 | tail -3</automated>
  </verify>

  <done>
`~/.config/eww` no longer exists and the repo has no `eww/` directory. The broken-symlink count under `~/.config` is still exactly the 2 pre-existing baseline entries — no stow link elsewhere was collateral damage. `stow.sh` and `install.sh` pass `bash -n` and `shellcheck -S error`, and the count greps above return `0` for all three files (comment blocks removed too, so even the comment-inclusive count is zero). `hyprctl configerrors` reports no errors, and the `ags-media` blur/ignore_alpha rules plus the `wleave` animation rule are all still present. `theme-parity` still reports 0 failures.

Commit with explicit pathspecs only (the `git rm` in step B already staged the deletions):
`git add stow.sh install.sh hypr/.config/hypr/config/windowrules.conf`
then commit as `chore: remove retired eww stow package, AUR dep and inert layerrules`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sweep the stale comments and prove the retirement is complete</name>
  <files>matugen/.config/matugen/config.toml, hypr/.config/hypr/config/autostart.conf, hypr/.config/hypr/scripts/media-status.sh, waybar/.config/waybar/config-athena.jsonc, waybar/.config/waybar/style-athena.css, waybar/.config/waybar/config-vertical.jsonc, ags/.config/ags/style.scss</files>

  <action>
Every edit in this task is **comment-only**. Not one line of executable config, CSS rule, JSON key or shell statement may change — the `<verify>` block enforces this mechanically via the diff, so a stray functional edit fails the task.

Rewrite each stale comment so it states what is true today. Do not simply delete the comments where they carry real rationale — the rationale is the point; only the dead toolkit reference is wrong. Where a comment's entire subject is the retired feature and the explanation no longer teaches anything, delete it outright.

- **`matugen/.config/matugen/config.toml` (~lines 91-98).** Keep the tombstone explaining that the media-popup colours template was removed in 10-06 if you judge it still useful, but it must stop naming the retired toolkit — the point worth preserving is that no template renders that state file any more. The second comment (~97-98) describes the AGS reload as mirroring a `pgrep`-guarded reload pattern in `theme-engine/lib/reload.sh` — that branch no longer exists after Task 1, so reword it to describe the AGS reload on its own terms (no `post_hook` here; reload is owned by `theme_engine_reload()`).
- **`hypr/.config/hypr/config/autostart.conf` (~lines 43-45).** The comment explains why the AGS media applet is autostarted by reference to the daemon it replaced. Reword so the rationale survives without naming the retired toolkit.
- **`hypr/.config/hypr/scripts/media-status.sh` (~line 7).** The header calls the script's stdout a `deflisten` payload for the retired toolkit — actively wrong now. Read `ags/.config/ags/lib/media.ts` to see how the output is actually consumed today and describe that instead. **Do not touch `media-players.sh` or `media-art-resolve.sh`**: their `~/.cache/eww-media-player` and `~/.cache/eww-media-art` literals are live runtime paths (classified in Task 1) and renaming them is out of scope.
- **`waybar/.config/waybar/config-athena.jsonc` (~line 27) and `style-athena.css` (~line 220).** Both describe the `custom/media` module as the opener for the retired popup. Read the module's actual `on-click` in `config-athena.jsonc` and describe what it opens today.
- **`waybar/.config/waybar/config-vertical.jsonc` (~line 71).** A provenance note crediting the playing-state icon to the deleted `.yuck` file. Keep the note about the icon, drop the reference to a file that no longer exists.
- **`ags/.config/ags/style.scss` (lines 3, 10-11, 52).** Three comments explain this file's `@import` path depth and border convention *by contrast with* the deleted stylesheet. The substantive point — why this file's relative `@import` depth is what it is, and the border convention — must survive; rewrite each so it stands on its own without the dead comparison. SCSS `//` comments only; touch no selector, property or value.

Then run the completion proof and the full gate sweep described in `<verify>`, and write the findings into the SUMMARY.
  </action>

  <verify>
    <automated>git diff -U0 -- matugen/.config/matugen/config.toml hypr/.config/hypr/config/autostart.conf hypr/.config/hypr/scripts/media-status.sh waybar/.config/waybar/config-athena.jsonc waybar/.config/waybar/style-athena.css waybar/.config/waybar/config-vertical.jsonc ags/.config/ags/style.scss | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*(#|//|/\*|\*)' | grep -vE '^[+-][[:space:]]*$' | wc -l | grep -qx 0 && echo "diff is comment-only across all 7 files"</automated>
    <automated>bash -n hypr/.config/hypr/scripts/media-status.sh && shellcheck -S error hypr/.config/hypr/scripts/media-status.sh && echo "media-status.sh clean"</automated>
    <automated>sass --no-source-map "$HOME/.config/ags/style.scss" /dev/null && echo "ags style.scss compiles"</automated>
    <automated>python3 -c "import tomllib,sys; tomllib.load(open('matugen/.config/matugen/config.toml','rb')); print('matugen config.toml: valid TOML')"</automated>
    <automated>hyprctl reload >/dev/null 2>&1; hyprctl configerrors 2>&1 | head -5</automated>
    <automated>grep -rnI 'eww' . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=superpowers --exclude-dir=@girs | grep -v 'cache/eww-media' | tee /dev/stderr | wc -l | grep -qx 0 && echo "COMPLETION PROOF: no lowercase reference remains outside the allowlisted runtime cache paths"</automated>
    <automated>grep -rniI 'eww' . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=superpowers --exclude-dir=@girs | grep -v 'cache/eww-media'</automated>
    <automated>./theme-engine/.config/theme-engine/theme-parity 2>&1 | tail -3</automated>
    <automated>./theme-engine/.config/theme-engine/theme-doctor 2>&1 | tail -8</automated>
    <automated>./theme-engine/.config/theme-engine/theme-stress-test 2>&1 | tail -8</automated>
  </verify>

  <done>
The diff across all 7 files is comment-only (0 non-comment changed lines). `media-status.sh` passes `bash -n` + `shellcheck -S error`; `ags/style.scss` compiles under `sass`; `matugen/config.toml` parses as TOML; `hyprctl configerrors` reports no errors.

**Completion proof:** the case-sensitive repo-wide grep returns zero hits outside `.planning/`, `.git/`, `docs/superpowers/` and the vendored `@girs` typings, once the two allowlisted live runtime cache paths are filtered out. The case-insensitive pass is run separately and every one of its hits is either one of those cache-path lines or a mixed-case substring inside an unrelated identifier — enumerate them in the SUMMARY rather than asserting it.

**Gates:** `theme-parity` reports **0 failed**. `theme-doctor` reports **1 failed**, and the report must name it explicitly as the pre-existing, out-of-scope `git status --porcelain is empty` check caused by the user's own uncommitted wallpaper/monitor changes — never describe the run as fully green. `theme-stress-test` is run and its result reported; treat any failure it surfaces that is unrelated to this retirement (including the same git-clean class of check) as pre-existing and out of scope, and say so rather than chasing it.

Commit with explicit pathspecs only:
`git add matugen/.config/matugen/config.toml hypr/.config/hypr/config/autostart.conf hypr/.config/hypr/scripts/media-status.sh waybar/.config/waybar/config-athena.jsonc waybar/.config/waybar/style-athena.css waybar/.config/waybar/config-vertical.jsonc ags/.config/ags/style.scss`
then commit as `docs: correct comments left dangling by the eww retirement`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → live host (`~/.config`, `~/.local/state`) | This plan runs without worktree isolation; every repo edit is immediately live through stow symlinks, and Task 2 mutates the host filesystem directly (`stow -D`, `rmdir`). |
| plan → user's uncommitted working tree | The tree carries unrelated dirt (wallpapers, monitors.conf, untracked `csv`) that a careless `git add`/`git checkout` would destroy or wrongly publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-vu6-01 | Tampering | `git add` / `git rm` scope | high | mitigate | Constraint 2 forbids `git add -A`/`.` and any checkout/restore/stash; every commit in every task lists explicit pathspecs and `git rm` names exactly one path. |
| T-vu6-02 | Denial of Service | `stow -D` on the live host | high | mitigate | Mandatory `stow -n -v -D` dry run first; explicit stop-and-report if it proposes touching anything outside `~/.config/eww`; post-check asserts the broken-symlink count under `~/.config` is still exactly the 2 baseline entries. |
| T-vu6-03 | Denial of Service | Hyprland config edit | medium | mitigate | `hyprctl reload` + `hyprctl configerrors` after the edit in Tasks 2 and 3; the surviving `ags-media` and `wleave` rules are asserted present, and the comment block owned by the `ags-media` rule is called out as must-survive. |
| T-vu6-04 | Tampering | `contract.json` hand-edit | medium | mitigate | The removed entry is the last array element, so the trailing-comma hazard is named explicitly in the action; `jq .` well-formedness plus an `18`-entry length assertion gate the task. |
| T-vu6-05 | Information Disclosure | Deleting a still-live consumer | medium | mitigate | Task 1 runs a mandatory repo-wide consumer sweep with a written classification table before any deletion; the two live `~/.cache/` path literals are explicitly ruled out of scope rather than deleted on assumption. |
| T-vu6-06 | Tampering | package-manager installs | low | accept | No package is installed by this plan — `install.sh` only loses an array member. The package-legitimacy gate does not apply. |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `./theme-engine/.config/theme-engine/theme-doctor` — the `~/.local/state/theme/eww.scss exists` FAIL is gone: 135 passed / **1** failed (was 135/2). The passed count stays at 135 because the check is removed, not converted to a pass; the single remaining FAIL is the out-of-scope `git status --porcelain is empty`.
2. `./theme-engine/.config/theme-engine/theme-parity` — 1542 passed / **0** failed (was 1542/22).
3. `./theme-engine/.config/theme-engine/theme-stress-test` — run and report; unrelated pre-existing failures are out of scope.
4. `bash -n` + `shellcheck -S error` on every modified shell script: `lib/contract.sh`, `lib/reload.sh`, `stow.sh`, `install.sh`, `media-status.sh`.
5. `jq . theme-engine/.config/theme-engine/contract.json` — valid JSON, `files[]` length 18.
6. `hyprctl configerrors` — no errors.
7. Repo-wide completion grep (case-sensitive, excluding `.git`, `.planning`, `superpowers`, `@girs`, filtered for the allowlisted `cache/eww-media` runtime paths) — zero hits.
8. `test ! -e "$HOME/.config/eww"` and `find "$HOME/.config" -maxdepth 3 -xtype l` returning exactly the 2 baseline entries.
9. `git status --porcelain` — the only changes beyond the user's pre-existing dirt are the files this plan names, and all of them are committed.
</verification>

<success_criteria>
- 1 `theme-doctor` failure cleared and 22 `theme-parity` failures cleared, with the remaining doctor failure reported honestly as pre-existing and out of scope.
- The retired toolkit is gone from `contract.json`, `lib/contract.sh`, `lib/reload.sh`, `stow.sh`, `install.sh`, `windowrules.conf`, the repo tree, and this host's `~/.config`.
- No live consumer was deleted: the consumer classification table is in the SUMMARY, and the two live `~/.cache/` path literals are documented as an intentional, reported residual rather than silently changed.
- Three commits, each with explicit pathspecs; the user's unrelated uncommitted changes are untouched.
</success_criteria>

<output>
Create `.planning/quick/260725-vu6-complete-the-eww-retirement-drop-orphane/260725-vu6-SUMMARY.md` when done.

The SUMMARY must include: (a) the Task 1 consumer classification table, (b) the enumerated residual references and why each was left, (c) the before/after gate numbers, and (d) a plain statement that `theme-doctor` still reports one failure and that it is the pre-existing git-clean check, not a regression.
</output>
