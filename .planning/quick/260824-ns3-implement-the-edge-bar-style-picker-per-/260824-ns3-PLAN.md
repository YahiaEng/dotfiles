---
quick_id: 260824-ns3
phase: quick-260824-ns3
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [S-1, S-2, S-3, S-4, S-5, S-6, S-7, S-8, S-9]
files_modified:
  - quickshell/.config/quickshell/modules/Prefs.qml
  - quickshell/.config/quickshell/shell.qml
  - quickshell/.config/quickshell/modules/launcher/Launcher.qml
  - quickshell/.config/quickshell/modules/Dashboard.qml
  - quickshell/.config/quickshell/modules/EdgeBar.qml
  - quickshell/.config/quickshell/modules/edgebarpath.js
  - quickshell/.config/quickshell/modules/Bar.qml
  - quickshell/.config/quickshell/modules/dashboard/Design.qml
  - quickshell/.config/quickshell/modules/settings/pages/BarPage.qml
  - quickshell/.config/quickshell/modules/settings/RowIndex.qml
  - hypr/.config/hypr/scripts/tests/edgebar-path-golden.json
  - hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs

estimate:
  tokens: 300000
  raw_tokens: 300000
  tasks: 8
  confidence: low

must_haves:
  truths:
    - "S-1 (brief Q2): one string key `edgeBar.style` with exactly five values — off, continuous, brackets, segmented, halo — chosen from a single SelectRow in Settings > Bar. The `edgeBar.enabled` bool no longer exists as a stored key."
    - "S-2 (brief Q4): a stored `edgeBar.enabled: true` comes back as `style: \"continuous\"` and `false` as `\"off\"`, on this host and on any other install, without the operator touching anything."
    - "S-3 (brief Q2, load-bearing): `Launcher.qml`'s direction branch reads a derived `edgeBarRailPresent` boolean and never the style string; attachment is a SEPARATE derived predicate, so a future style that renders nothing while being non-off cannot silently flip the launcher's direction."
    - "S-4 (brief Q1): Two Rails is gone. No code path, default or fallback renders it."
    - "S-5 (brief Q4 / hazard 5, REVERSES D-2): with Continuous selected — the default — the right-edge bar and the top/bottom rails read as ONE silhouette. `Bar.qml` changes; this is a deliberate reversal of D-2, recorded as such."
    - "S-6 (hazard 1): Halo draws four edges and Brackets draws four corner L-pieces, both with real VERTICAL runs, through a transposed coordinate emitter rather than a duplicated hand-authored path."
    - "S-7 (brief Q3): Continuous behaves byte-identically to round 11; Segmented merges the segments inside the bulge span into the bulge's own continuous silhouette; Brackets draws no bulge and panels open unattached; Halo swells 2 -> 12 and its toggle-OFF state is a STATIC PERMANENT 12px mass, never 'no bulge'."
    - "S-8 (hazard 2/6): no layer surface resizes on any frame of any animation, and each style's reservation is measured from `hyprctl -j monitors` and recorded rather than asserted."
    - "S-9: every colour is read from `Colours.qml` and every duration/curve from `Motion.qml`; the four gates stay green and `settings-index-check` goes from its current 117/1 to 118/0."
  artifacts:
    - quickshell/.config/quickshell/modules/edgebarpath.js
    - hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs
    - hypr/.config/hypr/scripts/tests/edgebar-path-golden.json
    - "an `edgeBar.style` key present in BOTH Prefs._allowedKeys and Prefs._defaults, and `edgeBar.enabled` absent from both"
    - "a five-entry SelectRow in modules/settings/pages/BarPage.qml plus its RowIndex.qml entries"
  key_links:
    - "Prefs `edgeBar.style` -> shell.qml's SINGLE resolution point -> `edgeBarStyle` / `edgeBarRailPresent` / `edgeBarPanelsAttach`: three derived values, one source; no consumer re-reads Prefs."
    - "`edgeBarRailPresent` -> Launcher direction ONLY; `edgeBarPanelsAttach` -> Launcher AND Dashboard attachment (flares, rim clip, corner radii, flush margin). Crossing these two wires is the single failure this task is built to prevent."
    - "edgebarpath.js -> EdgeBar.qml (the only path implementation) -> edgebar-path-test.mjs golden: a sweep-flag regression has no other detector — no QML error, no gate, no log line."
    - "EdgeBar `_surfaceDepth` -> the surface's implicitHeight/implicitWidth: pinned to each style's MAXIMUM reach, never to the animating value (hazard 2)."
    - "Bar.qml continuous slab -> the top/bottom strips' right ends: the strips stop at the bar's exclusive-zone boundary and the BAR paints the bridge, because a surface with a non-negative exclusive zone cannot be positioned inside another surface's zone."
---

<objective>
Replace the edge bar's on/off toggle with a five-entry **style picker** —
`off · Continuous · Brackets · Segmented · Halo` — implementing all four shapes
exactly as drawn in the approved design study, with Continuous as the default
and a migration for every install that currently stores `edgeBar.enabled`.

Purpose: the operator saw seven directions and could not pick one, so the shell
ships four of them behind a picker instead of one behind a switch. "Two Rails" —
the shape that ships today — is removed by operator decision and must not
survive as a fallback or a default.

Output: one string pref, three derived predicates in `shell.qml`, a transposed
path emitter that can draw vertical runs, four shapes, a settings picker, and an
operator sign-off on what cannot be self-verified on this host.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/notes/edge-bar-style-directions.md
@.planning/notes/edge-rail-studies.html
@.planning/STATE.md
@.claude/CLAUDE.md
</context>

<!-- planner-discipline-allow: edgeBarEnabled -->
<!-- planner-discipline-allow: edgeBar.enabled -->
These two retired identifiers are named literally throughout the plan because
Task 1's whole job is to DELETE them; an acceptance criterion then negative-greps
the source tree (never this plan) to prove they are gone.

<decisions_already_made>

**Nothing below is open. Do not ask the operator. Do not present options.**
Every line here is quoted from or directly derived from the brief, which the
operator settled on 2026-08-24.

| # | Decision | Source |
|---|---|---|
| Q1 | Two Rails is REMOVED. Picker is exactly five entries. | *"no two rails does not survive."* |
| Q2 | One `edgeBar.style` string; the `edgeBar.enabled` bool is retired. | brief "DECIDED — Q2 and Q4" |
| Q2b | `Launcher.qml` reads a derived `edgeBarRailPresent`, NEVER the style string. | brief, "the load-bearing detail that must not be lost" |
| Q3-continuous | Toggle visible, default ON, behaviour byte-identical to round 11. | operator judged it live and approved it |
| Q3-segmented | Toggle visible, default ON; segments inside the bulge span MERGE into the bulge's silhouette. | *"an attachment root cannot have gaps"* |
| Q3-brackets | NO bulge drawn; panels do NOT attach; toggle HIDDEN; hover landmark is the L arms extending toward the centre. | study's own table scores Brackets `0 direct` |
| Q3-halo | Toggle visible, default ON; swells 2 -> 12. **OFF = static permanent 12px mass, NOT "no bulge".** | brief, "do not implement it as 'no bulge'" |
| Q4 | Default `"continuous"`; `enabled:true` -> `"continuous"`, `false` -> `"off"`. | brief "DECIDED — Q2 and Q4" |
| Q4b | Continuous REVERSES D-2 and it is the DEFAULT, so this fires on first install. Record the reversal deliberately, the way D-3's was recorded in round 11. | brief hazard 5 |

**Two additional calls this plan makes, both stated openly rather than left to
discover mid-execution — neither reduces any operator decision:**

- **DC-1 — Brackets keeps hover-to-summon.** The brief moves the *landmark* to
  the corners; it does not say the corners stop summoning. R5/R6 (dwelled hover
  opens the dashboard / the launcher's menu mode) is shipped, operator-approved
  behaviour, and silently dropping it on one style would be a capability loss
  nobody asked for. So on Brackets the dwell hit region moves to the two corner
  L regions of each surface and still fires `bulgeHoverTriggered()`. The panels
  it summons open **unattached** (Q3-brackets). Listed explicitly in the
  operator checkpoint (Task 8) for confirmation.
- **DC-2 — position beats the study table's "8px" for Halo's right rail.**
  Hyprland's reservation is `margins.<anchored-edge> + exclusiveZone` (hazard 6).
  The right rail cannot both sit at the study's `RIGHT_EDGE` (10px inside the
  bar's own reserved boundary) *and* reserve only 2px — those are the same
  number. The drawn shape wins: the right rail takes `margins.right =
  Design.edgeBarSideMargin` and `exclusiveZone: 0`, overhanging exactly the way
  the bulge already does. Halo's measured cost therefore lands near 6px, not 8.
  The real vector is **measured and recorded**, never asserted (Task 4).

</decisions_already_made>

<measured_ground_truth>

Everything here was read off this repo and this host during planning. Do not
re-derive it; do re-measure anything you change.

**Gate baselines, run 2026-08-24 before any edit:**

```
quickshell-doctor     28 passed, 0 failed
colour-lint          359 passed, 0 failed
motion-lint          546 passed, 0 failed
keybind-doctor        13 passed, 0 failed
settings-index-check 117 passed, 1 FAILED   <- PRE-EXISTING
```

`settings-index-check` CHECK A already fails: *"pageIdx 2 (BarPage.qml):
RowIndex entries (19) != row declarations (21)"*. Those two missing rows are
`"Edge bar"` and `"Animate the bulge"`, added by quick task 260823-9ak without
their `RowIndex.qml` entries. This task edits both rows, so it closes that gap
in Task 1 and every later task runs the gate at **118/0**.

**Where the current wiring lives:**

| Fact | Location |
|---|---|
| `edgeBarEnabled` resolved once | `shell.qml:56` |
| `edgeBarAnimatedBulge` resolved once | `shell.qml:61` |
| both EdgeBar LazyLoaders | `shell.qml:586-616` |
| threaded into Dashboard | `shell.qml:446` |
| threaded into Launcher | `shell.qml:845` |
| dwell-hover Connections | `shell.qml:1477-1489` |
| Launcher direction + attachment (18 sites, one flag) | `Launcher.qml:67,110,482,483,599,647-650,692,693,699,703-706,726,730,739,743` |
| Dashboard attachment | `Dashboard.qml:300,302` and its two `AttachedCorner` blocks at `769-793` |
| Prefs allow-list / defaults | `Prefs.qml:144,151,194,195` |
| Settings rows | `BarPage.qml:175-197` |
| edge bar Design tokens | `Design.qml:705-827` |
| `quickshell-doctor` registry row | `hypr/.config/hypr/scripts/quickshell-doctor:446` — `"EdgeBar.qml\|quickshell-baredge-\|pattern\|2\|reserve\|permanent"`. The `2` is the LAYER LEVEL (WlrLayer.Top), **not** an instance count, and `reserve` means the gate does not assert `exclusiveZone: 0`. Adding `-left`/`-right` namespaces needs NO new row: the row is a `pattern` match and all four instances resolve to it. Do not add a second row — two matching rows fail the live cardinality check. |

**Hyprland layer rules:** `windowrules.lua` has NO `baredge`-specific rule. The
`^quickshell-.*` family pair (`blur = true` at line 367, `ignore_alpha = 0.5` at
line 401) covers any new namespace automatically. **No Hyprland config change is
needed anywhere in this task** — which is why `keybind-doctor` and
`hypr-equivalence-check` are untouched. Keep every fill opaque (the strip's
stops already are) so the 0.5 `ignore_alpha` floor never eats the blur.

**Geometry constants, from the study's own source (`edge-rail-studies.html:425-431`):**

```
W=1920  H=1080                     capture scale; real panel 2560x1440
BAR = {x:1870, y:10, w:44, h:1060} the floating vertical right-edge bar
INSET      = 10                    = Design.edgeBarSideMargin
RIGHT_EDGE = 1860                  = screenW - 50 - 10; strips stop before the bar
T          = 6                     = Design.edgeBarThickness
SWELL      = 10                    = Design.edgeBarBulgeSwellExtra
TOPW = 760  BOTW = 640             = Design.edgeBarBulgeWidthTop / ...Bottom
reserved   = [0, 6, 50, 6]         [left, top, right, bottom]
```

`RIGHT_EDGE` is **not a number to hardcode** — it is what today's strip surface
already resolves to. The strip is anchored left+right with
`margins.left/right = 10` and `exclusiveZone: 6`; a surface with a non-negative
exclusive zone is positioned inside every other surface's zone, so the bar's 50
already pushes the strip's right end to 1860. Leave that mechanism alone.

**The study's own drawing code, per shape** (`edge-rail-studies.html:552-627`) —
this is the approved geometry, quoted rather than paraphrased:

```js
// Continuous
railH(0, INSET, BAR.x+BAR.w/2, "top",    TOPW, d.top);
railH(H, INSET, BAR.x+BAR.w/2, "bottom", BOTW, d.bot);
rect{x:BAR.x,   y:0,  w:BAR.w,   h:H,    rx:BAR.w/2,     fill:gradV, opacity:.95}
rect{x:BAR.x+4, y:22, w:BAR.w-8, h:H-44, rx:(BAR.w-8)/2, fill:surface, opacity:.92}

// Brackets — L=170 at 1920 scale; DERIVE PROPORTIONALLY, do not hardcode 170
corner(x,y,dx,dy):
  rect{x:(dx>0?x:x-L), y:(dy>0?y:y-k), w:L, h:k, rx:k/2, fill:grad}    // k = T
  rect{x:(dx>0?x:x-k), y:(dy>0?y:y-L), w:k, h:L, rx:k/2, fill:gradV}
corner(INSET,0,1,1) corner(RIGHT_EDGE,0,-1,1) corner(INSET,H,1,-1) corner(RIGHT_EDGE,H,-1,-1)

// Halo — k = 2 (NOT T), rx = 1
rect{x:INSET,      y:0,     w:RIGHT_EDGE-INSET, h:k,          rx:1, fill:grad}
rect{x:INSET,      y:H-k,   w:RIGHT_EDGE-INSET, h:k,          rx:1, fill:grad}
rect{x:0,          y:INSET, w:k,                h:H-INSET*2,  rx:1, fill:gradV}
rect{x:RIGHT_EDGE, y:INSET, w:k,                h:H-INSET*2,  rx:1, fill:gradV}
// note: the corners do NOT close — the 10px break on both axes is deliberate

// Segmented — top rail only; bottom stays an ordinary rail
n=10, gap=8, total=RIGHT_EDGE-INSET, seg=(total-gap*(n-1))/n
rect{x:INSET+i*(seg+gap), y:0, w:seg, h:T, rx:T/2,
     fill:(i===act ? grad : outline), opacity:(i===act ? 1 : .45)}
```

**Colour translation is mandatory.** The study draws with the live Dracula hex
values (`primary #ff79c6`, `secondary #bd93f9`, `tertiary #8be9fd`, `outline
#6272a4`). In QML every one of those becomes `Colours.primary` / `.secondary` /
`.tertiary` / `.outline`. `colour-lint` GATE-04 rejects a literal.

**`Colours.*` roles are `property string`, not colour-typed.** Never write
`Qt.rgba(Colours.surface.r, ...)` — a JS string has no `.r`, and it silently
resolves to black. Assign the role straight to `color:` and put any transparency
on the Item's own `opacity`, or route through a local
`readonly property color <name>: Colours.<role>` first.

**Verification tooling that exists on this host:** `grim` (REGION captures only —
`grim -g "x,y WxH"`; a full-screen capture SIGSEGVs the compositor into safe
mode on this NVIDIA host), `jq`, `python3` with PIL 12.3.0, `node` v24.18.0.
**`qml6` probe scripts are banned** — each opens a real window and a loop of them
takes the session down.

</measured_ground_truth>

<tasks>

<task type="auto">
  <name>Task 1: The style key, its migration, and the two derived predicates — no shape changes</name>
  <files>quickshell/.config/quickshell/modules/Prefs.qml, quickshell/.config/quickshell/shell.qml, quickshell/.config/quickshell/modules/launcher/Launcher.qml, quickshell/.config/quickshell/modules/Dashboard.qml, quickshell/.config/quickshell/modules/EdgeBar.qml, quickshell/.config/quickshell/modules/settings/pages/BarPage.qml, quickshell/.config/quickshell/modules/settings/RowIndex.qml</files>
  <read_first>quickshell/.config/quickshell/modules/Prefs.qml (whole file — the allow-list at 139-152, `_defaults` at 194-195, `getValue`/`setValue`'s typeof discipline at 248-296, `_loadPrefs` at 310-336, `_persist` at 338-347), shell.qml:52-61 and 440-450 and 577-616 and 840-848 and 1469-1489, Launcher.qml:60-115 and 470-500 and 590-600 and 640-750, Dashboard.qml:266-305 and 760-795, BarPage.qml:163-197, RowIndex.qml:1-45</read_first>
  <action>
Land the whole key-and-resolution layer in one commit, per Q2 and Q4. Nothing
visual changes: after this task all four non-off styles still render today's
shape, `off` unmounts exactly as it does today. That is deliberate — the tree is
never left broken, and the shapes land in Tasks 3-6.

**Prefs.qml.** Add `"edgeBar.style"` to `_allowedKeys` and `"edgeBar.style":
"continuous"` to `_defaults`. REMOVE `"edgeBar.enabled"` from both — it is a
retired key per Q2, and leaving it in the allow-list would let a stale writer
resurrect it. `"edgeBar.animatedBulge"` stays untouched in both.

Add a `_migrate()` function that runs from `_loadPrefs()` immediately after the
parsed object is installed into `_data` and before the function returns. Declare
it ABOVE `_loadPrefs` in the file — this singleton's own header records the
declare-before-construction-time-use trap and every helper in it is already
declared above `Component.onCompleted` for that reason. The migration: if the
loaded tree has a boolean at `edgeBar.enabled` AND no string at `edgeBar.style`,
write `edgeBar.style = enabled ? "continuous" : "off"` (Q4's exact mapping),
delete `edgeBar.enabled` from the tree, and call `_persist()` so the retired key
is gone from disk rather than lingering as junk. `_loadPrefs()` sets
`_loaded = true` on its first line, so `_persist()` is legal from inside it.
Use the same immutable-reassignment discipline the rest of the file uses —
`_data` is reassigned wholesale, never mutated in place, or QML's change
notification does not fire. Log one line naming the old and new value so the
migration is visible in `~/.cache/quickshell.log` rather than silent.

Note in a comment, because it will be asked: `watchChanges: false` means a LIVE
shell does not observe a hand-edit to `prefs.json`. The migration therefore runs
at shell start, and its effect is only observable after
`systemctl --user restart quickshell.service`.

**shell.qml — the single resolution point.** Replace the `edgeBarEnabled`
property at line 56 with four `readonly` properties, keeping
`edgeBarAnimatedBulge` beside them and adding no second `Prefs.getValue` call
anywhere in the tree:

- `_edgeBarStyleRaw` (string) — the one and only `Prefs.getValue("edgeBar.style")`.
- `edgeBarStyle` (string) — `_edgeBarStyleRaw` clamped against the five-value
  array `off`, `continuous`, `brackets`, `segmented`, `halo` via `indexOf`,
  falling back to `"continuous"` on no match.
- `edgeBarRailPresent` (bool) — `edgeBarStyle !== "off"`, written exactly that
  way and nothing else.
- `edgeBarPanelsAttach` (bool) — `edgeBarRailPresent && edgeBarStyle !== "brackets"`.

The `indexOf` clamp is the value allow-list Q2 asks for: `Prefs.getValue`'s
`typeof` guard catches a wrong TYPE but happily returns an unknown STRING, and
an unrecognised style must fall back to the default rather than render nothing.

`edgeBarRailPresent` is written EXACTLY as the brief specifies — `style !== "off"`,
the "is any rail present" predicate, not "is the style non-off" reasoned about
at a call site. `edgeBarPanelsAttach` is the second, genuinely different
predicate that Q3-brackets creates: rails present, panels do not weld.

Carry a comment on both saying, in one line each, what question they answer and
that they are not interchangeable. That comment is the artefact that stops a
future edit from collapsing them back into one.

**shell.qml — wiring.** Both EdgeBar LazyLoaders take `active: root.edgeBarRailPresent`
and gain `style: root.edgeBarStyle` (EdgeBar accepts it and ignores it this
task). Dashboard's threaded property becomes `edgeBarPanelsAttach:
root.edgeBarPanelsAttach`. Launcher gets BOTH: `edgeBarRailPresent:
root.edgeBarRailPresent` and `edgeBarPanelsAttach: root.edgeBarPanelsAttach`.
The dwell-hover `Connections` blocks are untouched.

**Launcher.qml — split the flag across the two predicates.** Delete
`property bool edgeBarEnabled` and declare `property bool edgeBarRailPresent: false`
and `property bool edgeBarPanelsAttach: false`. Then repoint each of the 18
sites by what it actually controls:

- DIRECTION -> `edgeBarRailPresent`: `panel.anchors.top` / `.anchors.bottom`
  (lines 482-483) and the `Translate.y` closed-offset sign (line 599).
- ATTACHMENT -> `edgeBarPanelsAttach`: `margins.top` (line 110), the four
  `background` corner radii (647-650), `launcherBorderClip`'s two anchor margins
  (692-693), `launcherGradientBorder`'s `y` and four radii (699-706), and both
  `AttachedCorner` blocks' `edge` and `y` (726-743).

Add the third state that Q3-brackets creates and that today's code has no branch
for — rail present, panels unattached. In that state the launcher rises from the
bottom (direction) but must open UNATTACHED, per the brief: "no flare, no weld,
no rim clipping". Concretely:
  - both `AttachedCorner` instances get `visible: launcherWindow.edgeBarPanelsAttach`
    (no flare),
  - all four `background` and `GradientBorder` radii resolve to
    `launcherWindow.cornerRadius` when `!edgeBarPanelsAttach` (a floating panel
    is round on all four corners — the square/round flip only means something
    against an edge it is welded to),
  - `launcherBorderClip`'s two anchor margins are both 0 and
    `launcherGradientBorder.y` is 0 when `!edgeBarPanelsAttach` (no rim clipping
    — the full ring is drawn),
  - `margins.bottom` becomes `launcherWindow.drawerTopMargin` when
    `edgeBarRailPresent && !edgeBarPanelsAttach`, so the unattached panel floats
    off the edge by the same 10 that the off-mode top drop uses, instead of
    sitting flush against a screen edge it is not welded to. `margins.top` stays
    `drawerTopMargin` only in the `!edgeBarRailPresent` case, unchanged.

Do not restructure the file, do not rename anything else, and do not add a
second `Prefs.getValue` anywhere — this document has no `root` id and both
values arrive as properties.

**Dashboard.qml.** Rename `property bool edgeBarEnabled` to
`edgeBarPanelsAttach` and repoint `margins.top` (line 302) to it. Add
`visible: dashboardWindow.edgeBarPanelsAttach` to both `AttachedCorner`
instances at 769 and 782. The dashboard has no direction branch — it is always
top — so `edgeBarRailPresent` is not threaded into it at all.

**EdgeBar.qml.** Add `property string style: "continuous"` next to the existing
`bottom`/`bulgeWidth`/`animatedBulge` properties, with a comment saying it is
inert until Task 3. Nothing else in this file changes yet.

**BarPage.qml.** Replace the "Edge bar" `ToggleRow` with a `SelectRow` — the
minimal working form, since leaving the row pointed at a key that no longer
exists WOULD be a broken tree. Match `SessionPage.qml:124-135`'s shape exactly:
`label: "Edge bar style"`, a one-line `subtext`, a `model` array of
`{ value, display }` objects, `currentValue: Prefs.getValue("edgeBar.style")`,
and `onSelected: (value) => Prefs.setValue("edgeBar.style", value)`. The `value`
field carries the raw stored string and `display` the operator-facing name.

Five entries, in the picker order the brief names — Continuous, Brackets,
Segmented, Halo, Off. No switch anywhere, and no greyed-out row: Q2 forbids a
reachable `enabled:false + style:halo` dead state, and this row's five values
are the whole state space. Leave the "Animate the bulge" `ToggleRow` alone; its
per-style visibility is Task 7.

**RowIndex.qml.** Add the two missing `pageIdx: 2` entries — `"Edge bar style"`
(section `"Edge bar"`) and `"Animate the bulge"` (same section), each with a
keyword list. `label` must be BYTE-FOR-BYTE the row's own `label:` string; that
is the jump key. This closes the pre-existing `settings-index-check` CHECK A
failure and every later task then runs the gate clean.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles; for g in quickshell-doctor colour-lint motion-lint keybind-doctor settings-index-check; do OUT=$(hypr/.config/hypr/scripts/$g 2>&amp;1); printf '%s %s\n' "$g" "$(printf '%s' "$OUT" | tail -1)"; done</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/shell.qml quickshell/.config/quickshell/modules/launcher/Launcher.qml quickshell/.config/quickshell/modules/Dashboard.qml quickshell/.config/quickshell/modules/settings/pages/BarPage.qml | grep -c 'edgeBarEnabled'); echo "edgeBarEnabled references: $N"; [ "$N" -eq 0 ] || echo 'FAIL: retired flag still referenced'</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/launcher/Launcher.qml | grep -Ec 'edgeBarStyle|"continuous"|"brackets"|"segmented"|"halo"'); echo "style-string reads in Launcher.qml: $N"; [ "$N" -eq 0 ] || echo 'FAIL: Launcher.qml reads the style string'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; systemctl --user restart quickshell.service &amp;&amp; sleep 4 &amp;&amp; jq -e '.edgeBar | (has("style") and (.style == "continuous") and (has("enabled") | not))' ~/.local/state/quickshell/prefs.json</automated>
  </verify>
  <done>`edgeBar.style` is the only stored edge-bar mode key; this host's `enabled: true` has migrated to `"continuous"` on disk with `enabled` deleted; `shell.qml` carries exactly one resolution point and two named predicates; `Launcher.qml` contains no style string anywhere; the picker in Settings offers five entries and writing any of them is accepted by `Prefs.setValue`; `settings-index-check` reads 118/0 and the other four gates hold their baselines.</done>
</task>

<task type="auto">
  <name>Task 2: Axis-transposed path builder — extract to edgebarpath.js, prove it byte-identical, then teach it vertical (HAZARD 1 + HAZARD 4)</name>
  <files>quickshell/.config/quickshell/modules/edgebarpath.js, quickshell/.config/quickshell/modules/EdgeBar.qml, hypr/.config/hypr/scripts/tests/edgebar-path-golden.json, hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs</files>
  <read_first>quickshell/.config/quickshell/modules/EdgeBar.qml:320-444 in full — `_arcCentre`'s DOMAIN comment, `_shoulderSweep`, and every line of `_outlinePath` including the `Y()` mirror and the two-quarter-arc pill caps; quickshell/.config/quickshell/modules/launcher/fuzzy.js:1-20 for this repo's `.pragma library` JS-module idiom and why it needs no qmldir entry</read_first>
  <action>
This is hazard 1 and hazard 4, alone, in their own commit. The brief says
plainly: *"That is not a parameter flip — it is either a second path builder or
a transposed coordinate helper. Budget for it."* Do not fold any shape work into
this task.

**Step A — verbatim extraction, zero behaviour change.** Create
`modules/edgebarpath.js` with `.pragma library` on line 1, exactly as
`launcher/fuzzy.js` does (a `.js` module needs no `qmldir` entry — only `.qml`
types do). Move `_arcCentre`, `_shoulderSweep` and the whole body of
`_outlinePath` into it as pure functions taking every input as an explicit
numeric argument: `buildOutline({ t, b, re, f, rc, along, xl, xr, surfaceDepth,
flip })`. No `Design.*`, no `Colours.*`, no QML identifier of any kind may appear
in this file — that is what makes it testable under `node` and what keeps
`colour-lint` structurally satisfied.

Carry `_arcCentre`'s entire DOMAIN comment across word for word. It is the only
warning that exists for hazard 4: the function hardcodes SVG F.6.5's offset
scalar to 1, which is exact for a 90-degree arc and wrong for every other sweep
angle, and a wrong sweep flag produces a shape that looks deliberate and is
wrong with no QML error and no gate. **Every arc this task and Tasks 3-6 emit
must be a quarter circle.** If a non-quarter arc ever becomes genuinely
necessary, the comment's own instruction applies: add the scalar AND re-verify
all four existing arcs, in that order — not the reverse.

Point `EdgeBar.qml`'s `_outlinePath` at it via `import "edgebarpath.js" as
EdgeBarPath` and a single call. After this step the file has exactly one path
implementation.

**Step B — pin it.** Write `hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs`,
a plain `node` script. It reads `edgebarpath.js` off disk, strips the leading
`.pragma`/`.import` directive lines (node cannot parse them), evaluates the
remainder, and asserts:

1. **Golden, horizontal.** `buildOutline` with today's live parameter set
   (`t: 6, re: 3, f: 3, rc: 1, b: 4` static and `b: 10` swelled; `along: 1850`;
   `xl/xr` from `bulgeWidth: 760` and `640`; `surfaceDepth: 16`; `flip` both
   ways) produces strings character-identical to
   `hypr/.config/hypr/scripts/tests/edgebar-path-golden.json`. Generate that
   golden from the Step-A extraction — BEFORE Step C touches anything — and
   commit it. It is the only detector that exists for a sweep-flag regression.
2. **Sweep flags are resolved, not guessed.** Every `A` command in every emitted
   path carries a sweep flag of exactly `0` or `1`, no `NaN` and no `undefined`
   anywhere in the string.
3. **The fillet invariant holds at EVERY frame, not just the endpoints**
   (hazard 3). Sweep `b` across `0 -> 10` in 0.5 steps in animated mode and
   assert `f + rc <= b` at each value. Round 10 measured what breaking it looks
   like: a dark notch cutting into the strip from the outline self-intersecting,
   with nothing warning.

**Step C — the transposition.** Add an `axis` argument (`"horizontal"` |
`"vertical"`). Build the path in ALONG/DEPTH space and emit every coordinate
through one helper — `P(a, d)` returns `a + " " + d` for horizontal and
`d + " " + a` for vertical. `_shoulderSweep`'s expected-centre arguments go
through the SAME helper, so the flag keeps being resolved by matching the centre
the geometry demands rather than hand-derived. This matters more than it looks:
transposing swaps handedness, so every sweep flag flips — and because the
expected centre is transposed too, `_shoulderSweep` flips it for you. That is
precisely why the existing verify-the-centre mechanism is kept rather than
replaced with a lookup table.

Then extend the test:

4. **Golden unchanged.** Every horizontal assertion from Step B still passes
   character-for-character against the same committed golden. If it does not,
   the transposition changed horizontal output and is wrong.
5. **Vertical is the transpose.** For the same inputs, the vertical path is the
   horizontal path with every coordinate pair swapped — assert it by parsing
   both into coordinate lists and comparing element-wise, not by string
   manipulation of the whole path.

Wire the script so `node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs`
exits non-zero on any failure and prints one line per assertion.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/edgebarpath.js | grep -Ec 'Design\.|Colours\.|Motion\.'); echo "QML singleton reads in edgebarpath.js: $N"; [ "$N" -eq 0 ] || echo 'FAIL: QML singleton leaked into the pure JS module'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for g in quickshell-doctor colour-lint motion-lint keybind-doctor settings-index-check; do hypr/.config/hypr/scripts/$g 2>&amp;1 | tail -1; done</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; systemctl --user restart quickshell.service &amp;&amp; sleep 4 &amp;&amp; awk '/Starting quickshell/{n=NR} END{print n}' ~/.cache/quickshell.log &gt;/dev/null; tail -80 ~/.cache/quickshell.log | grep -Ei 'edgebarpath|is not a function|Unable to assign|TypeError' &amp;&amp; echo 'FAIL: import or reference error' || echo 'PASS: no QML import error'</automated>
  </verify>
  <done>`edgebarpath.js` is the single path implementation, contains no QML identifier, and draws in along/depth space through one transposition helper; the node test passes all five assertion groups including the character-identical horizontal golden; the live shell restarts with the strips looking exactly as they did before this task and no import or reference error below the last start marker in `~/.cache/quickshell.log`.</done>
</task>

<task type="auto">
  <name>Task 3: Continuous — rails run into the bar and stop, and the bar becomes the fourth side (REVERSES D-2)</name>
  <files>quickshell/.config/quickshell/modules/EdgeBar.qml, quickshell/.config/quickshell/modules/Bar.qml, quickshell/.config/quickshell/modules/dashboard/Design.qml, quickshell/.config/quickshell/shell.qml</files>
  <read_first>quickshell/.config/quickshell/modules/Bar.qml:1-135 (the reservation header and the `margins`/`implicit*`/`exclusiveZone` block — it records the +6 double-count bug twice, read it before changing any margin) and 338-345 (`barContent`); shell.qml:555-575 (the Bar instance and its threaded properties); Design.qml:299-310 (`barColumnWidth`) and 137-160 (`barHeight`/`barEdgeMargin`/`barSideMargin`)</read_first>
  <action>
Continuous is the DEFAULT (Q4), so this fires on first install. It is also the
D-2 reversal, and the brief is explicit that it must be recorded as a deliberate
reversal the way D-3's was in round 11 — **not written up as a mid-execution
deviation.** Put the reversal note in `Bar.qml`'s own header AND in the SUMMARY.

**The construction, and why it is this one and not the obvious one.** The study
draws the rails running from `INSET` to `BAR.x + BAR.w/2` — past the bar's left
edge, to its centre line. The strip surface cannot get there. It is anchored
left+right with `exclusiveZone: 6`, and layer-shell positions any surface with a
non-negative exclusive zone *inside* every other surface's zone, so the bar's 50
pins the strip's right end at 1860 no matter what margin is set. Setting
`exclusiveZone: -1` to escape would also surrender the strip's own 6px
reservation — one surface cannot do both.

So the strips stop where they already stop, and **the bar paints the bridge.**
That is what "the bar and the rail must share a shape" means mechanically, and
it is the whole content of the D-2 reversal.

**EdgeBar.qml.** Continuous's top/bottom outline is today's outline unchanged —
the round 7-11 vocabulary carries forward verbatim per Q1's "what is NOT
discarded" list, and Q3-continuous requires byte-identical behaviour to round 11
because the operator judged that live and approved it. Branch nothing here
beyond routing `style === "continuous"` to the existing call. Confirm by
re-running Task 2's golden after the branch is in.

**Bar.qml — the reversal.** Add `property string edgeBarStyle: "off"`, threaded
from `root.edgeBarStyle` at the Bar instance in `shell.qml`, and a derived
`readonly property bool _continuousWeld: barWindow.edgeBarStyle === "continuous"
&& barWindow.vertical`. The `&& vertical` is load-bearing: Continuous is drawn
against a right-edge vertical bar, and this shell's bar can be flipped
horizontal from Settings. In horizontal orientation `_continuousWeld` is false
and the bar renders exactly as it does today — the strips simply end at their
own right edge, no weld, no error.

When `_continuousWeld`:

- `margins.top` and `margins.bottom` go to 0 so the slab spans the full screen
  height, per the study's `rect{y:0, h:H}`. **Neither is the anchored edge** —
  the vertical bar anchors right — so `margins.<anchored-edge> + exclusiveZone`
  is untouched and the reservation cannot move. Say so in the comment; that file
  has shipped this bug twice.
- The surface widens LEFTWARD by `Design.edgeBarSideMargin + Design.barColumnWidth / 2`
  (10 + 22 = 32): `implicitWidth` becomes `Design.barColumnWidth + 32`, with
  `margins.right` and `exclusiveZone` both unchanged. A surface wider than its
  exclusive zone overhangs into the client area — exactly the idiom the strip's
  own bulge already uses, and `reserved` must still measure `[0, 6, 50, 6]`
  afterwards. **Measure it; do not assert it.**
- Wrap `barContent` so nothing about capsule layout moves: give it
  `anchors.right: parent.right` and `width: Design.barColumnWidth` instead of
  `anchors.fill: parent`. Every existing explicit x/y inside it then resolves
  against the same 44px box it does today. Do not touch the zone positioning
  logic — that file's own comment records what conditional anchors cost there.
- Paint, BEHIND `barContent`, three things, all reading the same three accent
  roles the strip's gradient uses so the two genuinely share one colour language:
  1. the full-height slab — a rounded rect at surface-local `x = 32`,
     `y = 0`, `width = Design.barColumnWidth`, `height = parent.height`,
     `radius = Design.barColumnWidth / 2`, filled with a VERTICAL
     `LinearGradient` of `Colours.primary -> .secondary -> .tertiary ->
     .primary`, `RepeatSpread`, scrolled on the same `_gradientPhase` idiom and
     the same `Motion.borderRotateDuration` period `EdgeBar.qml` uses. Reuse
     that idiom; do not invent a second one and do not introduce a new
     `Motion.*` token name (`motion-lint` CHECK A derives its allow-list from
     `motion.json`'s semantic keys plus `<camelKey>ReverseEasing`, and anything
     else reports dangling).
  2. the inset core — a rounded rect inset by 4 horizontally and 22 vertically
     (`x = 36, y = 22, width = barColumnWidth - 8, height = parent.height - 44,
     radius = (barColumnWidth - 8) / 2`), `color: Colours.surface`. Assign the
     role straight to `color:`; if it needs to be less than opaque use the
     Item's own `opacity`. **Never** `Qt.rgba(Colours.surface.r, ...)` — the
     roles are `property string` and that silently evaluates to black.
  3. two horizontal weld stubs at surface-local `y = 0` and
     `y = parent.height - Design.edgeBarThickness`, each
     `x = 0`, `width = 32`, `height = Design.edgeBarThickness`, filled with the
     same accent gradient on the horizontal axis. These are the 1860 -> 1892 run
     that closes the silhouette: strip end -> stub -> slab. They tuck under the
     slab's own rounded cap, which is exactly how the study's overlap welds.

Add `Design.qml` tokens only where a real constant is needed, each with a
one-line comment in the house style saying where the number comes from (the
study). Do not add a token for a value that is already derivable from
`barColumnWidth` or `edgeBarSideMargin`.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; qs ipc call prefs set edgeBar.style continuous 2>/dev/null; systemctl --user restart quickshell.service &amp;&amp; sleep 5 &amp;&amp; hyprctl -j monitors | jq -e '.[0].reserved == [0,6,50,6]'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; W=$(hyprctl -j monitors | jq -r '.[0].width') &amp;&amp; grim -g "$((W-120)),0 120x40" /tmp/claude-1000/-home-aorus-dotfiles/weld.png &amp;&amp; python3 -c "
from PIL import Image
im=Image.open('/tmp/claude-1000/-home-aorus-dotfiles/weld.png').convert('RGB')
w,h=im.size
for y in (1,3,5):
    print('row',y,[im.getpixel((x,y)) for x in range(0,w,6)])
"</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for g in quickshell-doctor colour-lint motion-lint keybind-doctor settings-index-check; do hypr/.config/hypr/scripts/$g 2>&amp;1 | tail -1; done</automated>
  </verify>
  <done>With Continuous selected the strip's gradient runs unbroken from the strip's right end through the weld stub into the bar's slab — read as raw RGB per column in the top 6 rows, with no background-coloured column between the strip's end and the slab; the slab spans the full screen height with a `Colours.surface` core; every bar capsule sits exactly where it sat before; `hyprctl -j monitors` still reports `reserved == [0,6,50,6]`; flipping the bar to horizontal renders today's bar unchanged; all five gates hold and Task 2's golden still passes.</done>
</task>

<task type="auto">
  <name>Task 4: Halo — four edges at 2px, left and right instances, and the static-bulge meaning of the toggle OFF</name>
  <files>quickshell/.config/quickshell/modules/EdgeBar.qml, quickshell/.config/quickshell/shell.qml, quickshell/.config/quickshell/modules/dashboard/Design.qml</files>
  <read_first>quickshell/.config/quickshell/modules/EdgeBar.qml:35-44 (the RESERVATION header) and 121-160 (surface depth vs painted depth, and why the surface is pinned to the maximum) and 269-318 (`_bulgeOut`, `_bTarget`, `_b`, the `Behavior`, and the animated-mode 0.6/0.4 fillet split); hypr/.config/hypr/scripts/quickshell-doctor:415-447 (the registry row and why `-left`/`-right` need no new row)</read_first>
  <action>
Halo is the first style that needs Task 2's vertical axis. Four edges, thickness
2 (NOT `T`), `rx = 1`, per the study.

**EdgeBar.qml — generalise the edge.** Replace `property bool bottom` with
`property string edge: "top"` taking `"top" | "bottom" | "left" | "right"`, and
derive from it: `_vertical` (`edge === "left" || edge === "right"`) and `_flip`
(`edge === "bottom" || edge === "right"`, the mirror-about-surface-depth case
the existing `Y()` already implements). Update the namespace binding to
`"quickshell-baredge-" + edgeBarWindow.edge` — it must stay exactly ONE
`WlrLayershell.namespace` binding in the file or the registry's forward check
counts wrong. The four namespaces all pattern-match the single existing
`quickshell-baredge-` row; **do not add a second row** — two matching rows fail
the live cardinality check by construction.

Route the anchors, `margins`, `implicitWidth`/`implicitHeight` and
`exclusiveZone` off `_vertical` rather than off `bottom`. A vertical instance
anchors top+bottom plus its own side, carries `margins.top/bottom =
Design.edgeBarSideMargin`, sets `implicitWidth = _surfaceDepth` and
`implicitHeight = 0` (the zero-is-inert idiom for a doubly-anchored axis, the
mirror of what the horizontal instances already do).

**Per-style thickness and bulge depth.** Derive all three off `style` in place,
not from a second copy of the tokens: `_t` selects
`Design.edgeBarHaloThickness` on Halo and `Design.edgeBarThickness` otherwise;
`_re` selects `Design.edgeBarHaloEndRadius` on Halo and
`Design.edgeBarEndRadius` otherwise; and a new `_staticBulgeExtra` selects
`Design.edgeBarBulgeSwellExtra` on Halo and `Design.edgeBarBulgeExtra`
otherwise.

`_staticBulgeExtra` replaces the bare `Design.edgeBarBulgeExtra` in `_bTarget`'s
static branch. **This is the whole of Q3-halo's warning and it is easy to get
wrong:** `animatedBulge: false` on Halo means the STATIC PERMANENT BULGE at full
depth — a 2px hairline frame plus two fixed 12px masses, a motionless landmark
and a legitimate state. It does not mean "no bulge". Swell is `2 -> 12`, so the
static extra is `edgeBarBulgeSwellExtra` (10), not `edgeBarBulgeExtra` (4).
Check the fillet invariant at both ends: static Halo is `f(3) + rc(1) = 4 <= 10`,
and animated mode's 0.6/0.4 derivation sums to `_b` at every value including 0.

Only the top and bottom instances carry a bulge. The left and right rails are
plain runs — `xl`/`xr` collapse to no excursion — because nothing attaches on
those edges (the measured attachment map: top = dashboard, bottom = launcher,
right = the bar, left = nothing).

**Surface depth stays pinned to the maximum** (hazard 2). Halo's
`_paintedDepth = 2 + 10 = 12` and `_surfaceDepth = max(12, edgeBarHoverDepth=16)
= 16`, the same as today. Nothing about the layer surface may change on any
frame of the swell — a resizing layer surface is re-configured and re-buffered
every frame and drags its own content, which cost round 5 an entire round.

**shell.qml — mount the vertical pair.** Add two more `LazyLoader` blocks, `left`
and `right`, each `active: root.edgeBarStyle === "halo" || root.edgeBarStyle ===
"brackets"` (Brackets in Task 5 needs the same pair). Per-instance loaders, never
a shared wrapper Item — R3's original reasoning still applies: a
mounted-but-`visible:false` layer surface keeps its `exclusiveZone`. Rename the
two existing loaders' `bottom: true/false` to `edge: "top"/"bottom"`.

**Reservation — DC-2, decided, and then measured.** Left rail: `margins.left: 0`,
`exclusiveZone: Design.edgeBarHaloThickness` -> it lands at `x = 0..2`, matching
the study exactly. Right rail: `margins.right: Design.edgeBarSideMargin`,
`exclusiveZone: 0` -> it lands at the study's `RIGHT_EDGE` and overhangs rather
than reserving, exactly as the bulge already does. That trade is DC-2 above: the
right rail cannot both sit 10px inside the bar's reserved boundary and reserve
only 2px, because reservation IS `margins.<anchored-edge> + exclusiveZone`. Top
and bottom keep `exclusiveZone: _t` (2 on Halo). **Measure the resulting vector
with `hyprctl -j monitors` and record the real numbers in the SUMMARY** — do not
state the study table's estimate as though it were the outcome.

The corners deliberately do NOT close: the study's top/bottom runs span
`INSET -> RIGHT_EDGE` while the verticals span `INSET -> H-INSET` at `x = 0` and
`x = RIGHT_EDGE`, leaving a 10px break on both axes. That gap is the drawn,
approved shape. Do not "fix" it.

Add `edgeBarHaloThickness: 2` and `edgeBarHaloEndRadius: 1` to `Design.qml` with
the usual provenance comment. Both are off the repo's 4px grid; say so and say
why (the study's own measured values — a hairline is not a spacing token).
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; qs ipc call prefs set edgeBar.style halo &amp;&amp; systemctl --user restart quickshell.service &amp;&amp; sleep 5 &amp;&amp; hyprctl -j layers | jq -r '..|objects|select(.namespace? // "" | startswith("quickshell-baredge-")) | .namespace' | sort | tr '\n' ' '</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; hyprctl -j monitors | jq -c '.[0] | {name, width, height, reserved}'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; H=$(hyprctl -j monitors | jq -r '.[0].height') &amp;&amp; grim -g "0,$((H/2 - 20)) 24x40" /tmp/claude-1000/-home-aorus-dotfiles/halo-left.png &amp;&amp; python3 -c "
from PIL import Image
im=Image.open('/tmp/claude-1000/-home-aorus-dotfiles/halo-left.png').convert('RGB')
w,h=im.size
y=h//2
print('raw RGB per column, left edge:', [(x, im.getpixel((x,y))) for x in range(w)])
"</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for g in quickshell-doctor colour-lint motion-lint keybind-doctor settings-index-check; do hypr/.config/hypr/scripts/$g 2>&amp;1 | tail -1; done</automated>
  </verify>
  <done>With Halo selected, `hyprctl -j layers` lists exactly four `quickshell-baredge-*` surfaces (top, bottom, left, right); the raw per-column RGB dump at the screen's left edge shows the accent colour occupying columns 0-1 and background from column 2 on — a real 2px run, read off raw pixels rather than a saturation or luminance threshold; the measured `reserved` vector is recorded verbatim in the SUMMARY alongside DC-2's reasoning; `edgeBar.animatedBulge` OFF leaves a permanent 12px mass at the top and bottom centres rather than a flat frame; all five gates hold and Task 2's golden still passes.</done>
</task>

<task type="auto">
  <name>Task 5: Brackets — four corner L-pieces, zero reservation, no bulge, panels unattached</name>
  <files>quickshell/.config/quickshell/modules/EdgeBar.qml, quickshell/.config/quickshell/modules/dashboard/Design.qml, quickshell/.config/quickshell/shell.qml</files>
  <read_first>quickshell/.config/quickshell/modules/EdgeBar.qml:493-581 (the bulge hit area, the HoverHandler, `_bulgeArmed`, the dwell Timer, `bulgeHoverTriggered`, and the `mask: Region`) — the hit region and the mask move together on this style, and the file's own click-inert posture must survive; quickshell/.config/quickshell/modules/edgebarpath.js as written in Task 2</read_first>
  <action>
Four L-pieces, one per corner. Each L is one horizontal arm on the top/bottom
surface plus one vertical arm on the left/right surface — the same four
instances Task 4 already mounts, so no new surface is introduced.

**Arm length is proportional, never 170.** The study measured 170 at a 1920-wide
capture; this panel is 2560x1440. Add
`Design.edgeBarBracketArmFraction: 170 / 1920` (≈ 0.0885) and derive each arm as
`Math.round(alongExtent * Design.edgeBarBracketArmFraction)` off the surface's
own along-axis extent. The brief says it in as many words: *"derive
proportionally, do not hardcode 170."*

**HAZARD 4 IS DISARMED BY CONSTRUCTION HERE — keep it that way.** The study draws
each arm as a rect with `rx = k/2`, i.e. a pill-capped bar, which is exactly the
shape `edgebarpath.js` already emits as its two-quarter-arc cap. Build every arm
from that same cap construction. **Add no new arc of any other sweep angle.**
`_arcCentre` hardcodes SVG F.6.5's offset scalar to 1, which is exact only for a
quarter circle; a 180-degree cap resolved through it is precisely the round-10
bug where both candidate centres missed, `_shoulderSweep` fell through to its
`: 0` branch, and the caps curved inward — biting 4px off the left end and 6px
off the right, with no QML error and no gate saying anything. The corner joints
are where it will try again.

**No bulge at all** (Q3-brackets). On this style `_bTarget` is 0 in both
animated and static modes and no bulge excursion is emitted. The study's own
table scores Brackets `0 direct` rails-with-a-panel; that is a property of the
shape, not a gap in the drawing. Two alternatives were offered to the operator
and declined — grafting a centre bulge on, and growing the arms until they meet
(which would make Brackets a resting state of Continuous rather than its own
shape). Do not reintroduce either.

**Reservation 0 on all four surfaces.** `exclusiveZone: 0` when
`style === "brackets"`; the corners overhang entirely, which is the shape's whole
cost advantage. The registry row is `reserve`, so the gate's `exclusiveZone: 0`
assertion (which only applies to `noreserve` rows) does not fire either way.
Positions: horizontal arms start at `Design.edgeBarSideMargin` from each side
(`margins.left/right` unchanged at 10); vertical arms sit at the same inset —
left surface `margins.left: 0` with the arm painted at depth
`edgeBarSideMargin .. edgeBarSideMargin + T`, right surface
`margins.right: Design.edgeBarSideMargin` with the arm at depth `0 .. T`. That
reproduces the study's `corner(INSET,0,1,1)` / `corner(RIGHT_EDGE,0,-1,1)`
exactly.

**Hover: the arms extend toward the centre** (Q3-brackets' replacement landmark),
and this is free of hazard 2 by construction — the arms grow ALONG the edge, not
into it, so no surface dimension changes on any frame. Add
`Design.edgeBarBracketArmHoverExtra` as a fraction of the base arm length
(0.4 is the recommended value; it is a visual constant, tune it at the operator
checkpoint if asked) and animate the arm length on the hovered surface with the
SAME reversal pair the bulge already uses — `Motion.standardDuration` with
`Motion.standardEasing` growing and `Motion.standardReverseEasing` retracting.
Introduce no new `Motion.*` token name: CHECK A derives its allow-list from
`motion.json`'s semantic keys plus `<camelKey>ReverseEasing` and reports
anything else dangling.

**DC-1 — the dwell still summons.** Replace the single centre `bulgeHitArea`
with two corner hit areas on this style, each `armLength` along the edge by
`Design.edgeBarHoverDepth` deep, and point `mask: Region` at both (a `Region`
with two child items, not a second mask). The HoverHandler / `_bulgeArmed` /
dwell-Timer / `bulgeHoverTriggered()` chain is reused verbatim — including the
re-arm-only-on-a-genuine-exit rule — so the top surface still opens the dashboard
and the bottom still opens the launcher's menu mode. What changes is only WHERE
you aim. Keep the file click-inert: no `TapHandler`, `MouseArea`, `WheelHandler`
or `DragHandler` may appear anywhere in `EdgeBar.qml`, permanently.

The panels those hovers summon open UNATTACHED — no flare, no weld, no rim
clipping — and that is already wired: `edgeBarPanelsAttach` is false for
`"brackets"` from Task 1, so `Launcher.qml` and `Dashboard.qml` need no further
change here. Confirm it live rather than assuming it.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; qs ipc call prefs set edgeBar.style brackets &amp;&amp; systemctl --user restart quickshell.service &amp;&amp; sleep 5 &amp;&amp; hyprctl -j monitors | jq -e '.[0].reserved == [0,0,50,0]'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; grim -g "0,0 320x24" /tmp/claude-1000/-home-aorus-dotfiles/brk-tl.png &amp;&amp; python3 -c "
from PIL import Image
im=Image.open('/tmp/claude-1000/-home-aorus-dotfiles/brk-tl.png').convert('RGB')
w,h=im.size
row=[im.getpixel((x,2)) for x in range(w)]
print('top-left horizontal arm, raw RGB per column:')
for x in range(0,w,4): print(x,row[x])
"</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/EdgeBar.qml | grep -Ec 'TapHandler|MouseArea|WheelHandler|DragHandler'); echo "press-accepting items in EdgeBar.qml: $N"; [ "$N" -eq 0 ] || echo 'FAIL: press-accepting item introduced'</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/dashboard/Design.qml | grep -c '170'); echo "bare 170 occurrences in Design.qml (comments stripped): $N — must be 1, inside the arm-fraction expression, never a length"; sed -e 's://.*::' quickshell/.config/quickshell/modules/dashboard/Design.qml | grep -n '170'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for g in quickshell-doctor colour-lint motion-lint keybind-doctor settings-index-check; do hypr/.config/hypr/scripts/$g 2>&amp;1 | tail -1; done</automated>
  </verify>
  <done>With Brackets selected, `reserved` measures `[0,0,50,0]` — the corners cost nothing; the raw per-column RGB dump across the top-left region shows the accent colour running from the side inset for a proportionally-derived arm length and then background, with pill caps at both ends and no inward-curving notch; the arm length scales with screen width rather than being pinned to 170; no bulge is painted anywhere; `EdgeBar.qml` still contains no press-accepting item; dwelling a corner still summons its panel, and the panel opens with four rounded corners, no flares and an unclipped rim; all five gates hold.</done>
</task>

<task type="auto">
  <name>Task 6: Segmented — ten workspace segments on the top rail, and the bulge merges the ones it covers</name>
  <files>quickshell/.config/quickshell/modules/EdgeBar.qml, quickshell/.config/quickshell/modules/dashboard/Design.qml</files>
  <read_first>quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml:360-375 and 525-540 — how this shell already reads `Hyprland.workspaces.values` and `Hyprland.focusedWorkspace.id`; reuse that binding shape rather than inventing a second workspace source</read_first>
  <action>
Segmented affects the TOP rail only. The bottom stays an ordinary rail, drawn by
the same call Continuous uses — the study's own `draw()` does exactly that
(`railH(H, INSET, RIGHT_EDGE, "bottom", BOTW, d.bot)` unchanged).

**Segment geometry, from the study verbatim:**
`n = 10`, `gap = 8`, `total = alongExtent`, `seg = (total - gap*(n-1)) / n`,
segment `i` at `x = i*(seg+gap)`, width `seg`, height `T`, pill-capped
(`rx = T/2` — same cap construction, no new arc, hazard 4 unchanged).
Add `edgeBarSegmentCount: 10`, `edgeBarSegmentGap: 8` and
`edgeBarSegmentInactiveOpacity: 0.45` to `Design.qml` with provenance comments.

**Colour.** Active segment: the scrolling accent gradient at full opacity —
`Colours.primary/.secondary/.tertiary`, the identical `LinearGradient` +
`RepeatSpread` + `_gradientPhase` instance the flat run already uses, so the
active segment reads as a window onto the same continuous flow rather than a
separately-animating object. Inactive: `Colours.outline` at
`edgeBarSegmentInactiveOpacity`. The study's `#6272a4` is Dracula's `outline`
role — translate it, never write the literal.

**Which segment is active.** `Hyprland.focusedWorkspace.id`, mapped to index
`id - 1` and clamped to `0 .. n-1`, guarded exactly the way
`WorkspaceCapsule.qml:533` guards it (`!!(Hyprland.focusedWorkspace && ...)`) so
a null focused workspace during a monitor event lights nothing rather than
throwing. A workspace id above 10 or a special workspace simply lights no
segment — that is correct, not a gap.

**The merge — Q3-segmented, and the reason matters more than the mechanism.**
When the bulge has any depth, the segments it covers must MERGE into the
bulge's own continuous silhouette; segments outside stay separate. The brief's
reason: *"an attachment root cannot have gaps in it, or the `AttachedCorner`
flare has nothing to weld to and the one-silhouette effect breaks at exactly the
joint it exists to hide."*

Implement it as a span union, not as a per-segment opacity trick:

1. Compute the bulge span `[xl, xr]` as the flat-run builder already does.
2. Find every segment whose `[x, x+seg]` intersects that span. Extend the merged
   span to include those segments WHOLE — `mergedStart = min(xl, first
   intersecting segment's start)`, `mergedEnd = max(xr, last intersecting
   segment's end)` — so no segment is ever cut in half at the bulge boundary.
3. Emit ONE continuous silhouette across `[mergedStart, mergedEnd]` using the
   existing flat-run-plus-bulge builder, with the flat run's own fillets and
   pill caps at the merged ends.
4. Emit only the segments lying entirely outside the merged span, unchanged.

At rest with `animatedBulge` ON the bulge depth is 0, so nothing merges and all
ten segments are separate. With `animatedBulge` OFF the static bulge is
permanent, so the merge is permanent. Both are correct; do not special-case
either. At the live scale the 760px bulge covers roughly four segments, so the
merged run is a visibly solid block in the middle with separate segments to
either side — that is the intended reading.

Everything else on this style — flat run `T = 6`, side inset 10, convex pill
caps, `edgeBarHoverDepth = 16`, swell 10, the static bulge shape (extra 4,
fillet 3, corner 1), the reversed-entrance dismiss — is inherited unchanged from
the round 7-11 vocabulary per Q1's "what is NOT discarded with it" list. The
hover hit area stays the centre bulge region exactly as on Continuous.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; qs ipc call prefs set edgeBar.animatedBulge true &amp;&amp; qs ipc call prefs set edgeBar.style segmented &amp;&amp; systemctl --user restart quickshell.service &amp;&amp; sleep 5 &amp;&amp; W=$(hyprctl -j monitors | jq -r '.[0].width') &amp;&amp; grim -g "0,0 ${W}x8" /tmp/claude-1000/-home-aorus-dotfiles/seg-rest.png &amp;&amp; python3 -c "
from PIL import Image
im=Image.open('/tmp/claude-1000/-home-aorus-dotfiles/seg-rest.png').convert('RGB')
w,h=im.size
row=[im.getpixel((x,2)) for x in range(w)]
bg=row[0]
runs=[];cur=None
for x,p in enumerate(row):
    on = p != bg
    if cur is None or cur[0]!=on: runs.append([on,x,x]); cur=runs[-1]
    else: cur[2]=x
print('painted runs (on,start,end):', [r for r in runs if r[0]])
print('run count:', len([r for r in runs if r[0]]))
"</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; qs ipc call prefs set edgeBar.animatedBulge false &amp;&amp; systemctl --user restart quickshell.service &amp;&amp; sleep 5 &amp;&amp; W=$(hyprctl -j monitors | jq -r '.[0].width') &amp;&amp; grim -g "0,0 ${W}x8" /tmp/claude-1000/-home-aorus-dotfiles/seg-static.png &amp;&amp; python3 -c "
from PIL import Image
im=Image.open('/tmp/claude-1000/-home-aorus-dotfiles/seg-static.png').convert('RGB')
w,h=im.size
row=[im.getpixel((x,2)) for x in range(w)]
bg=row[0]
runs=[];cur=None
for x,p in enumerate(row):
    on = p != bg
    if cur is None or cur[0]!=on: runs.append([on,x,x]); cur=runs[-1]
    else: cur[2]=x
on=[r for r in runs if r[0]]
print('merged-state painted runs:', on)
print('longest run width:', max(r[2]-r[1] for r in on))
"; qs ipc call prefs set edgeBar.animatedBulge true</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/EdgeBar.qml | grep -c 'Hyprland.focusedWorkspace'); echo "focusedWorkspace bindings: $N"; [ "$N" -ge 1 ] || echo 'FAIL: no workspace binding'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for g in quickshell-doctor colour-lint motion-lint keybind-doctor settings-index-check; do hypr/.config/hypr/scripts/$g 2>&amp;1 | tail -1; done</automated>
  </verify>
  <done>At rest with the animated bulge ON, the raw per-column scan of the top rail counts ten separate painted runs with nine background gaps between them; with the bulge static the same scan shows fewer runs and one long merged run spanning at least the bulge width in the middle, with whole segments either side — proving the merge unions whole segments rather than cutting one in half; the active segment tracks `Hyprland.focusedWorkspace` and the inactive ones paint `Colours.outline`; the bottom rail is unchanged; all five gates hold and Task 2's golden still passes.</done>
</task>

<task type="auto">
  <name>Task 7: Settings picker completion — per-style bulge-toggle visibility and honest subtexts</name>
  <files>quickshell/.config/quickshell/modules/settings/pages/BarPage.qml, quickshell/.config/quickshell/modules/settings/RowIndex.qml</files>
  <read_first>quickshell/.config/quickshell/modules/settings/pages/BarPage.qml as it stands after Task 1; quickshell/.config/quickshell/modules/settings/common/SelectRow.qml:14-45 (the `model` / `currentValue` / `selected` contract)</read_first>
  <action>
The shapes exist; this finishes the picker. Q3 gives a per-style answer for
`edgeBar.animatedBulge` and only one of the four hides the row.

**Hide the "Animate the bulge" row when the bulge does not exist.** Add
`visible: Prefs.getValue("edgeBar.style") !== "brackets"` to the `ToggleRow`.
Brackets draws no bulge, so there is nothing to animate and a live-looking row
that does nothing is worse than an absent one. Keep it VISIBLE — not disabled —
for `"off"`: the row's existing comment already records the right reasoning
(`ToggleRow` has no styled disabled state anywhere in this shell, so
`enabled: false` blocks input while looking identical, i.e. reads as broken), and
with the edge bar off the pref simply has no effect until it is switched back on.
Leave the row's `checked`/`onToggled` wiring untouched. The row DECLARATION stays
in the file either way, so `settings-index-check`'s static scan still counts 21
rows against 21 `RowIndex` entries.

**Write the subtexts so they describe the shapes, not the code.** One sentence
per entry in the `SelectRow` subtext, in plain English, naming what the operator
will actually see. Use the study's own language rather than plan vocabulary —
this repo's convention is that dotfile-facing text stays free of plan IDs and
workflow jargon:

- Continuous — the rails run into the bar and stop, so the frame and the bar
  read as one shape.
- Brackets — four short corner pieces, nothing in between; costs no screen space
  and panels open free-floating.
- Segmented — the top rail breaks into one piece per workspace with the current
  one lit.
- Halo — a 2px outline on all four edges, with the colour doing the work.
- Off — no frame; the launcher drops from the top as it did before.

Update the "Animate the bulge" subtext so it is true on every style it is
visible for — it must cover Halo, where switching it OFF leaves a permanent
mass rather than nothing.

**RowIndex.qml.** Confirm the two entries added in Task 1 still match the labels
byte-for-byte after any wording change, and extend their keyword lists with the
five style names so searching "halo" or "brackets" in Settings finds the row.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles; OUT=$(hypr/.config/hypr/scripts/settings-index-check 2>&amp;1); printf '%s\n' "$OUT" | tail -1; printf '%s\n' "$OUT" | grep FAIL || true</automated>
    <automated>cd /home/aorus/dotfiles; SRC=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/settings/pages/BarPage.qml); for s in continuous brackets segmented halo off; do N=$(printf '%s\n' "$SRC" | grep -c "\"$s\""); echo "$s: $N"; done</automated>
    <automated>cd /home/aorus/dotfiles; N=$(sed -e 's://.*::' quickshell/.config/quickshell/modules/settings/pages/BarPage.qml | grep -c 'edgeBar.enabled'); echo "edgeBar.enabled reads in BarPage.qml: $N"; [ "$N" -eq 0 ] || echo 'FAIL: retired key still read'</automated>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for g in quickshell-doctor colour-lint motion-lint keybind-doctor; do hypr/.config/hypr/scripts/$g 2>&amp;1 | tail -1; done</automated>
  </verify>
  <done>Settings > Bar shows one five-entry style picker and, beneath it, an "Animate the bulge" row that is present on Continuous, Segmented, Halo and Off and absent on Brackets; every subtext describes a shape in plain English with no plan vocabulary; `settings-index-check` reads 118/0 and the other four gates hold their baselines.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 8: Operator sign-off — the visual and hover judgements this host cannot make for itself</name>
  <what-built>
All five edge-bar styles behind one picker in Settings > Bar — Continuous (the
default), Brackets, Segmented, Halo and Off — with the retired on/off bool
migrated to a style string, the launcher's direction and its attachment split
onto two separate derived predicates, and the bar welded into the rail on
Continuous. Every gate is green and the path builder is pinned by a golden test;
what remains is everything this host structurally cannot answer.
  </what-built>
  <how-to-verify>
Before asking for anything, run
`systemctl --user restart quickshell.service` and wait for the shell to come up.
A `LazyLoader` serves an already-incubated STALE component after a file edit
(hazard 9) — touching `shell.qml` is NOT enough, and this is the exact trap that
makes a correct fix look broken. Restart AFTER the last edit, never before.

Then present this. Be plain about which items are blocked on the operator
because the host genuinely cannot answer them, and do not claim any of them were
verified automatically.

**What this host CANNOT self-verify, stated up front and honestly:**

- **Hover-dwell behaviour on any style.** No pointer-injection tool exists here
  — `ydotool`, `wlrctl`, `dotool` and `xdotool` are all absent, and `wtype` types
  into whatever window has focus. Every hover judgement below is the operator's,
  not a probe's.
- **An open panel's swell.** The bulge is exactly the panel's width and the
  flare extends ~24px past it, so a capture of an open-state swell reads 100%
  non-background. That probe is blind, not the state empty. Do not offer such a
  capture as evidence.
- **Sub-pixel and aesthetic judgement.** Whether the weld reads as one silhouette
  and whether the segments read as information rather than noise are visual
  calls, not measurements.

**Ask the operator to switch styles in Settings > Bar and look at each in turn.
Say exactly what to look at.**

1. **Continuous (the default).** Does the top rail's gradient run unbroken into
   the bar and come back out along the bottom rail — one silhouette, rail ->
   corner -> bar -> corner -> rail — or can you see the join? Does the bar's
   inset core read as the bar still being the bar, rather than a coloured slab?
   Does hovering the middle of the top edge swell the bulge and, after the
   dwell, open the dashboard exactly as it did before this task? (This style is
   meant to be byte-identical to the round-11 behaviour you already approved —
   if anything about the hover feels different, that is a defect.)

2. **Brackets.** Four corner pieces, nothing between them, and no bulge anywhere
   — is that what you see? Hovering near a corner should extend that corner's
   arms toward the centre; does the extension read as a landmark, and is 40%
   the right amount of growth? **Two things to confirm deliberately, because
   both are choices rather than defects:** (a) dwelling on a corner still opens
   the dashboard from the top and the launcher's menu from the bottom — the
   summon moved to the corners rather than being removed (DC-1); (b) those
   panels open FREE-FLOATING, with all four corners rounded, no concave flares
   and no clipped rim — that is the shape's own property (the study scores
   Brackets `0 direct`), not a missing feature.

3. **Segmented.** Ten pieces across the top with the current workspace lit —
   does switching workspaces move the lit segment? With "Animate the bulge" ON,
   the segments should be separate at rest and the middle ones should merge into
   one solid block while you hover the centre or a panel is open. With it OFF,
   that merged block is permanent. Does the merge read as the bulge absorbing
   the segments, or as segments disappearing?

4. **Halo.** A 2px outline on all four edges. **The one to judge carefully:**
   turn "Animate the bulge" OFF. You should get a hairline frame plus two fixed
   12px masses at the top and bottom centres — a motionless landmark, not a bare
   frame. If you see no masses at all, that is a bug, not a preference. With it
   ON, does the 2 -> 12 swell read at that thinness, or does the landmark get
   lost?

5. **Off.** Nothing drawn, launcher drops from the top, desktop identical to the
   pre-edge-bar behaviour.

6. **The removal.** Two Rails is gone from every style and every fallback. Is
   there anywhere it still appears?

7. **Screen cost.** The measured `reserved` vector for each style will be quoted
   from the SUMMARY. Does the amount of screen each style takes match what you
   expected from the study's table — particularly Brackets at zero and Halo at
   its measured value rather than the study's 8px estimate (DC-2)?

Record the operator's verdict per style. Any "no" becomes a follow-up round
against this same plan, not a new task file.
  </how-to-verify>
  <resume-signal>Type "approved" once all five styles pass, or name the style and the item number that is wrong (e.g. "brackets 2b" or "halo 4").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `prefs.json` on disk -> the shell | A hand-edited or corrupted state file crosses into the shell's render path at every start. |
| `qs ipc call prefs set` -> `Prefs.setValue` | A local IPC caller can write any allow-listed key with any value. |
| Layer surface -> the compositor's input routing | Any pixel this shell paints on a screen edge can consume a click the operator meant for a window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ns3-01 | Tampering | `Prefs` `edgeBar.style` | low | mitigate | Closed key allow-list plus the `indexOf` VALUE allow-list in `shell.qml`; an unknown style falls back to `"continuous"` rather than rendering nothing or throwing (Task 1). |
| T-ns3-02 | Denial of Service | `_migrate()` on a malformed `prefs.json` | low | mitigate | The migration runs inside the existing `_loadPrefs` try/catch, which already degrades a non-object or unparseable file to `{}` and hardcoded defaults; the migration is a no-op on an empty tree (Task 1). |
| T-ns3-03 | Elevation of Privilege | Edge-bar layer surfaces consuming clicks | medium | mitigate | `EdgeBar.qml` stays permanently click-inert — no `TapHandler`/`MouseArea`/`WheelHandler`/`DragHandler` — and `mask: Region` confines input to the hover regions only. Asserted by grep in Task 5. Brackets adds a second hit region, not a second input type. |
| T-ns3-04 | Denial of Service | A resizing layer surface | medium | mitigate | Every surface's depth is pinned to the maximum its style can reach; only the painted path animates (hazard 2, Tasks 4-6). A per-frame surface reconfigure re-buffers and drags its own content. |
| T-ns3-05 | Information Disclosure | Screen reservation | low | accept | The reservation change is visible to every client by design — that is what a reserved zone is. Measured and recorded per style rather than asserted. |
| T-ns3-SC | Tampering | npm/pip/cargo installs | high | accept | **No package manager runs in this task.** Nothing is installed: every tool used (`grim`, `jq`, `python3`+PIL, `node`) is already present on this host and was verified during planning. No package-legitimacy checkpoint is required because there are no install tasks. |
</threat_model>

<verification>

**Run after every task, without exception:**

```
hypr/.config/hypr/scripts/quickshell-doctor      -> 28 passed, 0 failed
hypr/.config/hypr/scripts/colour-lint            -> 359 passed, 0 failed
hypr/.config/hypr/scripts/motion-lint            -> 546 passed, 0 failed
hypr/.config/hypr/scripts/keybind-doctor         -> 13 passed, 0 failed
hypr/.config/hypr/scripts/settings-index-check   -> 118 passed, 0 failed  (was 117/1)
node hypr/.config/hypr/scripts/tests/edgebar-path-test.mjs   (from Task 2 on)
```

No gate script is edited by this task, so `motion-lint --self-test` and
`quickshell-doctor --self-test` are not required. If that changes — if any task
finds it must touch a gate — run `motion-lint --self-test` (12/0) and
`quickshell-doctor --self-test` (59/0) in that same commit.

**Rules that decide whether a gate result means anything:**

- `colour-lint` GATE-04 rejects a hardcoded colour in QML. The study draws in
  Dracula hex; every one of those becomes a `Colours.*` role. A literal in a
  comment can also trip a grep-shaped check — strip `//` lines before counting.
- `motion-lint` CHECK A derives its allow-list from `motion.json`'s semantic
  keys and also accepts `<camelKey>ReverseEasing`. Introduce no `Motion.*` name
  that is not derivable that way, or CHECK A reports it dangling.
- A green gate only proves what it can see. `colour-lint` once read 202/0 while
  18 unthemed tooltips shipped. Sweep flags, the fillet invariant and the
  reservation vector have NO gate — that is exactly why Task 2 builds a golden
  test and why Tasks 3-6 measure `reserved` rather than asserting it.
- `quickshell-doctor` reads live state and is flaky in the seconds after a QML
  edit; it has read 25/3 and then 28/0 on a re-run. Re-run twice before
  believing a failure.
- A `| grep -q` under `pipefail` can exit 141 on a match. Every gate invocation
  above is run bare and its summary line read, never piped into a quiet grep
  that decides the verdict.

**Measuring the strip — the only method that works on this host:**

Dump raw RGB per column or per row and read where the strip colour stops.
Everything else has been tried and lies: saturation thresholds pick up the
wallpaper; luminance thresholds pick up the Hyprland window border, which shares
the strip's hue; on/off diffs are worthless because the reservation changes and
every window moves; contiguous-run scans count uniform background as full depth.

Capture with `grim -g "x,y WxH"` — REGION captures only. A full-screen `grim`
SIGSEGVs the compositor into safe mode on this NVIDIA host. Never spawn a `qml6`
probe script; each opens a real window and a loop of them takes the session down.

**After any QML edit, restart before measuring:** `systemctl --user restart
quickshell.service`. A `LazyLoader` serves an already-incubated stale component
otherwise, and touching `shell.qml` does not help. Restart AFTER the last edit,
not between edits, or the pages you measure are stale.

**Reservation is `margins.<anchored-edge> + exclusiveZone`.** Read it with
`hyprctl -j monitors | jq '.[0].reserved'` — order is `[left, top, right,
bottom]`. Baseline today is `[0, 6, 50, 6]`. And remember the boundary is not the
window edge: tiled windows sit `gaps_out` (10) inside the reserved zone, so a
rail painted at the reserved boundary is 10px away from the nearest window
border, by design.

</verification>

<success_criteria>

- Settings > Bar offers exactly five styles and no on/off switch anywhere.
- `edgeBar.enabled` appears in no QML file and in no `prefs.json` on this host.
- This host's stored `true` came back as `"continuous"` without operator action.
- `Launcher.qml` contains no style string; its direction reads
  `edgeBarRailPresent` and its attachment reads `edgeBarPanelsAttach`.
- Two Rails is unreachable from every code path, default and fallback.
- Continuous, Brackets, Segmented and Halo each render the shape the study draws,
  with `Colours.*` roles substituted for every Dracula literal.
- Continuous's hover and bulge behaviour is indistinguishable from round 11.
- Halo with `animatedBulge` OFF shows two permanent 12px masses.
- Brackets draws no bulge, reserves nothing, and its panels open unattached.
- Segmented merges whole segments — never a half-segment — under the bulge.
- `reserved` is measured and recorded for all five styles.
- Five gates green (`settings-index-check` improved from 117/1 to 118/0) and the
  `edgebar-path` golden test passing.
- The SUMMARY records the **D-2 reversal** deliberately, in the same register
  round 11 used for D-3's reversal — as a decision taken with its cost stated up
  front, not as a deviation discovered during execution.
- The SUMMARY records DC-1 and DC-2 with their reasoning and the operator's
  verdict on each.

</success_criteria>

<output>
Create `.planning/quick/260824-ns3-implement-the-edge-bar-style-picker-per-/260824-ns3-SUMMARY.md` when done.

It must carry, at minimum:
- the **D-2 reversal**, written as a deliberate decision with its cost, mirroring
  how round 11 recorded D-3's reversal;
- the measured `reserved` vector for each of the five styles, quoted from
  `hyprctl -j monitors`, never estimated;
- DC-1 (Brackets keeps hover-to-summon) and DC-2 (Halo's right rail trades the
  study table's 8px for the study's drawn position) with the operator's verdict
  on each;
- the operator's per-style answers from Task 8, including every "no".
</output>