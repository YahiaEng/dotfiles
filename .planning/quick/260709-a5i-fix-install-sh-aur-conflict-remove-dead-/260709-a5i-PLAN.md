---
phase: quick
plan: 260709-a5i
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
autonomous: true
requirements: [INST-03]

must_haves:
  truths:
    - "install.sh no longer lists alpm_octopi_utils anywhere"
    - "octopi remains in AUR_PKGS as the sole octopi-related entry"
    - "paru can resolve AUR_PKGS with --noconfirm without a conflicting-package hard-fail"
  artifacts:
    - "install.sh (AUR_PKGS array, line ~177 removed)"
  key_links:
    - "VERIFY_PKGS (line ~399) derives from AUR_PKGS — removing the entry automatically drops it from verification, no separate edit"
---

<objective>
Fix the INST-03 container-gate failure by removing the dead `alpm_octopi_utils` entry from install.sh's `AUR_PKGS` array.

Purpose: `alpm_octopi_utils` no longer exists in the AUR (only `alpm_octopi_utils-git` remains, which paru silently substitutes as a provider). `octopi` 0.19.0 explicitly declares `Conflicts With: alpm_octopi_utils` (it dropped that dependency; deps are now qt6-base, qt6-multimedia, qt6-svg, qtermwidget, pacman, pacman-contrib, qt-sudo). Listing both makes paru hard-fail with `error: can not install conflicting packages with --noconfirm` during the container gate — the root cause of INST-03 run status=fail, overall=FAIL (evidence: verify/logs/run-20260709T041113Z/03-install.log).

Output: install.sh with the one dead line removed, keeping `octopi`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead alpm_octopi_utils entry from AUR_PKGS</name>
  <files>install.sh</files>
  <action>In the `AUR_PKGS` array (Utils section, around line 177), delete the single line containing `alpm_octopi_utils`. Keep `bibata-cursor-theme` above it and keep `octopi` (around line 200) untouched — `octopi` is the retained package and now provides the pacman-frontend functionality on its own. Do NOT touch VERIFY_PKGS at line ~399: it derives directly from `${AUR_PKGS[@]}`, so removing the array entry automatically drops it from post-install verification — no separate change is needed. Rationale (per orchestrator diagnosis): `alpm_octopi_utils` is gone from the AUR and `octopi` 0.19.0 declares `Conflicts With: alpm_octopi_utils`, so listing both triggers paru's `can not install conflicting packages with --noconfirm` hard-fail during the INST-03 container gate.</action>
  <verify>
    <automated>bash -n install.sh && test "$(grep -c alpm_octopi_utils install.sh)" -eq 0 && grep -q '^[[:space:]]*octopi$' install.sh && echo PASS</automated>
  </verify>
  <done>`bash -n install.sh` reports no syntax errors, `grep -c alpm_octopi_utils install.sh` returns 0, and `octopi` still present in AUR_PKGS. Output ends with PASS.</done>
</task>

</tasks>

<verification>
- `bash -n install.sh` exits 0 (no syntax error introduced by the deletion)
- `grep -c alpm_octopi_utils install.sh` returns 0 (dead entry fully gone, including any provider-suffixed form)
- `octopi` still present in AUR_PKGS (the intended package retained)
</verification>

<success_criteria>
- install.sh's AUR_PKGS no longer contains `alpm_octopi_utils`
- `octopi` remains as the sole octopi-family entry
- Script still parses cleanly under `bash -n`
- Commit follows the repo's established gap-closure pattern: `fix(03-02): remove dead alpm_octopi_utils AUR entry (conflicts with octopi)`
</success_criteria>

<output>
Create `.planning/quick/260709-a5i-fix-install-sh-aur-conflict-remove-dead-/260709-a5i-SUMMARY.md` when done.
</output>
