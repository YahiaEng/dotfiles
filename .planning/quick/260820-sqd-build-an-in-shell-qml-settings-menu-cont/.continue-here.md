---
context: quick-task
phase: quick-260820-sqd (in-shell QML settings window)
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-08-20T21:10:29.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through actual failure this session.

- [ ] CONSTRAINT: six QML files sit UNCOMMITTED in the tree (see Infrastructure
      State) — they are a dead executor's half-finished fix wave, UNVERIFIED.
      `git diff` them before anything else; finish-and-verify or discard, never
      blind-commit.
- [ ] CONSTRAINT: colour-lint CANNOT see default-styled QQC2 popups — every
      dropdown shipped unthemed with the gate green (zero literals in-repo; the
      colors come from Qt's style). Never cite colour-lint as evidence a QQC2
      popup is themed. Named follow-up: extend the lint.
- [ ] CONSTRAINT: machine keyboard/focus evidence does not transfer to real
      input on this host. wtype Esc passed 8/8 while the operator's real Esc
      failed — the real mechanism (follow_mouse=1 focus steal) is only
      reachable via `hl.dsp.focus` dispatch as the steal instrument, and
      click-outside/real-mouse behaviour is verifiable ONLY by the operator
      (no pointer-injection tool exists here).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Tautological verify | hypr-overrides.sh verified `.scale`/`.float` regardless of what changed — silently-failed applies got persisted (CR-01/02, fixed in 4ac0e9d2) | blocking | Verify the SPECIFIC field changed, against hyprctl -j oracles; extend round-trip proofs to each field class |
| Trusting a probe negative | `hl.dsp.cursor.move` warps without driving focus — 3 measured trials proved the instrument blind, not the mechanism absent | blocking | Positive control first; use `hl.dsp.focus` for focus-steal reproduction |
| Reverting on untested fear | HyprlandFocusGrab was reverted over a feared popup self-close that measurement later disproved (12/12) | advisory | Measure the fear before acting on it |
| systemctl restart hypridle | Unit is inactive/disabled on this host — restart is a silent no-op; live process is a uwsm scope from autostart.lua | blocking | Only `uwsm app -- hypridle` relaunch; gate greps for systemctl in idle-overrides.sh |
| hyprlang append-not-replace | Listener blocks APPEND across source= — earlier rule wins, so a tracked listener makes timeouts shortenable but never lengthenable | blocking | ALL five listeners live in the state-dir file; tracked hypridle.conf keeps 0 listeners + 1 source= (Task 4 verify check A) |

<current_state>
Quick task 260820-sqd (--full). Tasks 1-4 code-complete, gate-verified,
committed through 4ac0e9d2 (NOT pushed). Code review done: 3 Critical + 4
warnings; CR-01/02/03, WR-01/02/04 fixed and live-verified; WR-03 folded into
the in-flight wave. Operator live pass PARTIAL — see verdicts below. The
executor subagent died with the session MID-WAVE on the operator-defect fixes;
its uncommitted edits are in the tree.
</current_state>

<completed_work>
- Task 1 tracer + 3 amendment rounds (width-collapse fix, Esc focus-race fix,
  HyprlandFocusGrab + Up/Down arrows + click-outside) — a384b363, 5614e287,
  ff2dc558, cce14214
- Task 2 three pages, nothing rebuilt — 013b03d2
- Task 3 Display+input, hyprctl eval + state-dir Lua, reload-survival proven — acc4e5b1
- Task 4 editable idle/lock, mechanism re-measured — 914266c1
- SUMMARY.md written (c4e8a50e); code-review fixes — f0d933f4, 4ac0e9d2
</completed_work>

<remaining_work>
- Finish/verify/commit the in-flight defect wave (or redo). Defect list with
  operator verdicts:
  - Item 2 PARTIAL: Left/Right arrows do nothing (wire like Dashboard.qml:725 paging; Up/Down + Esc work)
  - Item 3 PARTIAL: page switches feel LAGGY (measure: re-incubation cost vs animation; log construction timestamps)
  - Item 10 FAIL: ALL dropdown popups hardcoded/unthemed (QQC2 style defaults; fix shared SelectRow popup background+delegates from Colours.qml; verify EVERY page's dropdowns)
  - Bug: hover on menu items flaky — inconsistent hitbox, hover appears/disappears (QQC2 contentItem-anchors trap and/or missing delegate implicit sizes; log settled delegate geometry)
  - Bug: window opens on LAST tab; must reset to page 0 on every open
  - Item 6 PARTIAL: "open nwg-displays" row dead click (compare exec mechanism with working picker rows; check `command -v nwg-displays` — maybe not installed)
  - Item 8 PARTIAL: "open in editor" row dead click ($EDITOR=nvim is a TUI — needs the floating-kitty wrap like nmtui-launch.sh, uwsm app -- per D-09)
  - WR-03: refresh-rate rounding marker (no impact on this 165Hz host, fix opportunistically)
- Operator re-check of all fixed items + item 9 (reboot persistence of idle timeouts)
- On operator pass: close WINDOWS.md row 92 (`gsd-tools windows fixed <id>`)
- Orchestrator completion steps never run: gsd-verifier (quick-verification
  step of ~/.claude/gsd-core/workflows/quick.md), STATE.md "Quick Tasks
  Completed" row + Session Continuity update, final docs commit
  (PLAN/SUMMARY/REVIEW/CONTEXT/RESEARCH + STATE.md), delete HANDOFF.json, PUSH.
</remaining_work>

<decisions_made>
- FloatingWindow KEPT over PanelWindow (operator overrode executor's HALT):
  HyprlandFocusGrab measured 12/12 Esc-after-steal; grab captures wl_keyboard
  routing below window activation (activewindow never moves back — that is
  normal, not a bug)
- Idle/lock EDITABLE (operator overrode research descope): hypridle source=
  measured working; 5 listeners in ~/.local/state/hypr/idle-overrides.conf;
  tracked conf has 0 listeners + 1 source=
- stayfocused window rule: DEAD in this build's Lua vocabulary (validator
  rejects it + 6 synonyms) — do not retry
- PROJECT.md "no full GUI settings app" reversed, dated 2026-08-20 (Task 1)
- Super+comma chord; walker settings.toml gained a top "Settings" row calling
  `qs ipc call settings open` (measured working bare — A4 discharged)
</decisions_made>

<blockers>
- Operator re-check + item 9 reboot: required to close WINDOWS row 92
</blockers>

## Required Reading (in order)
1. `260820-sqd-SUMMARY.md` (this dir) — full coverage table, key decisions, deviations
2. `260820-sqd-REVIEW.md` (this dir, UNTRACKED — commit with docs) — the 7 findings; WR-03 still open
3. `260820-sqd-PLAN.md` Task 4 `<human-check>` — the 10-item operator checklist WINDOWS row 92 closes on
4. `260820-sqd-CONTEXT.md` — locked operator decisions (do not re-litigate)

## Infrastructure State
- UNCOMMITTED (dead executor's in-flight wave, unverified): Pages.qml,
  Settings.qml, common/SelectRow.qml, pages/DisplayInputPage.qml,
  pages/ShellBehaviourPage.qml, shell.qml
- Untracked: 260820-sqd-REVIEW.md (commit with docs), .gsd/ (pre-existing,
  out of scope, leave alone)
- NOT PUSHED: everything from a384b363 through 4ac0e9d2 (~8 commits) — push
  at next wave boundary per standing rule
- Settings window live on the operator's desktop via hot reload; quickshell
  NOT restarted since cce14214's state
- Live idle/lock state: operator exercised timeout edits during item 8 and
  restored them; ~/.local/state/hypr/idle-overrides.conf is authoritative

<context>
Flow was: discussion (4 locked decisions + 2 late operator overrides) ->
research (Caelestia nexus/ mapped; hyprctl keyword dead, eval works; hypridle
mechanisms measured) -> 4-task plan (checker PASS) -> execution with 4
operator checkpoint rounds on the tracer -> code review -> operator live pass
(partial, 8 defects) -> fix wave (died mid-flight with the session).
</context>

<next_action>
Start with: `git diff` the six uncommitted QML files against the defect list
above; decide finish-and-verify vs discard-and-redo; then complete
<remaining_work> top to bottom.
</next_action>
