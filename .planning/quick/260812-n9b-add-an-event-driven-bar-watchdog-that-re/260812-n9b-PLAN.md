---
phase: quick-260812-n9b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hypr/.config/hypr/scripts/bar-watchdog.sh
  - hypr/.config/hypr/scripts/tests/test-bar-watchdog.sh
  - quickshell/.config/systemd/user/quickshell-bar-watchdog.service
  - hypr/.config/hypr/config/autostart.lua
autonomous: true
requirements: [QBAR-11]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A long-running listener attached to Hyprland's socket2 sees `monitoradded`, `monitoraddedv2` and `monitorremoved`, waits out a debounce, and only then judges whether the bar surface exists."
    - "The health check reads `hyprctl layers -j` and matches the namespace `quickshell-bar` by EXACT string equality. `quickshell-bar-hotzone` and `quickshell-bardrawer-*` are live sibling namespaces on this host and must NOT satisfy it. Proven by a fixture that contains only the siblings and is expected to read `absent`."
    - "The check NEVER consults `bar-visibility.sh status` — WINDOWS row 67 records that verb printing `visible` with no surface present. The watchdog tests reality, not intent."
    - "Recovery is exactly `systemctl --user restart quickshell.service`, proven by a PATH-shimmed `systemctl` recording its argv. A `pkill quickshell` sends SIGTERM, which quickshell.service's own header documents as NOT restarting the unit."
    - "Restarts are rate-limited: a minimum interval between them and a maximum count per rolling window, both proven to suppress by counting shim invocations against a burst of fixture events."
    - "An unreadable or non-JSON `hyprctl layers -j` yields INDETERMINATE and never a restart — the watchdog fails safe toward doing nothing."
    - "A dead socket read (EOF) breaks the loop and exits 0. It never spins — the retired listener's own guard against pegging a core for the rest of uptime, reproduced."
    - "The unit passes `systemd-analyze verify`, carries no `[Install]` section, and is started explicitly from autostart.lua so that file stays the single readable inventory of what a session launches."
    - "The unit is NOT `PartOf=quickshell.service`. It restarts that unit; binding its own lifetime to the unit it restarts would tear the watchdog down inside its own recovery."
  artifacts:
    - "hypr/.config/hypr/scripts/bar-watchdog.sh — the socket2 listener, health check and recovery, with a header stating the D-18-28 reversal"
    - "hypr/.config/hypr/scripts/tests/test-bar-watchdog.sh — fixture-socket + PATH-shim harness proving parse, exact-match, recovery argv and rate limit without a real monitor event"
    - "quickshell/.config/systemd/user/quickshell-bar-watchdog.service — the supervisor, beside quickshell.service, mirroring its four documented choices"
    - "hypr/.config/hypr/config/autostart.lua — one added entry with its own D-15/D-35 carve-out note"
  key_links:
    - "`~/.config/hypr/scripts` is a FOLDED whole-directory symlink into the repo — a new script needs no stow run. `~/.config/systemd/user` is a REAL directory with per-file symlinks — a new unit DOES need `stow -R quickshell` plus `systemctl --user daemon-reload`, or systemd will not see it. Verified live this session."
    - "The listener needs both a blocking read and a debounce timer in one thread: `select.select([s], [], [], timeout)` with a pending deadline. A bare `recv()` cannot expire the debounce; a bare loop without the EOF break is the core-pegging bug."
    - "Event payloads are compositor-supplied and must never reach a shell — every subprocess call is an argv LIST, never a shell string (the retired listener's T-08-20 discipline)."
    - "Tunables are read from the environment with the shipped values as defaults, so the harness can compress a 15-minute rate-limit window into seconds without editing the script under test."
---

<objective>
Add an event-driven watchdog that restores the QML bar after the failure recorded as
WINDOWS.md row 67: a monitor removal/re-add (display sleep, DPMS cycle, hotplug) destroys the
`quickshell-bar` layer surface, quickshell itself stays perfectly healthy — same pid,
`NRestarts=0`, `ActiveState=active`, zero QML errors — and the bar is gone until the service is
restarted by hand.

Purpose: this is a permanent-liveness defect against QBAR-11's own subject matter and it is
invisible to every automated gate in Phase 18. The shell cannot heal it from inside (see the
scope fence below), so the repair has to come from outside the shell.

Output: one socket2 listener script, one test harness, one systemd `--user` unit, one
autostart entry.
</objective>

<scope_fence>
Read this before writing a line. These are settled; do not re-derive them and do not relitigate them.

**The cause is already root-caused.** Live log, both occurrences:
`quickshell.hyprland.ipc: Got removal for monitor "FALLBACK" which was not previously tracked`,
immediately followed by `qt.qpa.wayland: There are no outputs - creating placeholder screen`.
The `PanelWindow` binds to the placeholder screen and never migrates back.

**An in-QML fix is OUT OF SCOPE and was already tried and reverted today.** Assigning the
writable `screen` property imperatively on the live `PanelWindow` spawned a SECOND,
default-namespaced `quickshell` layer surface at 25,73 instead of moving the existing one. That
is an FM2-class surface-creation misbehaviour matching exactly what **D-13** (PROJECT.md line
152) already accepted **one-way as a permanent limitation** of quickshell 0.3.0-2, after two
structurally distinct Phase 12 attempts plus an escape-hatch spike. D-13's own wording already
covers "including one hotplugged after startup".

Therefore this plan MUST NOT:
- edit `quickshell/.config/quickshell/modules/Bar.qml`
- reintroduce a `Variants` fan-out root (Bar.qml's own header forbids it under D-13)
- attempt any further in-QML re-attach, in any form

Also forbidden for the duration of this plan:
- `hyprctl reload` or `hyprctl eval` in any spelling, including `hyprctl dispatch 'hl.dsp.*'`
- `git stash`
- touching `waybar/`
- running plan 18-20
- editing `18-GATE-02-RECORD.md` or `18-BAR-SOAK.md`

`hyprctl layers -j` and `hyprctl monitors -j` are read-only and are fine.
</scope_fence>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@quickshell/.config/systemd/user/quickshell.service
@hypr/.config/hypr/config/autostart.lua

The single most important reference is the RETIRED listener this plan mirrors. Read it in full
before writing Task 1; do not invent a new listener shape:

```
git show adce9e6^:hypr/.config/hypr/scripts/waybar-fullscreen-watch.sh
```

It establishes: the `exec python3 - "$SOCKET" <<'PYEOF'` inline-stdlib idiom (it calls this
"theme-doctor's established shape"), the `XDG_RUNTIME_DIR`/`HYPRLAND_INSTANCE_SIGNATURE`/
socket-exists exit-0 guards, the argv-list-only subprocess discipline, and the EOF break whose
own comment explains that an unconditional loop around a dead read "would peg a core at 100%
for the rest of uptime". Reproduce that guard — it is load-bearing.

Facts already measured live this session; do not re-measure them:
- `hyprctl layers -j` on DP-1 currently reports namespaces `awww-daemon`, `quickshell-bar`,
  `quickshell-bar-hotzone`. Exact-match on `quickshell-bar` is mandatory, not stylistic.
- `python3` is 3.14.6. `socat`, `nc` and `ncat` are ABSENT on this host — python3 stdlib only.
  Adding a package would touch install.sh's reproducibility contract for no reason.
- `~/.config/hypr/scripts` → folded symlink into the repo (new script: no stow needed).
- `~/.config/systemd/user` → real directory, per-file symlinks (new unit: stow + daemon-reload).
- `stow.sh` already pre-creates `~/.config/systemd/user` as a real directory and already lists
  `quickshell`'s `~/.config/systemd/user/` tree as one of its three documented exceptions.
  A SECOND unit file in that same already-guarded directory adds no new exception —
  **do not edit stow.sh.**
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The listener, the health check, the recovery — one path, proven end-to-end on a fixture socket</name>
  <files>hypr/.config/hypr/scripts/bar-watchdog.sh, hypr/.config/hypr/scripts/tests/test-bar-watchdog.sh</files>
  <read_first>
    `git show adce9e6^:hypr/.config/hypr/scripts/waybar-fullscreen-watch.sh` in full (shape to
    mirror). Then skim `hypr/.config/hypr/scripts/tests/test-media-hardening.sh` for this repo's
    harness conventions (temp dir, trap-armed cleanup, PASS/FAIL counters, exit code).
  </read_first>
  <behavior>
    The harness proves these before the watchdog is trusted with a real session. Each case binds
    a fixture AF_UNIX socket at `$TMP/hypr/$FAKE_SIG/.socket2.sock`, puts a stub `hyprctl` and a
    recording stub `systemctl` first on PATH, writes event lines into the socket, and asserts:

    - Case A — event parse: writing `monitorremoved>>DP-1\n` to the fixture socket with a
      bar-ABSENT `hyprctl` fixture, in `--dry-run`, logs that the event was seen and logs
      `WOULD RESTART`. The recording `systemctl` shim is invoked ZERO times.
    - Case B — exact-match trap: `hyprctl` fixture whose only namespaces are
      `quickshell-bar-hotzone` and `quickshell-bardrawer-audio` reads **absent**, not present.
    - Case C — present is left alone: `hyprctl` fixture containing `quickshell-bar` logs
      no-action and the `systemctl` shim is invoked ZERO times.
    - Case D — recovery argv: same as A but in default watch mode. The `systemctl` shim records
      exactly one invocation and its recorded argv is exactly
      `--user restart quickshell.service`.
    - Case E — rate limit: with the tunables compressed via env (debounce 0.3s, min-interval
      0.5s, max 2 restarts, window 60s) and a bar-ABSENT fixture, four spaced monitor events
      produce exactly TWO shim invocations, and the log states the limit was reached.
    - Case F — indeterminate fails safe: `hyprctl` stub exits 1 (and a second sub-case prints
      non-JSON). Both log `indeterminate` and invoke the shim ZERO times.
    - Case G — ignored events: `activewindow>>foo,bar` and `workspace>>3` written to the socket
      never trigger a check at all, in any mode.
    - Case H — EOF: the fixture server closes the connection; the watchdog exits 0 within a
      second and its CPU time stays negligible (it must break, not spin).

    Every case has a bounded timeout so a hung watchdog fails the harness instead of hanging it.
  </behavior>
  <action>
Create `hypr/.config/hypr/scripts/bar-watchdog.sh`, executable, `set -euo pipefail`.

HEADER — write this before the code, in the boxed-title + long-rationale style this repo's
scripts use. It must state, in prose, all six of the following. Do not compress them away:

  1. What it does: a long-running Hyprland socket2 listener that, on monitor events, checks
     whether the bar's layer surface actually exists and restarts `quickshell.service` only if
     it genuinely does not. Cite WINDOWS.md row 67 as the defect, quoting the two log lines
     (`Got removal for monitor "FALLBACK" which was not previously tracked` → `There are no
     outputs - creating placeholder screen`).
  2. **This reverses D-18-28** — state that plainly. 18-15 deleted this repo's standalone
     socket2 listener outright: "the standalone socket2 listener is deleted outright, not
     repointed — the fullscreen intent is now reported by the QML shell itself". Then justify
     the reversal, because that justification is the whole argument: D-18-28 retired a listener
     because the SHELL could report the intent itself, whereas here **the shell is precisely the
     thing that fails** — it goes on logging `bar: visibility=visible zone=reserved` while no
     surface exists — so a self-healing mechanism structurally cannot live inside it.
  3. Why the check reads `hyprctl layers -j` and **never** `bar-visibility.sh status`: WINDOWS
     row 67 records that verb printing `visible` with no surface present, and its documented
     `reassert` recovery verb completing without error and changing nothing. Anything grepping
     it tests intent, not reality.
  4. Why the namespace match is exact equality on `quickshell-bar`: `quickshell-bar-hotzone`
     and `quickshell-bardrawer-*` are live sibling namespaces on this host; a prefix or
     substring match would report a healthy bar during exactly the outage this exists to catch.
  5. Why recovery is `systemctl --user restart quickshell.service` and not `pkill quickshell`:
     pkill sends SIGTERM, which `quickshell.service`'s own header documents as being in the
     clean-exit exemption list and therefore NOT restarting the unit — "the single most likely
     misdiagnosis this unit can cause".
  6. Why python3 stdlib only: `socat`, `nc` and `ncat` are all absent on this host (verified);
     adding a package would touch install.sh's reproducibility contract for no reason. Note the
     inline `python3 - <<'PYEOF'` idiom is theme-doctor's established shape, inherited from the
     retired `waybar-fullscreen-watch.sh`.

BASH PREAMBLE — mirror the retired listener:
  - Accept exactly one optional argument: `--check` (one-shot health check) or `--dry-run`
    (watch and log, never restart). No argument = watch mode. Anything else: usage to stderr,
    exit 2.
  - Headless/no-session safety: if `XDG_RUNTIME_DIR` or `HYPRLAND_INSTANCE_SIGNATURE` is unset,
    exit 0 immediately — never block, never spin. Build
    `SOCKET_PATH="$XDG_RUNTIME_DIR/hypr/$HYPRLAND_INSTANCE_SIGNATURE/.socket2.sock"`. In watch
    and dry-run modes, if that path is not a socket, exit 0. `--check` needs no socket and must
    still work without one.
  - Comment why exit 0 and not exit 1 here: under the unit added in Task 2, `Restart=on-failure`
    means a clean exit degrades to a logged skip and can never enter a restart cycle — the same
    reasoning quickshell.service's header gives for quickshell-launch.sh's two guard paths.
  - `exec python3 - "$SOCKET_PATH" "$MODE" <<'PYEOF' … PYEOF`.

PYTHON BODY — stdlib only (`socket`, `select`, `subprocess`, `json`, `time`, `sys`, `os`):
  - Constants, each read from an env override with the shipped value as the default, so the
    harness can compress them without editing this file. Document the defaults in a comment:
    `BAR_WATCHDOG_DEBOUNCE_SEC` (default 3.0), `BAR_WATCHDOG_MIN_INTERVAL_SEC` (default 30),
    `BAR_WATCHDOG_MAX_RESTARTS` (default 3), `BAR_WATCHDOG_WINDOW_SEC` (default 900).
    Parse each defensively — a malformed override falls back to the default rather than
    crashing the watchdog at session start.
  - `BAR_NAMESPACE = "quickshell-bar"`. `MONITOR_EVENTS = ("monitoradded", "monitoraddedv2",
    "monitorremoved")` — exactly these three, per the approved scope. Comment that the set is
    closed deliberately and that this tuple is the single place to widen it.
  - `log(msg)`: `print(f"bar-watchdog: {msg}", flush=True)`. stdout only — under the unit this
    lands in the journal, so there is no log file and no rotation logic to maintain. The
    `flush=True` is required, not decorative: stdout is a pipe under systemd and would otherwise
    block-buffer.
  - `bar_surface_present()` → `True` / `False` / `None`:
    `subprocess.run(["hyprctl", "layers", "-j"], capture_output=True, text=True, check=False,
    timeout=5)`. Non-zero return, timeout, non-dict or unparseable JSON → return `None`
    (INDETERMINATE). Otherwise walk every monitor's `levels` values and return `True` on the
    first layer whose `namespace` is **exactly equal** to `BAR_NAMESPACE`; `False` if none is.
    Comment the equality with reason 4 from the header.
  - `--check` mode: call it once, print `present` / `absent` / `indeterminate` on stdout, exit
    0 / 1 / 2 respectively, never open the socket, never restart. This is the verb every later
    verification uses.
  - Watch/dry-run loop: connect the AF_UNIX stream socket (`OSError` on connect → exit 0, as the
    retired listener does). Then loop with `select.select([s], [], [], timeout)` where `timeout`
    is `None` when no check is pending and the remaining debounce otherwise. Explain in a comment
    why `select` and not a bare `recv`: one thread has to serve both a blocking read and an
    expiring debounce timer, and `timeout=None` blocks at zero CPU.
    - Empty select result = the debounce expired → clear the pending deadline and evaluate.
    - Readable → `recv(4096)`. **Empty chunk = EOF: break.** Reproduce the retired listener's
      comment verbatim in spirit — the compositor closed the connection at session exit, and an
      unconditional loop around a dead read would peg a core at 100% for the rest of uptime.
      `OSError` on recv → break.
    - Split the buffer on `b"\n"`, decode with `errors="replace"`, split each line on the first
      `>>`. If the event name is not in `MONITOR_EVENTS`, ignore it and never look at the
      payload — window titles and classes are never parsed here. In dry-run mode only, log the
      event NAME of every line seen (payload excluded), so the harness and a live operator can
      both confirm real socket2 traffic is being read.
    - On a monitor event, set the pending deadline to `time.monotonic() + DEBOUNCE`. A further
      event before it expires PUSHES the deadline out — a sleep/wake burst collapses into one
      judgement, and the surface gets time to settle before it is judged missing.
  - `evaluate()`: `None` → log indeterminate, no action, return. `True` → log present, no
    action, return. `False` → `recover()`.
  - `recover()`: prune the restart-timestamp list to the rolling window; if
    `now - last < MIN_INTERVAL` log the cooldown skip and return; if the pruned list already has
    `MAX_RESTARTS` entries, log that the limit was reached (naming the window) and return
    without restarting — a genuinely broken shell must not be respawned in a loop. Otherwise
    append the timestamp and either log `WOULD RESTART quickshell.service` (dry-run) or run
    `subprocess.run(["systemctl", "--user", "restart", "quickshell.service"], check=False,
    timeout=30)` and log its return code. Record the timestamp in dry-run too, so the rate
    limiter is the same code path under test as in production.
  - Every subprocess call is an argv LIST. A shell-interpreting invocation is never used —
    event payloads are compositor-supplied and must never reach a shell.

Then create `hypr/.config/hypr/scripts/tests/test-bar-watchdog.sh`, executable, implementing
cases A-H from `<behavior>` above. Follow this repo's harness conventions: `mktemp -d`, a
cleanup trap armed BEFORE anything is created, PASS/FAIL counters, a summary line, exit 1 on any
failure. The stub `hyprctl` and the recording `systemctl` go in a `$TMP/bin` prepended to PATH;
the `systemctl` stub appends its `"$@"` to a file and exits 0, so the real service is never
touched. Drive the fixture socket from a small inline python server. The harness must never
contact the real compositor or the real user manager.
  </action>
  <verify>
    <automated>bash -n hypr/.config/hypr/scripts/bar-watchdog.sh &amp;&amp; hypr/.config/hypr/scripts/tests/test-bar-watchdog.sh</automated>
    <automated>awk '/&lt;&lt;.PYEOF.$/{f=1;next} /^PYEOF$/{f=0} f' hypr/.config/hypr/scripts/bar-watchdog.sh > /tmp/bw_body.py &amp;&amp; python3 -m py_compile /tmp/bw_body.py &amp;&amp; echo PYCOMPILE_OK</automated>
    <automated>hypr/.config/hypr/scripts/bar-watchdog.sh --check</automated>
    <automated>grep -v '^[[:space:]]*#' hypr/.config/hypr/scripts/bar-watchdog.sh | grep -c 'bar-visibility' | grep -qx 0 &amp;&amp; echo NO_INTENT_ORACLE_OK</automated>
  </verify>
  <done>
    `bash -n` clean; the extracted python body compiles (the recovery path exercised
    syntactically without firing); the harness reports all of A-H passing and exits 0;
    `--check` prints `present` and exits 0 against the live session, which is the ground truth
    right now; and no NON-COMMENT line of the script references `bar-visibility`.
  </done>
</task>

<task type="auto">
  <name>Task 2: The supervisor unit, beside quickshell.service, mirroring its documented choices</name>
  <files>quickshell/.config/systemd/user/quickshell-bar-watchdog.service</files>
  <read_first>
    `quickshell/.config/systemd/user/quickshell.service` — its header in full. It is a long,
    deliberate document explaining why this repo's only authored user unit is shaped the way it
    is. Match its choices AND its reasoning; do not restate its reasoning as if it were new.
  </read_first>
  <precondition>`~/.config/systemd/user` is a real directory (not a symlink) — stow.sh already guarantees this, and it is what lets a per-file unit symlink land there.</precondition>
  <action>
Create `quickshell/.config/systemd/user/quickshell-bar-watchdog.service`.

Unit body:
  `[Unit]` — `Description=Quickshell bar watchdog (monitor-event surface recovery)`,
  `PartOf=graphical-session.target`, `After=graphical-session.target`,
  `StartLimitIntervalSec=60`, `StartLimitBurst=5`.
  `[Service]` — `Type=simple`, `Slice=app-graphical.slice`,
  `ExecStart=%h/.config/hypr/scripts/bar-watchdog.sh`, `Restart=on-failure`, `RestartSec=2`.
  **No `[Install]` section.**

Header, in quickshell.service's own register — each point is a decision, not decoration:
  - What this supervises and why it is a SECOND authored unit rather than a uwsm scope: it must
    outlive transient failures the same way the thing it watches does, and it is the mechanism
    for WINDOWS row 67.
  - No `[Install]`, quoting the reason quickshell.service gives: enabling would write a
    wants-symlink into the user's own systemd tree that never lands in the repo, contradicting
    this project's core value that the whole setup reproduces from a clone plus one script.
    Omitting the stanza makes that failure mode structurally impossible (`is-enabled` reports
    `static`). autostart.lua starts it explicitly instead, keeping that file the single readable
    inventory of what a session launches.
  - `PartOf=`/`After=graphical-session.target` and `Slice=app-graphical.slice` for the same
    reasons quickshell.service gives — `PartOf=` is what stops this unit at session end, which
    is what stops the restart policy retrying forever against a compositor that no longer
    exists; the slice keeps it in the same cgroup parent as everything else the session starts.
  - **Explicitly NOT `PartOf=`/`BindsTo=quickshell.service`**, and say why: this unit restarts
    that unit. Binding its own lifetime to the unit it restarts would tear the watchdog down
    inside its own recovery, and would couple two restart policies that must stay independent.
  - The exit-0 contract: bar-watchdog.sh's guard paths (no session, no socket, clean EOF at
    session exit) all `exit 0`, and per systemd.service(5) a clean exit does NOT restart under
    `Restart=on-failure` — so a headless or post-session invocation degrades to a logged skip
    and can never enter a restart cycle. Same reasoning, same conclusion as quickshell.service's
    guard paths.
  - No `ExecReload=`: there is no reload verb, and inventing one would create a second untested
    control path no caller asks for.
  - Note that logs go to the journal (`journalctl --user -u quickshell-bar-watchdog.service`),
    which is why the script writes to stdout and carries no log file or rotation logic — a
    deliberate divergence from quickshell-launch.sh's ~1MiB rotation, which exists because that
    script predates having a unit.

Then install and start it on this host:
  - `stow -R -t "$HOME" quickshell` from the repo root (`~/.config/systemd/user` is a REAL
    directory with per-file symlinks, so a new unit file does NOT appear on its own — unlike
    `~/.config/hypr/scripts`, which is a folded directory symlink and needs nothing).
  - `systemctl --user daemon-reload`.
  - `systemctl --user start quickshell-bar-watchdog.service`.

Do not edit `stow.sh` — a second unit file in that same already-guarded directory adds no new
exception to its documented three.
  </action>
  <verify>
    <automated>systemd-analyze verify quickshell/.config/systemd/user/quickshell-bar-watchdog.service</automated>
    <automated>grep -c '^\[Install\]' quickshell/.config/systemd/user/quickshell-bar-watchdog.service | grep -qx 0 &amp;&amp; echo NO_INSTALL_SECTION_OK</automated>
    <automated>readlink -f ~/.config/systemd/user/quickshell-bar-watchdog.service | grep -q dotfiles &amp;&amp; systemctl --user show -p ActiveState -p SubState -p NRestarts quickshell-bar-watchdog.service</automated>
    <automated>sleep 45; systemctl --user show -p ActiveState -p NRestarts quickshell-bar-watchdog.service | grep -q 'ActiveState=active' &amp;&amp; systemctl --user show -p NRestarts --value quickshell-bar-watchdog.service | grep -qx 0 &amp;&amp; echo STAYED_UP_OK</automated>
    <automated>journalctl --user -u quickshell-bar-watchdog.service --since '-2 min' --no-pager | grep -q 'bar-watchdog' &amp;&amp; echo JOURNAL_OK</automated>
    <automated>systemctl --user show -p ActiveState -p NRestarts --value quickshell.service | tr '\n' ' '</automated>
  </verify>
  <done>
    `systemd-analyze verify` passes with no findings; the file has no `[Install]` section; the
    unit symlink resolves into the repo; the unit is `active (running)` with `NRestarts=0` after
    45 seconds of real uptime; the journal carries its output; and `quickshell.service` is still
    active with its own `NRestarts` unchanged — the watchdog has not fired at anything.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire it into the session inventory, with its own carve-out note</name>
  <files>hypr/.config/hypr/config/autostart.lua</files>
  <read_first>
    `hypr/.config/hypr/config/autostart.lua` lines 1-16 (the D-15/D-35 "no entry added, removed
    or reordered" prohibition and its ordering rationale) and lines 62-100 (the retired
    watcher's own removal note, and the quickshell.service entry's precedent for carving a
    narrow, plan-authorised exception out of that prohibition).
  </read_first>
  <action>
Add exactly ONE entry to `hl.on("hyprland.start", ...)`, placed IMMEDIATELY AFTER the
`hl.exec_cmd("systemctl --user start quickshell.service")` line:

    hl.exec_cmd("systemctl --user start quickshell-bar-watchdog.service")

Reorder nothing else. Remove nothing. The placement is not arbitrary — it puts the watchdog
directly after the thing it watches, and directly below D-18-28's own removal note, which is
where a reader looking for "why is there a socket2 listener here again" will actually land.

Above the line, write a comment block covering all four of:
  1. What it starts and which defect it exists for (WINDOWS.md row 67 — the bar does not survive
     a monitor removal / no-outputs event; the shell stays healthy and reports the bar as
     visible while no surface exists).
  2. **The D-18-28 reversal, stated as a reversal**, pointing at the note a few lines above in
     this same file. D-18-28 retired the standalone socket2 listener because the SHELL could
     report the fullscreen intent itself; here the shell is the thing that fails, so the
     self-healing mechanism cannot live inside it. One long-running process was removed from the
     session then; one is added back now, for a structurally different reason.
  3. **The D-15/D-35 carve-out.** This is an entry ADDED, which that prohibition otherwise
     forbids. Name it as a deliberate, plan-authorised addition in the same register D-18-28
     used for its authorised removal — not an oversight, and not a widening of the prohibition's
     exception surface beyond this one line.
  4. Why `systemctl --user start` and not `uwsm app --`: the unit is what carries the restart
     policy and the session-teardown binding, exactly as for quickshell.service directly above;
     `--no-block` is deliberately not used, for the same reason that entry gives.
  </action>
  <verify>
    <automated>grep -c 'quickshell-bar-watchdog.service' hypr/.config/hypr/config/autostart.lua | grep -qx 1 &amp;&amp; echo ONE_ENTRY_OK</automated>
    <automated>grep -n 'systemctl --user start' hypr/.config/hypr/config/autostart.lua</automated>
    <automated>grep -c 'hl.exec_cmd' hypr/.config/hypr/config/autostart.lua | grep -qx 17 &amp;&amp; echo ENTRY_COUNT_16_TO_17_OK</automated>
    <automated>hypr/.config/hypr/scripts/hypr-lua-harness hypr/.config/hypr/config/autostart.lua 2>&amp;1 | tail -5 || luac -p hypr/.config/hypr/config/autostart.lua &amp;&amp; echo LUA_PARSE_OK</automated>
  </verify>
  <done>
    Exactly one watchdog entry exists; it appears immediately after the `quickshell.service`
    start line (confirmed by the line numbers the second check prints); the `hl.exec_cmd` count
    is 17, measured at 16 before this task; the file still parses as Lua; and the
    comment block states the D-18-28 reversal and the D-15/D-35 carve-out in the file itself,
    not only in the plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compositor → watchdog | socket2 event lines are compositor-supplied text arriving on an untrusted-shaped channel; they include window titles and classes on other event names |
| watchdog → user service manager | the watchdog can restart a user unit, i.e. it can terminate and respawn the desktop shell process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-N9B-01 | Tampering | bar-watchdog.sh event parsing | medium | mitigate | Every subprocess call is an argv LIST; no shell-interpreting invocation exists in the script. Non-monitor event names are ignored before their payload is examined at all, so titles/classes are never parsed. Inherits the retired listener's T-08-20 discipline. |
| T-N9B-02 | Denial of Service | recovery path | high | mitigate | Rate limit (min interval + max restarts per rolling window) proven by harness case E; INDETERMINATE health results never restart (case F); a genuinely broken shell therefore cannot be respawned in a loop. The unit's own `StartLimitIntervalSec=60`/`StartLimitBurst=5` bounds the watchdog itself. |
| T-N9B-03 | Denial of Service | socket read loop | medium | mitigate | EOF breaks the loop and exits 0 (harness case H) rather than spinning — the retired listener's documented core-pegging failure mode, reproduced as a guard. |
| T-N9B-04 | Spoofing | health check source | medium | mitigate | Health is read from `hyprctl layers -j`, the compositor's own view of live surfaces, never from `bar-visibility.sh status`, which WINDOWS row 67 records reporting a false `visible`. |
| T-N9B-SC | Tampering | package installs | low | accept | No package is installed by this plan — python3 stdlib only, verified present at 3.14.6. install.sh is untouched, so its reproducibility contract is unaffected. |
</threat_model>

<verification>
The trigger CANNOT be safely reproduced and nobody is to try. WINDOWS.md row 14 records that
`quickshell-doctor`'s headless-output add/remove test previously **SEGV-crashed this
compositor** during a DP-1 hotplug. Do not simulate monitor removal that way. Do not put the
operator's display to sleep. Do not run `quickshell-doctor` casually.

What IS checked, and is sufficient to ship:
- the listener starts and stays up under its unit (45s real uptime, `NRestarts=0`)
- it parses real socket2 framing — proven deterministically on a fixture socket (cases A, G)
- the health check reports the bar as present RIGHT NOW on the live session (`--check` →
  `present`, exit 0), which is independently true: `hyprctl layers -j` currently lists
  `quickshell-bar` on DP-1
- the exact-match trap is proven to fail closed against the live sibling namespaces (case B)
- the recovery path is exercised syntactically (python compile of the extracted body) and
  behaviourally against a PATH-shimmed `systemctl` (case D) — the real service is never
  restarted by any verification step
- the rate limiter is proven to suppress (case E) and the indeterminate path proven not to fire
  (case F)
- the unit passes `systemd-analyze verify` and carries no `[Install]` section
- `quickshell.service` ends the plan with the same `ActiveState` and `NRestarts` it started with
</verification>

<success_criteria>
- `hypr/.config/hypr/scripts/tests/test-bar-watchdog.sh` exits 0 with all of A-H passing
- `bar-watchdog.sh --check` prints `present` and exits 0 against the live session
- `quickshell-bar-watchdog.service` is active, `NRestarts=0`, no `[Install]` section,
  `systemd-analyze verify` clean
- autostart.lua has exactly one new entry, immediately after the `quickshell.service` line
- no NON-COMMENT reference to `bar-visibility` anywhere in the new script
- `Bar.qml`, `18-BAR-SOAK.md`, `18-GATE-02-RECORD.md`, `stow.sh`, `install.sh` and `waybar/` are
  all byte-unchanged at the end of the plan
- `hyprctl reload` / `hyprctl eval` / `hyprctl dispatch 'hl.dsp.*'` were never run
</success_criteria>

<output>
Create `.planning/quick/260812-n9b-add-an-event-driven-bar-watchdog-that-re/260812-n9b-SUMMARY.md` when done.

The SUMMARY MUST state all three of the following explicitly. These are not optional prose —
they are the honesty obligations this plan exists under, and the orchestrator records them:

1. **This reverses D-18-28.** 18-15 deleted this repo's standalone socket2 listener outright —
   "the standalone socket2 listener is deleted outright, not repointed — the fullscreen intent
   is now reported by the QML shell itself". State the reversal and the justification: D-18-28
   retired a listener because the shell could report the intent itself; here the shell is
   precisely the thing that fails, so a self-healing mechanism cannot live inside it.

2. **This adds ONE long-lived process to the desktop**, which changes QBAR-11's
   permanent-liveness inventory in `18-BAR-SOAK.md` Section one — that artifact currently states
   the bar carries exactly one permanent child process, and after this plan that is no longer
   true. **Do not edit `18-BAR-SOAK.md`.** State the consequence in the SUMMARY so the
   orchestrator records it and can decide where the correction lands.

3. **End-to-end recovery is UNPROVEN until the operator's display next sleeps.** Everything
   verified here is fixture-driven or live-read; the actual monitor-removal → surface-loss →
   restore path has not been observed once, because reproducing it is unsafe on this host
   (WINDOWS row 14, SEGV during a DP-1 hotplug). Say so plainly rather than implying the defect
   is closed. WINDOWS row 67 stays **open** — this plan works around it, it does not fix it.
</output>
