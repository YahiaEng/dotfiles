---
quick_id: 260818-ne8
date: 2026-08-18
mode: quick
---

# Quick Task 260818-ne8 — Two v4.0 carried debts

## Objective

Close two of the four items listed under "Known carried debt" in
`.planning/milestones/v4.0-MILESTONE.md`: the SC-3 surviving-prose item and the
Hyprland `options.jsonl` equivalence-exemption item. The other two carried items
(container allowlist rejection path proven only by dry-run; no milestone audit
run) are out of scope for this task.

## Measured ground truth (2026-08-18, before any edit)

**SC-3.** `retirement-check --all` reports `failed_classes=0` for all eight
registered surfaces — every blocking class is already clean. The surviving debt
is entirely in the non-failing `repo-prose` report class: **183 hits across
exactly five files.**

| File | Hits | Character |
|---|---|---|
| `.claude/CLAUDE.md` | 16 (waybar 8, swaync 4, swayosd 3, wleave 1) | Live agent-facing instruction — four entries are materially wrong |
| `docs/superpowers/plans/2026-07-15-ags-media-applet.md` | 127 | Historical design record of a retired surface |
| `docs/superpowers/specs/2026-07-15-ags-media-applet-design.md` | 31 | Historical design record of a retired surface |
| `docs/superpowers/specs/2026-08-11-qml-bar-athena-restoration-design.md` | 7 | waybar lineage inside shipped work |
| `docs/superpowers/specs/2026-07-28-hyprland-lua-config-migration-design.md` | 2 | waybar lineage inside shipped work |

Also measured and deliberately **not** touched:

- 11 `wleave` retirement-lineage comment lines in
  `hypr/.config/hypr/config/windowrules.lua` / `autostart.lua`. These pass the
  blocking tier by design: `scan_layer_window_rules` requires `DISPATCH_LAYER_RE`
  to match on the same line as the token, so a comment can never be a `[FAIL]`.
  They are deliberate provenance records and are worth more than their hit count.
- 1 line in `.claude/settings.local.json` — a frozen historical permission string
  quoting an old commit message. Not in `repo-prose` scope (not `.md`) and not
  in any blocking class.
- `retirement-check` naming its own registry targets — that is the checker's job,
  and it self-excludes via `SELF_PATH`.

**The four materially-wrong `.claude/CLAUDE.md` entries.** These do not merely
mention a retired name in passing; they present retired surfaces as the current
stack, which actively misdirects any agent reading the file:

- L7 — project description lists `swaync` and `wleave` among the components the
  theme pipeline propagates colors to.
- L36 — `swaync (SwayNotificationCenter) | 0.12.6 | Notification daemon + control
  center | Standard Hyprland notification daemon; already chosen.`
- L42 — `swayosd | 0.3.1 | ... | For the planned OSD indicators.`
- L73 — "Alternatives Considered" recommends `swayosd` over `wob`.

The other twelve hits (L35, L44, L45, L72, L100, L109-L112, L116) already
describe these surfaces correctly as retired, or are entries in a research
sources list. They stay.

**options.jsonl.** `VERIFICATION.md:283` records the exemption and names its own
two resolutions verbatim: *"resolved the day the baseline is re-captured against
the VM's Hyprland version, or moot if `options.jsonl` ever gains an
`ACCEPTED_ADDITIONS`-style mechanism like `binds.json` already has."*

- The mirror exists: `hypr-equivalence-check:528-550` (`ACCEPTED_ADDITIONS`,
  consumed at `:552-559`).
- The gap exists: `_compare_options_normalized`'s `b is None` / `l is None`
  branches at `:347-351` append an unconditional diff with no escape hatch.
- **The drift has not yet fired.** `hyprctl version` is 0.56.2; the baseline
  `MANIFEST.md:3` is 0.56.1; a live run right now is `PASS: 3 FAIL: 0`. The
  0.56.1 -> 0.56.2 bump added, renamed and removed **zero** of the 46 tracked
  keys. The `int` -> `bool` type-key notes come from the hyprlang -> Lua
  migration, not the version bump, and already normalize.

That measurement picks the resolution: **build the mechanism, do not re-capture
the baseline.** A re-capture is not merely premature, it is destructive — the
baseline's own `MANIFEST.md` records that a re-snapshot would overwrite the two
deliberately un-loosened `bindm` mouse-field records that 13.1-04 Task 3
explicitly forbade loosening, which is exactly why 14-10 Task 3 used a surgical
one-record insertion instead.

## Tasks

### Task 1 — Correct the four stale `.claude/CLAUDE.md` entries

**Files:** `.claude/CLAUDE.md`

Rewrite L7, L36, L42 and L73 so they describe the shipped v4.0 state. Each
retired surface is named in the past tense with its retiring requirement ID, so
the file keeps its lineage without asserting the surface is current. Do not
touch the twelve already-correct hits. Do not delete the research-sources list —
it is provenance for how the file was built, and rewriting it would falsify the
record of what was actually searched.

**Verify:** `grep -niE '\b(swaync|swayosd|wleave)\b' .claude/CLAUDE.md` returns
only lines that name the surface as retired.

**Done:** No line in `.claude/CLAUDE.md` presents a retired surface as current
or planned.

### Task 2 — Banner the four `docs/superpowers/` historical design docs

**Files:**
`docs/superpowers/plans/2026-07-15-ags-media-applet.md`,
`docs/superpowers/specs/2026-07-15-ags-media-applet-design.md`,
`docs/superpowers/specs/2026-08-11-qml-bar-athena-restoration-design.md`,
`docs/superpowers/specs/2026-07-28-hyprland-lua-config-migration-design.md`

Insert a short historical-record banner immediately after each file's title
heading, naming the retired surface(s), the retiring requirement, and where the
replacement lives. **Bodies stay verbatim** — these are the design record of
work that actually shipped, and rewriting them to erase the package names would
falsify what was designed at the time. The banner removes the only real residual
risk, which is a reader mistaking the doc for current guidance.

**Verify:** each of the four files opens with its original title followed by the
banner; `git diff --stat` shows only insertions for these four files.

**Done:** No reader can mistake any of the four docs for current stack guidance.

### Task 3 — Give `options.jsonl` an `ACCEPTED_OPTION_CHANGES` mechanism

**Files:** `hypr/.config/hypr/scripts/hypr-equivalence-check`, `VERIFICATION.md`

Add an enumerated, reviewable accepted-changes table to
`_compare_options_normalized`, mirroring `ACCEPTED_ADDITIONS`'s shape and its
enumerate-never-pattern-match discipline. It must cover both no-escape-hatch
arms — `b is None` (present in live only) and `l is None` (present in baseline
only) — and must ship **empty**, because nothing has drifted yet. An entry
carries a reason string and is reported as a `note`, never silently dropped.
Anything not listed still FAILs exactly as before.

Then update the `VERIFICATION.md:283` exemption row's status from `Provisional`
to resolved, citing the mechanism by name and stating which of the row's own two
named resolutions was taken and why.

**Verify:** `hypr-equivalence-check` still reports `PASS: 3 FAIL: 0` (no
behaviour change on the current tree, which is the point — an empty table must
be a no-op), and a synthetic added/removed key is accepted only when listed.

**Done:** The exemption's stated escape hatch exists in code and the
`VERIFICATION.md` row no longer reads as an open provisional item.

## Out of scope

- Re-capturing `.hypr-baseline/` (destructive; see ground truth above)
- The other two v4.0 carried-debt items
- The 11 lua lineage comments and the 1 `settings.local.json` line
- Rewriting the bodies of the four `docs/superpowers/` files
