---
phase: quick-260820-2yz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tmux/.config/tmux/tmux.conf
  - matugen/.config/matugen/templates/tmux-colors.conf
  - matugen/.config/matugen/config.toml
  - matugen/.config/matugen/templates/zellij-config.kdl
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/contract.sh
  - theme-engine/.config/theme-engine/lib/reload.sh
  - hypr/.config/hypr/scripts/retirement-check
  - install.sh
  - stow.sh
  - .gitignore
autonomous: false
requirements: [RETIRE-TMUX-01, RETIRE-TMUX-02, RETIRE-TMUX-03, RETIRE-TMUX-04]

estimate:
  tokens: 70000
  raw_tokens: 47000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`git grep -i tmux -- . ':!.planning' ':!hypr/.config/hypr/scripts/retirement-check'` returns NOTHING (exit 1) — zero surviving references outside the two deliberate historical records (D-06)."
    - "A theme render produces 20 contract files, none of them tmux-colors.conf, and ~/.local/state/theme/ carries no tmux-colors.conf afterwards (D-02)."
    - "zellij is still a fully themed surface: [templates.zellij], the zellij.kdl/kdl contract entry, contract_kdl_theme_pairs with BOTH extractor branches, lib/commit.sh's ln -sf wiring and stow.sh's ~/.config/zellij pre-create are all present and behaviourally unchanged (D-06)."
    - "Every reasoning comment that previously explained a SURVIVING mechanism by contrast with the retired multiplexer still states that reasoning, without naming it — the load-bearing WHY survives the deletion (D-06)."
    - "theme-parity reports 0 failed with a total below the 1897 baseline; colour-lint stays 150/0; theme-doctor reports 0 failed apart from the ONE predicted dangling-symlink failure that only the operator's host cleanup can clear (D-07)."
    - "retirement-check carries a tmux registry entry, so the consumer sweep is permanent and re-runnable rather than a one-off grep (RETIRE-TMUX-04)."
  artifacts:
    - "DELETED: tmux/ (whole stow package, 1 file, 83 lines)"
    - "DELETED: matugen/.config/matugen/templates/tmux-colors.conf (86 lines)"
    - "MODIFIED: matugen/.config/matugen/config.toml — [templates.tmux] block gone, [templates.zellij] header restated"
    - "MODIFIED: theme-engine/.config/theme-engine/contract.json — 21 -> 20 files entries"
    - "MODIFIED: theme-engine/.config/theme-engine/lib/contract.sh — tmux-set gone from BOTH extractors"
    - "MODIFIED: theme-engine/.config/theme-engine/lib/reload.sh — hook, call site and header comment gone; role-named tombstone left behind"
    - "MODIFIED: install.sh, stow.sh, .gitignore — package/stow/ignore wiring gone"
    - "MODIFIED: hypr/.config/hypr/scripts/retirement-check — tmux registry entry (pending)"
    - "CREATED: .planning/quick/260820-2yz-retire-tmux-now-that-zellij-is-the-theme/260820-2yz-SUMMARY.md"
  key_links:
    - "BOTH extractors in lib/contract.sh (contract_extract_names AND contract_extract_values) — removing tmux-set from only one is the silent false-pass that file's own comments warn about twice (D-01)."
    - "contract.json entry count -> theme-doctor's state-manifest gate -> a theme-apply re-render: dropping the entry without re-rendering leaves an UNACCOUNTED tmux-colors.conf in the state dir and turns theme-doctor red (measured: theme-doctor:113)."
    - "~/.config/tmux/tmux.conf (a symlink into the repo) -> stow-link-check's dangling sweep: deleting the repo tree makes it dangle, and only the operator's host cleanup clears it (D-07)."
    - "The zellij kdl format -> contract_kdl_theme_pairs -> both `kdl)` branches: the one shared emitter that replaced the mirrored-regex idiom must survive the removal of its foil."
    - "retirement-check's blocking classes matugen-templates / install-stow-lists / cross-package-refs are what mechanically catch the survivor-comment trap in zellij-config.kdl, stow.sh, install.sh, contract.sh and reload.sh."
---

<objective>
Retire tmux from the repo now that zellij (quick task 260820-0ha) is the themed multiplexer.

Purpose: tmux is redundant. Retiring it BEFORE the follow-on zellij plugin work means that work lands in a clean tree. This repo has a scar here — the eww retirement was left half-done and needed a later quick task (260725-vu6) to sweep an orphaned contract entry and stale layer rules, which is where PROJECT.md's "Consumer-check before retiring a toolkit" decision came from. This plan honours it with the repo's own purpose-built checker rather than a one-off grep.

Output: a clean-removal commit series, a permanent retirement-check registry entry, measured gate numbers, and a documented host-cleanup handoff for the operator.

**Requirements**
- RETIRE-TMUX-01 — remove the tmux surface: stow package, matugen template, `[templates.tmux]`, the contract.json entry and the `tmux-set` format from BOTH extractors, the reload hook.
- RETIRE-TMUX-02 — remove the install/stow/.gitignore wiring (repo-side only).
- RETIRE-TMUX-03 — preserve the load-bearing reasoning carried by comments that explained SURVIVING mechanisms by contrast with tmux.
- RETIRE-TMUX-04 — prove zero surviving references mechanically, and leave that proof re-runnable.

**Decisions (locked by the operator, non-negotiable)**
- D-01 — `tmux-set` comes out of BOTH extractors in `lib/contract.sh`. The `kdl` format (and `contract_kdl_theme_pairs`), `fish-set`, `kitty-kv`, `env-kv` and every other format STAY.
- D-02 — contract.json goes 21 -> 20 entries.
- D-03 — `theme_engine_reload_tmux` comes out of `lib/reload.sh`. zellij needs no hook (measured — it live-reloads itself), so reload.sh gains nothing back.
- D-04 — repo retirement is NOT host uninstall. Remove `tmux` from PACMAN_PKGS and `tmux-plugin-manager` from AUR_PKGS, but run no pacman/paru and assume nothing about whether the operator wants the binary gone.
- D-05 — `stow.sh`: remove the `tmux` PACKAGES entry, the `~/.config/tmux/plugins` pre-create and the headless tpm fetch. Leave every other pre-create alone — especially `~/.config/zellij`.
- D-06 — nothing under `zellij/`-facing wiring may regress: `zellij-config.kdl` (beyond the comment rewrite), `lib/commit.sh`'s symlink wiring, `[templates.zellij]`, the `kdl` contract format. A gate asserts zellij is still intact — the mirror image of the gate the zellij task used to assert tmux was untouched.
- D-07 — the operator's live host has tmux stowed with tpm plugins fetched. Document the exact cleanup commands; do not run them.

**TRACER_MODE does not apply.** This is a removal from an already-proven architecture; there is no thin end-to-end slice to prove. Tasks are ordered delete -> restate -> re-render/measure, which is the shape a deletion actually takes.

**Comment-text discipline note for the executor:** this plan and its SUMMARY live under `.planning/`, which every acceptance grep in it excludes by pathspec. The plan's own prose can therefore never trip its own gates. The second excluded path, `hypr/.config/hypr/scripts/retirement-check`, is excluded for the reason that script states about itself in its Class 7 comment: a registry naming the surfaces it checks is not a reference to them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

Measured ground truth gathered during planning — do NOT re-measure these before starting, they were taken on this host minutes ago:

| Fact | Value |
|------|-------|
| Tracked files referencing tmux | 10 (`git grep -l -i tmux -- . ':!.planning'`) |
| contract.json `.files \| length` | 21 |
| theme-doctor baseline | 596 passed, 0 failed |
| theme-parity baseline | 1897 / 0 failed |
| colour-lint baseline | 150 / 0 failed |
| `tmux/` tree | exactly one file, `tmux/.config/tmux/tmux.conf`, 83 lines |
| `tmux-colors.conf` template | 86 lines |
| `theme_engine_reload_tmux` | reload.sh lines 184-200, header comment 157-183, call site + comment block 84-99 |
| `tmux-set` name extractor | contract.sh lines 178-190 |
| `tmux-set` value extractor | contract.sh lines 445-460 |
| Live host | `~/.config/tmux/tmux.conf` is a SYMLINK to `../../dotfiles/tmux/.config/tmux/tmux.conf`; `~/.config/tmux/plugins/` holds 4 real cloned plugin repos |
| Live state dir | carries `tmux-colors.conf` (4.8K) and `zellij.kdl` (6.8K); current theme `catppuccin`, mode `dark` |
| theme-doctor state-manifest gate | theme-doctor:113 fails on any UNACCOUNTED entry under `$STATE_DIR` |
| retirement-check | `hypr/.config/hypr/scripts/retirement-check`, 766 lines, 16 reference classes, registry at `REGISTRY_RAW` |

Files to read first (all measured above — read only the ranges you are editing):
@theme-engine/.config/theme-engine/lib/contract.sh
@theme-engine/.config/theme-engine/lib/reload.sh
@matugen/.config/matugen/templates/zellij-config.kdl
@matugen/.config/matugen/config.toml
@stow.sh
@install.sh
@hypr/.config/hypr/scripts/retirement-check
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the tmux surface and its repo wiring</name>
  <files>tmux/.config/tmux/tmux.conf, matugen/.config/matugen/templates/tmux-colors.conf, matugen/.config/matugen/config.toml, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/contract.sh, theme-engine/.config/theme-engine/lib/reload.sh, install.sh, stow.sh, .gitignore</files>
  <read_first>
    theme-engine/.config/theme-engine/lib/contract.sh lines 50-60, 165-200, 425-470 (the format-list comment and the two `tmux-set` branches).
    theme-engine/.config/theme-engine/lib/reload.sh lines 80-120 and 150-205 (call site + comment block, then header comment + function).
    matugen/.config/matugen/config.toml lines 78-92. install.sh lines 76-96 and 440-456. stow.sh lines 30-32, 60-72, 294-322. .gitignore lines 30-38.
  </read_first>
  <action>
Perform the mechanical removal only. Restating survivor comments is Task 2 — do not do it here, and do not be alarmed that this commit leaves prose referring to something the tree no longer contains.

Delete outright: the whole `tmux/` stow package (`git rm -r tmux`, one file), and `matugen/.config/matugen/templates/tmux-colors.conf`.

In `matugen/.config/matugen/config.toml` (D-01): remove the `[templates.tmux]` table together with the comment header that introduces it. Stop at the boundary — the `[templates.zellij]` block immediately below stays exactly as it is for now.

In `theme-engine/.config/theme-engine/contract.json` (D-02): remove the object whose name is the tmux colours file. The `zellij.kdl` object that follows it becomes the last element of `files`, so fix the trailing comma; the result must parse and count 20.

In `theme-engine/.config/theme-engine/lib/contract.sh` (D-01, the highest-risk edit in this plan): remove the `tmux-set` case arm from `contract_extract_names` AND the `tmux-set` case arm from `contract_extract_values`. Removing one and not the other is precisely the silent false-pass the surrounding comments warn about twice, and it is the single failure this task most needs to avoid. Also drop the retired format tag from the format enumeration in the `contract_format` header comment. Leave every other case arm in both extractors untouched — `fish-set`, `kitty-kv`, `hypr-vars`, `env-kv`, `toml`, `json`, `css-vars`, `scss-vars`, `css-literal`, `gtk-css`, `lua-table` and `kdl` all stay, and `contract_kdl_theme_pairs` stays exactly as written.

In `theme-engine/.config/theme-engine/lib/reload.sh` (D-03): remove the call site, the comment block introducing it, the function's header comment and the function body. Leave a role-named tombstone in the fan-out where the call stood, matching the shape this file already uses four times (search it for the RETIRE-02, RETIRE-03, RETIRE-04 and RETIRE-06 tombstones and copy their register): they describe the retired thing by its ROLE — "the retired bar", "the retired notification daemon", "the standalone GTK3 OSD daemon" — and deliberately never name it. Follow that convention exactly; a tombstone that names the surface is a surviving reference that Task 3's sweep will flag, and the existing four are why the sweep passes for the surfaces already retired. Record in the tombstone that the hook was removed by quick task 260820-2yz and that nothing replaces it.

In `install.sh` (D-04): remove `tmux` from `PACMAN_PKGS` with its comment, and `tmux-plugin-manager` from `AUR_PKGS` with its comment. Install or uninstall NOTHING — this edits two shell arrays and nothing else. Do not invoke pacman or paru at any point in this plan.

In `stow.sh` (D-05): remove the `tmux` entry from `PACKAGES`, the `~/.config/tmux/plugins` pre-create block, and the headless tpm plugin-fetch block near the end (guard, mktemp socket-dir isolation, fetch and cleanup — the whole section including its `else` branch). Before deleting the pre-create block, read the fish pre-create comment immediately above it and confirm the fold-bug reasoning survives there; if the fish version is thinner than the block being deleted, port the missing sentence into Task 2's zellij pre-create restatement rather than losing it. Leave every other pre-create alone — the `~/.config/zellij` one especially.

In `.gitignore`: remove the tmux plugin-ignore block including its comment header.

Commit as a single clean removal.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && python3 - <<'PYEOF'
import os, re, subprocess, sys
def sh(*a):
    return subprocess.run(list(a), capture_output=True, text=True)
fails = []
def want(cond, msg):
    if not cond: fails.append(msg)

want(not os.path.exists('tmux'), 'the tmux stow package still exists')
want(not os.path.exists('matugen/.config/matugen/templates/tmux-colors.conf'), 'the matugen template still exists')
want(sh('jq','-e','(.files|length) == 20 and ([.files[]|select(.name|test("tmux"))]|length) == 0 '
        'and ([.files[]|select(.name == "zellij.kdl" and .format == "kdl")]|length) == 1',
        'theme-engine/.config/theme-engine/contract.json').returncode == 0,
     'contract.json is not 20 entries / still names the retired file / lost the zellij kdl entry')

# Comment-stripped extractor-arm census. A `grep -c` over the raw file would
# count the prose ABOUT these arms as if it were an arm (this repo has been
# bitten): strip `#` lines first, then anchor on a case-arm line exactly.
src = open('theme-engine/.config/theme-engine/lib/contract.sh').read().splitlines()
body = '\n'.join(l for l in src if not l.lstrip().startswith('#'))
def arms(tag):
    return len(re.findall(r'^\s*' + re.escape(tag) + r'\)\s*$', body, re.M))
want(arms('tmux-set') == 0, 'retired format still present in %d extractor arm(s) — BOTH must go' % arms('tmux-set'))
for tag, expect in (('fish-set', 2), ('kdl', 2), ('kitty-kv', 2), ('env-kv', 2)):
    want(arms(tag) == expect, 'surviving format %s: %d arm(s), want %d' % (tag, arms(tag), expect))
want('contract_kdl_theme_pairs()' in body, 'shared kdl emitter lost')

for f in ['stow.sh','install.sh','theme-engine/.config/theme-engine/lib/contract.sh',
          'theme-engine/.config/theme-engine/lib/reload.sh']:
    if sh('bash','-n',f).returncode: fails.append('bash -n failed: ' + f)
want('theme_engine_reload_tmux' not in open('theme-engine/.config/theme-engine/lib/reload.sh').read(),
     'the reload hook or its call site survives in reload.sh')

print('\n'.join(fails) if fails else 'TASK 1 VERIFY PASS — 20 contract entries, retired format in 0 arms, survivors at 2 each')
sys.exit(1 if fails else 0)
PYEOF</automated>
  </verify>
  <done>The tmux stow package and matugen template are gone from the tree; contract.json parses at exactly 20 files entries with the zellij/kdl entry intact; the retired format tag appears in ZERO extractor arms after comment-stripping while fish-set, kdl, kitty-kv and env-kv each still appear in exactly two; the reload hook and its call site are gone with a role-named tombstone left behind; stow.sh, install.sh, contract.sh and reload.sh all pass `bash -n`; one commit, and `git show --stat` on it reads as deletions — roughly 169 lines of whole-file deletion plus ~150 lines removed across the seven edited files, with near-zero insertions.</done>
</task>

<task type="auto">
  <name>Task 2: Restate the survivor comments that explained themselves by contrast</name>
  <files>matugen/.config/matugen/templates/zellij-config.kdl, matugen/.config/matugen/config.toml, theme-engine/.config/theme-engine/lib/contract.sh, theme-engine/.config/theme-engine/lib/reload.sh, stow.sh, install.sh</files>
  <read_first>
    matugen/.config/matugen/templates/zellij-config.kdl lines 1-115 (six reference sites: the KDL-has-no-include paragraph, the no-reload-hook paragraph, the role-assignment rule, `default_shell`, `copy_command`, the session-serialization note and the keybinds note).
    stow.sh's zellij pre-create comment; install.sh's zellij PACMAN_PKGS comment; config.toml's `[templates.zellij]` header; contract.sh's `contract_kdl_theme_pairs` header and the `kdl)` value-extractor comment; reload.sh's zellij no-hook paragraph.
  </read_first>
  <action>
These six files carry comments whose REASONING is load-bearing and must survive, but which currently express that reasoning by contrast with a tool the repo no longer contains (D-06, RETIRE-TMUX-03). Rewrite, do not delete. Deleting them would destroy the record of WHY the zellij architecture is shaped as it is, which is the entire reason this retirement was planned instead of done by hand.

Two rules govern every rewrite:
1. Restate the reasoning in ABSOLUTE terms wherever the contrast partner is gone. "It live-reloads a running session by itself, measured this session through commit.sh's rsync inode replacement" carries the same information as "unlike the other one, which needs a hook" — and survives the retirement.
2. Where a surviving sibling makes the same point, retarget the contrast to it. `fish` still gets no reload hook because it re-reads at shell start; `fish-set` still carries the mirrored-regex drift warning; the fisher and quickshell pre-creates still demonstrate the stow fold bug. Prefer a live contrast to an absolute restatement where one exists.

Site by site:

`zellij-config.kdl` — the header paragraph explaining that KDL has no include mechanism currently lists the fragment-sourcing apps as evidence; keep the evidence list, dropping only the retired member. The no-reload-hook paragraph currently ends by pointing at the hook function in reload.sh for the full contrast; that function no longer exists, so the paragraph must carry its own reasoning: zellij watches its own config and live-reloads through the symlink-plus-rsync path measured this session, and the byte-count evidence already in this header is what proves it. The role-assignment rule cites three files that document the container-role category error; drop the retired one, keep the other two — the rule itself is unchanged and stays stated in full. `default_shell` and the keybinds note currently borrow another config's reasoning; restate each in its own terms (fish is declared in PACMAN_PKGS so a reproduced machine always has it; zellij's defaults are discoverable from the status bar and nothing in scope justifies overriding them). `copy_command` and the session-serialization note describe what plugins used to provide; restate as what zellij provides natively, which is the actual point being made.

`config.toml` — the `[templates.zellij]` header's closing contrast becomes the absolute statement: no post_hook because lib/reload.sh is the sole fan-out owner (D-04), and no hook at all because zellij watches its own config.

`contract.sh` — `contract_kdl_theme_pairs`'s header explains that one shared emitter is a deliberate improvement over a pair of hand-mirrored regexes that can drift. `fish-set` still IS that pair and still carries the warning in its own comments, so retarget the contrast to `fish-set` alone. Same for the `kdl)` value-extractor comment. The drift-is-a-silent-false-pass warning must survive verbatim in substance.

`reload.sh` — the zellij no-hook paragraph currently contrasts against two siblings, one of which is gone. Keep the fish contrast (fish re-reads at shell start, so existing shells are deliberately not re-themed) and restate zellij's own reason absolutely, preserving the "this is a MEASURED decision, not an oversight" framing and the PTY-probe evidence. Do not name the retired surface; the tombstone added in Task 1 is the one place the history is recorded, and it is role-named.

`stow.sh` — the `~/.config/zellij` pre-create comment cites the deleted sibling twice, once for the fold-bug idiom and once for why placement before the PACKAGES loop is load-bearing. Both claims are true independently: restate the fold-bug reasoning inline (stow folds an unclaimed `~/.config/X` into a single whole-directory symlink, after which anything written there lands inside the cloned repo tree and breaks the git-clean invariant theme-doctor's state-manifest gate exists to protect) and state the placement rule as its own rule rather than as a citation. Retarget the idiom list to the fisher and quickshell pre-creates, which survive. If Task 1 found the fish block thinner than the deleted one, this is where the missing sentence lands.

`install.sh` — the zellij PACMAN_PKGS comment contrasts against the retired entry and its AUR plugin manager. Restate absolutely: zellij needs no plugin manager, no git clone and no headless fetch step, because session serialization and pane-viewport serialization are built-in options — which is why nothing was added to stow.sh's plugin-fetch section for it. That last clause now describes a section that no longer exists, so say instead that stow.sh needs no fetch step for it at all.

Then register the retirement so the sweep is permanent (RETIRE-TMUX-04). In `hypr/.config/hypr/scripts/retirement-check`, add one line to `REGISTRY_RAW` after the `eww` entry and before the self-test fixture entry, using the four-field pipe format: surface name, status `pending`, own-tree `tmux/` (recording the tree that was deleted, matching how the four surfaces with deleted trees already record theirs), and this quick task's id as the requirement token. Add three or four lines to the registry's explanatory comment block stating why this one ships `pending` rather than `retired`: the `host-package` class is a BLOCKING class and runs `pacman -Q` against the surface name, the package is deliberately still installed on the host (D-04/D-07), so marking it retired now would arm the blocking tier against a reference the repo has no authority to clear. Note that the flip to `retired` is the operator's follow-up once the host package is gone — exactly the `eww`/`wlogout` precedent the block above it already describes.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && python3 - <<'PYEOF'
import subprocess, sys
def sh(*a):
    return subprocess.run(list(a), capture_output=True, text=True)
fails = []
for f in ['hypr/.config/hypr/scripts/retirement-check','stow.sh','install.sh',
          'theme-engine/.config/theme-engine/lib/contract.sh',
          'theme-engine/.config/theme-engine/lib/reload.sh']:
    if sh('bash','-n',f).returncode: fails.append('bash -n failed: '+f)
RC='hypr/.config/hypr/scripts/retirement-check'
if 'tmux' not in sh(RC,'--list').stdout: fails.append('tmux missing from retirement-check registry')
st = sh(RC,'--self-test')
print(st.stdout.strip().splitlines()[-1] if st.stdout.strip() else '(no self-test output)')
if st.returncode: fails.append('retirement-check --self-test exit %d' % st.returncode)
BLOCKING={'own-tree','layer-window-rules','autostart','keybinds','contract-json',
          'matugen-templates','checker-internals','test-fixtures','cross-package-refs',
          'install-stow-lists','systemd-units','dbus-activation','xdg-autostart','host-package'}
seen={}
for line in sh(RC,'tmux','--root','.').stdout.splitlines():
    if '] tmux/' not in line: continue
    tag, rest = line.split('] tmux/', 1)
    cls = rest.split(':', 1)[0]
    if cls not in BLOCKING or 'SKIP' in tag: continue
    seen[cls] = int(rest.split(':', 1)[1].strip().split()[0])
print('blocking classes:', sorted(seen.items()))
for cls, n in sorted(seen.items()):
    if n and cls != 'host-package': fails.append('SURVIVING REFERENCES in %s: %d' % (cls, n))
print('\n'.join(fails) if fails else 'TASK 2 VERIFY PASS (host-package=%s is the only permitted non-zero)' % seen.get('host-package','skipped'))
sys.exit(1 if fails else 0)
PYEOF</automated>
  </verify>
  <done>Every rewritten comment states its reasoning without naming the retired surface, and no reasoning was lost — the KDL-has-no-include evidence, the fold-bug explanation, the mirrored-regex drift warning, the container-role rule and the measured live-reload evidence all still read as complete arguments. `retirement-check --self-test` still replays its five fixtures with the expected verdicts. `retirement-check tmux --root .` reports ZERO references in every blocking class except `host-package`, which reports the still-installed host package and nothing else. One commit.</done>
</task>

<task type="auto">
  <name>Task 3: Re-render, measure the gates, record the predicted red</name>
  <files>.planning/quick/260820-2yz-retire-tmux-now-that-zellij-is-the-theme/260820-2yz-SUMMARY.md</files>
  <read_first>None — this task runs tools and records numbers.</read_first>
  <action>
Re-render the theme so the live state dir stops carrying the retired file. Run `theme-apply catppuccin` (the current theme, mode dark — measured during planning). This is permitted and it is REQUIRED, not optional: theme-doctor's state-manifest gate fails on any entry under the state dir that contract.json no longer accounts for, so dropping the contract entry without re-rendering leaves the gate red for a reason unrelated to the deletion's correctness. The fan-out touches hyprctl, kitty, GTK, walker and Zen — never quickshell. Do not restart quickshell, do not spawn any compositor surface, do not take screenshots, do not run qml6.

Then run the three static gates and record the ACTUAL numbers rather than the expected ones: `theme-engine/.config/theme-engine/theme-doctor`, `theme-engine/.config/theme-engine/theme-parity`, `hypr/.config/hypr/scripts/colour-lint`. Baselines measured during planning: 596/0, 1897/0, 150/0. theme-doctor and theme-parity should both come in BELOW their baseline totals because one contract file stopped existing; colour-lint should be unchanged at 150/0 because this plan touches no QML.

One failure is PREDICTED and is a real result, not a defect to route around: `~/.config/tmux/tmux.conf` on the live host is a symlink into the repo tree deleted in Task 1, so stow-link-check's dangling sweep will now report it. Only the operator's host cleanup can clear it (D-07 forbids doing it here). Confirm the failure is exactly that one, name it in the SUMMARY, and state the command that clears it. If theme-doctor reports any OTHER failure, stop and report it — that is a genuine defect in this plan's execution.

Assert the state dir: no tmux colours file, `zellij.kdl` still present and non-empty.

Assert the zellij surface is intact end to end (D-06) — the mirror of the gate the zellij task used to assert tmux was untouched: `[templates.zellij]` in config.toml with both paths, the `zellij.kdl`/`kdl` entry in contract.json, `contract_kdl_theme_pairs` plus both `kdl)` arms in contract.sh, the `ln -sf` wiring in lib/commit.sh, the `~/.config/zellij` pre-create in stow.sh, and `~/.config/zellij/config.kdl` still resolving to the state-dir file.

Write the SUMMARY. It must carry: the measured before/after numbers for all three gates; the named predicted failure and its exact clearing command; the `git show --stat` shape of each commit; the retirement-check registry status (`pending`) with the condition for flipping it to `retired`; and the full host-cleanup command block for the operator, written to be pasted verbatim. That block is: remove the now-dangling stow link and the orphaned tpm plugin clones with `rm -rf ~/.config/tmux` (measured contents: one dangling `tmux.conf` symlink plus a real `plugins/` directory holding four cloned plugin repos — tmux-sensible, tmux-resurrect, tmux-continuum, tmux-yank); optionally uninstall the binaries with `sudo pacman -Rns tmux` and `paru -Rns tmux-plugin-manager`; and, once the package is gone, flip the retirement-check registry entry from `pending` to `retired` so the blocking tier arms and theme-doctor gains a permanent PASS block. Mark the package removal as OPTIONAL and entirely the operator's call — the repo has stopped declaring it, which is a different thing from wanting it gone.

Commit the SUMMARY and any state files the workflow updates.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && python3 - <<'PYEOF'
import os, subprocess, sys
H = os.path.expanduser('~')
def sh(*a):
    return subprocess.run(list(a), capture_output=True, text=True)
fails = []
def want(cond, msg):
    if not cond: fails.append(msg)

want(not os.path.exists(H+'/.local/state/theme/tmux-colors.conf'), 'retired colours file still in the state dir')
want(os.path.getsize(H+'/.local/state/theme/zellij.kdl') > 0, 'zellij.kdl missing or empty in the state dir')
want(os.path.realpath(H+'/.config/zellij/config.kdl').endswith('state/theme/zellij.kdl'), '~/.config/zellij/config.kdl no longer resolves to the state-dir file')
for path, needle, label in [
    ('matugen/.config/matugen/config.toml', '[templates.zellij]', 'config.toml zellij template'),
    ('theme-engine/.config/theme-engine/lib/commit.sh', 'zellij.kdl', 'commit.sh ln -sf wiring'),
    ('stow.sh', 'config/zellij', 'stow.sh zellij pre-create'),
    ('theme-engine/.config/theme-engine/lib/contract.sh', 'contract_kdl_theme_pairs', 'shared kdl emitter'),
]:
    want(needle in open(path).read(), 'zellij anchor lost: ' + label)
want(sh('jq','-e','[.files[]|select(.name=="zellij.kdl" and .format=="kdl")]|length == 1',
        'theme-engine/.config/theme-engine/contract.json').returncode == 0, 'contract.json zellij/kdl entry lost')

g = sh('git','grep','-n','-i','tmux','--','.',':!.planning',':!hypr/.config/hypr/scripts/retirement-check')
want(g.returncode == 1 and not g.stdout.strip(), 'SURVIVING REFERENCES:\n' + g.stdout)

for tool, base in [('theme-engine/.config/theme-engine/theme-parity', 1897),
                   ('hypr/.config/hypr/scripts/colour-lint', 150),
                   ('theme-engine/.config/theme-engine/theme-doctor', 596)]:
    out = sh(tool)
    tail = [l for l in out.stdout.splitlines() if l.strip()][-3:]
    print('== %s (baseline %d) ==' % (tool.split('/')[-1], base))
    print('\n'.join(tail))
    print('   failing lines:')
    for l in out.stdout.splitlines():
        if '[FAIL]' in l: print('   ' + l.strip())
print('\n'.join(fails) if fails else 'TASK 3 VERIFY PASS — read the gate tails above and confirm the only FAIL is the dangling .config/tmux link')
sys.exit(1 if fails else 0)
PYEOF</automated>
    <human-check>Read the three gate tails: theme-parity must end 0 failed with a total under 1897; colour-lint must end 150/0; theme-doctor must end with exactly one failure and it must be the dangling `.config/tmux/tmux.conf` link.</human-check>
  </verify>
  <done>The state dir carries no tmux colours file and still carries a non-empty zellij.kdl reachable through `~/.config/zellij/config.kdl`; the repo-wide grep returns nothing outside `.planning/` and retirement-check's own registry; theme-parity and colour-lint are green with theme-parity below its 1897 baseline; theme-doctor's only failure is the named, predicted dangling symlink; the SUMMARY records every measured number, the paste-ready host-cleanup block and the registry-flip condition. Committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator checkpoint — host cleanup, green gates, zellij unharmed</name>
  <what-built>
tmux is retired from the repo. The stow package and matugen template are deleted; `[templates.tmux]`, the contract.json entry (21 -> 20) and the `tmux-set` format in BOTH extractors are gone; `theme_engine_reload_tmux` and its call site are gone with a role-named tombstone in their place; the install, stow and .gitignore wiring is gone. Six survivor files had their tmux-referencing comments restated so their reasoning survives without naming a tool the repo no longer contains. `retirement-check` now carries a `tmux` registry entry (status `pending`) so the consumer sweep is permanent and re-runnable — the answer to the eww half-retirement scar.

Nothing was uninstalled and nothing on your host was touched or deleted.
  </what-built>
  <how-to-verify>
Step 1 — host cleanup (this is what clears the one predicted red gate). The repo no longer ships the tmux stow package, so `~/.config/tmux/tmux.conf` is now a dangling symlink and `~/.config/tmux/plugins/` holds four orphaned tpm clones. Clear both:

    rm -rf ~/.config/tmux

Step 2 — re-run the gates and confirm all three are green:

    ~/.config/theme-engine/theme-doctor | tail -3
    ~/.config/theme-engine/theme-parity | tail -3
    ~/.config/hypr/scripts/colour-lint  | tail -3

theme-doctor should now report 0 failed. If it still reports the dangling link, step 1 did not take.

Step 3 — confirm zellij is untouched. Open a kitty window, run `zellij`, split a pane, then switch themes from the walker picker and confirm the status bar, powerline wedges and pane frames all re-theme live with the session open. This is the same check that closed the zellij task; it is here to prove the deletion did not clip it.

Step 4 — decide on the binaries. The repo has stopped DECLARING tmux, which is not the same as wanting it gone from this machine. If you do want it gone:

    sudo pacman -Rns tmux
    paru -Rns tmux-plugin-manager

Both are optional and entirely your call. If you run them, say so — the retirement-check registry entry can then flip from `pending` to `retired`, which arms its blocking tier and gives theme-doctor a permanent PASS block for the surface. Until then `pending` is correct, because the `host-package` class would otherwise fail on a package the repo has no authority to remove.

Step 5 — sanity-check the sweep yourself if you want the receipt:

    ~/.config/hypr/scripts/retirement-check tmux --root ~/dotfiles
  </how-to-verify>
  <resume-signal>Type "approved" once theme-doctor is 0 failed and zellij still re-themes live. Say whether you uninstalled the packages so the registry entry can be flipped, or describe anything that came back wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree -> live host (`~/.config`, `~/.local/state/theme`) | stow symlinks and commit.sh's rsync cross here; a repo-side deletion silently changes host state |
| repo package lists -> pacman/paru | install.sh declares what a fresh machine installs; editing it changes future machines, never this one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2yz-01 | Tampering | contract.json / lib/contract.sh | high | mitigate | An orphaned contract entry or a half-removed format red-lights theme-doctor/theme-parity — the exact eww scar (260725-vu6). Task 1's verify asserts `.files\|length == 20` and comment-stripped extractor-arm counts; Task 2's sweep asserts the `contract-json` class reports zero. |
| T-2yz-02 | Tampering | lib/contract.sh, BOTH extractors | critical | mitigate | Removing `tmux-set` from one extractor and not the other is a SILENT false-pass in theme-parity — the failure mode that file's own comments warn about twice. Task 1's verify counts arms in the comment-stripped source and requires 0 for the retired tag and exactly 2 each for fish-set/kdl/kitty-kv/env-kv. |
| T-2yz-03 | Denial of Service | live host `~/.config/tmux` | medium | accept | Deleting the stow package leaves a dangling symlink that fails stow-link-check. Accepted deliberately: D-07 makes host cleanup the operator's step. Mitigated by PREDICTING the failure by name in Task 3 and putting the one-line clearing command first in the checkpoint. |
| T-2yz-04 | Denial of Service | ~/.local/state/theme | medium | mitigate | A stale rendered file the contract no longer accounts for fails theme-doctor's state-manifest gate (theme-doctor:113). Task 3 re-runs theme-apply, which lets commit.sh's `rsync --delete` clear it. |
| T-2yz-05 | Tampering | zellij surface (template, contract format, commit.sh wiring, stow pre-create) | high | mitigate | Collateral damage from an over-broad deletion is the main hazard of a same-domain retirement. Task 3 asserts six independent zellij anchors plus a live resolving symlink — the mirror of the gate the zellij task used to assert tmux was untouched. |
| T-2yz-06 | Repudiation | survivor comments | medium | mitigate | Deleting comments rather than restating them would destroy the record of WHY the zellij architecture is shaped as it is. Task 2 rewrites all six sites under two explicit rules, and reload.sh keeps a role-named tombstone matching the four already in that file. |
| T-2yz-SC | Tampering | package installs | n/a | accept | No package-legitimacy gate applies: this plan INSTALLS nothing. It removes two entries from two shell arrays and runs no pacman, paru, npm, pip or cargo command at any point. |
</threat_model>

<verification>
Run in order, after Task 3's commit:

1. `git grep -n -i tmux -- . ':!.planning' ':!hypr/.config/hypr/scripts/retirement-check'` — must print nothing (exit 1).
2. `jq '.files | length' theme-engine/.config/theme-engine/contract.json` — must print 20.
3. `hypr/.config/hypr/scripts/retirement-check tmux --root .` — every blocking class 0 except `host-package`.
4. `hypr/.config/hypr/scripts/retirement-check --self-test` — exit 0, five fixtures as expected.
5. `theme-engine/.config/theme-engine/theme-parity` — 0 failed, total < 1897.
6. `hypr/.config/hypr/scripts/colour-lint` — 150 passed, 0 failed.
7. `theme-engine/.config/theme-engine/theme-doctor` — total < 596, and 0 failed apart from the single predicted dangling-symlink failure.
8. `test ! -e ~/.local/state/theme/tmux-colors.conf && test -s ~/.local/state/theme/zellij.kdl`.

Never during this plan: qml6, any compositor surface, screenshots, a quickshell restart, sudo, pacman or paru.
</verification>

<success_criteria>
- The tmux stow package, matugen template, `[templates.tmux]`, contract.json entry, `tmux-set` format in both extractors, reload hook, and install/stow/.gitignore wiring are all gone in clean-removal commits.
- Zero surviving references outside `.planning/` and retirement-check's own registry.
- Every survivor comment that explained a surviving mechanism by contrast still states its reasoning, without naming the retired surface.
- zellij is provably intact across six anchors plus a live resolving symlink.
- theme-parity and colour-lint green; theme-doctor green apart from the one named, predicted, operator-clearable failure.
- The operator has a paste-ready host-cleanup block and a stated condition for flipping the registry entry to `retired`.
</success_criteria>

<output>
Create `.planning/quick/260820-2yz-retire-tmux-now-that-zellij-is-the-theme/260820-2yz-SUMMARY.md` when done.
</output>
</content>
</invoke>
