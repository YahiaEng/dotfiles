---
phase: quick-260812-59l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickshell/.config/quickshell/modules/bar/BarDrawer.qml
  - quickshell/.config/quickshell/modules/bar/qmldir
  - quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml
  - quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml
  - hypr/.config/hypr/scripts/quickshell-doctor
autonomous: true
requirements: [QBAR-02, GATE-02, GATE-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "In vertical orientation, opening either drawer creates a floating strip surface that extends horizontally leftward over the desktop, clear of the 44px bar column — not along the column (D-18-11)."
    - "While collapsed, the drawer host does not exist: no wl_surface, no items, zero idle cost (D-18-24's LazyLoader shape, HotZone.qml's lifecycle)."
    - "The bar's own reserved array is unchanged by this work — still [0, 48, 0, 0] horizontally (QBAR-12's byte-identical-reservation proof)."
    - "quickshell-doctor's GATE-03 bar-surface-registry covers the new surface in both closure directions and reports no unregistered frame."
    - "quickshell-doctor's D-20 bar-colour-role-routing still reports bad=0 with the new file in its scan set."
    - "The horizontal orientation's in-bar drawer behaviour is byte-for-byte unchanged in shape and timing (GATE-02 round 4's tuning survives)."
  artifacts:
    - "quickshell/.config/quickshell/modules/bar/BarDrawer.qml — the shared LazyLoader-gated anchored strip surface"
    - "quickshell/.config/quickshell/modules/bar/qmldir — exactly one added registration line"
    - "hypr/.config/hypr/scripts/quickshell-doctor — exactly one added QSD_BAR_SURFACE_ROWS row"
  key_links:
    - "BarDrawer's vertical anchoring arithmetic is the SAME expression pair SectionPopout.qml already uses (_verticalRightMargin / _verticalClampedTop) — one anchoring rule shared, not two grown (18-UI-SPEC.md § Anchoring per orientation)."
    - "Both capsules already publish expandedCrossExtent / expanded / requestExpand() / requestCollapse(); BarDrawer consumes that existing contract and adds no new seam to either capsule's public surface."
    - "The drawer namespace must resolve to EXACTLY ONE registry row in _qsd_assert_bar_surface_registry_live — the prefix choice is load-bearing, see Task 1."
    - "Each capsule's existing reportDrawerHover('strip', ...) entry point is the sole hover relay; the new surface reports through it, growing no second hover path (D-18-19)."
---

<objective>
Close GATE-02 row `B.4-DRAWER` by taking 18-11's Option B verbatim: add one registered QML
type, `modules/bar/BarDrawer.qml`, as a `LazyLoader`-gated anchored strip surface that hosts
the launcher and settings drawers in **vertical** orientation, so they expand inward and
horizontally — "a floating strip growing leftward over the desktop" — instead of along the
44px column.

Purpose: `B.4-DRAWER` is the only machine-resolved `FAIL` on the GATE-02 sheet, and it blocks
`RETIRE-02` (the waybar deletion in 18-20). The direction is already implemented and
`expandedCrossExtent` is already published by both capsules; only the host that can render it
is missing, because a wlr-layer-shell surface cannot draw outside its own buffer and
`Bar.qml` pins the vertical window to `Design.barColumnWidth`.

Output: one new file, one new `qmldir` line, one new `quickshell-doctor` registry row, and the
two capsule files wired to mount the new host in vertical orientation only.

**This is a pre-specified remedy, not a design task.** 18-11 wrote Option B out in full at
`.planning/phases/18-qml-bar-retirement-machinery/18-11-PLAN.md` § `## Scope correction
required — raised, not taken` (line 842). The operator has selected it. Do not re-derive it,
do not improve on it, and do not drift toward Option A.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

Read these before writing any code — each is named because a specific decision below is
traceable to it, not for background:

- `.planning/phases/18-qml-bar-retirement-machinery/18-11-PLAN.md` § `## Scope correction
  required — raised, not taken` (line 842) — the verbatim specification being implemented.
- `quickshell/.config/quickshell/modules/bar/SectionPopout.qml` — the anchored-surface pattern
  to mirror: layer posture (lines 125-152), the anchoring arithmetic (lines 167-178), the
  `readonly property bool hovered` relay (line 267).
- `quickshell/.config/quickshell/modules/bar/HotZone.qml` — the whole file. The
  create/destroy lifecycle, the transparent-window posture, and the "one job, nothing else"
  discipline to copy.
- `quickshell/.config/quickshell/shell.qml` lines 197-202 — the exact `LazyLoader` mounting
  idiom (`LazyLoader { active: <bool>; HotZone {} }`, default property, no `component:` key).
- `quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml` lines 83-99 (the public
  drawer seam), 120-172 (the hover/dwell/grace machinery), 252-278 (the trigger cell),
  280-375 (`stripHost` — the strip whose vertical branch this plan replaces).
- `quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` lines 514-609 (the same
  seam and machinery), 701-782 (`settingsStripHost` and `settingsTriggerCell`).
- `quickshell/.config/quickshell/modules/bar/qmldir` — the manifest header states the rule:
  a type MUST be registered in the SAME commit that creates it, or it becomes unresolvable
  with NO load error.
- `hypr/.config/hypr/scripts/quickshell-doctor` lines 256-297 (`QSD_BAR_SURFACE_ROWS`, whose
  header already anticipates `bar/BarDrawer.qml` by name), 1540-1590 (both source-closure
  assertions), 1611-1665 (the live matcher), 1727-1780 (the D-20 colour-role check and its
  exemption array).
- `quickshell/.config/quickshell/modules/bar/BarRoles.qml` lines 51-91 — the role names this
  new file must use.
</context>

<execution_environment>
Carry every line of this into execution. These are not preferences.

- **PROHIBITED: `hyprctl reload` and `hyprctl eval`.** Banned phase-wide, no exception, no
  acceptable spelling. `hl.dsp.*` invoked through `hyprctl dispatch` is `hyprctl eval` in
  disguise and is equally banned.
- **Do NOT run `git stash`.**
- **Prefer hot reload over a service restart.** Quickshell watches its config tree and
  reloads on file change; `quickshell-doctor` has a hot-reload check that depends on exactly
  this. A restart is permitted (`systemctl --user restart quickshell.service`) but it VOIDS
  the running QBAR-11 soak window anchored on pid `528309` (window ends ~05:09 EEST). Before
  and after each edit, run `systemctl --user show quickshell.service -p MainPID` — if the pid
  is still `528309`, the soak is intact. If you restart for any reason, the SUMMARY must say
  so in its own section so the orchestrator re-anchors the soak.
- **The quickshell log is NOT in journalctl.** `quickshell-launch.sh` redirects it. Read the
  live instance log at `/run/user/1000/quickshell/by-id/*/log.log`.
- **An agent cannot move the physical pointer on this host.** `hyprctl dispatch movecursor`
  is rejected by Hyprland 0.56.2's Lua dispatch form and `wtype` is keyboard-only. Every
  `verify` below is therefore **structural**: the file loads with zero QML errors, the doctor
  checks pass, the namespace is registered, `qmldir` parses. **The visual leftward-expansion
  behaviour is explicitly NOT verified by this plan** — it is left for the operator's GATE-02
  pass. Do not write or run a verify step that requires a pointer gesture, and do not claim
  one passed.
- **Recorded project lesson that applies directly to this file family:** three confident
  code-reading hypotheses about these exact files were disproven by live measurement during
  Phase 18.1, and a fourth was disproven on 2026-08-12 (the `PopoutTrigger` hover-walk
  finding). If something does not behave as expected, **instrument the running shell** — log
  to the quickshell log and read it back — rather than reasoning from source.
- Do NOT touch `waybar/` or anything under it. Do NOT run plan 18-20.
</execution_environment>

<locked_constraints>
From 18-11's own text and from the operator. Do not revisit any of these.

1. **Option B only.** Option A (widening `Bar.qml`'s vertical `implicitWidth`, re-anchoring
   the three zone containers, adding an input mask) was rejected by 18-11's own recommendation
   and again by the operator.
2. **NO change to `quickshell/.config/quickshell/modules/Bar.qml`.** It is 18-05's frozen file
   and the most load-bearing file in the phase.
3. **NO change to the reservation or to any `exclusiveZone` expression on the bar.** QBAR-12's
   byte-identical-reservation proof must survive. The reserved array is `[[0,48,0,0]]`
   horizontally — **48, not the 46** older phase-18 documents state (18.1 raised
   `Design.barHeight` 40 → 42).
4. **Exactly ONE new line in `modules/bar/qmldir`** and **ONE new file**,
   `modules/bar/BarDrawer.qml`. No second new type, no shared-helper singleton, no new
   `Design.qml` token.
5. **No input mask.** Avoiding that novel concern is a stated reason Option B was chosen.
6. **`LazyLoader`-gated**, created and destroyed with the expanded state, so it costs nothing
   while collapsed — the shape D-18-24 already blesses for `HotZone.qml`.
7. **One shared host consumed by BOTH** `LauncherCapsule.qml` and `ClockActionsCapsule.qml`.
   Neither capsule grows its own bespoke floating strip.
8. **`BarDrawer` and `SectionPopout` share ONE anchoring rule**, not two. Implement this as
   the identical expression pair in identical `Design` tokens, with a comment in `BarDrawer`
   naming `SectionPopout.qml` as the file to diff against — the same kept-in-step-BY-REVIEW
   discipline `SectionPopout.qml`'s own header already accepts toward `PanelDialog.qml`.
   Do NOT extract a shared helper: constraint 4 forbids the extra file and `qmldir` line.
9. **18-17's GATE-03 checks in `hypr/.config/hypr/scripts/quickshell-doctor` must cover the
   new surface**, exactly as they already cover `SectionPopout.qml` and `HotZone.qml`. Extend
   the EXISTING registry; do not invent a new check shape. This is named in 18-11's own cost
   statement, so it is in scope, not extra.
</locked_constraints>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end — BarDrawer.qml exists, is registered in both manifests, and hosts the launcher drawer in vertical orientation</name>

  <files>
quickshell/.config/quickshell/modules/bar/BarDrawer.qml (new)
quickshell/.config/quickshell/modules/bar/qmldir
quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml
hypr/.config/hypr/scripts/quickshell-doctor
  </files>

  <precondition>
`systemctl --user show quickshell.service -p MainPID` reports a live pid and
`/run/user/1000/quickshell/by-id/*/log.log` resolves to exactly one readable file — the
instrumentation every verify step below depends on. Halt and report if either is missing.
  </precondition>

  <action>
Create ONE new file, `quickshell/.config/quickshell/modules/bar/BarDrawer.qml`, wire the
launcher to it, and register it in both manifests. One commit, per `qmldir`'s own rule that a
type is registered in the commit that creates it.

**(a) `BarDrawer.qml` — the surface.**

Root is a `PanelWindow`, copying `SectionPopout.qml`'s layer posture and `HotZone.qml`'s
one-job discipline. Public contract, all set by the consuming capsule:

- `property string drawerId: ""` — the namespace suffix and the diagnostic name.
- `property real triggerCentre: 0` — the trigger cell's own scene-space centre along the
  bar's long axis, published ONCE at expand time, never a live binding. Copy the reasoning in
  `SectionPopout.qml` lines 40-45 and `PopoutTrigger.publishAnchor()` lines 55-68 verbatim in
  spirit: scene mapping does not re-evaluate when an ancestor moves, so a binding goes stale
  silently.
- `property int crossExtent: 0` — the consumer's published `expandedCrossExtent`.
- `property int cellPitch: 0` — the consumer's published `cellPitch`.
- `default property alias cells: drawerGrid.data` — where the consumer's own `Repeater`
  lands.
- `readonly property bool hovered: <the one HoverHandler>.hovered` — the relay, mirroring
  `SectionPopout.qml` line 267 exactly.

Layer posture:
- `anchors { top: true; right: true }` — vertical-orientation only surface; this host is
  never mounted in horizontal orientation, so there is no orientation ternary in this file
  and none should be added.
- `exclusiveZone: 0` — declared as a bare literal, not an expression. The GATE-03 forward
  closure greps for `exclusiveZone[[:space:]]*:[[:space:]]*0\b` on every `noreserve` row; an
  expression fails that grep, and the reservation must not move (constraint 3).
- `exclusionMode: ExclusionMode.Ignore` — same reason `SectionPopout.qml` line 144 gives:
  margins must measure from the true screen edge to be computable from `Design` tokens alone.
- `WlrLayershell.layer: WlrLayer.Overlay`, `WlrLayershell.keyboardFocus:
  WlrKeyboardFocus.None`, `focusable: false`, `color: "transparent"`. A drawer acts on click
  through its own cells; it never asks for keyboard focus and holds no focus grab. Declare NO
  `HyprlandFocusGrab` in this file.

**Namespace — read this whole paragraph before choosing a string.** Use
`WlrLayershell.namespace: "quickshell-bardrawer-" + drawerRoot.drawerId`, yielding
`quickshell-bardrawer-launcher`. The prefix is load-bearing and is NOT a style choice:
`_qsd_assert_bar_surface_registry_live` (doctor line 1630) walks EVERY registry row for each
live namespace and requires `matched -eq 1`; the existing `SectionPopout` row is a `pattern`
row storing the prefix `quickshell-bar-`, which matches any string beginning with that prefix
plus at least one character. Any drawer namespace of the shape `quickshell-bar-…` would
therefore match two rows, be counted as `unmatched`, and fail the live half the moment a
drawer is actually open. `quickshell-bardrawer-…` still starts with `quickshell-bar`, so the
broad candidate net at line 1611 still captures and judges it, and still starts with
`quickshell-`, so the family-wide `^quickshell-.*` blur and `ignore_alpha` layer rules apply
with zero new Hyprland config. Do not "tidy" this prefix.

Geometry — the strip is a single horizontal row of cells, so:
- `implicitWidth: drawerRoot.crossExtent`
- `implicitHeight: drawerRoot.cellPitch`

Anchoring — the shared rule (constraint 8). Declare these two readonly properties with the
same names and the same expressions `SectionPopout.qml` lines 168 and 174 already carry:
`_verticalRightMargin` = `Design.barEdgeMargin + Design.barColumnWidth + Design.spacingXs`,
and `_verticalClampedTop` = the trigger-centred value
`Design.barSideMargin + triggerCentre - height / 2`, clamped between `Design.barSideMargin`
and `(screen ? screen.height : height) - height - Design.barSideMargin`. Then
`margins.right: _verticalRightMargin` and `margins.top: _verticalClampedTop`. Single-edge
margin arithmetic, never doubled — the shape D-18-38 already fixed for the bar's own
reservation. Add a comment naming `SectionPopout.qml` as the file to diff against whenever
either changes.

Chrome — the strip reads as a continuation of the bar capsule, NOT as a popout, so do not
copy `SectionPopout`'s header/foot/state-placeholder vocabulary. One `Rectangle` filling the
window, `radius: Design.barCapsuleRadius`, `color: BarRoles.capsule`. `BarRoles.capsule` is
0.85 alpha, comfortably above the `^quickshell-.*` family layer rule's `ignore_alpha` floor
of 0.5 — going under that floor kills the blur silently, the recorded regression this repo
has already paid for. **Every colour in this file must resolve through `BarRoles.*`.** This
file is new, is NOT in `QSD_BAR_COLOUR_ROLE_EXEMPT`, and is therefore inside the D-20
bar-colour-role-routing check's scan set — unlike `SectionPopout.qml`, which is exempt. Do
not add it to that exemption array.

Contents — one `Grid { id: drawerGrid; anchors.fill: parent; rows: 1; columns: -1; spacing:
Design.spacingXs }`, and one `HoverHandler` whose `hovered` the `hovered` property above
relays. Nothing else in this file: no timer, no process, no `MouseArea`, no second
positioner.

Motion — the surface is created and destroyed by the consumer's `LazyLoader`, so the
container-slide `Behavior` that `stripHost` carries has nothing to animate here (there is no
0-to-N width transition; the window simply exists or does not). Do NOT add a `Behavior on
width`. If an entrance is wanted later that is a separate decision; ship none now and say so
in a comment.

**(b) `qmldir` — exactly one added line.** Append `BarDrawer 1.0 BarDrawer.qml` with a short
header comment in the file's established style recording that this is 18-11's Option B taken
as an 18-05 scope correction, that it is an ordinary instance type and NOT a singleton, and
that it is mounted per-consumer behind a `LazyLoader`. One registration line, one comment
block. Do not reorder or touch any existing line.

**(c) `quickshell-doctor` — exactly one added registry row.** Append to
`QSD_BAR_SURFACE_ROWS`:
`"bar/BarDrawer.qml|quickshell-bardrawer-|pattern|3|noreserve|transient"`.
Then update the array's own header note at lines 273-278 — it currently states that
`bar/BarDrawer.qml` "was an OPEN DECISION at the time this array was written, not yet taken —
checked on disk at commit time and ABSENT, so it has no row." Replace that paragraph with the
fact: the decision was taken, the file ships, the row is below, and record why the prefix is
`quickshell-bardrawer-` rather than `quickshell-bar-drawer-` (the two-row match failure
described above), so a future editor does not "fix" it. Add nothing else to this file — the
array's own header instructs a future editor to add one row and nothing else.

**(d) `LauncherCapsule.qml` — mount it, vertical only.**

- Add a published anchor: `property real _publishedDrawerCentre: 0` and a
  `publishDrawerAnchor()` function computing `triggerCell.mapToItem(null, 0, 0).y +
  triggerCell.height / 2`. Call it at the top of `requestExpand()`, before `expanded` is set
  true, so both the dwell path and any direct caller publish a fresh centre — the same
  discipline `PopoutTrigger.qml` applies on both its open paths.
- Mount the host with the exact `shell.qml` idiom:
  `LazyLoader { active: launcherCapsule.vertical && launcherCapsule.expanded; BarDrawer { … } }`
  — default property, no `component:` key. Inside the `BarDrawer` block set `drawerId:
  "launcher"`, `crossExtent`, `cellPitch`, `triggerCentre: launcherCapsule._publishedDrawerCentre`,
  `onHoveredChanged: launcherCapsule.reportDrawerHover("strip", hovered)`, and a `Repeater`
  over `launcherCapsule.appEntries.length` with the same plain-`Item` delegate wrapper and
  `LauncherCell` body `stripHost` already uses. Reuse `LauncherCell` — it is an inline
  component in this same file and resolves inside the loaded block.
- **`reportDrawerHover` is the sole hover relay.** Do not add a second hover path, a second
  dwell timer, or a second grace timer (D-18-19). The existing `drawerGraceTimer` is what
  covers the pointer crossing the `spacingXs` gap between the bar column and the floating
  strip.
- Change `stripHost`'s vertical branch so it contributes nothing in vertical orientation:
  both `width` and `height` resolve to `0` when `launcherCapsule.vertical` is true, replacing
  the current "expands along the column" fallback. Gate the strip's `Grid` behind a
  `Loader { active: !launcherCapsule.vertical }` so the seven cells are destroyed rather
  than merely clipped in vertical orientation. **The horizontal branch of every expression in
  `stripHost` — width, height, both `Behavior` blocks, the `clip: true`, the delegate
  wrapper, the absence of per-cell animation — must be left exactly as it is.** GATE-02 round
  4 tuned that path against the operator's "slow" and "not smooth" reports; a regression there
  is worse than the bug being fixed.
- Update the `stripHost` header comment (lines 280-292), which currently describes the
  vertical fallback and points at `## Scope correction required`. Replace it with what is now
  true: horizontal is hosted here, vertical is hosted by `BarDrawer` because a layer-shell
  surface cannot render outside its own buffer and `Bar.qml` pins the vertical window to
  `Design.barColumnWidth`.

Commit as one change: `feat(quick): add BarDrawer vertical drawer host and wire the launcher`.
  </action>

  <verify>
    <automated>
cd /home/aorus/dotfiles &amp;&amp; \
test -f quickshell/.config/quickshell/modules/bar/BarDrawer.qml &amp;&amp; \
[ "$(grep -vE '^\s*//' quickshell/.config/quickshell/modules/bar/BarDrawer.qml | grep -cF 'WlrLayershell.namespace')" -eq 1 ] &amp;&amp; \
grep -vE '^\s*//' quickshell/.config/quickshell/modules/bar/BarDrawer.qml | grep -qE 'exclusiveZone[[:space:]]*:[[:space:]]*0\b' &amp;&amp; \
[ "$(grep -c '^BarDrawer 1.0 BarDrawer.qml$' quickshell/.config/quickshell/modules/bar/qmldir)" -eq 1 ] &amp;&amp; \
[ "$(grep -c 'bar/BarDrawer.qml|quickshell-bardrawer-|pattern|3|noreserve|transient' hypr/.config/hypr/scripts/quickshell-doctor)" -eq 1 ] &amp;&amp; \
bash -n hypr/.config/hypr/scripts/quickshell-doctor &amp;&amp; \
echo TASK1-STRUCTURE-OK
    </automated>
    <automated>
# Load health, via hot reload — no restart, so the QBAR-11 soak survives.
cd /home/aorus/dotfiles &amp;&amp; PID_BEFORE=$(systemctl --user show quickshell.service -p MainPID --value) &amp;&amp; \
LOG=$(ls -t /run/user/1000/quickshell/by-id/*/log.log | head -1) &amp;&amp; \
touch quickshell/.config/quickshell/shell.qml &amp;&amp; sleep 4 &amp;&amp; \
PID_AFTER=$(systemctl --user show quickshell.service -p MainPID --value) &amp;&amp; \
echo "pid_before=$PID_BEFORE pid_after=$PID_AFTER" &amp;&amp; \
tail -n 120 "$LOG" | grep -iE 'BarDrawer|error|is not a type|Cannot assign|unresolved' || echo "NO-QML-ERRORS-IN-TAIL"
    </automated>
    <automated>
# GATE-03 source-half closure and the D-20 colour-role check, both restart-free.
hypr/.config/hypr/scripts/quickshell-doctor 2>&amp;1 | grep -E 'bar-surface-registry|bar-colour-role-routing'
    </automated>
  </verify>

  <done>
- `BarDrawer.qml` exists, declares its layer namespace exactly once outside comments, carries a
  literal `exclusiveZone: 0`, and contains no direct global-palette reference (proven by
  bar-colour-role-routing reporting `bad=0`, not by a hand grep).
- `qmldir` gained exactly one registration line; `git diff --stat` on that file shows no
  removed or reordered existing line.
- `quickshell-doctor` gained exactly one `QSD_BAR_SURFACE_ROWS` row, its stale
  "OPEN DECISION / has no row" paragraph is corrected, and `bash -n` parses the script.
- `quickshell-doctor` reports `bar-surface-registry` PASS with `missing=0`,
  `unexpected-reservation=0` and `unregistered=0`, and `bar-colour-role-routing` PASS with
  `bad=0`.
- The live pid is unchanged across the reload, so the QBAR-11 soak window is intact. If it is
  NOT unchanged, that fact is recorded for the SUMMARY.
- The quickshell log tail shows no QML error naming `BarDrawer`, no unresolved-type error,
  and no failed assignment.
- **Not verified and not claimed:** that the strip visually appears leftward of the column in
  vertical orientation. No pointer gesture was performed. Left for the operator's GATE-02 pass.
  </done>

  <reversibility rating="reversible">
    One new file plus three additive edits; `git revert` of the single commit restores the
    prior state exactly, and `Bar.qml` and the reservation are untouched by construction.
  </reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the settings drawer to the same host — one shared type, two consumers</name>

  <files>
quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml
  </files>

  <action>
Repeat Task 1(d) against `ClockActionsCapsule.qml`, consuming the SAME `BarDrawer` type — no
second host type, no copy of the surface, no per-capsule variant (constraint 7). This is the
half of Option B that stops the strip shape being duplicated across two files.

- Add `property real _publishedDrawerCentre: 0` and a `publishDrawerAnchor()` computing
  `settingsTriggerCell.mapToItem(null, 0, 0).y + settingsTriggerCell.height / 2`. Call it at
  the top of `requestExpand()`, before `settingsExpanded` is set true.
- Mount with `LazyLoader { active: clockActionsCapsule.vertical &amp;&amp;
  clockActionsCapsule.settingsExpanded; BarDrawer { … } }` — the `shell.qml` default-property
  idiom again. Set `drawerId: "settings"` (yielding namespace `quickshell-bardrawer-settings`,
  which the Task 1 registry row's prefix already covers — no second row is needed and none
  may be added), `crossExtent: clockActionsCapsule.expandedCrossExtent`, `cellPitch`,
  `triggerCentre: clockActionsCapsule._publishedDrawerCentre`, and
  `onHoveredChanged: clockActionsCapsule.reportDrawerHover("strip", hovered)`.
- Inside the block, one `Repeater { model: clockActionsCapsule.settingsAxes; delegate:
  SettingsAxisCell { axis: modelData } }` — the same delegate `settingsGrid` already uses,
  reached as an inline component of this same file.
- Change `settingsStripHost` so both `width` and `height` resolve to `0` when
  `clockActionsCapsule.vertical` is true, and gate its `Grid` behind a
  `Loader { active: !clockActionsCapsule.vertical }` so the five cells are destroyed rather
  than clipped in vertical orientation.
- **Leave the horizontal path untouched**, including the declaration ORDER of
  `settingsStripHost` before `settingsTriggerCell`. That order is load-bearing: it is what
  makes the horizontal strip grow leftward out of a fixed trigger, and reversing it is
  literally the operator's already-reported "expands in the opposite direction" defect. The
  file's own comment at lines 707-720 records the arithmetic; do not disturb it.
- Update the `settingsStripHost` header comment, which currently says it "carries the same
  flagged vertical-orientation host gap Task 2's launcher strip carries — see this plan's
  `## Scope correction required`". That gap is now closed; say where vertical is hosted
  instead.

Do not touch `NotificationSource`, the four extras, the clock, or any cell body. The only
edits in this file are the anchor publisher, the `LazyLoader` mount, the two
`settingsStripHost` geometry expressions, the `Grid` gating `Loader`, and two comments.

Commit: `feat(quick): host the settings drawer on BarDrawer in vertical orientation`.
  </action>

  <verify>
    <automated>
cd /home/aorus/dotfiles &amp;&amp; \
[ "$(grep -c 'BarDrawer' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml)" -ge 1 ] &amp;&amp; \
[ "$(grep -c 'drawerId: "settings"' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml)" -eq 1 ] &amp;&amp; \
[ "$(grep -c 'reportDrawerHover("strip"' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml)" -eq 2 ] &amp;&amp; \
[ "$(grep -c 'bar/BarDrawer.qml|' hypr/.config/hypr/scripts/quickshell-doctor)" -eq 1 ] &amp;&amp; \
echo TASK2-STRUCTURE-OK
    </automated>
    <automated>
# Both consumers resolve the one shared type; declaration order in the clock capsule preserved.
cd /home/aorus/dotfiles &amp;&amp; \
STRIP=$(grep -n 'id: settingsStripHost' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml | cut -d: -f1) &amp;&amp; \
TRIG=$(grep -n 'id: settingsTriggerCell' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml | cut -d: -f1) &amp;&amp; \
[ "$STRIP" -lt "$TRIG" ] &amp;&amp; echo "DECLARATION-ORDER-PRESERVED strip=$STRIP trigger=$TRIG"
    </automated>
    <automated>
cd /home/aorus/dotfiles &amp;&amp; PID_BEFORE=$(systemctl --user show quickshell.service -p MainPID --value) &amp;&amp; \
LOG=$(ls -t /run/user/1000/quickshell/by-id/*/log.log | head -1) &amp;&amp; \
touch quickshell/.config/quickshell/shell.qml &amp;&amp; sleep 4 &amp;&amp; \
echo "pid_before=$PID_BEFORE pid_after=$(systemctl --user show quickshell.service -p MainPID --value)" &amp;&amp; \
tail -n 120 "$LOG" | grep -iE 'BarDrawer|SettingsAxisCell|error|is not a type|Cannot assign|unresolved' || echo "NO-QML-ERRORS-IN-TAIL"
    </automated>
  </verify>

  <done>
- Both capsules mount the same registered `BarDrawer` type; exactly one row exists in the
  doctor registry and exactly one line in `qmldir`.
- `settingsStripHost` is still declared before `settingsTriggerCell`.
- Each capsule reports strip hover through its existing `reportDrawerHover("strip", …)` entry
  point — two call sites in the clock capsule (trigger and strip), no third hover path.
- Hot reload completed with the pid unchanged and no QML error in the log tail.
- **Not verified and not claimed:** the settings strip's on-screen position or direction.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the invariants that must not have moved, and record honestly what was not verified</name>

  <files>
.planning/quick/260812-59l-add-bardrawer-qml-vertical-drawer-host-1/260812-59l-SUMMARY.md
  </files>

  <action>
No source edits. This task proves the three things that could have been broken silently, runs
the full doctor, and writes the SUMMARY with an explicit unverified section.

**(a) The frozen files were not touched.** Assert the changed-file set positively — enumerate
what changed and compare against the expected list, rather than grepping for the absence of a
path. The expected set under `quickshell/` and `hypr/` for this whole plan is exactly:
`quickshell/.config/quickshell/modules/bar/BarDrawer.qml`,
`quickshell/.config/quickshell/modules/bar/qmldir`,
`quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml`,
`quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml`,
`hypr/.config/hypr/scripts/quickshell-doctor`. Any sixth path is a constraint violation —
halt and report rather than continue.

**(b) The reservation did not move.** Read `hyprctl monitors -j | jq '.[0].reserved'` and
assert it equals `[0,48,0,0]` in horizontal orientation. This is a READ, not `hyprctl reload`
or `hyprctl eval` — those remain banned. 48 is the live-measured value; do not "correct" it to
the 46 older phase-18 documents state.

**(c) The full doctor is no worse than before.** Run
`hypr/.config/hypr/scripts/quickshell-doctor` in full plus its `--self-test`, and compare the
PASS/FAIL tallies against what the phase-18 records report. A pre-existing failure unrelated
to this work is recorded, not fixed here.

**(d) Write the SUMMARY** at
`.planning/quick/260812-59l-add-bardrawer-qml-vertical-drawer-host-1/260812-59l-SUMMARY.md`.
It must carry, each as its own named section:

1. **What shipped** — the one new type, its public contract (`drawerId`, `triggerCentre`,
   `crossExtent`, `cellPitch`, the `cells` default alias, `hovered`), and its namespace
   pattern, stated as the contract 18-19 and any later consumer reads.
2. **The namespace-prefix finding, stated as a finding.** Record that
   `_qsd_assert_bar_surface_registry_live` matches a live namespace against every row and
   requires exactly one match, that the existing `SectionPopout` pattern row's prefix
   `quickshell-bar-` therefore claims every `quickshell-bar-…` string, and that this is why
   the drawer prefix is `quickshell-bardrawer-`. **Also record the latent defect this
   uncovered, which this plan deliberately did NOT fix:** `HotZone.qml`'s own namespace
   `quickshell-bar-hotzone` matches BOTH the `SectionPopout` pattern row and its own exact
   row, so the live half would count it `unmatched` and fail — dormant today only because the
   hot zone is transient and is not mapped while the bar is visible. This belongs on the
   operator's desk as an open item, not fixed inside a quick task.
3. **GATE-02 row `B.4-DRAWER`.** State plainly that the structural remedy 18-11 named as
   Option B has now landed, that the row's machine-resolved `FAIL` was resolved against the
   ABSENCE of this file, and that **re-resolving the row is 18-19's job, not this plan's** —
   this plan did not edit `18-GATE-02-RECORD.md`.
4. **What was NOT verified.** In its own section, unhedged: no pointer gesture was performed
   and no visual observation was made, because an agent cannot move the physical pointer on
   this host (`hyprctl dispatch movecursor` is rejected by Hyprland 0.56.2's Lua dispatch form
   and `wtype` is keyboard-only). The leftward-expansion behaviour D-18-11 requires is
   therefore **claimed by construction and unproven by observation.** List the specific things
   the operator should look at during the GATE-02 pass: (i) does the strip appear leftward of
   the column, clear of the bar; (ii) is it vertically centred on its trigger cell and clamped
   inside the screen; (iii) does the drawer survive the pointer crossing the `spacingXs` gap
   between the bar column and the floating strip, or does the grace timer expire mid-crossing.
   For (iii), pre-specify the remedy so the operator is not handed a fresh design problem:
   drop the `Design.spacingXs` term from `_verticalRightMargin` so the strip abuts the bar, or
   lengthen the existing grace interval — one expression either way, no new machinery.
5. **The QBAR-11 soak.** Whether the service pid changed at any point. If it did NOT, say so
   explicitly so the orchestrator knows the window anchored on pid `528309` is still valid. If
   it DID, say so in the first line of the section so the orchestrator re-anchors.
6. **Residue, named rather than absorbed.** The horizontal in-bar strips (`stripHost`,
   `settingsStripHost`) still exist in both capsule files, because a strip that lives inside
   the bar window's own `Grid` layout cannot be a separate surface. Option B's
   "stop duplicating the strip shape" benefit is therefore realised for the vertical case
   only. Do not record this as complete de-duplication. Also record that
   `MediaConnectivityCapsule.qml`'s sibling audio/connections strips were deliberately NOT
   migrated — 18-11's Option B names exactly two consumers, and widening that here would be
   scope drift.

Commit: `docs(quick): record the BarDrawer vertical host and what it does not prove`.
  </action>

  <verify>
    <automated>
cd /home/aorus/dotfiles &amp;&amp; \
git diff --name-only HEAD~2 -- quickshell/ hypr/ | sort > /tmp/claude-1000/-home-aorus-dotfiles/97d1f9f5-36f8-49c7-b545-8bf6bc9baf63/scratchpad/actual.txt &amp;&amp; \
printf '%s\n' \
 'hypr/.config/hypr/scripts/quickshell-doctor' \
 'quickshell/.config/quickshell/modules/bar/BarDrawer.qml' \
 'quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml' \
 'quickshell/.config/quickshell/modules/bar/LauncherCapsule.qml' \
 'quickshell/.config/quickshell/modules/bar/qmldir' | sort > /tmp/claude-1000/-home-aorus-dotfiles/97d1f9f5-36f8-49c7-b545-8bf6bc9baf63/scratchpad/expected.txt &amp;&amp; \
diff /tmp/claude-1000/-home-aorus-dotfiles/97d1f9f5-36f8-49c7-b545-8bf6bc9baf63/scratchpad/expected.txt /tmp/claude-1000/-home-aorus-dotfiles/97d1f9f5-36f8-49c7-b545-8bf6bc9baf63/scratchpad/actual.txt &amp;&amp; echo FILE-SET-EXACT
    </automated>
    <automated>
hyprctl monitors -j | jq -e '.[0].reserved == [0,48,0,0]' &gt;/dev/null &amp;&amp; echo RESERVATION-UNCHANGED-48
    </automated>
    <automated>
hypr/.config/hypr/scripts/quickshell-doctor 2>&amp;1 | tail -n 25
    </automated>
    <automated>
hypr/.config/hypr/scripts/quickshell-doctor --self-test 2>&amp;1 | tail -n 15
    </automated>
    <automated>
cd /home/aorus/dotfiles &amp;&amp; test -f .planning/quick/260812-59l-add-bardrawer-qml-vertical-drawer-host-1/260812-59l-SUMMARY.md &amp;&amp; \
for s in "What shipped" "namespace" "B.4-DRAWER" "NOT verified" "soak" "Residue"; do \
  grep -qi "$s" .planning/quick/260812-59l-add-bardrawer-qml-vertical-drawer-host-1/260812-59l-SUMMARY.md || { echo "MISSING SECTION: $s"; exit 1; }; done &amp;&amp; echo SUMMARY-SECTIONS-PRESENT
    </automated>
  </verify>

  <done>
- The changed-file set under `quickshell/` and `hypr/` is exactly the five expected paths —
  `Bar.qml`, `BarEntryModel.qml`, `shell.qml`, `Design.qml` and everything under `waybar/`
  are absent by enumeration.
- `hyprctl monitors -j` reports `reserved` still `[0,48,0,0]`, so QBAR-12's proof survives.
- The full doctor and its `--self-test` are no worse than the phase-18 baseline, and
  `bar-surface-registry` and `bar-colour-role-routing` both PASS.
- The SUMMARY exists and carries all six required sections, including the unhedged
  "what was NOT verified" section and the operator's three GATE-02 look-at items with a
  pre-specified remedy for the gap-traversal risk.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compositor → shell surface | A new wlr-layer-shell surface on the Overlay layer receives pointer input above fullscreen clients |
| shell → registry checks | `quickshell-doctor` is the structural instrument D-18-15 accepted as compensation for bespoke frames; a frame outside it is unchecked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59L-01 | Denial of Service | `BarDrawer.qml` on the Overlay layer | medium | mitigate | Bounded lifetime: the surface exists only while `vertical && expanded`, is destroyed by the `LazyLoader` on collapse, holds no focus grab and requests no keyboard focus (`WlrKeyboardFocus.None`, `focusable: false`), so it cannot swallow input from the focused client while idle. |
| T-59L-02 | Tampering | the bar's reserved screen space | high | mitigate | `exclusiveZone: 0` declared as a bare literal (also enforced by the GATE-03 forward closure's `noreserve` grep), and Task 3 asserts `reserved == [0,48,0,0]` post-change. |
| T-59L-03 | Repudiation | GATE-03 registry coverage | high | mitigate | The reverse closure fails on any `*.qml` under the scan root declaring a namespace without a registry row, so this frame cannot ship unregistered; the row is added in the same commit as the file. |
| T-59L-04 | Information Disclosure | the strip surface over the desktop | low | accept | The strip renders only the same launcher/settings glyphs already visible in the bar; nothing new is exposed. |
| T-59L-05 | Elevation of Privilege | drawer cell click targets | low | accept | The new surface adds no new action: cells are the same `LauncherCell` / `SettingsAxisCell` instances the in-bar strip already hosts, with the same `startDetached()` launch discipline. |

No package-manager install occurs in this plan, so no `T-59L-SC` supply-chain row applies.
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | Close GATE-02 row `B.4-DRAWER`, the only failing row, which blocks `RETIRE-02` | COVERED (Tasks 1-2 land the structural remedy; Task 3 records that re-resolving the row is 18-19's job, per that plan's own ownership) |
| REQ | QBAR-02 — one bar switches between horizontal strip and right-edge vertical column | COVERED (Tasks 1-2 make the vertical orientation's drawers behave as decided rather than degrading to a column expansion) |
| REQ | GATE-02 — blocking human render pass | PARTIALLY COVERED by design, and flagged rather than quietly claimed: the structural half is closed here; the visual half is impossible for an agent on this host and is handed to the operator with three named look-at items and a pre-specified remedy (Task 3(d)(4)) |
| REQ | GATE-03 — namespace/structural checks cover every frame this phase added | COVERED (Task 1(c) — one registry row, both closure directions) |
| RESEARCH | No RESEARCH.md for this quick task; the specification source is 18-11's `## Scope correction required` | COVERED — Option B implemented as written, Option A explicitly excluded by `<locked_constraints>` 1 |
| CONTEXT | D-18-11 — vertical drawers expand inward, horizontally | COVERED (Tasks 1-2), the sole reason this plan exists |
| CONTEXT | D-18-24 — LazyLoader-gated create/destroy so a transient surface costs nothing at rest | COVERED (Task 1(a), Task 1(d), Task 2 — the `shell.qml`/`HotZone.qml` idiom) |
| CONTEXT | D-18-15 — a bespoke frame is permitted only if the structural registry covers it | COVERED (Task 1(c)) |
| CONTEXT | D-18-19 — no second unlatched hover path | COVERED — the new surface reports through each capsule's existing `reportDrawerHover` entry point and adds no dwell or grace timer of its own |
| CONTEXT | D-18-38 — single-edge margin arithmetic, never doubled | COVERED (Task 1(a) anchoring, reusing `SectionPopout.qml`'s expression pair verbatim) |
| CONTEXT | D-20 — every bar colour routes through `BarRoles` | COVERED (Task 1(a); the new file is deliberately NOT added to the exemption array) |

**Deliberately out of scope, named so a reader does not look for it:** `MediaConnectivityCapsule.qml`'s
sibling audio/connections strips (18-11's Option B names exactly two consumers);
`18-GATE-02-RECORD.md`'s `B.4-DRAWER` verdict (18-19 owns the sheet); the dormant `HotZone`
double-match defect Task 3(d)(2) surfaces (recorded for the operator, not fixed here); plan
18-20 and everything under `waybar/`.

No source item is MISSING. No item was reduced, simplified, deferred to a "v1", or renamed
into something smaller.
</source_coverage>

<verification>
1. **One new type, registered twice.** `BarDrawer.qml` exists; `qmldir` carries exactly one
   new registration line; `quickshell-doctor` carries exactly one new registry row. Both
   closure directions of GATE-03 pass.
2. **Exactly one namespace declaration, resolving to exactly one registry row.** The
   comment-stripped file declares `WlrLayershell.namespace` once, and the chosen prefix
   provably matches one and only one row of `QSD_BAR_SURFACE_ROWS`.
3. **Nothing reserves that did not reserve before.** A literal `exclusiveZone: 0` in the new
   file, and `hyprctl monitors -j` still reporting `[0,48,0,0]`.
4. **The frozen files are absent from the diff**, proven by enumerating the changed-file set
   and matching it exactly against the five expected paths.
5. **Both drawers consume the one shared host.** Two `LazyLoader` mounts, one type, one
   registry row, zero per-capsule surface variants.
6. **Nothing costs anything while collapsed.** Both mounts are keyed on
   `vertical && <expanded>`; the in-bar strip's cells are destroyed by an `active: !vertical`
   `Loader` in vertical orientation rather than merely clipped.
7. **Every colour routes through the role layer**, proven by the D-20 check reporting
   `bad=0` with the new file inside its scan set.
8. **The horizontal path is untouched**, including `settingsStripHost`'s declaration position
   before `settingsTriggerCell`.
9. **The shell still loads clean**, proven by hot reload with an unchanged pid and a log tail
   free of QML errors — never by a service restart unless the restart is recorded.
10. **The visual behaviour is NOT verified here** and is not claimed anywhere in the SUMMARY,
    the commit messages, or the task output.
</verification>

<success_criteria>
- In vertical orientation, both drawers are hosted by a surface that can render outside the
  44px bar column, so D-18-11's leftward expansion is structurally possible for the first
  time — the only thing that was missing, since both capsules already publish
  `expandedCrossExtent`.
- `Bar.qml` is untouched, the reservation is untouched, no input mask exists, and the whole
  change is one new file plus four additive edits.
- GATE-03's registry covers the new frame by construction: adding the file without adding the
  row would have failed the reverse closure, and the row was added in the same commit.
- The operator receives an honest handoff: the structural half proven and the visual half
  explicitly unproven, with three named look-at items and a one-expression remedy
  pre-specified for the only interaction risk this design introduces.
- The QBAR-11 soak window survives, or its loss is reported in the SUMMARY's first line of
  that section.
</success_criteria>

<output>
Create `.planning/quick/260812-59l-add-bardrawer-qml-vertical-drawer-host-1/260812-59l-SUMMARY.md`
when done, carrying all six sections enumerated in Task 3(d).
</output>
