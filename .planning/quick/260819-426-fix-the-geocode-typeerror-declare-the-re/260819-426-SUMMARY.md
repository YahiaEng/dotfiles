---
quick_id: 260819-426
date: 2026-08-19
mode: quick
status: complete
one_liner: the reverse geocode never executed — TWO independent silent faults, a declaration-order TypeError and a binding-propagation race, each of which alone left the label on the timezone fallback that prints "Cairo" in Alexandria too
---

# Quick Task 260819-426 — Summary

## What was wrong

Not the lookup. `_revalidateAgainstSettings()` runs during construction (the state
`FileView` load calls `loadCache()` → `_revalidateAgainstSettings()`), and it called
`_resolveCityIfNeeded()` — which, along with the `GeocodeBackend` child, was declared
about 200 lines further down the file and did not yet exist on the object.

```
WeatherBackend.qml[313]: TypeError: Property '_resolveCityIfNeeded'
of object WeatherBackend_QMLTYPE_21 is not a function        (x18)
```

Every invocation threw. `city` stayed empty. The resolution chain fell through to
step 3, the timezone fallback — and `Africa/Cairo` prints "Cairo" whether the
coordinates are Cairo's or Alexandria's.

**That is what made it dangerous.** The bug produced a plausible wrong answer, not a
visible failure. It was equally invisible in Cairo, where the wrong path happened to
agree with reality.

## What was NOT wrong (each eliminated by measurement, not reasoning)

- **Nominatim** returns HTTP 200 and `Alexandria` for the component's exact URL and
  exact User-Agent. With no User-Agent it returns 403 — so the header matters, and it
  is sent.
- **Qt's User-Agent handling.** A local HTTP listener received the component's UA
  string verbatim from a QML XHR. The earlier plan-time check only proved `user-agent`
  is absent from Qt's forbidden-header table — necessary, not sufficient. This closes
  that gap end-to-end.
- **Qt → Nominatim over TLS.** A `qml6` harness got HTTP 200 and the Alexandria
  payload.
- **The wiring.** Both call sites present; brace-depth analysis confirmed the resolver
  is a root-level member, not accidentally nested.

## Fix

`GeocodeBackend` child and `_resolveCityIfNeeded()` moved above
`_revalidateAgainstSettings()`, with a comment stating that the order is load-bearing
and must not be undone.

**Verified: 18 TypeErrors before the reload, 0 after.**

`cityLabelSource` still reports `timezone` at rest, correctly — `resolve()` returns
early while the drawer is closed. It fires on the next drawer open.

## Two things that made this hard to see

1. **The systemd unit journals nothing.** `journalctl --user -u quickshell` is empty;
   every QML warning and `console.log` goes to `~/.cache/quickshell.log`, the
   launcher's own sink. Looking in the journal first cost real time.
2. **`console.log` is invisible under `qml6`** in this environment, so the throwaway
   harness had to report its result over a local HTTP socket instead of printing it.

## Gates

qmllint exit 0 · colour-lint 146/0 · motion-lint 293/0 · quickshell-doctor 28/0
(runs 2-4; run 1 read 27/1, the known post-hot-reload transient) · theme-doctor 587/0.

Commit: `2853d13`


---

# Second cause — found after the first fix did not change the symptom

The operator reported the label STILL read "Cairo" after the declaration-order
fix. The TypeError was genuinely gone (18 -> 0), so this was a second,
independent fault sitting behind the first.

## Why it was invisible

Across `_resolveCityIfNeeded()` and `GeocodeBackend.resolve()` there are seven
guard clauses, and **not one of them logs on an early return**. The feature could
therefore fail completely while producing no error, no warning, and a plausible
label. Diagnosis required instrumenting every guard.

## What the instrumentation showed

```
PROBE _resolveCityIfNeeded: drawerOpen=true   coordsValid=true  geocoder=object
PROBE Geocode.resolve:      drawerOpen=false  coordsValid=true  lat=31.20176 ...
```

The parent sees `drawerOpen=true` and calls through; the child sees `false` in the
same tick. `_resolveCityIfNeeded()` runs from the parent's `onDrawerOpenChanged`,
and at that moment the child's `drawerOpen: root.drawerOpen` binding has not been
re-evaluated. `resolve()` returned early at its own guard, every time.

## Fix

`Qt.callLater(geocoder.resolve)` — defer until bindings settle. This keeps every
guard on both sides intact, rather than deleting the child's defence-in-depth
check to paper over the ordering.

## Verified live

Instrumented:
```
Geocode sending: https://nominatim.openstreetmap.org/reverse?lat=31.20176&...
Geocode parsed city='Alexandria' status=200
WeatherBackend: cityLabelSource = geocoded
```
`weather-cache.json` gained `city: "Alexandria"`. Instrumentation then removed and
re-verified: `cityLabelSource` transitions `timezone` -> `geocoded`, 0 TypeErrors,
qmllint clean, colour-lint 146/0, motion-lint 293/0, quickshell-doctor 28/0 across
three consecutive runs.

## Method note

`hyprctl dispatch global quickshell:dashboard` FAILS on this host — the Hyprland
config is Lua, so hyprctl wraps arguments in `hl.dispatch(...)` and the bare form
is a syntax error. The working form is:

```
hyprctl dispatch 'hl.dsp.global("quickshell:dashboard")'
```

That is what made it possible to open the drawer under instrumentation instead of
waiting on a human round-trip for each hypothesis.

Commits: `2853d13` (declaration order), `dcf6f35` (binding race)
