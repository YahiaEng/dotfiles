---
phase: 19-notification-server-centre
plan: 08
type: execute
wave: 4
depends_on: [19-01, 19-02, 19-03, 19-04, 19-05, 19-06]
files_modified:
  - .planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md
  - .planning/phases/19-notification-server-centre/19-RETIREMENT-AFTER-swaync.md
  - hypr/.config/hypr/scripts/quickshell-doctor
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-busctl-list.txt
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-two-owner-busctl-list.txt
  - hypr/.config/hypr/config/autostart.lua
  - hypr/.config/hypr/scripts/swaync-launch.sh
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/reload.sh
  - matugen/.config/matugen/config.toml
  - matugen/.config/matugen/templates/swaync-colors.css
  - hypr/.config/hypr/scripts/retirement-check
  - install.sh
  - swaync/
autonomous: false
requirements: [QNOTIF-11, RETIRE-03, LEDGER-04, LEDGER-07]

estimate:
  tokens: 104000
  raw_tokens: 52000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "A live bus check against the running session — the existing poisoned-two-owner fixture re-pointed and run for real, not self-tested — shows exactly one owner of `org.freedesktop.Notifications` (ROADMAP SC-4, QNOTIF-11)."
    - "The same check still shows exactly one owner after the server is deliberately killed and respawned (ROADMAP SC-4, QNOTIF-11, D-19-41)."
    - "The outgoing daemon is gone from repo and host — package, config, its 2 contract entries, its matugen template, its reload step, its autostart line, and its doctor fixtures re-pointed (ROADMAP SC-5, RETIRE-03)."
    - "The retirement checklist script reports zero hits both before and after the deletion (ROADMAP SC-5, RETIRE-03, D-19-42)."
    - "The human render gate judged the replacement at least as good as what it replaced, and nothing was deleted before that judgment (ROADMAP SC-5, GATE-02, D-19-42)."
    - "The autostart swap landed as exactly one commit — no boot between two commits ever ran a silent two-owner race (ROADMAP note, D-19-42)."
    - "A real bluetooth pairing renders through the new server with working accept and reject, and no external toolkit dialog appears (D-19-39, G-15-7's own stated closing verification)."
    - "`theme-stress-test` still reaches a full clean run after the deletion, with the tree still clean afterwards (ROADMAP SC-6, LEDGER-07)."
  artifacts:
    - ".planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md"
    - ".planning/phases/19-notification-server-centre/19-RETIREMENT-AFTER-swaync.md"
  key_links:
    - "The two-owner fixture's declared owner string and the autostart file's launched-daemon line are the same fact recorded in two places; flipping them in one commit is what closes the boot-order race."
    - "The render gate reads plan 19-02's behaviour baseline for its capability half and plan 19-04's fault-injection fixture for its highest-weight mechanics — neither is a memory test."
    - "Nothing in this plan deletes anything until the blocking gate has passed and the one-way-door decision has been taken."
---

<objective>
The closing plan. Prove exactly one owner of the notification bus name against the real session,
put the replacement in front of a human under a blocking gate, take the one-way-door decision
explicitly, then delete the outgoing daemon — with the autostart swap as one atomic commit.

There is no soak window and no rollback. The package leaves the host in this same phase. That is
why the fault-injection fixtures must be green before the gate rather than after, why the
two-owner check is blocking rather than advisory, and why the decision to proceed is taken by a
human with the evidence in front of them rather than inferred from a passing test suite.

Purpose: ROADMAP success criteria 4 and 5, plus the closing verifications for criterion 6.

Output: a single-owner proof, a passed render gate, a deleted daemon, and before/after checklist
runs that both read zero.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-notification-server-centre/19-CONTEXT.md
@.planning/phases/19-notification-server-centre/19-RESEARCH.md
@.planning/phases/19-notification-server-centre/19-UI-SPEC.md
@.planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md
@.planning/phases/19-notification-server-centre/19-SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion baseline, fixture re-point, and the live single-owner proof</name>
  <files>.planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md, hypr/.config/hypr/scripts/quickshell-doctor, hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-busctl-list.txt, hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-two-owner-busctl-list.txt</files>
  <read_first>
    - `.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md` and
      `18-RETIREMENT-AFTER-waybar.md` — the before/after checklist-run pair this phase mirrors,
      including how each records its run and what "zero hits" means for each tier.
    - `hypr/.config/hypr/scripts/retirement-check` — the generic checklist script built in
      Phase 18, its two tiers, and its registry rows.
    - `hypr/.config/hypr/scripts/quickshell-doctor` — the declared-owner registry near the top of
      the file, the single-owner check that reads it, and the two self-test assertions plus the
      isolated registry redeclaration inside the self-test harness. RESEARCH.md verified all four
      locations; find them by reading rather than by trusting line numbers, since earlier plans in
      this phase have already edited this file.
    - `hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-busctl-list.txt` and
      `poisoned-two-owner-busctl-list.txt` — the two fixtures to regenerate.
    - `.planning/phases/19-notification-server-centre/19-CONTEXT.md` D-19-41 — the three-round
      proof shape: fixture self-test, then a live un-self-tested run, then again after a
      deliberate kill and respawn.
  </read_first>
  <action>
    Run the retirement checklist for the outgoing surface BEFORE anything is deleted and record
    its full output at
    `.planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md`, mirroring
    the Phase 18 baseline document's shape. This run is expected to find the surface everywhere it
    currently lives — that is the point. A baseline that already reads zero would mean the
    checklist is not looking where the surface actually is, and that is a finding to stop on, not
    a pass.

    Regenerate both bus-list fixtures with the shell as the legitimate owner: the compliant
    fixture showing one owner, the poisoned fixture showing two. Flip the doctor's declared-owner
    registry entry and both self-test assertions to match, plus the isolated registry
    redeclaration inside the self-test harness — all four locations, or the self-test passes
    against a stale expectation while the live check uses the new one.

    Hold the autostart edit back. The registry flip and the autostart line removal are the same
    fact in two places and they land together in Task 4's single commit; doing the registry half
    here would leave a window where the doctor expects an owner the autostart file does not
    launch.

    Run the doctor's self-test, then run the single-owner check LIVE against the real session —
    not against a fixture. The requirement's own wording is that the fixture is "pointed at the
    new owner and run against a real session, not only self-tested," and a fixture proving a
    fixture proves nothing about the running system.

    Then kill the shell process deliberately, let the restart wrapper bring it back, and run the
    live check again. This round exercises the restart path the bar already depends on and is the
    one that would catch a server that acquires the name on a cold start but not on a respawn.

    Re-run plan 19-04's fault-injection fixture and record its output. Both injections must be
    green before the gate in Task 2 opens — the in-place update and action invocation carry extra
    verification weight precisely because there is no rollback after Task 4.

    Do not use a bare match-and-quit filter on the right-hand side of a pipe anywhere in this
    task's commands. Under strict pipe failure handling a successful early match can terminate
    the producer and surface a spurious failure exit, flipping a gate's verdict as a function of
    input length rather than content.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; hypr/.config/hypr/scripts/quickshell-doctor --self-test &amp;&amp; O=$(busctl --user list --no-pager | grep -cE '^org\.freedesktop\.Notifications'); test "$O" -eq 1 || { echo "owner count $O"; exit 1; }; systemctl --user kill quickshell.service; sleep 6; O2=$(busctl --user list --no-pager | grep -cE '^org\.freedesktop\.Notifications'); test "$O2" -eq 1 || { echo "post-respawn owner count $O2"; exit 1; }; hypr/.config/hypr/scripts/tests/notif-fault-inject &amp;&amp; test -s .planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md &amp;&amp; echo PREGATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `19-RETIREMENT-BASELINE-swaync.md` exists and records a checklist run whose hit count is
      greater than zero — a zero baseline before deletion is a checklist failure, not a pass.
    - Both bus-list fixtures name the shell as the legitimate owner; the poisoned fixture still
      contains two owners and the compliant fixture exactly one.
    - All four doctor locations agree on the declared owner: the registry, the two self-test
      assertions, and the self-test's isolated registry redeclaration. A grep for the old owner
      string across the whole doctor script returns 0.
    - `hypr/.config/hypr/config/autostart.lua` is UNCHANGED by this task — `git diff --name-only`
      for this task's commits does not list it.
    - The live bus check returns exactly one owner, and that owner is the shell process.
    - The live check returns exactly one owner again after a deliberate kill and an automatic
      respawn; the respawn is confirmed to have produced a new process id.
    - `notif-fault-inject` exits 0 and its output is recorded in the SUMMARY.
    - No command in this task pipes into a bare match-and-quit filter.
  </acceptance_criteria>
  <done>Exactly one owner of the notification bus name is proven against the real session and again after a respawn, and the deletion's before-baseline is captured while the surface still exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GATE-02 — blocking human render gate, before anything is deleted</name>
  <what-built>
    The complete replacement: a shell-owned notification server, popups with gestures, in-place
    updates and ring progress, a right-edge slide-out centre with grouped history and clear-all,
    a shared quick-toggle grid, three live sliders, persistent do-not-disturb, suppression, and a
    transient toast frame. Nothing has been deleted yet.
  </what-built>
  <how-to-verify>
    Run every criterion in `19-UI-SPEC.md` § "GATE-02 Render-Gate Criteria" — section A
    (aesthetic judgment, 5 items) and section B (capability audit, 7 items). Judge each one live:
    a real notification sent, a real drag performed, a real theme switch. "Looks right in the
    source" does not pass any of them.

    Cross-check section B against `19-BEHAVIOUR-BASELINE.md` from plan 19-02 — it records what
    the outgoing daemon actually did, and its `## Dead Definitions` table is the exclusion list,
    so a capability the user never actually had is not scored as a regression. Its
    `## Not a Port Specification` section names the widget-order inversion explicitly: the centre
    deliberately puts history at the top and pins the controls at the bottom, which is a design
    decision, not a regression.

    Three items need specific setup:
    - **B.3, in-place update:** plan 19-04's fault-injection fixture already proved this
      mechanically in Task 1 and its output is in the SUMMARY. Confirm it visually too — send a
      progress-style notification and watch the ring advance on one unmoving card.
    - **B.4, action buttons end to end:** pair a real phone over bluetooth. Confirm three things
      the deferred item itself specifies: no external toolkit dialog appears, the confirmation
      renders through the new server with working accept and reject, and `GetCapabilities` lists
      both the body and actions capabilities. This is G-15-7's own stated closing verification
      and it is the concrete required proof, not an optional extra.
    - **B.7, do-not-disturb across a restart:** set it, run `systemctl --user restart
      quickshell.service`, confirm it comes back set.

    Also surface for a decision, not a check: plan 19-06 recorded an explicit assumption that a
    slider row whose backend is entirely absent renders disabled rather than hiding. Confirm that
    default is acceptable or say otherwise.
  </how-to-verify>
  <resume-signal>Type "approved" to pass the gate, or describe each failing criterion by its section letter and number. Any single failure blocks the deletion until fixed and re-checked — Phase 18's own gate needed three iterations before passing, so a failure here is expected behaviour, not a setback.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: The one-way door — authorise the deletion</name>
  <decision>Proceed with deleting the outgoing notification daemon from repo and host, with no soak window and no rollback path.</decision>
  <context>
    This is the phase's one-way door. The package leaves the host in the same commit sequence as
    the config removal; there is no disabled-but-installed soak period. If the replacement
    misbehaves after this point, recovery means reinstalling and reconfiguring the old daemon by
    hand, not reverting a flag.

    The evidence in front of this decision:
    - The render gate above passed on all 12 criteria, judged live.
    - The fault-injection fixture is green on both injections (Task 1).
    - Exactly one bus-name owner, proven live and again after a respawn (Task 1).
    - `19-SECURITY.md` from plan 19-07 reports its open-threat count — check it is zero. A
      non-zero count is a legitimate reason to hold here.
    - `theme-stress-test` was clean in plan 19-03 and is re-run after the deletion in Task 5.

    The research recommended a disabled-but-installed soak. The user overrode that at scoping.
    This checkpoint exists so that override is re-affirmed with the actual replacement in front of
    you rather than inherited from a decision made before it was built.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the deletion</name>
      <pros>Closes the phase as scoped; no half-migrated state; the two-owner race cannot recur because only one owner remains installed.</pros>
      <cons>No fast rollback. A defect found next week is a manual reinstall.</cons>
    </option>
    <option id="hold">
      <name>Hold the deletion and fix a named issue first</name>
      <pros>Keeps the fallback available while a specific concern is addressed.</pros>
      <cons>Leaves the phase open and leaves two potential owners installed on the host, which is the state the atomic swap exists to end.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, or hold — and if holding, name the issue to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: The atomic autostart swap — one task, one commit</name>
  <files>hypr/.config/hypr/config/autostart.lua, hypr/.config/hypr/scripts/quickshell-doctor</files>
  <read_first>
    - `hypr/.config/hypr/config/autostart.lua` — read the whole file, especially its own header
      prohibition on entries being added, removed or reordered, the carve-outs previous phases
      recorded against it, and the comment block above the shell service start which already
      anticipates that from this phase the same process carries the notification server.
    - `hypr/.config/hypr/scripts/quickshell-doctor` — the declared-owner registry entry, still
      holding the old owner string after Task 1 deliberately left it alone.
    - `.planning/phases/19-notification-server-centre/19-CONTEXT.md` D-19-42 — the atomic-swap
      rule and its reasoning.
  </read_first>
  <action>
    <!-- planner-discipline-allow: swaync -->
    <!-- planner-discipline-allow: swaync-launch.sh -->
    ONE task, ONE commit, both halves of the swap together.

    Remove the outgoing daemon's launch entry and its comment block from `autostart.lua`. In the
    SAME edit, flip `quickshell-doctor`'s declared-owner registry entry to the shell, and update
    the comment block above the shell service start so it states in present tense that this
    process is now the notification owner rather than anticipating that it will be.

    Commit those changes as a single commit. This is not a style preference. The launch entry and
    the declared owner are the same fact recorded in two places; splitting them across two commits
    means every boot from the state after commit one to the state after commit two silently runs
    two processes racing for the bus name, and the loser fails quietly rather than loudly. The
    file's own header records a standing prohibition on entries being added, removed or reordered
    — this removal is a carve-out that must be recorded in the file the same way previous phases
    recorded theirs, naming this plan.

    Record the removal-carve-out justification in the file itself, not only in the commit message.
    A commit message is not where the next reader of this file looks.

    Do not delete the launch script, the config directory, the package or anything else in this
    task. Those are Task 5. This task's diff is exactly two files.

    When editing, do not leave the removed daemon's name behind in any comment in either file.
    The verification greps both whole files, comments included, because a name surviving only in
    prose is exactly how the retirement checklist reports a false hit two phases later.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; A=hypr/.config/hypr/config/autostart.lua; D=hypr/.config/hypr/scripts/quickshell-doctor; HA=$(grep -ci 'swaync' "$A" || true); HD=$(grep -ci 'swaync' "$D" || true); test "$HA" -eq 0 || { echo "autostart residual $HA"; exit 1; }; test "$HD" -eq 0 || { echo "doctor residual $HD"; exit 1; }; C=$(git log --format=%H -1 -- "$A"); N=$(git show --name-only --format= "$C" | grep -c .); F=$(git show --name-only --format= "$C" | sort | tr '\n' ' '); echo "commit=$C files=$F"; test "$N" -eq 2 || { echo "atomic swap touched $N files, expected exactly 2"; exit 1; }; git show --name-only --format= "$C" | grep -q 'quickshell-doctor' || { echo "doctor not in the same commit"; exit 1; }; hypr/.config/hypr/scripts/quickshell-doctor --self-test &amp;&amp; echo ATOMIC_SWAP_OK</automated>
  </verify>
  <acceptance_criteria>
    - **The swap is exactly one commit.** The most recent commit touching `autostart.lua` contains
      exactly two files and one of them is `quickshell-doctor`. This is asserted mechanically in
      `verify` and is the single most important criterion in this plan.
    - A case-insensitive grep for the outgoing daemon's name over the whole of `autostart.lua`,
      comments included, returns 0.
    - The same grep over the whole of `quickshell-doctor` returns 0.
    - The shell service start entry is still present, in its original position and relative order —
      only the outgoing daemon's entry was removed.
    - The comment block above the shell service start states in present tense that this process
      owns the notification server.
    - A removal carve-out is recorded inside `autostart.lua` itself, naming this plan, in the same
      form previous carve-outs in that file take.
    - `quickshell-doctor --self-test` exits 0 against the flipped registry.
    - No other file is touched by this task.
  </acceptance_criteria>
  <done>The launched daemon and the declared owner changed together in one commit, so no boot state ever exists in which two processes race for the notification bus name.</done>
</task>

<task type="auto">
  <name>Task 5: Remove the rest of the surface and re-run every closing check</name>
  <files>swaync/, hypr/.config/hypr/scripts/swaync-launch.sh, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/reload.sh, matugen/.config/matugen/config.toml, matugen/.config/matugen/templates/swaync-colors.css, hypr/.config/hypr/scripts/retirement-check, install.sh, .planning/phases/19-notification-server-centre/19-RETIREMENT-AFTER-swaync.md, .planning/milestones/v3.0-phases/15-audio-connectivity-panels/deferred-items.md</files>
  <read_first>
    - `.planning/phases/19-notification-server-centre/19-CONTEXT.md` § "Files the retirement and
      debt items touch" — the verified removal surface with per-file citations.
    - `.planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md` from
      Task 1 — the authoritative list of where the surface actually is on this host, which
      supersedes any list written earlier.
    - `hypr/.config/hypr/scripts/retirement-check` — its registry rows and how the previous
      surface's row was flipped after its own deletion.
    - `theme-engine/.config/theme-engine/contract.json` and
      `theme-engine/.config/theme-engine/lib/reload.sh` — the two contract entries and the reload
      step to remove.
    - `matugen/.config/matugen/config.toml` — the template block, and note its input template file
      also needs removing.
    - `install.sh` — the package list.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md` — the
      after-document shape to mirror.
  </read_first>
  <action>
    <!-- planner-discipline-allow: swaync -->
    <!-- planner-discipline-allow: swaync.css -->
    <!-- planner-discipline-allow: swaync-style.css -->
    <!-- planner-discipline-allow: swaync-colors.css -->
    Remove everything the baseline found, working from that document rather than from a list
    written before it: the launch script, the stow package directory with its config and its
    stylesheet, the two theme-contract entries, the matugen template block and its input template
    file, the reload step, the package list entry in the installer, and the package itself from the
    host. Also remove the two engine-rendered artefacts from the theme state directory, since
    nothing will regenerate them.

    Flip the checklist registry row from pending to retired — but only AFTER the removal, matching
    how the previous surface's row was handled. Flipping it first would make the after-run pass
    against a registry that no longer expects to find anything.

    Re-run the checklist and record the full output at
    `.planning/phases/19-notification-server-centre/19-RETIREMENT-AFTER-swaync.md`. This run must
    report zero hits. A residual hit is not a formatting nit — it is a file that still references
    a daemon that no longer exists, which is exactly what the checklist was built to catch.

    Re-run the closing checks whose results this deletion could invalidate: the doctor's full live
    run, the live single-owner bus check, and the full theme stress test with a clean-tree
    assertion afterwards. The stress test's representative-file set was already corrected in plan
    19-03 precisely so this deletion would not break it — this run confirms that held.

    Finally, close G-15-7 in Phase 15's deferred-items document. Plan 19-02 deliberately left it
    open pointing at this plan, because its own closing condition requires a real phone to have
    been paired against the new server — which the render gate in Task 2 did. Record the gate's
    result there and mark it closed. Do not close it on the strength of the capability declaration
    alone; the pairing is the proof.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; hypr/.config/hypr/scripts/retirement-check swaync; RC=$?; echo "checklist exit=$RC"; test "$RC" -eq 0 || exit 1; test ! -d swaync &amp;&amp; test ! -f hypr/.config/hypr/scripts/swaync-launch.sh &amp;&amp; C=$(grep -ci 'swaync' theme-engine/.config/theme-engine/contract.json matugen/.config/matugen/config.toml theme-engine/.config/theme-engine/lib/reload.sh install.sh | awk -F: '{s+=$2} END {print s}'); test "$C" -eq 0 || { echo "residual refs $C"; exit 1; }; pacman -Q swaync >/dev/null 2>&1 &amp;&amp; { echo "package still installed"; exit 1; }; hypr/.config/hypr/scripts/quickshell-doctor &amp;&amp; O=$(busctl --user list --no-pager | grep -cE '^org\.freedesktop\.Notifications'); test "$O" -eq 1 &amp;&amp; theme-engine/.config/theme-engine/theme-stress-test &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; test -s .planning/phases/19-notification-server-centre/19-RETIREMENT-AFTER-swaync.md &amp;&amp; echo RETIRE_COMPLETE</automated>
    <human-check>Log out and back in. Confirm the desktop comes up with notifications working from a cold boot — the autostart path is the one thing the running session cannot prove, since the shell service was already running when its entry changed.</human-check>
  </verify>
  <acceptance_criteria>
    - `retirement-check` for the retired surface exits 0 with zero hits, and its full output is
      recorded at `19-RETIREMENT-AFTER-swaync.md`.
    - The stow package directory and the launch script no longer exist.
    - A case-insensitive grep for the retired name across `contract.json`, the matugen config, the
      reload library and the installer sums to 0.
    - The matugen input template file is deleted, and the two engine-rendered artefacts are gone
      from the theme state directory.
    - `pacman -Q` for the retired package fails — it is uninstalled from the host.
    - The checklist registry row reads retired, and the flip happened after the removal commits,
      not before — verifiable from commit order.
    - `quickshell-doctor` full live run passes; the live bus check still returns exactly one owner.
    - `theme-stress-test` exits 0 and `git status --porcelain` is empty afterwards.
    - G-15-7 in `deferred-items.md` is marked closed with the render gate's pairing result
      recorded as its evidence.
    - After a full logout and login, notifications work from a cold boot.
  </acceptance_criteria>
  <done>The outgoing daemon is gone from repo and host, the checklist reads zero before and after, and every closing check the deletion could have invalidated has been re-run green.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
## Artifacts this phase produces (this plan's share)

**Documents (new)**
- `.planning/phases/19-notification-server-centre/19-RETIREMENT-BASELINE-swaync.md` —
  pre-deletion checklist run, expected non-zero
- `.planning/phases/19-notification-server-centre/19-RETIREMENT-AFTER-swaync.md` — post-deletion
  checklist run, required zero

**Scripts amended**
- `hypr/.config/hypr/scripts/quickshell-doctor` — declared-owner registry entry, both self-test
  assertions, and the self-test's isolated registry redeclaration all re-pointed to the shell
- `hypr/.config/hypr/scripts/retirement-check` — registry row for the retired surface flipped
  from pending to retired

**Fixtures regenerated**
- `hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-busctl-list.txt`
- `hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-two-owner-busctl-list.txt`

**Config amended**
- `hypr/.config/hypr/config/autostart.lua` — outgoing daemon's launch entry removed and a
  removal carve-out recorded in-file, in the same commit as the doctor's owner flip

**Removed from repo**
- `swaync/` (stow package: `config.json`, `style.scss`)
- `hypr/.config/hypr/scripts/swaync-launch.sh`
- `matugen/.config/matugen/templates/swaync-colors.css`
- `matugen/.config/matugen/config.toml` → the `[templates.swaync]` block
- `theme-engine/.config/theme-engine/contract.json` → the `swaync.css` and `swaync-style.css`
  entries
- `theme-engine/.config/theme-engine/lib/reload.sh` → the reload step
- `install.sh` → the package list entry

**Removed from host**
- The `swaync` package
- `~/.local/state/theme/swaync.css` and `~/.local/state/theme/swaync-style.css`

**Documents closed**
- `deferred-items.md` → G-15-7 marked closed with the render gate's real-pairing result as
  evidence
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot sequence → D-Bus name acquisition | Between the compositor starting the shell service and the shell acquiring the bus name there is a window in which a second launched daemon could claim it first. Removing the second launcher and the declared owner in one commit closes this window structurally. |
| package manager → host | The retired package is removed from the host; nothing replaces it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-29 | Spoofing | two-owner boot race | critical | mitigate | The autostart entry removal and the declared-owner flip land in exactly one commit (Task 4), asserted mechanically by inspecting the commit's file list. No repository state exists in which one has changed and the other has not. |
| T-19-30 | Spoofing | residual second notification server on host | high | mitigate | The package is uninstalled from the host in the same plan, and the live single-owner check is re-run after the removal. The checklist's zero-hit after-run proves no launcher survives in the repo. |
| T-19-31 | Repudiation | deletion without a passed gate | high | mitigate | The blocking render gate and the one-way-door decision checkpoint both precede every deletion task; the plan is marked non-autonomous so neither can be skipped by an auto-advance setting. |
| T-19-32 | Tampering | checklist flipped before removal | medium | mitigate | The registry row flip is required to occur after the removal commits, verifiable from commit order — flipping first would let the after-run pass against a registry that no longer looks. |
| T-19-33 | Denial of Service | cold-boot regression invisible to a running session | medium | mitigate | The autostart path cannot be proven from the session that was already running when its entry changed, so Task 5 requires an actual logout and login before the plan is done. |
| T-19-SC | Tampering | package operations | medium | mitigate | The only package operation is a removal of an already-installed official-repository package. No package is installed, so no legitimacy audit applies — RESEARCH.md § Package Legitimacy Audit records this explicitly. |
</threat_model>

<verification>
- `19-RETIREMENT-BASELINE-swaync.md` records a non-zero pre-deletion run;
  `19-RETIREMENT-AFTER-swaync.md` records a zero-hit post-deletion run.
- The atomic swap commit contains exactly two files, one of which is the doctor script.
- `quickshell-doctor --self-test` and the full live run both pass.
- The live bus check returns exactly one owner, before the deletion, after a respawn, and after
  the deletion.
- `notif-fault-inject` green before the gate.
- `theme-stress-test` exits 0 with a clean tree afterwards.
- The retired package is absent from the host and from the installer's list.
- Notifications work after a full logout and login.
</verification>

<success_criteria>
Exactly one owner of `org.freedesktop.Notifications` proven live and again after a respawn; the
human render gate passed on all twelve criteria before anything was deleted; the outgoing daemon
gone from repo and host with the checklist reading zero before and after; the autostart swap
landed as one atomic commit; and the stress test still clean with a clean tree afterwards.
</success_criteria>

<output>
Create `.planning/phases/19-notification-server-centre/19-08-SUMMARY.md` when done.
</output>
