---
phase: 19-notification-server-centre
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - theme-engine/.config/theme-engine/lib/wallpaper.sh
  - theme-engine/.config/theme-engine/theme-stress-test
  - hypr/.config/hypr/hyprlock.conf
  - hypr/.config/hypr/scripts/theme-init.sh
  - hypr/.config/hypr/scripts/wallpaper-picker.sh
  - stow.sh
  - .gitignore
autonomous: true
requirements: [LEDGER-07]

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`theme-stress-test` completes a full run to exit 0 with zero failures (ROADMAP SC-6, LEDGER-07)."
    - "`git status --porcelain` reports a clean tree both partway through the stress run and after it finishes (ROADMAP SC-6, LEDGER-07)."
    - "The active-wallpaper pointer lives under `~/.local/state/theme/`, outside the stow tree, and every consumer resolves it from there (D-19-45)."
    - "No path under the stow tree needs a `.gitignore` exemption for runtime wallpaper state — the exemption block is deleted rather than kept (D-19-45's explicit rejection of the gitignore approach)."
    - "A fresh install still lands on a wallpaper: `stow.sh` seeds the pointer at its new location (D-19-45, reproducibility constraint)."
  artifacts:
    - "theme-engine/.config/theme-engine/lib/wallpaper.sh"
    - "theme-engine/.config/theme-engine/theme-stress-test"
    - "stow.sh"
  key_links:
    - "The wallpaper pointer's new home is the same `~/.local/state/theme/` directory the theme engine already owns and `commit.sh` already excludes from `rsync --delete` — so the move reuses an established engine-owned location rather than inventing one."
    - "Dropping the retiring daemon's stylesheet from the stress test's representative set must happen BEFORE plan 19-08 deletes the template that renders it, or the stress test starts failing on a missing file mid-phase."
---

<objective>
Close LEDGER-07: `theme-stress-test` reaches a full clean run and the git tree is still clean
afterwards.

Two changes get it there. First, D-19-45 moves the active-wallpaper pointer out of the stow tree
into engine-owned state and repoints every consumer — replacing a `.gitignore` exemption with a
structural fix, so Phase 22's fresh-install gate has nothing left to excuse. Second, D-19-46
drops the retiring daemon's compiled stylesheet from the stress test's representative-file set
before plan 19-08 deletes the template that produces it.

Purpose: Phases 20, 21 and 22 all run their gates against this script. It has to be trustworthy
before they do, and RETIRE-08's contract check in Phase 21 needs a baseline that isn't already
red for an unrelated reason.

Output: a wallpaper pointer that lives in state, a stress test that passes end to end, and a
tree that stays clean through a full run.

**Correction the executor must carry into the SUMMARY:** REQUIREMENTS.md describes LEDGER-07 as
"the tracked-symlink repoint that dirties the tree on every static switch." That premise is
stale. The pointer is already untracked and already carries a `.gitignore` exemption from
13-06's "untrack-and-seed" fix, and `git status` on this repo is currently clean. RESEARCH.md
flagged this as an open question (whether 13-06's fix and D-19-45 are the same symlink) — they
are the same symlink. D-19-45 is still live work, but it is a *structural* change that removes
the exemption, not a bug fix for a dirty tree. Do not skip it on the grounds that the tree is
already clean, and do not report it as having fixed a tree-dirtying bug.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-notification-server-centre/19-CONTEXT.md
@.planning/phases/19-notification-server-centre/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the active-wallpaper pointer out of the stow tree into engine-owned state</name>
  <files>theme-engine/.config/theme-engine/lib/wallpaper.sh, hypr/.config/hypr/hyprlock.conf, hypr/.config/hypr/scripts/theme-init.sh, hypr/.config/hypr/scripts/wallpaper-picker.sh, stow.sh, .gitignore</files>
  <read_first>
    - `theme-engine/.config/theme-engine/lib/wallpaper.sh` — read the whole file. Specifically:
      the `WALLPAPER_DIR` and `FRAME_DIR` definitions near the top, the three `ln -sfr` call
      sites that repoint the pointer, the `! -name` exclusions that stop the pointer being
      offered as a selectable wallpaper, and every comment block explaining the D-06/D-08/WR-01
      live-choice rules — those rules must survive the move unchanged.
    - `.gitignore` — the wallpaper runtime-pointer block and its explanatory comment, which
      records 13-06's untrack-and-seed reasoning. This is the block D-19-45 supersedes.
    - `stow.sh` — the seeding branch that creates the pointer at install time when it is absent,
      and the surrounding comment explaining why a cloned repo must not be written into.
    - `hypr/.config/hypr/hyprlock.conf`, `hypr/.config/hypr/scripts/theme-init.sh`,
      `hypr/.config/hypr/scripts/wallpaper-picker.sh` — the three known external consumers.
    - `.planning/phases/19-notification-server-centre/19-RESEARCH.md` § "LEDGER-07 Mechanics" —
      the verified line citations and the engine-owned-location precedent.
  </read_first>
  <action>
    Before editing, run a repo-wide grep for the pointer filename and enumerate EVERY consumer.
    The five files named in `read_first` are the ones research verified; treat the grep as
    authoritative over that list, because a consumer missed here becomes a broken lock screen or
    a broken Material You palette source, both of which fail silently until the next theme
    switch.

    Introduce one new variable in `wallpaper.sh` for the pointer's new home under the theme
    engine's own state directory — the same directory that already holds the extracted frames,
    the recorded theme name and every rendered template, and that `commit.sh` already excludes
    from its `rsync --delete`. Point all three `ln -sfr` call sites at it. Leave the
    wallpaper *source* directory alone: the images themselves are legitimate stow-managed repo
    content and are not what D-19-45 moves.

    Once the pointer no longer lives beside the images, the `! -name` exclusions that kept it
    out of the selectable-wallpaper listings are dead code guarding a file that is no longer in
    the searched tree. Remove them rather than leaving them as inert defences — a stale guard
    reads as a live constraint to the next person.

    Repoint every consumer the grep found. Update the install seeding so a fresh clone still
    lands on a wallpaper: create the state directory if absent, then seed the pointer there.
    Reproducibility is a project constraint, and a fresh Arch system running the installer must
    still come up with a wallpaper and a working lock screen background.

    Delete the `.gitignore` block entirely. D-19-45 chose the move specifically over keeping the
    exemption, on the grounds that an exemption leaves runtime state inside the stow tree for
    Phase 22's fresh-install gate to keep excusing. Leaving the block behind as a belt-and-braces
    measure would preserve exactly the thing the decision removes.
  </action>
  <precondition>`~/.local/state/theme/` exists and is writable — it already holds the rendered template set on this host, so the migration writes into an established directory rather than creating one at runtime.</precondition>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; grep -rn 'current\.jpg' . --exclude-dir=.git --exclude-dir=.planning | grep -vE '^\./(theme-engine/\.config/theme-engine/lib/wallpaper\.sh|stow\.sh|hypr/\.config/hypr/(hyprlock\.conf|scripts/(theme-init|wallpaper-picker)\.sh))' | grep -v 'local/state/theme' ; grep -c 'Pictures/Wallpapers/current\.jpg' .gitignore 2>/dev/null | grep -qx 0 &amp;&amp; echo GITIGNORE_BLOCK_GONE</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Pictures/Wallpapers/current.jpg' .gitignore` returns 0 — the exemption block is
      deleted, not commented out.
    - No file anywhere in the repo (excluding `.planning/`) still resolves the pointer under the
      stow-managed wallpapers directory; a repo-wide grep for the old path returns zero hits.
    - `wallpaper.sh`'s three symlink call sites all target the state-directory path via the one
      new variable — `grep -c 'ln -sfr' theme-engine/.config/theme-engine/lib/wallpaper.sh`
      returns 3 and none of the three lines contains the wallpapers-directory variable.
    - The `! -name` exclusions that guarded the pointer inside the image listings are removed:
      `grep -c 'name "current.jpg"' theme-engine/.config/theme-engine/lib/wallpaper.sh` and the
      same over `hypr/.config/hypr/scripts/wallpaper-picker.sh` both return 0.
    - `stow.sh` seeds the pointer at the new location when absent and creates the state directory
      first; running `bash -n stow.sh` exits 0.
    - `bash -n` exits 0 for `wallpaper.sh`, `theme-init.sh` and `wallpaper-picker.sh`.
    - After a static theme switch, `readlink` on the new pointer path resolves to a real image
      file and `git status --porcelain` is empty.
  </acceptance_criteria>
  <done>The active-wallpaper pointer is engine-owned state at a state-directory path, every consumer reads it there, the install seeds it there, and the stow tree needs no exemption.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the retiring daemon's stylesheet from the stress test's representative set</name>
  <files>theme-engine/.config/theme-engine/theme-stress-test</files>
  <read_first>
    - `theme-engine/.config/theme-engine/theme-stress-test` — read the `REPRESENTATIVE_FILES`
      array declaration and both surrounding commentary blocks that discuss which format
      families the set covers and why entries have been dropped before. RESEARCH.md verified
      three locations: the array itself and two commentary passages above it.
    - `theme-engine/.config/theme-engine/contract.json` — confirm which format each remaining
      representative file belongs to, so the format-family coverage claim in the updated
      commentary is true rather than assumed.
    - `.planning/phases/19-notification-server-centre/19-CONTEXT.md` D-19-46 — the locked
      decision, including its explicit instruction that nothing replaces the dropped entry.
  </read_first>
  <action>
    <!-- planner-discipline-allow: swaync -->
    Remove the retiring notification daemon's compiled stylesheet from the `REPRESENTATIVE_FILES`
    array and add nothing in its place (D-19-46). The four remaining entries still cover the
    gtk-css, lua-table and conf format families between them, so the check loses no format
    coverage — state that in the updated commentary and make it true against `contract.json`
    rather than repeating this plan's claim.

    Both commentary blocks above the array reference the same dropped file in their
    token-parity prose. Update both in the same pass. The Phase 18 precedent in this file's own
    history shows the pattern: when the previous bar's sheet was dropped, the commentary was
    revised alongside the array, not left describing a set that no longer exists.

    Do NOT add a QML palette representative as a replacement. It was considered during
    discussion and declined — it is a different artefact class from the CSS and conf sheets this
    check parses, and under D-19-43 the QML surfaces produce no theme-engine artefact to check
    at all.

    This must land before plan 19-08 deletes the matugen template that renders the dropped file.
    Doing it in the other order leaves the stress test red for an unrelated reason during the
    exact wave the render gate is being judged.

    When editing, do not leave the retired name behind in a comment. The verification below
    greps the whole file, comments included, because a name that survives only in prose is
    exactly how a retirement checklist reports a false hit two phases later.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; F=theme-engine/.config/theme-engine/theme-stress-test; H=$(grep -ci 'swaync' "$F" || true); test "$H" -eq 0 || { echo "residual hits $H"; exit 1; }; grep -q 'REPRESENTATIVE_FILES=(hyprland-tokens.lua wleave.css gtk-4.0-colors.css kitty.conf)' "$F" &amp;&amp; bash -n "$F" &amp;&amp; echo STRESS_SET_OK</automated>
  </verify>
  <acceptance_criteria>
    - A case-insensitive grep for the retired daemon's name over the entire stress-test file,
      comments included, returns 0.
    - `REPRESENTATIVE_FILES` contains exactly four entries and they are the four named in
      D-19-46; no new entry was added.
    - Both commentary blocks describe the current four-entry set and their format-family claim
      matches what `contract.json` records for each entry.
    - `bash -n theme-engine/.config/theme-engine/theme-stress-test` exits 0.
  </acceptance_criteria>
  <done>The stress test's representative set matches D-19-46 exactly, with no residual reference to the retiring surface anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Full clean stress run with a clean tree throughout</name>
  <files>.planning/phases/19-notification-server-centre/19-03-SUMMARY.md (run evidence only — no source file is modified by this task)</files>
  <read_first>
    - `theme-engine/.config/theme-engine/theme-stress-test` — read its run structure and what it
      reports at completion (switch count, pass/fail tallies, exit code), so the run is judged
      against the script's own contract rather than a guess.
    - `.planning/phases/19-notification-server-centre/19-RESEARCH.md` § "LEDGER-07 Mechanics" —
      the previously-proven-runnable evidence and what a full clean run looked like the last
      time one was recorded.
  </read_first>
  <action>
    Run the stress test to completion and capture its full output into the SUMMARY: switch count,
    pass tally, fail tally and exit code, verbatim. LEDGER-07 asks for a full clean run, so a
    partial run or a run with any failure does not close it.

    Assert tree cleanliness at two distinct points, not one: partway through the run and after it
    finishes. The requirement's own wording is "a full clean run with the tree still clean
    afterwards", and a mid-run check is what proves no intermediate step writes into the repo and
    then tidies up after itself. Capture both `git status --porcelain` outputs into the SUMMARY.

    If the run fails, do not adjust the script's assertions to make it pass. Diagnose the failure
    and fix the cause, or stop and report it — a stress test edited into passing is worth less
    than one that is honestly red, and Phases 20 through 22 all depend on this signal.

    Confirm the lock screen background and the Material You palette source both still resolve
    after the wallpaper-pointer move, since both read the pointer and both fail silently rather
    than loudly.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; theme-engine/.config/theme-engine/theme-stress-test; RC=$?; echo "exit=$RC"; test "$RC" -eq 0 &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; echo STRESS_CLEAN_OK</automated>
    <human-check>Lock the screen once and confirm the background image renders. Then switch to a different static theme and confirm the desktop wallpaper actually changes — both read the moved pointer and both fail silently if it is wrong.</human-check>
  </verify>
  <acceptance_criteria>
    - `theme-stress-test` exits 0 with zero reported failures; the switch count and pass/fail
      tallies are recorded verbatim in the SUMMARY.
    - `git status --porcelain` produces empty output at a mid-run checkpoint AND after the run
      completes; both outputs are recorded in the SUMMARY.
    - The lock screen renders its background image after the move.
    - A static theme switch changes the visible desktop wallpaper and leaves the tree clean.
    - No assertion inside `theme-stress-test` was weakened or removed to achieve the pass —
      `git diff` on that file for this plan shows only the representative-set and commentary
      changes from Task 2.
  </acceptance_criteria>
  <done>LEDGER-07 is closed on evidence: a full run to exit 0 with a clean tree recorded at two points.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
## Artifacts this phase produces (this plan's share)

**Shell variables / paths (new)**
- One new pointer-path variable in `theme-engine/.config/theme-engine/lib/wallpaper.sh`
  resolving the active-wallpaper symlink under `~/.local/state/theme/` — the engine-owned state
  directory that already holds the extracted frames, the recorded theme name and every rendered
  template

**Paths retired**
- The active-wallpaper symlink under the stow-managed wallpapers directory, and its
  `.gitignore` exemption block

**Config keys amended**
- `theme-engine/.config/theme-engine/theme-stress-test` → `REPRESENTATIVE_FILES` reduced to four
  entries (`hyprland-tokens.lua`, `wleave.css`, `gtk-4.0-colors.css`, `kitty.conf`) per D-19-46

**Scripts amended (consumer repoints)**
- `hypr/.config/hypr/hyprlock.conf`, `hypr/.config/hypr/scripts/theme-init.sh`,
  `hypr/.config/hypr/scripts/wallpaper-picker.sh`, `stow.sh`

**No QML type, D-Bus member or design token is produced by this plan** — it is entirely
theme-engine and install-path work, which is why it runs in wave 1 with no dependency on the
tracer.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → theme engine | `wallpaper.sh` resolves a symlink and passes the resolved path to image tooling and to the Material You generator. The pointer's new location is engine-owned state rather than a user-writable content directory, which narrows rather than widens this boundary. |
| installer → user home | `stow.sh` seeds the pointer into the user's state directory on a fresh install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-07 | Tampering | `wallpaper.sh` symlink resolution | medium | mitigate | The pointer moves into the engine-owned state directory, so it is no longer co-located with a directory the user drops arbitrary image files into. The existing validation on the per-video seek-offset sidecar and the array-expanded tooling invocations are preserved unchanged by this plan — verified by `git diff` showing no change to those blocks. |
| T-19-08 | Denial of Service | fresh-install seeding | medium | mitigate | `stow.sh` creates the state directory before seeding and the seeding branch stays guarded on absence, so a re-run neither overwrites a live choice nor fails on a missing parent. Covered by the `bash -n` and fresh-clone criteria in Task 1. |
| T-19-09 | Repudiation | `theme-stress-test` result | medium | mitigate | The run's exit code and both `git status` captures are recorded verbatim in the SUMMARY, and Task 3's criteria forbid weakening any assertion to reach a pass — so a green result cannot be produced by editing the test. |
| T-19-SC | Tampering | package installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
- `bash -n` exits 0 for `wallpaper.sh`, `theme-init.sh`, `wallpaper-picker.sh`, `stow.sh` and
  `theme-stress-test`.
- Repo-wide grep for the old stow-tree pointer path returns zero hits outside `.planning/`.
- `theme-stress-test` exits 0 with zero failures.
- `git status --porcelain` empty mid-run and post-run.
</verification>

<success_criteria>
`theme-stress-test` reaches a full clean run with the tree still clean afterwards, the
active-wallpaper pointer is engine-owned state outside the stow tree with every consumer
repointed and the install still seeding it, and no `.gitignore` exemption remains for runtime
wallpaper state.
</success_criteria>

<output>
Create `.planning/phases/19-notification-server-centre/19-03-SUMMARY.md` when done.
</output>
