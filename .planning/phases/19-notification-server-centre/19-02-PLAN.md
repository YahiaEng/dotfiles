---
phase: 19-notification-server-centre
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md
  - .planning/debug/bluetooth-enable-inert.md
  - .planning/debug/wifi-hidden-network-not-detected.md
  - .planning/debug/wifi-hidden-network-unsupported.md
  - .planning/debug/wifi-scan-progress-feedback.md
  - .planning/debug/wifi-wrong-password-external-dialog.md
  - .planning/milestones/v3.0-phases/15-audio-connectivity-panels/deferred-items.md
autonomous: true
requirements: [LEDGER-04]

estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`19-BEHAVIOUR-BASELINE.md` exists and enumerates the outgoing notification daemon's live behaviour read off the running implementation while it still exists, in gesture-and-observation form, not as a feature narrative (GATE-01)."
    - "The baseline asserts completeness mechanically as an empty unaccounted-key set, not as a claim of thoroughness (GATE-01 protocol step 4)."
    - "Every one of the six LEDGER-04 items carries a written disposition — resolved, or an explicitly-reasoned deferral naming what would close it and where that work belongs (ROADMAP SC-6)."
    - "The plan set records the corrected LEDGER-04 count in writing: five open session files, not six, plus one separately-tracked deferred item — the sixth historical session was already closed in Phase 18."
  artifacts:
    - ".planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md"
  key_links:
    - "The baseline's capability list is the exclusion-aware input to plan 19-08's GATE-02 render gate — a criterion there is only checkable because a capability was written down here first."
    - "The G-15-7 deferred item's own stated closing condition points at this phase's declared capability set, so its disposition is written here and its live proof is executed in plan 19-08."
---

<objective>
Two closing-condition obligations that have no code dependency and therefore belong in wave 1,
before any retirement work is written.

First, GATE-01: enumerate the outgoing notification daemon's behaviour off the LIVE
implementation while it still exists. Its whole value is that it reads a running system, and
every later wave erodes that value — plan 19-08 deletes every one of its sources.

Second, LEDGER-04: give each open debug session a written disposition, against the corrected
ground truth rather than the roadmap's stale count.

Purpose: the render gate in 19-08 can only judge "at least as good as what it replaced" against
a written record of what the replaced thing actually did. Without this document that judgment is
a memory test.

Output: `19-BEHAVIOUR-BASELINE.md` and six dispositioned ledger items.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-notification-server-centre/19-CONTEXT.md
@.planning/phases/19-notification-server-centre/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GATE-01 — enumerate the live notification daemon's behaviour before anything is deleted</name>
  <files>.planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md</files>
  <read_first>
    - `.planning/phases/18-qml-bar-retirement-machinery/18-BEHAVIOUR-BASELINE.md` § "GATE-01
      Recurrence Protocol" — the six numbered steps and the per-phase surface table. Read the
      whole document, not only that section: its `## Unaccounted Keys`, `## Dead Definitions`
      and `## Not a Port Specification` sections are the shape this document must mirror.
    - `swaync/.config/swaync/config.json` — the live behaviour source. The protocol's own
      surface table records that no dedicated resolver exists for this format, so it is read
      directly.
    - `swaync/.config/swaync/style.scss` — its themed surface.
    - `hypr/.config/hypr/scripts/swaync-launch.sh` — the launch wrapper and the state-dir
      stylesheet indirection it implements.
    - `.planning/research/PITFALLS.md` — the redesign-not-port failure mode this enumeration
      exists to contain: a small interaction relied on unconsciously that was never written
      down as a requirement.
  </read_first>
  <action>
    Write `.planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md` following the
    Phase 18 document's structure section for section.

    Enumerate from the four live sources named in `read_first`, plus the outgoing daemon's CLI
    surface as it is actually invoked in this repo — the toggle verb bound in `keybinds.lua`,
    the reload verb called from `reload.sh`, and the do-not-disturb enable and disable verbs
    called from both toggle grids. Record what each verb does from the user's seat, not what its
    flag letters are.

    Record capabilities in gesture-and-observation form: "a notification arrives and clears
    itself after N seconds unless it is critical", not "config key `timeout` is 5". The consumer
    is a human running the replacement side by side under a blocking gate, so every line must be
    something they can perform and observe.

    Include an `## Unaccounted Keys` section that asserts completeness mechanically: enumerate
    every top-level key of the live config with a runnable loop, subtract every key this document
    accounts for, and show the remainder as an explicitly-empty list. Do not write a sentence
    claiming the enumeration felt thorough.

    Include a `## Dead Definitions` table separating deliberate prior cuts from live
    capabilities — these become 19-08's GATE-02 exclusion list, so a capability the user never
    actually had cannot be scored as a regression.

    Include a `## Not a Port Specification` section naming this surface's concrete trap: the
    outgoing centre's widget order, which D-19-17 deliberately inverts. A later executor reading
    only a capability list could mistake that inversion for a regression; name it here so they
    cannot.

    Record positions, pixel values, colours and durations ONLY where they are a capability the
    user would notice losing. This document is not a port specification — REQUIREMENTS.md and
    PROJECT.md both frame this milestone as a redesign.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; B=.planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md; test -f "$B" &amp;&amp; for s in "## Unaccounted Keys" "## Dead Definitions" "## Not a Port Specification"; do grep -qF "$s" "$B" || { echo "MISSING SECTION $s"; exit 1; }; done; python3 -c "import json,sys; d=json.load(open('swaync/.config/swaync/config.json')); b=open('$B').read(); m=[k for k in d if k not in b]; print('UNACCOUNTED',m); sys.exit(1 if m else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `19-BEHAVIOUR-BASELINE.md` exists and contains the three named sections.
    - Every top-level key of the live `config.json` appears somewhere in the document — proven by
      the runnable loop in `verify`, which exits non-zero on any remainder, not by assertion.
    - The `## Unaccounted Keys` section shows an explicitly-empty remainder list, with the loop
      that produced it quoted inline.
    - The document records the toggle, reload, DND-enable and DND-disable CLI verbs and what each
      does observably; each of the four is traceable to the repo call site that invokes it.
    - `## Dead Definitions` is present and non-empty, or states in one line why the surface has
      no deliberate prior cuts.
    - No section of the document prescribes a pixel position, colour or transition duration as a
      requirement for the replacement — the widget-order inversion trap is named explicitly in
      `## Not a Port Specification`.
  </acceptance_criteria>
  <done>The outgoing daemon's live behaviour is written down in checkable gesture form, with completeness proven by an empty remainder rather than claimed, while its sources still exist.</done>
</task>

<task type="auto">
  <name>Task 2: LEDGER-04 — six dispositions across two ledgers, against corrected ground truth</name>
  <files>.planning/debug/bluetooth-enable-inert.md, .planning/debug/wifi-hidden-network-not-detected.md, .planning/debug/wifi-hidden-network-unsupported.md, .planning/debug/wifi-scan-progress-feedback.md, .planning/debug/wifi-wrong-password-external-dialog.md, .planning/milestones/v3.0-phases/15-audio-connectivity-panels/deferred-items.md</files>
  <read_first>
    - `.planning/phases/19-notification-server-centre/19-RESEARCH.md` § "LEDGER-04 Ground Truth"
      — the live count, the per-file root causes, and the finding that none of the five is fixed
      by notification-server work.
    - Each of the five open session files in `.planning/debug/` — read each one's frontmatter
      `status` and its recorded root cause before writing its disposition.
    - `.planning/milestones/v3.0-phases/15-audio-connectivity-panels/deferred-items.md` — the
      G-15-7 item, its "Owner condition" line, and its own stated closing verification.
    - `.planning/debug/resolved/panels-missing-animated-border.md` — confirm its frontmatter
      status field, since it is the historical sixth that Phase 18 already closed.
  </read_first>
  <action>
    Record the correction explicitly in the SUMMARY and in each touched file's disposition note:
    the roadmap's "six open debug sessions" conflates two different ledgers. The live count in
    `.planning/debug/` is FIVE session files; the historical sixth was already moved to
    `.planning/debug/resolved/` by Phase 18's LEDGER-01. The bluetooth pairing prompt the
    roadmap names is a SEPARATE item, G-15-7, tracked in Phase 15's deferred-items document and
    not a session file at all. Six dispositions are therefore owed, across two ledgers — do not
    silently produce five, and do not fabricate a resolution to reach six.

    For each of the five session files, append a dated Phase 19 disposition block to the file and
    set its frontmatter status. Research found none of the five is fixed as a byproduct of
    notification-server work: four are wifi-panel bugs or feature gaps and one is a
    bluetooth-adapter rfkill-state bug. This phase's declared boundary covers the notification
    server, the outgoing daemon's retirement, and the LEDGER-04/07/08 debt items — it does not
    cover wifi or bluetooth panel code. The expected disposition for all five is therefore an
    explicitly-reasoned deferral, and "explicitly-reasoned" is the load-bearing word: each block
    must name the root cause already diagnosed, the concrete change that would close it, and why
    that change lies outside this phase's boundary. A deferral that only says the words "out of
    scope" is a fabricated resolution wearing a different word.

    For G-15-7, record the disposition as resolved-by-construction in this phase and point at
    where its proof is executed. Its own document states the closing condition — the replacement
    ships with the capability declared and a real pairing is verified against it — and this
    phase's declared capability set satisfies the first half. Do NOT mark it closed here: update
    it to name plan 19-08's live pairing verification as the remaining step, and let 19-08 flip
    it. Marking it resolved before a phone has actually been paired is exactly the fabricated
    resolution the roadmap note warns against.

    Do not open any wifi or bluetooth panel source file in this task. If one of the five looks
    cheap to fix, that is a scope-boundary decision for the developer, not a planner-authorised
    drive-by — record it as a deferral with the low cost noted so the developer can act on it.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; N=$(ls .planning/debug/*.md 2>/dev/null | wc -l); test "$N" -eq 5 || { echo "expected 5 open session files found $N"; exit 1; }; for f in .planning/debug/*.md; do grep -qE '^status:[[:space:]]*(resolved|deferred)' "$f" || { echo "NO DISPOSITION $f"; exit 1; }; grep -qF 'Phase 19' "$f" || { echo "NO PHASE 19 BLOCK $f"; exit 1; }; done; grep -qF '19-08' .planning/milestones/v3.0-phases/15-audio-connectivity-panels/deferred-items.md || { echo "G-15-7 not pointed at its proof plan"; exit 1; }; echo LEDGER04_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five files remain in `.planning/debug/` and each carries a frontmatter status of
      either resolved or deferred plus a dated Phase 19 disposition block.
    - Each of the five deferral blocks names three things: the already-diagnosed root cause, the
      concrete change that would close it, and the boundary reason it is not made here. A block
      missing any of the three fails this criterion.
    - `deferred-items.md`'s G-15-7 entry names plan `19-08` as the location of its live pairing
      verification and is NOT marked closed by this plan.
    - The SUMMARY states the corrected count in one sentence: five open session files plus one
      separately-tracked deferred item, with the historical sixth already closed in Phase 18.
    - No file under `quickshell/.config/quickshell/modules/dashboard/WifiPanel.qml` or
      `BluetoothPanel.qml` is modified by this plan — `git diff --name-only` for this plan's
      commits lists no QML file at all.
  </acceptance_criteria>
  <done>All six LEDGER-04 obligations carry a written, reasoned disposition against the corrected ground truth, with no fabricated resolutions and no drive-by panel edits.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
## Artifacts this phase produces (this plan's share)

**Documents (new)**
- `.planning/phases/19-notification-server-centre/19-BEHAVIOUR-BASELINE.md` — GATE-01
  enumeration of the outgoing daemon, with the mandatory sections `## Unaccounted Keys`,
  `## Dead Definitions`, `## Not a Port Specification`

**Documents (amended)**
- Five session files in `.planning/debug/` — each gains a frontmatter status of resolved or
  deferred plus a dated Phase 19 disposition block
- `.planning/milestones/v3.0-phases/15-audio-connectivity-panels/deferred-items.md` — the
  G-15-7 entry gains a pointer to plan `19-08` as its live-verification location

**No code symbols, no file paths, no D-Bus members, no tokens and no config keys are produced by
this plan** — it is deliberately documentation-only, which is what lets it run in wave 1 with
zero dependency on the tracer.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | This plan writes and amends planning documents only; it adds no code path, no inbound data surface and no new privilege. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-05 | Repudiation | LEDGER-04 dispositions | medium | mitigate | Each disposition is written into the session file itself with a date and a named reason, so a deferral cannot later be mistaken for a resolution or for work nobody decided on. The mechanical five-file count assertion in `verify` prevents a silently-dropped item. |
| T-19-06 | Information Disclosure | `19-BEHAVIOUR-BASELINE.md` | low | accept | The enumeration reads local config for a notification daemon that holds no secrets and no env-var-driven behaviour (RESEARCH.md § Runtime State Inventory confirms the config carries none). Nothing sensitive is transcribed. |
| T-19-SC | Tampering | package installs | low | accept | This plan installs nothing. |
</threat_model>

<verification>
- `19-BEHAVIOUR-BASELINE.md` exists with all three mandatory sections and an empty unaccounted-key
  remainder proven by a runnable loop.
- Five session files, five dispositions, plus a pointed-forward G-15-7 entry.
- `git diff --name-only` over this plan's commits contains no `.qml`, `.lua`, `.sh` or `.json`
  source file.
</verification>

<success_criteria>
The outgoing notification daemon's live behaviour is enumerated in checkable form before any of
its sources are touched, and all six LEDGER-04 obligations carry an honest written disposition
against the corrected count rather than the roadmap's stale one.
</success_criteria>

<output>
Create `.planning/phases/19-notification-server-centre/19-02-SUMMARY.md` when done.
</output>
