---
phase: 19-notification-server-centre
plan: 07
type: execute
wave: 4
depends_on: [19-01, 19-04, 19-05, 19-06]
files_modified:
  - quickshell/.config/quickshell/modules/dashboard/WifiPanel.qml
  - .planning/phases/19-notification-server-centre/19-SECURITY.md
autonomous: true
requirements: [LEDGER-08]

estimate:
  tokens: 74000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The panel family carries a completed security review, and the verifier has been re-run over its gap-closure round (ROADMAP SC-6, LEDGER-08)."
    - "Phase 15's named-but-unclosed gap is closed: every text element in the wifi panel that renders an over-the-air network name carries an explicit text-format pin (LEDGER-08, `15-SECURITY.md` Unregistered Flags item 2)."
    - "`19-SECURITY.md` registers the phase's own new inbound D-Bus surface, not only the inherited panel gaps — untrusted summary, body, application-name and image data from arbitrary session processes now renders inside the shell (D-19-44)."
    - "Every threat in the register carries a severity and a disposition, and the document closes with zero threats left open."
  artifacts:
    - ".planning/phases/19-notification-server-centre/19-SECURITY.md"
  key_links:
    - "The review runs after every notification surface exists, so its inbound-data half covers the popup card, the history round-trip and the centre's group rows — reviewing the panels without the new inbound surface would miss the larger half."
    - "`19-SECURITY.md` mirrors `15-SECURITY.md`'s structure exactly, so the phase's own tooling and the next phase's baseline both read a familiar shape."
---

<objective>
Close LEDGER-08: a completed security review over the panel family, with the verifier re-run
over its gap-closure round.

The scope is deliberately wider than "Phase 15's leftovers." Phase 15's named gaps are the floor.
This phase took a system-wide bus name that any process on the session can send to, so untrusted
text, application names and image references from arbitrary senders now render inside the shell,
and the body-markup allowlist widens that further. Reviewing the panels without the new inbound
surface would miss the larger half of what changed.

Purpose: this is the right pass to run alongside taking a system-wide D-Bus role, and it runs
after every notification surface exists so there is something real to review rather than a
specification.

Output: a closed wifi-panel gap and `19-SECURITY.md` with zero threats open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-notification-server-centre/19-CONTEXT.md
@.planning/phases/19-notification-server-centre/19-RESEARCH.md
@.planning/milestones/v3.0-phases/15-audio-connectivity-panels/15-SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close Phase 15's named panel gap and audit the family for the same class</name>
  <files>quickshell/.config/quickshell/modules/dashboard/WifiPanel.qml</files>
  <read_first>
    - `.planning/milestones/v3.0-phases/15-audio-connectivity-panels/15-SECURITY.md` — read the
      whole document, and in particular its "Unregistered Flags" table. Item 2 there is the
      concrete, named, still-open gap: the wifi panel has zero text-format pins while network
      names arrive over the air, which is the same untrusted-string class an earlier threat
      already closed for the bluetooth panel. Read that closed bluetooth threat too — it is the
      worked example of what closing this one looks like in this codebase.
    - `quickshell/.config/quickshell/modules/dashboard/WifiPanel.qml` — the three locations
      `15-SECURITY.md` cites (the row label, the unelided full-name tooltip, and the
      forget-confirm), plus a full read to find any fourth location it did not cite.
    - `quickshell/.config/quickshell/modules/dashboard/BluetoothPanel.qml` — the already-closed
      counterpart, for the exact pin idiom this repo uses.
    - `quickshell/.config/quickshell/modules/dashboard/AudioPanel.qml` — the third panel in the
      family, to check for the same class rather than assuming it is clean.
  </read_first>
  <action>
    Pin the text format on every text element in the wifi panel that renders a network name. The
    cited three locations are the floor, not the ceiling — read the whole file and pin every
    binding that carries an over-the-air string, including any the prior review did not
    enumerate. A named gap that gets closed at exactly its three cited line numbers while a
    fourth instance sits ten lines away is a gap that was documented, not closed.

    Apply the same audit to the audio panel and to any other panel-family file that renders a
    string it did not itself author. Record what you found in each, including "nothing" — a panel
    reported clean without being read is the failure mode this whole requirement exists to
    correct.

    Use the exact pin idiom the bluetooth panel already uses. This repo has one convention for
    this and adding a second would make the next audit harder, not easier.

    Do not change any panel's layout, colour or behaviour in this task. It is a security pin
    pass; a behavioural change smuggled in alongside it would need its own render gate and this
    phase's gate is already scoped.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; W=quickshell/.config/quickshell/modules/dashboard/WifiPanel.qml; P=$(grep -c 'textFormat' "$W" || true); test "$P" -ge 3 || { echo "wifi panel pins: $P (expected >=3)"; exit 1; }; hypr/.config/hypr/scripts/quickshell-doctor &amp;&amp; echo PANEL_PINS_OK</automated>
    <human-check>Open the wifi panel and confirm the network list renders normally — names, tooltips and the forget-confirm all still display the same text they did before. The pins must be invisible in normal use; if any name now renders escaped entities or lost its wording, the pin was applied to the wrong property.</human-check>
  </verify>
  <acceptance_criteria>
    - Every text element in `WifiPanel.qml` bound to a network name carries an explicit text-format
      pin; the count is at least the three cited locations and matches the number of such bindings
      actually present in the file.
    - The audio panel and every other panel-family file rendering an externally-sourced string has
      been read and its finding recorded in the SUMMARY — including files found to need no change.
    - The pin idiom matches the one already used in `BluetoothPanel.qml` character for character.
    - `git diff` for this task shows no layout, colour, sizing or behavioural change in any panel.
    - `quickshell-doctor` passes, including its colour lint.
    - The wifi panel still renders network names, tooltips and the forget-confirm with their
      original wording — no escaped entities appear in normal use.
  </acceptance_criteria>
  <done>Phase 15's named panel gap is closed at every instance of the class, not only at its cited line numbers, with the rest of the family audited and reported.</done>
</task>

<task type="auto">
  <name>Task 2: Author the phase threat register and re-run the verifier over the gap closure</name>
  <files>.planning/phases/19-notification-server-centre/19-SECURITY.md</files>
  <read_first>
    - `.planning/milestones/v3.0-phases/15-audio-connectivity-panels/15-SECURITY.md` — the exact
      document structure to mirror: trust boundaries, threat register, accepted-risks log, audit
      trail, sign-off. This phase's tooling and the next phase's baseline both expect this shape.
    - `.planning/phases/19-notification-server-centre/19-RESEARCH.md` § "Security Domain" — the
      applicable categories, the four named threat patterns for this stack with their standard
      mitigations, and § "LEDGER-08 Mechanics" for what the re-run means concretely.
    - Every threat-model block already authored in this phase's plans — `19-01`, `19-04`, `19-05`
      and `19-06` each carry one with threat IDs already allocated. This document consolidates
      them and verifies each against the shipped implementation; it does not re-derive them from
      scratch.
    - The shipped implementation of each mitigation: the text-format pins in `NotifCard.qml` and
      the centre's group rows, the allowlist filter in `NotifMarkdown.qml`, the link
      confirmation, the progress-value clamp, the asynchronous image loading, and the icon
      resolution path.
  </read_first>
  <action>
    Write `.planning/phases/19-notification-server-centre/19-SECURITY.md` mirroring the Phase 15
    document's structure section for section.

    Consolidate the per-plan threat registers from `19-01`, `19-04`, `19-05` and `19-06` into one
    phase-level register. Every threat keeps its ID, its severity and its disposition. For each
    one, cite the concrete implementation that mitigates it — a file and the binding or function
    that does the work — in the evidence-cited style Phase 15's document uses. A register row
    whose mitigation column restates the plan's intention rather than pointing at shipped code is
    not verified, and this document's whole value is that it was checked against the
    implementation rather than against the plan.

    Cover, at minimum, the four surfaces research named for this phase's own new attack surface:
    untrusted body and summary text rendering through the markup path; untrusted application-icon
    and image hint values reaching an icon resolver and an image loader; the link-confirmation
    flow, which the locked decision already mitigates by design but which still needs its own
    register row and a mitigation-verified disposition; and the second-owner spoofing case for
    the bus name, whose live proof runs in plan 19-08.

    Include the panel-family gap closed in Task 1 as a closed row with its own evidence citation,
    and carry forward any still-open item from Phase 15's accepted-risks log with an explicit
    re-disposition rather than silently dropping it.

    Then re-run the verifier over the resulting diff — the same tooling that produced Phase 15's
    own document — and record its output in the audit trail. "Verifier re-run over its
    gap-closure round" is the requirement's literal wording: the review is not the artifact, the
    verified review is.

    Close with a completed sign-off checklist and zero threats open. If the verifier surfaces a
    finding at or above the blocking severity, do not lower the severity to close the document —
    fix the finding, re-run, and record both rounds in the audit trail. Plan 19-08's decision
    checkpoint reads this document's open count as one of its inputs, so an honestly non-zero
    count there is a signal that reaches the right place.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; S=.planning/phases/19-notification-server-centre/19-SECURITY.md; test -f "$S" &amp;&amp; for h in "Trust Boundaries" "Threat Register" "Accepted Risks" "Audit Trail" "Sign-Off"; do grep -qF "$h" "$S" || { echo "MISSING SECTION $h"; exit 1; }; done; grep -qE 'threats_open:[[:space:]]*0' "$S" || { echo "threats_open not zero"; exit 1; }; R=$(grep -cE '^\| T-19-' "$S"); test "$R" -ge 15 || { echo "register rows $R (expected >=15 consolidated)"; exit 1; }; echo SECURITY_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `19-SECURITY.md` exists and carries all five sections from the Phase 15 template.
    - Every threat ID allocated across plans `19-01`, `19-04`, `19-05` and `19-06` appears in the
      consolidated register with its severity and disposition preserved.
    - Every mitigated row cites a concrete file and the binding or function implementing it — a
      row citing only a plan or a decision ID fails this criterion.
    - Rows exist for all four of the phase's own new-surface threat patterns: markup rendering of
      untrusted text, sender-supplied icon and image references, the link-confirmation flow, and
      second-owner spoofing of the bus name.
    - Task 1's panel gap appears as a closed row with an evidence citation, and every item from
      Phase 15's accepted-risks log is either carried forward with a re-disposition or shown as
      closed — none is silently dropped.
    - The audit trail records the verifier invocation and its output; if more than one round was
      needed, both rounds are recorded.
    - The document reports zero threats open, and no severity was lowered to achieve that — the
      audit trail shows any finding was fixed rather than reclassified.
  </acceptance_criteria>
  <done>The panel family and the phase's own new inbound D-Bus surface are both reviewed in one verified document with zero threats open and every mitigation cited against shipped code.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
## Artifacts this phase produces (this plan's share)

**Documents (new)**
- `.planning/phases/19-notification-server-centre/19-SECURITY.md` — phase-level threat register
  mirroring `15-SECURITY.md`'s structure: Trust Boundaries, Threat Register, Accepted Risks Log,
  Audit Trail, Sign-Off. Frontmatter carries `threats_open: 0`.

**Threat IDs consolidated (allocated in earlier plans, verified here)**
- `T-19-01` … `T-19-25` from plans `19-01`, `19-04`, `19-05`, `19-06`, plus the supply-chain row
  `T-19-SC` recorded as accepted-not-applicable since this phase installs no package

**QML types (extended)**
- `WifiPanel` — explicit text-format pins on every element rendering an over-the-air network
  name; no layout, colour or behavioural change

**No new QML type, D-Bus member, token, script or config key is produced by this plan.**
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| over-the-air network data → panel rendering | Network names arrive from radio broadcast and reach text elements in the wifi panel. This is the boundary Phase 15 named and left open. |
| session D-Bus → shell rendering | The phase's own new boundary, reviewed here in full for the first time against shipped code rather than against a plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-26 | Tampering | `WifiPanel.qml` network-name bindings | high | mitigate | Explicit text-format pins on every binding carrying an over-the-air string, applied at every instance in the file rather than only at the three previously-cited locations, using the idiom already established for the bluetooth panel. |
| T-19-27 | Repudiation | `19-SECURITY.md` completeness | medium | mitigate | Every mitigated row must cite a file and the binding implementing it; the register-row count is asserted mechanically in `verify`, so a consolidated document that silently dropped rows from an earlier plan fails rather than passes. |
| T-19-28 | Tampering | severity reclassification to reach zero open | medium | mitigate | Task 2's criteria forbid lowering a severity to close the document and require the audit trail to show any finding was fixed and re-verified. Plan 19-08's decision checkpoint reads the open count, so an honest non-zero value reaches a human rather than being absorbed here. |
| T-19-SC | Tampering | package installs | low | accept | This plan installs no package. |
</threat_model>

<verification>
- `19-SECURITY.md` exists with all five sections, `threats_open: 0`, and a consolidated register
  containing every threat ID allocated across the phase.
- The verifier's invocation and output are recorded in the audit trail.
- `WifiPanel.qml` carries text-format pins on every network-name binding.
- `quickshell-doctor` passes; no panel layout or colour changed.
</verification>

<success_criteria>
The panel family carries a completed security review with Phase 15's named gap closed at every
instance of its class, the phase's own new system-wide D-Bus inbound surface is registered and
mitigated with citations to shipped code, and the verifier has been re-run over the gap-closure
round with zero threats left open.
</success_criteria>

<output>
Create `.planning/phases/19-notification-server-centre/19-07-SUMMARY.md` when done.
</output>
