---
phase: 06-themed-surfaces-utility-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - matugen/.config/matugen/templates/hyprlock-colors.conf
  - matugen/.config/matugen/templates/swayosd-colors.css
  - matugen/.config/matugen/templates/zen-userchrome.css
  - matugen/.config/matugen/templates/satty-colors.toml
  - matugen/.config/matugen/config.toml
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/commit.sh
autonomous: true
requirements: [LOCK-01, OSD-01, THM-05, SHOT-02]
user_setup: []

must_haves:
  truths:
    - "A theme-apply run renders 4 new state-dir files: hyprlock.conf, swayosd.css, zen-userchrome.css, satty.toml — each carrying the active palette"
    - "contract.json grows from 13 to 17 files entries (D-30 reconciliation: +4 new matugen targets; wlogout.css already exists and is edited elsewhere, not added here)"
    - "theme-parity passes render + key-parity + color-wellformedness for all 17 targets under both a dark fixture (catppuccin) and a light fixture (catppuccin-latte)"
    - "commit.sh idempotently symlinks ~/.config/satty/config.toml to the rendered satty.toml (walker/yazi symlink model), guarded like the gtk dir wiring"
    - "The satty palette contains the E53935ff literal true-red as the single documented literal-hex exemption (D-31), every other slot a rendered palette key"
  artifacts:
    - matugen/.config/matugen/templates/hyprlock-colors.conf (hypr-vars, 19 keys)
    - matugen/.config/matugen/templates/swayosd-colors.css (gtk-css @define-color, 19 keys)
    - matugen/.config/matugen/templates/zen-userchrome.css (css-literal :root custom properties + chrome selectors)
    - matugen/.config/matugen/templates/satty-colors.toml (toml, [general] static + [color-palette] themed)
    - theme-engine/.config/theme-engine/contract.json (17 files entries)
  key_links:
    - "config.toml [templates.hyprlock|swayosd|zen|satty] -> ~/.local/state/theme/{hyprlock.conf,swayosd.css,zen-userchrome.css,satty.toml}"
    - "commit.sh symlink -> ~/.config/satty/config.toml points at the rendered satty.toml"
    - "contract.json files array -> theme-parity dynamic palette enumeration (automatic light+dark coverage)"
  prohibitions:
    - statement: "No literal hex color value may appear in any of the 4 new templates except the E53935ff true-red in satty-colors.toml (D-31 exemption, established by the Phase 5 fzf bg:-1 precedent)"
      status: resolved
      verification: "grep each template for 6-hex-digit literals not inside a {{colors...}} substitution and not equal to the satty E53935 exemption; expect none"
    - statement: "wlogout.css must NOT be added as a new contract.json entry — it already exists in the 13-file contract and is only edited (in plan 06-03); adding it would make the array 18 and break the D-30 reconciliation"
      status: resolved
      verification: "contract.json files array length is exactly 17; wlogout.css appears exactly once"
---

<objective>
Register the four new pipeline-themed render targets that later surface plans consume: hyprlock's dedicated color file (LOCK-01/D-30 — stops hyprlock piggybacking on hyprland.conf), SwayOSD's pill CSS (OSD-01/D-24), Zen's chrome userChrome CSS (THM-05/D-27), and satty's annotation palette (SHOT-02/D-31). This is the single owner of contract.json + matugen/config.toml for the phase — all four templates land here so no downstream plan touches these shared hub files.

Purpose: Every new surface must flow through the established render→commit→parity contract (Phases 1-5). Centralizing registration here keeps contract.json/config.toml edited exactly once (Wave 1), and resolves the D-30 file-count ambiguity (Pitfall 1) to 13→17.
Output: 4 matugen templates, 4 config.toml blocks, 4 contract.json entries, 1 commit.sh satty symlink, verified by theme-parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md
@.planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md
@matugen/.config/matugen/templates/hyprland-colors.conf
@matugen/.config/matugen/templates/swaync-colors.css
@matugen/.config/matugen/config.toml
@theme-engine/.config/theme-engine/contract.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 new matugen templates (D-24/D-27/D-30/D-31)</name>
  <files>matugen/.config/matugen/templates/hyprlock-colors.conf, matugen/.config/matugen/templates/swayosd-colors.css, matugen/.config/matugen/templates/zen-userchrome.css, matugen/.config/matugen/templates/satty-colors.toml</files>
  <read_first>
    - matugen/.config/matugen/templates/hyprland-colors.conf (exact 19-key hypr-vars format to copy verbatim for hyprlock-colors.conf; note the $image blank-value comment stays out — hyprlock uses $image from hyprland's own background, this template is colors only)
    - matugen/.config/matugen/templates/swaync-colors.css (exact 19-key gtk-css @define-color block to copy for swayosd-colors.css)
    - matugen/.config/matugen/templates/walker-style.css (css-literal format precedent for zen-userchrome.css — CSS custom properties, not @define-color)
    - matugen/.config/matugen/templates/yazi-theme.toml (toml template precedent for satty-colors.toml)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Color section: per-surface 60/30/10 mappings + the satty [color-palette] array with the E53935ff exemption; Zen chrome selectors #nav-bar/#TabsToolbar/#sidebar-box/active-tab/urlbar per D-27)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Code Examples: hyprlock-colors.conf key list, satty [color-palette] palette array, Pitfall 4 Zen version note)
  </read_first>
  <action>
    Create matugen/.config/matugen/templates/hyprlock-colors.conf using the hypr-vars format copied from hyprland-colors.conf: the same 19 palette keys in `$key = rgba({{colors.KEY.default.hex_stripped}}ff)` form. Create swayosd-colors.css as a gtk-css @define-color block identical in key set to swaync-colors.css. Create zen-userchrome.css in css-literal format: a `:root` block mapping the 19 palette keys to `--KEY: {{colors.KEY.default.hex}};` custom properties, followed by chrome-colors-only selector rules (toolbar #nav-bar/#TabsToolbar background = var surface, sidebar #sidebar-box = var surface, URL bar = var surface_variant, active tab = var primary_container/on_primary_container pair, urlbar focus border = var primary) per D-27 — no new-tab-page or deep-element styling. Create satty-colors.toml with a static [general] section encoding D-02 behavior (default annotation tool = arrow, Enter/Ctrl+C = copy+save+exit — exact key spelling verify-at-execution via `satty --help`, see A1) and a [color-palette] section whose `palette` array is the 7-swatch D-31 row: primary, secondary, tertiary, on_surface, background as `{{colors.KEY.default.hex_stripped}}ff`, then the E53935ff literal true-red (the single exemption — themes like hackerman/vantablack have no red in `error`), then outline. Default selection = the primary (first) slot. Do NOT template a font-family on any of these four surfaces (UI-SPEC Design System: FiraCode Nerd Font stays hardcoded literal, the switchable font axis is kitty/vscodium/GTK/waybar only).
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && test -f matugen/.config/matugen/templates/hyprlock-colors.conf && test -f matugen/.config/matugen/templates/swayosd-colors.css && test -f matugen/.config/matugen/templates/zen-userchrome.css && test -f matugen/.config/matugen/templates/satty-colors.toml && grep -c 'colors.primary' matugen/.config/matugen/templates/hyprlock-colors.conf && grep -q 'E53935ff' matugen/.config/matugen/templates/satty-colors.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 template files exist
    - hyprlock-colors.conf contains 19 `$key = rgba({{colors...}}ff)` lines (grep count matches hyprland-colors.conf's key count)
    - swayosd-colors.css contains `@define-color` lines matching swaync-colors.css's key set
    - zen-userchrome.css contains a `:root` custom-property block and at least the #nav-bar and active-tab chrome selectors
    - satty-colors.toml contains a `[color-palette]` section, a `palette = [` array with 7 entries, and the E53935ff literal (the only non-templated hex)
    - No template contains a literal 6-hex-digit color outside a `{{colors...}}` substitution except satty's E53935ff
  </acceptance_criteria>
  <done>4 templates authored to the UI-SPEC surface mappings; satty carries the documented E53935ff exemption; no font-family templated.</done>
</task>

<task type="auto">
  <name>Task 2: Register templates in config.toml, contract.json (13→17), and commit.sh satty symlink (D-30)</name>
  <files>matugen/.config/matugen/config.toml, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/commit.sh</files>
  <read_first>
    - matugen/.config/matugen/config.toml (copy the [templates.hyprland]/[templates.swaync] block shape for the 4 new [templates.*] entries; output_path targets ~/.local/state/theme/)
    - theme-engine/.config/theme-engine/contract.json (the 13-entry files array — add exactly 4 entries; note hyprland.conf has exempt_keys ["image"], the others do not)
    - theme-engine/.config/theme-engine/lib/commit.sh (the walker/yazi/gtk idempotent `ln -sf` symlink block lines 78-104, including the `[[ -L ... ]]` folded-stow guard — copy that idiom for satty)
    - .planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md (exact contract.json entry JSON for each of the 4 targets)
  </read_first>
  <action>
    Add 4 blocks to matugen/config.toml: [templates.hyprlock] → ~/.local/state/theme/hyprlock.conf, [templates.swayosd] → swayosd.css, [templates.zen] → zen-userchrome.css, [templates.satty] → satty.toml, each with input_path pointing at the template from Task 1. Add exactly 4 entries to contract.json's files array: {name: hyprlock.conf, format: hypr-vars}, {name: swayosd.css, format: gtk-css}, {name: zen-userchrome.css, format: css-literal}, {name: satty.toml, format: toml} — taking the array from 13 to 17. Do NOT add wlogout.css (it already exists as entry #5 and is edited in 06-03, not added here — D-30 reconciliation). In commit.sh, after the yazi symlink block (~line 83) and before/after the gtk settings.ini wiring, add an idempotent `ln -sf "$STATE_DIR/satty.toml" "$HOME/.config/satty/config.toml"` with a `mkdir -p "$HOME/.config/satty"` first, following the same folded-stow-symlink guard shape used for gtk-3.0/gtk-4.0 (skip + warn if ~/.config/satty is itself a symlink). Cite D-30.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && bash -c 'command -v jq >/dev/null && [ "$(jq ".files | length" theme-engine/.config/theme-engine/contract.json)" = "17" ]' && grep -q 'templates.hyprlock' matugen/.config/matugen/config.toml && grep -q 'templates.swayosd' matugen/.config/matugen/config.toml && grep -q 'templates.zen' matugen/.config/matugen/config.toml && grep -q 'templates.satty' matugen/.config/matugen/config.toml && grep -q 'satty.toml' theme-engine/.config/theme-engine/lib/commit.sh && bash -n theme-engine/.config/theme-engine/lib/commit.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.files | length' contract.json` returns 17
    - contract.json contains entries named hyprlock.conf, swayosd.css, zen-userchrome.css, satty.toml with the correct format tags
    - contract.json contains exactly one wlogout.css entry (unchanged)
    - config.toml contains [templates.hyprlock], [templates.swayosd], [templates.zen], [templates.satty] blocks with correct output paths
    - commit.sh contains an `ln -sf` wiring ~/.config/satty/config.toml to $STATE_DIR/satty.toml, guarded against a folded stow symlink; `bash -n commit.sh` exits 0
  </acceptance_criteria>
  <done>4 targets registered in config.toml + contract.json (17 total); satty config wired via commit.sh symlink; wlogout.css untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify theme-parity renders all 17 targets under light + dark fixtures (D-29)</name>
  <files>theme-engine/.config/theme-engine/theme-parity (executed, not modified), theme-engine/.config/theme-engine/lib/contract.sh (read)</files>
  <read_first>
    - theme-engine/.config/theme-engine/theme-parity (how it enumerates contract_files, runs render-only across all palettes, and asserts the catppuccin dark + catppuccin-latte light mode fixtures — the new targets get automatic coverage via dynamic palettes/*.json enumeration)
    - theme-engine/.config/theme-engine/lib/contract.sh (per-format extractors for hypr-vars/gtk-css/css-literal/toml — confirm all 4 new formats are already supported, they are: walker-style.css is css-literal, yazi.toml is toml)
  </read_first>
  <action>
    Run theme-parity (render-only, headless-safe) and confirm it now validates 17 targets across every palette with both the dark (catppuccin) and light (catppuccin-latte) fixtures passing, including the 4 new files. This is the D-29 guarantee: theme-parity validates the rendered state-dir CSS only (zen profile symlinking is the reload step's job, not parity's). If any new template fails render or key-parity, fix the template's key set to match its analog (hyprland-colors.conf / swaync-colors.css / walker-style.css / yazi-theme.toml) before finalizing. The satty E53935ff literal must NOT trip color-wellformedness (it is a valid RRGGBBAA hex) or key-parity (identical across all themes).
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && HOME="$HOME" theme-engine/.config/theme-engine/theme-parity 2>&1 | tail -20 | grep -Eiq 'pass|ok|17' && theme-engine/.config/theme-engine/theme-parity >/dev/null 2>&1 && echo PARITY_OK</automated>
  </verify>
  <acceptance_criteria>
    - theme-parity exits 0 (render-only gate green)
    - theme-parity output references the 4 new targets (hyprlock.conf, swayosd.css, zen-userchrome.css, satty.toml) as rendered/checked, not skipped
    - The catppuccin (dark) and catppuccin-latte (light) mode fixtures both pass
    - No color-wellformedness or key-parity failure on any of the 17 targets
  </acceptance_criteria>
  <done>theme-parity green across 17 targets, light + dark fixtures; the 4 new surfaces are contract-covered automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| palette JSON → matugen render | palette hex values flow into rendered config; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | rendered satty.toml consumed by satty as its config | low | accept | Rendered from trusted palette JSONs through the same render→commit pipeline as every Phase 1-5 target; no user-supplied input reaches the template |
| T-06-03 | Information Disclosure | commit.sh symlink writing ~/.config/satty/config.toml | low | mitigate | Idempotent `ln -sf` with the existing folded-stow guard; state dir is chmod 700 (commit.sh existing invariant) |
</threat_model>

<verification>
- theme-parity exits 0 with 17 targets across all palettes, light + dark fixtures
- `jq '.files | length' contract.json` == 17
- `bash -n commit.sh` exits 0
</verification>

<success_criteria>
- 4 new render targets live in the contract and render for every theme (D-30: 13→17)
- satty config consumable via commit.sh symlink (D-31 palette + E53935ff exemption)
- Downstream plans 06-04 (hyprlock), 06-05 (satty), 06-06 (swayosd+zen) can consume these rendered files without touching contract.json/config.toml again
</success_criteria>

## Artifacts this phase produces

- matugen templates: `hyprlock-colors.conf`, `swayosd-colors.css`, `zen-userchrome.css`, `satty-colors.toml`
- config.toml blocks: `[templates.hyprlock]`, `[templates.swayosd]`, `[templates.zen]`, `[templates.satty]`
- contract.json entries: `hyprlock.conf`, `swayosd.css`, `zen-userchrome.css`, `satty.toml`
- state-dir rendered outputs: `~/.local/state/theme/{hyprlock.conf,swayosd.css,zen-userchrome.css,satty.toml}`
- commit.sh symlink: `~/.config/satty/config.toml → $STATE_DIR/satty.toml`

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-02-SUMMARY.md` when done.
</output>
