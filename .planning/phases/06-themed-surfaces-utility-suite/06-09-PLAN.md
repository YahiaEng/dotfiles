---
phase: 06-themed-surfaces-utility-suite
plan: 09
type: execute
wave: 4
depends_on: ['06-01', '06-03', '06-06']
files_modified:
  - hypr/.config/hypr/scripts/emoji-picker.sh
  - hypr/.config/hypr/scripts/color-picker.sh
  - hypr/.config/hypr/scripts/clipboard-wipe.sh
  - hypr/.config/hypr/config/autostart.conf
  - wlogout/.config/wlogout/layout
  - hypr/.config/hypr/config/keybinds.conf
autonomous: true
requirements: [UTIL-01, UTIL-02, UTIL-03]
user_setup: []

must_haves:
  truths:
    - "Super+Z opens an emoji picker (walker + elephant-symbols); the selection is typed into the focused app via wtype AND copied to the clipboard as backup (D-21)"
    - "Super+X opens the screen color picker (hyprpicker); the hex is copied to the clipboard and a notification shows the hex value + a color swatch (D-22)"
    - "Clipboard history is capped at ~100 entries (cliphist -max-items in autostart) and wiped on session end (wired into wlogout logout/shutdown/reboot action commands) with a manual 'wipe now' entry — no unbounded plaintext secrets (D-15)"
    - "The existing Super+C cliphist walker-dmenu flow stays untouched; the manual wipe uses a destructive-safe default-No confirm (D-15)"
    - "Utility keybinds land on the freed X/Z family: Super+Z emoji, Super+Shift+Z icon-theme, Super+X color, Super+Shift+X font (D-32/UI-SPEC)"
  artifacts:
    - hypr/.config/hypr/scripts/emoji-picker.sh, color-picker.sh, clipboard-wipe.sh
    - hypr/.config/hypr/config/autostart.conf (cliphist -max-items on both watchers)
    - wlogout/.config/wlogout/layout (cliphist wipe prepended to logout/shutdown/reboot actions)
    - hypr/.config/hypr/config/keybinds.conf (X/Z utility chords)
  key_links:
    - "keybinds Super+Z -> emoji-picker.sh -> walker symbols -> wtype + wl-copy; Super+X -> color-picker.sh -> hyprpicker -> wl-copy + notify swatch"
    - "autostart cliphist store -max-items 100 -> bounded history; wlogout logout/shutdown/reboot actions -> cliphist wipe (session-end secret wipe)"
    - "keybinds Super+Shift+Z -> icon-theme-picker.sh (from 06-07); Super+Shift+X -> font-switcher.sh (from 06-08)"
  prohibitions:
    - statement: "Clipboard history must NOT persist secrets across sessions — the cap (~100) AND the session-end wipe AND the manual wipe entry must all land in THIS plan/wave, never as a follow-up (D-15, RESEARCH Security Domain: Information Disclosure mitigation ships with the feature)"
      status: resolved
      verification: "autostart cliphist has -max-items; wlogout logout/shutdown/reboot actions run cliphist wipe; clipboard-wipe.sh exists with a default-No confirm"
    - statement: "The manual clipboard wipe confirmation must default to No (never pre-select the destructive option) — destructive-safe default (UI-SPEC Copywriting)"
      status: resolved
      verification: "clipboard-wipe.sh presents a Yes/No walker --dmenu with No first/default-focused"
---

<objective>
Ship the three quick utility pickers: emoji (UTIL-01/D-21 — walker+elephant-symbols typed via wtype AND copied), screen color picker (UTIL-02/D-22 — hyprpicker → hex copied + notification with swatch), and the clipboard cap + wipe policy (UTIL-03/D-15 — ~100-entry cap, session-end wipe, manual wipe entry, so no unbounded plaintext secrets). Also lands the freed Super+X/Z utility chord family (D-32), including the binds for the icon-theme (06-07) and font (06-08) pickers. Wave 4 — last, since it owns the final keybinds.conf, autostart.conf, and wlogout/layout edits.

Purpose: These are thin callers of already-deployed tools (walker/elephant, hyprpicker, cliphist). The clipboard security posture (cap+wipe from day one) is a launch requirement, not a follow-up. This plan closes the utility suite.
Output: emoji-picker.sh, color-picker.sh, clipboard-wipe.sh, cliphist cap, wlogout wipe hooks, X/Z keybinds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md
@.planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md
@.planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md
@hypr/.config/hypr/scripts/theme-switch.sh
@hypr/.config/hypr/config/autostart.conf
@hypr/.config/hypr/config/keybinds.conf
@wlogout/.config/wlogout/layout
</context>

<tasks>

<task type="auto">
  <name>Task 1: emoji-picker.sh + color-picker.sh (UTIL-01/UTIL-02, D-21/D-22)</name>
  <files>hypr/.config/hypr/scripts/emoji-picker.sh, hypr/.config/hypr/scripts/color-picker.sh</files>
  <read_first>
    - hypr/.config/hypr/scripts/theme-switch.sh (walker --dmenu exit-130 cancel pattern to reuse for the emoji picker)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Code Examples emoji-picker.sh: walker symbols provider -> wtype + wl-copy; Assumption A3 + Open Question 2 — VERIFY the elephant-symbols dmenu output format FIRST, and check whether walker's built-in symbols action already types+copies, which could shrink this to zero new code; Standard Stack: wtype recommended over ydotool; hyprpicker -a -f hex for the color picker)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Copywriting: Emoji Inserted + Color Copied notifications with icons/timeouts; color notification includes a swatch image hint, degrade to text-only if unsupported)
  </read_first>
  <action>
    First verify (A3/Open Question 2): run walker's symbols provider and observe the exact stdout format and whether its built-in action already types AND copies — if it already satisfies D-21, keep emoji-picker.sh a thin wrapper (or note it needs no wrapper). Create hypr/.config/hypr/scripts/emoji-picker.sh: invoke walker's symbols provider (dmenu or -s symbols), reuse the exit-130 cancel branch, extract the glyph from the selection (verify the separator), then wtype the glyph into the focused app AND wl-copy it as backup (D-21 — validate the selection is a recognized symbol-list entry before typing, not raw stdout, per Security Domain); fire the Emoji Inserted backup notification. Create hypr/.config/hypr/scripts/color-picker.sh: run hyprpicker to grab a screen color as hex, wl-copy it, and fire the Color Copied notification showing the hex value plus a color-swatch image hint (degrade gracefully to text-only hex if the notification daemon lacks image-hint support — no hard dependency); sanitize any hyprpicker error before notify-send. Cite D-21/D-22.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && [ -f hypr/.config/hypr/scripts/emoji-picker.sh ] && bash -n hypr/.config/hypr/scripts/emoji-picker.sh && [ -f hypr/.config/hypr/scripts/color-picker.sh ] && bash -n hypr/.config/hypr/scripts/color-picker.sh && grep -q 'wtype' hypr/.config/hypr/scripts/emoji-picker.sh && grep -q 'wl-copy' hypr/.config/hypr/scripts/emoji-picker.sh && grep -q 'hyprpicker' hypr/.config/hypr/scripts/color-picker.sh && grep -q 'wl-copy' hypr/.config/hypr/scripts/color-picker.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - emoji-picker.sh + color-picker.sh exist, bash -n clean
    - emoji-picker.sh types via wtype AND copies via wl-copy (D-21); validates the selection before typing
    - color-picker.sh uses hyprpicker and wl-copy, with a swatch-or-text notification (D-22)
    - The elephant-symbols output format was verified at execution (noted in a script comment)
  </acceptance_criteria>
  <done>Emoji picker types+copies; color picker copies hex with a swatch notification.</done>
</task>

<task type="auto">
  <name>Task 2: Clipboard cap + session-end wipe + manual wipe (UTIL-03, D-15)</name>
  <files>hypr/.config/hypr/config/autostart.conf, wlogout/.config/wlogout/layout, hypr/.config/hypr/scripts/clipboard-wipe.sh</files>
  <read_first>
    - hypr/.config/hypr/config/autostart.conf (the two cliphist watcher lines 32-33 — add the -max-items cap to the store commands)
    - wlogout/.config/wlogout/layout (the logout/shutdown/reboot action commands from 06-03 — prepend a cliphist wipe so secrets never persist past a session; RESEARCH: no native session-end hook exists, wire into wlogout's own action commands)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (UTIL-03: cliphist -max-items + cliphist wipe; Security Domain Information Disclosure — cap+wipe IS the mitigation, must ship in the same wave)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Copywriting: manual wipe picker row, destructive confirmation prompt with default-No, wipe-completed notification, silent session-end wipe)
  </read_first>
  <action>
    (1) In autostart.conf, add a cap to both cliphist store watchers — change the two `wl-paste ... --watch cliphist store` lines to `cliphist store -max-items 100` (D-15 ~100-entry cap; verify the exact cliphist flag name at execution). (2) In wlogout/.config/wlogout/layout, prepend `cliphist wipe &&` (or a wrapped `cliphist wipe;`) to the logout, shutdown, and reboot action commands so clipboard secrets are wiped at session end (silent, no notification — the session is ending); keep the uwsm/hyprshutdown action tail unchanged (do not break the Phase-4-audited power commands). (3) Create hypr/.config/hypr/scripts/clipboard-wipe.sh: a manual "wipe now" flow — a walker --dmenu Yes/No destructive confirmation with No first/default-focused (never pre-select destructive), running `cliphist wipe` on Yes and firing the History Wiped notification. This does NOT touch the existing Super+C cliphist list flow (stays untouched, D-15). Cite D-15.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && grep -q 'max-items' hypr/.config/hypr/config/autostart.conf && grep -q 'cliphist wipe' wlogout/.config/wlogout/layout && [ -f hypr/.config/hypr/scripts/clipboard-wipe.sh ] && bash -n hypr/.config/hypr/scripts/clipboard-wipe.sh && grep -q 'cliphist wipe' hypr/.config/hypr/scripts/clipboard-wipe.sh && python3 -c "import json; [json.loads(l) for l in open('wlogout/.config/wlogout/layout') if l.strip()]" && grep -q 'hyprshutdown --post-cmd' wlogout/.config/wlogout/layout && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - autostart.conf cliphist store watchers carry a max-items cap
    - wlogout/layout logout/shutdown/reboot actions run cliphist wipe (session-end wipe); layout is still valid JSON per line; hyprshutdown power tails unchanged
    - clipboard-wipe.sh exists, bash -n clean, Yes/No confirm with default-No, runs cliphist wipe
    - Super+C flow (keybinds.conf line 45) is unchanged
  </acceptance_criteria>
  <done>Clipboard is capped, wiped on session end, and manually wipeable — no unbounded plaintext secrets (D-15).</done>
</task>

<task type="auto">
  <name>Task 3: Utility keybinds on the freed X/Z family (D-32)</name>
  <files>hypr/.config/hypr/config/keybinds.conf</files>
  <read_first>
    - hypr/.config/hypr/config/keybinds.conf (confirm Super+X/Z were freed by 06-05's screenshot-bind removal; Super+C/T/B/W must stay untouched per D-32; add the four X/Z utility chords)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Interaction Contract utility keybinds table: Super+Z emoji, Super+Shift+Z icon-theme, Super+X color, Super+Shift+X font)
  </read_first>
  <action>
    Add four binds to keybinds.conf (D-32/UI-SPEC): Super+Z → emoji-picker.sh, Super+Shift+Z → icon-theme-picker.sh (from 06-07), Super+X → color-picker.sh, Super+Shift+X → font-switcher.sh (from 06-08). Add a manual clipboard-wipe entry reachable per D-15 (either a Super-chord or documented as reachable via the picker — clipboard-wipe.sh can be bound or invoked from the existing clipboard flow; keep Super+C itself unchanged). Confirm Super+C (clipboard), Super+T (theme), Super+B (waybar), Super+W (wallpaper) are untouched. Document the chord assignments in a comment for Phase 7's MENU-07 cheat-sheet. Cite D-32.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && grep -q 'emoji-picker.sh' hypr/.config/hypr/config/keybinds.conf && grep -q 'icon-theme-picker.sh' hypr/.config/hypr/config/keybinds.conf && grep -q 'color-picker.sh' hypr/.config/hypr/config/keybinds.conf && grep -q 'font-switcher.sh' hypr/.config/hypr/config/keybinds.conf && grep -q 'mainMod, C, exec, cliphist list' hypr/.config/hypr/config/keybinds.conf && grep -q 'mainMod, T, exec' hypr/.config/hypr/config/keybinds.conf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - keybinds.conf binds Super+Z emoji, Super+Shift+Z icon-theme, Super+X color, Super+Shift+X font
    - Super+C cliphist flow and Super+T/B/W remain unchanged
    - Chord assignments documented in a comment for the Phase 7 cheat-sheet
  </acceptance_criteria>
  <done>All four utility chords bound on the freed X/Z family; existing binds intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard history (cliphist sqlite) | may hold pasted passwords/tokens across sessions |
| elephant-symbols output → wtype | provider output typed into the focused window |
| hyprpicker error → notify-send | subprocess error text surfaced to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-16 | Information Disclosure | clipboard history retaining secrets across sessions | high | mitigate | D-15 cap (~100) + session-end wipe (wlogout actions) + manual wipe entry — all ship in THIS plan/wave, never a follow-up (RESEARCH Security Domain requires the mitigation land with the feature) |
| T-06-17 | Tampering | wtype typing unsanitized elephant-symbols output | low | mitigate | Validate the selected string is a recognized symbol-list entry before typing, not raw walker stdout (Security Domain) |
| T-06-18 | Tampering | unsanitized hyprpicker stderr → notify-send | low | mitigate | Sanitize/truncate error output before notify-send (existing head -c 200 pattern) |
</threat_model>

<verification>
- emoji-picker.sh (wtype + wl-copy), color-picker.sh (hyprpicker + wl-copy + swatch), clipboard-wipe.sh (default-No confirm) all bash -n clean
- autostart cliphist capped; wlogout logout/shutdown/reboot run cliphist wipe; layout still valid JSON
- X/Z utility chords bound; Super+C/T/B/W untouched
- Live-session manual: Super+Z types an emoji; Super+X copies a hex with a swatch; a session-end wipe clears history
</verification>

<success_criteria>
- Emoji picker (type+copy), color picker (hex+swatch), clipboard cap+wipe policy from day one (UTIL-01/02/03)
- Freed X/Z utility chord family wired without breaking any existing bind (D-32)
</success_criteria>

## Artifacts this phase produces

- `hypr/.config/hypr/scripts/emoji-picker.sh`, `color-picker.sh`, `clipboard-wipe.sh` (new)
- `hypr/.config/hypr/config/autostart.conf` cliphist `-max-items` cap on both store watchers
- `wlogout/.config/wlogout/layout` `cliphist wipe` prepended to logout/shutdown/reboot actions
- keybinds: Super+Z (emoji), Super+Shift+Z (icon-theme), Super+X (color), Super+Shift+X (font)

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-09-SUMMARY.md` when done.
</output>
