---
phase: 22-fresh-install-proof
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - README.md
  - hypr/.config/hypr/config/env.lua
  - VERIFICATION.md
autonomous: true
requirements: [RETIRE-09]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`README.md` no longer advertises a surface this desktop does not have — the feature-table row and the repo-tree diagram describe the surfaces that actually ship today."
    - "`env.lua`'s native-Wayland-client enumeration names clients that currently exist, rewritten rather than scrubbed, per D-21-19."
    - "The human-run VM checklist in `VERIFICATION.md` §6 and §8 names the current surface inventory — bar, notification popups and centre, dashboard drawer, OSD indicators, power menu, workspace overview, Media tab with the cava ring, walker, Thunar — by requirement ID, not by package name."
    - "`VERIFICATION.md` §5 instructs the VM tier to run `install.sh` unflagged, and records in prose that this reverses a prior decision rather than slipping the change in silently (D-22-11)."
    - "D-22-02: `VERIFICATION.md` §7's pass bar is `theme-doctor` exit 0 AND `theme-parity` 0 failures AND human visual confirmation, minus a written exemption list — and that list is authored BEFORE any VM run, with a reason per entry."
    - "`retirement-check --all` still exits 0 with zero `[FAIL]` lines after every prose edit — the rewrites did not introduce a new blocking hit, the failure mode Phase 21 hit twice."
    - "The three correctly-written retirement-lineage comments D-22-07 names — in `autostart.lua`, `windowrules.lua` and `quickshell-doctor` — are byte-unchanged."
  artifacts:
    - README.md
    - hypr/.config/hypr/config/env.lua
    - VERIFICATION.md
  key_links:
    - "`VERIFICATION.md` §5's install invocation → whether `section_hardware` and `section_personal` run → whether the `system/` tree is installed for the first time in any reproduction proof."
    - "`VERIFICATION.md` §7's pass bar → what the human is allowed to accept at §8 → whether INST-03 actually closes."
    - "The §7 exemption list, authored before the run → the only permitted deviations from a clean bar. Anything outside it is a real defect, not a rationalisation."
    - "Every prose edit → `retirement-check`'s report tier → the `repo-prose` hit counts, which must go down, never up."
  prohibitions:
    - "Do not rewrite `README.md` wholesale. Its wider staleness — `.conf`-era Hyprland config paths, a missing cava package, absent OSD/power-menu/dashboard surfaces — is real but explicitly out of this phase's scope; the scope is the SC-3 review-pass fix only."
    - "Do not promote any report-tier stale-prose hit to `retirement-check`'s blocking tier. D-22-07 rejects this because it re-arms the exemption interlock that stalled Phase 20's final plan."
    - "Do not touch the retirement-lineage comments in `hypr/.config/hypr/config/autostart.lua`, `hypr/.config/hypr/config/windowrules.lua` or `hypr/.config/hypr/scripts/quickshell-doctor` — D-22-07 names these as correctly rewritten already, and sweeping them would violate D-21-19's rewrite-never-scrub rule in the opposite direction."
    - "Do not delete a retired surface's name from a comment and leave a gap. D-21-19: comments naming a retired surface are rewritten to name what currently exists, never scrubbed."
    - "Do not author the §7 exemption list after a VM run, or extend it in response to a red line. Authoring it in advance is the whole mechanism that prevents a failure being rationalised away."
    - "Do not restructure `install.sh` to add a hardware-without-personal entry point. It is the most precise answer to the §5 question and it is explicitly deferred — this phase's job is to prove `install.sh`, not restructure it."
    - "Do not add an `[Install]` section to, or an enable step for, the Quickshell systemd units when describing the VM procedure. Their absence is the reproducibility guarantee that no wants-symlink is ever written outside the repository, stated in their own headers."
---

<objective>
Correct the reproduction procedure and the repository prose that five package
deletions made factually wrong, and author the VM tier's pass bar before the VM
tier runs.

Purpose: SC-3's mechanical half is already satisfied — `retirement-check --all`
reports `failed_classes=0` across all eight registered surfaces. What earns SC-3 is
the one-time human read of the surviving non-`.planning` hits, and that read has
already found its targets: the feature table and repo-tree diagram in `README.md`
make a false factual claim about which notification surface this desktop runs, and
`env.lua`'s client enumeration lists two surfaces that no longer exist. Separately,
`VERIFICATION.md` is half-migrated: it already says "the Quickshell bar (including
its in-process power menu)" while §6 and §8 still name a package deleted in Phase
19, and its §5 invocation and §7 pass bar both need amending before the VM tier
can run against them.

Output: three corrected files, a `VERIFICATION.md` that describes the desktop that
actually exists, and a written exemption list that fixes the VM bar in advance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-fresh-install-proof/22-CONTEXT.md
@.planning/phases/22-fresh-install-proof/22-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
Produced by THIS plan (new sections in existing files — excluded from drift
verification):

| Artifact | Kind | Notes |
|---|---|---|
| `VERIFICATION.md` § "Pre-authored exemption list (D-22-02)" | new section | A table of check-name, reason and the source the reason was read from; the only permitted deviations from the §7 pass bar. |
| `VERIFICATION.md` §5 D-61-reversal note | new prose | Records that the VM tier now runs `install.sh` unflagged, and that this reverses a standing decision. |

Referenced but produced elsewhere in this phase:
`hypr/.config/hypr/scripts/stow-link-check` and
`verify/theme-doctor-session-allowlist.txt` (plans 22-02 and 22-04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two factually-wrong claims in README.md and the stale client enumeration in env.lua</name>

  <read_first>
    - `README.md` lines 16-30 (the stack/feature table) and lines 32-88 (the repo-structure fenced diagram). Read both regions in full before editing — the diagram block for the retired package spans more lines than a single-line edit would suggest.
    - `hypr/.config/hypr/config/env.lua` lines 1-25 — the whole cursor-theme comment block, so the rewrite reads as a coherent sentence rather than a patched fragment.
    - `.planning/REQUIREMENTS.md` — the QBAR / QNOTIF / QOSD / QPOWER / QMEDIA requirement blocks, so surfaces are named by the IDs that actually exist rather than invented ones.
    - `hypr/.config/hypr/config/autostart.lua` around line 186, `hypr/.config/hypr/config/windowrules.lua`, and `hypr/.config/hypr/scripts/quickshell-doctor` around line 723 — read these to see what a CORRECTLY rewritten retirement-lineage comment looks like, and to confirm you are leaving them alone.
    - `.planning/phases/22-fresh-install-proof/22-PATTERNS.md` § "README.md — feature table row + tree diagram" and § "env.lua lines 15-16".
  </read_first>

  <files>README.md, hypr/.config/hypr/config/env.lua</files>

  <action>
    Before editing, record the current per-surface `repo-prose` report counts by
    running `retirement-check --all` and capturing every `[REPORT] <surface>/repo-prose:
    N reference(s)` line. This is the before-measurement the acceptance criteria
    compare against; without it "the count went down" is unverifiable.

    `README.md`, the stack table around line 24: the Notifications row names a
    package retired under RETIRE-03. Rewrite the row's value to name the surface
    that actually delivers notifications today — the Quickshell notification server
    and centre built under the QNOTIF requirement family — using the same
    short-product-name register the neighbouring rows use, not a requirement ID
    (this table is user-facing prose, and its neighbours read "Quickshell",
    "Walker", "Kitty").

    `README.md`, the repo-structure diagram: the block for the RETIRE-03 package
    spans roughly lines 70-74 — its directory line plus three file lines plus the
    trailing separator. Remove that whole block. The package's tree no longer
    exists in this repository, so unlike a comment this is not a rewrite-vs-scrub
    question: the diagram is a factual inventory of directories that exist, and a
    directory that does not exist has no correct rewritten form. Verify the fenced
    block's tree-drawing characters still line up after the removal.

    Do not touch anything else in `README.md`. Its wider staleness is real and out
    of scope.

    `hypr/.config/hypr/config/env.lua`, the cursor-theme comment block around lines
    14-20: the enumeration of native Wayland clients names two surfaces retired
    under RETIRE-02 and RETIRE-03 alongside clients that still exist. Rewrite the
    enumeration — do not scrub it — so it lists the clients that currently render
    cursors on this desktop: the terminal, the launcher, the Quickshell shell
    process (which now carries the bar, the notification surfaces, the OSD, the
    power menu and the dashboard in one process), and Thunar under Wayland. The
    rewritten sentence must still explain WHY the variables are set, which is the
    comment's actual job.

    After all three edits, re-run `retirement-check --all` and compare against the
    before-measurement.
  </action>

  <verify>
    <automated>hypr/.config/hypr/scripts/retirement-check --all > /tmp/rc-after.txt 2>&amp;1; echo "rc=$?"; grep -c '^\[FAIL\]' /tmp/rc-after.txt</automated>
    <automated>grep -c 'failed_classes=0' /tmp/rc-after.txt</automated>
    <automated>git diff --quiet -- hypr/.config/hypr/config/autostart.lua hypr/.config/hypr/config/windowrules.lua hypr/.config/hypr/scripts/quickshell-doctor &amp;&amp; echo "lineage comments untouched"</automated>
    <automated>awk '/^```/{n++} END{print "fence count:", n}' README.md</automated>
  </verify>

  <acceptance_criteria>
    - `retirement-check --all` exits 0 and `grep -c '^\[FAIL\]'` on its output returns 0.
    - Every surface's `Summary:` line still reads `failed_classes=0` — the count of `failed_classes=0` occurrences equals the number of registered surfaces.
    - The `repo-prose` `[REPORT]` count for the RETIRE-02 surface and for the RETIRE-03 surface are each strictly lower than the before-measurement recorded at the start of this task.
    - `git diff --quiet -- hypr/.config/hypr/config/autostart.lua hypr/.config/hypr/config/windowrules.lua hypr/.config/hypr/scripts/quickshell-doctor` exits 0 — the three correctly-written lineage comments are byte-unchanged.
    - `README.md`'s fenced-block count is even (the structure diagram's fence pair is intact after the block removal).
    - `git diff --stat -- README.md` shows changes confined to the table region and the structure-diagram region; no other section of the file is touched.
    - The `env.lua` comment block still contains a `why` clause — it explains the purpose of the cursor variables, not merely a list of names.
    - `hypr/.config/hypr/scripts/hypr-lua-harness` (or `hyprctl` config reload if the harness is unavailable) parses `env.lua` without error after the edit.
  </acceptance_criteria>

  <done>
    `README.md` describes the desktop that ships today in both its feature table and
    its structure diagram; `env.lua`'s client enumeration is rewritten to name
    surviving clients; `retirement-check --all` is still green and the report-tier
    prose counts have gone down, not up.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite VERIFICATION.md §5, §6 and §8 to the current install scope and surface inventory</name>

  <read_first>
    - `VERIFICATION.md` in full — you need §1-§4 for continuity (the NOPASSWD scoping warning at the top is load-bearing and must survive intact), §5 lines 152-171, §6 lines 173-186, §8 lines 209-223, §9 lines 224-236, and the closing "Re-running this gate" section which already states the push-before-either-tier rule (D-56).
    - `install.sh` lines 630-700 (`section_hardware`, including the `sudo install` of the `system/` tree around line 698), lines 735-746 (`section_personal` and the D-61 comment explaining why it is skipped), and lines 783-800 (the dispatch and the `VERIFY_PKGS` construction, which adds the fallback-kernel package set only on a non-`--core-only` run).
    - `system/` — list the tree so §5's new prose names the two files it installs accurately.
    - `.planning/REQUIREMENTS.md` — the QBAR / QNOTIF / QOSD / QPOWER / QMEDIA blocks, for the requirement IDs §6 and §8 will name.
    - `.planning/phases/22-fresh-install-proof/22-PATTERNS.md` § "VERIFICATION.md" — the four sections quoted verbatim at their real line numbers.
  </read_first>

  <reversibility rating="reversible">
    Carried forward verbatim from D-22-11: a flag choice in a documented procedure.
    Reverting is a one-line edit to §5.
  </reversibility>

  <files>VERIFICATION.md</files>

  <action>
    §5 — the heading and the install invocation both currently carry the
    `--core-only` flag. Change the VM tier to run `install.sh` unflagged, and
    retitle the section accordingly.

    Write the justification into §5's prose, concretely, because the change is a
    decision reversal and must read as one:
      - `--core-only` skips exactly two sections. One is the hardware section: NVIDIA
        packages behind an `lspci` guard and limine bootloader steps behind a
        bootloader guard. The other is the personal section: git identity and
        timezone.
      - Every package install, including the shell and visualiser packages, lives in
        the core section and always runs; the post-install verification table
        hard-fails on that set regardless of flag. So the container tier's scope was
        already correct and stays `--core-only` — say this explicitly so a reader
        does not "helpfully" change the container harness to match.
      - The gap the unflagged run closes: the tracked `system/` tree —
        `system/usr/local/bin/kernel-module-verify` and
        `system/etc/pacman.d/hooks/99-kernel-module-verify.hook` — is deliberately
        not stowed; `install.sh` installs it with `sudo install` inside the hardware
        section, and its post-install verification is skipped under `--core-only`
        too. Neither tier has ever installed it. A VM is a real machine with a real
        bootloader, so an unflagged run there proves the hardware section's
        non-NVIDIA path and installs `system/` for the first time in any
        reproduction proof.
      - Record the reversal explicitly: the personal section writes a git identity
        and a timezone, and has been skipped since v1.0 under D-61 as "not
        meaningful, potentially wrong" in a disposable environment. Running it on
        the VM is harmless because the VM is deleted at §9, but it IS a reversal of
        D-61 for this tier and is being recorded as one, not slipped in. Name D-61
        and D-22-11 in the prose so a future reader can trace it.
      - Also note that an unflagged run adds the fallback-kernel package set to the
        post-install verification table, so the expected `All N packages verified
        installed.` count is higher on the VM tier than in the container.
    Leave §5's `stow.sh` half unchanged, including its note that this seeds the
    first-boot theme baseline.

    §6 (per D-22-03) — the "confirm these come up themed" sentence names a package retired under
    RETIRE-03 in a list alongside surviving clients. Rewrite the whole sentence to
    the current surface inventory, naming each surface by its requirement ID with a
    short plain-language gloss so a human can actually find it on screen. The
    inventory: the bar (QBAR), notification popups and the notification centre
    (QNOTIF), the dashboard drawer, OSD indicators (QOSD), the power menu (QPOWER),
    the workspace overview, the Media tab with its cava ring (QMEDIA), plus walker,
    elephant and Thunar which are separate processes and keep their own names.
    Keep §6's existing closing sentence that this is the moment the container tier
    cannot exercise at all.

    Note in §6 that the bar, the notification surfaces, the OSD, the power menu, the
    dashboard and the overview are all one Quickshell process — a human checking
    "did they all come up" is checking one process's surfaces, not six daemons. Do
    NOT add any instruction to enable a systemd unit: the Quickshell units
    deliberately carry no `[Install]` section so enabling can never write a
    wants-symlink outside the repository, and the autostart config starts them.

    §8 — the first bullet of the human visual confirmation names the same retired
    package. Rewrite the three bullets to the same requirement-ID inventory §6 now
    uses: every listed surface shows the same theme; switching themes live-updates
    every visible surface with no relogin; nothing is unstyled, blank or still
    showing stock GTK defaults. Keep the D-53 non-negotiable framing, the
    "look at the VM's own display, not a script's screenshot" instruction, and the
    closing requirement to record the verdict alongside the container-tier logs.

    Across all three sections: name surfaces by requirement ID, never by a retired
    package name. This is load-bearing rather than stylistic — this file sits in
    `retirement-check`'s report tier, and Phase 21 was bitten twice by retirement
    prose that named retired packages literally, once tripping `theme-doctor` and
    once tripping the checker-internals class.

    Re-run `retirement-check --all` after the edits.
  </action>

  <verify>
    <automated>hypr/.config/hypr/scripts/retirement-check --all > /tmp/rc-v.txt 2>&amp;1; echo "rc=$?"; grep -c '^\[FAIL\]' /tmp/rc-v.txt</automated>
    <automated>grep -n 'core-only' VERIFICATION.md</automated>
    <automated>grep -c 'D-61\|D-22-11' VERIFICATION.md</automated>
    <automated>for id in QBAR QNOTIF QOSD QPOWER QMEDIA; do printf '%s: ' "$id"; grep -c "$id" VERIFICATION.md; done</automated>
  </verify>

  <acceptance_criteria>
    - `retirement-check --all` exits 0 with zero `[FAIL]` lines after the edits.
    - Every remaining occurrence of the core-only flag in `VERIFICATION.md` is in a sentence describing the CONTAINER tier, not the VM tier's own invocation — verified by reading each hit from the `grep -n` output.
    - `VERIFICATION.md` names both `D-61` and `D-22-11`, so the reversal is traceable.
    - §5 names both files of the `system/` tree by path.
    - Each of `QBAR`, `QNOTIF`, `QOSD`, `QPOWER`, `QMEDIA` appears at least once in `VERIFICATION.md`.
    - §6 and §8 each list at least seven distinct surfaces from the current inventory.
    - The NOPASSWD scoping warning block near the top of the file is byte-unchanged.
    - No section instructs the reader to `systemctl enable` a Quickshell unit: `grep -c 'systemctl enable' VERIFICATION.md` returns 0.
    - `git diff --stat -- VERIFICATION.md` shows no changes in §1-§4 or §9.
  </acceptance_criteria>

  <done>
    The VM procedure installs the full `install.sh` scope with the reversal recorded,
    and its two human-facing surface lists describe the desktop that actually exists,
    named by requirement ID.
  </done>
</task>

<task type="auto">
  <name>Task 3: Amend VERIFICATION.md §7's pass bar and author the pre-run exemption list</name>

  <read_first>
    - `VERIFICATION.md` §7 lines 187-207 as edited by Task 2, plus the "Pass condition (unambiguous, D-53)" blockquote near the top of the file — both state the bar and must end up saying the same thing.
    - `hypr/.config/hypr/scripts/hypr-equivalence-check` lines 60-115 — the baseline-directory resolution: it prefers a live `.planning/phases/13.1-.../.hypr-baseline` and falls back to the archived `.planning/milestones/v3.0-phases/13.1-hyprland-lua-config-migration/.hypr-baseline`. Confirm which path resolves today and what the baseline captures (binds, animations, options — no monitor or hardware data).
    - `theme-engine/.config/theme-engine/theme-doctor` — enough of it to identify which checks depend on the host machine rather than on the repository. You are looking for checks that could legitimately differ on a genuinely fresh VM even when everything is healthy.
    - `.planning/phases/22-fresh-install-proof/22-BASELINE.md` — the container-tier failure inventory. The VM exemption list is NOT the same as the container allowlist (a VM has a live session, so the session-dependent container failures do not apply), but the inventory tells you which checks are environment-sensitive at all.
  </read_first>

  <files>VERIFICATION.md</files>

  <action>
    §7 currently says both commands must exit 0 and that the `theme-doctor` summary
    line must read `Summary: N passed, 0 failed`. Amend that literal sentence so the
    bar reads: `theme-doctor` exits 0 and reports zero failures EXCEPT for entries on
    the pre-authored exemption list below, AND `theme-parity` reports zero failures
    across all render targets, AND the human visually confirms the themed desktop.
    Update the "Pass condition (unambiguous, D-53)" blockquote near the top of the
    file to match, so the two statements of the bar cannot drift apart.

    Add a new subsection immediately after §7's amended pass-bar sentence, titled to
    make its timing explicit — that it is authored before any VM run. Its opening
    paragraph must state the rule plainly: authoring this list in advance is what
    prevents a red line being rationalised away after it appears, and anything
    outside the written set is a real defect. Note the discipline it copies — the
    retirement registry, the motion lint's exemption list and the equivalence
    check's accepted-additions table all work the same way: the rule is data, not a
    memory.

    The list is a table with four columns: the check as `theme-doctor` names it; the
    reason it may legitimately differ on a genuinely fresh machine; the source that
    reason was read from (a file and line, or a named tool behaviour — not "it
    seems"); and whether the entry is expected to be permanent or is provisional.

    Populate it with the one candidate CONTEXT.md names, verified rather than
    assumed: Hyprland version drift between the fresh VM's Hyprland and the version
    the equivalence-check baseline was captured against. Establish the concrete
    facts before writing the row — which baseline directory resolves, what the
    baseline captures, what the current version is on this host, and what the
    equivalence check actually does when it sees an option the baseline does not
    contain. If the check has an accepted-additions mechanism that already absorbs
    version drift, say so and mark the row provisional-or-unnecessary rather than
    claiming an exemption the tool does not need.

    Add any additional row only if you can name its source-level reason. A check
    with no such reason is a defect, not an exemption — the same rule D-22-09
    applies to the container allowlist. If the honest answer is that only one
    candidate is justifiable, the list has one row; a short defensible list is the
    goal, not a comprehensive one.

    State in the subsection that this list governs the VM tier ONLY, and that the
    container tier has its own separate, machine-read allowlist committed next to
    the container harness — so nobody later merges the two or applies one tier's
    exemptions to the other.

    Re-run `retirement-check --all` after the edit.
  </action>

  <verify>
    <automated>hypr/.config/hypr/scripts/retirement-check --all >/dev/null 2>&amp;1; echo "retirement-check rc=$?"</automated>
    <automated>grep -n 'Summary: N passed' VERIFICATION.md</automated>
    <automated>grep -c 'exemption' VERIFICATION.md</automated>
    <automated>awk '/exemption list/,/^## /' VERIFICATION.md | grep -c '^|'</automated>
  </verify>

  <acceptance_criteria>
    - `retirement-check --all` exits 0 after the edit.
    - `VERIFICATION.md` contains a table under the new exemption-list subsection with at least three body rows' worth of structure (header row, separator row, and at least one data row) — the `grep -c '^|'` count within the section is at least 3.
    - Every data row has all four columns populated; no cell is empty, `TBD`, or `n/a` without an accompanying explanation.
    - Every data row's source column names a concrete file path, a file:line, or a named tool behaviour — no row is justified by inference alone.
    - The §7 pass-bar sentence and the top-of-file `Pass condition (unambiguous, D-53)` blockquote both state the same three-part bar including the exemption-list qualifier — verified by reading both.
    - The subsection states in prose that it is authored before any VM run and that anything outside the written set is a real defect.
    - The subsection states that it governs the VM tier only and points at the separate container-tier allowlist.
    - `theme-parity`'s half of the bar is unqualified — no exemption row applies to `theme-parity`, whose zero-failure requirement stands unchanged.
  </acceptance_criteria>

  <done>
    §7's bar is `theme-doctor` clean minus a written, sourced, pre-authored exemption
    list; `theme-parity` zero failures; human visual confirmation — and the list
    exists on disk before any VM has been built.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented procedure → operator's real machine | `VERIFICATION.md` instructs a human to create a NOPASSWD sudoers drop-in and run an installer with `sudo`. Prose errors here become privilege errors. |
| repository prose → `retirement-check`'s scanners | `README.md`, `env.lua` and `VERIFICATION.md` are all scan targets; edited prose feeds back into the gate. |
| exemption list → what a human is allowed to accept | The §7 list defines the only permitted deviations from the INST-03 pass bar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-03-EOP | Elevation of Privilege | §3/§5 NOPASSWD instructions + unflagged `install.sh` now running `sudo install` into `/usr/local/bin` and `/etc/pacman.d/hooks/` | high | mitigate | The NOPASSWD scoping warning block is required by acceptance criteria to remain byte-unchanged, and §9's teardown is unedited. The two paths the hardware section installs are repository-tracked files under `system/`, reviewable in-repo, and §5's new prose names both by path so the operator knows exactly what lands outside the home directory. |
| T-22-03-INFO | Information Disclosure | `section_personal` writing the maintainer's git identity and timezone onto the VM (the D-61 reversal) | low | accept | The VM is destroyed at §9, and the same identity is already public in every commit in this repository's history. Accepted explicitly, and the reversal is recorded in §5 rather than left implicit. |
| T-22-03-TAMP | Tampering | the §7 exemption list | high | mitigate | Every row requires a source-level reason naming a file, line or tool behaviour; the list is authored before any run; the subsection states in prose that anything outside it is a real defect. The prohibition against extending the list in response to a red line is recorded in this plan's `must_haves.prohibitions`. |
| T-22-03-REPUD | Repudiation | a silent decision reversal | medium | mitigate | §5 must name both D-61 and D-22-11, so the reversal is attributable rather than discovered later as an unexplained flag change. |
| T-22-03-SC | Tampering (supply chain) | package-manager installs | n/a | accept | This plan adds no package-manager install task; `install.sh`'s package sets are untouched, so the package-legitimacy audit precondition does not fire. |
</threat_model>

<verification>
- `retirement-check --all` exits 0 with zero `[FAIL]` lines after every edit in this plan.
- The report-tier `repo-prose` counts for the RETIRE-02 and RETIRE-03 surfaces are strictly lower than before.
- The three named retirement-lineage comments are byte-unchanged.
- `VERIFICATION.md` states the same three-part pass bar in both places it states it.
- The §7 exemption list exists, is fully populated, and every row cites a source.
</verification>

<success_criteria>
- SC-3's human review pass is discharged: the identified factual errors are corrected and the report-tier counts moved in the right direction.
- The VM tier has a procedure that matches the desktop it is verifying and an install scope that covers the `system/` tree for the first time.
- The VM tier's pass bar is fixed in writing before any VM exists to argue with it.
</success_criteria>

<output>
Create `.planning/phases/22-fresh-install-proof/22-03-SUMMARY.md` when done.
</output>
