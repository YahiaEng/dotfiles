---
phase: 21-media-fold-in-contract-close
plan: 08
type: execute
wave: 4
depends_on: ["21-02", "21-04", "21-05", "21-06", "21-07"]
files_modified:
  - .planning/phases/21-media-fold-in-contract-close/21-GATE-02-RECORD.md
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/reload.sh
  - theme-engine/.config/theme-engine/theme-parity
  - matugen/.config/matugen/config.toml
  - matugen/.config/matugen/templates/ags-colors.scss
  - hypr/.config/hypr/config/windowrules.lua
  - hypr/.config/hypr/config/autostart.lua
  - hypr/.config/hypr/scripts/retirement-check
  - hypr/.config/hypr/scripts/media-status.sh
  - hypr/.config/hypr/scripts/media-players.sh
  - hypr/.config/hypr/scripts/media-player.py
  - hypr/.config/hypr/scripts/tests/test-media-hardening.sh
  - quickshell/.config/quickshell/modules/dashboard/MediaTab.qml
  - install.sh
  - stow.sh
  - ags
autonomous: false
requirements: [QMEDIA-01, QMEDIA-02, QMEDIA-03, RETIRE-06, RETIRE-08]

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ONE combined gate unlocks the deletion — the parity checklist and the rendered look are judged in a single sitting with one verdict, not as two independent approval rounds (D-21-20)"
    - "Nothing is deleted before the gate passes, and the gate is recorded with an explicit deletion authorisation (D-21-20, GATE-02)"
    - "The behaviour baseline is committed at a commit strictly earlier than the commit that deletes the retiring tree, proven by git log ordering (RETIRE-06 / ordering edge, D-21-08)"
    - "The pre-deletion checklist run emits a per-class report in which every reference class prints an explicit zero, skip or hit line — so a class with zero hits is distinguishable in the committed output from a class that was never evaluated (RETIRE-06 / empty edge)"
    - "The contract entry removal, the template removal, the reload-step removal, the layer-rule removals, the registry flip, the install/stow list edits, the retiring tree deletion and the three orphaned script deletions all land in exactly ONE commit — config-then-package, matching the four prior retirements in this milestone (RETIRE-06 / concurrency edge, D-21-13, WINDOWS #1 precedent)"
    - "Comments naming the dead surface are REWRITTEN so the finding survives and the name goes — never scrubbed, and never exempted from the leftover sweep (D-21-19)"
    - "The album-art resolver STAYS — it is the media backend's only subprocess and carries the scheme allowlist and the loopback/private-range pre-flight rejection that mitigate request forgery against this machine (D-21-14)"
    - "The sass compiler STAYS — the install script's comment attributing it solely to the retiring package is wrong, and correcting that comment is part of this phase; the stow script's GTK3 seed block depends on the same binary with zero relation to the retiring package (D-21-18)"
    - "The audio analyser STAYS, with its install comment rewritten to describe what now consumes it (D-21-18)"
    - "The theme contract reaches 17 file entries, down from 18 — the second entry of the same format family remains, so no placeholder or repoint is needed (RETIRE-08, D-21-17)"
    - "The stress test's representative-file list is verified NOT to name the removed entry before the removal — checked, not assumed, following the precedent where removing a named representative broke that test (D-21-17)"
    - "The retirement registry row for this surface flips from pending to retired in the same commit as the deletion (D-21-21)"
    - "The hardening test is trimmed to its surviving coverage: the checks exercising deleted scripts are removed and the checks exercising the retained resolver are kept intact (D-21-15)"
    - "The host no longer carries the retired package, verified by name — the registry's own host-package class queries the surface name rather than the package name, so a name-only check would pass vacuously"
  artifacts:
    - path: ".planning/phases/21-media-fold-in-contract-close/21-GATE-02-RECORD.md"
      provides: "The combined gate's record and its explicit deletion authorisation"
      contains: "Deletion Authorisation"
    - path: "theme-engine/.config/theme-engine/contract.json"
      provides: "The theme contract at its post-migration size"
  key_links:
    - from: ".planning/phases/21-media-fold-in-contract-close/21-GATE-02-RECORD.md"
      to: ".planning/phases/21-media-fold-in-contract-close/21-BEHAVIOUR-BASELINE.md"
      via: "the gate's checklist half is the baseline's parity checklist, judged row by row"
      pattern: "BEHAVIOUR-BASELINE|Parity"
  prohibitions:
    - "The leftover sweep must not be satisfied by deleting explanatory findings or by adding an exemption — scrubbing destroys the only written record of why several still-live surfaces carry the values they do, and exempting installs a permanent blind spot in the tool whose entire job is catching what deletions leave behind"
    - "Trimming the hardening test must not drop coverage that still applies to the retained album-art resolver — see the security review breadcrumb below"
---

<!-- planner-discipline-allow: ags-media -->
<!-- planner-discipline-allow: ags.scss -->
<!-- planner-discipline-allow: templates.ags -->
<!-- planner-discipline-allow: aylurs-gtk-shell -->
<!-- planner-discipline-allow: ags/ -->

<objective>
Judge the replacement against the retiring surface in one sitting, get the deletion
authorised, and then remove the surface from repo and host in a single commit.

This is the phase's one-way door. Four prior retirements in this milestone set the pattern
and this one closes it: the fifth and final theme-contract entry goes, and the next phase
proves the result on a genuinely fresh install — so it inherits whatever this plan gets
wrong.

Ordering is not negotiable. The behaviour enumeration was taken off the live surface in
wave 1; every gap it recorded was built in wave 3; the visualiser that supersedes the
surface's one unmatched capability landed in wave 2. Only now does the gate run, and only
after the gate passes does anything get deleted.

Purpose: retire the surface without losing a capability, a finding, or a dependency that
only looked like it belonged to it.
Output: a recorded gate verdict with an explicit authorisation, and one deletion commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-media-fold-in-contract-close/21-CONTEXT.md
@.planning/phases/21-media-fold-in-contract-close/21-RESEARCH.md
@.planning/phases/21-media-fold-in-contract-close/21-PATTERNS.md
@.planning/phases/21-media-fold-in-contract-close/21-BEHAVIOUR-BASELINE.md
@.planning/phases/21-media-fold-in-contract-close/21-06-SUMMARY.md
@.planning/phases/21-media-fold-in-contract-close/21-07-SUMMARY.md
</context>

## Artifacts this phase produces

New in this plan: `.planning/phases/21-media-fold-in-contract-close/21-GATE-02-RECORD.md`
and its `## Deletion Authorisation` section.

Consumed from earlier plans in this phase (new, not drift): `CavaService` and its API,
`Design.cavaLingerMs`, `cava/.config/cava/config`, the 60-bar ring and 12-lobe mask on both
media surfaces, `MediaBackend.setVolumeForPlayer()`, the `quickshell:media` shortcut and
`Super+M`, `BarRoles.dndSurface`/`dndSurfaceFg`, `21-BEHAVIOUR-BASELINE.md` and its parity
checklist, and the repaired MPRIS-reader check.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: THE COMBINED GATE — parity checklist and rendered look, one sitting, one verdict</name>
  <what-built>
    The dashboard Media tab and the bar media popout now carry everything the standalone
    media card carried, plus a live 60-bar audio ring around hand-authored 12-lobe cover
    art on both surfaces, a volume control on every player's own row in the switcher,
    duplicate-source collapsing, and a one-letter shortcut that opens the dashboard
    directly on the Media tab.
  </what-built>
  <how-to-verify>
    **This is ONE gate with ONE verdict, deliberately.** The prior phase ran two independent
    gates because its two surfaces shared no backend; here there is one surface, so two
    approval rounds for one thing is pure overhead. Judge the checklist and the look
    together and give a single answer.

    Nothing is deleted before this passes.

    **Part A — the parity checklist.**
    1. Open `.planning/phases/21-media-fold-in-contract-close/21-BEHAVIOUR-BASELINE.md` and
       go to `## Parity Checklist`.
    2. The card is still running and still summonable
       (`ags request -i media toggle-media`). Run the two side by side.
    3. Walk the checklist row by row. For each row, perform the gesture on the card, then
       perform it on the Media tab or the bar popout, and judge whether the replacement is
       at least as good. Rows in `## Dead Definitions` are excluded by construction — they
       are not parity obligations.
    4. The row for the card's audio underlay is marked satisfied-by-supersession. Judge the
       superseding treatment (the live ring), not a reproduction of the flat underlay.
    5. The per-player volume row is an addition beyond parity, not a parity obligation.
       Judge it, but a complaint there is a polish note, not a parity failure.

    **Part B — the rendered look.**
    6. With audio playing, look at the ring on the Media tab: 60 bars, each moving with its
       own frequency band. A bass hit and a cymbal should NOT look identical.
    7. Stop the audio. The ring must settle to an even ring of slivers that reads like the
       static dashed ring you approved at round 3. If it does not, say so — that
       equivalence is the entire argument the live-ring design was accepted on.
    8. Look at the cover art: a 12-lobe scalloped blob, with the bars orbiting the lobes.
    9. **Non-square cover art — an explicit check, not an incidental one.** Play tracks
       whose album art is deliberately non-square: one roughly 16:9 and one roughly 3:4.
       The art must FILL the lobed mask without letterboxing and without being stretched
       horizontally or vertically. The mask clips; the image must not distort. This
       re-opens a clipping test the previous circular mask satisfied.
    10. Open the bar media popout with audio playing. The same ring must be live there.
        With both the popout and the Media tab open simultaneously, run
        `pgrep -fc "cava -p .*/.config/cava/config"` — it must report exactly 1, not 2.
    11. **Dropdown with many players — explicit check.** Get six or more players running
        (browser tabs count) and open the switcher dropdown. It must clip and scroll rather
        than growing past the panel. Say which if it overflows.
    12. **Panel at its tallest — explicit check.** With the dropdown expanded and long track
        metadata, confirm the pinned controls are not pushed off-surface.
    13. Press the new shortcut from a normal desktop: the dashboard opens on the Media tab.

    **Part C — the verdict.**
    14. Answer once: does the replacement do everything the card did, and does it look at
        least as good? If yes, this authorises the irreversible deletion. If no, name the
        rows and the visual issues; nothing is deleted and the phase iterates.

    Record the outcome in
    `.planning/phases/21-media-fold-in-contract-close/21-GATE-02-RECORD.md` with a
    per-row result table, the render observations, and — only on a pass — a
    `## Deletion Authorisation` section naming the commit SHA judged, so a later reader can
    confirm nothing changed between the judgment and the deletion.
  </how-to-verify>
  <resume-signal>Type "approved" to authorise the deletion, or list the failing checklist rows and visual issues</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: One-way door — authorise the package deletion and the contract reduction</name>
  <decision>
    Proceed with deleting the retiring package from repo and host — its whole tree, its
    theme-contract entry, its matugen template, its reload step, its two layer rules, its
    autostart entry, its install and stow list entries, and three orphaned shell/python
    scripts — and reduce the theme contract from 18 file entries to 17.
  </decision>
  <context>
    **What undo costs.** This is not a refactor. The tree is 156 files. Recovering it means
    a git revert plus reinstalling an AUR package, and every downstream artifact — the
    contract's post-migration size, the registry row, the next phase's fresh-install proof —
    is written against the deleted state. The next phase rebuilds the whole desktop from a
    clean clone and inherits whatever this commit gets wrong.

    **What has already been established, so this is not re-asking a settled question.** The
    behaviour enumeration was taken off the live surface. Every gap it found was built. The
    combined gate in Task 1 has passed with an explicit authorisation. Three of this
    phase's decisions specifically exist to make this door safe to walk through, and all
    three are already satisfied.

    **What must NOT be deleted alongside it**, each verified independently rather than
    assumed:
    - The album-art resolver. It is the media backend's only subprocess — the backend
      builds its path by string concatenation, so a naive filename grep misses the
      reference — and it carries the scheme allowlist and the loopback/private-range
      pre-flight rejection that mitigate request forgery against this machine.
    - The sass compiler. The install script's comment attributes it entirely to the
      retiring package. That comment is wrong: the stow script's GTK3 seed block shells out
      to the same binary to compile stylesheets at install time, with zero relation to the
      retiring package. Removing it would leave GTK3 surfaces unstyled on a fresh install —
      a failure the next phase would catch expensively.
    - The audio analyser. It now feeds the QML visualiser this phase built.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the deletion as one commit</name>
      <pros>Closes the fifth and final retirement of the milestone; the theme contract reaches its post-migration size; the next phase gets a clean tree to prove against; matches the config-then-package pattern applied four times already this milestone.</pros>
      <cons>Irreversible without a revert plus a package reinstall; any capability missed by the enumeration is gone from the running system.</cons>
    </option>
    <option id="hold">
      <name>Hold the deletion and iterate on the gate's findings first</name>
      <pros>Nothing is lost; the surface stays available for re-enumeration.</pros>
      <cons>The theme contract does not close this phase, and the next phase's fresh-install proof runs against a tree that still contains the surface it is meant to prove gone.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, or hold</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The deletion — config then package, one commit</name>
  <reversibility rating="one-way">Deleting a 156-file package tree, three orphaned scripts and a theme-contract entry; undo requires a git revert plus an AUR package reinstall, and every downstream artifact including the next phase's fresh-install proof is written against the deleted state.</reversibility>
  <precondition>`21-GATE-02-RECORD.md` exists and carries a `## Deletion Authorisation` section naming a judged commit SHA, and `git diff --quiet <that-sha> -- quickshell/.config/quickshell/` still holds — nothing in the replacement surface changed between the judgment and this deletion. If the interlock fails, halt: the gate judged a different tree than the one about to be shipped.</precondition>
  <files>theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/reload.sh, theme-engine/.config/theme-engine/theme-parity, matugen/.config/matugen/config.toml, matugen/.config/matugen/templates/ags-colors.scss, hypr/.config/hypr/config/windowrules.lua, hypr/.config/hypr/config/autostart.lua, hypr/.config/hypr/scripts/retirement-check, hypr/.config/hypr/scripts/media-status.sh, hypr/.config/hypr/scripts/media-players.sh, hypr/.config/hypr/scripts/media-player.py, hypr/.config/hypr/scripts/tests/test-media-hardening.sh, quickshell/.config/quickshell/modules/dashboard/MediaTab.qml, install.sh, stow.sh, ags/</files>
  <read_first>
    - `hypr/.config/hypr/scripts/retirement-check` — the registry block around line 92 (the pipe-delimited row for this surface), the sixteen reference classes and their order, the host-package class around line 517 which queries the SURFACE name via the package manager, and the note near lines 655-670 recording the known precision limit of the short-token sweep for this particular surface name against longer words containing it. Also read the CLI dispatch at the file tail for the single-surface and all-surfaces invocations.
    - `theme-engine/.config/theme-engine/contract.json` — all 18 file entries. The entry to remove is the one named `ags.scss` with format `scss-vars`; note that `_motion.scss` is a second entry of the same format family, so the family stays represented and nothing needs repointing.
    - `theme-engine/.config/theme-engine/theme-stress-test` around line 315 — the representative-files list. Verify by reading that the entry being removed is NOT a member before removing it. A prior retirement broke this test by removing a file that was the named representative of its format; check, do not assume.
    - `matugen/.config/matugen/config.toml` lines 94-103 — the template block and its ten-line header comment, one contiguous span.
    - `matugen/.config/matugen/templates/ags-colors.scss` — the template file itself.
    - `theme-engine/.config/theme-engine/lib/reload.sh` lines 110-132 — the guarded reload fan-out step for this surface and the comment block above it. Note the remaining steps' order, which must not change.
    - `theme-engine/.config/theme-engine/theme-parity` around line 239 — the comment naming the removed contract entry as the example for its format family.
    - `hypr/.config/hypr/config/windowrules.lua` — the two real layer rules at lines 305 and 352, and the eight comment sites at lines 228, 278, 295, 301, 418, 461, 465 and 527. Read each comment's full surrounding block; several of them explain why still-live surfaces carry the alpha values they do.
    - `hypr/.config/hypr/config/autostart.lua` lines 160-168 — the autostart line and its preceding comment.
    - `install.sh` lines 236-246 and 345-356 — the audio-analyser and sass-compiler entries with the incorrect attribution comment, and the AUR package entry with its comment.
    - `stow.sh` line 20 and lines 415-465 — the package array entry to remove, and the GTK3 sass seed block that must stay untouched.
    - `hypr/.config/hypr/scripts/tests/test-media-hardening.sh` — the whole file (329 lines). Note the exact boundaries: checks 1/2/3 around lines 149-182, checks 4-7 around lines 184-229, checks 8/9/9b/10 around lines 231-305, and check 11 around lines 307-325.
    - `quickshell/.config/quickshell/modules/dashboard/MediaTab.qml` — the comment-only references at lines 67, 117-118, 276, 280, 428 and 433.
    - `hypr/.config/hypr/scripts/media-art-resolve.sh` — read enough to confirm it is the RETAINED one and is not in the deletion set.
  </read_first>
  <action>
Run the pre-deletion sweep, make every edit, and land it all as ONE commit — config first,
then package — matching the four prior retirements in this milestone.

**Step 1 — pre-deletion sweep, committed as evidence.** Run the retirement checklist for
this surface and capture its full raw output to a file under the phase directory before
touching anything. The report must show a line for every one of the sixteen reference
classes, so a class with zero hits is visibly distinguishable from a class that was never
evaluated — a bare "no hits" summary is not acceptable evidence. Note the known precision
limit the tool itself records for this surface's short name against longer words containing
it, and account for any resulting false positives explicitly in the summary rather than
letting them read as real hits.

**Step 2 — config edits.**

*Theme contract.* First verify by reading that the entry being removed is NOT a member of
the stress test's representative-files list. Then remove the one `ags.scss` / `scss-vars`
entry. The file goes from 18 file entries to 17. No placeholder and no repoint is needed
because a second entry of the same format family remains.

*Matugen.* Delete the template block and its ten-line header comment as one contiguous
span, and delete the template file it points at.

*Reload fan-out.* Delete the guarded step that probes for the binary and issues a reload
request to the retiring surface, together with the comment block above it. Nothing replaces
it — the QML surfaces hot-reload natively. Leave the ordering of the remaining fan-out
steps untouched.

*Parity checker comment.* The comment naming the removed contract entry as the example for
its format family must be rewritten to name a surviving entry of that same family instead.

*Layer rules.* Delete both real layer rules for the dead namespace — the blur rule and the
ignore-alpha rule.

*Autostart.* Delete the autostart line and its preceding comment.

*Install script.* Remove the AUR package entry and its comment. Rewrite the audio
analyser's comment: it now feeds the QML visualiser this phase built, not the retired
applet. **Correct** the sass compiler's comment: it currently attributes the binary
entirely to the retiring package, which is wrong — the stow script's GTK3 seed block shells
out to the same binary to compile stylesheets at install time, with zero relation to the
retiring package. State the real reason. Both packages STAY.

*Stow script.* Remove the one package-array entry. The GTK3 sass seed block is confirmed
independent and stays byte-unchanged.

*Registry.* Flip this surface's registry row from pending to retired.

**Step 3 — comment rewrites, the part that stalled a prior phase's final plan.** Eight
comment sites in the window-rules file name the dead namespace, plus one in the parity
checker and several in the Media tab. These are NOT scrubbed and they are NOT exempted from
the sweep. Rewrite each so the FINDING survives and the NAME goes. Concretely, restate each
finding on its own terms:
- the GTK4 layer-shell opaque-background-by-default finding, stated directly rather than by
  pointing at a rule two lines below that no longer exists;
- the finding that translucency without blur reads as raw unblurred transparency;
- the finding that blur strength is global rather than a per-surface boolean;
- the finding that a fill alpha at or below the threshold silently discards blur, and that
  the symptom reads as a design problem rather than a blur bug;
- the finding that lowering the threshold unlocks the rest of the alpha range;
- the consistency-across-surfaces finding.

Where provenance genuinely matters, cite the recording plan ID rather than the namespace
string — a plan ID is a planning artifact, not a surface, so it does not trip the leftover
sweep and does not imply the surface still exists.

Scrubbing is rejected because it deletes the only written record of why several STILL-LIVE
surfaces carry the alpha values they do, and that record has already prevented one
misdiagnosis. Exempting the sweep is rejected because it installs a permanent comment-only
blind spot in the tool whose entire job is catching what deletions leave behind, immediately
before the phase that rebuilds this machine from scratch.

**Step 4 — hardening test trim.** Trim the test to its surviving coverage. Remove the
checks that exercise deleted scripts: the three hostile title/artist checks through the
status script, the four identifier/verb/argument allowlist checks through the players
script, and the zero-player gate check. KEEP checks 8, 9, 9b and 10 intact and unmodified —
they exercise the RETAINED album-art resolver's scheme gate, its loopback rejection, its
literal-encoding request-forgery bypass rejection, and its cache separation for two distinct
art URLs. That coverage is live network-forgery protection for a script that still runs on
every non-local album-art fetch; deleting the whole file was considered and rejected for
exactly that reason. The trim must not renumber or restructure the retained checks in a way
that obscures which upstream coverage survived — note the mapping in the summary.

**Step 5 — package deletion.** Delete the whole retiring tree, and delete the three
orphaned scripts in the SAME commit: the status script (consumed only by the retiring
tree's own reader), the players script (same), and the python player script (zero consumers
already — a pre-existing orphan swept up here rather than left behind). A prior retirement's
leftovers needed their own cleanup task because they were left on disk; that lesson is
already paid for.

**Do NOT delete the album-art resolver.** It is the media backend's only subprocess and the
backend builds its path by string concatenation, so a naive filename grep misses the
reference.

**Step 6 — host.** Remove the package from this host by its real package name. Note that
the registry's host-package class queries the SURFACE name via the package manager, and the
surface name and the package name differ here — so that class passes vacuously and is NOT
evidence the host is clean. Verify explicitly by the real package name instead.

**Step 7 — one commit.** Everything above goes in a single commit, config-then-package.
There must be no intermediate commit in which the contract is at 17 entries while the tree
still exists, or vice versa.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; B=.planning/phases/21-media-fold-in-contract-close/21-BEHAVIOUR-BASELINE.md; BC=$(git log --format=%ct -1 -- "$B"); DC=$(git log --format=%ct -1 -- ags 2>/dev/null || echo 0); echo "baseline_ts=$BC deletion_ts=$DC"; [ -n "$BC" ] &amp;&amp; [ "$BC" -lt "$DC" ]'</automated>
    <automated>bash -c 'set -uo pipefail; N=$(node -e "const j=require(process.cwd()+\"/theme-engine/.config/theme-engine/contract.json\");process.stdout.write(String(j.files.length))"); echo "contract file entries: $N"; [ "$N" -eq 17 ]'</automated>
    <automated>bash -c 'set -uo pipefail; N=$(node -e "const j=require(process.cwd()+\"/theme-engine/.config/theme-engine/contract.json\");process.stdout.write(String(j.files.filter(f=>f.format===\"scss-vars\").length))"); echo "scss-vars entries remaining: $N"; [ "$N" -ge 1 ]'</automated>
    <automated>bash -c 'set -uo pipefail; test -d ags &amp;&amp; { echo "TREE STILL PRESENT"; exit 1; }; for f in hypr/.config/hypr/scripts/media-status.sh hypr/.config/hypr/scripts/media-players.sh hypr/.config/hypr/scripts/media-player.py; do test -e "$f" &amp;&amp; { echo "ORPHAN STILL PRESENT: $f"; exit 1; }; done; test -x hypr/.config/hypr/scripts/media-art-resolve.sh || { echo "RESOLVER WAS DELETED — it must stay"; exit 1; }; echo DELETION_SET_CORRECT'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(grep -rn "ags-media" --include="*.lua" --include="*.qml" --include="*.sh" --include="*.json" --include="*.toml" . 2>/dev/null || true); printf "%s\n" "$OUT"; N=$(printf "%s\n" "$OUT" | grep -c . || true); echo "dead-namespace hits outside planning: $N"; [ "$N" -eq 0 ]'</automated>
    <automated>bash -c 'set -uo pipefail; grep -q "dart-sass" install.sh &amp;&amp; grep -qE "^\s*(\"|'"'"')?cava" install.sh &amp;&amp; ! grep -q "aylurs-gtk-shell" install.sh &amp;&amp; echo INSTALL_DISPOSITIONS_CORRECT'</automated>
    <automated>bash -c 'set -uo pipefail; C=$(sed -n "415,465p" stow.sh | grep -c "sass"); echo "sass references in GTK3 seed block: $C"; [ "$C" -ge 1 ] &amp;&amp; echo GTK3_SEED_BLOCK_INTACT'</automated>
    <automated>bash -c 'set -uo pipefail; grep -qE "^ags\|retired\|" hypr/.config/hypr/scripts/retirement-check &amp;&amp; echo REGISTRY_FLIPPED'</automated>
    <automated>bash -c 'set -uo pipefail; F=hypr/.config/hypr/scripts/tests/test-media-hardening.sh; K=$(grep -c "media-art-resolve.sh" "$F"); D=$(grep -cE "media-status\.sh|media-players\.sh" "$F" || true); echo "retained-resolver checks: $K, deleted-script checks: $D"; [ "$K" -ge 6 ] &amp;&amp; [ "$D" -eq 0 ]'</automated>
    <automated>bash -c 'set -uo pipefail; if pacman -Q aylurs-gtk-shell &gt;/dev/null 2&gt;&amp;1; then echo "HOST STILL HAS THE PACKAGE"; exit 1; fi; echo HOST_CLEAN'</automated>
    <automated>bash -c 'set -uo pipefail; F=.planning/phases/21-media-fold-in-contract-close/21-PRE-DELETION-SWEEP.txt; test -f "$F" &amp;&amp; L=$(grep -cE "^\s*(\[|class|CLASS|[0-9]+\.)" "$F"); echo "per-class report lines: $L"; [ "$L" -ge 16 ]'</automated>
  </verify>
  <acceptance_criteria>
    - The behaviour baseline's last commit timestamp is strictly earlier than the deletion commit's timestamp — the enumeration was never taken from a deleted tree.
    - The pre-deletion sweep output is committed under the phase directory and carries at least 16 per-class lines, so a zero-hit class is distinguishable from an unevaluated one.
    - The theme contract has exactly 17 file entries and still has at least one entry of the removed entry's format family.
    - The stress test's representative-files list was verified before the removal and does not name the removed entry — recorded in the summary as a checked fact, not an assumption.
    - The retiring tree and all three orphaned scripts are gone; the album-art resolver is still present and still executable.
    - A repo-wide grep for the dead namespace across lua/qml/sh/json/toml returns 0 hits outside the planning directory.
    - Each of the eight rewritten comment sites still carries its explanatory finding — the surrounding comment block was not emptied. Asserted by checking that each of the six named findings still appears somewhere in the window-rules file.
    - `install.sh` still installs the sass compiler and the audio analyser, no longer installs the AUR package, and the sass compiler's comment no longer attributes it to the retired package.
    - `stow.sh`'s GTK3 seed block still shells out to the sass binary and is otherwise unchanged.
    - The registry row for this surface reads retired.
    - The hardening test retains at least 6 references to the album-art resolver and zero references to the two deleted shell scripts.
    - `pacman -Q` for the real package name fails — the host no longer carries it. The registry's own host-package class is NOT accepted as evidence here, since it queries the surface name rather than the package name and would pass vacuously.
    - `git show --stat HEAD` for the deletion commit lists the contract, the matugen template, the reload step, the layer rules, the registry, the install and stow lists, the tree and all three orphaned scripts — and `git log` shows no intermediate commit with the contract at 17 entries while the tree still exists.
  </acceptance_criteria>
  <done>The surface is gone from repo and host in one commit, every finding its comments carried survives under its own name, and nothing that merely looked like its dependency went with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retiring tree → retained surfaces | Deleting a package can silently remove a dependency another surface relies on; the boundary is every cross-package reference the deletion crosses. |
| album-art resolver → remote hosts | The RETAINED resolver fetches remote artwork; its scheme allowlist and loopback/private-range rejection are the only thing between a crafted art URL and this machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-21 | Tampering | hardening-test trim | high | mitigate | The trim removes checks for deleted scripts and MUST retain checks 8, 9, 9b and 10 unmodified — the retained resolver's scheme gate, loopback rejection, literal-encoding bypass rejection and cache separation. Asserted by an acceptance criterion requiring at least 6 surviving references to the resolver and zero to the deleted scripts. Deleting the whole file was considered and rejected. |
| T-21-22 | Denial of Service | sass compiler removal | high | mitigate | Removing the sass binary on the strength of a wrong install comment would leave GTK3 surfaces unstyled on a fresh install — caught expensively only by the next phase. Mitigated by an independent dependency trace (the stow script's seed block) and by acceptance criteria asserting both the package entry and the seed block survive. |
| T-21-23 | Information Disclosure | album-art resolver deletion | high | mitigate | Deleting the resolver would remove the scheme allowlist and the loopback/private-range pre-flight rejection, re-opening request forgery against this machine. The backend builds the resolver's path by string concatenation, so a naive filename grep misses the reference — mitigated by an explicit acceptance criterion asserting the file is still present and executable. |
| T-21-24 | Repudiation | leftover sweep | medium | mitigate | A sweep satisfied by scrubbing findings or by an exemption reports clean while leaving the repo worse. Mitigated by requiring rewrites (each of six named findings must still appear) and by forbidding an exemption entry. |
| T-21-25 | Spoofing | host-package verification | medium | mitigate | The registry's host-package class queries the surface name, which differs from the real package name here, so it passes vacuously. Mitigated by an explicit acceptance criterion checking the real package name directly. |
| T-21-SC | Tampering | package installs | low | accept | This plan installs nothing; it REMOVES one already-vetted AUR package and edits config. The two packages that stay were re-verified as official-repo packages this session. Registry-style legitimacy gating is not applicable to a pacman/AUR removal. |

**Canon-referral breadcrumbs (not minted as prohibitions):** request-forgery coverage for
the retained resolver and subprocess argv injection are canon security concerns owned by
`/gsd-secure-phase` and by T-21-21/T-21-23/T-21-01 above rather than by a bespoke
prohibition.
</threat_model>

<verification>
- The gate record exists with an explicit deletion authorisation naming a judged SHA, and the interlock against that SHA held at deletion time.
- The pre-deletion sweep is committed with a per-class report.
- Exactly one commit carries the whole change set, config-then-package.
- The theme contract is at 17 entries with its format family still represented.
- A repo-wide grep for the dead namespace is clean outside the planning directory, and every rewritten comment still carries its finding.
- The retained resolver, the sass compiler and the audio analyser all survive.
</verification>

<success_criteria>
RETIRE-06 is closed and RETIRE-08's contract reduction has landed: the surface is gone from
repo and host in one commit, judged against its replacement in one sitting, with every
finding preserved under its own name and every retained dependency intact.
</success_criteria>

<output>
Create `.planning/phases/21-media-fold-in-contract-close/21-08-SUMMARY.md` when done
</output>
