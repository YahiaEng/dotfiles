---
phase: 21-media-fold-in-contract-close
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md
  - .planning/milestones/v3.0-phases/16-workspace-overview/16-05-SUMMARY.md
  - .planning/milestones/v3.0-phases/16-workspace-overview/16-06-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements: [LEDGER-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A 16-VERIFICATION.md exists for the workspace-overview phase, reconstructed from that phase's own eight plan summaries, its UAT record and its measurement record — an honest historical record of what was true at that phase's close (LEDGER-06, D-21-23)"
    - "The reconstructed report records the frame-rate floor/target as UNMEASURED and the missing panel rim as an open gap, exactly as they stood at that phase's close, with no later phase's closure folded in (D-21-23)"
    - "The invalid status enum in the 16-05 summary's coverage block carries a valid value paired with its existing rationale, and the three coverage items in the 16-06 summary that declare human judgment each carry a non-empty rationale field (LEDGER-06 / unclassified edge, D-21-24)"
    - "No coverage-block validator is added — the malformed shapes are fixed in place (D-21-24)"
    - "The quick task with no recoverable directory is closed as already-done with on-disk evidence recorded, and its outstanding row in STATE.md no longer reads as missing (LEDGER-06, D-21-25)"
  artifacts:
    - path: ".planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md"
      provides: "The historically-reconstructed verification report for the workspace-overview phase"
      min_lines: 80
      contains: "UNMEASURED"
  key_links:
    - from: ".planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md"
      to: ".planning/milestones/v3.0-phases/16-workspace-overview/16-UAT.md"
      via: "the report's evidence column cites the UAT record and the eight plan summaries it was reconstructed from"
      pattern: "16-UAT|16-0[1-8]-SUMMARY"
  prohibitions:
    - "The reconstructed report must not state any outcome that was not true at that phase's close — a report about a phase that closed months ago, written against code four phases newer, is fiction dressed as evidence"
    - "A gap that a later phase has since closed must not be shown as closed in this report; the historical record is the deliverable, and a what-has-since-closed view belongs in a milestone audit instead"
---

<objective>
Close the three outstanding paperwork items from the workspace-overview phase:
its missing verification report, its two malformed coverage blocks, and a quick
task whose record no longer exists on disk.

This is independent debt. It shares no file with any other plan in this phase, gates
nothing, and can run fully in parallel with the media and retirement work.

Purpose: clear the last three rows of the v3.0 override-closeout ledger that this
milestone owns, before the fresh-install proof phase inherits them.
Output: a reconstructed `16-VERIFICATION.md`, two corrected coverage blocks, and a
cleared STATE.md row.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-media-fold-in-contract-close/21-CONTEXT.md
@.planning/phases/21-media-fold-in-contract-close/21-RESEARCH.md
</context>

## Artifacts this phase produces

New in this plan:
`.planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md`.

Other symbols created elsewhere in this phase, listed so a source-grounding pass does
not flag them: `CavaService` and its `claim()`/`release()`/`alwaysOn` API,
`Design.cavaLingerMs`, `BarRoles.dndSurface`/`dndSurfaceFg`,
`MediaBackend.setVolumeForPlayer()`, the `quickshell:media` shortcut, `Super+M`,
`cava/.config/cava/config`, `21-BEHAVIOUR-BASELINE.md`, `21-GATE-02-RECORD.md`, and the
repaired MPRIS-reader check in `quickshell-doctor`.

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct the workspace-overview phase's verification report as it stood at its close</name>
  <precondition>All three reconstruction sources exist under `.planning/milestones/v3.0-phases/16-workspace-overview/` — the eight plan summaries, the UAT record and the measurement record. If any is absent, halt rather than reconstructing from a partial source set.</precondition>
  <files>.planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md</files>
  <read_first>
    - `.planning/milestones/v3.0-phases/16-workspace-overview/16-01-SUMMARY.md` through `16-08-SUMMARY.md` — all eight. These are the primary source; the report is derived from what they recorded, not from today's code.
    - `.planning/milestones/v3.0-phases/16-workspace-overview/16-UAT.md` — the operator's own acceptance record (30/30 passed, 0 issues).
    - `.planning/milestones/v3.0-phases/16-workspace-overview/16-OVER04-MEASUREMENT.md` — the measurement record, including the CPU half that passed at 2.4x headroom and the frame-rate half that was left unmeasured.
    - A `NN-VERIFICATION.md` from any phase in the same milestone tree that was written contemporaneously — use it as the structural template. Locate one with `ls .planning/milestones/v3.0-phases/*/[0-9]*-VERIFICATION.md`.
    - `.planning/STATE.md` lines 640-660 — the audit rows recording this phase's open items.
    - `.planning/REQUIREMENTS.md` — the OVER-series requirement text this phase's criteria map to.
  </read_first>
  <action>
Write `.planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md` as a
faithful historical reconstruction of what was true when that phase closed.

Follow the structural template of a contemporaneously-written verification report from
the same milestone tree, so the reconstructed document sits alongside its siblings
without looking foreign.

Derive every finding from the three source artifacts only. Do NOT run any verifier
against today's code, and do NOT open the current implementation to check whether a
finding still holds — four phases have landed since, and a report written against
today's tree would be a report about a different system.

Record the gaps that phase actually left open, honestly and without softening:
- The frame-rate term of the overview performance requirement: the CPU half passed with
  2.4x headroom; the frame-rate floor and target were recorded UNMEASURED. Report both
  halves, and report the frame-rate half as UNMEASURED.
- The animated rim missing from the three panels built in the preceding phase — a real
  gap, diagnosed and deliberately not fixed at that time.

**Do not annotate either gap with what a later phase did about it.** The rim gap has
since been closed by a later phase's ledger item, and the frame-rate term has since been
measured. Neither closure belongs in this report. D-21-23 rejects the annotated variant
explicitly: it reads better but blurs the historical record, and a what-has-since-closed
view belongs in a milestone audit instead. State the report's as-of date and its
reconstruction sources prominently at the top so no reader mistakes it for a live report.

Add a `## Reconstruction Provenance` section naming every source file the report was
derived from, and stating plainly that the document was written retroactively in this
phase rather than at that phase's close.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; F=.planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md; test -f "$F" &amp;&amp; grep -qF "## Reconstruction Provenance" "$F" &amp;&amp; grep -q "UNMEASURED" "$F" &amp;&amp; L=$(wc -l &lt; "$F") &amp;&amp; echo "lines: $L" &amp;&amp; [ "$L" -ge 80 ]'</automated>
    <automated>bash -c 'set -uo pipefail; F=.planning/milestones/v3.0-phases/16-workspace-overview/16-VERIFICATION.md; N=$(grep -cE "16-0[1-8]-SUMMARY|16-UAT|16-OVER04-MEASUREMENT" "$F"); echo "source citations: $N"; [ "$N" -ge 3 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `16-VERIFICATION.md` exists, is at least 80 lines, and carries a `## Reconstruction Provenance` section.
    - The provenance section cites all three source classes: at least one of the eight plan summaries, the UAT record, and the measurement record — `grep -cE "16-0[1-8]-SUMMARY|16-UAT|16-OVER04-MEASUREMENT"` is at least 3.
    - The frame-rate term appears with the literal token `UNMEASURED`, and the CPU half is recorded as passing with its headroom figure.
    - The missing panel rim appears as an open gap.
    - The report contains no reference to any phase numbered higher than 16 having closed either gap — a later-phase closure appearing here is a defect, not extra helpfulness.
    - The document states its as-of date and that it was written retroactively, in its first 20 lines.
    - No file outside `.planning/milestones/v3.0-phases/16-workspace-overview/` is modified by this task.
  </acceptance_criteria>
  <done>An honest historical verification report for the workspace-overview phase exists, including the gaps that phase genuinely left open.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the two malformed coverage blocks in place</name>
  <files>.planning/milestones/v3.0-phases/16-workspace-overview/16-05-SUMMARY.md, .planning/milestones/v3.0-phases/16-workspace-overview/16-06-SUMMARY.md</files>
  <read_first>
    - `.planning/milestones/v3.0-phases/16-workspace-overview/16-05-SUMMARY.md` — read the whole `coverage:` block. Item D5's second verification entry (the manual-procedural one) carries an invalid status value; note that the same block already carries a human-judgment flag and a rationale explaining the gap honestly.
    - `.planning/milestones/v3.0-phases/16-workspace-overview/16-06-SUMMARY.md` — read the whole `coverage:` block. Item D1 is the correctly-shaped sibling to copy; items D2, D3 and D4 each declare human judgment but carry no rationale field at all. Each of those three already names, in its own manual-procedural reference field, the render-gate step it maps to — the rationale is derivable from context already present in the same block.
    - Any two other phase summaries in `.planning/milestones/v3.0-phases/` carrying a well-formed `coverage:` block — to confirm which status values the rest of the corpus actually uses before choosing a replacement value.
  </read_first>
  <action>
Correct both malformed blocks in place. Change nothing else in either file.

In the 16-05 summary: item D5's manual-procedural verification entry declares a status
value that is the sole outlier across the entire coverage corpus in both files. Replace
it with a value that is actually used elsewhere in the corpus, chosen to be honest about
what happened — the check genuinely was not run, and the entry already carries a
human-judgment flag and a rationale saying so. Do not delete the rationale, do not
soften it, and do not claim the check passed. Record the reasoning for the chosen value
in one inline comment or in the plan summary, so a future reader can see it was a
judgment call rather than a mechanical rename.

In the 16-06 summary: add a `rationale:` field to items D2, D3 and D4, matching item D1's
own shape and indentation in the same file. Derive each rationale from the render-gate
step that item's own manual-procedural reference field already names — do not invent a
justification, and do not reuse D1's text verbatim for all three.

Do NOT add a validator, a schema file or any write-time check. D-21-24 declines that
explicitly: it would be new tooling to maintain for a problem two files old that stopped
recurring on its own, since every later phase writes these blocks correctly.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; D=.planning/milestones/v3.0-phases/16-workspace-overview; N=$(grep -c "status: not_run" "$D/16-05-SUMMARY.md" || true); echo "outlier status values remaining: $N"; [ "$N" -eq 0 ]'</automated>
    <automated>bash -c 'set -uo pipefail; D=.planning/milestones/v3.0-phases/16-workspace-overview; HJ=$(grep -c "human_judgment: true" "$D/16-06-SUMMARY.md"); RA=$(grep -c "rationale:" "$D/16-06-SUMMARY.md"); echo "human_judgment:true=$HJ rationale=$RA"; [ "$RA" -ge "$HJ" ]'</automated>
    <automated>bash -c 'set -uo pipefail; D=.planning/milestones/v3.0-phases/16-workspace-overview; for F in 16-05-SUMMARY.md 16-06-SUMMARY.md; do node -e "const fs=require(\"fs\");const t=fs.readFileSync(process.argv[1],\"utf8\");if(!/coverage:/.test(t)){process.exit(1)}" "$D/$F" || exit 1; done; echo COVERAGE_BLOCKS_INTACT'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status: not_run" 16-05-SUMMARY.md` returns 0.
    - The replacement status value in 16-05 D5 is one that appears elsewhere in the same milestone tree's coverage blocks — confirmed by grepping the corpus for that value.
    - 16-05 D5 still carries its original `human_judgment` flag and its original rationale text, unmodified.
    - In 16-06, the count of `rationale:` keys is greater than or equal to the count of `human_judgment: true` keys.
    - Items D2, D3 and D4 in 16-06 each have a `rationale:` field whose text is distinct from the others and traceable to that item's own manual-procedural reference.
    - No new file is created anywhere for validation purposes: `git status --porcelain` shows only the two summary files as modified by this task.
    - Neither file's `coverage:` block was restructured, reordered or reformatted beyond the specific fields named above.
  </acceptance_criteria>
  <done>Both malformed coverage blocks parse and carry the fields their shape requires, with no validator added.</done>
</task>

<task type="auto">
  <name>Task 3: Close the unrecoverable quick task as already-done, with evidence</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` lines 640-660 — the audit table containing the quick-task row marked missing, plus the surrounding rows so the edit matches the table's existing shape.
    - `hypr/.config/hypr/config/keybinds.lua`, `hypr/.config/hypr/config/windowrules.lua`, `hypr/.config/hypr/config/autostart.lua` — the live Lua config tree that constitutes the on-disk evidence.
    - `matugen/.config/matugen/config.toml` — read its header comment, which records the pipeline that superseded the hyprlang template block.
  </read_first>
  <action>
Close the quick task `260728-51j` as already complete, and record why.

Its directory does not exist under `.planning/quick/`; only a row in STATE.md remembers
it, so its scope is unrecoverable beyond its title, which named a Hyprland Lua config
migration. Do not recreate the task record from that title and re-do the work — that
risks redoing finished work off a guess, which is exactly the alternative D-21-25
rejects.

Gather and record the on-disk evidence that the underlying work is demonstrably
complete:
- The Lua config tree is live and in active use — name the three config files by path.
- The hyprlang template block was retired in favour of the token pipeline; cite the
  matugen config header that records this, and confirm by grep that no hyprlang template
  block remains in `matugen/config.toml`.

Update the row in `.planning/STATE.md` so its status is no longer `missing`. Replace it
with a resolved status plus a one-line evidence citation pointing at the files above.
Keep the row in the same table and the same column shape as its neighbours — do not
delete the row (the historical record of the override closeout matters) and do not
restructure the table.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; N=$(grep -c "260728-51j" .planning/STATE.md); M=$(grep "260728-51j" .planning/STATE.md | grep -ci "missing" || true); echo "row present: $N, still-missing: $M"; [ "$N" -ge 1 ] &amp;&amp; [ "$M" -eq 0 ]'</automated>
    <automated>bash -c 'set -uo pipefail; T=$(grep -c "\[templates\.hyprland\]" matugen/.config/matugen/config.toml || true); echo "hyprlang template blocks remaining: $T"; [ "$T" -eq 0 ]'</automated>
    <automated>bash -c 'set -uo pipefail; for F in keybinds.lua windowrules.lua autostart.lua; do test -f "hypr/.config/hypr/config/$F" || { echo "MISSING EVIDENCE FILE: $F"; exit 1; }; done; echo LUA_TREE_LIVE'</automated>
  </verify>
  <acceptance_criteria>
    - The quick-task row still exists in `.planning/STATE.md` (it is not deleted) and no longer carries a `missing` status.
    - The row or an adjacent line cites at least one concrete on-disk evidence path from the live Lua config tree.
    - `grep -c "\[templates\.hyprland\]" matugen/.config/matugen/config.toml` returns 0, confirming the superseded template block is genuinely gone.
    - All three named Lua config files exist on disk.
    - No directory is created under `.planning/quick/` for this task — the record is unrecoverable and is closed on evidence, not reconstructed.
    - The STATE.md table's column count and neighbouring rows are unchanged.
  </acceptance_criteria>
  <done>The unrecoverable quick task is closed on recorded on-disk evidence rather than re-done from a guess, and no longer reads as outstanding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical artifacts → reconstructed report | Prior-phase summaries are ingested as the sole evidence for a retroactive record; nothing executable crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Repudiation | `16-VERIFICATION.md` | medium | mitigate | A retroactive report can silently become a fabricated one. Mitigated by a mandatory `## Reconstruction Provenance` section citing all three source classes, a prominent as-of date, and an acceptance criterion forbidding any later-phase closure from appearing. |
| T-21-07 | Tampering | STATE.md audit table | low | mitigate | The outstanding row is updated in place rather than deleted, preserving the override-closeout record; acceptance criteria assert the row still exists and the table shape is unchanged. |
| T-21-SC | Tampering | package installs | low | accept | No package-manager install in this plan; documentation only. |
</threat_model>

<verification>
- The reconstructed report cites its sources and records both open gaps as they stood, with no later closure folded in.
- Neither corrected coverage block gained a validator, and both carry the fields their shape requires.
- The quick-task row is resolved with evidence and still present in the audit table.
</verification>

<success_criteria>
All three workspace-overview debt items are closed: the verification report exists as an
honest historical record, both malformed coverage blocks are corrected in place, and the
unrecoverable quick task is resolved on evidence.
</success_criteria>

<output>
Create `.planning/phases/21-media-fold-in-contract-close/21-03-SUMMARY.md` when done
</output>
