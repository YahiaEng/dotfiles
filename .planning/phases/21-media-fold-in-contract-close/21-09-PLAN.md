---
phase: 21-media-fold-in-contract-close
plan: 09
type: execute
wave: 5
depends_on: ["21-08"]
files_modified:
  - hypr/.config/hypr/scripts/tests/test-media-hardening.sh
  - .planning/phases/21-media-fold-in-contract-close/21-CONTRACT-CLOSE-EVIDENCE.md
autonomous: true
requirements: [RETIRE-08, QMEDIA-03, RETIRE-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The theme contract is at its post-migration size with the theme doctor and the parity checker both green and no orphaned entries (RETIRE-08)"
    - "The post-deletion checklist run reports zero hits for the retired surface across every reference class, and its raw output is committed alongside the pre-deletion run so the before/after pair is the evidence the roadmap's own success criterion asks for (RETIRE-06, D-21-16 reconciliation)"
    - "Exactly one MPRIS reader remains desktop-wide, proven both by the one-time sweep and by the standing check that now holds the line afterwards (QMEDIA-03, D-21-16)"
    - "The album-art handoff is covered by a test: the resolved art path must remain a bare local filesystem path, because the consuming surface prefixes the file scheme itself — a URL-valued path produces a doubled scheme and a silently blank art area with no error and no log. Nothing tested this before (D-21-15)"
    - "The stress test still passes after the contract entry removal — verified rather than assumed, following the precedent where removing a named representative file broke it (D-21-17)"
    - "The phase closes on a VERIFIER RUN, not on operator attestation — the prior phase closed on attestation with no verifier run and this phase deliberately does not repeat that, because the next phase inherits whatever this one gets wrong (D-21-22)"
  artifacts:
    - path: ".planning/phases/21-media-fold-in-contract-close/21-CONTRACT-CLOSE-EVIDENCE.md"
      provides: "Committed raw output of every gate run after the deletion, plus the before/after sweep pair"
      min_lines: 60
      contains: "theme-parity"
    - path: "hypr/.config/hypr/scripts/tests/test-media-hardening.sh"
      provides: "The trimmed suite plus the new bare-path handoff check"
      contains: "media-art-resolve.sh"
  key_links:
    - from: "hypr/.config/hypr/scripts/tests/test-media-hardening.sh"
      to: "hypr/.config/hypr/scripts/media-art-resolve.sh"
      via: "the new check asserts the resolver's stdout contract — a bare local path, never a URL"
      pattern: "media-art-resolve\\.sh"
  prohibitions:
    - "Contract close must not be declared green by narrowing a gate's scope, skipping a gate, or accepting a skip as a pass — a gate tuned until it passes reads as coverage while providing none"
    - "A gate's verdict must not be read through a pipeline into a quiet grep; under this repo's shell settings that construct can exit on the match itself and flip a verdict by output length"
---

<!-- planner-discipline-allow: | *grep -q -->

<objective>
Close the theme contract with every gate green and committed evidence, and add the one
piece of hardening coverage that nothing tests today.

The phase's remaining work is proof, not construction. The contract entry is already gone;
what is owed is a run of every affected gate with its raw output committed, the after half
of the before/after retirement sweep, and one new test covering the handoff the media
backend's own header calls the breakable link.

Per D-21-22, this phase closes on a verifier run rather than on operator attestation. The
prior phase closed on attestation with no verifier run; this phase does not repeat that,
because it lands the final deletion and closes the theme contract immediately before the
phase that rebuilds the whole desktop from a clean clone — so the next phase inherits
whatever this one gets wrong. This plan's job is to leave that verifier run with real
evidence to read.

Purpose: make RETIRE-08's "green with no orphans" a committed fact rather than a claim.
Output: the new hardening check, and a committed evidence file covering every gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-media-fold-in-contract-close/21-CONTEXT.md
@.planning/phases/21-media-fold-in-contract-close/21-RESEARCH.md
@.planning/phases/21-media-fold-in-contract-close/21-08-SUMMARY.md
</context>

## Artifacts this phase produces

New in this plan: the bare-path handoff check inside
`hypr/.config/hypr/scripts/tests/test-media-hardening.sh`, and
`.planning/phases/21-media-fold-in-contract-close/21-CONTRACT-CLOSE-EVIDENCE.md`.

Consumed from earlier plans in this phase (new, not drift): `CavaService` and its API,
`Design.cavaLingerMs`, `cava/.config/cava/config`, the 60-bar ring and 12-lobe mask,
`MediaBackend.setVolumeForPlayer()`, the `quickshell:media` shortcut and `Super+M`,
`BarRoles.dndSurface`/`dndSurfaceFg`, `21-BEHAVIOUR-BASELINE.md`, `21-GATE-02-RECORD.md`,
`21-PRE-DELETION-SWEEP.txt`, `16-VERIFICATION.md`, and the repaired MPRIS-reader check.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover the album-art handoff nothing tests today</name>
  <reversibility rating="reversible">One added check in one test file.</reversibility>
  <behavior>
    - Given a well-formed remote art URL, the resolver's stdout is a bare local filesystem path — it begins with a path separator and carries no URL scheme.
    - Given a local file URL, the resolver's stdout is likewise a bare local path with no scheme, not the input echoed back.
    - The check FAILS if the resolver ever emits a value carrying a scheme separator, because the consuming surface prefixes the file scheme itself and a doubled scheme renders as a blank art area with no error and no log.
    - The check runs inside the suite's existing hermetic harness — its stubbed network client and its isolated home — and makes no real network call.
  </behavior>
  <files>hypr/.config/hypr/scripts/tests/test-media-hardening.sh</files>
  <read_first>
    - `hypr/.config/hypr/scripts/tests/test-media-hardening.sh` — the whole trimmed file as plan 08 left it. Read the hermetic harness at the top (the stubbed executables and the isolated home directory), the shared assertion helper, and the four retained resolver checks around lines 231-305 so the new check matches their construction, their assertion style and their cleanup exactly.
    - `hypr/.config/hypr/scripts/media-art-resolve.sh` — the whole script. Confirm what it writes to stdout on each path: the cache-hit path, the fresh-fetch path, the local-file path and every rejection path.
    - `quickshell/.config/quickshell/modules/dashboard/MediaBackend.qml` — its header, which names this handoff "the breakable link", and the two sites that invoke the resolver and consume its output.
    - `quickshell/.config/quickshell/modules/dashboard/MediaTab.qml` around line 613 — the site that prefixes the file scheme onto the resolved path. This is the other half of the contract the new check protects.
  </read_first>
  <action>
Add one check to the hardening suite covering the handoff the media backend's own header
calls the breakable link, and which nothing tests today.

The contract: the resolver writes a BARE local filesystem path to stdout. The consuming
surface adds the file scheme itself. If the resolver ever emitted a scheme-carrying value,
the consumer would produce a doubled scheme, and the result is a silently blank art area —
no error, no log, no signal of any kind. That silent-failure property is exactly why this
needs a test rather than a code comment.

Assert, using the suite's existing hermetic harness and its existing assertion helper:
- for a well-formed remote art URL, stdout begins with a path separator and contains no
  scheme separator;
- for a local file URL input, stdout is likewise a bare local path — not the input echoed
  back with its scheme intact;
- the assertion is on the ABSENCE of a scheme separator anywhere in the emitted value, so
  it catches any scheme, not only the two tested inputs.

Follow the retained checks' construction exactly: same harness setup, same stubbed network
client so no real request is made, same isolated home, same assertion helper, same cleanup.
Number and label the new check so its relationship to the retained ones is obvious, and
record in the summary which upstream checks survived the trim and which were removed, so a
future reader can see the suite shrank by design rather than by accident.

Write the assertion first and confirm it can fail — feed it a deliberately scheme-carrying
value and observe the check report a failure — before wiring it against the real resolver.
A check that has never been able to report red is not coverage.

Do NOT re-add or resurrect any check that exercised a deleted script.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/tests/test-media-hardening.sh 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -25; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; F=hypr/.config/hypr/scripts/tests/test-media-hardening.sh; N=$(grep -c "media-art-resolve.sh" "$F"); D=$(grep -cE "media-status\.sh|media-players\.sh|media-player\.py" "$F" || true); echo "resolver checks: $N, deleted-script refs: $D"; [ "$N" -ge 8 ] &amp;&amp; [ "$D" -eq 0 ]'</automated>
    <automated>bash -c 'set -uo pipefail; F=hypr/.config/hypr/scripts/tests/test-media-hardening.sh; grep -qiE "bare|scheme|artPath|file://" "$F" &amp;&amp; echo HANDOFF_CHECK_PRESENT'</automated>
    <automated>bash -c 'set -uo pipefail; bash -n hypr/.config/hypr/scripts/tests/test-media-hardening.sh &amp;&amp; echo SYNTAX_OK'</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0 with every check passing.
    - The suite references the retained resolver at least 8 times (the four retained checks plus the new one) and references none of the three deleted scripts.
    - The new check asserts the absence of a scheme separator anywhere in the resolver's emitted value, not merely that a specific scheme is absent.
    - The new check covers both a remote-URL input and a local-file-URL input.
    - The new check runs through the suite's existing hermetic harness: no real network call is made — asserted by the stubbed network client's invocation record, matching how the retained checks already prove this.
    - The check was proven able to fail: the summary records the deliberately-scheme-carrying value that made it report red before it was wired against the real resolver.
    - The summary records the full trim mapping — which upstream checks were removed and which survived — so the suite's smaller size is auditable.
    - `bash -n` on the suite exits 0.
  </acceptance_criteria>
  <done>The bare-path handoff contract is tested on both input shapes, and the test has been shown to be capable of failing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run every affected gate after the deletion and commit the raw evidence</name>
  <reversibility rating="reversible">Adds one evidence document; changes no behaviour.</reversibility>
  <precondition>The deletion commit has landed — the retiring tree is absent from the working directory and the theme contract carries 17 file entries. If either is untrue, this plan is running out of order.</precondition>
  <files>.planning/phases/21-media-fold-in-contract-close/21-CONTRACT-CLOSE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/21-media-fold-in-contract-close/21-PRE-DELETION-SWEEP.txt` — the before half of the sweep pair, committed by plan 08. The after half must be directly comparable to it: same invocation, same class ordering.
    - `theme-engine/.config/theme-engine/theme-doctor` — its usage header, and the block near lines 520-547 where it folds in the colour lint and reparses that tool's own pass/fail lines into its own checks. Note that a SKIP from the folded tool (script not found or not executable) is a real skip, not a pass.
    - `theme-engine/.config/theme-engine/theme-parity` — its usage line; the target argument is optional and omitting it covers all targets.
    - `theme-engine/.config/theme-engine/theme-stress-test` — its usage line and its representative-files list.
    - `hypr/.config/hypr/scripts/retirement-check` — its single-surface and all-surfaces invocations, and its self-test mode.
    - `hypr/.config/hypr/scripts/quickshell-doctor` — its usage line and its flags for suppressing summon, headless output and panel checks, plus its self-test mode.
    - `.planning/phases/21-media-fold-in-contract-close/21-RESEARCH.md` § "Contract Close Mechanics" — the gate invocation table and the shell-pipeline trap documented there.
  </read_first>
  <action>
Run every gate this phase's changes could affect, capture each one's raw output verbatim,
and commit the lot as a single evidence document.

Run and capture, each with its exit code:
- the theme doctor, with no arguments;
- the parity checker, with no target argument so it covers all targets;
- the stress test — this is the one that a prior retirement broke by removing a file that
  was the named representative of its format family, so it is run here specifically to
  confirm this removal did not repeat that;
- the retirement checklist for the retired surface (the AFTER half of the before/after
  pair), plus an all-surfaces run;
- the retirement checklist's own self-test;
- the quickshell doctor, with summon, headless output and panel checks suppressed;
- the quickshell doctor's self-test, which now carries the four reader-count fixtures;
- the keybind cross-check, which must account for the new shortcut;
- the colour lint and the motion lint;
- the media hardening suite as extended by Task 1.

**Read every verdict correctly.** Capture each gate's output into a variable and its exit
code separately, then match against the materialised variable. Do NOT read a verdict by
piping a gate straight into a quiet grep: under this repo's shell settings that construct
can exit on the match itself, the instant the matcher closes its input, which makes the
verdict depend on how long the output happened to be. This repo has already been bitten by
exactly that.

**A SKIP is not a PASS.** The theme doctor folds in the colour lint and reparses its
pass/fail lines; if that tool is not found or not executable the doctor records a skip. A
skip means the check did not run, and this evidence document must show it as such rather
than counting it toward green.

Write `.planning/phases/21-media-fold-in-contract-close/21-CONTRACT-CLOSE-EVIDENCE.md`
containing:
- a summary table: gate, invocation, exit code, verdict, and any skips called out
  explicitly;
- the raw output of each run, in fenced blocks;
- an explicit before/after comparison of the two retirement-checklist runs for the retired
  surface, showing that both halves ran and both report zero real hits, with the known
  short-name precision limit accounted for rather than silently absorbed;
- an explicit statement of the theme contract's file-entry count and that its removed
  entry's format family is still represented;
- an explicit statement that exactly one MPRIS reader remains, citing both the one-time
  sweep and the standing check — the sweep is this phase's evidence, the standing check is
  the line held afterwards.

**Two pre-existing conditions to record honestly rather than route around.** The quickshell
doctor carried two failures before this phase: the reader-count check, which this phase
repaired and which must now be green, and an unrelated permissions-allowlist path
resolution failure, which is out of this phase's scope and must still be reported as
failing. Do not fix it, do not mask it, and do not let its presence be read as this phase
leaving something broken — state plainly which failure this phase owned and closed, and
which one it did not own.

If any gate this phase DOES own comes back red, do not narrow it, do not skip it and do not
adjust its scope to make it green. Fix the cause, re-run, and record both runs.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash theme-engine/.config/theme-engine/theme-doctor 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -15; echo "theme-doctor rc=$RC"; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash theme-engine/.config/theme-engine/theme-parity 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -15; echo "theme-parity rc=$RC"; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash theme-engine/.config/theme-engine/theme-stress-test 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -15; echo "theme-stress-test rc=$RC"; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/retirement-check --all 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -25; echo "retirement-check rc=$RC"; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/retirement-check --self-test 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -10; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/quickshell-doctor --self-test 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -20; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/quickshell-doctor --no-summon --no-headless-output --no-panel-checks 2&gt;&amp;1); F=$(printf "%s\n" "$OUT" | grep -c "\[FAIL\]" || true); printf "%s\n" "$OUT" | grep "\[FAIL\]" || true; echo "quickshell-doctor failures: $F"; [ "$F" -le 1 ]'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/quickshell-doctor --no-summon --no-headless-output --no-panel-checks 2&gt;&amp;1); printf "%s\n" "$OUT" | grep -iE "MPRIS" ; printf "%s\n" "$OUT" | grep -E "\[FAIL\].*MPRIS" &amp;&amp; { echo "READER CHECK STILL RED"; exit 1; }; echo READER_CHECK_GREEN'</automated>
    <automated>bash -c 'set -uo pipefail; OUT=$(bash hypr/.config/hypr/scripts/keybind-doctor 2&gt;&amp;1); RC=$?; printf "%s\n" "$OUT" | tail -15; exit $RC'</automated>
    <automated>bash -c 'set -uo pipefail; E=.planning/phases/21-media-fold-in-contract-close/21-CONTRACT-CLOSE-EVIDENCE.md; test -f "$E" &amp;&amp; for G in theme-doctor theme-parity theme-stress-test retirement-check quickshell-doctor keybind-doctor colour-lint motion-lint; do grep -qF "$G" "$E" || { echo "MISSING GATE IN EVIDENCE: $G"; exit 1; }; done; echo ALL_GATES_IN_EVIDENCE'</automated>
    <automated>bash -c 'set -uo pipefail; N=$(node -e "const j=require(process.cwd()+\"/theme-engine/.config/theme-engine/contract.json\");process.stdout.write(String(j.files.length))"); echo "contract entries: $N"; [ "$N" -eq 17 ]'</automated>
  </verify>
  <acceptance_criteria>
    - The theme doctor, the parity checker and the stress test all exit 0.
    - The retirement checklist exits 0 for all surfaces, and its self-test exits 0.
    - The quickshell doctor's self-test exits 0, including the four reader-count fixtures added earlier in this phase.
    - The quickshell doctor's live run reports at most 1 failure, and that failure is the unrelated pre-existing permissions one — the reader-count check reports no failure.
    - The keybind cross-check exits 0 with the new shortcut accounted for.
    - The colour lint and the motion lint both exit 0, and the theme doctor's folded colour-lint result is a genuine pass rather than a skip — stated explicitly in the evidence document.
    - The media hardening suite exits 0.
    - The evidence document names all eight gate families and carries the raw output of each run in fenced blocks.
    - The evidence document carries an explicit before/after comparison of the retirement-checklist runs, showing both halves ran, with the known short-name precision limit accounted for rather than absorbed.
    - The evidence document states the theme contract's entry count as 17 and confirms the removed entry's format family is still represented.
    - The evidence document states that exactly one MPRIS reader remains, citing both the sweep and the standing check.
    - The evidence document distinguishes the failure this phase owned and closed from the pre-existing one it did not own.
    - No gate was narrowed, skipped or scope-adjusted to reach green — asserted by the evidence document recording each gate's invocation verbatim, unchanged from its documented usage.
  </acceptance_criteria>
  <done>Every affected gate has been run after the deletion with its raw output committed, and the theme contract's post-migration state is a documented fact rather than a claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| album-art resolver → consuming surface | The resolver's stdout crosses into an image source after the consumer prefixes a scheme; a scheme-carrying value silently produces a blank area with no error path. |
| gate output → verdict | A gate's textual output is parsed into a pass/fail decision; a misread verdict is indistinguishable from a real pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-26 | Tampering | album-art handoff contract | high | mitigate | A scheme-carrying resolver output silently blanks the art with no error and no log. Mitigated by the new check asserting the absence of any scheme separator across both input shapes, run inside the existing hermetic harness with no real network call. |
| T-21-27 | Repudiation | gate verdict reading | medium | mitigate | Under this repo's shell settings a gate piped into a quiet matcher can exit on the match, making the verdict depend on output length. Mitigated by capturing output and exit code separately and matching against the materialised variable, and by committing raw output so a verdict can be independently re-read. |
| T-21-28 | Repudiation | folded lint skip | medium | mitigate | The theme doctor folds in the colour lint and records a SKIP when that tool is missing or not executable; a skip counted as a pass would report coverage that never ran. Mitigated by an acceptance criterion requiring the folded result to be a genuine pass, stated explicitly. |
| T-21-29 | Information Disclosure | retained resolver coverage | high | mitigate | The retained resolver's scheme allowlist, loopback rejection, literal-encoding bypass rejection and cache separation must all still be exercised after the trim. Asserted by requiring at least 8 resolver references in the suite and a fully-passing run. |
| T-21-SC | Tampering | package installs | low | accept | No package-manager install in this plan; one test file and one evidence document. |

**Canon-referral breadcrumbs (not minted as prohibitions):** request-forgery and
encoding-bypass coverage for the retained resolver are canon security concerns owned by
`/gsd-secure-phase` and by T-21-29 above rather than by a bespoke prohibition.
</threat_model>

<verification>
- Every gate this phase affects has been run after the deletion and exits as expected.
- The before/after retirement sweep pair is committed and comparable.
- The theme contract's entry count and format-family coverage are documented facts.
- The reader-count check is green and the only remaining doctor failure is the pre-existing one this phase does not own.
- The evidence document exists so the phase's closing verifier run reads real output rather than a claim.
</verification>

<success_criteria>
RETIRE-08 is closed: the theme contract is at its post-migration size with every gate green
and no orphaned entries, the before/after sweep pair is committed, the album-art handoff is
covered by a test that can fail, and the phase has real evidence for its closing verifier
run.
</success_criteria>

<output>
Create `.planning/phases/21-media-fold-in-contract-close/21-09-SUMMARY.md` when done
</output>
