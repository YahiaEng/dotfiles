---
phase: 21
slug: media-fold-in-contract-close
status: approved
shadcn_initialized: false
preset: none
created: 2026-08-16
---

# Phase 21 — UI Design Contract

> Visual and interaction contract for the AGS-media-card fold-in and the
> resulting `contract.json` close. Generated by gsd-ui-researcher, verified
> by gsd-ui-checker. This is a Quickshell/QML desktop-shell surface, not a
> web app — there is no shadcn/Tailwind/npm stack in this repo. The design
> system is this repo's own token pipeline: `Colours.qml` / `Motion.qml`
> singletons reading matugen-rendered `~/.local/state/theme/{palette,
> motion}.json`, plus `modules/dashboard/Design.qml` (spacing/type/icon
> constants) and `modules/bar/BarRoles.qml` (bar-specific colour roles
> blended from `Colours.*`). This document does not re-decide anything
> `21-CONTEXT.md` already locked (D-21-00..27) — it restates those decisions
> in implementable token/geometry terms, resolves the two things CONTEXT.md
> explicitly left for this UI phase to render (D-21-10's per-player-volume
> ×dedup interaction, D-21-26/27's exact tint values), and specifies the
> required `14-UI-SPEC.md` amendment verbatim (D-21-04).
>
> **Operator question round: run 2026-08-16, after checker approval.** The
> authoring agent had no interactive round and self-resolved every open call;
> three of those were then put to the operator as rendered ASCII options and
> answered. Two **changed** the authored recommendation or confirmed it against
> a stated alternative:
>
> | Call | Outcome |
> |------|---------|
> | Cava claim condition (§ Appendix) | **Changed** — release on surface-hidden only; pause no longer releases the claim |
> | Per-player volume placement (D-21-10) | **Confirmed** — inline mini-sliders in the player-switcher dropdown |
> | Probe element kinds (§ UI Considerations) | **Confirmed** — authored overrides applied; 0 unclassified |
>
> Values still left to the operator's eye rather than settled here: the linger
> duration (5000 ms), the DND tint alpha (`0.28`), the frost value (`0.38`), and
> the five visualiser geometry numbers. All are single-constant tunables — final
> pixel judgment happens at D-21-20's blocking combined render gate.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — no shadcn/Tailwind/npm in this repo; QML + this repo's own matugen-driven token pipeline |
| Preset | not applicable |
| Component library | Qt Quick Controls 2 (Material style) — `Slider`, `MouseArea`/`HoverHandler` already in use throughout `MediaTab.qml`/`MediaPopout.qml` |
| Icon library | Material Symbols Rounded (variable font, FILL axis) — `Design.symbolFontFamily`, already installed and used by every glyph in these files |
| Font | Inherits the shell's existing system font stack — unchanged, no new font decision in this phase |

**Component source of truth:**
- `quickshell/.config/quickshell/modules/Colours.qml` — the only legitimate source of colour values (19 Material You semantic roles: `primary`, `onPrimary`, `primaryContainer`, `onPrimaryContainer`, `secondary`, `onSecondary`, `secondaryContainer`, `onSecondaryContainer`, `tertiary`, `onTertiary`, `surface`, `onSurface`, `surfaceVariant`, `onSurfaceVariant`, `background`, `onBackground`, `outline`, `error`, `onError`).
- `quickshell/.config/quickshell/modules/Motion.qml` — the only legitimate source of duration/easing values: `standardDuration`/`standardEasing`, `emphasizedInDuration`/`emphasizedInEasing`, `emphasizedOutDuration`/`emphasizedOutEasing`, `staggerOffsetDuration`/`staggerOffsetEasing`, `ambientDuration`/`ambientEasing`, `borderRotateDuration`, plus `motionEnabled`/`motionScale`/`motionMultiplier`.
- `quickshell/.config/quickshell/modules/dashboard/Design.qml` — spacing (`spacingXs/Sm/Md/Lg/Xl`, `panelPadding`), type (`fontDisplay/Heading/Body/Label`, `weightDisplay/Emphasis/Bold/Body`), icon (`iconSizeMd`, `symbolFontFamily`) constants already consumed by `MediaTab.qml`/`MediaPopout.qml`.
- `quickshell/.config/quickshell/modules/bar/BarRoles.qml` — bar-specific colour roles blended from `Colours.*` (`accent`/`onAccent` = `Colours.primary`/`onPrimary`, `warn`/`onWarn` = `Colours.tertiary`/`onTertiary`, `danger`/`onDanger` = `Colours.error`/`onError`, `capsule`/`capsuleHover`/`capsuleFg`, `fillClock`/`fillNotification`/`fillUpdates` pill pairs). **Any new bar-side blended colour this phase needs (the DND capsule tint, § DND Capsule Tint below) is added here, following the existing `Qt.rgba(roleColour.r/.g/.b, alpha)` pattern already used for `barSurface`/`capsule`/`notifSurface` — never a hex literal in a consuming file.**

`colour-lint` (GATE-04) and `motion-lint` both reject hex/raw-number literals outside these four files. Every value this contract names is a reference to one of them, or a recommended new blended token to be *added* to `BarRoles.qml` (never inlined into `MediaTab.qml`/`MediaPopout.qml`/`ClockActionsCapsule.qml`).

---

## Spacing Scale

No new spacing tokens — this phase extends two already-token-driven surfaces (`MediaTab.qml`, `MediaPopout.qml`) using the scale already live in `Design.qml`.

| Token | Value | Usage in this phase |
|-------|-------|----------------------|
| xs (`Design.spacingXs`) | 4px | Icon-to-label gaps in player-switcher rows, checkmark column width |
| sm (`Design.spacingSm`) | 8px | Art-column internal spacing (art → source pill), mini-volume-slider-to-label gap (new) |
| md (`Design.spacingMd`) | 16px | Player-menu row internal margins |
| lg (`Design.panelPadding`) | 24px | Tab panel padding — unchanged |

Exceptions (geometry that is deliberately NOT on the 4px grid, same house convention as the dial components in `14-UI-SPEC.md`):

- **Visualiser ring/bar geometry** — `ringGap: 8`, `ringStrokeWidth: 3`, `artCircleSize`/`ringRadius` (all already-declared `MediaTab.qml` constants, unchanged by this phase) plus the new bar-specific geometry in § Visualiser Geometry below. Custom `QtQuick.Shapes` arc/line geometry, not spacing-scale values.
- **Player-menu mini volume slider width** — recommend a fixed **56px** control (not a scale multiple): narrow enough to fit inside the existing `selectorPill.width`-bounded dropdown without widening the whole menu, wide enough to show a usable drag target. See § Per-Player Volume below for the full row layout.

---

## Typography

No new type roles — reuses the four roles `14-UI-SPEC.md` already declared and `MediaTab.qml`/`MediaPopout.qml` already consume via `Design.*`.

| Role | Size | Weight | Line Height | Usage in this phase |
|------|------|--------|-------------|----------------------|
| Heading (`Design.fontHeading`) | 20px | `Font.DemiBold` (600) | 1.2 | Unchanged — track title stays on this role |
| Body (`Design.fontBody`) | 16px | `Font.Normal` (400) | 1.5 | Unchanged — artist/album, popout title |
| Label (`Design.fontLabel`) | 12px | `Font.Normal` (400) | 1.5 | Player-menu row labels (existing), **new**: per-player volume percentage readout, "N players — switch in the dashboard" popout line (existing) |

**New element, no new size:** the per-player mini-slider's percentage readout (if shown — see below) uses `Design.fontLabel` (12px), matching every other secondary numeric readout in this file (e.g. the popout's existing player-count line).

---

## Color

This repo's colour system is 19 Material You semantic roles (`Colours.*`), matugen-rendered per-theme/per-wallpaper — not fixed hex values. This section maps the 60/30/10 framing onto that role system, extending (not replacing) `14-UI-SPEC.md`'s existing Color section, which already governs the Media tab as part of the Dashboard Drawer surface.

| Role | Token | Usage in this phase |
|------|-------|----------------------|
| Dominant (60%) | `Colours.surface` / `Colours.onSurface` | Drawer/tab background, default text — unchanged |
| Secondary (30%) | `Colours.surfaceVariant` / `Colours.onSurfaceVariant` | Art placeholder background, player-menu surface, inactive player-row label, mini-slider track — unchanged pattern |
| Accent (10%) | `Colours.primary` / `Colours.onPrimary` | **Extended this phase** — see the required `14-UI-SPEC.md` amendment below. Also covers: the active player-row checkmark stays `Colours.onSurface` (unchanged, not accent — only the enumerated list below gets accent) |
| Destructive | `Colours.error` / `Colours.onError` | Unchanged — no new destructive UI surface in this phase (the AGS package deletion is a one-way phase gate, not an in-app control; see Copywriting Contract) |

### Required amendment to `14-UI-SPEC.md` (D-21-04)

**This is a required deliverable of this phase's implementation, not an implied one.** `14-UI-SPEC.md`'s Color section (lines 86 and 89) currently reads:

> `86` — `| Accent (10%) | Colours.primary / Colours.onPrimary | **Reserved for:** the active tab indicator (D-16), the lit/filled state of the 3 mirrored toggle chips (D-25 — "active = filled with primary"), the active segment of the motion-scale row (D-24), the play/pause button's pressed/active state (D-40), the calendar's "today" highlight (D-34), the pending-pulse affordance (D-22) |`
>
> `89` — `Accent reserved for: active tab indicator, lit toggle chips (Gaming/DND/Dark when ON), active motion-scale segment, play/pause active/pressed state, calendar "today" cell, pending-pulse animation — **never** applied as a general interactive-element default (e.g. all buttons are NOT tinted primary; only the ones enumerated above and in D-25/D-26).`

**Both lines must be edited to append one clause each**, naming the new reserved use:

- Line 86 appends, before the closing `|`: `, the Media tab's / bar media popout's cava-driven cover-art visualiser ring (D-21-01/D-21-04, Phase 21) — carries live audio signal, not decorative chrome`
- Line 89 appends, before the em-dash: `, cava-driven media visualiser ring`

Rationale carried from `21-CONTEXT.md` D-21-04: the reservation exists to stop accent being spent on decorative chrome; a ring rendering live audio amplitude is not decorative chrome, and Caelestia's own reference implementation tints its equivalent ring `m3primary`. The ring's **silence-state colour stays `Colours.outline`** unless amplitude is present — see § Visualiser States below; the amendment reserves the *lit/reactive* state for accent, it does not make the ring permanently primary-tinted.

### Cover-art mask background

The mask mechanism (`MultiEffect.maskEnabled`/`maskSource`, established at round 4) is unchanged; only its source shape changes from a circular `Rectangle` to the hand-authored 12-lobe cookie path (D-21-02). `artBackground`'s fill stays `Colours.surfaceVariant` (unchanged — this is the placeholder-state background visible before art loads or when no player exists), now painted through the cookie mask instead of a circle.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | No traditional CTA — the Media tab is a control surface, not a form. `Super+M` opens the dashboard directly on the Media tab (D-21-12); the popout's existing wayfinding label **"Open Media tab"** is unchanged |
| Empty state heading | Not applicable — D-41's in-place-placeholder pattern continues unchanged: no separate empty screen |
| Empty state body | Unchanged: **"Nothing is playing"** (`MediaPopout.qml`'s existing `emptyStateText`; the Media tab's own D-41 placeholder is unchanged by this phase). New: while empty, the visualiser ring renders its static silence state (see § Visualiser States) — no separate copy, the quiet ring itself is part of the existing empty register |
| Error state | No new user-facing error copy. Two failure modes this phase introduces both degrade silently to an existing calm state, per house convention (`14-UI-SPEC.md`'s "never a scary error string"): (1) cava binary missing/crashes → ring falls back permanently to the static silhouette, logged only, never shown to the user; (2) a torn/partial cava output line → ignored, last-good frame held (D-21-01, mirrors `ags/lib/cava.ts`'s existing rule) |
| Destructive confirmation | **None in-app.** The one destructive action this phase performs — deleting the `ags` package and its tree — is gated by D-21-20's blocking combined render gate plus a `checkpoint:decision` in the plan, not by an in-app confirmation dialog. No delete/remove affordance exists anywhere in the Media tab or popout |
| Player-switcher row (unchanged) | Active player: `check` glyph + label. Inactive: label only, `Colours.onSurfaceVariant`. **New:** each row with `volumeSupported: true` gains an inline percentage readout beside its mini-slider (e.g. `72%`) — Label role, same tint as the row's own label (active → `onSurface`, inactive → `onSurfaceVariant`) |
| Multi-player popout line (unchanged) | `"{N} players — switch in the dashboard"` |

---

## UI Considerations

> Shape-rooted UI **state** coverage, produced by `ui-consideration-probe.cjs` over the seven
> surfaces this phase adds or changes, then resolved item by item. Empty-state and error-state
> **copy** lives in `## Copywriting Contract` above — rows here reference it rather than
> restating it (de-dup).
>
> **Element kinds were operator-confirmed, not taken from the heuristic.** The prose classifier
> mis-tagged two elements: the visualiser ring detected as `nav` only (it matched the word
> "tab" in "Media tab"), and the DND capsule detected as nothing at all (`unclassified`).
> Authored `elements` overrides were applied — ring/popout gained `media` (raising the `empty`
> and `populated` rows below, which the heuristic would have silently dropped), the DND capsule
> gained `interactive-control` + `static-content`. Re-running with overrides moved the probe
> from 39 applicable/1 unclassified to **35 applicable / 0 unclassified**.

**Coverage: 35 applicable — 29 resolved (explicit), 3 resolved (backstop), 3 dismissed with reason, 0 unresolved.**

| Element | Kinds | Category | Status | Resolution |
|---------|-------|----------|--------|------------|
| **E1** — Visualiser ring (Media tab + popout) | `media` | `empty` | ✅ explicit | No player at all: ring renders the static silhouette — Colours.outline, RoundCap, 3px minimum sliver, geometrically identical to the round-3-accepted dashed ring. Nothing previously approved changes. |
|  |  | `loading` | ✅ explicit | ~350ms cava cold start: ring HOLDS the silence silhouette until the first real frame arrives. Never a blank ring, never a zero-length ring, never a flash of accent before data exists. Drawer opens on Motion.standardDuration (200ms), so the first frame lands ~150ms after the drawer settles. |
|  |  | `error` | ✅ explicit | cava binary missing or process exits unexpectedly: permanent fallback to the static silhouette, logged only. No user-facing error copy (house rule: never a scary error string). A torn/partial output line is ignored and the last-good frame is held (mirrors ags/lib/cava.ts). |
|  |  | `populated` | ✅ explicit | Playing with live cava data: 60 radial bars, per-bar length proportional to that band's amplitude, Colours.primary accent, RoundCap, 3px minimum sliver so no bar ever fully vanishes. Colour animates outline->primary via the existing Behavior on strokeColor (Motion.standardDuration/standardEasing). PAUSED is NOT this state: per the locked claim condition, cava keeps running while paused with a surface open, but the ring shows the silence silhouette because there is no amplitude to render. |
| **E2** — Player-switcher dropdown | `list-collection, interactive-control, static-content` | `empty` | ✅ explicit | Zero players: existing 'No players' label, no pill content, no dropdown. Unchanged. |
|  |  | `loading` | ✅ explicit | Menu open transition uses the existing Motion.standardDuration/standardEasing opacity fade. No new motion token. MPRIS enumeration is synchronous from Mpris.players.values — no separate loading state exists. |
|  |  | `error` | ✅ explicit | A player disappearing mid-interaction (process exits while its row is open): the row leaves the model on the next Mpris.players.values change; a mini-slider drag in flight targets a dead id and the clamped-write in setVolumeForPlayer becomes a no-op rather than throwing. No error copy. |
|  |  | `populated` | ✅ explicit | 2+ players: dropdown with one row each — checkmark column, elided label, 56px mini-slider, percentage readout (Design.fontLabel). Active row tinted onSurface, inactive onSurfaceVariant. |
|  |  | `partial` | ✅ explicit | volumeSupported:false rows omit the slider AND the readout entirely and render label-only, mirroring the Media tab's existing hasVolume-gated bottom volume row. A mixed list (some rows with sliders, some without) is the expected normal case, not a defect. |
|  |  | `overflow` | ◐ backstop | Many players (more than fit the drawer height): the dropdown must clip and scroll rather than grow past the panel. The exact scroll treatment is not specified here — verify at the D-21-20 render gate with a synthetic 6+ player fixture. |
|  |  | `zero-one-many` | ✅ explicit | 0 -> existing 'No players' empty label; 1 -> pill shows the label with NO expand chevron (existing hasChoice gate, unchanged); 2+ -> full dropdown. Dedup collapses duplicate perceptual sources BEFORE this count, so a deduped pair reads as one player throughout. |
|  |  | `long-text` | ✅ explicit | Row labels keep elide: Text.ElideRight on a narrower budget. Existing formula (menuColumn.width - spacingSm*2 - (fontLabel+4) - spacingXs) additionally subtracts 56 (slider) + spacingSm (gap) + ~28 (readout) on rows that have a slider. Menu widens to Math.max(selectorPill.width, artSize*1.3) so the label keeps a usable budget. |
| **E3** — Bottom volume row (active player) | `interactive-control` | `loading` | ✅ explicit | No loading state — the Slider binds directly to the active player's live volume property. Unchanged parity baseline. |
|  |  | `error` | ✅ explicit | Active player without volume support: the whole row is hidden (existing hasVolume gate). A write to a player that rejects it is a clamped no-op. No error copy. Unchanged. |
|  |  | `long-text` | ⊘ dismissed | The bottom volume row carries no text — it is a bare Slider with an icon. There is no string that can overflow. |
| **E4** — Cookie-masked cover art | `media` | `empty` | ✅ explicit | No player / art absent: the surfaceVariant placeholder background shows through the SAME 12-lobe cookie mask, so the silhouette is identical in shape to the loaded state. No separate empty artwork. |
|  |  | `loading` | ✅ explicit | Art resolving (album-art resolver's single-flighted subprocess in flight): placeholder holds through the cookie mask until the image source is ready. No spinner — the mask shape is stable across the transition so nothing reflows. |
|  |  | `error` | ✅ explicit | Art fetch fails or the URL is rejected by the resolver's security discipline: falls back to the same surfaceVariant placeholder as the empty state. Logged only, no user-facing copy. |
|  |  | `populated` | ◐ backstop | Loaded art at a NON-SQUARE aspect ratio must fill the cookie mask without letterboxing or horizontal/vertical distortion — the mask clips, the image does not stretch. This re-opens the round-4 clipping test that the circular mask previously satisfied; D-21-20's render gate must include a deliberately non-square cover fixture (e.g. a 16:9 and a 3:4 source) as an explicit check, not an incidental one. |
| **E5** — DND-tinted bar clock/actions capsule | `interactive-control, static-content` | `loading` | ⊘ dismissed | The DND boolean is read synchronously from NotifServer.dnd, already live in ClockActionsCapsule. There is no asynchronous fetch and therefore no in-flight state to design. |
|  |  | `error` | ✅ explicit | If the notification server is unavailable, dndActive reads false and the capsule renders its normal BarRoles.capsule fill — the tint is purely additive, so its absence is the existing correct appearance rather than a broken one. |
|  |  | `overflow` | ⊘ dismissed | The capsule's content (clock text plus a fixed set of action glyphs) is unchanged by this phase; the DND change is a fill-colour swap on the outer surface only and adds no content that could overflow. |
|  |  | `long-text` | ✅ explicit | Clock text and glyphs keep their existing tint (BarRoles.dndSurfaceFg = Colours.onSurface, deliberately NOT onAccent) because a 0.28-alpha accent wash over the capsule surface does not reach onPrimary's contrast target. Legibility of the longest clock string is therefore unchanged from the untinted state. |
| **E6** — Bar media popout | `media, nav, interactive-control, static-content` | `empty` | ✅ explicit | No player: existing 'Nothing is playing' copy (MediaPopout.qml emptyStateText), with the ring in its static silence state. No new copy — the quiet ring is part of the existing empty register. |
|  |  | `loading` | ✅ explicit | Same as the ring's cold-start rule: silhouette held until the first cava frame. The popout is the surface most likely to hit a cold start, since it is hover-driven and short-lived. |
|  |  | `error` | ✅ explicit | cava unavailable: permanent silhouette, no copy. Identical to the Media tab's rule — one behaviour across both surfaces. |
|  |  | `populated` | ✅ explicit | Playing with one player: live reactive ring plus the existing now-playing rows and the 'Open Media tab' wayfinding label. |
|  |  | `overflow` | ✅ explicit | The popout does not host the player list — with 2+ players it shows the existing single line 'N players — switch in the dashboard' and defers switching to the Media tab. This is what keeps the popout's height bounded regardless of player count. |
|  |  | `long-text` | ✅ explicit | Long track/artist strings keep the popout's existing elide treatment, unchanged by this phase. The ring is fixed-diameter and does not participate in text layout. |
| **E7** — Dashboard Media tab panel | `nav, media, interactive-control, static-content` | `empty` | ✅ explicit | D-41's in-place-placeholder pattern is unchanged: no separate empty screen. Transport controls remain visible but inert, art shows the cookie-masked placeholder, ring shows the silence silhouette. |
|  |  | `loading` | ✅ explicit | Tab opens on the existing drawer motion (Motion.standardDuration, 200ms). Super+M opens the dashboard directly on this tab (D-21-12). The cava cold start, when one is paid, resolves ~150ms after the drawer has settled. |
|  |  | `error` | ✅ explicit | No new user-facing error surface. Every failure this phase introduces (cava missing, cava crash, torn frame, art fetch failure) degrades to an existing calm state and is logged only. |
|  |  | `populated` | ✅ explicit | Full parity with the retired AGS card (QMEDIA-01): transport, seek, cookie-masked cover art, per-player volume via the dropdown mini-sliders, player switching, plus the cava ring the AGS card also had. |
|  |  | `overflow` | ◐ backstop | Panel content at its tallest (many players expanded, long metadata) must stay within the drawer without pushing the pinned controls off-surface. Verify at the D-21-20 render gate together with E2's scroll check. |
|  |  | `long-text` | ✅ explicit | Title on Design.fontHeading, artist/album on Design.fontBody, both keeping their existing elide behaviour. No new type role and no new size, so the longest-string behaviour is unchanged from what 14-UI-SPEC.md already governs. |

**Planner note.** The three `◐ backstop` rows are the only ones without a concrete authored
truth — each names a held-out visual check rather than a spec'd value, and all three land on
the D-21-20 combined render gate:

- `E2/overflow` — dropdown scroll/clip behaviour with a synthetic 6+ player fixture
- `E4/populated` — **non-square cover art through the 12-lobe cookie mask.** This re-opens the
  round-4 clipping test the circular mask previously satisfied; the render gate must carry a
  deliberately non-square fixture (e.g. 16:9 and 3:4 sources) as an explicit check, not an
  incidental one. The mask must clip without stretching the image.
- `E7/overflow` — panel content at its tallest must not push the pinned controls off-surface

---

## Registry Safety

Not applicable — QML/Quickshell project, no shadcn/component-registry mechanism. No new third-party package this phase adds to the design surface (the phase's only new package, `cava/` as a stow config package, carries no UI code and is not a design-system dependency).

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — not applicable to this stack | not applicable |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** APPROVED by `gsd-ui-checker` — 6/6 dimensions, 0 blocking issues.

Checker's non-blocking implementation notes, carried forward as planner obligations:

1. `Design.qml` — add `readonly property int cavaLingerMs: 5000` (render-gate adjustable).
2. `BarRoles.qml` — add the `dndSurface` / `dndSurfaceFg` pair (§ DND Capsule Tint).
3. `14-UI-SPEC.md` — apply the verbatim amendment at § Required amendment above. **This is a plan task in its own right, not an implied side effect of building the ring** (D-21-04 names the amendment itself as a required deliverable).

---

## Appendix: Resolved Design — Cava Lifecycle, Visualiser Geometry, Per-Player Volume, DND Tint

These are the specific renderings this phase's own `21-CONTEXT.md` requires ("render that interaction... before implementing", D-21-10) plus the two remaining Claude's-Discretion visual calls (D-21-06 linger duration, D-21-27 tint strength). Everything else visual in this appendix (bar count, cookie shape, accent colour, refcount+linger *mechanism*) is already locked in CONTEXT.md and is restated here only as implementable geometry, not re-decided.

### Cava claim condition (new resolution — not explicitly stated in CONTEXT.md)

D-21-06 locks *refcount + linger* as the ownership mechanism but does not state exactly when a surface "claims" the process. **Resolved by the operator (2026-08-16, `/gsd-ui-phase` question round): a surface claims cava when it is VISIBLE, and releases only when it is HIDDEN. Playback state does NOT gate the claim — pausing keeps the process alive.**

```
surface visible? ──► claim cava (refcount++)
                     (visible = Media tab is the active dashboard
                      tab AND the dashboard is open; OR the bar's
                      media popout is open)

release (refcount--) on: surface hidden ONLY — never on pause
refcount reaches 0 ──► start Design.cavaLingerMs timer
timer fires with refcount still 0 ──► kill cava process
refcount becomes >0 before timer fires ──► cancel timer, process stays alive (no re-spawn, no 350ms cost)
```

**Why pause does not release.** The linger cannot itself produce lag — it delays *killing* a process nobody is watching, so a longer linger is strictly less perceived lag, never more. The only user-visible cost is the ~350 ms cold start on the way back in, and CONTEXT.md's own measurement puts that first frame roughly **150 ms after the drawer has already settled** (drawer opens on `Motion.standardDuration`, 200 ms), landing on the designed silence silhouette rather than a blank or a spinner.

The release *trigger*, not the linger value, is what decides how often that cost is paid. A 5 s linger comfortably covers the navigation flows (popout → dashboard, accidental close-and-reopen) but not pause-then-resume, which is typically tens of seconds. Gating the claim on `playing` would therefore re-pay the cold start on nearly every resume. Releasing on visibility alone makes resume seamless at negligible idle cost, because **both media surfaces are inherently transient** — the popout is hover-driven and the dashboard is a drawer, so "open **and** paused" is a narrow, short-lived state rather than a resting one.

Rejected: *claim = visible AND playing* (strictest zero-idle reading, but ~150 ms of quiet ring on most resumes); *always-on* (1.20% of a core permanently — CONTEXT.md rejected it, and D-21-06's one-knob requirement keeps it reachable later regardless).

**Ring behaviour while paused is unchanged by this choice:** the process runs, but there is no amplitude, so the ring shows the same static silhouette as the no-player state. Keeping cava alive buys instant resume; it does not put a live ring on a paused track.

**Linger duration (Claude's discretion, D-21-06):** recommend **5000ms (5s)**, as a new named constant `Design.cavaLingerMs` (not reusing the bar's sub-second `barDrawerGraceMs`/`popoutDismissGraceMs` family — those govern hover-dismiss grace at a completely different scale/purpose; this is a process-lifecycle decision). Rationale: long enough to survive the navigation flows this value is actually responsible for — a popout-close-then-dashboard-open, or an accidental close-and-reopen — without re-paying the measured ~350ms cold start; short enough to still read as "a few seconds" per D-21-06's own wording and to matter for the zero-idle budget. **Pause/resume is deliberately NOT this value's job** — it is handled structurally by the claim condition above (pause never releases), because pause gaps run to tens of seconds and no defensible linger value would cover them. **Must be reachable by changing this single constant to a very large value (or a dedicated `Design.cavaAlwaysOn: bool` bypass) if the operator's reserved revisit lands on always-on** — do not restructure the refcount to satisfy that reversal.

### Visualiser States

Two ring states, both reusing the existing art-column layout (`artSize`, `ringGap`, `ringStrokeWidth`, `ringRadius` — all unchanged `MediaTab.qml` constants):

**Silence (no player / paused / cava not yet started / cava failed):**
```
        ·  ·  ·
     ·           ·
   ·      ┌───┐    ·
  ·       │ART│     ·     ← Colours.outline, static, RoundCap dashes
   ·      └───┘    ·         (identical to today's round-3 dashed ring —
     ·           ·           nothing previously approved changes here)
        ·  ·  ·
```

**Playing (cava streaming live amplitude):**
```
        ╻ ╹╻╻╻ ╻
     ╻╹           ╻╹
   ╻      ┌───┐    ╹╻      ← Colours.primary/accent, per-bar length ∝
  ╹       │ART│     ╻          that band's live amplitude, RoundCap,
   ╻      └───┘    ╹╻          minimum 3px sliver even at near-zero
     ╹╻           ╻╹          amplitude (never a bar that fully vanishes)
        ╻ ╹╻╻╻ ╻
```

### Visualiser Geometry (Claude's discretion, D-21-01/D-21-03)

60 bars (locked, D-21-03), arranged as a `Repeater` of `ShapePath` line segments replacing the single dashed `PathAngleArc` at `MediaTab.qml:525-551` — one straight radial segment per bar, matching Caelestia's "clock hand" construction rather than an arc:

| Property | Recommended value | Rationale |
|----------|---------------------|-----------|
| Inner radius (segment start) | `root.ringRadius` (existing constant, unchanged) | Bars start exactly where today's dashed ring sits — preserves the silence-state footprint pixel-for-pixel |
| Max outer radius (full amplitude) | `root.ringRadius + 14` | Fits inside the existing `artSize`/`artColumn` layout budget without resizing anything else this phase touches |
| Minimum sliver length at silence | `3px` | Matches the existing `dashPattern: [1, 3]` dash length at `ringStrokeWidth: 3` — this is the exact property the silence-state argument (§ round-3 preservation) depends on |
| Bar stroke width | `2px` | Slightly thinner than the old single stroke (3px) since 60 moving bars read denser than 56 static dashes at the same width; render-gate-adjustable |
| Cap style | `ShapePath.RoundCap` | Matches the existing ring exactly |
| Angular slot | `360° / 60 = 6°` per bar | Fixed by the locked bar count |
| Colour | `Colours.outline` at silence, animates to `Colours.primary` when `amplitude > 0` via the existing `Behavior on strokeColor` pattern already present at `MediaTab.qml:560-567` (`Motion.standardDuration`/`standardEasing`) | Reuses the file's own existing colour-transition idiom, no new motion token |

All five geometry values are render-gate discretion, not spec-locked pixels — same status `14-UI-SPEC.md` already gives the Performance tab's dial geometry ("dial-specific render-gate discretion... do not force dial geometry onto the 4px grid").

### Per-Player Volume + Dedup Resolution (required by D-21-10, rendered here)

**Operator-confirmed 2026-08-16** against two rendered alternatives (*active-player-only*, which would have dropped a QMEDIA-01 parity item; *expand-on-select rows*, which preserved the label elide budget but cost two taps to reach any background volume).

**Where the controls live:** the existing bottom-of-tab `volumeRow` (`MediaTab.qml:1282`, `Slider` bound to `mediaBackend.setVolume`) **stays wired to the active player only** — this is the parity baseline (QMEDIA-01) and needs no change. The **new** per-player capability lives inside the player-switcher dropdown itself (`playerMenu`/`menuColumn`, `MediaTab.qml:824-899`): each row gains an inline mini-slider, so a background (non-selected) player's volume is reachable without switching to it first — discoverable exactly where the player's identity already lives, not as a second detached control surface.

```
┌──────────────────────────────────┐
│  🎧  Firefox            ⌄        │   ← selector pill (existing, unchanged)
└──────────────────────────────────┘
   ┌──────────────────────────────────────┐
   │ ✓  Firefox        ▬▬▬▬▬▭▭  72%       │   ← active row: checkmark, label,
   │    Spotify         ▬▬▬▬▬▬▬ 100%      │      mini-slider, % (Label role)
   │    VLC              (no volume)      │   ← volumeSupported:false → slider
   └──────────────────────────────────────┘      omitted entirely, label-only
```

- Mini-slider width: **56px** (spacing-scale exception, see § Spacing Scale).
- Dragging the mini-slider must not select that row's player — it needs its own `MouseArea`/drag surface separate from the row's existing select-on-click `MouseArea` (`MediaTab.qml:887-895`), i.e. the slider consumes its own pointer events and does not propagate to the row click handler.
- Label elide width shrinks to make room: existing formula `menuColumn.width - spacingSm*2 - (fontLabel+4) - spacingXs` (checkmark column) needs a further `- 56 (slider) - spacingSm (gap) - ~28 (% readout)` subtracted when the row has a slider.
- Backend needs one new function, scoped by player id rather than acting on the active player: `mediaBackend.setVolumeForPlayer(playerId, fraction)` — the existing `setVolume(fraction)` (`MediaBackend.qml:355`) stays for the bottom `volumeRow`'s active-player-only path; the new function is the same clamped-write pattern applied to the matching entry in `Mpris.players.values` instead of `activePlayer`.
- Menu width: currently `width: selectorPill.width` (`MediaTab.qml:834`), which is only wide enough for a label. **Widen the dropdown when any row needs a slider** — recommend `width: Math.max(selectorPill.width, root.artSize * 1.3)` so the slider has room without the label losing all its budget; adjustable at render gate.

**Dedup interaction (the required resolution):** D-21-09's collapsing is a **display-list-only** operation. When two `Mpris.players.values` entries are recognised as the same perceptual source (title-substring or position/length proximity match), the dedup heuristic picks one canonical entry to appear in `players` (the switcher's model) and drops the other from that list — it does not merge two live volume-capable objects into one control. The surviving canonical entry's own `.volume`/`.volumeSupported` are what the row's mini-slider (and, if it's the active player, the bottom `volumeRow`) read and write, via the same `setVolumeForPlayer`/`setVolume` calls as any other row. **No fan-out, no dual-write:** a collapsed row's volume control behaves exactly like any single player's — it is only the *display* that is collapsed, never the underlying MPRIS object being controlled.

### DND Capsule Tint (D-21-27, tint strength is Claude's discretion)

Source of truth stays `NotifServer.dnd`, already read into `ClockActionsCapsule.qml`'s `sourceRoot.dndActive` (`ClockActionsCapsule.qml:621`) — this phase only needs to route that existing boolean into the capsule's own fill, plus add the blended token pair to `BarRoles.qml`.

**Recommended new `BarRoles.qml` tokens** (following the file's own established `Qt.rgba(roleColour.r/.g/.b, alpha)` blend pattern, e.g. `barSurface`/`capsuleHover`):

```qml
readonly property color dndSurface: Qt.rgba(root.accent.r, root.accent.g, root.accent.b, 0.28)
readonly property color dndSurfaceFg: Colours.onSurface   // kept on onSurface, not onAccent —
                                                            // 0.28 alpha over the existing capsule
                                                            // surface doesn't reach onPrimary's
                                                            // contrast target; glyphs/clock text
                                                            // stay legible unchanged
```

`ClockActionsCapsule.qml`'s outer fill (currently inherited from `BarCapsule.color`, the shared `!surfaced ? "transparent" : (hovered ? capsuleHover : capsule)` expression) needs one additional branch specific to this capsule: when `sourceRoot.dndActive` is true, the outer surface uses `BarRoles.dndSurface` instead of `BarRoles.capsule`/`capsuleHover`. The inner clock pill (`clockFillPill`, already filled `BarRoles.fillClock` = `Colours.secondary`) is **unchanged** — DND tints the outer capsule surface only, not the clock pill nested inside it, avoiding a colour clash between two simultaneously-tinted layers.

```
Normal:                          DND active:
┌───────────────────────────┐    ┌───────────────────────────┐
│ ⏰ 14:32   💡 🔔 ⚙ ⏻       │    │▓⏰ 14:32   💡 🔔 ⚙ ⏻      ▓│
└───────────────────────────┘    └───────────────────────────┘
  BarRoles.capsule (30%)           BarRoles.dndSurface (accent
                                    wash) — whole capsule, clock
                                    pill's own secondary fill unchanged
```

Colour transition uses the same `Behavior on color { ColorAnimation { duration: Motion.standardDuration; easing... Motion.standardEasing } }` idiom already present on `clockFillPill` (`ClockActionsCapsule.qml:163` area) — no new motion token.

**Alpha `0.28` is the recommended starting point, not a locked value** — CONTEXT.md's own escape valve applies here: "if it reads as visually loud the fix is tuning the tint strength, not rethinking the approach." Render-gate adjustable between roughly `0.20`–`0.35` without touching the mechanism.

### Frost unification value (D-21-26, resolved — not open)

The measured table in `21-CONTEXT.md` already names the target implicitly ("the dashboard and overview move to join" the notification/OSD pair) — this is not a genuinely open choice, just a value to carry through:

| Surface | Current | Target |
|---------|---------|--------|
| Dashboard (`quickshell-dashboard`) | fill `0.5` (inherited `^quickshell-.*` family default) | fill `0.38`, `ignore_alpha 0.2` |
| Overview (`quickshell-overview`) | fill `0.25` | fill `0.38`, `ignore_alpha 0.2` |
| Notifications / OSD | fill `0.38`, `ignore_alpha 0.2` | unchanged — this is the value everything else joins |
| Bar | fill `0.55`, `ignore_alpha 0.5` | **unchanged** — not named in D-21-26's "everything comes down to the threshold" set; the bar's own opaque-chrome treatment is a deliberate divergence recorded separately, not touched by this phase |

Both per-surface rows must be declared **after** the `^quickshell-.*` family regex (`windowrules.lua:368-397`) or they will not override it — worked example already at `windowrules.lua:499-526`. Verify with a screenshot after `hyprctl eval` or a full restart; `hyprctl reload` silently drops layer-rule edits (known trap, restated from CONTEXT.md).
