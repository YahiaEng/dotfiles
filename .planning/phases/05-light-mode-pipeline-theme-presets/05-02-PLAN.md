---
phase: 05-light-mode-pipeline-theme-presets
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - theme-engine/.config/theme-engine/palettes/matte-black.json
  - theme-engine/.config/theme-engine/palettes/osaka-jade.json
  - theme-engine/.config/theme-engine/palettes/ristretto.json
  - theme-engine/.config/theme-engine/palettes/everfrost.json
  - theme-engine/.config/theme-engine/palettes/kanagawa.json
  - theme-engine/.config/theme-engine/palettes/hackerman.json
  - theme-engine/.config/theme-engine/palettes/miasma.json
  - theme-engine/.config/theme-engine/palettes/ethereal.json
  - theme-engine/.config/theme-engine/palettes/vantablack.json
  - theme-engine/.config/theme-engine/palettes/catppuccin-latte.json
  - theme-engine/.config/theme-engine/palettes/rosepine-dawn.json
  - theme-engine/.config/theme-engine/palettes/gruvbox-light.json
  - theme-engine/.config/theme-engine/palettes/tokyonight-day.json
  - theme-engine/.config/theme-engine/palettes/kanagawa-lotus.json
  - theme-engine/.config/theme-engine/theme-parity
  - theme-engine/.config/theme-engine/theme-stress-test
  - hypr/.config/hypr/scripts/theme-switch.sh
  - stow.sh
  - themes/
autonomous: true
requirements: [THM-02, THM-01]

must_haves:
  truths:
    - "All 9 Omarchy-lineup dark presets exist as palette JSONs matching the 20-key schema and pass theme-parity individually (D-01)"
    - "All 5 canonical-light presets exist (catppuccin-latte, rosepine-dawn, gruvbox-light, tokyonight-day, kanagawa-lotus) as standalone palette JSONs (D-02/D-03)"
    - "theme_engine_detect_mode classifies every shipped palette correctly: exactly the 5 light presets return light, all others dark (D-06)"
    - "theme-parity with no argument covers every palette JSON dynamically plus materialyou and materialyou-light, and asserts a dark fixture (catppuccin → dark) and a light fixture (catppuccin-latte → light) (RESEARCH Pitfall 2)"
    - "theme-stress-test enumerates static presets dynamically from palettes/*.json and alternates materialyou with materialyou-light on even positions"
    - "The walker theme picker (theme-switch.sh) offers every palette plus both Material You entries"
    - "The legacy themes/ stow package is deleted and stow.sh no longer lists it (D-04)"
  artifacts:
    - path: "theme-engine/.config/theme-engine/palettes/catppuccin-latte.json"
      provides: "light fixture palette — 20-key schema, light-classified"
      contains: "on_background"
    - path: "theme-engine/.config/theme-engine/palettes/vantablack.json"
      provides: "representative Omarchy dark preset"
      contains: "background"
    - path: "theme-engine/.config/theme-engine/theme-parity"
      provides: "dynamic TARGETS enumeration + light/dark mode fixture assertions"
      contains: "materialyou-light"
  key_links:
    - from: "theme-engine/.config/theme-engine/theme-parity"
      to: "theme-engine/.config/theme-engine/palettes/"
      via: "glob enumeration of palettes/*.json builds TARGETS"
      pattern: "PALETTES_DIR.*\\*\\.json"
    - from: "hypr/.config/hypr/scripts/theme-switch.sh"
      to: "theme-engine/.config/theme-engine/palettes/"
      via: "dynamic list generation from palette basenames"
      pattern: "palettes"
---

<objective>
Expand the preset library ~2.5x through the existing pipeline (THM-02): transcribe the 9 Omarchy-lineup dark presets (D-01), author the 5 canonical-upstream light presets as standalone palette JSONs (D-02/D-03), fix the two hardcoded preset arrays that would silently exclude them from test coverage (RESEARCH Pitfall 2), extend theme-parity with light + dark mode fixtures (THM-01 SC 2), surface every preset in the walker theme picker, and delete the legacy themes/ stow package (D-04).

Purpose: presets = palette JSONs in theme-engine becomes the single source of truth, with every preset actually covered by the parity/stress gates and reachable from the picker UI.
Output: 20 palette JSONs (6 existing + 14 new), dynamic gates, updated picker, themes/ removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-CONTEXT.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02 subset)

- 9 new dark palette JSONs: `matte-black.json`, `osaka-jade.json`, `ristretto.json`, `everfrost.json`, `kanagawa.json`, `hackerman.json`, `miasma.json`, `ethereal.json`, `vantablack.json`
- 5 new light palette JSONs: `catppuccin-latte.json`, `rosepine-dawn.json`, `gruvbox-light.json`, `tokyonight-day.json`, `kanagawa-lotus.json`
- Dynamic `TARGETS` enumeration in theme-parity (replaces hardcoded array)
- Dynamic `STATIC_PRESETS` enumeration in theme-stress-test (replaces hardcoded array)
- Mode fixture assertions in theme-parity (catppuccin → dark, catppuccin-latte → light)
- Dynamically generated theme list in theme-switch.sh (includes "Material You Light" entry)
- Deletion: `themes/` stow package and its stow.sh PACKAGES entry

<tasks>

<task type="auto">
  <name>Task 1: Transcribe the 9 Omarchy-lineup dark presets as palette JSONs (D-01)</name>
  <files>theme-engine/.config/theme-engine/palettes/matte-black.json, theme-engine/.config/theme-engine/palettes/osaka-jade.json, theme-engine/.config/theme-engine/palettes/ristretto.json, theme-engine/.config/theme-engine/palettes/everfrost.json, theme-engine/.config/theme-engine/palettes/kanagawa.json, theme-engine/.config/theme-engine/palettes/hackerman.json, theme-engine/.config/theme-engine/palettes/miasma.json, theme-engine/.config/theme-engine/palettes/ethereal.json, theme-engine/.config/theme-engine/palettes/vantablack.json</files>
  <read_first>
    - theme-engine/.config/theme-engine/palettes/rosepine.json AND theme-engine/.config/theme-engine/palettes/catppuccin.json (the exact 20-key + image schema every new JSON must match — key set, nesting shape, hex format)
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md (Pattern 5 role-mapping method; Sources section: Omarchy per-theme file structure is a 16-color colors.toml, not the 20-key schema)
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-UI-SPEC.md (Color section 1 — the canonical 20-key list)
  </read_first>
  <action>
    For each of the 9 themes, fetch Omarchy's upstream theme sources from raw.githubusercontent.com/basecamp/omarchy/master/themes/{name}/ (start with colors.toml; consult that theme's hyprland.conf / waybar.css / walker.css in the same directory when colors.toml alone does not disambiguate accents) and transcribe into this repo's 20-key matugen custom-keyword schema (D-01: transcribed from Omarchy's published themes, never invented). Naming note: the palette for the locally-staged `everfrost` wallpaper folder is transcribed from Omarchy's `everforest` theme but the JSON is named `everfrost.json` — D-01 locks the lineup name and D-09 requires the palette name to match the wallpaper folder exactly (resolves RESEARCH Open Question 1 in favor of the locked lineup name).

    Role-mapping convention (follow rosepine.json's structural shape; document any per-theme judgment call in the SUMMARY):
    - `background` and `surface` ← theme background; `on_background`, `on_surface`, `on_primary_container` ← foreground
    - `surface_variant`, `primary_container`, `secondary_container` ← selection/highlight background (or the theme's darker panel shade)
    - `primary` ← the theme's dominant accent; `on_primary`, `on_secondary`, `on_tertiary`, `on_error` ← background
    - `secondary` and `tertiary` ← two further accents; `on_secondary_container` ← secondary; `tertiary_container` ← a muted/darker tertiary companion
    - `on_surface_variant` ← muted/subtle foreground; `outline` ← border/highlight color; `error` ← the theme red
    - `image` ← empty string, exactly like every existing palette
    Every value must be a 6-digit hex with leading hash; no extra keys, no omitted keys. Do not add a mode override key to these (all 9 are dark; auto-detection must classify them — vantablack's near-black background is the trivial case).
  </action>
  <verify>
    <automated>bash -c 'cd theme-engine/.config/theme-engine && ref=$(jq -S ".colors | keys" palettes/rosepine.json) && for n in matte-black osaka-jade ristretto everfrost kanagawa hackerman miasma ethereal vantablack; do [[ "$(jq -S ".colors | keys" "palettes/$n.json")" == "$ref" ]] || { echo "key-set mismatch: $n"; exit 1; }; ./theme-parity "$n" >/dev/null || { echo "parity fail: $n"; exit 1; }; done && echo all-9-ok'</automated>
  </verify>
  <acceptance_criteria>
    - All 9 JSON files exist under palettes/ and `jq -S '.colors | keys'` for each is byte-identical to rosepine.json's key set
    - `theme-parity <name>` exits 0 for each of the 9 (structure + name-set + semantic layers on the extended 12-file contract)
    - Every color value matches a 6-hex-digit hash-prefixed pattern under jq inspection
    - SUMMARY documents which upstream Omarchy file(s) each palette was transcribed from
  </acceptance_criteria>
  <done>All 9 Omarchy dark presets render cleanly through the existing pipeline as drop-in palette JSONs.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 5 canonical light presets and validate mode classification across all palettes (D-02/D-03/D-06)</name>
  <files>theme-engine/.config/theme-engine/palettes/catppuccin-latte.json, theme-engine/.config/theme-engine/palettes/rosepine-dawn.json, theme-engine/.config/theme-engine/palettes/gruvbox-light.json, theme-engine/.config/theme-engine/palettes/tokyonight-day.json, theme-engine/.config/theme-engine/palettes/kanagawa-lotus.json</files>
  <read_first>
    - theme-engine/.config/theme-engine/palettes/catppuccin.json, rosepine.json, gruvbox.json, tokyonight.json, and the kanagawa.json created in Task 1 (each dark sibling's key→upstream-color-name role mapping is the template — Pattern 5)
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md (Pattern 5 worked rosepine-dawn example with verified hex values; Assumptions A1/A3 — hex values to re-verify against upstream; Pitfall 4 — nord and dracula stay dark-only)
    - theme-engine/.config/theme-engine/lib/mode.sh (the classification function this task validates against)
  </read_first>
  <action>
    1. For each of the 5 families with a free canonical upstream light variant (D-02 — the definitive list pinned by research: Catppuccin Latte, Rosé Pine Dawn, Gruvbox Light, TokyoNight Day, Kanagawa Lotus), create a standalone palette JSON (D-03: no variant-flag plumbing — applied via its own name, e.g. `theme-apply catppuccin-latte`). Method (Pattern 5, locked by UI-SPEC): copy the dark sibling's exact key→upstream-color-name role mapping and substitute the light variant's own upstream hex values — never re-derive role assignments. Upstream sources: catppuccin.com/palette (Latte flavor), rosepinetheme.com/palette (Dawn — RESEARCH already carries a near-complete worked example; fetch the missing highlight hex values per Assumption A1), gruvbox.nvim lua source (light palette), tokyonight.nvim extras tokyonight_day.lua, kanagawa.nvim colors.lua (lotus). Spot-check each hex against the fetched source (Assumptions A1/A3).
    2. Nord and Dracula get NO light sibling — Nord has no official light variant and Dracula's is paid-exclusive (D-02's own criterion; RESEARCH Pitfall 4). Do not create nord-light or any Alucard transcription.
    3. Validate mode classification across ALL palettes (UI-SPEC mode-detection contract step 4): loop every palettes/*.json through theme_engine_detect_mode; expected output — exactly these 5 return light, all 15 others return dark. If any palette misclassifies, add the top-level `"mode"` override key to that one JSON (D-06 escape hatch) rather than moving the global 0.5 threshold; record any override added in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd theme-engine/.config/theme-engine && export PALETTES_DIR=$PWD/palettes && source lib/mode.sh && fails=0 && for f in palettes/*.json; do n=$(basename "$f" .json); m=$(theme_engine_detect_mode "$n"); case "$n" in catppuccin-latte|rosepine-dawn|gruvbox-light|tokyonight-day|kanagawa-lotus) want=light;; *) want=dark;; esac; [[ "$m" == "$want" ]] || { echo "misclassified: $n=$m want=$want"; fails=1; }; done && [[ $fails -eq 0 ]] && ref=$(jq -S ".colors | keys" palettes/rosepine.json) && for n in catppuccin-latte rosepine-dawn gruvbox-light tokyonight-day kanagawa-lotus; do [[ "$(jq -S ".colors | keys" "palettes/$n.json")" == "$ref" ]] && ./theme-parity "$n" >/dev/null || { echo "fail: $n"; exit 1; }; done && echo all-light-ok'</automated>
  </verify>
  <acceptance_criteria>
    - The 5 light JSONs exist, match the 20-key schema exactly, and `theme-parity <name>` exits 0 for each
    - The classification loop over all 20 palettes prints zero misclassifications: the 5 light names → light, the other 15 → dark
    - No nord-light.json, dracula-light.json, or alucard.json exists in palettes/
    - Any `"mode"` override key added is documented in the SUMMARY with the reason
  </acceptance_criteria>
  <done>Light presets ship as first-class standalone palettes and the mode detector proves itself against the full shipped set.</done>
</task>

<task type="auto">
  <name>Task 3: Dynamic gate coverage, picker surface, and legacy themes/ deletion (Pitfall 2, D-04)</name>
  <files>theme-engine/.config/theme-engine/theme-parity, theme-engine/.config/theme-engine/theme-stress-test, hypr/.config/hypr/scripts/theme-switch.sh, stow.sh, themes/</files>
  <read_first>
    - theme-engine/.config/theme-engine/theme-parity (file being modified — hardcoded TARGETS at line 77, single-target allowlist at lines 70-75, render loop and check() accumulator)
    - theme-engine/.config/theme-engine/theme-stress-test (file being modified — STATIC_PRESETS at line 83, sequence builder at lines 322-331, abort-on-first-failure philosophy comment)
    - hypr/.config/hypr/scripts/theme-switch.sh (file being modified — hardcoded THEME_LIST + case mapping)
    - stow.sh (PACKAGES array containing the themes entry)
    - theme-engine/.config/theme-engine/theme-apply lines 30-36 (the dynamic palettes glob idiom to replicate)
  </read_first>
  <action>
    1. theme-parity: replace the hardcoded default TARGETS array with dynamic enumeration — loop `"$PALETTES_DIR"/*.json`, append each basename, then append the literals materialyou and materialyou-light (RESEARCH Pitfall 2 permanent fix). Extend the single-target allowlist condition to also accept materialyou-light (mirror theme-apply's two-literal check). Add a mode-fixture section after the render loop: for every successfully-rendered target assert the rendered tmp tree contains a `mode` file whose content is light or dark (report via the existing check() accumulator, never abort early — this file's report-only philosophy); then two fixture assertions pinning THM-01 SC 2: target catppuccin rendered mode equals dark, target catppuccin-latte rendered mode equals light.
    2. theme-stress-test: replace the hardcoded STATIC_PRESETS array with the same dynamic palettes-glob enumeration. In the sequence builder, make even positions alternate between materialyou and materialyou-light so the light dynamic path gets real-switch coverage. Do not change the abort-on-first-failure philosophy or any check primitive.
    3. theme-switch.sh: replace the hardcoded 7-entry THEME_LIST and case-mapping with dynamic generation — one entry per palettes/*.json basename plus two Material You entries (dark and light). Display names may be prettified (hyphen→space, title case, optional emoji); the selected entry must map back deterministically to the exact palette basename or materialyou/materialyou-light literal, which theme-apply re-validates anyway (defense in depth, Security Domain V5). Keep the thin-caller contract: this script only picks a name and execs theme-apply.
    4. Legacy deletion (D-04; research verified zero references repo-wide): re-verify with a fresh grep that no file outside the legacy package references its config path, run `stow -D themes --target="$HOME"` best-effort to remove live symlinks, remove the themes entry from stow.sh's PACKAGES array, then `git rm -r themes/`. If `~/.config/themes` remains as a dangling symlink afterwards, remove it.
  </action>
  <verify>
    <automated>bash -n theme-engine/.config/theme-engine/theme-parity && bash -n theme-engine/.config/theme-engine/theme-stress-test && bash -n hypr/.config/hypr/scripts/theme-switch.sh && test ! -d themes && [[ -z "$(sed -n '/^PACKAGES=(/,/^)/p' stow.sh | grep -x '\s*themes')" ]] && theme-engine/.config/theme-engine/theme-parity 2>&1 | tee /tmp/parity-run.log | tail -1 && grep -q "catppuccin-latte" /tmp/parity-run.log && grep -q "materialyou-light" /tmp/parity-run.log && tail -1 /tmp/parity-run.log | grep -q " 0 failed"</automated>
  </verify>
  <acceptance_criteria>
    - A full no-argument theme-parity run exits 0, and its output names catppuccin-latte, vantablack, and materialyou-light among rendered targets (proof the dynamic enumeration covers new presets)
    - theme-parity output contains a passing fixture line for catppuccin mode=dark and catppuccin-latte mode=light
    - theme-stress-test passes bash -n and contains the dynamic palettes-glob enumeration plus a materialyou-light alternation in the sequence builder (verified by grep; a live 10-switch run is deferred to phase verification since it mutates the desktop)
    - theme-switch.sh passes bash -n, contains no hardcoded 7-name case ladder, and derives entries from the palettes glob
    - themes/ directory no longer exists; the PACKAGES array block in stow.sh has no themes line; `git status` shows the deletion staged/committed
  </acceptance_criteria>
  <done>Every preset — present and future — is automatically covered by parity and stress gates and reachable from the theme picker; the legacy static-theme package is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg → filesystem path | theme-parity optional target argument interpolated into palette path |
| upstream web sources → palette JSONs | transcribed hex values flow into every rendered config |
| walker dmenu selection → theme-apply arg | picker selection becomes a process argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | theme-parity target-arg validation | medium | mitigate | Extend the existing validate-against-real-filenames check with the materialyou-light literal only (Task 3), mirroring theme-apply |
| T-05-05 | Tampering | transcribed palette values → rendered configs | low | mitigate | theme-parity Layer 3 semantic gate validates every value is a well-formed color; jq key-set check pins the schema (Tasks 1-2) |
| T-05-06 | Spoofing | picker display-name → palette-name mapping | low | mitigate | Mapping derives from real palette basenames; theme-apply re-validates the final name against palettes/*.json regardless (Task 3) |
</threat_model>

<verification>
- Full theme-parity run: 0 failed, 22 targets (20 palettes + 2 Material You entries)
- Mode classification loop: 5 light / 15 dark, zero misclassifications
- themes/ deleted; repo grep for the legacy config path returns nothing; stow.sh PACKAGES clean
- theme-switch.sh offers all 22 entries
</verification>

<success_criteria>
- ROADMAP Phase 5 SC 2 (parity passes with light + dark fixtures) and SC 3 (expanded preset set incl. light, via palette JSONs) are both satisfied
- No invented palettes: 9 dark transcribed from Omarchy, 5 light from canonical upstream sources, nord/dracula stay dark-only
</success_criteria>

<output>
Create `.planning/phases/05-light-mode-pipeline-theme-presets/05-02-SUMMARY.md` when done.
</output>
