---
phase: 05-light-mode-pipeline-theme-presets
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - matugen/.config/matugen/templates/fzf-colors.conf
  - matugen/.config/matugen/config.toml
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/contract.sh
  - theme-engine/.config/theme-engine/theme-parity
  - hypr/.config/hypr/scripts/wallpaper-picker.sh
autonomous: false
requirements: [THM-04, THM-03]

must_haves:
  truths:
    - "The picker preview pane renders wallpapers pixel-perfectly via the kitty graphics protocol, with filename/resolution metadata and an active-wallpaper marker (D-13/D-14)"
    - "The live awww desktop preview on navigate still works (D-13 keeper feature)"
    - "fzf colors are sourced from ~/.local/state/theme/fzf-colors.conf rendered by the pipeline; catppuccin hex literals survive only as ${VAR:-fallback} defaults (D-15)"
    - "With a static theme active the picker opens restricted to that theme's folder with the theme named in the header, and Ctrl-A temporarily browses the full collection (D-16)"
    - "Empty/missing theme folder falls open to all wallpapers with the fall-open header; Material You always browses everything (D-12)"
    - "Selecting a wallpaper under materialyou-light re-runs theme-apply materialyou-light, not materialyou (D-05 active-variant integration)"
    - "Selecting a wallpaper from the active static theme's folder records it as that theme's last-used wallpaper (D-11)"
  artifacts:
    - path: "matugen/.config/matugen/templates/fzf-colors.conf"
      provides: "FZF_COLOR_* render template mapped per UI-SPEC color-slot contract"
      contains: "FZF_COLOR_PROMPT"
    - path: "hypr/.config/hypr/scripts/wallpaper-picker.sh"
      provides: "redesigned picker: icat preview, restriction, theming, marker"
      contains: "kitten icat"
    - path: "theme-engine/.config/theme-engine/contract.json"
      provides: "fzf-colors.conf contract entry (env-kv format)"
      contains: "fzf-colors.conf"
  key_links:
    - from: "hypr/.config/hypr/scripts/wallpaper-picker.sh"
      to: "~/.local/state/theme/fzf-colors.conf"
      via: "sourced at startup with graceful fallback defaults"
      pattern: "fzf-colors.conf"
    - from: "hypr/.config/hypr/scripts/wallpaper-picker.sh"
      to: "theme-engine/.config/theme-engine/theme-apply"
      via: "re-runs the active dynamic variant on selection (materialyou or materialyou-light)"
      pattern: "theme-apply"
    - from: "matugen/.config/matugen/config.toml"
      to: "matugen/.config/matugen/templates/fzf-colors.conf"
      via: "templates.fzf input_path/output_path pair"
      pattern: "fzf-colors"
---

<objective>
Redesign the wallpaper picker to Omarchy-level polish (THM-04) on the kept fzf-in-floating-kitty stack: pixel-perfect previews via the kitty graphics protocol with metadata and an active marker (D-13/D-14), pipeline-sourced fzf theming through a new render target (D-15), and the THM-03 restriction UI — static themes open on their own set with Ctrl-A escape, Material You browses everything, empty sets fall open visibly (D-16/D-12) — while integrating the active dynamic variant re-apply (D-05) and last-used recording (D-11).

Purpose: ROADMAP Phase 5 SC 4 and SC 5 — the picker restricts by theme and looks the part, themed by the same pipeline it serves, including light mode.
Output: fzf color fragment render target + fully redesigned wallpaper-picker.sh, human-verified visually.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-CONTEXT.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-PATTERNS.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 04 subset)

- `matugen/.config/matugen/templates/fzf-colors.conf` — new matugen template
- `~/.local/state/theme/fzf-colors.conf` — new rendered contract target (shell-sourceable FZF_COLOR_* vars)
- `[templates.fzf]` — new matugen config.toml entry
- `env-kv` — new format tag in lib/contract.sh extractors + contract.json + theme-parity enforce list
- `FZF_COLOR_FG`, `FZF_COLOR_BG`, `FZF_COLOR_FG_PLUS`, `FZF_COLOR_BG_PLUS`, `FZF_COLOR_HL`, `FZF_COLOR_HL_PLUS`, `FZF_COLOR_INFO`, `FZF_COLOR_PROMPT`, `FZF_COLOR_POINTER`, `FZF_COLOR_MARKER`, `FZF_COLOR_SPINNER`, `FZF_COLOR_HEADER`, `FZF_COLOR_BORDER` — sourced variables
- Redesigned `wallpaper-picker.sh` — icat preview pane, theme restriction, Ctrl-A browse-all keybind, active-wallpaper marker, relpath-based enumeration

<tasks>

<task type="auto">
  <name>Task 1: fzf color fragment render target (D-15)</name>
  <files>matugen/.config/matugen/templates/fzf-colors.conf, matugen/.config/matugen/config.toml, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/contract.sh, theme-engine/.config/theme-engine/theme-parity</files>
  <read_first>
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-UI-SPEC.md (Color section 3 — the LOCKED slot-to-palette-role mapping this template must implement; UI-SPEC supersedes the RESEARCH example where they differ, e.g. spinner belongs to the tertiary accent group)
    - matugen/.config/matugen/config.toml (file being modified — existing templates entry shape: input_path/output_path pairs)
    - matugen/.config/matugen/templates/walker-style.css (an existing template using the colors.*.default.hex keyword syntax to copy)
    - theme-engine/.config/theme-engine/lib/contract.sh (extractor case statements — env-kv slots in beside the ini-kv branch added by Plan 05-01)
    - theme-engine/.config/theme-engine/theme-parity (Layer 3 enforce_emptiness format case list, file modified by Plan 05-02 — re-read current state before editing)
    - theme-engine/.config/theme-engine/contract.json (current entries after Plan 05-01)
  </read_first>
  <action>
    1. Create the matugen template rendering shell-sourceable assignments, one per line, double-quoted values, using matugen's colors.NAME.default.hex keyword syntax. Slot mapping per UI-SPEC Color section 3 (locked): FG ← on_background; BG ← literal -1 (terminal transparency kept); BG_PLUS ← surface_variant; HEADER ← on_surface_variant; HL, HL_PLUS, POINTER, MARKER, SPINNER ← tertiary (the 10% accent group, reserved for match highlights/pointer/marker/spinner only); PROMPT ← primary; INFO ← secondary; BORDER ← outline; FG_PLUS ← on_primary_container (selected-row text readable on surface_variant).
    2. Add a `[templates.fzf]` block to matugen config.toml pointing input_path at the new template and output_path at the state-dir fzf-colors.conf, matching the existing entries' shape (no post_hook — reload ownership stays with reload.sh).
    3. In lib/contract.sh: add an `env-kv` format branch to contract_extract_names and contract_extract_values — parse NAME=VALUE assignment lines, strip surrounding double quotes from values, skip blanks/comments; names sorted, values as name TAB value.
    4. In contract.json: append the fzf-colors.conf entry with format env-kv.
    5. In theme-parity: add env-kv to the Layer 3 enforce_emptiness=1 format list (every fzf slot is definitionally a color assignment; the BG slot's -1 literal is non-empty and does not match the color regex, so it passes untouched).
    6. Re-apply the current theme so the fragment renders into the live state dir.
  </action>
  <verify>
    <automated>bash -n theme-engine/.config/theme-engine/lib/contract.sh && jq -e '.files | map(.name) | index("fzf-colors.conf") != null' theme-engine/.config/theme-engine/contract.json && ~/.config/theme-engine/theme-apply "$(cat ~/.local/state/theme/current-theme)" && test -f ~/.local/state/theme/fzf-colors.conf && bash -c 'source ~/.local/state/theme/fzf-colors.conf && [[ "$FZF_COLOR_PROMPT" =~ ^#[0-9a-fA-F]{6}$ && "$FZF_COLOR_BORDER" =~ ^#[0-9a-fA-F]{6}$ && "$FZF_COLOR_BG" == "-1" ]]' && theme-engine/.config/theme-engine/theme-parity 2>&1 | tail -1 | grep -q " 0 failed"</automated>
  </verify>
  <acceptance_criteria>
    - fzf-colors.conf renders in the state dir on theme-apply; sourcing it yields hex values for all 12 color slots and -1 for BG
    - contract.json lists fzf-colors.conf with format env-kv; a full theme-parity run exits 0 with the 13-file contract across all targets
    - The template contains zero literal hex values — every color slot is a matugen keyword (BG's -1 is the only literal)
    - theme-parity Layer 3 output includes value checks for fzf-colors.conf (env-kv in the enforce list)
  </acceptance_criteria>
  <done>The picker's color source is a first-class pipeline render target validated by the parity gate for every theme, light and dark.</done>
</task>

<task type="auto">
  <name>Task 2: Pixel-perfect preview pane + pipeline-sourced fzf theming (D-13/D-14/D-15)</name>
  <files>hypr/.config/hypr/scripts/wallpaper-picker.sh</files>
  <read_first>
    - hypr/.config/hypr/scripts/wallpaper-picker.sh (file being modified — PREVIEW_SCRIPT heredoc at lines 44-67, LIVE_SCRIPT at lines 71-83, fzf invocation with hardcoded --color lines at lines 86-104)
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md (Pattern 4 — the exact kitten icat invocation from fzf's upstream fzf-preview.sh including the trailing sed pair and the --clear stale-image fix; Code Examples — the source-with-fallback consumer snippet)
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-UI-SPEC.md (Spacing Scale — the four fzf layout params to keep verbatim; Typography — ANSI bold is the only emphasis device)
  </read_first>
  <action>
    1. Replace the PREVIEW_SCRIPT chafa block-art invocation with the kitty-graphics pattern from fzf's own upstream fzf-preview.sh (RESEARCH Pattern 4, verbatim technique): when KITTY_WINDOW_ID is set and kitten is on PATH, run kitten icat with --clear --transfer-mode=memory --unicode-placeholder --stdin=no and --place sized from FZF_PREVIEW_COLUMNS by FZF_PREVIEW_LINES minus two reserved metadata rows, anchored at 0x0, piping through the two-sed tail fix (delete last line, append ANSI reset) that prevents fzf's line-count confusion and stale-image-on-scroll artifacts. First fallback when kitten is unavailable: chafa with the kitty output format flag (same protocol, pixel-perfect). Last resort: the current block-symbols chafa call. Keep the metadata line below the image: ANSI-bold filename, dimensions via identify, human size via du — the existing single emphasis convention (UI-SPEC typography).
    2. Replace the three hardcoded --color argument lines: source the state-dir fzf-colors.conf best-effort at script start (2>/dev/null || true), then build the --color strings from FZF_COLOR_* with the current catppuccin-mocha hex literals as ${VAR:-fallback} inline defaults (D-15 + UI-SPEC: literals may survive ONLY as fallback defaults for fresh installs; gutter stays -1).
    3. Keep unchanged: the awww live-preview focus binding (D-13 keeper), ctrl-r random-transition binding, cancel/restore flow, and the four layout params verbatim — border rounded, margin 1,2, padding 1, preview-window right,60%,border-left (UI-SPEC spacing contract: do not introduce new spacing values).
  </action>
  <verify>
    <automated>bash -n hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "kitten icat" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "FZF_COLOR_PROMPT" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "fzf-colors.conf" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "right,60%,border-left" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "focus:execute-silent" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -cE '\-\-color=' hypr/.config/hypr/scripts/wallpaper-picker.sh | grep -qv '^0$' && bash -c 'grep -E "^\s*--color=" -A0 hypr/.config/hypr/scripts/wallpaper-picker.sh | grep -vq FZF_COLOR_ && exit 1 || exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - PREVIEW_SCRIPT contains the kitten icat invocation with --clear and --unicode-placeholder plus the sed tail-fix pipeline, a chafa kitty-format fallback, and the metadata line with ANSI-bold filename
    - Every --color argument line in the fzf invocation references FZF_COLOR_ variables; catppuccin hexes appear only inside ${VAR:-...} default expansions
    - The awww focus binding, layout params (border rounded / margin 1,2 / padding 1 / preview-window right,60%,border-left), and cancel-restore flow are byte-preserved in behavior
    - Script passes bash -n
  </acceptance_criteria>
  <done>The preview pane is pixel-perfect kitty-graphics rendering and the picker's chrome is themed by the live pipeline with graceful fresh-install fallbacks.</done>
</task>

<task type="auto">
  <name>Task 3: Theme restriction, fall-open, Ctrl-A, active marker, and selection flow (D-16/D-12/D-14/D-11/D-05)</name>
  <files>hypr/.config/hypr/scripts/wallpaper-picker.sh</files>
  <read_first>
    - hypr/.config/hypr/scripts/wallpaper-picker.sh (current enumeration at lines 29-32, selection/materialyou re-apply block at lines 132-145 — re-read after Task 2's edits)
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-UI-SPEC.md (Copywriting Contract — the three exact header strings incl. restricted/fall-open variants and the ThemeName title-casing rule; Color section 3 marker/pointer glyph reservations)
    - theme-engine/.config/theme-engine/lib/wallpaper.sh (last-wallpaper state convention from Plan 05-03 — bare filename, atomic temp+mv, one file per theme)
  </read_first>
  <action>
    1. Enumeration becomes relpath-based (paths relative to the Wallpapers root, so entries from subfolders work in every script): restricted list = find inside the active theme's folder, maxdepth 1, image extensions, printing entries prefixed with the theme folder name; full list = find from the root, maxdepth 2, image extensions, excluding current.jpg, printing paths relative to the root (this includes loose root files and non-preset dirs like anime — the shared pool, D-12). Keep the enumerate-real-files-only discipline: entries come exclusively from find output, never free text (Security Domain V5 pattern already in this script).
    2. Mode selection at startup from the current-theme state file: static preset + non-empty theme folder → restricted list with the UI-SPEC restricted header (theme name title-cased per the UI-SPEC rule, with special-cases like rosepine → Rosé Pine, tokyonight → Tokyo Night) and a ctrl-a binding that reloads the full list and swaps to the full-browse header (fzf reload + change-header actions — D-16's temporary browse-all escape). Static + empty/missing folder → full list with the UI-SPEC fall-open header naming the theme and explaining the fallback (D-12: visible, never silent). materialyou/materialyou-light → full list with the standard header.
    3. Active-wallpaper marker (D-14): resolve current.jpg's target, compute its root-relative path, and append a marker suffix (the reserved marker glyph) to the matching list entry; strip the suffix in the preview script, the awww live-preview binding, and the final selection handling before any path use. Additionally show an active indicator on the preview metadata line when the previewed file is the active one.
    4. Selection flow: after stripping the marker, join the relpath to the Wallpapers root, require the result to be an existing regular file (defense in depth), then ln -sfr to current.jpg and fire the final awww transition as today. Post-selection branching: if the current theme is materialyou OR materialyou-light, re-run theme-apply with that exact active variant name (D-05 — never hardcode the dark variant); else notify as today AND, when the selected file lives inside the active theme's own folder, record its bare filename to that theme's last-wallpaper state file using the same atomic temp+mv idiom as lib/wallpaper.sh (D-11 — the picker is the second writer of last-used state; selections outside the theme folder via Ctrl-A are not recorded).
    5. Keep the cancel path (restore previous wallpaper) and the truly-empty-library message exactly as the UI-SPEC copywriting contract specifies (both unchanged from today).
  </action>
  <verify>
    <automated>bash -n hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "maxdepth 2" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "ctrl-a:" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "none yet — showing full library" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "materialyou-light" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "last-wallpaper" hypr/.config/hypr/scripts/wallpaper-picker.sh && grep -q "Ctrl-A browse all" hypr/.config/hypr/scripts/wallpaper-picker.sh</automated>
  </verify>
  <acceptance_criteria>
    - Script contains all three UI-SPEC header variants (standard, restricted with "Ctrl-A browse all", fall-open with "none yet — showing full library")
    - ctrl-a binding uses fzf reload+change-header to switch from restricted to full browsing
    - The materialyou-light literal appears in both the startup mode selection and the post-selection re-apply branch; the re-apply call passes the active variant name, not a hardcoded one
    - Selection handling validates the joined path is an existing regular file before ln -sfr; last-used recording writes bare filenames atomically and only for in-folder selections
    - bash -n passes
  </acceptance_criteria>
  <done>The picker enforces D-16 restriction with D-12 fall-open semantics, marks the active wallpaper, and integrates correctly with both dynamic variants and per-theme last-used memory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — visual verification of the redesigned picker</name>
  <action>Pause and present the walkthrough below to the user; all automation (Tasks 1-3) is complete before this checkpoint fires. Do not proceed to the SUMMARY until the user responds.</action>
  <what-built>Redesigned wallpaper picker: pixel-perfect kitty-graphics previews with metadata + active marker, pipeline-themed fzf colors (light-mode aware), per-theme restriction with Ctrl-A browse-all and visible fall-open, live awww preview retained, materialyou-light integration.</what-built>
  <how-to-verify>
    1. Run: ~/.config/theme-engine/theme-apply catppuccin — desktop re-themes AND the wallpaper switches to one from the catppuccin set (Plan 05-03 auto-set).
    2. Press Super+W (or run ~/.config/hypr/scripts/wallpaper-switch.sh). Expect: floating kitty picker, header reading "Catppuccin Wallpapers │ Ctrl-A browse all │ ...", only catppuccin-folder wallpapers listed, the active one marked, pixel-perfect (not block-art) preview with filename/resolution/size line.
    3. Navigate with arrows — the desktop behind live-previews each highlighted wallpaper.
    4. Press Ctrl-A — the list expands to the full library and the header changes to the standard browse header.
    5. Press Esc — the picker closes and the pre-picker wallpaper is restored.
    6. Run: ~/.config/theme-engine/theme-apply catppuccin-latte — desktop flips to light (GTK included). Re-open the picker: its colors now match the light palette (no dark leftover chrome). Header names Catppuccin Latte; folder is empty so it must show the fall-open header "(none yet — showing full library)" with everything listed.
    7. Run: ~/.config/theme-engine/theme-apply materialyou — re-open the picker: full library, standard header. Select any wallpaper → wallpaper sets AND the palette regenerates from it.
    8. Restore your preferred theme (e.g. ~/.config/theme-engine/theme-apply dracula).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, what you saw vs expected)</resume-signal>
  <verify>
    <automated>MISSING — human checkpoint: visual verification cannot be automated; the automated gates for this plan live in Tasks 1-3</automated>
  </verify>
  <done>User has approved the 8-step walkthrough or reported issues that were then fixed and re-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fzf selection → filesystem path | selected list entry joined to the Wallpapers root |
| state-dir shell fragment → picker process | fzf-colors.conf is sourced (executes as shell) |
| picker → theme-apply | current-theme state content becomes a process argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | fzf selection path join | medium | mitigate | Entries derive exclusively from find enumeration; marker stripped; joined path must be an existing regular file under the Wallpapers root before use (Task 3) |
| T-05-10 | Elevation of Privilege | sourcing fzf-colors.conf | medium | mitigate | Fragment is engine-rendered into the 700-perm state dir (commit.sh chmod) with quoted assignments; sourced best-effort with existence guard; no other writer exists (Task 1/2) |
| T-05-11 | Tampering | current-theme state → theme-apply arg | low | mitigate | theme-apply re-validates every name against real palette files + the two materialyou literals (existing allowlist, unchanged) |
</threat_model>

<verification>
- theme-parity full run exits 0 with the 13-file contract (fzf fragment validated per theme)
- All grep-gated structural assertions in Tasks 2-3 pass; bash -n clean
- Human checkpoint approves the 8-step visual walkthrough covering restriction, fall-open, Ctrl-A, marker, live preview, light-mode theming, and both dynamic variants
</verification>

<success_criteria>
- ROADMAP Phase 5 SC 4 (restriction with Material You unrestricted) and SC 5 (Omarchy-level picker polish) verified by a human on the live desktop
- No hardcoded catppuccin color remains as a sole source anywhere in the picker (D-15)
</success_criteria>

<output>
Create `.planning/phases/05-light-mode-pipeline-theme-presets/05-04-SUMMARY.md` when done.
</output>
