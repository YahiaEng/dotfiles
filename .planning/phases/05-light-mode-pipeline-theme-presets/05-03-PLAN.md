---
phase: 05-light-mode-pipeline-theme-presets
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - wallpapers/Pictures/Wallpapers/
  - theme-engine/.config/theme-engine/lib/wallpaper.sh
  - theme-engine/.config/theme-engine/theme-apply
autonomous: true
requirements: [THM-03]

must_haves:
  truths:
    - "Every wallpaper folder name matches its palette JSON name exactly: rose-pine renamed to rosepine, tokyo-night renamed to tokyonight (D-09)"
    - "A dracula/ wallpaper folder exists (empty, .gitkeep) so the strict 1:1 convention holds for every preset (D-09, resolves RESEARCH Open Question 2)"
    - "Each of the 5 light presets has its own wallpaper folder (D-10)"
    - "theme-apply of a static preset auto-sets the wallpaper: last-used for that theme if recorded, else first in the folder; empty/missing folder keeps the current wallpaper (D-11/D-12)"
    - "Material You entries never auto-set a wallpaper — wallpaper drives palette in dynamic mode (D-11)"
    - "Wallpaper auto-set never hangs or fails theme-apply in a headless context (container gate safety)"
  artifacts:
    - path: "theme-engine/.config/theme-engine/lib/wallpaper.sh"
      provides: "theme_engine_wallpaper_autoset — per-theme wallpaper selection + last-used state"
      contains: "theme_engine_wallpaper_autoset"
    - path: "wallpapers/Pictures/Wallpapers/rosepine"
      provides: "renamed per-theme wallpaper set matching palette name"
    - path: "wallpapers/Pictures/Wallpapers/catppuccin-latte"
      provides: "light-variant wallpaper folder (D-10)"
  key_links:
    - from: "theme-engine/.config/theme-engine/theme-apply"
      to: "theme-engine/.config/theme-engine/lib/wallpaper.sh"
      via: "theme_engine_wallpaper_autoset called after commit, before reload"
      pattern: "theme_engine_wallpaper_autoset"
    - from: "theme-engine/.config/theme-engine/lib/wallpaper.sh"
      to: "~/.local/state/theme/last-wallpaper/<preset>"
      via: "atomic temp+mv per-theme last-used state files"
      pattern: "last-wallpaper"
---

<objective>
Organize wallpapers into strict per-theme sets matching palette names 1:1 (D-09), give light variants their own folders (D-10), and make `theme-apply <static-preset>` land a fully coherent desktop in one action by auto-setting that theme's wallpaper — last-used first, first-in-folder fallback, keep-current when empty (D-11/D-12). Material You keeps its existing direction: wallpaper drives palette (THM-03).

Purpose: the Omarchy-style one-action coherence named in CONTEXT specifics — a theme switch lands wallpaper + colors together — and the folder convention the restricted picker (Plan 05-04) is built on.
Output: renamed/complete folder tree, lib/wallpaper.sh, theme-apply hook, per-theme last-used state convention.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-CONTEXT.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md
@.planning/phases/05-light-mode-pipeline-theme-presets/05-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 03 subset)

- `theme-engine/.config/theme-engine/lib/wallpaper.sh` — new lib file
- `theme_engine_wallpaper_autoset()` — bash function: static preset → wallpaper set + state write-back
- `~/.local/state/theme/last-wallpaper/` — new state subdirectory, one flat file per theme containing a bare filename (RESEARCH Open Question 4 resolution: one-file-per-theme, matching current-theme conventions)
- Renamed wallpaper folders: `rosepine/` (was rose-pine), `tokyonight/` (was tokyo-night)
- New wallpaper folders: `dracula/`, `catppuccin-latte/`, `rosepine-dawn/`, `gruvbox-light/`, `tokyonight-day/`, `kanagawa-lotus/` (each seeded with .gitkeep)

<tasks>

<task type="auto">
  <name>Task 1: Rename drifting wallpaper folders and complete the 1:1 folder set (D-09/D-10)</name>
  <files>wallpapers/Pictures/Wallpapers/</files>
  <read_first>
    - .planning/phases/05-light-mode-pipeline-theme-presets/05-RESEARCH.md (Runtime State Inventory — current-theme already stores no-dash names, only folders drift; current.jpg points to a loose root file so renames are transparent to it; Open Questions 1-2)
    - stow.sh (the stow --restow loop that must re-link ~/Pictures/Wallpapers entries after the renames)
  </read_first>
  <action>
    1. In `wallpapers/Pictures/Wallpapers/`: `git mv rose-pine rosepine` and `git mv tokyo-night tokyonight` — the one-time rename locked by D-09 (strict convention: folder name equals palette JSON name; no mapping/manifest file). `everfrost/` keeps its name — the palette from Plan 05-02 is named everfrost.json to match it (D-01/D-09).
    2. Create `dracula/` containing only a `.gitkeep` — D-09's strict convention says every preset has a folder; empty means the picker falls open to the shared pool (D-12), never a dead end (resolves RESEARCH Open Question 2 in favor of the convention).
    3. Create the five light-variant folders per D-10 (own folders, no family sharing or fallback resolution): `catppuccin-latte/`, `rosepine-dawn/`, `gruvbox-light/`, `tokyonight-day/`, `kanagawa-lotus/`, each with a `.gitkeep`. The user populates them with light wallpapers later; empty folders fall open per D-12.
    4. Re-run `stow --restow wallpapers --target="$HOME"` from the repo root so `~/Pictures/Wallpapers/` reflects the renames (restow prunes the old rose-pine/tokyo-night links). Confirm `current.jpg` still resolves (it targets a loose root-level file, unaffected per research inventory).
  </action>
  <verify>
    <automated>test -d wallpapers/Pictures/Wallpapers/rosepine && test ! -e wallpapers/Pictures/Wallpapers/rose-pine && test -d wallpapers/Pictures/Wallpapers/tokyonight && test ! -e wallpapers/Pictures/Wallpapers/tokyo-night && test -f wallpapers/Pictures/Wallpapers/dracula/.gitkeep && test -f wallpapers/Pictures/Wallpapers/catppuccin-latte/.gitkeep && test -f wallpapers/Pictures/Wallpapers/rosepine-dawn/.gitkeep && test -f wallpapers/Pictures/Wallpapers/gruvbox-light/.gitkeep && test -f wallpapers/Pictures/Wallpapers/tokyonight-day/.gitkeep && test -f wallpapers/Pictures/Wallpapers/kanagawa-lotus/.gitkeep && test -e ~/Pictures/Wallpapers/rosepine && test ! -e ~/Pictures/Wallpapers/rose-pine && test -e "$(readlink -f ~/Pictures/Wallpapers/current.jpg)"</automated>
  </verify>
  <acceptance_criteria>
    - Repo tree: rosepine/ and tokyonight/ exist with their previous contents (6 and 7 files); the dashed originals are gone; git history shows renames (git mv), not delete+add of binaries
    - dracula/ plus all five light-variant folders exist, each containing .gitkeep
    - Live tree: ~/Pictures/Wallpapers/rosepine resolves, ~/Pictures/Wallpapers/rose-pine does not; current.jpg still resolves to an existing file
    - Folder set now covers every palette name shipped by Plan 05-02 that has staged wallpapers, 1:1
  </acceptance_criteria>
  <done>Wallpaper folder names and palette names are in strict 1:1 correspondence with no drift and no mapping file.</done>
</task>

<task type="auto">
  <name>Task 2: Wallpaper auto-set on static theme-apply with per-theme last-used state (D-11/D-12)</name>
  <files>theme-engine/.config/theme-engine/lib/wallpaper.sh, theme-engine/.config/theme-engine/theme-apply</files>
  <read_first>
    - theme-engine/.config/theme-engine/theme-apply (file being modified — lib sourcing block, post-commit/pre-reload call site)
    - theme-engine/.config/theme-engine/lib/commit.sh (atomic temp+mv idiom for current-theme at lines 66-67 — copy for last-used writes)
    - theme-engine/.config/theme-engine/lib/reload.sh (headless-guard pattern at lines 33-36 — the WAYLAND_DISPLAY/DBUS check shape to reuse for the awww call)
    - hypr/.config/hypr/scripts/theme-init.sh (login-time wallpaper handling that must stay consistent — it sets current.jpg's target via awww then calls theme-apply)
    - hypr/.config/hypr/scripts/wallpaper-picker.sh (current.jpg ln -sfr idiom and image-extension find filter to mirror)
  </read_first>
  <action>
    1. Create `theme-engine/.config/theme-engine/lib/wallpaper.sh` with one function `theme_engine_wallpaper_autoset` taking the theme name:
       - Return 0 immediately for materialyou and materialyou-light (D-11: dynamic mode keeps wallpaper→palette direction; auto-set is static-only).
       - Resolve dir as `$HOME/Pictures/Wallpapers/<name>`; if missing or containing no image files (same extension filter as the picker: jpg/jpeg/png/webp/gif, maxdepth 1, exclude current.jpg), return 0 silently — empty set keeps the current wallpaper (D-12 never-a-dead-end semantics on the apply side).
       - Candidate selection: read `$HOME/.local/state/theme/last-wallpaper/<name>` if present; treat its content as a bare filename — reject it (fall through to first-in-folder) if it contains a slash or does not exist as a file inside dir (Security Domain V5: never interpolate state-file content with path components). Else pick the first image in the folder by sorted name (D-11 fallback).
       - Apply: `ln -sfr` the chosen file to `$HOME/Pictures/Wallpapers/current.jpg`; then, only when a graphical session is present (same WAYLAND_DISPLAY/DBUS_SESSION_BUS_ADDRESS guard shape as reload.sh) and awww is on PATH, fire the awww img transition best-effort (`|| true`, matching theme-init.sh's transition flags). The headless container gate must never hang here.
       - Write-back: mkdir -p the last-wallpaper state subdir, write the chosen filename via the temp-file+mv atomic idiom from commit.sh, `|| true` throughout — auto-set is cosmetic and must never fail theme-apply's exit code.
    2. In `theme-apply`: source lib/wallpaper.sh alongside the other libs, and call `theme_engine_wallpaper_autoset "$NAME"` after theme_engine_commit succeeds and before theme_engine_reload — so the desktop lands wallpaper + colors together (CONTEXT "Omarchy-style one-action coherence"), and a failed render never touches the wallpaper (atomic ordering).
    3. Sequencing note honored by task order: the D-09 renames (Task 1) land before any auto-set logic can point current.jpg into a per-theme folder (research Runtime State Inventory warning).
  </action>
  <verify>
    <automated>bash -n theme-engine/.config/theme-engine/lib/wallpaper.sh && bash -n theme-engine/.config/theme-engine/theme-apply && ~/.config/theme-engine/theme-apply catppuccin && [[ "$(readlink -f ~/Pictures/Wallpapers/current.jpg)" == *"/Wallpapers/catppuccin/"* ]] && f=$(cat ~/.local/state/theme/last-wallpaper/catppuccin) && [[ "$f" != */* ]] && test -f ~/Pictures/Wallpapers/catppuccin/"$f" && ~/.config/theme-engine/theme-apply dracula && [[ "$(readlink -f ~/Pictures/Wallpapers/current.jpg)" == *"/Wallpapers/catppuccin/"* ]]</automated>
  </verify>
  <acceptance_criteria>
    - `theme-apply catppuccin` repoints current.jpg into the catppuccin/ folder and records the chosen bare filename in the last-wallpaper state file for catppuccin
    - A second `theme-apply catppuccin` picks the same recorded wallpaper (stability), verified by comparing readlink output across two runs
    - `theme-apply dracula` (empty folder) leaves current.jpg untouched and exits 0 — the keep-current branch, which also restores the user's active dracula theme after this verification
    - lib/wallpaper.sh contains the slash-rejection and file-existence validation on the state-file content before any path join
    - Function returns early for both materialyou names (verified by grep for the two literals in the early-return condition)
  </acceptance_criteria>
  <done>One `theme-apply <static-preset>` action lands palette + matching wallpaper coherently, with per-theme memory, safe fallbacks, and headless safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state file content → filesystem path | last-wallpaper file content joined into a wallpaper path |
| theme name → filesystem path | preset name (already allowlist-validated by theme-apply) names the folder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | last-wallpaper state content path join | medium | mitigate | Bare-filename validation: reject content containing a slash, require the file to exist inside the theme's folder before use (Task 2) |
| T-05-08 | Denial of Service | awww call in headless context | low | mitigate | Session guard (WAYLAND_DISPLAY/DBUS check) + command -v + best-effort `|| true`; container gate cannot hang (Task 2) |
</threat_model>

<verification>
- Folder tree 1:1 with palette names; renames visible as git renames
- Auto-set behavior: populated folder → themed wallpaper + state write-back; empty folder → keep current; materialyou → untouched
- theme-apply exit code unaffected by any auto-set failure path
</verification>

<success_criteria>
- ROADMAP Phase 5 SC 4's data foundation is complete: per-theme sets exist and static theme application is wallpaper-coherent (picker restriction UI lands in Plan 05-04)
</success_criteria>

<output>
Create `.planning/phases/05-light-mode-pipeline-theme-presets/05-03-SUMMARY.md` when done.
</output>
