---
phase: 04-reliability-fixes-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hypr/.config/hypr/hyprlock.conf
  - hypr/.config/hypr/hypridle.conf
autonomous: false
requirements: [FIX-02]
user_setup: []

must_haves:
  truths:
    - "After manual lock, the very first keystrokes register — the password is typed in one attempt with no dropped-input failed-auth loop, 100% across 10 trials (D-23)"
    - "The idle-lock trigger path also registers first keystrokes — the 10-trial protocol covers both manual-lock and idle-lock paths (D-23)"
    - "The grace-period double-unlock root cause is documented with the hyprlock -v log signature from hyprwm/hyprlock#423, captured with grace=5 BEFORE the fix is applied (D-19)"
    - "A lockout-recovery procedure (second TTY logged in + pkill hyprlock escape hatch) is written before the first hyprlock test in this phase (D-20)"
  artifacts:
    - "hypr/.config/hypr/hyprlock.conf"
    - ".planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md"
  key_links:
    - "hyprlock.conf general{} grace value -> grace-period unlock routine -> PAM auth keystroke input path"
  prohibitions:
    - statement: "hyprlock label{} blocks must NOT change — time label 96px FiraCode Nerd Font Bold, date 22px, greeting 18px, and all $primary/$on_surface/$secondary/$tertiary/$error colors preserved"
      status: resolved
      verification: "git diff shows no changes to font_size, font_family, or color directives; only general{} grace (and optionally no_fade_in/no_fade_out) changed"
    - statement: "No color value may be hardcoded — all colors stay sourced from ~/.local/state/theme/hyprland.conf"
      status: resolved
      verification: "hyprlock.conf still has `source = ~/.local/state/theme/hyprland.conf` and no literal hex added"
    - statement: "input-field fade_on_empty/fade_timeout must NOT be touched unless Task 2 evidence specifically implicates fade timing (D-19 scopes the fix to grace first)"
      status: resolved
      verification: "git diff shows input-field{} block unchanged unless the SUMMARY records fade-timing evidence"
---

<objective>
Root-cause and fix the hyprlock first-keystroke drop (FIX-02): after manual lock the first characters vanish, forcing a failed-auth retry. Confirm the grace-period double-unlock signature (hyprwm/hyprlock#423) in this machine's logs, then apply the grace=0 config mitigation — a documented-evidence-backed fix, not a workaround (D-18/D-19). Write the lockout-recovery procedure first (D-20) so testing a lock screen never risks a real lockout.

Purpose: The user must be able to type their password in one attempt. This is a daily-friction reliability defect with an exact upstream match, and the lockout-recovery doc is reused by Phase 6 LOCK-01.

Output: grace=0 in hyprlock.conf with the #423 log signature captured as evidence, a hypridle.conf audit, a lockout-recovery procedure, and a 100%-pass 10-trial verification record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md
@.planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md
@.planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md
@.planning/phases/04-reliability-fixes-tech-debt/04-UI-SPEC.md
@hypr/.config/hypr/hyprlock.conf
@hypr/.config/hypr/hypridle.conf
</context>

## Artifacts this phase produces (Plan 02)

New or changed symbols introduced by this plan (excluded from drift verification):

- `.planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md` — FIX-02 root-cause write-up + the lockout-recovery procedure (D-20/D-25)
- Changed `grace` value in `hypr/.config/hypr/hyprlock.conf` general{} block (5 -> 0)
- A documented note in the SUMMARY that the grace-period move-cursor-to-unlock convenience is removed (interaction behavior change, per UI-SPEC Copywriting Contract)

<tasks>

<task type="auto">
  <name>Task 1: Write the lockout-recovery procedure and prepare the diagnosis capture harness</name>
  <files>.planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md (D-17 symptom fingerprint, D-20 lockout-recovery-first, D-23 10-trial protocol, D-25 documentation)
    - .planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md (Summary FIX-02; Pattern 2 grace mitigation; Pitfall 2; Code Examples "confirm hyprlock grace-race signature")
    - hypr/.config/hypr/hyprlock.conf (current general{} block with grace=5; the label{} and input-field{} blocks that must NOT change)
    - hypr/.config/hypr/hypridle.conf (lock_cmd / before_sleep_cmd / the 600s lock listener)
  </read_first>
  <action>
    Write the lockout-recovery procedure into 04-02-SUMMARY.md BEFORE any hyprlock test happens (D-20). Document: (a) log into a second TTY first (Ctrl+Alt+F2, sign in) so a stuck lock screen never locks the user out; (b) the escape hatch `pkill hyprlock` from that TTY; (c) how to restore a working session. This exact document is reused by Phase 6 LOCK-01.

    Also record the diagnosis capture procedure — do NOT lock the screen in this autonomous task; the actual lock+observe happens at the human checkpoint. The human will run `uwsm app -- hyprlock -v` in the foreground, lock, immediately type, and grep the output for the hyprwm/hyprlock#423 signature: the line "In grace and cursor moved more than 5px, unlocking!" followed by "Unlock already happend?". Note where hyprlock logs (foreground stderr, or ~/.cache/hyprlock/). Confirm the current grace value is 5 in hyprlock.conf and that the symptom fingerprint (D-17) is manual-lock-then-immediate-type. Do not modify hyprlock.conf or hypridle.conf in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md && grep -qiE "second TTY|Ctrl\+Alt\+F2|tty" .planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md && grep -qi "pkill hyprlock" .planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 04-02-SUMMARY.md exists and contains a "Lockout-recovery procedure" section naming the second-TTY login and the `pkill hyprlock` escape hatch
    - The document records the #423 diagnosis capture procedure (hyprlock -v foreground run + the two log lines to grep for)
    - hyprlock.conf and hypridle.conf are unchanged (git diff shows only the SUMMARY added)
  </acceptance_criteria>
  <done>The lockout-recovery procedure exists before any lock test, and the #423 diagnosis capture procedure is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reproduce the keystroke drop at grace=5 and capture the #423 evidence</name>
  <action>Human checkpoint — the executor pauses and does NOT apply grace=0 until the resume-signal. With a second TTY logged in first, the human runs hyprlock -v, reproduces the drop, and captures the #423 log signature (or alternative evidence) so the fix is evidence-backed (D-19).</action>
  <what-built>The lockout-recovery procedure and the #423 diagnosis capture procedure in 04-02-SUMMARY.md. This checkpoint reproduces the keystroke drop with grace=5 and captures the log-signature evidence REQUIRED before the fix (D-19, per Pitfall 2).</what-built>
  <how-to-verify>
    1. FIRST, log into a second TTY (Ctrl+Alt+F2, sign in) so you cannot be locked out. Return to the graphical session (Ctrl+Alt+F1).
    2. Run `uwsm app -- hyprlock -v` from a terminal so its verbose output is visible/captured.
    3. With the lock screen up, IMMEDIATELY type a few characters. Observe whether the first characters are dropped.
    4. In the hyprlock -v output, look for "In grace and cursor moved more than 5px, unlocking!" followed by "Unlock already happend?" — the exact hyprwm/hyprlock#423 signature.
    5. If stuck, use the second TTY: `pkill hyprlock`.
    6. Report: (a) were the first keystrokes dropped? (b) paste the #423 signature lines, or "signature not present — different root cause".
    If the signature is absent, do NOT proceed to grace=0 blindly — the root cause may be input-grab/focus timing at startup instead; capture whatever hyprlock -v shows and report it.
  </how-to-verify>
  <resume-signal>Paste the #423 log signature (or the alternative evidence) and confirm the keystroke drop reproduced, then type "approved".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply grace=0 and audit the hypridle idle-lock path</name>
  <files>hypr/.config/hypr/hyprlock.conf, hypr/.config/hypr/hypridle.conf, .planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md (the captured #423 evidence from the checkpoint)
    - .planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md (hyprlock.conf analog — Pattern to apply; "do not touch input-field fade unless implicated")
    - .planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md (Pattern 2; D-18/D-19 rationale)
    - hypr/.config/hypr/hyprlock.conf (general{} block, the grace directive; label{} and input-field{} blocks to leave untouched)
    - hypr/.config/hypr/hypridle.conf (idle listeners)
  </read_first>
  <action>
    In hypr/.config/hypr/hyprlock.conf, set the grace directive in the general{} block to 0 (was 5). Scope the edit to the grace directive only. Keep hide_cursor, and only change no_fade_in/no_fade_out if the captured evidence specifically implicated fade timing (D-19). Do NOT touch any label{} block (time 96px, date 22px, greeting 18px, their colors), the input-field{} block (including fade_on_empty/fade_timeout), or the `source = ~/.local/state/theme/hyprland.conf` line. If the checkpoint evidence showed a different root cause than #423, STOP and record it in the SUMMARY rather than applying grace=0 blindly.

    Audit hypridle.conf (D-14 completeness for the idle path): confirm lock_cmd, before_sleep_cmd, and the 600s `loginctl lock-session` listener are consistent with the manual-lock path and do not introduce a divergent unlock behaviour. Per D-17 the symptom is manual-lock-only, so this file is expected to need no change — document "audited, no change required" (or the change made) in the SUMMARY.

    Record in the SUMMARY (D-25): the confirmed root cause, that grace=0 removes the move-cursor-to-unlock convenience (a behavior change to surface to the user, per UI-SPEC Copywriting Contract), and that no visual/typography/color directive changed.
  </action>
  <verify>
    <automated>grep -Eq '^[[:space:]]*grace[[:space:]]*=[[:space:]]*0([[:space:]]|$)' hypr/.config/hypr/hyprlock.conf && grep -q 'source = ~/.local/state/theme/hyprland.conf' hypr/.config/hypr/hyprlock.conf && [ "$(grep -cE '^[[:space:]]*font_size[[:space:]]*=[[:space:]]*(96|22|18)' hypr/.config/hypr/hyprlock.conf)" = "3" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - hyprlock.conf general{} sets `grace = 0`: `grep -Eq '^[[:space:]]*grace[[:space:]]*=[[:space:]]*0' hypr/.config/hypr/hyprlock.conf`
    - The `source = ~/.local/state/theme/hyprland.conf` line is still present (no hardcoded colors)
    - All three label font_size values (96, 22, 18) remain present and unchanged (grep -c == 3); label colors unchanged
    - input-field{} block unchanged unless the SUMMARY records fade-timing evidence
    - hypridle.conf audit outcome recorded in the SUMMARY
  </acceptance_criteria>
  <done>grace=0 is applied with evidence, the idle path is audited, and no typography/color/visual directive changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify FIX-02 with the D-23 10-trial lock-and-type protocol</name>
  <action>Human checkpoint — the executor pauses until the resume-signal. With a second TTY logged in, the human runs ~10 lock-then-immediately-type trials across both manual-lock and idle-lock paths; pass requires 100% first-try unlock. Do not sign off below 100%.</action>
  <what-built>grace=0 in hyprlock.conf, backed by the captured #423 signature, plus the hypridle idle-path audit. This checkpoint runs the D-23 10-trial reliability protocol.</what-built>
  <how-to-verify>
    Keep a second TTY logged in first (per the lockout-recovery procedure). Then run the D-23 protocol: perform ~10 lock -> immediately-type-password trials that must unlock on the first try. Cover BOTH trigger paths — manual lock (keybind/menu) and idle lock (or `loginctl lock-session`). Pass = 100% (every trial unlocks first attempt with no dropped first character and no failed-auth loop). If any trial drops input, record which path and how many, and report — do not sign off below 100%.
  </how-to-verify>
  <resume-signal>Report "10/10 first-try unlock, both paths" (or the failing trials), then type "approved".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard -> hyprlock input field | Untrusted keystrokes cross into the lock screen's password buffer |
| hyprlock -> PAM | Authentication decision crosses from hyprlock to the system auth stack |
| grace-period routine -> unlock decision | The grace timer can unlock without a password within its window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | hyprlock grace-period unlock-without-password window | medium | mitigate | grace=0 removes the timed grace-unlock window entirely, so the lock screen always requires a password — tightens the auth boundary while fixing the keystroke-drop race (#423) |
| T-04-05 | Denial of Service | hyprlock keystroke drop -> repeated failed auth | high | mitigate | The #423 double-unlock race consumes real keystrokes and forces failed-auth loops; grace=0 eliminates the race so first keystrokes register (FIX-02) |
| T-04-06 | Denial of Service | testing the lock screen could lock the user out | medium | mitigate | Lockout-recovery procedure (second TTY + pkill hyprlock) written and used BEFORE the first test (D-20) |
</threat_model>

<verification>
- FIX-02: 10 lock -> immediately-type trials, 100% first-try unlock across manual-lock and idle-lock paths (D-23) — one-time documented protocol in 04-02-SUMMARY.md (D-24)
- Root cause documented with the #423 hyprlock -v log signature captured at grace=5 before the fix (D-19/D-25)
- Parity: git diff shows only the grace directive changed in general{}; label/input-field/color/source lines unchanged (UI-SPEC Typography + Color contracts)
- Lockout-recovery procedure present before any lock test (D-20)
</verification>

<success_criteria>
- After lock activation, the very first keystrokes register — password typed in one attempt, no dropped-input failed-auth loop (10/10)
- Root cause diagnosed with the #423 log signature and documented — not patched around
- grace=0 applied; no typography, color, or visual directive changed
- Lockout-recovery procedure written first and reusable by Phase 6 LOCK-01
</success_criteria>

<output>
Create `.planning/phases/04-reliability-fixes-tech-debt/04-02-SUMMARY.md` when done — including the lockout-recovery procedure, the #423 root-cause evidence, the hypridle audit outcome, the behavior-change note, and the D-23 verification record.
</output>
