---
phase: 18.1-qml-bar-athena-restoration
plan: 01
subsystem: ui
tags: [quickshell, qml, hyprland, waybar-parity, nerd-font, athena-theme]

# Dependency graph
requires:
  - phase: 12-unified-design-token-pipeline
    provides: Colours.qml singleton (17 role aliases BarRoles.qml resolves through)
  - phase: 18-qml-bar-retirement-machinery
    provides: WorkspaceCapsule.qml, BarCapsule.qml, Design.qml (bar chrome/geometry tokens), quickshell shell scaffolding
provides:
  - "modules/bar/BarRoles.qml — a new bar-scoped colour role singleton (17 roles, 5 alpha-blended byte-exact to Athena's theme.scss) every sibling plan in phase 18.1 consumes"
  - "WorkspaceCapsule.qml's focused workspace slot converted from a foreground tint to a real filled BarRoles.accent pill (D-07)"
  - "WorkspaceCapsule.qml's plain numeral replaced with Athena's three-state Nerd Font glyph identity (active/default/urgent, D-08)"
  - "All 12 appGlyphMap entries now carry real Athena codepoints — the seven previously-empty glyph literals are fixed (D-11/D-27 closed)"
  - "Live screenshot proof (.planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png) that real Nerd Font glyphs render, not tofu"
affects: [18.1-02, 18.1-03, 18.1-04, 18.1-05, 18.1-06, 18.1-07]

# Actuals (#2632)
actuals:
  tokens: 4645
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Bar-scoped colour role singleton (BarRoles.qml) resolving exclusively through Colours.qml's 12 existing role aliases via the repo's proven Qt.rgba(X.r, X.g, X.b, N) alpha idiom — no new blend helper, no independent colour source"
    - "Slot-level state-glyph resolver (stateGlyphFor(focused, urgent)) mirroring the existing slotTextColour precedence (focused > urgent > default) — a pure function pattern any future capsule can copy for glyph/colour parity"

key-files:
  created:
    - quickshell/.config/quickshell/modules/bar/BarRoles.qml
    - .planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png
  modified:
    - quickshell/.config/quickshell/modules/bar/qmldir
    - quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml

key-decisions:
  - "Athena's fourth workspace state, `empty` (U+F444, style-athena.scss:180-182, opacity 0.6, for a workspace that exists but holds zero windows), is deliberately NOT implemented — D-08 named only active/default/urgent. Recorded here as a named delta routed to GATE-02 rather than silently dropped; such a slot currently renders `stateGlyphDefault`."
  - "Accepted cost of the state-glyph identity (named by D-08): the numeral's one-to-one visual mapping onto the Super+N keybinds is gone — a slot no longer shows which digit activates it."
  - "zen's glyph (U+F269, the fa-firefox codepoint) is carried verbatim from Athena's own config-athena.jsonc window-rewrite table (config-athena.jsonc:150-151) — not a transcription error; Athena's own font has no dedicated Zen glyph."

patterns-established:
  - "Pattern: a bar-scoped colour/text role singleton lives beside its first real consumer's directory (modules/bar/), registered via qmldir's `singleton` keyword AND the source's own `pragma Singleton` (12-06's both-required finding) — sibling plans 02/04/05/06/07 extend BarRoles.qml rather than creating parallel role sources."

requirements-completed: [GATE-02, QBAR-01, QBAR-03, QBAR-10, QBAR-12]

coverage:
  - id: D1
    description: "BarRoles.qml singleton exists with exactly 17 readonly property color rows (5 Qt.rgba alpha-blended, 12 direct Colours.* aliases), registered in modules/bar/qmldir, and resolves to real colour values at runtime (not undefined)"
    requirement: "QBAR-10"
    verification:
      - kind: other
        ref: "grep -c 'readonly property color' BarRoles.qml == 17; grep -c 'Qt.rgba(' == 5; grep -c '^singleton BarRoles 1.0 BarRoles.qml$' qmldir == 1; live BARROLES_PROBE log line confirmed accent resolved to a real colour object before Task 1's commit"
        status: pass
    human_judgment: false
  - id: D2
    description: "WorkspaceCapsule.qml's focused workspace slot renders as a filled BarRoles.accent pill with BarRoles.onAccent text instead of a foreground tint; urgent-and-unfocused survives as a BarRoles.danger foreground tint; default reads BarRoles.capsuleFg; zero Colours.* references remain in the file"
    requirement: "GATE-02"
    verification:
      - kind: other
        ref: "sed 's|//.*||' WorkspaceCapsule.qml | grep -c 'Colours\\.' == 0; grep -n 'BarRoles.accent|BarRoles.onAccent|BarRoles.danger|BarRoles.capsuleFg' each >=1 hit"
        status: pass
      - kind: manual_procedural
        ref: "screenshots/18.1-01-workspace-glyphs.png — direct visual inspection shows a real filled lavender/purple pill around the focused slot's icons"
        status: pass
    human_judgment: false
  - id: D3
    description: "All 12 appGlyphMap entries carry a non-empty, byte-exact-to-Athena glyph (the seven previously-empty literals fixed); glyphFor()/appIdFor()/appGlyphFontFamily/appGlyphFallback byte-unchanged (data-only fix, D-11/D-27 closed)"
    requirement: "QBAR-01"
    verification:
      - kind: other
        ref: "python3 codepoint-level re-parse of appGlyphMap confirmed all 12 entries non-empty and the 7 filled entries match config-athena.jsonc's window-rewrite table byte-for-byte (firefox U+E658, zen U+F269, discord U+F1FF, spotify U+F1BC, net.lutris.Lutris U+F11B, steam U+F1B6, thunar U+F07B); git diff shows zero changed lines inside glyphFor/appIdFor/activateWorkspace"
        status: pass
    human_judgment: false
  - id: D4
    description: "Workspace slot renders one of Athena's three Nerd Font state glyphs (active/default/urgent, format-icons codepoints) instead of a plain numeral, via a new stateGlyphFor(focused, urgent) resolver; the fourth 'empty' state is a named, unimplemented delta"
    requirement: "QBAR-03"
    verification:
      - kind: other
        ref: "python3 codepoint check: stateGlyphActive=U+F0BAF, stateGlyphDefault=U+F02A0, stateGlyphUrgent=U+F09F5 all match config-athena.jsonc's format-icons table; grep -c 'String(slotItem.slotId)' == 0; grep -c 'numeralMetrics' == 0 (renamed slotIdentityMetrics)"
        status: pass
      - kind: manual_procedural
        ref: "screenshots/18.1-01-workspace-glyphs.png — unfocused slot renders the ghost default-state glyph (U+F02A0) outside the pill; focused slot's state-glyph cell renders a distinct arc/circle shape, not a numeral"
        status: pass
    human_judgment: false
  - id: D5
    description: "A live screenshot proves the workspace capsule's glyphs render as real Nerd Font shapes, not tofu boxes, with a browser, terminal, and file manager all open (D-22)"
    requirement: "QBAR-12"
    verification:
      - kind: manual_procedural
        ref: "screenshots/18.1-01-workspace-glyphs.png; hyprctl clients -j confirmed class strings zen/kitty/thunar; edge-detection cluster check over the capsule region found 4 distinct glyph clusters (>1 required), 0 flagged as a .notdef hollow-box shape"
        status: pass
    human_judgment: true
    rationale: "Tofu-vs-real-glyph judgment is fundamentally a visual call; the mechanical edge-cluster check is a supporting heuristic, not a substitute for a human confirming the screenshot at GATE-02."

duration: "~12min this session (Tasks 2-3); ~11min total commit span across both sessions (16:48:56-17:00:07 local)"
completed: 2026-08-11
status: complete
---

# Phase 18.1 Plan 01: BarRoles Colour Layer + Workspace Capsule Restoration Summary

**New `BarRoles.qml` bar-scoped colour singleton, focused workspace slot converted from foreground tint to a real filled accent pill, and Athena's three-state Nerd Font workspace glyph identity restored alongside the fix for all seven previously-empty per-app glyph literals**

## Performance

- **Duration:** ~12 min this continuation session (Tasks 2 and 3); full plan commit span (including Task 1, executed in a prior session that halted at the tracer feedback gate) ~11 min, 2026-08-11T16:48:56+03:00 to 2026-08-11T17:00:07+03:00
- **Started (this session):** 2026-08-11T16:56Z (approx, continuation resume)
- **Completed:** 2026-08-11T14:00:34Z (UTC)
- **Tasks:** 3/3
- **Files modified:** 4 (2 created, 2 modified — see key-files)

## Accomplishments

- Built `BarRoles.qml`, a new bar-scoped colour role singleton exposing all 17 roles Athena's `theme.scss` defines (5 alpha-blended via `Qt.rgba`, byte-exact to 0.55/0.78/0.85/0.95/0.5; 12 direct `Colours.*` aliases), registered in `modules/bar/qmldir` with both the `singleton` keyword and `pragma Singleton` (12-06's both-required finding).
- Converted `WorkspaceCapsule.qml`'s focused workspace slot from a foreground colour tint to a real filled `BarRoles.accent` pill with `BarRoles.onAccent` text — the architectural reversal this whole phase exists to make (D-01/D-02). The urgent-and-unfocused branch was preserved exactly as Athena's own CSS treats it: a foreground tint only (`BarRoles.danger`), never a filled pill. Default reads `BarRoles.capsuleFg`.
- Filled all seven previously-empty glyph literals in `appGlyphMap` with Athena's own `config-athena.jsonc` `window-rewrite` codepoints (firefox, zen, discord, spotify, net.lutris.Lutris, steam, thunar), closing the D-11/D-27 diagnosis as a pure data fix — `glyphFor()`, `appIdFor()`, `appGlyphFontFamily`, and `appGlyphFallback` are byte-unchanged.
- Replaced the workspace slot's plain numeral with Athena's three-state Nerd Font glyph identity (D-08): `stateGlyphActive`/`stateGlyphDefault`/`stateGlyphUrgent` properties and a `stateGlyphFor(focused, urgent)` pure resolver, matching `slotTextColour`'s existing focused-over-urgent precedence.
- Captured and committed a live screenshot (`screenshots/18.1-01-workspace-glyphs.png`) with a real browser (zen), terminal (kitty), and file manager (thunar) all open, proving real Nerd Font glyphs render — not tofu — with a supporting mechanical edge-cluster check.

## Task Commits

Each task was committed atomically:

1. **Task 1: BarRoles singleton wired end-to-end through the focused workspace pill** - `c033ba4` (feat) — completed in a prior session, halted at the plan's tracer-feedback checkpoint; human approved before this continuation resumed.
2. **Task 2: Athena state-glyph slot identity and the seven empty glyph literals** - `8996fa8` (feat)
3. **Task 3: Live screenshot proof that workspace glyphs render, not tofu (D-22)** - `f1e7486` (feat)

_No separate plan-metadata commit yet — STATE.md/ROADMAP.md/REQUIREMENTS.md updates and the final `docs(18.1-01)` commit follow this SUMMARY._

## Files Created/Modified

- `quickshell/.config/quickshell/modules/bar/BarRoles.qml` - New bar-scoped colour role singleton (17 roles)
- `quickshell/.config/quickshell/modules/bar/qmldir` - Registers `singleton BarRoles 1.0 BarRoles.qml`
- `quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` - Focused-slot fill, state-glyph identity, glyph-map data fix
- `.planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png` - Live D-22 proof screenshot

## Decisions Made

- Athena's fourth workspace state (`empty`, U+F444, opacity 0.6) is a named, deliberately unimplemented delta — D-08 only named active/default/urgent. A workspace that exists but holds zero windows renders `stateGlyphDefault` instead. Routed to GATE-02 for the operator to call, per the plan's own instruction, not silently dropped.
- Accepted cost of D-08: the numeral's one-to-one visual mapping onto the Super+N keybinds is gone.
- zen's glyph (U+F269, fa-firefox) is Athena's own deliberate choice, carried verbatim — not corrected, since no dedicated Zen glyph exists in the font.

## Deviations from Plan

None - all three tasks executed exactly as specified in the plan. The seven glyph literals, three state-glyph codepoints, and all structural changes (rename, comment rewrites) match the plan's `<action>` text precisely, and all listed acceptance criteria/automated verify commands passed.

## Issues Encountered

During Task 2's mid-edit sequence, an intermediate save (rename of `numeralMetrics` → `slotIdentityMetrics` applied to the declaration before all reference sites were updated in the same edit) triggered a transient QML `ReferenceError: numeralMetrics is not defined` in `$HOME/.cache/quickshell.log` on quickshell's hot-reload. This was self-resolved within the same task by the immediately following `sed` pass that renamed every remaining reference; the log shows clean `Configuration Loaded` entries after the final edit, with no error following it. Not a deviation from the plan — no code behavior differs from what was specified, and the file's final state carries zero `numeralMetrics` references (confirmed by `grep -c`).

## Known Stubs

None — no hardcoded empty values, placeholder text, or unwired components were introduced by this plan. Athena's fourth `empty` workspace state is a named, plan-acknowledged delta (see Decisions Made), not a silent stub.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `BarRoles.qml` is now available for every sibling plan in phase 18.1 (02, 04, 05, 06, 07) to consume — proven end-to-end on one real, visible consumer before any other capsule migrates, per the plan's tracer-first design.
- `WorkspaceCapsule.qml` is fully restored: filled focused pill, three-state Nerd Font glyph identity, and all 12 app glyphs rendering correctly.
- Two probe-surfaced flagged assumptions (QBAR-03 interaction-affordance risk, QBAR-10 restart-wrapper inheritance) remain unresolved as documented in the plan and are carried to plan 18.1-07's GATE-02 checkpoint — not discharged here.
- Athena's fourth `empty` workspace state (U+F444) remains a named, unimplemented delta for GATE-02 to rule on.
- No blockers for plans 02-07 proceeding.

---
*Phase: 18.1-qml-bar-athena-restoration*
*Completed: 2026-08-11*
