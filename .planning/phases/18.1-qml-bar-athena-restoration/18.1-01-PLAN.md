---
phase: 18.1-qml-bar-athena-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickshell/.config/quickshell/modules/bar/BarRoles.qml
  - quickshell/.config/quickshell/modules/bar/qmldir
  - quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml
autonomous: true
requirements: [GATE-02, QBAR-01, QBAR-03, QBAR-10, QBAR-12]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01/D-02: the focused workspace slot renders as a FILLED pill (Athena's one resting accent-filled element), not a foreground tint — colour arrives as background, which is the architectural reversal this phase exists to make."
    - "D-03: a new bar-scoped singleton exists at modules/bar/BarRoles.qml and is registered in modules/bar/qmldir with the `singleton` keyword AND carries `pragma Singleton` in its own source (12-06's both-required finding; without both it resolves undefined with no load error)."
    - "D-04: all 17 BarRoles rows exist with the exact Athena theme.scss sources — barSurface=alpha(surface,0.55), barSurfaceHover=alpha(surface,0.78), capsule=alpha(surfaceVariant,0.85), capsuleFg=onSurfaceVariant, capsuleHover=alpha(surfaceVariant,0.95), capsuleTrack=alpha(outline,0.5), accent=primary, onAccent=onPrimary, warn=tertiary, danger=error, onDanger=onError, fillClock=secondary, fillClockFg=onSecondary, fillUpdates=tertiary, fillUpdatesFg=onTertiary, fillNotification=primary, fillNotificationFg=onPrimary."
    - "D-05: every alpha row is expressed with the repo's proven Qt.rgba(Colours.X.r, Colours.X.g, Colours.X.b, N) idiom (PanelDialog.qml:168 / Dashboard.qml:367 / Overview.qml:974) and reproduces its alpha value byte-exactly — no rounding, no new blend helper."
    - "D-07: WorkspaceCapsule's focused slot uses background BarRoles.accent with text BarRoles.onAccent; the urgent-and-unfocused branch survives as a BarRoles.danger FOREGROUND tint (Athena's own style-athena.scss:175-177 treats urgent as foreground-only, not a filled pill); the default branch reads BarRoles.capsuleFg."
    - "D-08: slot identity is Athena's three-state glyph set with no numeral — active U+F0BAF, default U+F02A0, urgent U+F09F5 — matching config-athena.jsonc:220-225's format-icons table."
    - "D-09: workspace glyph cells render in FiraCode Nerd Font; the switch away from Design.symbolFontFamily was already shipped at WorkspaceCapsule.qml:62 for matched cells, and the state-glyph cell must use the same family."
    - "D-10: all twelve appGlyphMap entries (kitty, firefox, zen, codium, VSCodium, discord, spotify, obsidian, net.lutris.Lutris, steam, thunar, yazi) carry a non-empty glyph, with U+F02A0 as the unmatched default."
    - "D-11/D-27: the diagnosis is CLOSED — the fix is DATA ONLY. glyphFor()'s four matching stages, appGlyphFontFamily and appIdFor() are byte-unchanged; only the seven empty-string glyph literals change."
    - "D-22: a live screenshot of the bar with a browser, a terminal and a file manager open is committed to the phase directory, and a mechanical tofu check over the rendered region is recorded — the glyph fix is precisely the class that passes code review and renders as boxes."
    - "GATE-02 (adjacency edge): a side-by-side verdict of EXACTLY EQUAL passes — D-23 requires 'at least as good', so a tie is a pass, not a fail; only 'worse' fails."
    - "QBAR-01 (boundary edge): at exactly barSurface alpha 0.55 the ^quickshell-.* family blur rule's ignore_alpha floor must still permit blur; if the bar renders unblurred after the change, capture a screenshot BEFORE tuning any alpha and re-apply layer rules via hyprctl eval or a compositor restart (hyprctl reload silently drops layer-rule edits and the symptom is indistinguishable from a wrong alpha)."
    - "QBAR-03 (empty edge): a workspace that exists but holds zero windows renders the D-08 `default` glyph U+F02A0 at full slot extent and stays clickable — Athena carries a FOURTH `empty` state (U+F444, style-athena.scss:180-182 at opacity 0.6) that D-08 does not name; that fourth state is a NAMED DELTA routed to GATE-02, recorded in the SUMMARY, never silently implemented or silently dropped."
    - "QBAR-03 (boundary edge): clicking a persistent slot for a never-opened workspace still dispatches through activateWorkspace()'s range-checked integer fallback — WorkspaceCapsule.qml:200-212 is byte-unchanged by this plan."
    - "QBAR-10 (boundary edge): after the qmldir row lands, `BarRoles` resolves to a real object rather than undefined — a missing `singleton` keyword or a missing `pragma Singleton` produces silent undefined with NO load error, so absence-of-error is not sufficient evidence."
    - "QBAR-12 (adjacency edge): the bar's reserved zone extent is unchanged by this plan — Design.barHeight/barEdgeMargin/barSideMargin are not read, written or referenced anywhere in this plan's diff."
    - statement: "GATE-02 (empty edge): if the Athena waybar cannot be brought up for the side-by-side (package already gone, launcher fails), the gate does not silently pass — it halts and reports."
      verification: backstop
    - statement: "GATE-02 (ordering edge): when two GATE-02 criteria are judged equally, the verdict is a single overall operator judgment, not a per-criterion ranking — no tie-break ordering is defined or needed."
      verification: backstop
    - statement: "QBAR-01 (empty edge): a bar with every capsule empty still reserves its zone — BarCapsule.visible drops empty capsules from the positioner but the bar window's own reservation is independent."
      verification: backstop
  artifacts:
    - "quickshell/.config/quickshell/modules/bar/BarRoles.qml"
    - "quickshell/.config/quickshell/modules/bar/qmldir (one added `singleton BarRoles 1.0 BarRoles.qml` row)"
    - "quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml (filled focused pill + state glyph set + 7 filled glyph literals)"
    - ".planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png"
  key_links:
    - "BarRoles.qml -> Colours.qml: all 17 rows resolve through the 12 Colours role aliases at Colours.qml:148-166; this is the ONLY colour source, so both the matugen-dynamic and static-preset pipelines keep working by construction."
    - "modules/bar/qmldir -> BarRoles.qml: same-directory type resolution is qmldir-driven for this directory (the scanner is disabled), so an unregistered BarRoles is unreachable from every capsule with no load error."
    - "WorkspaceCapsule.qml -> BarRoles.accent/onAccent/danger/capsuleFg: the first real consumer, proving the whole role layer end-to-end before any other capsule migrates."
  prohibitions:
    - "Never restate a behavioural promise in a comment that the code does not implement. WorkspaceCapsule.qml:90 promised 'state is carried by colour instead' while line 349 applied a foreground tint — that comment is the origin of this entire phase. A comment describing behaviour must be deletable without changing what the file does."
    - "Never reduce a locked decision to a 'v1', 'static for now', 'placeholder' or 'wired later' form. If D-07 says filled pill, ship a filled pill; if D-10 says twelve glyphs, ship twelve glyphs."
    - "Never touch the bar's geometry tokens (barHeight, barEdgeMargin, barSideMargin, barColumnWidth, iconsPerSlot) to chase the operator's 'big and clunky' complaint. Those were measured identical to Athena's; changing them is the wrong fix and is fenced out of this phase."
    - "Never record a live visual verification (screenshot, glyph render, colour appearance) as performed when it was not. This repo already carries four open WINDOWS.md unrun-verify rows from exactly that pattern; an honest 'not performed' beats a claimed pass."
---

<objective>
Build the bar-scoped colour role layer Athena's `theme.scss` defines, and prove it end-to-end by converting the single most visible piece of the operator's "no colors" complaint — the focused workspace pill — from foreground tint to background fill. Then finish the workspace capsule: Athena's three-state glyph identity and the seven empty-string glyph literals that made every non-terminal window render nothing.

Purpose: `BarRoles` is the foundation every other plan in this phase consumes. Proving it on one real, visible consumer before expanding catches an architectural dead-end after one commit instead of after five.
Output: `BarRoles.qml` + its `qmldir` registration + a fully restored `WorkspaceCapsule.qml` + a committed live screenshot proving real glyphs, not tofu.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md
@quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml
@quickshell/.config/quickshell/modules/bar/qmldir
@waybar/.config/waybar/theme.scss
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — exclude from drift verification)

**New file:** `quickshell/.config/quickshell/modules/bar/BarRoles.qml`

**New `qmldir` row:** `singleton BarRoles 1.0 BarRoles.qml`

**New `BarRoles` singleton properties (17 rows, all `readonly property color`):**
`barSurface`, `barSurfaceHover`, `capsule`, `capsuleFg`, `capsuleHover`, `capsuleTrack`,
`accent`, `onAccent`, `warn`, `danger`, `onDanger`,
`fillClock`, `fillClockFg`, `fillUpdates`, `fillUpdatesFg`, `fillNotification`, `fillNotificationFg`

**New `WorkspaceCapsule.qml` properties:**
`stateGlyphActive`, `stateGlyphDefault`, `stateGlyphUrgent`, `stateGlyphFor(focused, urgent)`
and on the slot delegate: `slotStateGlyph`, `slotFillColour`, `slotTextColour`

**New screenshot artifact:** `.planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png`

(Symbols produced by sibling plans in this phase — `Readout.valueRevealed`, `severityColour`,
`clockFillPill`, `ActionCell.fillColour`, `drawerHoverActive`, `drawerDwellTimer`,
`drawerGraceTimer`, `reportDrawerHover`, `_qsd_check_bar_colour_role_routing`,
`_qsd_assert_bar_colour_roles`, `QSD_BAR_COLOUR_ROLE_EXEMPT` — are listed in their own plans.)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: BarRoles singleton wired end-to-end through the focused workspace pill</name>
  <files>quickshell/.config/quickshell/modules/bar/BarRoles.qml, quickshell/.config/quickshell/modules/bar/qmldir, quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml</files>
  <read_first>
    - `waybar/.config/waybar/theme.scss` lines 56-145 — the byte-exact source of every role; read the `@define-color` rows and the comment blocks that explain WHY each alpha was tuned.
    - `waybar/.config/waybar/style-athena.scss` lines 167-192 — Athena's own workspace-button CSS: `.active` is `background: @accent; color: @on-accent`, `.urgent` is `color: @danger` (foreground ONLY), `.empty` is `color: @capsule-fg; opacity: 0.6`, `:hover` is `background: @capsule-hover`.
    - `quickshell/.config/quickshell/modules/Colours.qml` lines 148-166 — confirm the 12 consumed role aliases exist under those exact names.
    - `quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml` line 168 — the repo's proven `Qt.rgba(X.r, X.g, X.b, N)` alpha idiom. Do not invent a second one.
    - `quickshell/.config/quickshell/modules/bar/qmldir` — read the whole header; it documents 12-06's both-required singleton finding and the manifest's own "register in the same commit that creates the type" rule.
    - `quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` lines 275-415 — the slot delegate, the numeral `Text`, and the three-branch colour expression at line 349.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` "Pattern 1" and "Pitfall 1".
  </read_first>
  <action>
Create `quickshell/.config/quickshell/modules/bar/BarRoles.qml` as a `pragma Singleton` QtObject exposing exactly seventeen `readonly property color` rows, each carrying an inline comment naming its Athena `theme.scss` line and its `Colours.*` source, so D-21's line-by-line diff is mechanical.

The five alpha rows use `Qt.rgba(Colours.<role>.r, Colours.<role>.g, Colours.<role>.b, <alpha>)` per D-05 and RESEARCH Pattern 1: `barSurface` = surface @ 0.55 (theme.scss:56), `barSurfaceHover` = surface @ 0.78 (theme.scss:58), `capsule` = surfaceVariant @ 0.85 (theme.scss:110), `capsuleHover` = surfaceVariant @ 0.95 (theme.scss:120), `capsuleTrack` = outline @ 0.5 (theme.scss:125). The twelve non-alpha rows are direct aliases: `capsuleFg`=`Colours.onSurfaceVariant` (theme.scss:116), `accent`=`Colours.primary` (:93), `onAccent`=`Colours.onPrimary` (:94), `warn`=`Colours.tertiary` (:95), `danger`=`Colours.error` (:96), `onDanger`=`Colours.onError` (:97), `fillClock`=`Colours.secondary` (:140), `fillClockFg`=`Colours.onSecondary` (:141), `fillUpdates`=`Colours.tertiary` (:142), `fillUpdatesFg`=`Colours.onTertiary` (:143), `fillNotification`=`Colours.primary` (:144), `fillNotificationFg`=`Colours.onPrimary` (:145).

Register it in `modules/bar/qmldir` with `singleton BarRoles 1.0 BarRoles.qml`, appended as its own commented block following the file's existing convention. Both the `singleton` keyword here and `pragma Singleton` in the source are required (12-06); with only one the type reads `undefined` at every call site with no load error, so verification must assert a resolved value, never merely absence of errors.

Then convert `WorkspaceCapsule.qml`'s focused slot from tint to fill. The live expression at line 349 has THREE branches, not the two D-07's paraphrase quotes — preserve all three (RESEARCH Pitfall 1). Introduce a `Rectangle` behind the slot's `Grid` inside the slot delegate `Item`, radius `height / 2` (the repo's full-pill idiom, matching `BarCapsule.qml:35`), whose `color` is `BarRoles.accent` when `slotFocused` and `"transparent"` otherwise, with the same motion-gated `Behavior on color` shape `BarCapsule.qml:42-49` already uses (`enabled: Motion.motionEnabled`, `ColorAnimation` at `Motion.standardDuration` / `Motion.standardEasing`). Add slot-delegate readonly properties `slotFillColour` and `slotTextColour`; `slotTextColour` resolves `BarRoles.onAccent` when focused, `BarRoles.danger` when urgent-and-unfocused, `BarRoles.capsuleFg` otherwise. Bind the slot's numeral `Text` and both cell `Text` elements to `slotTextColour`, replacing every direct global-palette reference and every bare `workspaceCapsule.contentColour` reference in this file's render path. After the edit, no code line in this file may name the global palette singleton followed by a dot — and do not restate that forbidden token in a trailing comment on a code line either, since trailing comments are stripped by the gate and would hide rather than fix a real violation.

Rewrite the comment block at lines 85-93 so it describes what the code now does. Its current text promises "State is carried by colour instead" while the code applied a tint — that broken promise is the documented origin of this whole phase and must not survive in any form, including a rephrased one.

Do not touch `glyphFor()`, `appIdFor()`, `appGlyphFontFamily`, `activateWorkspace()`, `slotIds`, `persistentSlotCount`, `iconsPerSlot`, or any geometry expression in this file.
  </action>
  <acceptance_criteria>
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` exists and its first non-comment line is `pragma Singleton`.
    - `grep -c 'readonly property color' quickshell/.config/quickshell/modules/bar/BarRoles.qml` returns exactly `17`.
    - `grep -c 'Qt.rgba(' quickshell/.config/quickshell/modules/bar/BarRoles.qml` returns exactly `5`.
    - Each of the strings `0.55`, `0.78`, `0.85`, `0.95`, `0.5` appears in `BarRoles.qml` on a `Qt.rgba(` line.
    - `grep -c '^singleton BarRoles 1.0 BarRoles.qml$' quickshell/.config/quickshell/modules/bar/qmldir` returns exactly `1`.
    - `sed 's|//.*||' quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml | grep -c 'Colours\.'` returns exactly `0`. (Comments are stripped to end-of-line first, so a header note may still discuss the rule — but do not restate the forbidden token in a trailing comment on a code line, since that is stripped too and would merely hide, not fix, a real violation.)
    - `grep -n 'BarRoles.accent' quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` returns at least one hit, and `grep -n 'BarRoles.onAccent'`, `grep -n 'BarRoles.danger'` and `grep -n 'BarRoles.capsuleFg'` each return at least one hit — all three state branches survive.
    - `git diff quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` shows NO change inside `function glyphFor`, `function appIdFor`, `function activateWorkspace`, or the `slotIds` binding.
    - `$HOME/.cache/quickshell.log` carries no new `QML` / `Error` / `is not a type` line dated after this task's edits.
    - A live probe asserts `BarRoles.accent` is a resolved colour and not `undefined` — absence of a load error is explicitly NOT accepted as evidence (12-06's silent-undefined failure mode).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; test "$(grep -c 'readonly property color' quickshell/.config/quickshell/modules/bar/BarRoles.qml)" = 17 &amp;&amp; test "$(grep -c 'Qt.rgba(' quickshell/.config/quickshell/modules/bar/BarRoles.qml)" = 5 &amp;&amp; test "$(grep -c '^singleton BarRoles 1.0 BarRoles.qml$' quickshell/.config/quickshell/modules/bar/qmldir)" = 1 &amp;&amp; test "$(sed 's|//.*||' quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml | grep -c 'Colours\.')" = 0 &amp;&amp; echo TRACER-OK</automated>
    <human-check>The focused workspace slot is a solid accent-coloured pill with contrasting text, not a tinted numeral. Unfocused slots stay neutral. Blur under the bar is unchanged.</human-check>
  </verify>
  <reversibility rating="reversible">BarRoles is purely additive; the WorkspaceCapsule colour change is a single-file revert.</reversibility>
  <done>`BarRoles` resolves at runtime, all 17 rows carry their Athena source in a comment, `WorkspaceCapsule.qml` contains zero `Colours.*` references outside comments, and the focused slot renders as a filled accent pill with the urgent branch preserved as a foreground tint.</done>
</task>

<task type="auto">
  <name>Task 2: Athena state-glyph slot identity and the seven empty glyph literals</name>
  <files>quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml</files>
  <read_first>
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` "Pitfall 2" — the closed D-11/D-27 diagnosis, with the raw-byte evidence that 7 of 12 map entries carry a literal empty string, and the confirmation that `class: "zen"` matches the map key exactly and `appGlyphFontFamily` is already `"FiraCode Nerd Font"`.
    - `waybar/.config/waybar/config-athena.jsonc` lines 200-226 — the `window-rewrite` map, `window-rewrite-default`, and the four-entry `format-icons` state table. Read it with a byte-level tool (`python3 -c` printing codepoints) rather than trusting terminal rendering.
    - `quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` lines 56-107 and 275-402 — the map, the font-family property, the fallback, and the slot delegate as Task 1 left it.
  </read_first>
  <action>
Fill the seven empty-string glyph literals in `appGlyphMap` with Athena's own codepoints, verbatim from `config-athena.jsonc`'s `window-rewrite` object: `firefox` U+E658, `zen` U+F269, `discord` U+F1FF, `spotify` U+F1BC, `net.lutris.Lutris` U+F11B, `steam` U+F1B6, `thunar` U+F07B. Write them as real UTF-8 characters, matching how `kitty` (U+F018D), `codium`/`VSCodium` (U+F0A1E), `obsidian` (U+F0E55) and `yazi` (U+F01E5) are already written in this file. Verify the written bytes by re-reading the file through a codepoint-printing parse — a terminal that renders a glyph as a box proves nothing about the bytes, and a terminal that renders it correctly does not prove it is the right codepoint. Zen's U+F269 is the fa-firefox glyph and is Athena's own deliberate choice (recorded at `config-athena.jsonc:150-151` — no dedicated Zen glyph exists in this font); it is not a transcription error to "correct".

This is a DATA change only. `glyphFor()`'s four matching stages, `appIdFor()`, `appGlyphFontFamily` and `appGlyphFallback` are byte-unchanged. Per D-27 do not add any diagnostic step, do not re-run an appId investigation, and do not touch `Design.symbolFontFamily`.

Then replace the slot's numeral with Athena's three-state glyph identity per D-08. Add capsule-level `readonly property string` rows `stateGlyphActive` (U+F0BAF), `stateGlyphDefault` (U+F02A0) and `stateGlyphUrgent` (U+F09F5), each with a comment naming its `config-athena.jsonc` `format-icons` key, and a `stateGlyphFor(focused, urgent)` pure function resolving them in that precedence order (focused wins over urgent). On the slot delegate add `readonly property string slotStateGlyph` bound to that function. Change the numeral `Text` from `text: String(slotItem.slotId)` to `text: slotItem.slotStateGlyph` and set `font.family: workspaceCapsule.appGlyphFontFamily` on it — the state glyphs are Nerd Font codepoints and will render as tofu in Material Symbols Rounded.

The hidden `numeralMetrics` `Text` reserves the slot's main-axis extent at a two-digit string; retarget it to reserve the widest state glyph instead (set its `text` to `stateGlyphActive` and its `font.family` to `appGlyphFontFamily`), and rename it `slotIdentityMetrics` so the name does not describe a numeral that no longer exists. Update every reference to it in `numeralMainAxisExtent`. Do not change `iconsPerSlot`, `persistentSlotCount`, `Design.iconSizeMd` or any spacing token — the extent formula's SHAPE stays identical, only what it measures changes.

Athena carries a FOURTH state, `empty` (U+F444, rendered at `opacity: 0.6` per `style-athena.scss:180-182`), that D-08 does not name. Do not implement it. Record it in the SUMMARY as a named delta routed to GATE-02, in the same shape `WorkspaceCapsule.qml`'s existing "five slots, not six" delta is recorded — so a windowless-but-existing workspace renders `stateGlyphDefault`, and the operator gets to call the fourth state at the gate rather than discovering it silently absent.

Rewrite the deviation comment at lines 76-93 to describe the restored behaviour and to state plainly the accepted cost D-08 named: the numeral's one-to-one visual mapping onto the Super+N keybinds is gone.
  </action>
  <acceptance_criteria>
    - A codepoint-level read of `appGlyphMap` shows all twelve entries with a non-empty `glyph`, and `firefox`→U+E658, `zen`→U+F269, `discord`→U+F1FF, `spotify`→U+F1BC, `net.lutris.Lutris`→U+F11B, `steam`→U+F1B6, `thunar`→U+F07B exactly.
    - `git diff` shows zero changed lines inside `function glyphFor`, `function appIdFor`, and zero change to `appGlyphFontFamily` or `appGlyphFallback`.
    - `grep -c 'String(slotItem.slotId)' quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` returns `0`.
    - `grep -c 'stateGlyphActive\|stateGlyphDefault\|stateGlyphUrgent' quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` returns at least `6` (three declarations plus their uses).
    - A codepoint read confirms `stateGlyphActive`=U+F0BAF, `stateGlyphDefault`=U+F02A0, `stateGlyphUrgent`=U+F09F5.
    - `grep -c 'numeralMetrics' quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml` returns `0` (renamed to `slotIdentityMetrics`).
    - `fc-list | grep -qi 'FiraCodeNerdFont'` succeeds, confirming the font backing every codepoint above is installed.
    - The SUMMARY contains a named-delta entry for Athena's unimplemented fourth `empty` state (U+F444).
    - `$HOME/.cache/quickshell.log` carries no new QML error line after these edits.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; python3 -c "
import re,sys
s=open('quickshell/.config/quickshell/modules/bar/WorkspaceCapsule.qml',encoding='utf-8').read()
m=re.search(r'appGlyphMap:\s*\[(.*?)\]',s,re.S)
rows=re.findall(r'appId:\s*\"([^\"]+)\",\s*glyph:\s*\"([^\"]*)\"',m.group(1))
assert len(rows)==12, 'expected 12 map rows, got %d'%len(rows)
want={'firefox':0xE658,'zen':0xF269,'discord':0xF1FF,'spotify':0xF1BC,'net.lutris.Lutris':0xF11B,'steam':0xF1B6,'thunar':0xF07B}
for k,g in rows:
    assert g!='', 'empty glyph for '+k
    if k in want: assert ord(g)==want[k], '%s got U+%04X want U+%04X'%(k,ord(g),want[k])
for name,cp in (('stateGlyphActive',0xF0BAF),('stateGlyphDefault',0xF02A0),('stateGlyphUrgent',0xF09F5)):
    mm=re.search(name+r':\s*\"([^\"]+)\"',s); assert mm and ord(mm.group(1))==cp, name
assert 'String(slotItem.slotId)' not in s
assert 'numeralMetrics' not in s
print('GLYPHS-OK')
" &amp;&amp; fc-list | grep -qi FiraCodeNerdFont &amp;&amp; echo FONT-OK</automated>
  </verify>
  <reversibility rating="reversible">Single-file data + binding change; git revert restores the numeral identity.</reversibility>
  <done>All twelve map entries carry the correct Athena codepoint, the slot renders one of three Nerd Font state glyphs instead of a numeral, `glyphFor()` is byte-unchanged, and Athena's fourth `empty` state is recorded as a named GATE-02 delta.</done>
</task>

<task type="auto">
  <name>Task 3: Live screenshot proof that workspace glyphs render, not tofu (D-22)</name>
  <files>.planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png</files>
  <read_first>
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md` — D-22's exact requirement (browser, terminal, file manager open).
    - `hypr/.config/hypr/scripts/quickshell-launch.sh` — the sanctioned launch path, if a restart proves necessary.
    - `.planning/WINDOWS.md` rows 30 and 32 — the repo's own record of what an unrun visual verification looks like when it is filed honestly.
  </read_first>
  <action>
Confirm a browser (`zen` or `firefox`), a terminal (`kitty`) and a file manager (`thunar`) are running by reading `hyprctl clients -j` and extracting each window's `class` field; if any of the three is absent, launch it detached with `uwsm app --` before capturing. Record the observed `class` strings verbatim in the SUMMARY — this is the cheap final confirmation RESEARCH assumption A1 asks for, broadened past the two apps it could check live, and it is a READ, not a new diagnostic effort.

If the shell needs restarting for the qmldir registration to take effect, launch it through `quickshell-launch.sh` under `setsid` so the process survives this agent's shell exiting — a non-detached restart kills the shell silently and the symptom looks exactly like a broken keybind.

Capture the bar region with `grim` to `.planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png`, non-interactively (no `slurp`, no operator pointer input) by computing the bar's geometry from `hyprctl monitors -j`. Then run a mechanical tofu check over the captured image's workspace-capsule region: count distinct non-background pixel clusters and assert the count is greater than one, and assert no cluster is a filled axis-aligned rectangle with a uniform 1px border (the .notdef box shape). Record the numbers in the SUMMARY.

If any part of the capture cannot be performed — no live Wayland session, `grim` denied, apps refuse to launch — do NOT record it as passed. Write the honest state, file a `.planning/WINDOWS.md` `unrun-verify` row naming exactly what was not observed, and hand the visual half to the operator at GATE-02. Four rows of that exact shape already exist in this repo from Phase 18; a fifth honest one costs far less than a false pass.
  </action>
  <acceptance_criteria>
    - `.planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png` exists and is a valid PNG (`file` reports `PNG image data`) with non-zero dimensions — OR a `.planning/WINDOWS.md` `unrun-verify` row exists naming the specific blocker and the deferred observation.
    - The SUMMARY records the verbatim `class` string of each of the three windows read from `hyprctl clients -j`.
    - The SUMMARY records the tofu-check cluster count and the "no .notdef box" result as concrete numbers, not as a prose claim.
    - No claim of visual correctness appears in the SUMMARY that is not backed by either the committed PNG or an explicit "not observed" statement.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; { file .planning/phases/18.1-qml-bar-athena-restoration/screenshots/18.1-01-workspace-glyphs.png 2>/dev/null | grep -q 'PNG image data' || grep -q 'unrun-verify.*18.1-01' .planning/WINDOWS.md; } &amp;&amp; echo SCREENSHOT-EVIDENCE-OK</automated>
  </verify>
  <done>A committed PNG shows the workspace capsule with browser, terminal and file-manager glyphs rendering as real glyphs — or an honest, specific unrun-verify row exists in WINDOWS.md in its place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hyprland compositor → QML | `toplevel.wayland.appId` / `hyprctl clients -j` `class` are attacker-influenceable strings (any application chooses its own app id) crossing into the shell process. |
| matugen / theme engine → `Colours.qml` → `BarRoles.qml` | Colour values arrive from a repo-managed generated file; not remote, not untrusted. |

There is no network surface, no authentication, no session state and no remote input anywhere in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18.1-01 | Tampering | `WorkspaceCapsule.glyphFor(appId)` | low | mitigate | Already mitigated and PRESERVED byte-unchanged by this plan: the untrusted string is only ever the subject of a bounded comparison against a 12-entry repo-literal array, never interpolated into an expression, and the returned text is always one of the repo's own literals or the fallback. Verified at `WorkspaceCapsule.qml:133-157`; acceptance criteria assert a zero-line diff inside the function. |
| T-18.1-02 | Tampering | `WorkspaceCapsule.activateWorkspace(slotId)` | low | mitigate | Already mitigated and untouched: only a range-checked integer (1..10) is ever concatenated into a `Hyprland.dispatch()` string; no appId, window title or address reaches it. Acceptance criteria assert a zero-line diff. |
| T-18.1-03 | Information Disclosure | `grim` screenshot capture (Task 3) | low | mitigate | The capture is cropped to the bar region computed from `hyprctl monitors -j`, never a full-desktop grab, and the artifact is committed into the repo — so any window content that leaked into it would be permanently public. The crop bound is the mitigation; verify the PNG's dimensions match the bar zone before committing. |
| T-18.1-04 | Denial of Service | `BarRoles` unresolved singleton | low | accept | A missing `qmldir` row degrades every consumer's colour to `undefined` rather than crashing the shell — visually broken, not exploitable. Accepted; the acceptance criteria's resolved-value assertion is the practical guard. |
| T-18.1-SC | Tampering | npm/pip/cargo/pacman installs | n/a | accept | This plan installs no packages. RESEARCH's Package Legitimacy Audit records "not applicable"; `FiraCode Nerd Font` is already installed on this host and is read, never installed. |
</threat_model>

<verification>
1. `BarRoles.qml` exists, is a `pragma Singleton`, carries exactly 17 `readonly property color` rows and exactly 5 `Qt.rgba(` alpha rows.
2. `modules/bar/qmldir` carries exactly one `singleton BarRoles 1.0 BarRoles.qml` row.
3. `sed 's|//.*||' WorkspaceCapsule.qml | grep -c 'Colours\.'` is `0`.
4. All twelve `appGlyphMap` glyphs are non-empty and match Athena's codepoints byte-for-byte.
5. The three state glyphs match `format-icons`' `active`/`default`/`urgent` codepoints byte-for-byte.
6. `git diff` shows zero changes inside `glyphFor`, `appIdFor` and `activateWorkspace`.
7. `git diff` touches no geometry token (`barHeight`, `barEdgeMargin`, `barSideMargin`, `barColumnWidth`, `iconsPerSlot`, `persistentSlotCount`).
8. `$HOME/.cache/quickshell.log` shows no new QML load error, AND a live probe confirms `BarRoles.accent` resolves to a colour rather than `undefined`.
9. A committed screenshot or an honest `unrun-verify` WINDOWS.md row exists for D-22.
</verification>

<success_criteria>
The bar's colour role layer exists, resolves at runtime, and is proven end-to-end by exactly one visible consumer: the focused workspace slot renders as a filled `BarRoles.accent` pill with `BarRoles.onAccent` text, the urgent branch survives as a `BarRoles.danger` foreground tint matching Athena's own CSS, every workspace slot shows one of Athena's three Nerd Font state glyphs, and every one of the twelve mapped applications renders a real glyph instead of nothing.
</success_criteria>

<output>
Create `.planning/phases/18.1-qml-bar-athena-restoration/18.1-01-SUMMARY.md` when done.
</output>
