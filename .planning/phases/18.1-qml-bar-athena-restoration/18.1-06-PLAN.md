---
phase: 18.1-qml-bar-athena-restoration
plan: 06
type: execute
wave: 4
depends_on: ["18.1-01", "18.1-02", "18.1-03", "18.1-04", "18.1-05"]
files_modified:
  - hypr/.config/hypr/scripts/quickshell-doctor
  - .planning/WINDOWS.md
autonomous: true
requirements: [GATE-02, QBAR-01, QBAR-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-20: quickshell-doctor carries a new structural check asserting no direct global-palette reference survives in the bar's capsule family, extending the checks already in hypr/.config/hypr/scripts/quickshell-doctor. This converts the exact failure mode that produced today's bar into a check that fails loudly."
    - "D-20: the check is modelled on the established _qsd_assert_panel_source idiom (quickshell-doctor:691-701) — strip comments, grep a forbidden pattern across a directory, count, exempt by basename — and registers itself in the live-run sequence immediately after _qsd_check_bar_surface_registry, using the existing check() reporter."
    - "D-20/Pitfall 3: the grep pattern is the literal `Colours\\.` and BarRoles.qml is exempt by basename — it is the one file legitimately permitted to reference the global palette. `contentColour` needs no exemption because it never appears as a palette-dot reference textually."
    - "The check strips comments to END OF LINE, not just full-comment lines, so a trailing comment cannot hide a violation and a header note restating the rule cannot cause a false failure. A bare uncommented count is forbidden."
    - "The check is proven able to FAIL before it is trusted to pass — a poisoned fixture or a temporary real violation must make it report FAIL, and that demonstration is recorded. A check that has only ever passed has demonstrated nothing."
    - "SCOPE FLAG (must be surfaced, not silently absorbed): D-06 and D-20 say 'no Colours.* under modules/bar/', but roughly sixty such references live in the SEVEN SectionPopout-family files under that same directory (SectionPopout, AudioPopout, WifiPopout, BluetoothPopout, ClockPopout, ResourcesPopout, MediaPopout) — a surface the phase's own scope_fence explicitly places OUT of scope. Count them live rather than trusting this number. Those seven files are exempt by basename with a documented reason and a WINDOWS.md deferred row; the exemption is named, never silent, and the check still fails closed for any NEW file added to the directory."
    - "D-21: the BarRoles table is diffed line-by-line against Athena's theme.scss, every row traced to a named source, and the result recorded as a table rather than as a claim."
    - "D-06 holds for the whole capsule family after this phase: BarCapsule, WorkspaceCapsule, SystemCapsule, ClockActionsCapsule, MediaConnectivityCapsule, LauncherCapsule, BarEntryModel, BarReveal, HotZone, PopoutController and PopoutTrigger all pass the check."
    - "GATE-02 (probe edge — adjacency): this check is a precondition of GATE-02, not a substitute for it — a green doctor run does not make the bar good, and the SUMMARY must not present it as evidence toward the operator's judgment."
    - "QBAR-01 (probe edge — boundary): the check must be robust at the empty-directory and single-file boundaries — a directory that matches zero .qml files must report a SKIP, never a vacuous PASS, since a silently-vacuous pass is exactly how the original failure survived review."
    - "QBAR-06 (plan-authored guard, NOT probe-derived): a bar capsule file containing zero colour bindings at all is a legitimate pass, not an anomaly — the check asserts absence of a forbidden pattern, never presence of a required one."
    - statement: "QBAR-01 (plan-authored guard, NOT probe-derived, checker): if the shell QML root cannot be resolved on the running host, the check reports SKIP with the reason rather than PASS."
      verification: backstop
  artifacts:
    - "hypr/.config/hypr/scripts/quickshell-doctor — new `_qsd_assert_bar_colour_roles` assert half, new `_qsd_check_bar_colour_role_routing` check half, new `QSD_BAR_COLOUR_ROLE_EXEMPT` exemption array, and one new call in the live-run sequence"
    - ".planning/WINDOWS.md — one deferred row recording the SectionPopout-family palette references left unmigrated under the phase scope fence"
    - "The BarRoles-to-theme.scss diff table, recorded in 18.1-06-SUMMARY.md"
  key_links:
    - "_qsd_check_bar_colour_role_routing -> the run-order seam immediately after _qsd_check_bar_surface_registry: the doctor script's checks only execute if they are called; a defined-but-uncalled check function is the same class of dead code as PopoutController.barSettled."
    - "QSD_BAR_COLOUR_ROLE_EXEMPT -> BarRoles.qml: a missing self-exemption makes the check fail on its own foundation file; RESEARCH names this as the specific warning sign of a wrong implementation."
    - "the check's comment-stripping -> the executor's own comments: strip to end-of-line, or a code-line trailing comment naming the forbidden token both hides real violations and creates false ones."
  prohibitions:
    - "Never let an exemption go unnamed. Every basename in the exemption array carries a written reason and, where the reason is 'fenced out of this phase', a WINDOWS.md row so it resurfaces."
    - "Never trust a check that has only ever passed. Prove it fails first."
    - "Never write a check whose empty or missing input yields PASS. Vacuous truth is how the original defect survived review, and this check exists to end that class."
    - "Never present a green mechanical check as evidence that the bar looks good. GATE-02 is the operator's judgment and this check is only its precondition."
    - "Never restate a behavioural promise in a comment that the code does not implement — this check is the executable counterpart of exactly that failure."
    - "Never record a live verification as performed when it was not."
---

<objective>
Turn the rule that this phase's whole failure mode violated into a check that fails loudly, and record the mechanical `BarRoles`-to-`theme.scss` diff that makes the colour table auditable.

Purpose: `WorkspaceCapsule.qml:90` promised colour-carried state in a comment while the code applied a tint, and nothing caught it for a whole phase. D-20 exists so that cannot happen silently again. This is the executable counterpart of the phase's central prohibition.
Output: a `quickshell-doctor` check that is proven able to fail, a named exemption set with no silent members, and a committed line-by-line colour-table diff.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-01-SUMMARY.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-03-SUMMARY.md
@quickshell/.config/quickshell/modules/bar/BarRoles.qml
@waybar/.config/waybar/theme.scss
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces (new symbols — exclude from drift verification)

**New `quickshell-doctor` shell functions and variables:**
`_qsd_assert_bar_colour_roles` (the assert half — scans a directory, strips comments, counts violations),
`_qsd_check_bar_colour_role_routing` (the check half — resolves the scan root, calls the assert half,
reports through the existing `check()` reporter), and `QSD_BAR_COLOUR_ROLE_EXEMPT` (the named
exemption array).

**Consumed (pre-existing, not created here):** `check()` (`quickshell-doctor:302-312`),
`_qsd_shell_qml_root()` (`:407`), `_qsd_assert_panel_source()` (`:691-701`, the idiom being modelled),
and the run-order seam at `_qsd_check_bar_surface_registry` (`:2734`).

**Consumed (created by plan 18.1-01, not drift):** the `BarRoles` singleton and its 17 role properties
`barSurface`, `barSurfaceHover`, `capsule`, `capsuleFg`, `capsuleHover`, `capsuleTrack`, `accent`,
`onAccent`, `warn`, `danger`, `onDanger`, `fillClock`, `fillClockFg`, `fillUpdates`, `fillUpdatesFg`,
`fillNotification`, `fillNotificationFg`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The colour-routing check, with every exemption named</name>
  <files>hypr/.config/hypr/scripts/quickshell-doctor, .planning/WINDOWS.md</files>
  <precondition>Every migration plan in this phase (18.1-01 through 18.1-05) has landed — this check can only pass once every consumer is migrated, and running it against a half-migrated tree produces a failure that says nothing about the check's own correctness. Confirm with a live `grep -rn 'Colours\.' quickshell/.config/quickshell/modules/bar/*.qml` before starting and reconcile the result against the exemption set below.</precondition>
  <read_first>
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 296-320 — the `check(desc, ok)` reporter and its PASS/FAIL counters.
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 400-415 — `_qsd_shell_qml_root()`, the sanctioned way to resolve the shell's QML root on this host.
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 685-702 — `_qsd_assert_panel_source()`, the exact idiom to model: `find -L "$dir" -maxdepth 1 -type f -name '*.qml'`, skip by `basename`, strip comments, grep, count, `printf 'bad=%s'`, return the boolean.
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 1697-1715 and 2725-2744 — how `_qsd_check_bar_surface_registry` composes its assert halves and reports, and the exact run-order seam where the new call goes.
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 1755-1770 — a compact check half with a SKIP branch, showing the established SKIP wording for a missing input.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` "Pitfall 3" — the two warning signs of a wrong implementation (failing on its own new file; exempting something that should have been caught because the pattern was copied from the wrong check).
    - Live output of `grep -rn 'Colours\.' quickshell/.config/quickshell/modules/bar/*.qml` — the actual, current violation set. Reconcile it against the exemption list in the action below and report any difference.
  </read_first>
  <action>
Add `_qsd_assert_bar_colour_roles` to `quickshell-doctor`, modelled directly on `_qsd_assert_panel_source`. It takes a directory, iterates `find -L "$dir" -maxdepth 1 -type f -name '*.qml'`, skips any file whose `basename` appears in the `QSD_BAR_COLOUR_ROLE_EXEMPT` array, strips comments from each remaining file to END OF LINE (a `sed` substitution removing `//` through end of line — not `grep -vE '^\s*//'`, which drops only whole-comment lines and lets a trailing comment on a code line hide a real violation), greps the stripped text for the literal pattern `Colours\.`, and counts offenders. Emit a machine-readable `printf` of the count AND the offending basenames, so a failure names what to fix rather than only that something is wrong. Return non-zero when the count is above zero.

Populate `QSD_BAR_COLOUR_ROLE_EXEMPT` with a written reason beside every member:
- `BarRoles.qml` — the one file legitimately permitted to reference the global palette; it IS the role layer. Omitting this self-exemption makes the check fail on its own foundation, which RESEARCH names as warning sign one.
- `SectionPopout.qml`, `AudioPopout.qml`, `WifiPopout.qml`, `BluetoothPopout.qml`, `ClockPopout.qml`, `ResourcesPopout.qml`, `MediaPopout.qml` — the `SectionPopout` family. These are separate anchored surfaces with their own `surfaceBase`, and phase 18.1's `scope_fence` places popout content and the `SectionPopout` framework explicitly OUT of scope. They are not bar capsules and their palette references were never part of the fill-versus-tint defect this rule exists to catch. Record the count found at the time of writing so a future reader can tell whether the set grew.

Do NOT exempt `contentColour` or `capsuleRoot.*` — they need no exemption, because neither ever appears as a palette-dot reference textually. Adding a pattern exemption for them would silently widen the check's blind spot.

Handle both empty boundaries explicitly. If `_qsd_shell_qml_root` does not resolve, or the bar directory does not exist, report `SKIP` with the reason using the established SKIP wording — never `PASS`. If the directory resolves but `find` returns zero non-exempt files, that is also a `SKIP`, not a pass: a check that reports green over an empty scan set is the exact vacuous-truth shape that let the original defect through review, and this check exists to end that class.

Add `_qsd_check_bar_colour_role_routing` as the check half: resolve the scan root via `_qsd_shell_qml_root`, append the bar subdirectory, call the assert half, and report through the existing `check()` reporter with a description naming D-20, the scanned directory, the offender count and the exempted basenames. Register it in the live-run sequence immediately after the `_qsd_check_bar_surface_registry` call, before the remaining checks and the closing summary — a defined-but-uncalled check function is the same class of dead code as the `barSettled` latch this phase refuses to copy.

File one `.planning/WINDOWS.md` row recording the `SectionPopout`-family exemption: the rule as written in D-06/D-20 says the whole of `modules/bar/`, the phase's own scope fence excludes the popout family, and those files therefore keep their palette references. Give the row the live-counted reference total, name the seven exempted popout-family files, and state plainly that this is a deferred decision for a later phase, not a closed one. An exemption that never resurfaces is indistinguishable from a rule nobody enforces.
  </action>
  <acceptance_criteria>
    - `grep -c '_qsd_assert_bar_colour_roles' hypr/.config/hypr/scripts/quickshell-doctor` returns at least `2` (definition plus call).
    - `grep -c '_qsd_check_bar_colour_role_routing' hypr/.config/hypr/scripts/quickshell-doctor` returns exactly `2` — defined once and CALLED once in the run sequence.
    - The call to `_qsd_check_bar_colour_role_routing` appears after the `_qsd_check_bar_surface_registry` call line and before the `Summary:` echo, verified by comparing their line numbers.
    - `grep -c 'QSD_BAR_COLOUR_ROLE_EXEMPT' hypr/.config/hypr/scripts/quickshell-doctor` returns at least `2`.
    - The exemption array contains exactly eight basenames: `BarRoles.qml` plus the seven `SectionPopout`-family files (`SectionPopout.qml`, `AudioPopout.qml`, `WifiPopout.qml`, `BluetoothPopout.qml`, `ClockPopout.qml`, `ResourcesPopout.qml`, `MediaPopout.qml`); each has a reason on an adjacent comment line.
    - The assert half strips comments with a `sed`-style end-of-line substitution, not with `grep -vE '^\s*//'`, verified by reading the function.
    - The assert half's grep pattern is the literal `Colours\.`; `grep -c 'contentColour' ` inside the new function body returns `0` — no alias exemption was invented.
    - Running `bash -n hypr/.config/hypr/scripts/quickshell-doctor` exits 0 (the script still parses).
    - Running the doctor reports the new check as `[PASS]` with a zero offender count and names the exempted basenames in its description.
    - `.planning/WINDOWS.md` gains exactly one new row naming the `SectionPopout`-family exemption, its reference count, the eight files and the deferral.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; D=hypr/.config/hypr/scripts/quickshell-doctor &amp;&amp; bash -n $D &amp;&amp; test "$(grep -c '_qsd_check_bar_colour_role_routing' $D)" = 2 &amp;&amp; grep -q 'QSD_BAR_COLOUR_ROLE_EXEMPT' $D &amp;&amp; A=$(grep -n '^_qsd_check_bar_surface_registry$' $D | tail -1 | cut -d: -f1) &amp;&amp; B=$(grep -n '^_qsd_check_bar_colour_role_routing$' $D | tail -1 | cut -d: -f1) &amp;&amp; C=$(grep -n 'Summary: ' $D | tail -1 | cut -d: -f1) &amp;&amp; test "$B" -gt "$A" &amp;&amp; test "$B" -lt "$C" &amp;&amp; echo "CHECK-REGISTERED-OK after=$A new=$B summary=$C"</automated>
  </verify>
  <reversibility rating="reversible">Two new shell functions, one array and one call site in a script.</reversibility>
  <done>The doctor carries a colour-routing check that is called in the run sequence, reports PASS with a zero offender count over the capsule family, names every exemption with a reason, reports SKIP rather than PASS on any empty input, and the popout-family exemption is filed as a deferred WINDOWS.md row.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the check can fail before trusting it to pass</name>
  <files>hypr/.config/hypr/scripts/quickshell-doctor</files>
  <read_first>
    - `hypr/.config/hypr/scripts/quickshell-doctor` — the new functions as Task 1 left them.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` "Pitfall 3" — the two named warning signs, each of which is one of the falsifications below.
    - This repository's own precedent for this discipline: prior phases proved a gate could fail by fault injection before trusting it (the `hypr-equivalence-check` degradation proofs and `motion-lint`'s poisoned fixtures are the established shape). Follow that shape rather than inventing a new one.
  </read_first>
  <action>
Run four falsifications, restoring the tree after each, and record each one's observed output in the SUMMARY. A check that has only ever passed has demonstrated nothing, and this particular check exists because a rule that lived only in a comment went unenforced for a whole phase.

1. **Real violation detected.** Temporarily add a direct palette reference on a code line in one non-exempt bar capsule file. Run the doctor. The new check must report FAIL and must NAME that file in its output. Restore.

2. **Self-exemption works.** Confirm the check reports PASS while `BarRoles.qml` — which necessarily references the global palette on many lines — sits in the scanned directory. This is RESEARCH's warning sign one, tested directly rather than assumed.

3. **Trailing-comment hiding is closed.** Temporarily add a violation as a trailing comment on an existing code line (a real code line, with the forbidden token after `//`). The check must report PASS, proving the end-of-line strip works as intended and that a comment cannot cause a false failure. Then move the same token onto the code half of that line and confirm it now FAILS. Restore. Both halves are needed: the first proves no false positives, the second proves the strip did not swallow the real case.

4. **Empty input does not pass vacuously.** Point the assert half at a directory containing no `.qml` files, or at a nonexistent path. It must report SKIP with a reason, never PASS. This is the vacuous-truth case, and the acceptance criteria in Task 1 depend on it holding.

After all four, confirm the working tree is byte-identical to its pre-falsification state (`git status --porcelain` clean for every touched QML file) before finishing. A falsification left in place is a poisoned tree, and this repo has an established fault-injection discipline precisely because that mistake is easy to make under time pressure.

Record each falsification's exact observed doctor output line in the SUMMARY. Do not paraphrase it and do not record a falsification that was not actually run.
  </action>
  <acceptance_criteria>
    - The SUMMARY records four falsifications, each with the verbatim `[PASS]` or `[FAIL]` line the doctor emitted.
    - Falsification 1's recorded FAIL line names the specific file that was poisoned.
    - Falsification 3 records BOTH halves: a trailing-comment violation reporting PASS, and the same token moved onto the code half reporting FAIL.
    - Falsification 4's recorded line is a `[SKIP]` with a reason, and is neither `[PASS]` nor `[FAIL]`.
    - `git status --porcelain quickshell/.config/quickshell/modules/bar/` returns empty at the end of the task — no poison survived.
    - `git diff hypr/.config/hypr/scripts/quickshell-doctor` shows no change from this task (the falsifications exercise the check; they do not edit it).
    - A full doctor run afterwards reports the new check as `[PASS]`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; test -z "$(git status --porcelain quickshell/.config/quickshell/modules/bar/)" &amp;&amp; bash -n hypr/.config/hypr/scripts/quickshell-doctor &amp;&amp; echo "TREE-CLEAN-AFTER-FALSIFICATION-OK"</automated>
  </verify>
  <reversibility rating="reversible">Verification only; the tree is restored to its pre-task state.</reversibility>
  <done>All four falsifications are recorded with verbatim doctor output, the check is proven to catch a real violation and to name it, proven not to false-positive on a comment, proven not to pass vacuously on empty input, and the working tree is clean.</done>
</task>

<task type="auto">
  <name>Task 3: The line-by-line BarRoles-to-theme.scss diff (D-21)</name>
  <files>hypr/.config/hypr/scripts/quickshell-doctor</files>
  <read_first>
    - `waybar/.config/waybar/theme.scss` lines 50-150 — every `@define-color` row this phase consumes, with its own comment explaining the alpha tuning.
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` as plan 18.1-01 wrote it, including the per-row source comments.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md` D-04 and D-21 — the authoritative table and the requirement that the diff be mechanical.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` "Summary" — the confirmation that the table was already verified byte-exact with zero corrections; this task RE-runs that verification against the shipped file rather than re-deciding the table.
  </read_first>
  <action>
Produce the D-21 diff as a recorded table in the SUMMARY: one row per `BarRoles` property, carrying the property name, the `Colours.*` source it resolves, the alpha value if any, the Athena `theme.scss` role name, and that role's `theme.scss` line number. Seventeen rows, no gaps.

Derive both sides mechanically rather than by reading and retyping — extract the `BarRoles` rows from the QML source and the `@define-color` rows from `theme.scss`, and compare them programmatically so the table is generated, not transcribed. A hand-typed table is exactly as trustworthy as the comment that started this phase.

Assert three things and state each result explicitly:
- Every one of the seventeen `BarRoles` rows maps to a named `theme.scss` role. A row with no named source is a defect in the role layer, not a formatting problem.
- Every alpha value matches its `theme.scss` counterpart exactly: 0.55, 0.78, 0.85, 0.95, 0.5. No rounding, no substitution, no "close enough".
- Every `Colours.*` source named in `BarRoles.qml` exists as a real property on `Colours.qml`, so no row silently resolves undefined. A `color` property bound to an undefined source renders as an invalid colour rather than raising an error, which is precisely the kind of failure this phase is paying for once already.

If any row disagrees, do NOT adjust `BarRoles.qml` to make the diff pass. Report the disagreement — RESEARCH diffed this table byte-for-byte and found zero corrections needed, so a disagreement now means either the shipped file drifted from the decision or `theme.scss` changed, and both are findings the operator needs rather than defects the executor should quietly reconcile.

State plainly in the SUMMARY that this mechanical diff and Task 1's check are PRECONDITIONS of GATE-02, not evidence toward it. The bar being provably wired to the right colours says nothing about whether it looks better than Athena, which is the only question GATE-02 asks.
  </action>
  <acceptance_criteria>
    - The SUMMARY contains a seventeen-row table with columns: `BarRoles` property, `Colours.*` source, alpha (or `—`), Athena role name, `theme.scss` line number.
    - No row has an empty Athena role name or an empty `theme.scss` line number.
    - The five alpha values recorded are exactly `0.55`, `0.78`, `0.85`, `0.95`, `0.5`, each matched to its `theme.scss` line.
    - The SUMMARY records the method used to generate the table and states that it was generated programmatically rather than transcribed.
    - Every `Colours.*` source named in `BarRoles.qml` is confirmed present in `Colours.qml`, with the confirmation method recorded.
    - Any disagreement between the two sides is reported as a finding, not silently reconciled; if there were none, the SUMMARY says so explicitly.
    - The SUMMARY states that this diff and the doctor check are preconditions of GATE-02 and not evidence toward the operator's judgment.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; python3 -c "
import re
q=open('quickshell/.config/quickshell/modules/bar/BarRoles.qml',encoding='utf-8').read()
q=re.sub(r'//.*','',q)
rows=re.findall(r'readonly property color (\w+)\s*:\s*(.+)',q)
assert len(rows)==17, 'expected 17 BarRoles rows, got %d'%len(rows)
alphas=sorted(re.findall(r'Qt\.rgba\([^)]*?,\s*([0-9.]+)\s*\)',' '.join(v for _,v in rows)))
assert alphas==['0.5','0.55','0.78','0.85','0.95'], alphas
srcs={re.findall(r'Colours\.(\w+)',v) for _,v in rows}
names=set()
for _,v in rows: names.update(re.findall(r'Colours\.(\w+)',v))
c=open('quickshell/.config/quickshell/modules/Colours.qml',encoding='utf-8').read()
missing=[n for n in sorted(names) if not re.search(r'property\s+\w+\s+'+n+r'\b',c) and not re.search(r'alias\s+'+n+r'\b',c)]
assert not missing, 'BarRoles names absent from Colours.qml: '+repr(missing)
print('ROLE-TABLE-OK rows=%d alphas=%s sources=%d'%(len(rows),alphas,len(names)))
"</automated>
  </verify>
  <reversibility rating="reversible">Verification and documentation only.</reversibility>
  <done>A generated seventeen-row table traces every `BarRoles` property to a named `theme.scss` role and line, all five alpha values match byte-exactly, every `Colours.*` source is confirmed to exist, and the SUMMARY states that this is a precondition of GATE-02 rather than evidence toward it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem paths → the new doctor check's `find`/`grep`/`sed` invocations | Paths originate from `_qsd_shell_qml_root()` and repo-literal subdirectory names, and pass through a shell. Filenames on this host are not attacker-controlled, but a path containing whitespace or a glob metacharacter would break the check silently. |
| `theme.scss` / `BarRoles.qml` file content → Task 3's parser | Repo-managed source files, read and parsed but never executed. |

No network surface, no authentication, no remote input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18.1-22 | Tampering | Shell quoting in `_qsd_assert_bar_colour_roles` | medium | mitigate | Every path expansion is double-quoted, `find` output is consumed through a `while IFS= read -r` loop with process substitution (the exact shape `_qsd_assert_panel_source` already uses), `grep`/`sed` arguments are separated with `--`, and the forbidden pattern is a fixed literal never composed from a variable. This is the one place in the phase where an unquoted expansion could reach a shell. |
| T-18.1-23 | Repudiation | A check that cannot fail reported as passing | high | mitigate | Task 2 is the dedicated mitigation: four falsifications, each recorded with verbatim doctor output, covering a real violation, the self-exemption, both halves of the comment-strip behaviour, and the empty-input case. Without this, a check with a typo'd pattern reports green forever and is strictly worse than no check — it converts an open risk into a false assurance. |
| T-18.1-24 | Repudiation | A silent exemption hiding a real rule violation | high | mitigate | Every exemption carries a written reason, and the `SectionPopout`-family exemption additionally gets a `.planning/WINDOWS.md` deferred row with its current reference count so it resurfaces. The check still fails closed for any NEW file added to the directory, so the exemption cannot grow silently. |
| T-18.1-25 | Denial of Service | A vacuous PASS on empty or unresolvable input | high | mitigate | Both empty boundaries report `SKIP` with a reason rather than `PASS`, asserted in Task 1's criteria and demonstrated by falsification 4. Vacuous truth is the specific mechanism by which the original defect survived review. |
| T-18.1-26 | Tampering | A falsification left in the working tree | medium | mitigate | Task 2's acceptance criteria require `git status --porcelain` to be empty for the bar directory at task end, and the automated verify asserts it. |
| T-18.1-SC | Tampering | npm/pip/cargo/pacman installs | n/a | accept | This plan installs no packages; RESEARCH's Package Legitimacy Audit records "not applicable" for this phase. Task 3's parser uses the host Python already present. |
</threat_model>

<verification>
1. `_qsd_check_bar_colour_role_routing` is defined once and called once, and its call sits after `_qsd_check_bar_surface_registry` and before the summary echo.
2. `bash -n hypr/.config/hypr/scripts/quickshell-doctor` exits 0.
3. The exemption array holds exactly nine named basenames, each with a written reason, and no alias-pattern exemption was invented.
4. Comments are stripped to end of line, and the grep pattern is the literal `Colours\.`.
5. Both empty boundaries report SKIP, never PASS.
6. Four falsifications are recorded with verbatim doctor output, and the working tree is clean afterwards.
7. A generated seventeen-row table traces every `BarRoles` row to a named `theme.scss` role and line, with all five alpha values matching exactly and every `Colours.*` source confirmed present.
8. One `.planning/WINDOWS.md` row records the `SectionPopout`-family exemption as a deferred, named decision.
9. A full doctor run reports the new check as `[PASS]`.
</verification>

<success_criteria>
The failure mode that produced this bar — a rule stated in a comment and enforced nowhere — is now a `quickshell-doctor` check that has been proven able to fail, names every file it exempts and why, refuses to pass on empty input, and sits in the live run sequence; and the colour table it guards has a generated, line-by-line diff against Athena's own `theme.scss` with zero unexplained rows.
</success_criteria>

<output>
Create `.planning/phases/18.1-qml-bar-athena-restoration/18.1-06-SUMMARY.md` when done.
</output>
