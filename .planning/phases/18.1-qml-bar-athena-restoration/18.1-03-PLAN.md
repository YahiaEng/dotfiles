---
phase: 18.1-qml-bar-athena-restoration
plan: 03
type: execute
wave: 2
depends_on: ["18.1-01"]
files_modified:
  - quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml
  - quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml
autonomous: true
requirements: [QBAR-01, QBAR-04, QBAR-06]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-13: the clock renders on a BarRoles.fillClock background with BarRoles.fillClockFg content — Athena's secondary hue, the second of its three filled pills, and the reason `Colours.secondary` went from never-referenced-under-modules/bar to referenced."
    - "D-13: the notification entry fills with BarRoles.fillNotification / BarRoles.fillNotificationFg when the unread count is non-zero, and returns to the neutral capsule content colour when it is zero — Athena's #custom-notification.notification rule read literally."
    - "D-24: the five action-cell tints in ClockActionsCapsule move onto BarRoles rows. The rule is literal and extends past D-04's named table; the live grep-derived set is the badge fill and its label (currently lines 163 and 172) plus the gaming, notification and idle-inhibitor cell tints (currently 410, 427, 439). CONTEXT's D-24 cites line 129, which carries `property color tint: clockActionsCapsule.contentColour` and holds no palette token at all — the actual fifth reference is the badge label. Reconcile against a live grep, never against the cited line numbers."
    - "D-06: after this plan, neither ClockActionsCapsule.qml nor MediaConnectivityCapsule.qml contains a direct global-palette reference on a code line. MediaConnectivityCapsule's error tint is the one bar-capsule reference RESEARCH's violation list omitted; it is in scope because it is a capsule, not a popout."
    - "D-24: the gaming and idle-inhibitor active tints map to BarRoles.accent and the notification-unavailable tint to BarRoles.danger — both are already-named Athena roles (@accent on #idle_inhibitor.activated and #custom-gaming-mode.on, @danger on failure), so no role is invented for them."
    - "QBAR-06 (empty edge): a notification source that reports zero unread renders no badge at all and the bell cell falls back to the neutral capsule content colour — the fill is state, not decoration, and an empty state must render as empty rather than as a zero-count badge."
    - "QBAR-06 (boundary edge): the unread count at exactly 9 renders `9`, and at exactly 10 renders `9+` — the existing `> 9` clamp is preserved byte-unchanged so the fill change cannot silently shift the label boundary."
    - "QBAR-04 (adjacency edge): adding a filled background behind the clock and the bell must not swallow pointer events that the scroll-adjustable sections rely on — the new fill Rectangles are siblings BEHIND their content with no MouseArea and no HoverHandler of their own, so no hit area is added or moved."
    - "QBAR-01 (empty edge): a filled pill whose visibility condition is false contributes zero extent and zero positioner spacing, so the capsule reflows with no gap — the same QtQuick positioner mechanism BarCapsule already relies on."
    - statement: "QBAR-02 (boundary edge): in vertical orientation at barColumnWidth 44 a filled pill's radius resolves to width/2 and the fill must not overflow the capsule's own rounded bounds at either orientation."
      verification: backstop
    - statement: "QBAR-02 (ordering edge): capsule and cell declaration order in ClockActionsCapsule is unchanged by this plan — clock, gaming, notifications, idle inhibitor, settings, power, in that order."
      verification: backstop
  artifacts:
    - "quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml (fillClock clock pill, fillNotification bell pill, five action-cell tints on BarRoles)"
    - "quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml (error tint on BarRoles.danger)"
  key_links:
    - "ClockActionsCapsule clock Grid -> clockFillPill: the fill must sit behind the clock's existing time and date Text elements without changing their positioner, or the pill and the text drift apart in one orientation."
    - "ActionCell.fillColour -> the bell cell's unread state: the ONLY input is notificationSource.unreadCount, the same input the existing badge and `filled` variable-axis already read — no second source of truth for 'there are notifications'."
    - "MediaConnectivityCapsule error tint -> BarRoles.danger: closes the last bar-capsule palette reference, which is what lets plan 18.1-06's doctor check pass over the capsule family."
  prohibitions:
    - "Never restate a behavioural promise in a comment that the code does not implement — the failure that produced this bar."
    - "Never reduce a locked decision to a 'v1', 'static for now' or 'placeholder' form."
    - "Never touch the bar's geometry tokens (cellPitch, iconSizeMd, spacing tokens, barHeight) to chase 'big and clunky'."
    - "Never trust a line number cited in a design document over a live grep of the file. D-24's own cited line 129 does not hold what it claims to."
    - "Never record a live visual verification as performed when it was not."
---

<!-- planner-discipline-allow: id: clockFillPill -->
<!-- planner-discipline-allow: BarRoles.fillClockFg -->
<!-- Both literals are named in <action> bodies because each is a symbol the executor must CREATE
     by that exact name. Their paired acceptance criteria assert positive counts (1 and 2); the only
     `= 0` test on those chained verify lines is the unrelated palette-token gate over the QML
     source, which neither literal can affect. -->

<objective>
Give the clock and the notification bell Athena's filled-pill treatment, and move every remaining action-cell tint in the clock/actions capsule — plus the one error tint the research sweep missed on the media/connectivity capsule — onto `BarRoles`.

Purpose: `fillClock` is Athena's `secondary` hue, which the QML bar never referenced once; restoring it is a large part of the "no colors" verdict. Finishing the tint migration is also what makes plan 18.1-06's doctor check reachable at all.
Output: three of Athena's filled pills present on the bar (accent workspace from plan 01, secondary clock and primary notification here), and a capsule family with zero direct palette references.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-01-SUMMARY.md
@quickshell/.config/quickshell/modules/bar/BarRoles.qml
@quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces (new symbols — exclude from drift verification)

**New `ClockActionsCapsule` nodes/properties:**
`clockFillPill` (a `Rectangle` behind the clock content), and on the `ActionCell` component:
`fillColour`, `fillActive`, plus the cell-level fill `Rectangle` `cellFillPill`.

**Consumed (created by plan 18.1-01, not drift):** the `BarRoles` singleton and its 17 role properties
`barSurface`, `barSurfaceHover`, `capsule`, `capsuleFg`, `capsuleHover`, `capsuleTrack`, `accent`,
`onAccent`, `warn`, `danger`, `onDanger`, `fillClock`, `fillClockFg`, `fillUpdates`, `fillUpdatesFg`,
`fillNotification`, `fillNotificationFg`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The clock as Athena's fillClock pill</name>
  <files>quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` lines 1-115 — the file header, the clock block and its two `Text` children (time and, in vertical only, weekday), and the `cellPitch` derivation.
    - `waybar/.config/waybar/style-athena.scss` lines 276-286 — `#clock` is `padding: 6px 14px; font-weight: bold; background: @fill-clock; color: @fill-clock-fg`, and the comment above it explaining that this rule deliberately overrides the neutral `@capsule` background from the shared rule.
    - `waybar/.config/waybar/theme.scss` lines 127-145 — the "filled, but fewer" comment block: exactly three filled hues exist and every other pill stays neutral. Do not add a fourth.
    - `quickshell/.config/quickshell/modules/bar/BarCapsule.qml` lines 35-49 — the full-pill `radius` idiom and the motion-gated `Behavior on color` shape to copy.
  </read_first>
  <action>
Add a `Rectangle` with `id: clockFillPill` behind the clock's content, sized to the clock content's own implicit extent plus the capsule's existing spacing, `radius: height / 2` in horizontal and `width / 2` in vertical using the same `vertical ?` expression `BarCapsule.qml:35` uses, `color: BarRoles.fillClock`, and the same motion-gated `Behavior on color` block (`enabled: Motion.motionEnabled`, `ColorAnimation` at `Motion.standardDuration` / `Motion.standardEasing`). The pill is a sibling declared BEFORE the clock content so it renders behind it, with no `MouseArea` and no `HoverHandler` of its own — it must not become a hit target, because the bar's existing hover and scroll contracts route through handlers this plan does not touch.

Bind the clock's time `Text` and its vertical-only weekday `Text` to `BarRoles.fillClockFg`, replacing `clockActionsCapsule.contentColour` on those two elements only. Do not change `contentColour` itself and do not change any other cell's colour in this task.

Athena's `#clock` is `font-weight: bold`. If `Design.qml` exposes a bold weight token, apply it to the time `Text`; if it does not, leave the weight as-is and record the omission in the SUMMARY. Do not introduce a raw numeric font weight — this repo's token discipline is enforced by a lint that would flag it, and an unflagged raw weight here would be a silent second convention.

Do not change `cellPitch`, `Design.iconSizeMd`, any spacing token, or the capsule's content `Grid`. Athena's `#clock` padding is `6px 14px`, which is geometry; this phase's geometry is fenced and measured identical already, so reproduce the COLOUR of Athena's clock rule, not its padding.
  </action>
  <acceptance_criteria>
    - `grep -c 'id: clockFillPill' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns exactly `1`.
    - `grep -c 'BarRoles.fillClock\b' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns at least `1` and `grep -c 'BarRoles.fillClockFg'` returns exactly `2` (the time `Text` and the weekday `Text`).
    - The block containing `id: clockFillPill` contains no `MouseArea` and no `HoverHandler`, verified by reading the declaration.
    - `git diff quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` introduces no change to `readonly property int cellPitch`, and introduces no bare numeric literal on a `padding`, `spacing`, `width` or `height` line.
    - `grep -c 'font.weight: [0-9]' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns `0` — no raw numeric weight was minted.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after the edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; F=quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml &amp;&amp; test "$(grep -c 'id: clockFillPill' $F)" = 1 &amp;&amp; test "$(grep -c 'BarRoles.fillClockFg' $F)" = 2 &amp;&amp; test "$(grep -c 'font.weight: [0-9]' $F)" = 0 &amp;&amp; git diff --unified=0 -- $F | grep -q 'cellPitch' &amp;&amp; echo "FAIL: cellPitch changed" &amp;&amp; exit 1; echo CLOCK-FILL-OK</automated>
    <human-check>The clock sits on a solid coloured pill in a hue clearly distinct from both the neutral capsules and the accent workspace pill, with legible contrasting text.</human-check>
  </verify>
  <reversibility rating="reversible">One added Rectangle plus two colour bindings in one file.</reversibility>
  <done>The clock renders on Athena's `secondary`-hued filled pill with its on-colour text, the pill is not a hit target, and no geometry token moved.</done>
</task>

<task type="auto">
  <name>Task 2: The notification bell as a state-driven fillNotification pill</name>
  <files>quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` lines 117-193 — the `ActionCell` component: its `tint`, `filled`, `available`, `badgeVisible`, `badgeText` properties, the glyph `Text` with its `FILL` variable axis, the badge `Rectangle` and its label, and the `MouseArea` with its tooltip.
    - `quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` lines 414-432 — the live `bellCell` instance: its four-branch glyph expression, its `filled`, its `tint`, its badge bindings, and its click/right-click handlers.
    - `waybar/.config/waybar/style-athena.scss` lines 320-345 — `#custom-notification` is neutral at rest and takes `background: @fill-notification; color: @fill-notification-fg` only on the notification/dnd/inhibited classes.
  </read_first>
  <action>
Give `ActionCell` an optional filled background. Add `property bool fillActive: false` and `property color fillColour: "transparent"`, plus a `Rectangle` with `id: cellFillPill` declared as the FIRST child of the cell so it renders behind the glyph — `anchors.fill: parent`, `radius: height / 2`, `visible: cellItem.fillActive`, `color: cellItem.fillColour`, with the same motion-gated `Behavior on color` shape used elsewhere in the bar, and no `MouseArea` or `HoverHandler` of its own. Every existing `ActionCell` instance leaves `fillActive` at its `false` default, so this addition changes nothing anywhere it is not explicitly opted into — Athena has exactly three filled hues and this must not become a fourth.

On `bellCell` set `fillActive` to the same non-zero-unread condition its `filled` variable-axis already reads (`notificationSource.unreadCount > 0`), and `fillColour: BarRoles.fillNotification`. Change its `tint` so it resolves `BarRoles.fillNotificationFg` when that same condition holds, `BarRoles.danger` when the notification source is unavailable, and `clockActionsCapsule.contentColour` otherwise — a three-branch expression replacing the current two-branch one, with the unavailable branch preserved rather than dropped.

Read the unread count from `notificationSource.unreadCount` and nowhere else. The badge, the `FILL` variable axis and the new fill must all derive from that one input, so there is no way for the pill to be filled while the badge says zero.

Preserve the badge's `> 9` clamp byte-unchanged: at exactly 9 the badge reads `9`, at 10 it reads `9+`. Preserve `onClicked: notificationSource.openCentre()` and `onRightClicked: notificationSource.toggleDnd()` exactly. Do not change the four-branch glyph expression.
  </action>
  <acceptance_criteria>
    - `grep -c 'property bool fillActive' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns exactly `1` and `grep -c 'id: cellFillPill'` returns exactly `1`.
    - `grep -c 'fillActive:' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns exactly `1` — only `bellCell` opts in; no other `ActionCell` instance sets it.
    - `grep -c 'BarRoles.fillNotification\b' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns at least `1` and `grep -c 'BarRoles.fillNotificationFg'` returns at least `1`.
    - `grep -c 'notificationSource.unreadCount > 9 ? "9+"' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns exactly `1` — the badge clamp is byte-unchanged.
    - `grep -c 'onClicked: notificationSource.openCentre()' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns exactly `1` and `grep -c 'onRightClicked: notificationSource.toggleDnd()'` returns exactly `1`.
    - The `cellFillPill` declaration contains no `MouseArea` and no `HoverHandler`.
    - `grep -c 'unreadCount' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` shows every occurrence reading `notificationSource.unreadCount` — no second source of unread state was introduced.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after the edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; F=quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml &amp;&amp; test "$(grep -c 'property bool fillActive' $F)" = 1 &amp;&amp; test "$(grep -c 'id: cellFillPill' $F)" = 1 &amp;&amp; test "$(grep -c 'fillActive:' $F)" = 1 &amp;&amp; grep -q 'BarRoles.fillNotificationFg' $F &amp;&amp; test "$(grep -c 'notificationSource.unreadCount > 9 ? \"9+\"' $F)" = 1 &amp;&amp; test "$(grep -c 'onRightClicked: notificationSource.toggleDnd()' $F)" = 1 &amp;&amp; echo BELL-FILL-OK</automated>
    <human-check>With unread notifications present the bell sits on a solid accent-hued pill with its badge legible on top; with zero unread it is a plain neutral glyph with no badge and no fill.</human-check>
  </verify>
  <reversibility rating="reversible">Two additive properties, one Rectangle, one widened tint expression.</reversibility>
  <done>The bell fills with `BarRoles.fillNotification` exactly when the unread count is non-zero and returns to neutral when it is zero, the badge clamp and both click handlers are byte-unchanged, and no other action cell gained a fill.</done>
</task>

<task type="auto">
  <name>Task 3: Finish the capsule-family tint migration (D-24 plus the omitted media/connectivity error tint)</name>
  <files>quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml, quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml</files>
  <read_first>
    - The live output of `grep -n 'Colours\.' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml` — run it FIRST and migrate what it reports. D-24 cites lines 129/163/410/427/439 for ClockActionsCapsule; line 129 is `property color tint: clockActionsCapsule.contentColour` and holds no palette token, while line 172 (the badge label) does. The cited numbers are stale; the grep is the source of truth.
    - `quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml` lines 95-120 — the `Readout` glyph `Text` whose error branch is the one bar-capsule reference RESEARCH's violation list omitted.
    - `waybar/.config/waybar/style-athena.scss` — `#idle_inhibitor.activated` and `#custom-gaming-mode.on` are `@accent`, foreground-only; failure states are `@danger`. Both roles already exist in `BarRoles`, so nothing needs inventing.
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` — the `accent`, `onAccent`, `danger` row names.
  </read_first>
  <action>
Run the grep named in `<read_first>` and migrate every reported reference. Expected mapping, all onto rows that already exist:

- The badge `Rectangle`'s fill (currently line 163) → `BarRoles.accent`; Athena's badge is an attention marker in the accent hue, which is what `#custom-notification`'s own filled treatment already uses.
- The badge label `Text` (currently line 172) → `BarRoles.onAccent`, the on-colour for the fill above it.
- `gamingCell.tint`'s active branch (currently line 410) → `BarRoles.accent`, matching `#custom-gaming-mode.on`.
- `bellCell.tint`'s unavailable branch (currently line 427) → `BarRoles.danger`. Plan Task 2 already rewrote this expression; confirm the migration landed there rather than migrating it twice.
- `idleCell.tint`'s active branch (currently line 439) → `BarRoles.accent`, matching `#idle_inhibitor.activated`.
- `MediaConnectivityCapsule.qml`'s `Readout` glyph error branch (currently line 109) → `BarRoles.danger`, the same shape `SystemCapsule.qml`'s error branch takes in plan 18.1-02.

Preserve each expression's inactive branch exactly — every one of these currently falls through to `contentColour`, which plan 18.1-02 already rebased onto `BarRoles`, so the inactive branches need no edit and must not gain one.

After the edits, no code line in either file may name the global palette singleton followed by a dot. If the grep still reports a reference this action's mapping does not cover, do NOT invent a new `BarRoles` row for it and do NOT leave it unmigrated: stop, name the reference and its file:line in the SUMMARY, and report it — an unnamed extra reference means the design's role table is incomplete, which is a scoping question for the operator, not a judgment call for the executor.

`MediaConnectivityCapsule.qml`'s audio and brightness scroll handlers are QBAR-04's shipped capability. This task changes exactly one colour binding in that file and must touch no `WheelHandler`, `MouseArea`, `onWheel` or scroll-step expression.
  </action>
  <acceptance_criteria>
    - `sed 's|//.*||' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml | grep -c 'Colours\.'` returns exactly `0`.
    - `sed 's|//.*||' quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml | grep -c 'Colours\.'` returns exactly `0`.
    - `grep -c 'BarRoles.accent' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns at least `3` (badge fill, gaming active, idle active).
    - `grep -c 'BarRoles.onAccent' quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` returns at least `1` (badge label).
    - `grep -c 'BarRoles.danger' quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml` returns exactly `1`.
    - `git diff --stat quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml` shows exactly 1 insertion and 1 deletion.
    - `git diff quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml` contains no `WheelHandler`, `onWheel`, `MouseArea` or scroll-step line.
    - `git diff` shows every migrated ternary still carries its original inactive branch (`contentColour`), verified by reading each changed expression.
    - The SUMMARY names any grep-reported reference the mapping above did not cover, or states explicitly that there were none.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after the edits.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; for F in quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml; do test "$(sed 's|//.*||' $F | grep -c 'Colours\.')" = 0 || { echo "FAIL $F"; exit 1; }; done &amp;&amp; test "$(grep -c 'BarRoles.danger' quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml)" = 1 &amp;&amp; test "$(git diff --numstat -- quickshell/.config/quickshell/modules/bar/MediaConnectivityCapsule.qml | awk '{print $1"/"$2}')" = "1/1" &amp;&amp; echo TINT-MIGRATION-OK</automated>
    <human-check>Gaming mode and keep-awake still light up in the accent hue when active and stay neutral when not; a disconnected wifi or bluetooth glyph still reads red.</human-check>
  </verify>
  <reversibility rating="reversible">Six single-line binding changes across two files.</reversibility>
  <done>Every bar capsule file routes colour through `BarRoles`, the media/connectivity error tint the research sweep missed is closed, and QBAR-04's scroll handlers are provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `swaync` notification-source IPC → `notificationSource.unreadCount` / `dndActive` | An external daemon's reported counts and flags cross into the shell. Only numeric counts and booleans are consumed here; no daemon-supplied string is rendered by this plan. |
| Local shell scripts (`theme-switch.sh`, `wleave.sh`, …) → `Process` argv | Fixed literal paths built from `homeDir` plus a repo-literal filename; unchanged by this plan. |

No network surface, no authentication, no remote input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18.1-09 | Spoofing | `notificationSource.unreadCount` driving a filled accent pill | low | mitigate | The fill, the badge and the `FILL` variable axis all derive from the ONE `unreadCount` input, so a daemon cannot make the bar assert attention in one channel while another says zero. Acceptance criteria assert no second unread source was introduced. |
| T-18.1-10 | Tampering | Badge label text | low | mitigate | The label is never the daemon's own string — it is a locally clamped integer rendered as `"9+"` above 9. The clamp is asserted byte-unchanged, which also bounds the label's width and prevents a hostile count from resizing the capsule. |
| T-18.1-11 | Elevation of Privilege | `SettingsAxisCell` / `powerCell` `Process` launches | medium | mitigate | Already mitigated and untouched: each argv is a single literal path composed of `homeDir` plus a repo-literal script filename, launched with `startDetached()`, with an executable-bit probe first. This plan changes no `Process`, no `command` array and no script path; the `git diff` scope is colour bindings only. |
| T-18.1-12 | Denial of Service | New fill `Rectangle`s intercepting pointer input | low | mitigate | Both new fills are declared with no `MouseArea` and no `HoverHandler`; acceptance criteria assert this by reading the declarations. A fill that swallowed clicks would silently break the bell, the settings drawer and QBAR-04's scroll. |
| T-18.1-SC | Tampering | npm/pip/cargo/pacman installs | n/a | accept | This plan installs no packages; RESEARCH's Package Legitimacy Audit records "not applicable" for this phase. |
</threat_model>

<verification>
1. Neither `ClockActionsCapsule.qml` nor `MediaConnectivityCapsule.qml` contains a direct global-palette reference on a code line.
2. `clockFillPill` and `cellFillPill` exist, neither carries a pointer handler, and `fillActive` is opted into by exactly one cell.
3. The badge `> 9` clamp and both bell click handlers are byte-unchanged.
4. `MediaConnectivityCapsule.qml`'s diff is exactly one insertion and one deletion, containing no scroll-related line (QBAR-04 guard).
5. No raw numeric font weight and no bare geometry literal was introduced.
6. `$HOME/.cache/quickshell.log` shows no new QML load error.
</verification>

<success_criteria>
Athena's three filled pills are all present on the bar — accent on the focused workspace, secondary on the clock, primary on the notification bell when there is something to notify about — every remaining action-cell tint resolves through `BarRoles`, and the bar's capsule family contains zero direct palette references.
</success_criteria>

<output>
Create `.planning/phases/18.1-qml-bar-athena-restoration/18.1-03-SUMMARY.md` when done.
</output>
