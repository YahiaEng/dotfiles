---
phase: 18.1-qml-bar-athena-restoration
plan: 04
type: execute
wave: 2
depends_on: ["18.1-01"]
files_modified:
  - quickshell/.config/quickshell/modules/bar/TrayCapsule.qml
  - quickshell/.config/quickshell/modules/bar/qmldir
  - quickshell/.config/quickshell/modules/bar/BarEntryModel.qml
  - quickshell/.config/quickshell/modules/Bar.qml
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [QBAR-05, QBAR-02, QBAR-12]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-15: TrayCapsule.qml is deleted along with its qmldir row, both of its Bar.qml consumers and its BarEntryModel entry — five removal points total, matching Athena, whose own config records dropping the tray because nm-applet and blueman icons duplicated the connections group."
    - "D-15: the consumer sweep is RE-RUN live before deletion rather than trusted from the research document — the sweep is cheap, the research is explicitly scoped as stale-on-any-intervening-commit, and sibling plans in this same wave are editing neighbouring files."
    - "QBAR-05 ('the system tray shows running tray applications and their menus open on click') is currently marked Complete in REQUIREMENTS.md; this plan records its WITHDRAWAL with the D-15 rationale rather than letting a Complete requirement quietly point at deleted code. The deletion is not reversed — the ledger is corrected to reflect it."
    - "QBAR-05 (plan-authored guard, NOT probe-derived): after removal, no capsule id `tray` survives anywhere — BarEntryModel's capsules array, Bar.qml's componentFor() dispatch and the qmldir manifest all agree, so componentFor(\"tray\") is unreachable rather than returning undefined into a Loader."
    - "QBAR-02 (probe edge — ordering): capsule order is a property of BarEntryModel's config-driven entry list and is identical in both orientations, so splicing the tray element out must leave every remaining element's relative order byte-identical — removing an array member must never reorder its neighbours. Extract the ordered `id:` sequence before and after and compare it literally; equal-comparing neighbours must stay in declaration order, not settle into whatever order the splice happens to produce."
    - "QBAR-02 (probe edge — boundary): both orientations still render every remaining capsule after the removal; the vertical column in particular gains height budget rather than losing a slot it needed."
    - "QBAR-02 (probe edge — adjacency): the two capsules that become neighbours once the tray is spliced out must SEPARATE cleanly with the zone's normal inter-capsule spacing — they must not merge into one visual slab, collide, or double their gap. QtQuick positioners exclude a removed child AND its spacing, so this holds by construction; assert it visually in both orientations rather than assuming it."
    - "QBAR-02 (probe edge — empty): if removing the tray leaves its zone with zero visible capsules, the zone contributes zero extent and zero spacing rather than an empty gap — the same mechanism BarCapsule already uses to drop an empty capsule. A single remaining capsule in that zone renders identically to how it renders beside a sibling, with no orphan-child special case."
    - "QBAR-02 (probe edge — precision): the orientation-switchable pill radius is `vertical ? width / 2 : height / 2`, which is exact at both token values — barColumnWidth 44 halves to 22 and barHeight 40 halves to 20, both integers. Removing a capsule changes neither token, so no radius is newly subject to truncation. Confirm both tokens are still even in Design.qml; an odd value would silently truncate and leave a one-pixel flat edge on every capsule in that orientation."
    - "QBAR-12 (probe edge — adjacency): the bar's reserved zone extent is unchanged by the removal — the reservation is driven by Design.barHeight/barColumnWidth, not by capsule count, so removing a capsule must not move the reserved edge."
    - "QBAR-12 (probe edge — empty): the reservation is independent of capsule content. A bar whose capsules are all empty, and a bar in the hidden-soft state, both still reserve — `zoneReserved` is bound to `visibilityState !== \"hidden-hard\"` and reads no capsule at all. Removing the tray therefore cannot produce a zero or absent reserved entry; a missing entry in `hyprctl monitors -j` after the removal is a real defect, not an expected consequence."
    - "QBAR-12 (probe edge — ordering): the `reserved` array read from `hyprctl monitors -j` is compared before and after as a whole, and its element order must be identical, not merely its contents — an order-insensitive comparison would hide a drift that reorders which edge is reserved. Capture both readings and diff them literally."
    - "Deleting this file also stops the shell self-hosting org.kde.StatusNotifierWatcher on the session bus (18-10-SUMMARY.md:101 recorded the shell acquiring that name the moment TrayCapsule first referenced SystemTray.items). This matches Athena's own behaviour and is not a regression: quickshell-doctor's QSD_NAME_OWNERS and QSD_CLIENT_CONSUMERS registries name only org.freedesktop.Notifications and the PipeWire/NetworkManager/BlueZ consumers — no tray-watcher row exists to update."
    - "The four open .planning/WINDOWS.md rows against TrayCapsule.qml (#30 unrun-verify, #31 deviation, #32 unrun-verify, #37 lint-warning) are closed as resolved-by-deletion with a one-line 18.1 citation, rather than left open against a file that no longer exists."
    - statement: "QBAR-06 (plan-authored guard, NOT probe-derived, tray-adjacent): removing the tray must not remove any clock, battery, network, bluetooth, audio or CPU/RAM/disk readout — those live on other capsules and QBAR-06 stays Complete."
      verification: backstop
  artifacts:
    - "quickshell/.config/quickshell/modules/bar/TrayCapsule.qml — DELETED"
    - "quickshell/.config/quickshell/modules/bar/qmldir — the `TrayCapsule 1.0 TrayCapsule.qml` row removed"
    - "quickshell/.config/quickshell/modules/Bar.qml — the componentFor() tray branch and the trayComponent Component block removed"
    - "quickshell/.config/quickshell/modules/bar/BarEntryModel.qml — the `tray` element removed from the capsules array"
    - ".planning/REQUIREMENTS.md — QBAR-05 recorded as Withdrawn/Superseded with the D-15 rationale"
    - ".planning/WINDOWS.md — rows 30, 31, 32 and 37 closed as resolved-by-deletion"
  key_links:
    - "BarEntryModel capsules array -> Bar.qml componentFor(): the entry list drives which components are ever instantiated; an id left in the array with no component branch produces a Loader with an undefined sourceComponent and no load error, which is exactly the silent-failure shape this repo's qmldir header warns about."
    - "qmldir -> TrayCapsule.qml: the directory scanner is disabled for modules/bar/, so an orphaned qmldir row pointing at a deleted file is a load-time resolution failure, not a no-op. The row and the file go in the same commit."
    - "REQUIREMENTS.md QBAR-05 status -> the phase ledger: a requirement marked Complete whose implementing file was deleted is a false green that survives into the milestone audit."
  prohibitions:
    - "Never reverse a locked decision because it conflicts with a requirement's recorded status. D-15 stands; the ledger is what gets corrected."
    - "Never mark a requirement Complete, or leave it Complete, when the code satisfying it has been removed. Correct the status and state why."
    - "Never restate a behavioural promise in a comment that the code does not implement."
    - "Never close a WINDOWS.md verification row by asserting the verification finally happened. These four close because the subject no longer exists — that is the honest reason and it must be the recorded one."
    - "Never record a live visual verification as performed when it was not."
---

<!-- planner-discipline-allow: trayComponent -->
<!-- planner-discipline-allow: QBAR-05 -->
<!-- planner-discipline-allow: Complete -->
<!-- All three literals are named in <action> bodies for the same reason the criteria negative-grep
     them: this is a DELETION plan, so the action must name the exact symbol being removed
     (`trayComponent`), the exact requirement id being re-statused (`QBAR-05`), and the exact stale
     status string being replaced (`Complete`). The greps run over Bar.qml and REQUIREMENTS.md, not
     over this plan, so naming them here cannot mask a real violation. -->

<objective>
Remove the tray capsule and every one of its consumers, and correct QBAR-05's status in the requirements ledger so a deliberate, approved deletion does not leave a Complete requirement pointing at code that no longer exists.

Purpose: the extra tray capsule is one of the three named causes of the operator's "big and clunky" verdict, and Athena's own config records dropping the tray for the same reason — its icons duplicated the connections group. This is also the phase's one deletion, so it needs a live sweep rather than a trusted one.
Output: a bar with no tray capsule, a `modules/bar/` tree with no dangling reference to it, and a requirements ledger that tells the truth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md
@quickshell/.config/quickshell/modules/bar/qmldir
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces (new symbols — exclude from drift verification)

This plan creates no new code symbols. It removes:
`TrayCapsule` (the QML type, its `qmldir` row and its source file), `trayComponent` (the `Bar.qml`
`Component` block), the `componentFor()` `"tray"` dispatch branch, and the `tray` element of
`BarEntryModel.capsules`.

It creates two documentation states: a `QBAR-05` **Withdrawn (superseded by D-15)** status row in
`.planning/REQUIREMENTS.md`, and four `resolved` closures in `.planning/WINDOWS.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Live consumer sweep, then the five-point tray removal</name>
  <files>quickshell/.config/quickshell/modules/bar/TrayCapsule.qml, quickshell/.config/quickshell/modules/bar/qmldir, quickshell/.config/quickshell/modules/bar/BarEntryModel.qml, quickshell/.config/quickshell/modules/Bar.qml</files>
  <precondition>`quickshell/.config/quickshell/modules/bar/TrayCapsule.qml` still exists and is tracked by git — this plan is a deletion and is a no-op if a sibling plan or an out-of-band fix already removed it. Assert with `git ls-files --error-unmatch` before proceeding; halt and report if it is already gone.</precondition>
  <read_first>
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` section "TrayCapsule consumer sweep — the exact removal checklist (D-15)" — the five removal points with their file:line citations and the exact code blocks to remove. Use this as the checklist to CONFIRM against, not as a substitute for running the sweep.
    - `quickshell/.config/quickshell/modules/Bar.qml` lines 230-315 — `componentFor()`'s full dispatch chain and all six `Component` blocks, so the tray branch is removed without disturbing the other five.
    - `quickshell/.config/quickshell/modules/bar/BarEntryModel.qml` lines 150-175 — the `capsules` array around the `tray` element, so the splice preserves neighbouring order and the array's trailing-comma syntax stays valid.
    - `quickshell/.config/quickshell/modules/bar/qmldir` — the whole header, which states this manifest's own rule that a type is registered in the same commit that creates it (and by symmetry, deregistered in the one that deletes it).
    - `.planning/phases/18-qml-bar-retirement-machinery/18-10-SUMMARY.md` around line 101 — the record of the shell acquiring `org.kde.StatusNotifierWatcher` when this file first referenced `SystemTray.items`.
  </read_first>
  <action>
Run the sweep live before deleting anything: `grep -rn 'TrayCapsule\|trayComponent\|"tray"' --include='*' .` across the whole repository, and separately `grep -rn 'StatusNotifierWatcher\|SystemTray' hypr/.config/hypr/scripts/`. RESEARCH found exactly four code-level references plus the file itself, and no reference in any doctor script, contract file or test fixture — but its own validity note scopes it to the repository state at research time, and two sibling plans are editing neighbouring files in this same wave. Record the live sweep's output in the SUMMARY. If the sweep finds a reference the checklist does not name, stop and report it rather than removing it on judgment.

Then perform the five removals:
1. Delete the `TrayCapsule 1.0 TrayCapsule.qml` row from `modules/bar/qmldir`, and add a short comment in the file's existing commented-block style recording that the type was removed by phase 18.1 under D-15, so a future reader does not re-add it wondering where it went.
2. Remove the `componentFor()` branch in `Bar.qml` that returns `trayComponent` for `capsuleId === "tray"`, leaving the other five branches and the function's fall-through untouched.
3. Remove the whole `trayComponent` `Component` block in `Bar.qml`, including its five backend property bindings.
4. Remove the `tray` element from `BarEntryModel.qml`'s `capsules` array. Splice the element out and fix the surrounding comma so the array stays syntactically valid; the relative order of every remaining element must be identical afterwards.
5. Delete `quickshell/.config/quickshell/modules/bar/TrayCapsule.qml` with `git rm`.

Note in the SUMMARY, as a recorded side effect rather than a blocker, that the shell will stop self-hosting `org.kde.StatusNotifierWatcher` on the next restart. Nothing in this repo depends on the shell owning that name — `quickshell-doctor`'s `QSD_NAME_OWNERS` and `QSD_CLIENT_CONSUMERS` registries name only `org.freedesktop.Notifications` and the PipeWire/NetworkManager/BlueZ consumers, so there is no registry row to update. Confirm that by grepping those two registries rather than by asserting it.

Do not touch `Design.barHeight`, `Design.barColumnWidth`, `Design.barEdgeMargin`, `Design.barSideMargin`, `zoneReserved`, `reservedZoneExtent`, or any other reservation expression. The reserved zone is driven by the bar's own extent, not by how many capsules it holds, and QBAR-12 is judged on that reservation surviving unchanged.
  </action>
  <acceptance_criteria>
    - `git ls-files quickshell/.config/quickshell/modules/bar/TrayCapsule.qml` returns empty — the file is deleted and staged, not merely untracked on disk.
    - `grep -rn 'TrayCapsule' quickshell/ hypr/ --include='*.qml' --include='qmldir' --include='quickshell-doctor'` returns no hit outside a comment.
    - `grep -c 'trayComponent' quickshell/.config/quickshell/modules/Bar.qml` returns `0`.
    - `grep -c 'id: "tray"' quickshell/.config/quickshell/modules/bar/BarEntryModel.qml` returns `0`.
    - `grep -c '^TrayCapsule 1.0 TrayCapsule.qml$' quickshell/.config/quickshell/modules/bar/qmldir` returns `0`.
    - `git diff` for `BarEntryModel.qml` shows only the tray element removed — no other array element's position changed, verified by diffing the extracted `id:` sequence before and after.
    - `git diff` touches no line containing `barHeight`, `barColumnWidth`, `barEdgeMargin`, `barSideMargin`, `zoneReserved` or `reservedZoneExtent`.
    - The live sweep output is recorded verbatim in the SUMMARY, and the SUMMARY states whether it matched RESEARCH's five-point checklist exactly.
    - `hyprctl monitors -j` reports the same `reserved` array as before the change (QBAR-12 guard) — capture it before and after and diff the two.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after a restart, and in particular no `TrayCapsule is not a type` or unresolved-type line.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; test -z "$(git ls-files quickshell/.config/quickshell/modules/bar/TrayCapsule.qml)" &amp;&amp; test "$(grep -c 'trayComponent' quickshell/.config/quickshell/modules/Bar.qml)" = 0 &amp;&amp; test "$(grep -c 'id: \"tray\"' quickshell/.config/quickshell/modules/bar/BarEntryModel.qml)" = 0 &amp;&amp; test "$(grep -c '^TrayCapsule 1.0 TrayCapsule.qml$' quickshell/.config/quickshell/modules/bar/qmldir)" = 0 &amp;&amp; test -z "$(sed 's|//.*||;s|#.*||' quickshell/.config/quickshell/modules/bar/qmldir quickshell/.config/quickshell/modules/Bar.qml quickshell/.config/quickshell/modules/bar/BarEntryModel.qml | grep -n 'TrayCapsule')" &amp;&amp; echo TRAY-REMOVED-OK</automated>
    <human-check>The bar renders every remaining capsule in both orientations with no gap where the tray was, and the reserved screen edge has not moved.</human-check>
  </verify>
  <reversibility rating="costly">A 30K file is removed. Fully recoverable from git history, but the D-Bus watcher side effect and the requirements-ledger correction in Task 2 both follow from it, so a reversal is a multi-file, multi-artifact undo rather than a one-line revert.</reversibility>
  <done>`TrayCapsule.qml` and all four of its code-level consumers are gone, the live sweep is recorded, the surviving capsule order is unchanged, and the reserved zone measured before and after is identical.</done>
</task>

<task type="auto">
  <name>Task 2: Record QBAR-05's withdrawal and close the four TrayCapsule ledger rows</name>
  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 18 (the QBAR-05 checklist entry, currently `- [x]`) and line 130 (its `| QBAR-05 | Phase 18 | Complete |` status-table row) — both must be corrected, not just one.
    - `.planning/REQUIREMENTS.md` around lines 100-115 — the "Out of Scope" / explicitly-declined table, to match its existing wording style for a deliberately-dropped capability rather than inventing a new format.
    - `.planning/WINDOWS.md` rows 30, 31, 32 and 37 — read each row's full text so the closure note names what specifically is being resolved, and read the file's own column format including its JSON mirror block further down (rows 425, 437, 449, 509) so both representations stay in sync.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md` D-15 — the rationale to cite verbatim.
  </read_first>
  <action>
Correct QBAR-05 in both places it appears in `.planning/REQUIREMENTS.md`. Change the checklist entry at line 18 from a checked box to a withdrawn marker consistent with how this file already expresses a dropped requirement (follow the existing QS-03 precedent, which this project dropped permanently and recorded rather than deleted), and change its status-table row from `Complete` to `Withdrawn — superseded by 18.1 D-15`. In both places carry a one-line rationale: the tray was deliberately dropped in phase 18.1 to match Athena, whose own config records removing it because nm-applet and blueman icons duplicated the connections group; the connections capsule continues to surface that state, so the capability is superseded rather than lost.

Do not delete the requirement, do not renumber anything, and do not touch any other requirement's status. QBAR-05 stays visible in the ledger with an honest status — a deleted row loses the audit trail that the milestone close reads.

Then close the four `.planning/WINDOWS.md` rows against `TrayCapsule.qml` — #30 (unrun live click-through), #31 (a self-contradictory acceptance-criterion deviation), #32 (unrun GATE-02 visual pass), #37 (a pre-existing untokened-colour lint warning at line 241). Set each to `resolved` with a closure note of the form "resolved by deletion of TrayCapsule.qml in phase 18.1 (D-15) — the subject file no longer exists". That is the true reason. Do not write, in any form, that the deferred verification was finally performed: it was not, and rows 30 and 32 exist precisely because this repo has a history of that claim being made too easily.

Update the JSON mirror block in the same file for all four rows so the table and the JSON agree, and update the file's `open_count` if it carries one. If the recomputed open count disagrees with any literal stated elsewhere in the file, follow this repo's own established precedent and record the discrepancy as a documented deviation rather than force-matching a stale number.
  </action>
  <acceptance_criteria>
    - `grep -n 'QBAR-05' .planning/REQUIREMENTS.md` returns exactly two rows and neither reads `Complete`.
    - `grep -c 'QBAR-05.*Withdrawn' .planning/REQUIREMENTS.md` returns at least `1`.
    - `grep -c 'D-15' .planning/REQUIREMENTS.md` returns at least `1` — the rationale cites the decision.
    - `grep -c '^- \[x\] \*\*QBAR-05\*\*' .planning/REQUIREMENTS.md` returns `0` — the checked box is gone.
    - `git diff .planning/REQUIREMENTS.md` shows no change to any requirement id other than QBAR-05.
    - Every one of `.planning/WINDOWS.md` rows 30, 31, 32 and 37 has status `resolved` and a closure note containing both `18.1` and `TrayCapsule.qml`.
    - `grep -c 'TrayCapsule' .planning/WINDOWS.md` shows every remaining occurrence sits on a resolved row — no open row references the deleted file.
    - No closure note in `.planning/WINDOWS.md` asserts that a previously-unrun verification was performed.
    - The JSON mirror block's four corresponding entries carry the same resolved status as the table rows.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; test "$(grep -c 'QBAR-05' .planning/REQUIREMENTS.md)" = 2 &amp;&amp; test "$(grep 'QBAR-05' .planning/REQUIREMENTS.md | grep -c 'Complete')" = 0 &amp;&amp; test "$(grep -c 'QBAR-05.*Withdrawn' .planning/REQUIREMENTS.md)" -ge 1 &amp;&amp; test "$(grep 'TrayCapsule' .planning/WINDOWS.md | grep -c '| open |')" = 0 &amp;&amp; echo LEDGER-OK</automated>
  </verify>
  <reversibility rating="reversible">Documentation-only edits in two planning files.</reversibility>
  <done>QBAR-05 reads Withdrawn/superseded in both of its locations with the D-15 rationale, no other requirement's status moved, and all four TrayCapsule ledger rows are closed with deletion — not a claimed verification — as the recorded reason.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged assumption — unresolved probe edge (QBAR-05)

The probe returned QBAR-05 as `unclassified`: it surfaced the requirement as carrying an edge but
could not determine which kind. Per §A of the spec-less probe fallback an `unclassified` row **stays
`unresolved`** — not auto-resolved, not downgraded to a backstop, and never auto-dismissed. This one
matters more than most, because QBAR-05 is the requirement this plan withdraws.

- **QBAR-05 — `unclassified`, unresolved.** "The system tray shows running tray applications and
  their menus open on click." D-15 deletes the capability outright, so the probe's edge — whatever it
  was — is being resolved by removal rather than by handling. Not resolved here. Assumption on
  record: the connections capsule continues to surface the state that the nm-applet and blueman tray
  icons duplicated, so the capability is superseded rather than lost. That assumption is Athena's own
  recorded rationale, not an independent verification on this host — and note that WINDOWS.md rows 30
  and 32 record that the tray's live click-through was NEVER observed working on this machine, because
  no application ever registered a StatusNotifierItem here. QBAR-05 is therefore being withdrawn
  without its edge having been classified and without the capability ever having been seen to work.
  Both facts belong in the operator's hands, not in a silent status change.

Not discharged by this plan. Carried to plan 18.1-07's GATE-02 checkpoint, where the tray's absence
is one of the four disclosed deltas.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D-Bus StatusNotifierItem/`com.canonical.dbusmenu` → `TrayCapsule` menu labels and icons | Arbitrary third-party applications supplied strings and pixmaps that this shell rendered. This plan REMOVES that boundary entirely. |

No network surface, no authentication, no remote input. This plan is net attack-surface-reducing.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18.1-13 | Information Disclosure / Tampering | Untrusted D-Bus menu labels rendered in `TrayCapsule` | medium | mitigate | Eliminated by deletion. The pre-existing mitigation (`textFormat: Text.PlainText` on every D-Bus-sourced string) is moot once the only consumer of `SystemTray.items` is gone; this plan reduces the phase's attack surface rather than adding to it. |
| T-18.1-14 | Denial of Service | Orphaned `qmldir` row or `BarEntryModel` id after partial removal | medium | mitigate | The directory scanner is disabled for `modules/bar/`, so a stale manifest row pointing at a deleted file is a real resolution failure and a stale entry id yields a `Loader` with an undefined `sourceComponent` and NO load error. All five removal points land in one commit and the acceptance criteria assert each one independently; the live sweep is the detection mechanism. |
| T-18.1-15 | Spoofing | Loss of `org.kde.StatusNotifierWatcher` ownership | low | accept | Releasing a well-known bus name lets another process claim it. Accepted: nothing in this repo consumes that name (verified against `QSD_NAME_OWNERS` and `QSD_CLIENT_CONSUMERS`), Athena's own configuration ran the same way for the whole of the prior milestone, and no privilege or secret rides on the name. |
| T-18.1-16 | Repudiation | A Complete requirement pointing at deleted code | medium | mitigate | Task 2 is the mitigation: QBAR-05's status is corrected in both of its locations with the D-15 rationale, so the milestone audit reads a true state rather than a false green. |
| T-18.1-SC | Tampering | npm/pip/cargo/pacman installs | n/a | accept | This plan installs no packages; RESEARCH's Package Legitimacy Audit records "not applicable" for this phase. |
</threat_model>

<verification>
1. `TrayCapsule.qml` is deleted and staged; no code-level reference to `TrayCapsule` or `trayComponent` survives anywhere outside a comment.
2. `BarEntryModel.capsules` no longer contains a `tray` element, and every remaining element's relative order is unchanged.
3. `git diff` touches no reservation or geometry expression, and `hyprctl monitors -j`'s `reserved` array is identical before and after (QBAR-12).
4. The live repository-wide sweep output is recorded in the SUMMARY, with an explicit statement of whether it matched RESEARCH's five-point checklist.
5. QBAR-05 reads Withdrawn/superseded in both of its REQUIREMENTS.md locations, citing D-15; no other requirement moved.
6. WINDOWS.md rows 30, 31, 32 and 37 are resolved with deletion as the stated reason, and the JSON mirror agrees.
7. `$HOME/.cache/quickshell.log` shows no unresolved-type or load-error line after a restart.
</verification>

<success_criteria>
The tray capsule and every consumer of it are gone, the bar renders complete in both orientations with an unchanged reserved zone, and the requirements ledger records QBAR-05 as deliberately withdrawn under D-15 rather than silently pointing at deleted code.
</success_criteria>

<output>
Create `.planning/phases/18.1-qml-bar-athena-restoration/18.1-04-SUMMARY.md` when done.
</output>
