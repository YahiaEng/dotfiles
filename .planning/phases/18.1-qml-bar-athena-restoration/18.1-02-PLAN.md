---
phase: 18.1-qml-bar-athena-restoration
plan: 02
type: execute
wave: 2
depends_on: ["18.1-01"]
files_modified:
  - quickshell/.config/quickshell/modules/bar/BarCapsule.qml
  - quickshell/.config/quickshell/modules/bar/SystemCapsule.qml
autonomous: true
requirements: [QBAR-01, QBAR-06, QBAR-09, QBAR-11]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-14: BarCapsule's shared chrome fill is BarRoles.capsule at rest and BarRoles.capsuleHover on hover — translucent (surfaceVariant at 0.85/0.95 over a bar surface at 0.55), which is what stops every capsule reading as a solid opaque grey slab."
    - "D-24: BarCapsule.contentColour keeps its NAME but is sourced from BarRoles rather than Colours.* — the alias survives as a one-hop indirection, its source does not."
    - "D-12: cpu, ram and disk render icon-only at rest with no value text and no reserved value width; the updates entry renders as a filled BarRoles.fillUpdates pill with BarRoles.fillUpdatesFg content; the three resource glyphs tint BarRoles.warn at their warning threshold and BarRoles.danger at their critical threshold."
    - "D-25: the value reveal is Athena's LITERAL in-place format-alt toggle — clicking a resource glyph swaps the value onto the SAME pill. The existing resourcesPopoutTrigger/ResourcesPopout anchored surface is NOT substituted for it and is NOT removed."
    - "D-06: after this plan, neither BarCapsule.qml nor SystemCapsule.qml contains a Colours.* reference on a code line."
    - "D-05: no alpha value is re-derived, re-rounded or re-tuned in these files — both capsule fills read BarRoles rows verbatim."
    - "QBAR-06 (boundary edge): a metric exactly AT its warning threshold tints warn, and exactly AT its critical threshold tints danger — the comparison is >= at each boundary, and one step below warning is neutral. Athena's own thresholds are cpu 75/90, memory 75/85, disk 80/90 (config-athena.jsonc states blocks)."
    - "QBAR-06 (empty edge): a metric whose D-41 readiness register reads `pending` (the ordinary pre-first-sample window) renders its glyph neutral with NO value even when revealed; only `empty` (a real read failure) tints BarRoles.danger. Pending is not an error and must not render as one."
    - "QBAR-09 (empty edge): the resources popout still opens — resourcesPopoutTrigger, its sectionId `resources` and its ResourcesPopout component are byte-unchanged, so QBAR-09's shipped capability survives the D-25 addition."
    - "QBAR-09 (ordering edge): when a click lands on a resource pill that is BOTH inside resourcesPopoutTrigger and carries the new in-place toggle, the in-place toggle wins and the popout trigger's own dwell/click path is not also fired — the resolution is stated in source, not left to event-propagation accident."
    - "QBAR-11 (boundary edge): the in-place toggle adds ZERO repeating timers. Any Timer introduced is a one-shot (`repeat: false`) armed only by a user action; the shell's idle timer count is unchanged at rest."
    - "QBAR-01 (boundary edge): BarCapsule's fill at alpha 0.85 must stay above the ^quickshell-.* family blur rule's ignore_alpha floor. If blur dies after this change, screenshot BEFORE tuning the alpha and re-apply layer rules via hyprctl eval or a compositor restart — hyprctl reload silently drops layer-rule edits and the symptom is identical to a wrong alpha."
    - statement: "QBAR-04 (boundary edge): scroll on the audio and brightness sections still clamps at 0 and at maximum — unchanged by this plan, which touches neither scroll handler."
      verification: backstop
    - statement: "QBAR-07 (ordering edge): two hide sources arriving together (idle plus fullscreen) still resolve through the single owner of visibility state — no code in this plan reads or writes visibilityState."
      verification: backstop
  artifacts:
    - "quickshell/.config/quickshell/modules/bar/BarCapsule.qml (fill + contentColour sourced from BarRoles)"
    - "quickshell/.config/quickshell/modules/bar/SystemCapsule.qml (icon-only rest state, in-place value toggle, threshold tints, filled updates pill)"
  key_links:
    - "BarCapsule.color -> BarRoles.capsule / BarRoles.capsuleHover: the single highest-leverage colour change in the phase — every capsule inherits this chrome, so getting the alpha wrong here makes the whole bar read wrong at once."
    - "BarCapsule.contentColour -> BarRoles: every capsule file binds to this alias; rebasing its source migrates six consumers with one edit."
    - "SystemCapsule Readout.valueRevealed -> the Readout's value Text visibility AND its width reservation: both must follow the toggle, or the pill is icon-only in appearance while still reserving the value's width and the capsule never actually gets narrower."
  prohibitions:
    - "Never restate a behavioural promise in a comment that the code does not implement — the failure that produced this bar."
    - "Never reduce a locked decision to a 'v1', 'static for now' or 'placeholder' form. D-25 says in-place toggle; a popout is not an in-place toggle no matter how much richer it is."
    - "Never substitute an existing mechanism for a decided one because the existing mechanism is cheaper to reach. D-25 explicitly forbids swapping ResourcesPopout in for the format-alt toggle."
    - "Never touch the bar's geometry tokens to chase 'big and clunky'."
    - "Never record a live visual verification as performed when it was not."
---

<!-- planner-discipline-allow: readonly property color severityColour -->
<!-- planner-discipline-allow: id: updatesFillPill -->
<!-- planner-discipline-allow: valueToggleable: true -->
<!-- planner-discipline-allow: onActivated: root.launchUpgrade() -->
<!-- These four literals are named in <action> bodies because each is a symbol the executor must
     CREATE or PRESERVE by that exact name. The paired acceptance criteria assert positive counts
     (1, 3, 1, 1); the only `= 0` test on those chained verify lines is the unrelated palette-token
     gate over the QML source, which none of these four literals can affect. -->

<objective>
Migrate the bar's shared chrome onto Athena's translucent capsule surface, then restore the resource pills to Athena's icon-only-at-rest, value-on-click shape with real threshold colour and a filled updates alert pill.

Purpose: `BarCapsule` is the one shared chrome every slot is built from — its opaque grey fill is the direct cause of the operator's "big and clunky" verdict, and `SystemCapsule`'s always-on `4% 36% 7%` text is the second named cause. Both are fixed here.
Output: a translucent capsule chrome sourced entirely from `BarRoles`, and resource pills that read as glyphs at rest and reveal their value in place on click.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-CONTEXT.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md
@.planning/phases/18.1-qml-bar-athena-restoration/18.1-01-SUMMARY.md
@quickshell/.config/quickshell/modules/bar/BarRoles.qml
@quickshell/.config/quickshell/modules/bar/BarCapsule.qml
@quickshell/.config/quickshell/modules/bar/SystemCapsule.qml
</context>

<artifacts_this_phase_produces>
## Artifacts this plan produces (new symbols — exclude from drift verification)

**New `SystemCapsule.Readout` component properties/signals:**
`valueRevealed`, `warnThreshold`, `dangerThreshold`, `metricFraction`, `severityColour`, `toggleValue()`

**New `SystemCapsule` capsule-level properties:**
`cpuValueRevealed`, `ramValueRevealed`, `diskValueRevealed`

**New `SystemCapsule` filled-pill node:** `updatesFillPill` (a `Rectangle` behind the updates `Readout`)

**Consumed (created by plan 18.1-01, not drift):** the `BarRoles` singleton and its 17 role properties
`barSurface`, `barSurfaceHover`, `capsule`, `capsuleFg`, `capsuleHover`, `capsuleTrack`, `accent`,
`onAccent`, `warn`, `danger`, `onDanger`, `fillClock`, `fillClockFg`, `fillUpdates`, `fillUpdatesFg`,
`fillNotification`, `fillNotificationFg`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: BarCapsule chrome onto Athena's translucent capsule surface</name>
  <files>quickshell/.config/quickshell/modules/bar/BarCapsule.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/BarCapsule.qml` — the whole file (98 lines). Note line 41's fill, line 55's `contentColour` alias, and the comment at 37-40 that names the `ignore_alpha` blur floor.
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` — as plan 18.1-01 wrote it; confirm the row names `capsule`, `capsuleHover`, `capsuleFg`.
    - `waybar/.config/waybar/theme.scss` lines 100-125 — the comment block explaining why `capsule` is built on `surface_variant` at 0.85 and why hover is 0.95 rather than a different hue.
    - `.planning/phases/18.1-qml-bar-athena-restoration/18.1-RESEARCH.md` "Pattern 2" — the enumerated consumption-rule violations and the `contentColour` one-hop-indirection discussion.
  </read_first>
  <action>
Replace `BarCapsule.qml`'s fill binding so it resolves `BarRoles.capsuleHover` when `hovered` and `BarRoles.capsule` otherwise. Keep the existing motion-gated `Behavior on color` block byte-identical — the crossfade shape is not what changed.

Rebase the `contentColour` alias onto `BarRoles`. Per D-24 the alias keeps its exported name (six consumer files bind to it and renaming it is a needless six-file churn), but its source becomes role rows: the active branch resolves `BarRoles.onAccent` and the inactive branch `BarRoles.capsuleFg`. `capsuleFg` is `onSurfaceVariant`, which is exactly what the inactive branch already resolved to, so the inactive appearance is unchanged; the active branch moves from `onSurface` to `onAccent`, which is the correct on-colour once a capsule can actually be filled with `accent`.

Update the comment block at lines 37-40 so it names `BarRoles` as the resolution path, and keep its warning about the `ignore_alpha` blur floor — that warning is load-bearing and now more so, since this fill is translucent where it previously was not. After the edit no code line in this file may name the global palette singleton followed by a dot, and do not restate that forbidden token in a trailing comment on a code line either.

Change nothing else in this file: `radius`, `implicitWidth`/`implicitHeight`, `visible`, the content `Grid`, `iconFill`, the five backend handles and the `HoverHandler` are all untouched. This file is the bar's shared chrome and every capsule inherits its geometry — a geometry edit here is a phase-wide regression and is fenced out.

After the edit, look at the bar. If the blur behind it is gone, capture a screenshot BEFORE changing any alpha value, then re-apply the layer rules with `hyprctl eval` or restart the compositor: this project has a recorded trap where a layer-rule edit does not survive `hyprctl reload` and the resulting symptom is indistinguishable from a QML alpha sitting under the `ignore_alpha` floor. Tuning the alpha first would silently undo D-05.
  </action>
  <acceptance_criteria>
    - `sed 's|//.*||' quickshell/.config/quickshell/modules/bar/BarCapsule.qml | grep -c 'Colours\.'` returns exactly `0`.
    - `grep -c 'BarRoles.capsuleHover' quickshell/.config/quickshell/modules/bar/BarCapsule.qml` returns at least `1` and `grep -c 'BarRoles.capsule\b'` returns at least `1`.
    - `grep -c 'readonly property color contentColour' quickshell/.config/quickshell/modules/bar/BarCapsule.qml` returns exactly `1` — the alias name survives.
    - `grep -c 'BarRoles.onAccent' quickshell/.config/quickshell/modules/bar/BarCapsule.qml` returns at least `1` and `grep -c 'BarRoles.capsuleFg'` returns at least `1`.
    - `git diff quickshell/.config/quickshell/modules/bar/BarCapsule.qml` shows no change to the lines defining `radius`, `implicitWidth`, `implicitHeight`, `visible`, `iconFill`, or the `Behavior on color` block.
    - `git diff` introduces no numeric literal into this file.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after the edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; test "$(sed 's|//.*||' quickshell/.config/quickshell/modules/bar/BarCapsule.qml | grep -c 'Colours\.')" = 0 &amp;&amp; grep -q 'BarRoles.capsuleHover' quickshell/.config/quickshell/modules/bar/BarCapsule.qml &amp;&amp; grep -q 'BarRoles.capsuleFg' quickshell/.config/quickshell/modules/bar/BarCapsule.qml &amp;&amp; test "$(grep -c 'readonly property color contentColour' quickshell/.config/quickshell/modules/bar/BarCapsule.qml)" = 1 &amp;&amp; echo CHROME-OK</automated>
    <human-check>Every capsule now reads as a translucent pill with the wallpaper's blur visible through it, not a flat opaque grey slab. Hover visibly lightens the pill.</human-check>
  </verify>
  <reversibility rating="reversible">Two binding changes in one file.</reversibility>
  <done>Every capsule in the bar draws its background and its content colour from `BarRoles`, the chrome is translucent at Athena's exact alphas, and the file's geometry is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Icon-only resource pills with Athena's in-place format-alt value toggle</name>
  <files>quickshell/.config/quickshell/modules/bar/SystemCapsule.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` — the whole file (279 lines). The `Readout` component (48-105), the three resource instances inside `resourcesPopoutTrigger` (132-171), and the file header's "one interactive element total" rule, which this task deliberately amends.
    - `waybar/.config/waybar/config-athena.jsonc` lines 243-287 — the `disk`, `memory` and `cpu` module blocks: each has an icon-only `format`, a `format-alt` carrying icon plus value, `"actions": {"on-click": "mode"}`, and a `states` block with `warning`/`critical` numbers.
    - `quickshell/.config/quickshell/modules/bar/PopoutTrigger.qml` — the whole file; understand exactly which pointer events it consumes before adding a click handler inside it.
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` — confirm the `warn`, `danger` and `capsuleFg` row names.
  </read_first>
  <action>
Give the `Readout` component an in-place value toggle matching Athena's `format-alt` exactly (D-25). Add `property bool valueRevealed: false` and a `function toggleValue()` that flips it. Bind the value `Text`'s `visible` to `valueRevealed` AND bind its `width` to `valueRevealed ? valueReserve.width : 0`, and set the entry `Grid`'s spacing so a hidden value contributes no gap — QtQuick positioners already exclude non-visible children and their spacing, so making the value `Text` genuinely invisible (not merely blank) is what actually shrinks the pill. A value that is hidden but still reserves `valueReserve.width` leaves the capsule exactly as wide as before and delivers nothing D-12 asked for.

Attach the toggle to a `MouseArea` on the `Readout` root. The existing `MouseArea` at lines 99-104 is gated on `clickable` and is used only by the updates entry; extend it rather than adding a second overlapping hit area — give `Readout` a `property bool valueToggleable: false`, enable the `MouseArea` when either `clickable` or `valueToggleable` is set, and in `onClicked` dispatch to `toggleValue()` when `valueToggleable` and to `activated()` when `clickable`. Set `valueToggleable: true` on the cpu, ram and disk instances only. Amend the file header's "one interactive element total" rule to state the new truth rather than leaving a comment that the code contradicts.

Per D-25 and QBAR-09, `resourcesPopoutTrigger` stays. The three `Readout` instances remain its children, its `sectionId` stays `resources`, and its `popoutComponent` is untouched — the popout keeps working. Resolve the click-ordering question explicitly in source and in a comment: the `Readout`'s own `MouseArea` sits inside the trigger and accepts the click, so a click on a resource glyph performs the in-place toggle and does NOT also summon the popout; the popout stays reachable through the trigger's own hover-dwell path. State this precedence in the file so it is a decision, not an event-propagation accident. If `PopoutTrigger`'s implementation makes that precedence unachievable without editing `PopoutTrigger.qml` itself, STOP and report — `PopoutTrigger.qml` is not in this plan's `files_modified` and editing it would collide with the popout family this phase has fenced off.

Any Timer this task introduces must be `repeat: false` and armed only by a user action. The zero-idle doctrine this repo enforces is what QBAR-11's soak measures, and a repeating timer added here would invalidate a soak window that already has to be re-run.
  </action>
  <acceptance_criteria>
    - `grep -c 'property bool valueRevealed' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1`.
    - `grep -c 'valueToggleable: true' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `3` — cpu, ram, disk and nothing else.
    - `grep -c 'id: resourcesPopoutTrigger' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1` and `grep -c 'sectionId: "resources"'` returns exactly `1` — QBAR-09's shipped surface survives.
    - `grep -c 'ResourcesPopout' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns at least `1`.
    - `grep -c 'repeat: true' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1` — the pre-existing 30-minute `updatesTimer` and no other.
    - `grep -c 'MouseArea' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1` — the existing hit area was extended, not duplicated.
    - The value `Text`'s `width` binding references `valueRevealed`, verified by `grep -c 'valueRevealed ? valueReserve.width : 0'` returning `1`.
    - The file header no longer asserts that this file carries exactly one interactive element.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after the edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; F=quickshell/.config/quickshell/modules/bar/SystemCapsule.qml &amp;&amp; test "$(grep -c 'property bool valueRevealed' $F)" = 1 &amp;&amp; test "$(grep -c 'valueToggleable: true' $F)" = 3 &amp;&amp; test "$(grep -c 'id: resourcesPopoutTrigger' $F)" = 1 &amp;&amp; test "$(grep -c 'repeat: true' $F)" = 1 &amp;&amp; test "$(grep -c 'MouseArea' $F)" = 1 &amp;&amp; test "$(grep -c 'valueRevealed ? valueReserve.width : 0' $F)" = 1 &amp;&amp; echo TOGGLE-OK</automated>
    <human-check>At rest the cpu/ram/disk pills show three glyphs and no numbers, and the capsule is visibly narrower. Clicking a glyph makes its value appear beside it on the same pill; clicking again hides it.</human-check>
  </verify>
  <reversibility rating="reversible">Additive property plus one extended MouseArea in one file.</reversibility>
  <done>cpu, ram and disk render icon-only at rest, each reveals its value in place on click and hides it again, the capsule actually narrows at rest, and `resourcesPopoutTrigger`/`ResourcesPopout` still work.</done>
</task>

<task type="auto">
  <name>Task 3: Threshold colour on the resource glyphs and a filled updates alert pill</name>
  <files>quickshell/.config/quickshell/modules/bar/SystemCapsule.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` — as Task 2 left it; specifically the `Readout` glyph `Text` (82-87), the three `cpuStateValue`/`memoryStateValue`/`storageStateValue` register reads (114-120), and the updates `Readout` (196-205).
    - `waybar/.config/waybar/style-athena.scss` lines 122-146 — the `.warning` / `.critical` rules for `#cpu`/`#memory`/`#disk` (foreground colour only, `@warn` then `@danger`), and the `#custom-updates` rule (a FILLED pill: `background: @fill-updates; color: @fill-updates-fg; font-weight: bold`).
    - `waybar/.config/waybar/config-athena.jsonc` — the `states` blocks giving cpu warning 75 / critical 90, memory warning 75 / critical 85, disk warning 80 / critical 90.
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` — the `warn`, `danger`, `capsuleFg`, `fillUpdates`, `fillUpdatesFg` row names.
  </read_first>
  <action>
Give `Readout` threshold-driven colour. Add `property real metricFraction: -1`, `property real warnThreshold: -1` and `property real dangerThreshold: -1` (all inert at their `-1` default), plus a `readonly property color severityColour` resolving in this precedence: `errored` → `BarRoles.danger`; else `metricFraction >= dangerThreshold` (with `dangerThreshold >= 0`) → `BarRoles.danger`; else `metricFraction >= warnThreshold` (with `warnThreshold >= 0`) → `BarRoles.warn`; else `root.contentColour`. The comparison is `>=` at BOTH boundaries, so a metric sitting exactly on its threshold takes the more severe colour — this is the boundary case the requirement is judged on, and it must be stated as `>=` in source, not left to a `>` that silently makes the threshold value itself neutral. Bind the glyph `Text`'s `color` to `severityColour` and the value `Text`'s `color` to `severityColour` as well, so the revealed value carries the same state.

Set the thresholds on the three instances from Athena's own `states` numbers, expressed as fractions to match what `systemResources` publishes: cpu `warnThreshold: 0.75` / `dangerThreshold: 0.90`, ram `0.75` / `0.85`, disk `0.80` / `0.90`. Bind each instance's `metricFraction` to the matching `systemResources` fraction (`cpuFraction`, `memoryFraction`, `storageFraction`), guarded against a null backend the same way the existing `valueText` bindings are.

A metric whose register reads `pending` is NOT an error — the existing code already distinguishes `pending` from `empty` and only `empty` sets `errored`. Preserve that distinction exactly: a pending metric renders neutral, with no value even when the user has revealed it, and never tints danger. Do not widen `errored` to cover `pending`.

Give the updates entry Athena's filled alert pill (D-12). Add a `Rectangle` with `id: updatesFillPill` behind the updates `Readout`, `radius: height / 2`, `color: BarRoles.fillUpdates`, `visible` bound to the same `pendingUpdatesCount > 0` condition the `Readout` already uses, with the same motion-gated `Behavior on color` shape `BarCapsule.qml` uses. Override the updates `Readout`'s content colour to `BarRoles.fillUpdatesFg` (add a `contentOverride` property on `Readout` defaulting to an unset sentinel, or bind the updates instance's `severityColour` inputs so it resolves `fillUpdatesFg`) and set `font.weight` on its value `Text` to `Design.weightBody`'s bold sibling if one exists, matching Athena's `font-weight: bold`; if no bold token exists in `Design.qml`, leave the weight alone and record the omission in the SUMMARY rather than minting a raw numeric weight.

The updates pill keeps `clickable: true` and its `onActivated: root.launchUpgrade()` — do not give it `valueToggleable`. Its click starts a package upgrade, and making that click a display toggle instead would silently remove a working action.

After the edit, this file must contain no direct global-palette reference on any code line.
  </action>
  <acceptance_criteria>
    - `sed 's|//.*||' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml | grep -c 'Colours\.'` returns exactly `0`.
    - `grep -c 'readonly property color severityColour' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1`.
    - `grep -o 'warnThreshold: 0\.[0-9]*' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml | sort` yields exactly `warnThreshold: 0.75`, `warnThreshold: 0.75`, `warnThreshold: 0.80`.
    - `grep -o 'dangerThreshold: 0\.[0-9]*' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml | sort` yields exactly `dangerThreshold: 0.85`, `dangerThreshold: 0.90`, `dangerThreshold: 0.90`.
    - `grep -c '>= readoutItem.dangerThreshold\|>= readoutItem.warnThreshold' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `2` — both boundaries are inclusive, and no `>` form appears in the severity expression.
    - `grep -c 'id: updatesFillPill' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1`, and `grep -c 'BarRoles.fillUpdates\b'` and `grep -c 'BarRoles.fillUpdatesFg'` each return at least `1`.
    - `grep -c 'valueToggleable: true' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` still returns exactly `3` — the updates entry did not gain one.
    - `grep -c 'onActivated: root.launchUpgrade()' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1` — the upgrade action survives.
    - `grep -c 'errored: root.cpuStateValue === "empty"' quickshell/.config/quickshell/modules/bar/SystemCapsule.qml` returns exactly `1` — the pending/empty distinction was not widened.
    - `$HOME/.cache/quickshell.log` carries no new QML error line after the edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles &amp;&amp; F=quickshell/.config/quickshell/modules/bar/SystemCapsule.qml &amp;&amp; test "$(sed 's|//.*||' $F | grep -c 'Colours\.')" = 0 &amp;&amp; test "$(grep -c 'readonly property color severityColour' $F)" = 1 &amp;&amp; test "$(grep -o 'warnThreshold: 0\.[0-9]*' $F | sort | tr '\n' ';')" = "warnThreshold: 0.75;warnThreshold: 0.75;warnThreshold: 0.80;" &amp;&amp; test "$(grep -o 'dangerThreshold: 0\.[0-9]*' $F | sort | tr '\n' ';')" = "dangerThreshold: 0.85;dangerThreshold: 0.90;dangerThreshold: 0.90;" &amp;&amp; test "$(grep -c 'id: updatesFillPill' $F)" = 1 &amp;&amp; test "$(grep -c 'valueToggleable: true' $F)" = 3 &amp;&amp; test "$(grep -c 'onActivated: root.launchUpgrade()' $F)" = 1 &amp;&amp; echo THRESHOLD-OK</automated>
    <human-check>With pending updates present, the updates entry is a solid coloured pill with contrasting text. Under load, a resource glyph visibly changes colour at its threshold.</human-check>
  </verify>
  <reversibility rating="reversible">Additive properties plus one Rectangle in one file.</reversibility>
  <done>Resource glyphs tint `warn` at and above their warning threshold and `danger` at and above their critical threshold, a pending metric stays neutral, and the updates entry renders as Athena's filled alert pill while keeping its upgrade click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `checkupdates` subprocess stdout → `pendingUpdatesCount` | Output of a local package-manager helper parsed by line count only; the strings themselves are never rendered or executed. |
| `systemResources` backend → `Readout.metricFraction` | In-process numeric values from the shell's own single sampler; no boundary crossed. |
| Pointer input → the extended `MouseArea` | Local user input only. |

No network surface, no authentication, no remote input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18.1-05 | Elevation of Privilege | `upgradeProcess` (`kitty -e paru -Syu`) | medium | mitigate | Already mitigated and PRESERVED unchanged: two fixed literal argv arrays, no shell invoked, nothing concatenated and nothing caller-supplied. This task must not convert the updates click into a display toggle nor rebuild the command as a string; the acceptance criteria assert `onActivated: root.launchUpgrade()` still exists exactly once. |
| T-18.1-06 | Tampering | `checkupdates` stdout parsing | low | mitigate | Already mitigated: only `split("\n")` and a non-empty-line count are taken; no field is rendered as text and no output reaches a command. Unchanged by this plan. |
| T-18.1-07 | Denial of Service | Timers added by the in-place toggle | low | mitigate | Acceptance criteria assert `repeat: true` occurs exactly once in the file (the pre-existing 30-minute updates poll), so no new repeating timer can land. This is also QBAR-11's zero-idle guard. |
| T-18.1-08 | Information Disclosure | Resource values revealed on a shared screen | low | accept | The values are the machine's own cpu/ram/disk percentages, already visible in the resources popout; making them click-revealed strictly reduces resting exposure. Accepted. |
| T-18.1-SC | Tampering | npm/pip/cargo/pacman installs | n/a | accept | This plan installs no packages; RESEARCH's Package Legitimacy Audit records "not applicable" for this phase. |
</threat_model>

<verification>
1. Neither `BarCapsule.qml` nor `SystemCapsule.qml` contains a direct global-palette reference on a code line.
2. `BarCapsule.contentColour` still exists under that name and resolves through `BarRoles`.
3. `BarCapsule.qml`'s geometry bindings (`radius`, `implicitWidth`, `implicitHeight`, `visible`, `iconFill`) are byte-unchanged.
4. cpu, ram and disk carry `valueToggleable: true`; the updates entry does not.
5. `resourcesPopoutTrigger`, its `sectionId` and its `ResourcesPopout` component are byte-unchanged (QBAR-09 guard).
6. Both threshold comparisons are `>=`, and the six threshold literals match Athena's `states` numbers exactly.
7. `repeat: true` occurs exactly once in `SystemCapsule.qml` (QBAR-11 guard).
8. `$HOME/.cache/quickshell.log` shows no new QML load error.
</verification>

<success_criteria>
Every capsule in the bar draws a translucent Athena capsule surface from `BarRoles`, the resource pills read as three bare glyphs at rest and reveal their value in place on click, threshold colour appears at exactly Athena's numbers, and pending updates render as a filled alert pill that still starts an upgrade when clicked.
</success_criteria>

<output>
Create `.planning/phases/18.1-qml-bar-athena-restoration/18.1-02-SUMMARY.md` when done.
</output>