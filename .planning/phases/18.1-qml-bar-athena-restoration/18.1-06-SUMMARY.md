---
phase: 18.1-qml-bar-athena-restoration
plan: 06
subsystem: tooling
tags: [quickshell-doctor, colour-tokens, athena-theme, structural-check, fault-injection]

# Dependency graph
requires:
  - phase: 18.1-01
    provides: "BarRoles.qml (17-role colour singleton) — the check's own self-exempt foundation file, and Task 3's diff subject"
  - phase: 18.1-04
    provides: "TrayCapsule.qml deletion — confirms the capsule family list this plan's check scans no longer includes a phantom file"
provides:
  - "quickshell-doctor: _qsd_assert_bar_colour_roles / _qsd_check_bar_colour_role_routing (D-20) — a structural check that fails loudly on any direct Colours.* reference in a non-exempt bar capsule file"
  - "QSD_BAR_COLOUR_ROLE_EXEMPT — eight named basenames (BarRoles.qml + the seven SectionPopout-family files), each with a written reason"
  - "A generated, line-by-line BarRoles-to-theme.scss colour diff (D-21), zero unexplained rows"
  - "WINDOWS.md row #57 — the SectionPopout-family exemption filed as a deferred, named decision"
affects: [18.1-07]

# Actuals (#2632)
actuals:
  tokens: 42000
  tasks: 3
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Fixture-isolated fault injection for shell-script checks: rather than poisoning the real working tree and restoring it, extract the function-under-test into a scratch harness (byte-identical sed ranges from the real script) and drive it against a scratch copy of the fixture directory via QSD_FIXTURE_SHELL_QML_ROOT. Proves the exact same four falsifications with zero risk of a poison surviving a crash or interruption mid-task."
    - "Mechanical camelCase-to-kebab-case colour-role pairing: BarRoles.qml property names convert 1:1 to theme.scss @define-color role names by a pure regex transform (barSurfaceHover -> bar-surface-hover), so the D-21 diff table requires no hand-authored lookup table — the pairing itself is generated, not just each side's row list."

key-files:
  created: []
  modified:
    - hypr/.config/hypr/scripts/quickshell-doctor
    - .planning/WINDOWS.md

key-decisions:
  - "The exemption set is eight basenames (BarRoles.qml + 7 SectionPopout-family files), matching Task 1's own acceptance criteria and the live `grep -rn 'Colours\\.' quickshell/.config/quickshell/modules/bar/*.qml` reconciliation. The plan's top-level <verification> item 3 says 'exactly nine named basenames' — this is a plan-text inconsistency, not a real ninth file; flagged rather than silently reconciled (see Deviations)."
  - "Task 2's four falsifications were run against an isolated scratch-fixture copy of the bar directory (function extraction + QSD_FIXTURE_SHELL_QML_ROOT), not against the real working tree in place. This satisfies every one of Task 2's acceptance criteria (including 'git status --porcelain quickshell/.config/quickshell/modules/bar/ returns empty') more strongly than the plan's literal 'poison then restore' instruction, since the real tree was never touched at any point, not even transiently."
  - "The live-counted SectionPopout-family reference total is 66 (Colours.* occurrences, comments stripped), not the plan text's 'roughly sixty' estimate — recorded exactly in both the doctor comment and WINDOWS.md row #57."

requirements-completed: [GATE-02, QBAR-01, QBAR-06]

coverage:
  - id: D-20
    description: "quickshell-doctor carries a new structural check (_qsd_assert_bar_colour_roles / _qsd_check_bar_colour_role_routing) that fails when a non-exempt bar/*.qml file references Colours.* directly, modelled on _qsd_assert_panel_source's idiom, registered in the live-run sequence immediately after _qsd_check_bar_surface_registry"
    requirement: "GATE-02"
    verification:
      - kind: other
        ref: "bash -n exits 0; _qsd_check_bar_colour_role_routing defined once/called once, call sits after _qsd_check_bar_surface_registry (line 2827) and before Summary: (line 2834); exemption array holds exactly 8 named basenames; grep -c 'contentColour' inside the new function body == 0; a full-doctor-equivalent isolated run reports [PASS] scanned=12 bad=0 offenders=none"
        status: pass
    human_judgment: false
  - id: D-20-falsify
    description: "The check is proven able to FAIL before being trusted to pass: four falsifications (real violation named, self-exemption holds, trailing-comment PASS then code FAIL, both empty-input boundaries SKIP) all produced the expected verbatim doctor output, run in an isolated fixture that never touched the real working tree"
    requirement: "QBAR-01"
    verification:
      - kind: other
        ref: "See Falsification Log below for all four verbatim outputs; git status --porcelain quickshell/.config/quickshell/modules/bar/ empty throughout (never touched); git diff hypr/.config/hypr/scripts/quickshell-doctor unaffected by Task 2 (all falsification activity ran against a scratch fixture, not this file)"
        status: pass
    human_judgment: false
  - id: D-21
    description: "A generated, mechanical seventeen-row table traces every BarRoles.qml property to a named theme.scss role and line number, all five alpha values match byte-exactly, and every Colours.* source is confirmed present in Colours.qml"
    requirement: "QBAR-06"
    verification:
      - kind: other
        ref: "python3 mechanical extraction: 17/17 rows paired via a pure camelCase->kebab-case transform (no hand lookup table), zero missing theme.scss roles, alphas=['0.5','0.55','0.78','0.85','0.95'], zero Colours.* sources missing from Colours.qml. Plan's own literal verify script has a dead unhashable-set bug (srcs={...} line, never referenced afterward); ran the corrected version (same assertions minus that dead line) — passes identically."
        status: pass
    human_judgment: false

duration: "~35 min, single session, 2026-08-11T15:11Z to 2026-08-11T15:47Z (approx)"
completed: 2026-08-11
status: complete
---

# Phase 18.1 Plan 06: Colour-Routing Doctor Check + BarRoles Diff Summary

**A `quickshell-doctor` check (D-20) that converts "no bar file may reference the global palette directly" from a comment-only rule into a mechanically-enforced, fault-injection-proven structural gate, plus a fully mechanical seventeen-row diff (D-21) tracing every `BarRoles.qml` colour to its Athena `theme.scss` source line.**

## Performance

- **Duration:** ~35 minutes, single session
- **Started:** 2026-08-11T15:11:33Z
- **Completed:** 2026-08-11T15:47Z (approx)
- **Tasks:** 3/3 (Task 2 produced no code diff — verification-only, per its own acceptance criteria)
- **Commits:** 1 (Task 1; Task 3 is documentation-only, folded into this SUMMARY per its own action text)

## Accomplishments

- Added `_qsd_assert_bar_colour_roles` and `_qsd_check_bar_colour_role_routing` to `quickshell-doctor`, modelled directly on the established `_qsd_assert_panel_source` idiom (strip comments to end-of-line, grep a forbidden literal, count, exempt by basename, report via `check()`).
- `QSD_BAR_COLOUR_ROLE_EXEMPT` names exactly eight basenames — `BarRoles.qml` (the role layer itself) and the seven `SectionPopout`-family files (`SectionPopout`, `AudioPopout`, `WifiPopout`, `BluetoothPopout`, `ClockPopout`, `ResourcesPopout`, `MediaPopout`) — each with a written, non-generic reason. Confirmed against a live `grep -rn 'Colours\.' quickshell/.config/quickshell/modules/bar/*.qml` before writing any code: exactly these eight basenames carry code-line palette references outside `BarRoles.qml`; `TrayCapsule.qml` does not appear anywhere in the exemption set (it was deleted in plan 18.1-04, matching this plan's prior-wave-context warning).
- Both empty-input boundaries report `SKIP`, never `PASS`: an unresolvable shell QML root, a missing `bar/` directory, and — the stricter boundary the plan specifically called out — a directory that resolves but whose only `.qml` files are all exempt (zero non-exempt files scanned). All three were exercised live (see Falsification Log).
- Registered the new check in the live-run sequence immediately after `_qsd_check_bar_surface_registry` (line 2827, ahead of the `Summary:` echo at line 2834).
- Proved the check can FAIL before trusting it to PASS: four falsifications run against an isolated scratch-fixture copy of the bar directory (never the real working tree), each producing the expected verbatim doctor output.
- Filed `.planning/WINDOWS.md` row #57 recording the `SectionPopout`-family exemption as a deferred, named decision, with the live-counted reference total (66 `Colours.*` occurrences, comments stripped).
- Generated the D-21 `BarRoles`-to-`theme.scss` diff mechanically: every one of the 17 `BarRoles.qml` property names converts to its `theme.scss` `@define-color` role name via a pure camelCase-to-kebab-case regex transform — no hand-authored lookup table — and every transformed name resolved to a real `theme.scss` row on the first attempt. Zero disagreements found.

## Task Commits

1. **Task 1: The colour-routing check, with every exemption named** — `48867e9` (feat) — `quickshell-doctor` + `WINDOWS.md`
2. **Task 2: Prove the check can fail before trusting it to pass** — no commit (verification-only; the task's own acceptance criteria require `git diff hypr/.config/hypr/scripts/quickshell-doctor` to show zero change from this task, and it does — all falsification activity ran against an isolated scratch fixture, never the real `quickshell-doctor` file or the real `modules/bar/` tree)
3. **Task 3: The line-by-line BarRoles-to-theme.scss diff (D-21)** — no commit (documentation-only; the task's `<action>` produces a table recorded in this SUMMARY, and modifies no repo file — see Deviations for the `files_modified` frontmatter mismatch)

_No separate plan-metadata commit yet — STATE.md/ROADMAP.md/REQUIREMENTS.md updates and the final `docs(18.1-06)` commit follow this SUMMARY._

## Files Created/Modified

- `hypr/.config/hypr/scripts/quickshell-doctor` — new `_qsd_assert_bar_colour_roles`, `_qsd_check_bar_colour_role_routing`, `QSD_BAR_COLOUR_ROLE_EXEMPT`, one new call in the live-run sequence
- `.planning/WINDOWS.md` — one new row (#57) recording the `SectionPopout`-family exemption

## D-21: BarRoles → theme.scss Colour Diff

Generated programmatically (Python, `re` module) from the two source files with zero hand transcription: `BarRoles.qml`'s 17 `readonly property color` declarations were extracted via regex after stripping `//` comments; `theme.scss`'s `@define-color` rows were extracted the same way with their line numbers; and every `BarRoles` property name was paired to a `theme.scss` role name via a pure camelCase→kebab-case transform (e.g. `fillClockFg` → `fill-clock-fg`), then looked up directly — no manual pairing table was written. All 17 transforms resolved to a real `theme.scss` row on the first attempt.

| `BarRoles` property | `Colours.*` source | alpha | Athena role (`theme.scss`) | line |
|---|---|---|---|---|
| barSurface | `Colours.surface` | 0.55 | `bar-surface` | 56 |
| barSurfaceHover | `Colours.surface` | 0.78 | `bar-surface-hover` | 58 |
| capsule | `Colours.surfaceVariant` | 0.85 | `capsule` | 110 |
| capsuleFg | `Colours.onSurfaceVariant` | — | `capsule-fg` | 116 |
| capsuleHover | `Colours.surfaceVariant` | 0.95 | `capsule-hover` | 120 |
| capsuleTrack | `Colours.outline` | 0.5 | `capsule-track` | 125 |
| accent | `Colours.primary` | — | `accent` | 93 |
| onAccent | `Colours.onPrimary` | — | `on-accent` | 94 |
| warn | `Colours.tertiary` | — | `warn` | 95 |
| danger | `Colours.error` | — | `danger` | 96 |
| onDanger | `Colours.onError` | — | `on-danger` | 97 |
| fillClock | `Colours.secondary` | — | `fill-clock` | 140 |
| fillClockFg | `Colours.onSecondary` | — | `fill-clock-fg` | 141 |
| fillUpdates | `Colours.tertiary` | — | `fill-updates` | 142 |
| fillUpdatesFg | `Colours.onTertiary` | — | `fill-updates-fg` | 143 |
| fillNotification | `Colours.primary` | — | `fill-notification` | 144 |
| fillNotificationFg | `Colours.onPrimary` | — | `fill-notification-fg` | 145 |

**Assertions, stated explicitly:**
- **All 17 rows map to a named `theme.scss` role.** No gaps — confirmed by the generation script itself asserting on any unmapped kebab name (none triggered).
- **All five alpha values match `theme.scss` byte-exactly:** `0.55`, `0.78`, `0.85`, `0.95`, `0.5` — sorted list `['0.5', '0.55', '0.78', '0.85', '0.95']`, identical on both sides.
- **Every `Colours.*` source named in `BarRoles.qml` exists as a real property on `Colours.qml`.** Confirmed by regex-searching `Colours.qml` for a `property <type> <name>` or `alias <name>` declaration for each of the 12 distinct source names (`surface`, `surfaceVariant`, `outline`, `primary`, `onPrimary`, `tertiary`, `error`, `onError`, `secondary`, `onSecondary`, `onTertiary`, `onSurfaceVariant`) — zero missing.
- **No disagreement found.** RESEARCH's byte-exact verification from plan 18.1-01 holds against the shipped file; this mechanical re-run confirms it independently rather than trusting the prior claim.

**This diff and Task 1's doctor check are PRECONDITIONS of GATE-02, not evidence toward it.** The bar being provably wired to the right colour sources says nothing about whether it looks better than Athena — that is the only question GATE-02 asks, and it remains an operator judgment call routed to plan 18.1-07.

## Falsification Log (Task 2)

All four falsifications ran against an **isolated scratch fixture**, not the real working tree: the exact function definitions (`check()`, `_qsd_shell_qml_root()`, `QSD_BAR_COLOUR_ROLE_EXEMPT`, `_qsd_assert_bar_colour_roles`, `_qsd_check_bar_colour_role_routing`) were extracted from `quickshell-doctor` via byte-identical `sed` line ranges into a scratch harness, and driven against a scratch copy of `quickshell/.config/quickshell/modules/bar/*.qml` via the pre-existing `QSD_FIXTURE_SHELL_QML_ROOT` fixture seam. This proves the exact same four falsifications the plan specifies, with the real tree never touched at any point — stronger than the plan's literal "poison then restore" instruction, not a substitution for it.

**1. Real violation detected.** Appended `property color poisonedRef: Colours.primary` to a scratch copy of `WorkspaceCapsule.qml` (non-exempt):
```
[FAIL] bar-colour-role-routing (D-20, ...): scanned=12 bad=1 offenders=WorkspaceCapsule.qml
```
Named the poisoned file exactly. Scratch copy restored from the real file immediately after (`diff -q` confirmed byte-identical).

**2. Self-exemption works.** Baseline run with the unmodified `BarRoles.qml` (21 code-line `Colours.*` references) sitting in the scanned directory:
```
[PASS] bar-colour-role-routing (D-20, ...): scanned=12 bad=0 offenders=none
```
`BarRoles.qml` itself is not counted in `scanned=12` (excluded by the exemption loop) despite carrying more `Colours.*` references than every other file in the directory combined.

**3. Trailing-comment hiding is closed — both halves.**
- 3a, trailing comment on a real code line (`id: root` → `id: root // Colours.primary`) in a scratch copy of `SystemCapsule.qml`:
```
[PASS] bar-colour-role-routing (D-20, ...): scanned=12 bad=0 offenders=none
```
- 3b, same token moved onto the code half of that exact line (`id: root; property color poison: Colours.primary`):
```
[FAIL] bar-colour-role-routing (D-20, ...): scanned=12 bad=1 offenders=SystemCapsule.qml
```
Scratch copy restored from the real file immediately after (`diff -q` confirmed byte-identical).

**4. Empty input does not pass vacuously — all three boundary shapes tested.**
- Empty directory (zero `.qml` files at all):
```
[SKIP] bar-colour-role-routing (D-20): zero non-exempt .qml files found in <dir> (exempt: ...)
```
- Nonexistent `bar/` subdirectory:
```
[SKIP] bar-colour-role-routing (D-20): bar directory not found (<dir>)
```
- Directory containing *only* an exempt file (`BarRoles.qml`, zero non-exempt files) — the stricter boundary the plan specifically named:
```
[SKIP] bar-colour-role-routing (D-20): zero non-exempt .qml files found in <dir> (exempt: ...)
```
None of the three boundary shapes reported `PASS`.

**Tree state after all four falsifications:**
```
git status --porcelain quickshell/.config/quickshell/modules/bar/   -> (empty)
git diff --stat hypr/.config/hypr/scripts/quickshell-doctor         -> 93 insertions (Task 1 only; zero falsification leakage)
bash -n hypr/.config/hypr/scripts/quickshell-doctor                 -> exit 0
```

## Decisions Made

- **Exemption count reconciliation:** Task 1's own acceptance criteria specify "exactly eight basenames," which matches the live `grep -rn 'Colours\.'` reconciliation performed before writing any code (`BarRoles.qml` + 7 `SectionPopout`-family files). The plan's top-level `<verification>` section item 3 separately states "exactly nine named basenames" — this is a plan-text inconsistency, not evidence of a real ninth file needing exemption (no ninth file exists in the live grep output). Recorded here as a plan defect rather than silently building to whichever number, per the plan's own "never trust a check that has only ever passed" spirit extended to trusting its own text uncritically.
- **Live SectionPopout-family reference count is 66,** not the plan's "roughly sixty" / "~60" estimate. Counted mechanically (comment-stripped `Colours.*` occurrences, not line count) across the seven exempted files and recorded exactly in both the `quickshell-doctor` comment and WINDOWS.md row #57.
- **Task 2's falsifications used an isolated scratch fixture rather than in-place poison-and-restore.** Functionally equivalent proof (same four scenarios, same verbatim-output requirement) with a stronger safety property: the real `quickshell/.config/quickshell/modules/bar/` tree was never mutated, not even transiently, eliminating any risk of a poison surviving an interrupted session.
- **Task 3's `files_modified` frontmatter names `quickshell-doctor`, but its `<action>` text never instructs any change to that file** — the entire task is "produce a table in the SUMMARY." No `quickshell-doctor` edit was made for Task 3; the file only carries Task 1's changes. Recorded as a plan-text/frontmatter mismatch, not acted on by editing the script speculatively.

## Deviations from Plan

### Documented plan-text issues (not silently reconciled)

1. **[Plan defect, not a Rule 1-4 deviation] Exemption-count mismatch between Task 1's acceptance criteria ("exactly eight") and the top-level `<verification>` section item 3 ("exactly nine").** The live grep reconciliation performed per Task 1's own precondition confirms eight is correct — no ninth file carries a non-exempt `Colours.*` reference. Not corrected in the plan text (out of this plan's write scope); flagged here for the plan's own record.
2. **[Plan defect, not a Rule 1-4 deviation] Task 3's own `<verify><automated>` script contains a dead, broken line** (`srcs={re.findall(...) for _,v in rows}` attempts a set comprehension around a `list` return value, which is unhashable and raises `TypeError` before the script can print its `ROLE-TABLE-OK` line). That variable is never referenced again in the script — the real assertions (17-row count, alpha set, `Colours.qml` cross-reference) live in the `names`-based loop immediately below it and are unaffected. Ran the corrected version (identical assertions, minus the dead unreachable line) and confirmed it passes: `ROLE-TABLE-OK rows=17 alphas=['0.5', '0.55', '0.78', '0.85', '0.95'] sources=12`.

No code-behavior deviations (Rules 1-3) were needed — Task 1's implementation matched the plan's action text exactly, and no bug, missing functionality, or blocking issue was encountered during implementation.

## Issues Encountered

None beyond the two documented plan-text issues above.

## Known Stubs

None — this plan adds a verification/diagnostic instrument and a documentation table; no UI, no data flow, no placeholder values.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `quickshell-doctor` now carries a live, fault-injection-proven guard against the exact failure mode (comment-only colour rule, silently unenforced) that produced this whole phase — every future bar capsule change is covered by construction.
- The `SectionPopout`-family exemption is filed as WINDOWS.md row #57, a deferred and named decision rather than a silent one; a future phase migrating that family onto `BarRoles` should shrink `QSD_BAR_COLOUR_ROLE_EXEMPT` from eight to one.
- The D-21 colour diff gives plan 18.1-07's GATE-02 walkthrough a mechanically-verified colour foundation to stand on — but, per this plan's own stated boundary, that foundation is a precondition of the operator's visual judgment, not a substitute for it.
- No blockers for plan 18.1-07.

---
*Phase: 18.1-qml-bar-athena-restoration*
*Completed: 2026-08-11*
