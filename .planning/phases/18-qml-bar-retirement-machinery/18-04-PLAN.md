---
phase: 18-qml-bar-retirement-machinery
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/WINDOWS.md
  - .planning/debug/panels-missing-animated-border.md
  - .planning/debug/resolved/panels-missing-animated-border.md
autonomous: true
requirements: [LEDGER-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PROJECT.md § Active no longer claims the Phase 15 panel family lacks the animated rim; the bullet is ticked and cites commit 4f48847 as the evidence."
    - "PROJECT.md § Current State says one known gap was carried into v4.0 (OVER-04's frame-rate term), not two."
    - "WINDOWS.md row 14 reads status `fixed` with a non-null `resolved_at` in BOTH the markdown table and the JSON block — the two representations agree."
    - "The panels-missing-animated-border debug session carries `status: resolved` with populated `fix:` and `verification:` fields and lives under .planning/debug/resolved/."
    - "Every v3.0 archive correction in MILESTONES.md is an appended dated supersession; the original as-of-close wording remains greppable, so what the audit knew at close is not rewritten."
    - "The three corrections touch four disjoint files with no shared state, so any execution order of Tasks 1-3 produces an identical tree."
    - statement: "Each before-state string this plan replaces is unique within its target file; where a string occurs more than once, the executor disambiguates by surrounding context rather than blind-replacing."
      verification: backstop
    - statement: "If a quoted before-state string is absent from its target file, the task halts and reports rather than silently completing with no edit."
      verification: backstop
  artifacts:
    - .planning/debug/resolved/panels-missing-animated-border.md
  key_links:
    - "WINDOWS.md's markdown table row 14 <-> WINDOWS.md's JSON entry id 14 — `/gsd-ship` gates on the register; hand-editing one representation desynchronizes it."
    - "MILESTONES.md line 26 (the gap) <-> MILESTONES.md line 33 (the debug-session roster) — closing the session changes the roster count, and leaving the roster stale re-creates the exact defect this plan exists to remove."
    - "PROJECT.md § Active bullet <-> commit 4f48847 <-> PanelDialog.qml:191 — the documentation claim and the code it describes."
  prohibitions:
    - "Do not assert in any document that a fix is in place without having read the working tree and confirmed it."
    - "Do not re-request the operator's visual confirmation of the panel rim; it was given on 2026-08-10 and treating recorded operator testimony as untrustworthy is not this plan's call to make."
    - "Do not delete or rewrite what the v3.0 milestone audit recorded as true at close; supersede it with a dated annotation instead."
    - "Do not execute `quickshell-doctor` to test anything in this plan; its headless-output add/remove test has SEGV-crashed this compositor before."
---

<objective>
Close LEDGER-01 as pure bookkeeping. Both carry-ins named in the requirement are
already fixed in code, and the visual confirmation has already been taken — the
remaining defect is that four documents still assert both are open.

Purpose: LEDGER-01's whole content is the gap between what the repo does and what
the repo says about itself. Every document corrected here is one that a future
phase, a milestone audit, or the operator would otherwise read and act on as if
the rim were still missing and `quickshell-doctor` still dead.

Output: PROJECT.md, MILESTONES.md and WINDOWS.md corrected; the
`panels-missing-animated-border` debug session flipped to `resolved` and curated
into `.planning/debug/resolved/`.

**Scope facts (stated, not silently omitted):**
- **No QML work.** This plan writes zero lines of code. Both fixes shipped on
  2026-08-02 in commit `4f48847` and in the 13.1 Lua migration respectively.
- **No visual check is requested and none may be added.** REQUIREMENTS.md records
  it verbatim: "Visual confirmation TAKEN 2026-08-10: the operator confirmed the
  audio, wifi and bluetooth panels all render the glowing rim." This plan is
  `autonomous: true` and contains no `checkpoint:human-verify` task by design.
- **No `## UI Considerations` rows are in scope.** This plan is documentation-only
  and renders no surface, so the phase UI-SPEC lift contributes nothing here.
- **Deliberately NOT touched, with reasons:**
  - `.planning/STATE.md:498,500` — an as-of-close snapshot of the v3.0 audit's open
    artifacts. Rewriting it would falsify the record of what was known at close.
    LEDGER-04 (Phase 19) re-curates the debug-session roster wholesale.
  - `.planning/milestones/v3.0-MILESTONE-AUDIT.md` — a signed archive artifact.
  - `.planning/PROJECT.md:23` — the v4.0 milestone's own goal statement
    ("Debt paydown, in full — `GradientBorder` reuse …"). This is scope to be
    delivered, not a claim of current brokenness; it is ticked at milestone close.
  - `.planning/PROJECT.md:228` — the Key Decisions outcome column reads "Pending"
    against *two* carry-ins. LEDGER-01 closes one; MAINT-02's Logout half is Phase
    20's LEDGER-02. It stays Pending until both land.
  - The five other debug sessions — LEDGER-04, Phase 19.

**Pre-verified by the planner against the working tree (2026-08-10), re-asserted by
Task 1 rather than trusted:**
- `PanelDialog.qml:191` is exactly the line `    GradientBorder {`. Commit
  `4f48847` is `feat(15-10): instantiate GradientBorder inside PanelDialog.qml`,
  2026-08-02 19:35:30, +24 lines, that one file.
- `quickshell-doctor` dispatches only through `_qsd_dispatch_global()` at line 243,
  which emits the `hl.dsp.global("$1")` Lua expression form. Every remaining
  occurrence of the withdrawn bare string form in that file is inside a comment
  documenting why it was withdrawn. Zero live legacy call sites remain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<!-- planner-discipline-allow: deliberately not fixed -->
<!-- planner-discipline-allow: Two known gaps carried into v4.0 -->
<!-- planner-discipline-allow: panels-missing-animated-border -->

<task type="tracer">
  <name>Task 1: Re-assert both code claims against the working tree, then correct PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 113-136 (§ Requirements > Active) and lines 154-166 (§ Current State)
    - `quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml` lines 185-200
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 236-250
  </read_first>
  <precondition>
    `sed -n '191p' quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml` contains `GradientBorder {`, `git log --oneline -1 4f48847` resolves, and `grep -v '^[[:space:]]*#' hypr/.config/hypr/scripts/quickshell-doctor | grep -q 'dispatch global'` exits non-zero. If ANY of these three is false, HALT and report — the entire plan is premised on both fixes being live, and correcting documentation toward a fix that is not there is strictly worse than leaving it stale.
  </precondition>
  <action>
    This is the tracer: it proves the evidence-to-document correction shape end-to-end
    on the single highest-visibility target before Tasks 2 and 3 propagate it.

    First assert the precondition above with real commands and record the three outputs
    in the SUMMARY. Do not proceed on the planner's word.

    Then make three edits to `.planning/PROJECT.md`.

    Edit 1 — § Requirements > Active, under the "**Debt paydown:**" sub-heading, the
    bullet currently at line 130. Its exact current wording is:
    `- [ ] **Reuse `GradientBorder` across the panel family** — the animated rim has exactly one consumer (`Dashboard.qml:387`); the three Phase 15 panels built on `PanelDialog.qml` carry none. Diagnosed 2026-08-02 (debug session `panels-missing-animated-border`, 100% reproducible), deliberately not fixed. A stated user expectation v3.0 left unmet.`
    This is the live v4.0 scope list, not an archive, so REPLACE the bullet outright
    rather than annotating it. Replacement claim, exact: a ticked `- [x]` bullet with
    the same bold lead `**Reuse `GradientBorder` across the panel family**`, then an
    em-dash, then: CLOSED 2026-08-10 under LEDGER-01. The rim already shipped in commit
    `4f48847` (`feat(15-10): instantiate GradientBorder inside PanelDialog.qml`,
    2026-08-02) — `PanelDialog.qml:191` instantiates `GradientBorder` between
    `background` and `content`, so all three panels built on that shared frame inherit
    it. The 2026-08-02 diagnosis and its fix landed the same day and the debt entry was
    never updated; the code was never actually broken past that commit. Operator
    visually confirmed on 2026-08-10 that the audio, wifi and bluetooth panels all
    render the glowing rim. The replacement must not carry the words the old bullet used
    to describe the fix's absence, and must not name the debug session by filename —
    Task 3 relocates that file.

    Edit 2 — § Current State, the paragraph currently at line 160, whose exact current
    opening is `**Two known gaps carried into v4.0**, both real rather than bookkeeping: the animated `GradientBorder` has exactly one consumer, so the three Phase 15 panels have no rim (a stated user expectation the milestone left unmet); and OVER-04's frame-rate term was never measured, only its CPU term.`
    Replace with a one-gap statement: bold lead `**One known gap carried into v4.0**`,
    stating that OVER-04's frame-rate term was never measured (only its CPU term) and is
    LEDGER-03's subject in this same phase; then a clause recording that the second gap
    the v3.0 close listed — the panel-family rim — was found already fixed in code by
    `4f48847` and closed on 2026-08-10 under LEDGER-01. Keep OVER-04's wording intact.

    Edit 3 — § Current State, the Phase 13.1 paragraph at line 164. This one is a
    historical phase narrative, so ANNOTATE, do not rewrite. Its closing sentence
    currently ends `…and `quickshell-doctor` retains 8 legacy dispatch sites.` Append
    immediately after that sentence, in the same paragraph, a bracketed clause reading
    exactly: `[Closed 2026-08-10 (LEDGER-01): quickshell-doctor now dispatches only through _qsd_dispatch_global() at line 243, which emits the hl.dsp.global() Lua expression form; the bare string form survives only inside comments explaining its withdrawal. WINDOWS #14 marked fixed.]`
  </action>
  <acceptance_criteria>
    - `grep -c 'deliberately not fixed' .planning/PROJECT.md` returns `0`.
    - `grep -c 'Two known gaps carried into v4.0' .planning/PROJECT.md` returns `0`.
    - `grep -c 'One known gap carried into v4.0' .planning/PROJECT.md` returns `1`.
    - `grep -c '4f48847' .planning/PROJECT.md` returns `2` or more (Edit 1 and Edit 2 both cite it).
    - `grep -c 'Closed 2026-08-10 (LEDGER-01)' .planning/PROJECT.md` returns `1`.
    - `grep -c '^- \[x\] \*\*Reuse `GradientBorder` across the panel family\*\*' .planning/PROJECT.md` returns `1`.
    - `grep -c 'panels-missing-animated-border' .planning/PROJECT.md` returns `0`.
    - `grep -c 'the three Phase 15 panels have no rim' .planning/PROJECT.md` returns `0`.
    - `grep -c 'retains 8 legacy dispatch sites' .planning/PROJECT.md` returns `1` (the historical sentence is preserved, only annotated).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles && sed -n '191p' quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml | grep -q 'GradientBorder {' && ! grep -v '^[[:space:]]*#' hypr/.config/hypr/scripts/quickshell-doctor | grep -q 'dispatch global' && ! grep -q 'deliberately not fixed' .planning/PROJECT.md && ! grep -q 'Two known gaps carried into v4.0' .planning/PROJECT.md && grep -q 'One known gap carried into v4.0' .planning/PROJECT.md && grep -q '4f48847' .planning/PROJECT.md && grep -q 'Closed 2026-08-10 (LEDGER-01)' .planning/PROJECT.md && ! grep -q 'panels-missing-animated-border' .planning/PROJECT.md && grep -q 'retains 8 legacy dispatch sites' .planning/PROJECT.md && echo TASK1_PASS</automated>
  </verify>
  <done>PROJECT.md makes no live claim that either carry-in is open; the Active bullet is ticked and cites `4f48847`; the Current State gap count is one; the Phase 13.1 narrative is preserved with a dated closure annotation appended.</done>
</task>

<task type="auto">
  <name>Task 2: Supersede the three stale claims in the v3.0 archive and close WINDOWS #14</name>
  <files>.planning/MILESTONES.md, .planning/WINDOWS.md</files>
  <read_first>
    - `.planning/MILESTONES.md` lines 23-39 (§ v3.0 "Known gaps at close" and "Tech debt carried into v4.0")
    - `.planning/WINDOWS.md` lines 12-20 (register header and table columns) and lines 199-212 (JSON entry id 14)
    - `.planning/PROJECT.md` line 144 — the repo's existing precedent for a dated supersession annotation on a historical claim (`~~struck~~` + "**REVERSED 2026-07-26 …**"). Follow that shape.
  </read_first>
  <action>
    MILESTONES.md § v3.0 is a shipped-milestone archive. Its "Known gaps at close" list
    records what the audit knew on 2026-08-10 at close, and that record must stay
    readable. So for the two prose entries: APPEND a dated supersession sentence to the
    existing bullet. Do not delete, do not strike through the whole bullet, do not
    restate the gap as if it were never believed.

    Edit 1 — the bullet at line 26 whose exact current opening is
    `- **`GradientBorder` reuse across the 14 -> 15 seam.**` and which currently ends
    `…deliberately not fixed under diagnose-only mode. A stated user expectation the milestone left unmet, not an accepted limitation.`
    Append to the end of that same bullet, as a new sentence: a bold
    `**SUPERSEDED 2026-08-10 (LEDGER-01):**` lead, then the correction — this gap was
    already closed in code before the milestone shipped. Commit `4f48847`
    (`feat(15-10): instantiate GradientBorder inside PanelDialog.qml`, 2026-08-02
    19:35:30) added the rim to `PanelDialog.qml:191`, the same day as the diagnosis;
    all three panels inherit it from the shared frame. The audit's statement was
    accurate to the debug session's frontmatter, which was never updated after the fix
    landed. Operator visually confirmed all three panels render the rim on 2026-08-10.

    Edit 2 — the bullet at line 36 under "**Tech debt carried into v4.0:**" whose exact
    current opening is `- WINDOWS #14: ~8 legacy `hyprctl dispatch global <name>` call sites in `quickshell-doctor` remain on the withdrawn string form, silently dead under the Lua config manager.`
    Append to the end of that same bullet, as a new sentence: a bold
    `**SUPERSEDED 2026-08-10 (LEDGER-01):**` lead, then — the file now dispatches only
    through `_qsd_dispatch_global()` at line 243, which emits the
    `hl.dsp.global()` Lua expression form; the bare string form survives only inside
    comments documenting its withdrawal. Zero live legacy sites remain and WINDOWS #14
    is marked fixed. Preserve the existing SEGV caution sentence in that bullet verbatim
    — it remains a live operational hazard for anyone who picks the script up.

    Edit 3 — the bullet at line 33, which is a roster rather than a narrative claim and
    so IS edited in place. Its exact current opening is
    `- 6 unresolved debug sessions in `.planning/debug/` (5 `diagnosed`, 1 `unknown`):`
    followed by six backticked names. Change the count from 6 to 5, change the
    parenthetical from `(5 `diagnosed`, 1 `unknown`)` to `(4 `diagnosed`, 1 `unknown`)`,
    and remove the `panels-missing-animated-border` name from the comma-separated list
    so five names remain. Append a trailing clause noting that the removed session was
    resolved on 2026-08-10 under LEDGER-01 and that the remaining five are LEDGER-04's
    subject in Phase 19. Do not touch the existing sentence about
    `.planning/debug/resolved/` having been curated.

    WINDOWS.md — do NOT hand-edit. That file carries the same register twice, as a
    markdown table row and as a JSON object, and `/gsd-ship` gates on it; editing one
    representation and not the other silently desynchronizes the gate. Use the sanctioned
    mutator instead: run the `windows fixed` subcommand of the gsd-tools shim at
    `$HOME/.claude/gsd-core/bin/gsd-tools.cjs` against id 14, via `node`. Confirm
    afterwards that both representations moved together and that `resolved_at` is
    populated. Under no circumstances execute `quickshell-doctor` itself to "check" the
    fix — the claim is verified by static grep only, per this plan's prohibitions.
  </action>
  <acceptance_criteria>
    - `grep -c 'SUPERSEDED 2026-08-10 (LEDGER-01)' .planning/MILESTONES.md` returns `2`.
    - `grep -c 'deliberately not fixed under diagnose-only mode' .planning/MILESTONES.md` returns `1` — the as-of-close wording is preserved, not erased.
    - `grep -c 'SEGV-crashed this compositor' .planning/MILESTONES.md` returns `1` — the hazard caution survives.
    - `grep -c '5 unresolved debug sessions' .planning/MILESTONES.md` returns `1`.
    - `grep -c '6 unresolved debug sessions' .planning/MILESTONES.md` returns `0`.
    - `grep 'unresolved debug sessions' .planning/MILESTONES.md | grep -c 'panels-missing-animated-border'` returns `0` (region-scoped: the name legitimately remains on line 26's superseded bullet).
    - `grep 'unresolved debug sessions' .planning/MILESTONES.md | grep -c 'bluetooth-enable-inert'` returns `1` — the other five names are intact.
    - `grep -c '| 14 |' .planning/WINDOWS.md` returns `1`, and that line contains `| fixed |`.
    - `grep -A 10 '"id": 14,' .planning/WINDOWS.md | grep -c '"status": "fixed"'` returns `1`.
    - `grep -A 10 '"id": 14,' .planning/WINDOWS.md | grep -c '"resolved_at": "20'` returns `1` (a real ISO timestamp, not `null`).
    - `grep -c '"status": "open"' .planning/WINDOWS.md` returns one fewer than it did before this task ran (record both numbers in the SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles && grep -c 'SUPERSEDED 2026-08-10 (LEDGER-01)' .planning/MILESTONES.md | grep -qx 2 && grep -q 'deliberately not fixed under diagnose-only mode' .planning/MILESTONES.md && grep -q 'SEGV-crashed this compositor' .planning/MILESTONES.md && grep -q '5 unresolved debug sessions' .planning/MILESTONES.md && ! grep -q '6 unresolved debug sessions' .planning/MILESTONES.md && [ "$(grep 'unresolved debug sessions' .planning/MILESTONES.md | grep -c 'panels-missing-animated-border')" = "0" ] && grep '| 14 |' .planning/WINDOWS.md | grep -q '| fixed |' && grep -A 10 '"id": 14,' .planning/WINDOWS.md | grep -q '"status": "fixed"' && grep -A 10 '"id": 14,' .planning/WINDOWS.md | grep -q '"resolved_at": "20' && echo TASK2_PASS</automated>
  </verify>
  <done>MILESTONES.md's two stale narrative gaps carry dated supersession sentences with their original wording intact; its debug-session roster reads 5 and no longer names the resolved session; WINDOWS row 14 is `fixed` with `resolved_at` set in both the table and the JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the panels-missing-animated-border debug session to resolved and curate it</name>
  <files>.planning/debug/panels-missing-animated-border.md, .planning/debug/resolved/panels-missing-animated-border.md</files>
  <read_first>
    - `.planning/debug/panels-missing-animated-border.md` — full file, in particular the frontmatter block (lines 1-6) and the closing `## Resolution` block whose `fix:` and `verification:` fields both read `[not applied — goal: find_root_cause_only]`
    - `.planning/debug/resolved/waybar-workspace-click-dead.md` lines 1-6 — the frontmatter convention for a resolved session (`status: resolved`, `updated:` bumped)
  </read_first>
  <reversibility rating="reversible">A frontmatter field change plus a `git mv` inside `.planning/`; fully revertable with a single `git revert` and touches no runtime path.</reversibility>
  <action>
    Edit the frontmatter: change `status: diagnosed` to `status: resolved`, and bump
    `updated:` from `2026-08-02T08:50:00Z` to `2026-08-10T00:00:00Z`. Leave `trigger:`
    and `created:` untouched.

    In the `## Resolution` block at the end of the file, replace the two placeholder
    fields. `fix:` currently reads `"[not applied — goal: find_root_cause_only]"` —
    replace with a description of the fix that actually shipped: `GradientBorder` was
    instantiated inside `PanelDialog.qml` between `background` and `content` in commit
    `4f48847` (`feat(15-10): instantiate GradientBorder inside PanelDialog.qml`,
    2026-08-02 19:35:30, +24 lines, that file only), landing at `PanelDialog.qml:191`.
    All three panels inherit it from the shared frame with zero call-site changes —
    exactly the reuse path this session's own experiments predicted was unobstructed.

    `verification:` currently reads `"[not applied — goal: find_root_cause_only]"` —
    replace with the two-part evidence: static confirmation that
    `PanelDialog.qml:191` instantiates `GradientBorder`, plus the operator's visual
    confirmation on 2026-08-10 that the audio, wifi and bluetooth panels all render the
    glowing rim. Note explicitly that the session sat at `status: diagnosed` for eight
    days after its own fix had already shipped, because the fix landed through the normal
    Phase 15 plan track (15-10) rather than through this session, and nothing closed the
    loop back to the frontmatter — that lag is the reusable lesson and belongs in the
    record.

    Set `files_changed:` from `[]` to a single-element list naming
    `quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml`.

    Do NOT alter the investigation body — the hypothesis, evidence, falsification test,
    timestamped checks and `root_cause` block are the session's value and were correct.

    Finally, curate the file into the resolved directory with `git mv` (not copy-then-
    delete, so the history follows) to
    `.planning/debug/resolved/panels-missing-animated-border.md`. Both other resolved
    sessions already live there; leaving it at the top level would keep it counted as
    open by anything that enumerates `.planning/debug/*.md`.
  </action>
  <acceptance_criteria>
    - `.planning/debug/panels-missing-animated-border.md` does not exist.
    - `.planning/debug/resolved/panels-missing-animated-border.md` exists.
    - `sed -n '2p' .planning/debug/resolved/panels-missing-animated-border.md` is exactly `status: resolved`.
    - `grep -c 'not applied — goal: find_root_cause_only' .planning/debug/resolved/panels-missing-animated-border.md` returns `0`.
    - `grep -c '4f48847' .planning/debug/resolved/panels-missing-animated-border.md` returns `1` or more.
    - `grep -c 'PanelDialog.qml' .planning/debug/resolved/panels-missing-animated-border.md` returns `1` or more inside the `files_changed:` list.
    - `grep -c 'root_cause:' .planning/debug/resolved/panels-missing-animated-border.md` returns `1` — the investigation body is intact.
    - `ls .planning/debug/*.md | wc -l` returns `5`.
    - `git status --porcelain .planning/debug/` shows a rename (`R`) entry, not an add plus a delete.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aorus/dotfiles && [ ! -e .planning/debug/panels-missing-animated-border.md ] && [ -f .planning/debug/resolved/panels-missing-animated-border.md ] && [ "$(sed -n '2p' .planning/debug/resolved/panels-missing-animated-border.md)" = "status: resolved" ] && ! grep -q 'not applied — goal: find_root_cause_only' .planning/debug/resolved/panels-missing-animated-border.md && grep -q '4f48847' .planning/debug/resolved/panels-missing-animated-border.md && grep -q 'root_cause:' .planning/debug/resolved/panels-missing-animated-border.md && [ "$(ls .planning/debug/*.md | wc -l)" = "5" ] && echo TASK3_PASS</automated>
  </verify>
  <done>The session reads `status: resolved` with real `fix:`/`verification:`/`files_changed:` content, its investigation body untouched, and it has been `git mv`'d into `.planning/debug/resolved/` leaving exactly five open sessions for LEDGER-04.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan produces no code and no new runtime surface. It changes exactly the
following document sections and status fields:

| File | Section / field | Change |
|------|-----------------|--------|
| `.planning/PROJECT.md` | § Requirements > Active, "Debt paydown" `GradientBorder` bullet | `- [ ]` → `- [x]`, stale claim replaced with commit evidence |
| `.planning/PROJECT.md` | § Current State, v3.0 carried-gaps paragraph | two gaps → one gap (OVER-04 only) |
| `.planning/PROJECT.md` | § Current State, Phase 13.1 narrative | dated closure clause appended; original sentence preserved |
| `.planning/MILESTONES.md` | § v3.0 "Known gaps at close", `GradientBorder` bullet | dated `SUPERSEDED` sentence appended |
| `.planning/MILESTONES.md` | § v3.0 "Tech debt carried into v4.0", WINDOWS #14 bullet | dated `SUPERSEDED` sentence appended; SEGV caution preserved |
| `.planning/MILESTONES.md` | § v3.0 "Tech debt carried into v4.0", debug-session roster | count 6 → 5; one name removed |
| `.planning/WINDOWS.md` | row 14 — markdown table `status` + JSON `status`/`resolved_at` | `open` → `fixed`, via `gsd-tools windows fixed 14` |
| `.planning/debug/panels-missing-animated-border.md` | frontmatter `status`, `updated`; `## Resolution` `fix:`, `verification:`, `files_changed:` | `diagnosed` → `resolved`; placeholders replaced with evidence |
| — path | `.planning/debug/` → `.planning/debug/resolved/` | `git mv` |

Requirement status field to flip at phase close (not by this plan): `LEDGER-01`
in `.planning/REQUIREMENTS.md` and its § Coverage row.

## Edge coverage (specless fallback)

No `18-04-SPEC.md` exists, so the edge probe ran without an explicit edge table.
All three probes returned against LEDGER-01 arrived `unresolved`; each is resolved
here rather than dismissed:

| Probe | Resolution | Where |
|-------|-----------|-------|
| adjacency — "when two things are exactly equal or just touch, do they merge, collide, or separate?" | Resolved (backstop). Read as: a before-state string that occurs more than once in its target file makes the replacement ambiguous. The executor disambiguates by surrounding context; Task 2's `panels-missing-animated-border` gate is region-scoped for exactly this reason, since the name legitimately survives on one line and must vanish from another. | `must_haves.truths` (backstop) |
| empty — "what is the result for empty, single-element, or null input?" | Resolved (backstop). Read as: a quoted before-state string that is absent from its file. The task halts and reports rather than completing green with zero edits — the silent-no-op failure mode that makes a docs correction look done when nothing moved. | `must_haves.truths` (backstop) |
| ordering — "when elements compare equal, is output order specified and stable?" | Resolved (explicit). The three tasks touch four disjoint files and share no state, so any execution order yields an identical tree. Task 1 is nonetheless ordered first because its precondition can abort the whole plan. | `must_haves.truths` (explicit) |

## Prohibition provenance

Two-stage recall produced ten raw candidates. Routine-engineering items (do not
touch QML; do not hand-edit both WINDOWS representations separately; do not rewrite
git history) were dropped — they are already enforced by this plan's acceptance
criteria. Canon security and compliance items (no secrets in commits; validate
input before shelling out) were dropped as project canon rather than restated here.
The four survivors in `must_haves.prohibitions` are the values, integrity and
operational-safety constraints specific to this work.

<threat_model>
## Trust Boundaries

This plan crosses **no runtime trust boundary**. It executes no service, accepts no
external input, opens no socket, and installs no package — there is no
`T-18-04-SC` supply-chain row because no package-manager install occurs. The only
boundary in scope is the repository record itself: the boundary between what the
working tree does and what the planning documents assert it does, which is the exact
boundary LEDGER-01 exists to repair.

| Boundary | Description |
|----------|-------------|
| working tree ↔ planning record | Documented claims about code state are consumed by later phases, milestone audits and `/gsd-ship` gates as if authoritative |
| `.planning/WINDOWS.md` dual representation | One register serialized twice (markdown table + JSON), both read by tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04-01 | Tampering | `.planning/WINDOWS.md` — table row 14 and JSON entry id 14 | medium | mitigate | Hand-editing one representation leaves the register internally inconsistent and `/gsd-ship`'s `open_count` gate reading a different truth than the human-readable table. Task 2 mandates the `gsd-tools windows fixed 14` mutator, which writes both, and its acceptance criteria assert `status: fixed` plus a non-null `resolved_at` independently in each representation. |
| T-18-04-02 | Repudiation | `.planning/MILESTONES.md` § v3.0 "Known gaps at close" | medium | mitigate | Deleting an as-of-close gap statement destroys the evidence of what the v3.0 audit knew when it chose an `override_closeout`, making the closeout decision unauditable after the fact. Mitigated by treating archive entries as append-only: Task 2 appends a dated `SUPERSEDED` sentence and its acceptance criteria positively assert the original wording (`deliberately not fixed under diagnose-only mode`) still greps. |
| T-18-04-03 | Denial of Service | live Hyprland compositor session, via `quickshell-doctor` | medium | mitigate | WINDOWS #14 records that this script's headless-output add/remove test previously SEGV-crashed this compositor during a DP-1 hotplug, taking down the operator's live session. Running it to "confirm" the dispatch fix would risk the host for no added information. Mitigated absolutely: the dispatch claim is verified by static grep over the file only, the prohibition is stated in `must_haves.prohibitions`, and no task's `<verify>` invokes the script. |
| T-18-04-04 | Spoofing | documented fix claims | high | mitigate | Asserting in PROJECT.md and MILESTONES.md that a fix is live when it is not is the single high-severity failure mode of this plan — a future phase would build on a false premise and the operator would trust a repaired record over the code. Mitigated by Task 1's `<precondition>`, which re-asserts all three code facts (`PanelDialog.qml:191`, commit `4f48847`, zero live legacy dispatch sites) with real commands and HALTS the plan on any failure, before a single document is edited. |

ASVS L1, block on `high`. `T-18-04-04` is high and is mitigated by a blocking
precondition, not accepted. No threat is left open.
</threat_model>

<verification>
Run from the repository root after all three tasks:

1. `! grep -q 'deliberately not fixed' .planning/PROJECT.md` — no live document asserts the rim gap is open.
2. `grep -c 'SUPERSEDED 2026-08-10 (LEDGER-01)' .planning/MILESTONES.md` returns `2` and `grep -q 'deliberately not fixed under diagnose-only mode' .planning/MILESTONES.md` still succeeds — the archive is superseded, not rewritten.
3. `grep '| 14 |' .planning/WINDOWS.md | grep -q '| fixed |'` **and** `grep -A 10 '"id": 14,' .planning/WINDOWS.md | grep -q '"status": "fixed"'` **and** `grep -A 10 '"id": 14,' .planning/WINDOWS.md | grep -q '"resolved_at": "20'` — the table and the JSON agree, and `resolved_at` carries a real timestamp rather than `null`.
4. `[ "$(ls .planning/debug/*.md | wc -l)" = "5" ]` and `.planning/debug/resolved/panels-missing-animated-border.md` reads `status: resolved`.
5. `git status --porcelain` shows changes confined to `.planning/PROJECT.md`, `.planning/MILESTONES.md`, `.planning/WINDOWS.md` and the debug-session rename — **zero** files under `quickshell/`, `hypr/`, `waybar/` or any other stow package. This plan writes no code and any tracked change outside `.planning/` is a defect.
6. `sed -n '191p' quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml | grep -q 'GradientBorder {'` still succeeds — the code the corrected documents now describe is unchanged by this plan.
</verification>

<success_criteria>
- LEDGER-01's four named documentation targets are corrected: PROJECT.md § Active, MILESTONES.md "Known gaps at close", WINDOWS #14, and the debug session's status.
- No document under `.planning/` outside the `milestones/` and `phases/` archives asserts, as a present fact, that the Phase 15 panels lack the animated rim or that `quickshell-doctor` carries live legacy dispatch sites.
- The v3.0 archive's as-of-close statements remain greppable in their original wording, each carrying a dated supersession — the closeout decision stays auditable.
- WINDOWS.md's open-row count drops by exactly one, consistently across both representations.
- Exactly five debug sessions remain unresolved in `.planning/debug/`, correctly handing LEDGER-04 a roster of five rather than six.
- No QML, shell, config or install-path file is modified.
- No human checkpoint was introduced and the operator was not asked to re-confirm the panel rim.
</success_criteria>

<output>
Create `.planning/phases/18-qml-bar-retirement-machinery/18-04-SUMMARY.md` when done.

Record in the SUMMARY: the three precondition command outputs from Task 1 verbatim
(they are the evidence the whole plan rests on), the WINDOWS.md `"status": "open"`
count before and after Task 2, and the final `ls .planning/debug/*.md` listing.
</output>
