---
phase: 18-qml-bar-retirement-machinery
plan: 20
type: execute
wave: 10
depends_on:
  - 18-06
  - 18-19
autonomous: false
requirements:
  - RETIRE-01
  - RETIRE-02

files_modified:
  # Group A — own tree (deleted wholesale)
  - waybar/
  # Group B — own-named scripts (deleted)
  - hypr/.config/hypr/scripts/waybar-design-lint
  - hypr/.config/hypr/scripts/waybar-equivalence-check
  - hypr/.config/hypr/scripts/waybar-launch.sh
  - hypr/.config/hypr/scripts/waybar-switch.sh
  # Group C — matugen
  - matugen/.config/matugen/config.toml
  - matugen/.config/matugen/templates/waybar-colors.css
  - matugen/.config/matugen/templates/swayosd-colors.css
  # Group D — contract
  - theme-engine/.config/theme-engine/contract.json
  # Group E — theme-engine live code
  - theme-engine/.config/theme-engine/theme-doctor
  - theme-engine/.config/theme-engine/theme-parity
  - theme-engine/.config/theme-engine/theme-stress-test
  - theme-engine/.config/theme-engine/lib/motion.sh
  - theme-engine/.config/theme-engine/lib/font.sh
  - theme-engine/.config/theme-engine/lib/commit.sh
  - theme-engine/.config/theme-engine/lib/contract.sh
  - theme-engine/.config/theme-engine/lib/generate.sh
  - theme-engine/.config/theme-engine/lib/gtk.sh
  - theme-engine/.config/theme-engine/lib/wallpaper.sh
  - theme-engine/.config/theme-engine/lib/reload.sh
  # Group F — hypr config
  - hypr/.config/hypr/config/windowrules.lua
  - hypr/.config/hypr/config/autostart.lua
  - hypr/.config/hypr/config/keybinds.lua
  - hypr/.config/hypr/config/env.lua
  # Group G — tests and fixtures
  - hypr/.config/hypr/scripts/tests/test-walker-dmenu-cancel.sh
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-panel-layers.json
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-mpvpaper-layers.json
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-offlevel-panel-layers.json
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-offlevel-mpvpaper-layers.json
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-two-panel-layers.json
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-unknown-namespace-layers.json
  # Group H — residual cross-package references
  - hypr/.config/hypr/scripts/quickshell-doctor
  - hypr/.config/hypr/scripts/quickshell-launch.sh
  - hypr/.config/hypr/scripts/motion-lint
  - hypr/.config/hypr/scripts/hypr-equivalence-check
  - hypr/.config/hypr/scripts/hypr-lua-harness
  - hypr/.config/hypr/scripts/hyprpm-complete.sh
  - hypr/.config/hypr/scripts/gaming-mode-toggle.sh
  - hypr/.config/hypr/scripts/wallpaper-visibility.sh
  - hypr/.config/hypr/scripts/font-switcher.sh
  - hypr/.config/hypr/scripts/swaync-launch.sh
  - hypr/.config/hypr/scripts/record-toggle.sh
  - quickshell/.config/quickshell/shell.qml
  - quickshell/.config/quickshell/modules/Dashboard.qml
  - quickshell/.config/quickshell/modules/dashboard/AudioPanel.qml
  - wleave/.config/wleave/style.css
  - ags/.config/ags/widget/MediaWindow.tsx
  # Group I — reproducibility
  - install.sh
  - stow.sh
  # Group J — registry flip
  - hypr/.config/hypr/scripts/retirement-check

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`retirement-check waybar` run BEFORE any deletion, against a registry row still reading `pending`, exits 0 and emits `[REPORT]` lines for all fourteen blocking-domain classes — this run captures the pre-deletion baseline and proves the blocking tier is still disarmed, so a mid-flight abort leaves `theme-doctor` exactly as green as it was at wave 9 (D-18-35/37, RETIRE-01)."
    - "`retirement-check waybar` run AFTER the deletion, against a registry row reading `retired`, exits 0 and emits `[PASS]` for all fourteen blocking-domain classes with zero `[FAIL]` lines — this run proves RETIRE-02 complete. The before-run and after-run assert different things: the before-run proves the inventory matches expectation while the surface still exists; the after-run proves the blocking tier is armed and satisfied (RETIRE-01's run-before-and-after clause)."
    - "The registry row for the retired bar in `hypr/.config/hypr/scripts/retirement-check` reads `retired`, changed in the same commit as the deletion — this single field is what arms the blocking tier, and arming it in any earlier commit would have turned `theme-doctor` red for the eighteen plans 18-06 deliberately kept it green through."
    - "The stow package directory, its four own-named scripts, the matugen template block and its template file, and every layer rule, autostart line, contract entry and test fixture naming the retired surface are gone from the working tree in one commit — config and package die together, never config-now-package-later (the `config-then-package in one commit` precedent, WINDOWS #1)."
    - "`pacman -Q waybar` exits non-zero on this host after the plan, and the package is absent from `install.sh`'s pacman list and from `stow.sh`'s stow list, so a fresh clone reproduces a desktop with no trace of it — reproducibility is proven by the two list files, never by the host's own state."
    - "`contract.json` loses exactly nine entries: seven from `files[]` (29 to 22), `waybar-font.css` from `presence_only_files` (2 to 1) and `waybar-visibility.css` from `engine_owned_files` (12 to 11). RETIRE-08's post-migration target of ~17 `files[]` entries is reached by removing the five surviving surfaces' entries across Phases 19-21; this plan lands the running total at 22."
    - "`~/.local/state/theme/waybar-visibility.css` is deleted outright, not truncated. 18-15 kept it present-but-empty only because the four `style-*.scss` sheets each `@import` it and an unresolvable import makes GTK3 discard the whole stylesheet; those four sheets die in this commit, so the constraint dies with them and the file must not be left as an orphan (D-18-23/27)."
    - "`theme-stress-test`'s materialyou and materialyou-light sentinel no longer reads `@define-color primary` from the retired sheet — it reads `.primary` from `$STATE_DIR/palette.json`, which is contract entry `palette.json` and survives every remaining retirement in this milestone. Verified equal on this host before the change: both sources yield `#cba6f7`."
    - "Super+B still opens an orientation picker. `keybinds.lua:102` is REPOINTED at `bar-orientation.sh` (built by 18-11), never deleted — D-18-30 preserves the discoverable path already in daily use, and 18-11 repointed only the settings drawer and the Super-key menu, leaving this third caller to this plan."
    - "The notification bell still reaches `swaync-client -swb` after this commit — the retired bar's deletion must not disturb D-18-33's deliberately temporary binding, which Phase 19 owns."
    - "Zero blocking-domain references survive anywhere in the working tree outside `.planning/` and repo prose, including inside comments. `retirement-check`'s blocking tier is a word-boundary content scan that does not distinguish code from commentary, and 18-06 forbids any suppression mechanism, so a surviving historical comment is a genuine `[FAIL]` and is scrubbed like any other reference."
    - "Every historical rationale carried by a scrubbed comment survives the scrub — the reasoning is rewritten in surface-neutral terms, and any fact that cannot survive the rename is recorded in this plan's SUMMARY rather than destroyed."
    - "`theme-doctor` completes with a pass/fail tally no worse than its pre-deletion tally, and the four functional blocks it carried for the retired surface are gone rather than left skipping — a permanently-`[SKIP]` block is an invisible dead gate, which is the WINDOWS #1 failure mode restated."
    - statement: "The live session survives the package uninstall without a compositor restart — the QML bar keeps rendering and keeps its reserved zone at `[0,46,0,0]` while the retired binary is removed underneath it."
      verification: backstop
    - statement: "`theme-apply` completes cleanly after the contract, template and sass-compile-table edits, producing no render error for a removed output and leaving no orphaned file in the state directory."
      verification: backstop

  prohibitions:
    - "MUST NOT delete any file, edit any list, or uninstall any package before 18-19's GATE-02 render gate has recorded a pass — the user's judgment that the replacement is not worse is the only thing that authorises destroying the thing it replaces."
    - "MUST NOT remove a reference by suppressing, exempting or allowlisting it in `retirement-check` — an acknowledged exception that stops printing becomes a permanently-green gate hiding real breakage."
    - "MUST NOT destroy the historical reasoning recorded in a comment while scrubbing the retired surface's name from it; rewrite the reasoning or move it to the SUMMARY, never silently drop it."
    - "MUST NOT uninstall the package with dependency-cascading or dependency-ignoring flags, and MUST NOT proceed with the uninstall if anything on the host depends on it."
    - "MUST NOT stage or commit any deletion that git cannot revert — every removal is of a tracked file through git, so the user can recover the whole surface with a single revert of one commit."
    - "MUST NOT widen the deletion beyond the reconciled manifest; a reference discovered mid-execution that is not on the manifest is reported and added deliberately, never removed on sight."

  artifacts:
    - .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md
    - .planning/phases/18-qml-bar-retirement-machinery/18-20-SUMMARY.md

  key_links:
    - "`retirement-check`'s registry `status` field -> the blocking tier. Flipping `pending` to `retired` is the single switch that arms fourteen classes of assertion; it is the most likely thing to be forgotten, and forgetting it makes the after-run a meaningless repeat of the before-run."
    - "`contract.json` `files[]` -> `theme-apply`'s render loop -> `theme-doctor`'s parity walk -> `theme-parity`'s sheet array -> `motion.sh`'s sass-compile table. Five files must lose the same seven names together; any one of them missed leaves a renderer producing an output nothing consumes, or a consumer expecting an output nothing renders."
    - "`theme-stress-test`'s materialyou sentinel -> `palette.json`. The retired sheet was the sole source; every other gtk-css sheet defining `@define-color primary` (swaync, swayosd, wleave) is itself retired later this milestone, so repointing at another doomed sheet would only move the breakage into Phase 19 or 20."
    - "`keybinds.lua:102` -> `bar-orientation.sh`. The deleted picker is a live bind target; deleting the script without repointing the bind converts a working keybind into a silent no-op, which is exactly the capability regression GATE-02 exists to catch."
    - "The four `style-*.scss` `@import`s -> `~/.local/state/theme/waybar-visibility.css`. Their deletion is what finally releases 18-15's constraint and permits the state file's removal; the two must land in the same commit or the orphan survives."
---

<!-- planner-discipline-allow: waybar -->
<!-- planner-discipline-allow: waybar-visibility.css -->
<!-- planner-discipline-allow: waybar-switch.sh -->
<!-- Rationale: this plan's subject is the token its acceptance criteria negative-grep for.
     Every negative grep in this plan excludes `.planning/`, where this file lives, so the
     token's presence here is scanned by `retirement-check`'s report domain (class 15) and
     can never influence the blocking tier or the exit code. -->

<objective>
Retire the waybar surface completely: delete its config tree, its own-named scripts, its
contract entries, its matugen template, its checkers and fixtures, its window rules,
its autostart line and its package — config and package in **one commit** — and flip
`retirement-check`'s registry row from `pending` to `retired` in that same commit so the
blocking tier arms exactly as the surface disappears.

This closes RETIRE-02 and delivers RETIRE-01's second half: the checklist script built at
18-06 is run **before** the deletion (against a `pending` row, capturing the baseline) and
**after** it (against a `retired` row, asserting zero blocking-tier hits). Those two runs
prove different things and the plan states both.

Purpose: the milestone's whole premise is that a package dies in the phase that proves its
replacement better. 18-19's GATE-02 render gate is that proof (D-18-31/32) and is a hard
precondition here — nothing is deleted until it has passed. This is the phase's one
genuinely one-way step, and the first real exercise of the retirement machinery that
Phases 19, 20 and 21 will each reuse unmodified.

Output: a working tree with zero blocking-domain references to the retired surface, a
`retired` registry row, `contract.json` down to 22 `files[]` entries, and a host with the
package uninstalled.

## Decisions implemented by this plan

The remaining CONTEXT.md decisions belong to plans 18-01 through 18-19 and are not this
plan's to implement. (`check.decision-coverage-plan` reports all 40 as uncovered when run
against a single plan with `D-18-NN` three-part IDs — a known parser mismatch, not a gap.)

- **D-18-23**: the idle-dim CSS rule and its state file finally die — Task 2, Group J.
- **D-18-27**: the renamed owner's state file is deletable only now; the constraint 18-15
  recorded and deferred is released by this commit — Task 2, Group J.
- **D-18-28**: the retired fullscreen watcher is already consumed by 18-15, which is why
  four scripts remain rather than six — inventory reconciliation, Task 1.
- **D-18-30**: the four-layout picker's replacement is complete only when its third caller
  moves — Super+B is repointed at `bar-orientation.sh` and the superseded menu entry is
  removed — Task 2, Group F.
- **D-18-31**: the blocking final pass gates this plan; the precondition on Task 1 halts
  the whole plan if it did not pass.
- **D-18-32**: the aesthetic-plus-capability split is what Task 1 reads out of
  `18-19-SUMMARY.md` before authorising anything.
- **D-18-33**: the temporary bell binding must survive this deletion untouched, since
  Phase 19 owns swapping what sits behind it — success criteria and SUMMARY output.
- **D-18-34**: the generic script is consumed here unmodified, exactly as Phases 19-21 will.
- **D-18-35**: the fold into `theme-doctor` is what makes the after-run's zero binding
  rather than advisory — Task 3.
- **D-18-36**: all four added reference classes are exercised — checker internals (R1),
  test fixtures (Group G), cross-script references (Task 3), and planning prose (left
  untouched in the report domain).
- **D-18-37**: the two-tier split is why the after-run can reach zero at all; `.planning/`'s
  ~3796 historical mentions stay and never influence the exit code.
- **D-18-39**: Task 1 confirms B.3 is recorded as not demonstrable on this hardware, never
  as a pass or a failure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-qml-bar-retirement-machinery/18-CONTEXT.md
@.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md
@.planning/phases/18-qml-bar-retirement-machinery/18-06-SUMMARY.md
@.planning/phases/18-qml-bar-retirement-machinery/18-15-SUMMARY.md
@.planning/phases/18-qml-bar-retirement-machinery/18-19-SUMMARY.md
</context>

## Inventory reconciliation — RESEARCH.md vs. the live tree (verified at plan time)

RESEARCH.md's counts were **accurate when taken** and remain accurate for the five
categories it enumerates. Verified directly against the tree on 2026-08-10:

| RESEARCH.md claim | Live tree | Verdict |
|---|---|---|
| 12 config files | 12 files under `waybar/` | confirmed |
| 6 own-named scripts | 6 `waybar-*` under `hypr/.config/hypr/scripts/` | confirmed — but see below |
| 7 `contract.json` entries | `files[]` lines 3, 26-31 | confirmed |
| + 2 adjacent state files | `waybar-font.css` (line 34), `waybar-visibility.css` (line 43) | confirmed |
| 2 `windowrules.lua` rules | lines 222, 401 (plus 2 comment mentions at 216, 448) | confirmed |
| 6 `quickshell-doctor` fixtures | 6 `*-layers.json`, 1 hit each | confirmed |

**Two adjustments the plan must apply, neither a RESEARCH.md error:**

1. **Four scripts to delete, not six.** 18-15 renames `waybar-visibility.sh` to
   `bar-visibility.sh` and deletes `waybar-fullscreen-watch.sh` (D-18-27/28). By wave 10
   only `waybar-design-lint`, `waybar-equivalence-check`, `waybar-launch.sh` and
   `waybar-switch.sh` remain.
2. **`~/.local/state/theme/waybar-visibility.css` is deletable here.** 18-15 left it
   truncated-but-present because the four `style-*.scss` sheets `@import` it. Those sheets
   die in this commit, so the file goes too.

**Seven surfaces the source artifacts do NOT enumerate, found live. This is the material
finding of the reconciliation** — a deletion premised on the enumerated list alone would
leave `theme-doctor`, `theme-parity`, `theme-stress-test` and a live keybind broken:

| # | Surface | Why it is not optional |
|---|---|---|
| R1 | `theme-doctor` carries **four** functional blocks, not one. CONTEXT.md names only the equivalence-check gate at 466-559. Also live: the `WAYBAR_STYLE_SHEETS` glob at 343-369 feeding `GTK3_CSS_SHEETS`, the entrypoint compiled-sheet guard at 637-658, and the design-lint fold at 660-678 | Three of the four would survive as permanently-`[SKIP]` dead gates |
| R2 | `lib/motion.sh:591-596` — six rows in the sass-compile table | Sole compiler of the six sheets; orphaned rows fail the compile |
| R3 | `lib/font.sh:46-53` — renders `waybar-font.css` | Sole owner of that output; the contract entry and the renderer must die together |
| R4 | `theme-parity:379-381` — six sheet names in the parity walk array | Parity walks a file that no longer renders |
| R5 | `theme-stress-test:255` reads `@define-color primary` from the retired sheet as the **sole** materialyou/materialyou-light sentinel source, and line 300 lists it in `REPRESENTATIVE_FILES` | Breaks every materialyou switch in the stress test — and LEDGER-07's clean run is Phase 19's requirement |
| R6 | `keybinds.lua:102` — Super+B binds `waybar-switch.sh`. **No prior plan repoints it.** 18-11 reads that script only as a shape to copy and explicitly leaves `settings.toml`'s superseded entry to this plan | Deleting the script turns a live bind into a silent no-op — a capability regression |
| R7 | `stow.sh` carries **five** distinct surfaces, not just the package list: line 37 (stow list), 260 (`current-waybar-layout` seed), 274-289 (`waybar-visibility.css` seed), 399-453 (six-sheet sass-compile seed), 528 (closing hint text) | A fresh clone would still seed state for a package that no longer exists |

**Comment-only references are still blocking-tier.** `retirement-check`'s classes 7
(`checker-internals`) and 9 (`cross-package-refs`) are word-boundary content scans that do
not distinguish code from commentary, and 18-06 forbids any suppression mechanism. Roughly
25 further files carry the name only inside historical comments — every one must be
scrubbed for the after-run to reach zero. Task 4 owns them, under a rule that preserves the
reasoning while dropping the retired name.

**Report domain, deliberately untouched:** `.planning/` (~3796 hits — D-18-37's entire
rationale), `README.md`, `VERIFICATION.md`, `docs/`, `.claude/CLAUDE.md`. These are classes
15 and 16 and never influence the exit code.

## `contract.json` arithmetic for RETIRE-08

| Array | Before | Removed here | After this phase | Removed by Phases 19-21 | Final |
|---|---|---|---|---|---|
| `files[]` | 29 | 7 | **22** | 5 (`swaync.css`, `swaync-style.css`, `swayosd.css`, `wleave.css`, `ags.scss`) | **17** |
| `presence_only_files` | 2 | 1 | 1 | 0 | 1 |
| `engine_owned_files` | 12 | 1 | 11 | — | 11 |

RETIRE-08's "29 → ~17" lands exactly: this phase removes 7 of the 12 `files[]` entries the
milestone retires, and the remaining 5 belong to surfaces retired in Phases 19-21. **22 is
the running total this plan must leave behind**, and is the number Phase 21's RETIRE-08
close should expect to start from.

## UI scope

**No `## UI Considerations` rows are in scope for this plan.** 18-UI-SPEC.md's 56
considerations all sit on surfaces E1-E7 (the five bar capsules, the section popout and the
bar surface itself), authored by plans 18-05 and 18-08 through 18-16. RETIRE-02 removes a
surface and renders nothing, so it has no state coverage to satisfy. Stated as an explicit
scope fact rather than omitted.

## Specless fallback — coverage authorship

No `-SPEC.md` exists for this phase. Per `.planning/tmp/coverage.json`, RETIRE-01 and
RETIRE-02 carry five items between them:

- **RETIRE-01 — all four items (`adjacency`, `empty`, `ordering`, `concurrency`) were
  already authored by 18-06** and are NOT re-authored here. Named for the record so the gap
  is visibly closed rather than silently skipped: `adjacency` by 18-06's word-boundary truth
  (`bar` never matches inside `waybar`); `empty` by its unscannable-root `[SKIP]` truth;
  `ordering` by its byte-identical-stdout determinism truth; `concurrency` by its
  no-lock/no-temp-file/mutates-nothing truth. This plan **consumes** all four — the
  before/after diff is only meaningful because 18-06 made the output deterministic.
- **RETIRE-02 — one item, category `unclassified`, probe "review manually".** Reviewed
  manually at plan time and classified as **explicit**: the requirement is directly
  observable (zero blocking-tier hits, `pacman -Q` non-zero, both list files clean), so it
  is authored as plain-string truths in `must_haves.truths` rather than deferred as an
  unresolved assumption.

Two truths are authored as **backstop** flat scalars: live-session survival of the
uninstall, and a clean `theme-apply` after the contract edits. Both are specified
behaviours whose confirmation depends on a live session state this plan cannot guarantee
in advance.

## Artifacts this phase produces

**Removed** (one commit, config-then-package):

- 12 config files (the whole `waybar/` stow package)
- 4 own-named scripts (`waybar-design-lint`, `waybar-equivalence-check`,
  `waybar-launch.sh`, `waybar-switch.sh`)
- 1 matugen template file + 1 `[templates.waybar]` block
- 9 `contract.json` entries (7 `files[]`, 1 `presence_only_files`, 1 `engine_owned_files`)
- 4 functional blocks in `theme-doctor`, 6 rows in `motion.sh`, 6 names in `theme-parity`,
  1 render stanza in `font.sh`, 1 carve-out in `commit.sh`, 1 signal fan-out in `reload.sh`
- 2 `windowrules.lua` layer rules, 1 `autostart.lua` launch line
- 6 `quickshell-doctor` fixture entries + 1 whole test suite (`run_waybar_suite`)
- 5 surfaces in `stow.sh`, 1 package line in `install.sh`
- 1 pacman package on this host, 8 orphaned state files, 1 orphaned cache file

**Repointed, not removed:** `keybinds.lua:102` (Super+B → `bar-orientation.sh`),
`theme-stress-test`'s materialyou sentinel (→ `palette.json`).

**New state reached:**

- `retirement-check` registry: the waybar row reads `retired` — the blocking tier is armed
  for this surface, permanently, for every future `theme-doctor` run.
- `contract.json`: 22 `files[]` entries, 1 `presence_only_files`, 11 `engine_owned_files`.
- `.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md`: the
  after-run transcript, committed as the paired artifact to 18-06's baseline.

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion baseline run and manifest reconciliation — nothing is destroyed yet</name>
  <precondition>18-19's GATE-02 render gate has recorded a pass in `18-19-SUMMARY.md`. Assert this before the first command in this task and halt the whole plan if it is absent or records a failure — D-18-31/32 make the human judgment the sole authorisation for every later task.</precondition>
  <files>/tmp/before.txt (the captured before-run transcript; this task creates and modifies NOTHING under the repository — its final acceptance criterion asserts `git status --porcelain` is still empty)</files>
  <read_first>
    - `.planning/phases/18-qml-bar-retirement-machinery/18-19-SUMMARY.md` — the GATE-02 result. Read the criteria A and B outcomes and confirm B.3 is recorded as "not demonstrable on this hardware — structurally present" per D-18-39, not as a pass or a failure.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md` — 18-06's committed pre-deletion baseline. This is the artifact today's before-run is diffed against.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-06-SUMMARY.md` — the recorded final registry row, the exact re-run command, and the own-tree globs as shipped.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-15-SUMMARY.md` — confirm which of the six own-named scripts 18-15 already consumed and that the state CSS file is present-and-empty.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-11-SUMMARY.md` — confirm `bar-orientation.sh` exists and is executable, since Task 3 repoints a live keybind at it.
    - `hypr/.config/hypr/scripts/retirement-check` — the whole file: the registry here-doc, the sixteen class scan functions, the tier dispatch, and the `--list` output format.
  </read_first>
  <action>
Assert the precondition first. If `18-19-SUMMARY.md` is absent, or records any GATE-02
criterion as failed, stop the entire plan and report the blocking criterion. Do not
continue to any later task. This is D-18-31/32's blocking clause and it is not advisory.

Confirm the working tree is clean before anything else, so the deletion commit's diff is
attributable entirely to this plan.

Run `retirement-check waybar` while the registry row still reads `pending`. Capture stdout
verbatim. Because the row is `pending`, every blocking-domain class emits `[REPORT]` with a
count and the exit code is 0 — this is the disarmed state 18-06 deliberately preserved for
eighteen plans, and confirming it here proves an abort at this point leaves `theme-doctor`
exactly as green as wave 9 left it.

Diff today's before-run against 18-06's committed baseline. The counts WILL differ, and the
differences must all be explainable by plans 18-01 through 18-19 — chiefly 18-15's script
rename and watcher deletion, and 18-11's added menu entry. Any difference that cannot be
attributed to a named prior plan is a signal that the tree drifted, and is reported rather
than absorbed.

Reconcile the manifest. Walk the seven undocumented surfaces R1-R7 in this plan's
reconciliation table and confirm each is still present at the stated location, since the
line numbers were taken at plan time and prior waves have edited several of these files.
Resolve each to its current location by content, not by line number. Record the resolved
locations — Task 3 works from this reconciled manifest, not from the plan's line numbers.

Verify the two facts Task 3's edits depend on, and halt if either fails. First, that the
materialyou sentinel source and its replacement agree: `@define-color primary` in the
retired state sheet and `.primary` in `palette.json` must yield the same value, confirming
the repoint is value-preserving rather than merely plausible. Second, that the package has
no reverse dependencies, confirming the uninstall cannot cascade.

Write the reconciled manifest into the task's working notes for Task 3 to consume. Delete
nothing, edit nothing, uninstall nothing in this task.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/18-qml-bar-retirement-machinery/18-19-SUMMARY.md` exits `0`, and `grep -ci 'GATE-02.*fail\|criterion.*failed' .planning/phases/18-qml-bar-retirement-machinery/18-19-SUMMARY.md` returns `0`.
    - `git status --porcelain` produces no output before any command in this task runs.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar; echo $?` prints `0`.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar | grep -c '^\[FAIL\]'` returns `0` — the tier is still disarmed.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar | grep -c '^\[REPORT\]'` returns at least `14` — one per blocking-domain class, plus the two report-domain classes.
    - `bash hypr/.config/hypr/scripts/retirement-check --list | grep -E '^waybar\|' | cut -d'|' -f2` prints `pending`.
    - `ls -1 hypr/.config/hypr/scripts/waybar-* | wc -l` returns `4`, and `test ! -f hypr/.config/hypr/scripts/waybar-visibility.sh && test ! -f hypr/.config/hypr/scripts/waybar-fullscreen-watch.sh` exits `0` — 18-15's two scripts are already consumed.
    - `test -f ~/.local/state/theme/waybar-visibility.css && test ! -s ~/.local/state/theme/waybar-visibility.css` exits `0` — present and empty, exactly as 18-15 left it.
    - `test -x hypr/.config/hypr/scripts/bar-orientation.sh` exits `0` — 18-11's replacement picker exists as a repoint target.
    - `test "$(sed -nE 's/@define-color[[:space:]]+primary[[:space:]]+(.*);.*/\1/p' ~/.local/state/theme/waybar.css | head -1)" = "$(jq -r '.primary' ~/.local/state/theme/palette.json)"` exits `0` — the sentinel repoint is value-preserving.
    - `pactree -r waybar | tail -n +2 | wc -l` returns `0`, and `pacman -Qi waybar | grep -c '^Required By *: None'` returns `1` — the uninstall cannot cascade.
    - `find waybar -type f | wc -l` returns `12`; `jq '.files | length' theme-engine/.config/theme-engine/contract.json` returns `29`.
    - `git status --porcelain` still produces no output after the task — this task is read-only.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd /home/aorus/dotfiles &amp;&amp; set -e; D=.planning/phases/18-qml-bar-retirement-machinery; test -f $D/18-19-SUMMARY.md || { echo "HALT: GATE-02 summary absent — deletion is not authorised"; exit 1; }; test "$(grep -ci "GATE-02.*fail\|criterion.*failed" $D/18-19-SUMMARY.md)" -eq 0 || { echo "HALT: GATE-02 records a failure — deletion is not authorised"; exit 1; }; test -z "$(git status --porcelain)" || { echo "FAIL: tree dirty before baseline"; exit 1; }; R=hypr/.config/hypr/scripts/retirement-check; bash $R waybar > /tmp/before.txt; echo $? ; test "$(grep -c "^\[FAIL\]" /tmp/before.txt)" -eq 0 || { echo "FAIL: blocking tier already armed"; exit 1; }; test "$(grep -c "^\[REPORT\]" /tmp/before.txt)" -ge 14 || { echo "FAIL: fewer than 14 report lines"; exit 1; }; test "$(bash $R --list | grep -E "^waybar\|" | cut -d"|" -f2)" = "pending" || { echo "FAIL: registry row is not pending"; exit 1; }; test "$(ls -1 hypr/.config/hypr/scripts/waybar-* | wc -l)" -eq 4 || { echo "FAIL: expected 4 remaining scripts"; exit 1; }; test ! -f hypr/.config/hypr/scripts/waybar-visibility.sh || { echo "FAIL: 18-15 rename did not land"; exit 1; }; test -f ~/.local/state/theme/waybar-visibility.css &amp;&amp; test ! -s ~/.local/state/theme/waybar-visibility.css || { echo "FAIL: state css not present-and-empty"; exit 1; }; test -x hypr/.config/hypr/scripts/bar-orientation.sh || { echo "FAIL: repoint target missing"; exit 1; }; A=$(sed -nE "s/@define-color[[:space:]]+primary[[:space:]]+(.*);.*/\1/p" ~/.local/state/theme/waybar.css | head -1); B=$(jq -r ".primary" ~/.local/state/theme/palette.json); test "$A" = "$B" || { echo "FAIL: sentinel repoint not value-preserving ($A vs $B)"; exit 1; }; test "$(pactree -r waybar | tail -n +2 | wc -l)" -eq 0 || { echo "FAIL: package has reverse dependencies"; exit 1; }; test "$(find waybar -type f | wc -l)" -eq 12 || { echo "FAIL: config file count drifted"; exit 1; }; test "$(jq ".files | length" theme-engine/.config/theme-engine/contract.json)" -eq 29 || { echo "FAIL: contract files[] count drifted"; exit 1; }; test -z "$(git status --porcelain)" || { echo "FAIL: baseline task mutated the tree"; exit 1; }; echo "PASS: gate authorised, baseline captured, manifest reconciled, nothing destroyed"'</automated>
  </verify>
  <done>GATE-02 is confirmed passed, the before-run transcript is captured with zero `[FAIL]` lines against a `pending` row, the seven undocumented surfaces are resolved to current locations, the sentinel repoint is proven value-preserving, the uninstall is proven non-cascading, and the working tree is untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Open the one-way door: destroy the waybar surface and uninstall the package.</decision>
  <context>
Task 1 has confirmed 18-19's GATE-02 render gate passed, captured the pre-deletion
baseline, and reconciled the deletion manifest. Everything after this point is the phase's
single genuinely irreversible step.

**What is about to happen:** 12 config files, 4 scripts, 1 matugen template, 9 contract
entries, 4 checker blocks, 2 layer rules, 1 autostart line, 7 test fixtures and 5 `stow.sh`
surfaces are deleted in one commit; the pacman package is uninstalled from this host; and
`retirement-check`'s registry row flips to `retired`, arming fourteen classes of blocking
assertion permanently.

**What is recoverable and what is not.** The repository side is fully recoverable: every
deletion goes through git against tracked files in a single commit, so `git revert` of that
one commit restores the entire surface. The host side is recoverable but not automatic —
`pacman -S waybar` reinstalls the binary, and `stow waybar` plus `theme-apply` re-seeds the
config and state files. The genuinely one-way part is not the files: it is that from this
commit onward the desktop has no fallback bar. If the QML bar develops a fault after this
point, there is no second bar to switch to, and recovery means reverting a commit and
reinstalling a package rather than running one script.

**Why now rather than later.** RETIRE-02's requirement text is explicit that the package
dies in the phase that proves its replacement, and GATE-02 is that proof. Deferring the
deletion past this phase would leave the milestone carrying two bars — the double
maintenance the whole v4.0 premise exists to end — and would leave `retirement-check`'s
blocking tier unexercised before Phase 19 depends on it with no soak window and no
rollback.

**Blast radius of the uninstall, measured on this host:** `pacman -Qi waybar` reports
`Required By: None` and `Optional For: None`, and `pactree -r waybar` returns only the
package itself. Nothing on this host depends on it, so the removal cannot cascade.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the deletion commit and the host uninstall</name>
      <pros>Closes RETIRE-02 in the phase that proved the replacement, per the requirement text. Ends the double maintenance. Exercises the retirement machinery for real before Phase 19 depends on it. Leaves the milestone on schedule for RETIRE-08's contract close.</pros>
      <cons>No fallback bar from this commit onward. Recovery from a later QML bar fault means a git revert plus a package reinstall rather than a layout switch.</cons>
    </option>
    <option id="defer-uninstall">
      <name>Delete the repo config and contract entries, but leave the package installed on this host</name>
      <pros>Keeps the binary available as a manual emergency fallback.</pros>
      <cons>Breaks the `config-then-package in one commit` precedent this repo established at WINDOWS #1, and recreates exactly the RETIRE-07 condition — a repo-retired surface still installed on the host — that the milestone has a separate requirement to clean up. The fallback is also illusory: with the config tree deleted, the installed binary has nothing to load.</cons>
    </option>
    <option id="halt">
      <name>Halt and re-open GATE-02</name>
      <pros>Correct response if anything in Task 1's reconciliation surfaced an unexplained drift, or if daily use since 18-19 has revealed a regression the render gate did not catch.</pros>
      <cons>Leaves the phase open and the milestone carrying two bars.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, defer-uninstall, or halt</resume-signal>
</task>

<task type="auto">
  <name>Task 2: The deletion commit — config and package together, registry flipped, in one commit</name>
  <reversibility rating="one-way">This is the phase's single one-way step. The repository side is revertible as one commit and the package is reinstallable, but from this commit the desktop has no fallback bar — recovery becomes a revert-and-reinstall rather than a layout switch.</reversibility>
  <precondition>The `checkpoint:decision` immediately above resolved to `proceed`. If it resolved to `defer-uninstall` or `halt`, this task does not run in the form written.</precondition>
  <files>waybar/ (all 12, deleted), hypr/.config/hypr/scripts/waybar-design-lint, hypr/.config/hypr/scripts/waybar-equivalence-check, hypr/.config/hypr/scripts/waybar-launch.sh, hypr/.config/hypr/scripts/waybar-switch.sh, matugen/.config/matugen/config.toml, matugen/.config/matugen/templates/waybar-colors.css, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/theme-doctor, theme-engine/.config/theme-engine/theme-parity, theme-engine/.config/theme-engine/theme-stress-test, theme-engine/.config/theme-engine/lib/motion.sh, theme-engine/.config/theme-engine/lib/font.sh, theme-engine/.config/theme-engine/lib/commit.sh, theme-engine/.config/theme-engine/lib/reload.sh, hypr/.config/hypr/config/windowrules.lua, hypr/.config/hypr/config/autostart.lua, hypr/.config/hypr/config/keybinds.lua, elephant/.config/elephant/menus/settings.toml, hypr/.config/hypr/scripts/tests/test-walker-dmenu-cancel.sh, hypr/.config/hypr/scripts/tests/quickshell-fixtures/*-layers.json (6 files), install.sh, stow.sh, hypr/.config/hypr/scripts/retirement-check — knowingly one task over the five-file guideline, because the `config-then-package in one commit` precedent (WINDOWS #1) makes atomicity the requirement</files>
  <read_first>
    - Task 1's reconciled manifest — the authoritative per-surface locations. Prefer it over every line number quoted in this plan.
    - `theme-engine/.config/theme-engine/contract.json` — the whole file (49 lines). All three arrays.
    - `theme-engine/.config/theme-engine/theme-doctor` — lines 60-90, 335-375, 460-565, 630-700. The four functional blocks, the `check()` helper, and the `GTK3_CSS_SHEETS` assembly.
    - `theme-engine/.config/theme-engine/theme-parity` — lines 345-390. The sheet array and its surrounding walk.
    - `theme-engine/.config/theme-engine/theme-stress-test` — lines 232-305. `get_expected_sentinel_normalized()` and `REPRESENTATIVE_FILES`, including the long comment explaining why the retired sheet was chosen — that reasoning is what the repoint must preserve.
    - `theme-engine/.config/theme-engine/lib/motion.sh` — lines 578-600. The sass-compile table and its "pure data edit" contract comment.
    - `theme-engine/.config/theme-engine/lib/font.sh` — lines 8-60. The render stanza and its sole-owner claim.
    - `theme-engine/.config/theme-engine/lib/commit.sh` — lines 60-100. The state-file preservation carve-out and the incident it records.
    - `theme-engine/.config/theme-engine/lib/reload.sh` — lines 1-90. The signal fan-out and the ownership header.
    - `matugen/.config/matugen/config.toml` — lines 28-42. The template block and its comment header.
    - `hypr/.config/hypr/config/windowrules.lua` — lines 210-230 and 395-410. Both layer rules in context.
    - `hypr/.config/hypr/config/autostart.lua` — lines 35-70. The launch line and the surrounding comments 18-15 already edited.
    - `hypr/.config/hypr/config/keybinds.lua` — lines 98-110. Super+B and Super+Shift+B together, so the repoint matches the bind style 18-15 left behind.
    - `hypr/.config/hypr/scripts/bar-orientation.sh` — its argument contract, so the repointed bind passes what it accepts.
    - `hypr/.config/hypr/scripts/tests/test-walker-dmenu-cancel.sh` — the whole file. Its dispatch arms and usage string, not only the suite function.
    - The six `hypr/.config/hypr/scripts/tests/quickshell-fixtures/*-layers.json` files carrying a namespace entry.
    - `install.sh` around line 73, and `stow.sh` at lines 30-45, 255-295, 395-460, 520-535 — all five surfaces.
    - `hypr/.config/hypr/scripts/retirement-check` — the registry here-doc only.
  </read_first>
  <action>
Work from Task 1's reconciled manifest. Everything below lands in **one commit** — this is
the `config-then-package in one commit` precedent from WINDOWS #1, and it is why this task
knowingly exceeds the usual five-file limit: splitting it would produce an intermediate
commit in which the config is gone but the package is installed, or the contract lists an
output nothing renders. Atomicity is the point.

Use `git rm` for every file deletion so the whole surface is restorable by reverting this
one commit. Do not use unguarded recursive removal, and do not delete any path that Task 1's
manifest did not resolve.

**Group A — the stow package.** Remove the entire `waybar/` directory, all 12 files.

**Group B — the four own-named scripts.** Remove `waybar-design-lint`,
`waybar-equivalence-check`, `waybar-launch.sh` and `waybar-switch.sh`. The other two names
were already consumed by 18-15; confirm rather than assume.

**Group C — matugen.** Remove the `[templates.waybar]` block and its comment header from
`config.toml`, and remove the `waybar-colors.css` template file.

**Group D — the contract.** Remove the seven `files[]` entries, `waybar-font.css` from
`presence_only_files`, and `waybar-visibility.css` from `engine_owned_files`. Keep the JSON
valid and the remaining entries' order untouched. The result is 22, 1 and 11.

**Group E — theme-engine live code.** Remove all four functional blocks from `theme-doctor`,
not the one CONTEXT.md names: the style-sheet glob feeding the GTK3 sheet array, the
equivalence-check gate, the entrypoint compiled-sheet guard, and the design-lint fold. Do not
leave any of them as a permanently-skipping stub — a gate that can only ever skip is an
invisible dead gate, which is the WINDOWS #1 failure mode restated. Remove the six rows from
`motion.sh`'s sass-compile table, the six names from `theme-parity`'s sheet array, the
font-render stanza from `font.sh`, the state-file preservation carve-out from `commit.sh`,
and the signal fan-out from `reload.sh`.

In `theme-stress-test`, repoint rather than remove. The materialyou and materialyou-light
sentinel currently reads `@define-color primary` from the retired sheet; change it to read
`.primary` from `palette.json` in the state directory, which is a contract entry that
survives every remaining retirement in this milestone. Do not repoint at another gtk-css
sheet — swaync, swayosd and wleave all define the same token but are themselves retired in
Phases 19 and 20, so any of them would only move this breakage into a later phase. Drop the
retired sheet from `REPRESENTATIVE_FILES`, leaving the other five. Rewrite the long comment
explaining the original choice so it records why the palette file is now the source and why a
gtk-css sheet is no longer a durable one — that reasoning is the valuable part and must
survive.

**Group F — hypr config.** Remove both layer rules from `windowrules.lua` and the launch line
from `autostart.lua`. In `keybinds.lua`, **repoint Super+B at `bar-orientation.sh`** — do not
delete the bind. D-18-30 preserves the discoverable path already in daily use, and 18-11
repointed only the settings drawer and the Super-key menu, leaving this third caller here.
Match the bind style 18-15 left on Super+Shift+B, and pass whatever `bar-orientation.sh`'s
argument contract expects for its interactive picker mode. Also remove the superseded layout
entry from `elephant/.config/elephant/menus/settings.toml` that 18-11 deliberately left in
place, leaving 18-11's orientation entry as the only one.

**Group G — tests and fixtures.** Remove the namespace entry from each of the six layer
fixtures. In `test-walker-dmenu-cancel.sh`, remove the whole suite for the deleted picker —
its function, its script path variable, its dispatch arm and its usage string — leaving the
theme-switch suite and the file's own structure intact and still runnable.

**Group H — reproducibility.** Remove the package from `install.sh`'s pacman list, and all
five surfaces from `stow.sh`: the stow list entry, the layout-state seed, the state-CSS seed,
the six sass-compile seed rows, and the closing hint text. The hint text names a keybind that
no longer switches layouts; replace it with the orientation toggle's path rather than deleting
the line, so the closing output still tells a fresh installer how to change orientation.

**Group I — the registry flip.** In `retirement-check`, change the waybar row's status field
from `pending` to `retired`. Change nothing else in the registry — the other six real
surfaces stay `pending`, and 18-06 records explicitly that `wlogout` and `eww` must not be
tidied to `retired` while they still carry references.

**Group J — host state and package.** Unstow the package directory. Remove the orphaned state
files: the colour sheet, the font fragment, the theme and modules sheets, the four layout
sheets, and the visibility CSS — that last one is deletable now precisely because the four
stylesheets that `@import` it died in Group A, which is the constraint 18-15 recorded and
deferred to this plan. Remove the orphaned layout cache file. Then uninstall the package with
pacman's dependency-respecting removal, taking unneeded dependencies and configuration with
it; do not use any flag that ignores or cascades dependencies. Task 1 proved nothing depends
on it.

Commit everything above as one commit.
  </action>
  <acceptance_criteria>
    - `test ! -d waybar` exits `0`, and `git log -1 --stat | grep -c 'waybar/.config/waybar/'` returns at least `12`.
    - `ls hypr/.config/hypr/scripts/waybar-* 2>/dev/null | wc -l` returns `0`.
    - `test ! -f matugen/.config/matugen/templates/waybar-colors.css` exits `0`, and `grep -c 'templates.waybar' matugen/.config/matugen/config.toml` returns `0`.
    - `jq '.files | length' theme-engine/.config/theme-engine/contract.json` returns `22`; `jq '.presence_only_files | length'` returns `1`; `jq '.engine_owned_files | length'` returns `11`; `jq -e . theme-engine/.config/theme-engine/contract.json >/dev/null` exits `0`.
    - `grep -ci waybar theme-engine/.config/theme-engine/contract.json` returns `0`.
    - `grep -c 'palette.json' theme-engine/.config/theme-engine/theme-stress-test` returns at least `1`, and `grep -c 'REPRESENTATIVE_FILES=(hyprland-tokens.lua swaync.css wleave.css gtk-4.0-colors.css kitty.conf)' theme-engine/.config/theme-engine/theme-stress-test` returns `1`.
    - `grep -c 'bar-orientation.sh' hypr/.config/hypr/config/keybinds.lua` returns `1`, and `grep -c 'hl.bind' hypr/.config/hypr/config/keybinds.lua` is unchanged from its pre-edit value — the bind is repointed, never deleted.
    - `grep -c 'hl.layer_rule' hypr/.config/hypr/config/windowrules.lua` is exactly 2 lower than its pre-edit value.
    - `grep -c 'bar-orientation.sh' elephant/.config/elephant/menus/settings.toml` returns `1`, and `grep -ci waybar elephant/.config/elephant/menus/settings.toml` returns `0`.
    - `bash hypr/.config/hypr/scripts/retirement-check --list | grep -E '^waybar\|' | cut -d'|' -f2` prints `retired`, and `bash hypr/.config/hypr/scripts/retirement-check --list | grep -cE '\|pending\|'` returns `6`.
    - Every edited shell script still parses: `bash -n` exits `0` for `theme-doctor`, `theme-parity`, `theme-stress-test`, `lib/motion.sh`, `lib/font.sh`, `lib/commit.sh`, `lib/reload.sh`, `install.sh`, `stow.sh`, `test-walker-dmenu-cancel.sh` and `retirement-check`.
    - Every edited lua file still parses: `luajit -bl` or the repo's `hypr-lua-harness` reports no syntax error for `windowrules.lua`, `autostart.lua`, `keybinds.lua`.
    - Every edited JSON fixture still parses: `jq -e .` exits `0` for all six `*-layers.json` files, and `grep -ci waybar` returns `0` for each.
    - `pacman -Q waybar; echo $?` prints `1` with an error on stderr — the package is uninstalled.
    - `ls ~/.local/state/theme/waybar* 2>/dev/null | wc -l` returns `0`, and `test ! -f ~/.cache/current-waybar-layout` exits `0`.
    - `grep -ci waybar install.sh` returns `0`, and `grep -ci waybar stow.sh` returns `0`.
    - `git log -1 --format=%s` names both the config removal and the package removal, and `git rev-list --count HEAD ^HEAD~1` returns `1` — one commit, not two.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd /home/aorus/dotfiles &amp;&amp; set -e; test ! -d waybar || { echo "FAIL: package dir survives"; exit 1; }; test "$(ls hypr/.config/hypr/scripts/waybar-* 2>/dev/null | wc -l)" -eq 0 || { echo "FAIL: own-named scripts survive"; exit 1; }; C=theme-engine/.config/theme-engine/contract.json; jq -e . $C >/dev/null || { echo "FAIL: contract.json invalid"; exit 1; }; test "$(jq ".files | length" $C)" -eq 22 || { echo "FAIL: files[] is $(jq ".files|length" $C), expected 22"; exit 1; }; test "$(jq ".presence_only_files | length" $C)" -eq 1 || { echo "FAIL: presence_only_files count"; exit 1; }; test "$(jq ".engine_owned_files | length" $C)" -eq 11 || { echo "FAIL: engine_owned_files count"; exit 1; }; test "$(grep -ci waybar $C)" -eq 0 || { echo "FAIL: contract still names the surface"; exit 1; }; test ! -f matugen/.config/matugen/templates/waybar-colors.css || { echo "FAIL: template file survives"; exit 1; }; test "$(grep -ci waybar matugen/.config/matugen/config.toml)" -eq 0 || { echo "FAIL: matugen config still names it"; exit 1; }; K=hypr/.config/hypr/config/keybinds.lua; test "$(grep -c "bar-orientation.sh" $K)" -eq 1 || { echo "FAIL: Super+B not repointed"; exit 1; }; test "$(grep -ci waybar $K)" -eq 0 || { echo "FAIL: keybinds still names it"; exit 1; }; T=elephant/.config/elephant/menus/settings.toml; test "$(grep -c "bar-orientation.sh" $T)" -eq 1 || { echo "FAIL: menu entry wrong"; exit 1; }; test "$(grep -ci waybar $T)" -eq 0 || { echo "FAIL: superseded menu entry survives"; exit 1; }; R=hypr/.config/hypr/scripts/retirement-check; test "$(bash $R --list | grep -E "^waybar\|" | cut -d"|" -f2)" = "retired" || { echo "FAIL: registry row not flipped"; exit 1; }; test "$(bash $R --list | grep -cE "\|pending\|")" -eq 6 || { echo "FAIL: other rows disturbed"; exit 1; }; for f in theme-engine/.config/theme-engine/theme-doctor theme-engine/.config/theme-engine/theme-parity theme-engine/.config/theme-engine/theme-stress-test theme-engine/.config/theme-engine/lib/motion.sh theme-engine/.config/theme-engine/lib/font.sh theme-engine/.config/theme-engine/lib/commit.sh theme-engine/.config/theme-engine/lib/reload.sh install.sh stow.sh hypr/.config/hypr/scripts/tests/test-walker-dmenu-cancel.sh $R; do bash -n "$f" || { echo "FAIL: syntax in $f"; exit 1; }; done; S=theme-engine/.config/theme-engine/theme-stress-test; test "$(grep -c "palette.json" $S)" -ge 1 || { echo "FAIL: sentinel not repointed"; exit 1; }; test "$(grep -ci waybar $S)" -eq 0 || { echo "FAIL: stress-test still names it"; exit 1; }; for f in hypr/.config/hypr/scripts/tests/quickshell-fixtures/*-layers.json; do jq -e . "$f" >/dev/null || { echo "FAIL: invalid json $f"; exit 1; }; test "$(grep -ci waybar "$f")" -eq 0 || { echo "FAIL: fixture still names it: $f"; exit 1; }; done; if pacman -Q waybar >/dev/null 2>&1; then echo "FAIL: package still installed"; exit 1; fi; test "$(ls ~/.local/state/theme/waybar* 2>/dev/null | wc -l)" -eq 0 || { echo "FAIL: orphaned state files remain"; exit 1; }; test ! -f ~/.cache/current-waybar-layout || { echo "FAIL: orphaned cache file remains"; exit 1; }; test "$(grep -ci waybar install.sh)" -eq 0 || { echo "FAIL: install.sh still lists it"; exit 1; }; test "$(grep -ci waybar stow.sh)" -eq 0 || { echo "FAIL: stow.sh still references it"; exit 1; }; echo "PASS: config and package removed together, contract at 22, registry retired, Super+B repointed"'</automated>
  </verify>
  <done>The package directory, four scripts, matugen template, nine contract entries, four checker blocks, two layer rules, one autostart line, six fixtures, one test suite and five `stow.sh` surfaces are gone in one commit; Super+B and the stress-test sentinel are repointed rather than deleted; the registry row reads `retired` with the other six untouched; the pacman package and all orphaned host state are removed; every edited file still parses.</done>
</task>

<task type="auto">
  <name>Task 3: Scrub the residual references and prove the blocking tier reaches zero</name>
  <precondition>Task 2's deletion commit has landed and `retirement-check --list` reports the waybar row as `retired` — the blocking tier must be armed before this task's zero assertion means anything.</precondition>
  <files>hypr/.config/hypr/scripts/quickshell-doctor, hypr/.config/hypr/scripts/quickshell-launch.sh, hypr/.config/hypr/scripts/motion-lint, hypr/.config/hypr/scripts/hypr-equivalence-check, hypr/.config/hypr/scripts/hypr-lua-harness, hypr/.config/hypr/scripts/hyprpm-complete.sh, hypr/.config/hypr/scripts/gaming-mode-toggle.sh, hypr/.config/hypr/scripts/wallpaper-visibility.sh, hypr/.config/hypr/scripts/font-switcher.sh, hypr/.config/hypr/scripts/swaync-launch.sh, hypr/.config/hypr/scripts/record-toggle.sh, hypr/.config/hypr/config/env.lua, quickshell/.config/quickshell/shell.qml, quickshell/.config/quickshell/modules/Dashboard.qml, quickshell/.config/quickshell/modules/dashboard/AudioPanel.qml, theme-engine/.config/theme-engine/lib/contract.sh, theme-engine/.config/theme-engine/lib/generate.sh, theme-engine/.config/theme-engine/lib/gtk.sh, theme-engine/.config/theme-engine/lib/wallpaper.sh, matugen/.config/matugen/templates/swayosd-colors.css, wleave/.config/wleave/style.css, ags/.config/ags/widget/MediaWindow.tsx, .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md — the authoritative worklist is `retirement-check waybar`'s own `[FAIL]` output, which this list must be reconciled against before any edit</files>
  <read_first>
    - The output of `retirement-check waybar` as it stands after Task 2 — every remaining `[FAIL]` line names a file this task must handle. This is the authoritative worklist; the file list below is what plan-time inspection found, and the two must agree.
    - `hypr/.config/hypr/scripts/quickshell-doctor` — the header banner, the backend-cardinality registry string, and the hardcoded-height comment. Three references, all commentary, one inside a data structure.
    - `hypr/.config/hypr/scripts/motion-lint` — six references, all commentary.
    - `hypr/.config/hypr/scripts/hypr-equivalence-check` — three references in the header, including one naming the deleted checker as its own shape ancestor.
    - `hypr/.config/hypr/scripts/quickshell-launch.sh`, `hyprpm-complete.sh`, `swaync-launch.sh`, `record-toggle.sh`, `font-switcher.sh`, `gaming-mode-toggle.sh`, `wallpaper-visibility.sh`, `hypr-lua-harness` — each cites the retired surface's script as a shape precedent or a fan-out target.
    - `quickshell/.config/quickshell/shell.qml`, `modules/Dashboard.qml`, `modules/dashboard/AudioPanel.qml` — twelve references between them, all comments, several load-bearing: `Dashboard.qml`'s explain why its own exclusive-zone posture is what it is relative to the reserved zone.
    - `theme-engine/.config/theme-engine/lib/contract.sh`, `generate.sh`, `gtk.sh`, `wallpaper.sh` — residual comment references left after Task 2's functional edits.
    - `hypr/.config/hypr/config/env.lua`, `matugen/.config/matugen/templates/swayosd-colors.css`, `wleave/.config/wleave/style.css`, `ags/.config/ags/widget/MediaWindow.tsx` — one to two references each.
  </read_first>
  <action>
Take the worklist from `retirement-check waybar`'s own `[FAIL]` output rather than from this
plan's file list, and reconcile the two before starting. A file the script names that this
plan does not is a genuine discovery and is handled; a file this plan names that the script
does not was already cleaned by Task 2.

These are all comment references, and they are all blocking-tier: classes 7 and 9 are
word-boundary content scans that do not distinguish code from commentary, and 18-06
forbids adding any suppression mechanism to make them disappear from the tally. Zero means
zero.

Apply one rule to every reference: **rewrite the reasoning, drop the retired name, keep the
fact.** Most of these comments record why something is shaped the way it is, and that
reasoning usually survives the rename intact — a comment explaining that a script mirrors
another script's guard-then-exec shape stays true when the ancestor is named generically or
by what it did rather than by the retired package. A comment recording a real incident, such
as the stylesheet-discard failure that the state-file carve-out existed to prevent, keeps the
incident and loses only the package name.

Where a fact genuinely cannot survive the rename — where the retired surface's identity IS
the fact, such as a measurement taken against its specific reserved height, or a
cardinality note recording that it was one of several clients of a service — do not delete
the fact. Move it into this plan's SUMMARY under a heading recording what was scrubbed and
why, so the history survives outside the scanned tree. This is the difference between
scrubbing and destroying, and it is the reason this task is not a mechanical find-and-replace.

Give particular care to the QML comments. `Dashboard.qml`'s references explain why its
drawer sits where it does relative to a reserved zone it does not own — that reasoning is
now about the QML bar's reserved zone rather than the retired one, so these are rewrites
toward the current truth, not deletions. `shell.qml`'s reference to a captured socket2 event
sequence records a live observation; keep the observation and describe the event source
without the package name.

After the scrub, run `retirement-check waybar` again. This is the plan's **after-run**, and
it asserts something the before-run could not: with the registry row now `retired`, all
fourteen blocking-domain classes must emit `[PASS]` and the script must exit 0. The
before-run proved the inventory matched expectation while the surface still existed and the
tier was disarmed; this run proves the tier is armed and satisfied. Both are required by
RETIRE-01's run-before-and-after clause and neither substitutes for the other.

Run `theme-doctor` and confirm its tally is no worse than the pre-deletion tally captured in
Task 1, and that the retirement fold now contributes `[PASS]` lines for this surface rather
than `[REPORT]` lines. Confirm no block reports a permanent skip for the retired surface.

Write the after-run transcript to
`.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md` as the
paired artifact to 18-06's committed baseline, and commit the scrub.
  </action>
  <acceptance_criteria>
    - `bash hypr/.config/hypr/scripts/retirement-check waybar; echo $?` prints `0`.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar | grep -c '^\[FAIL\]'` returns `0`.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar | grep -c '^\[PASS\]'` returns `14` — one per blocking-domain class, all passing.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar | grep -c '^\[SKIP\]'` returns `0` — no class was unscannable, so no zero is a false zero.
    - `grep -ril waybar . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=wallpapers --exclude=README.md --exclude=VERIFICATION.md --exclude-dir=docs --exclude=CLAUDE.md | wc -l` returns `0`. This grep is deliberately unfiltered by comment syntax: the scrub target is total, including commentary, so filtering comment lines out would defeat the assertion rather than sharpen it.
    - `bash hypr/.config/hypr/scripts/retirement-check --all; echo $?` prints `0`, and its output contains `[PASS]` lines for the retired surface and `[REPORT]` summary lines for the six still-`pending` surfaces.
    - `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c '\[FAIL\] retirement-check'` returns `0`.
    - `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -ci 'SKIP.*waybar'` returns `0` — no dead permanently-skipping gate survives.
    - The `theme-doctor` `Summary: N passed, M failed` line reports an `M` no greater than the pre-deletion value recorded in Task 1.
    - `bash hypr/.config/hypr/scripts/quickshell-doctor 2>&1 | grep -c '\[FAIL\]'` is no greater than its pre-deletion value — the six edited fixtures still drive their checks correctly.
    - `bash hypr/.config/hypr/scripts/tests/test-walker-dmenu-cancel.sh theme-switch; echo $?` prints `0` — the surviving suite still runs after its sibling was removed.
    - `test -f .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md` exits `0`.
    - `git status --porcelain` produces no output after the commit.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd /home/aorus/dotfiles &amp;&amp; set -e; R=hypr/.config/hypr/scripts/retirement-check; bash $R waybar > /tmp/after.txt; rc=$?; test $rc -eq 0 || { echo "FAIL: after-run exit $rc"; exit 1; }; test "$(grep -c "^\[FAIL\]" /tmp/after.txt)" -eq 0 || { echo "FAIL: blocking-tier hits remain:"; grep "^\[FAIL\]" /tmp/after.txt; exit 1; }; test "$(grep -c "^\[PASS\]" /tmp/after.txt)" -eq 14 || { echo "FAIL: expected 14 PASS lines, got $(grep -c "^\[PASS\]" /tmp/after.txt)"; exit 1; }; test "$(grep -c "^\[SKIP\]" /tmp/after.txt)" -eq 0 || { echo "FAIL: an unscannable class makes its zero meaningless"; grep "^\[SKIP\]" /tmp/after.txt; exit 1; }; N=$(grep -ril waybar . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=wallpapers --exclude=README.md --exclude=VERIFICATION.md --exclude-dir=docs --exclude=CLAUDE.md | wc -l); test "$N" -eq 0 || { echo "FAIL: $N files still reference the retired surface:"; grep -ril waybar . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=wallpapers --exclude=README.md --exclude=VERIFICATION.md --exclude-dir=docs --exclude=CLAUDE.md; exit 1; }; bash $R --all > /tmp/all.txt; test $? -eq 0 || { echo "FAIL: --all nonzero"; exit 1; }; test "$(grep -c "^\[REPORT\]" /tmp/all.txt)" -ge 6 || { echo "FAIL: pending surfaces not summarised"; exit 1; }; D=theme-engine/.config/theme-engine/theme-doctor; bash $D > /tmp/doc.txt 2>&1 || true; test "$(grep -c "\[FAIL\] retirement-check" /tmp/doc.txt)" -eq 0 || { echo "FAIL: retirement fold reports a failure"; exit 1; }; test "$(grep -ci "SKIP.*waybar" /tmp/doc.txt)" -eq 0 || { echo "FAIL: a permanently-skipping dead gate survives"; exit 1; }; bash hypr/.config/hypr/scripts/tests/test-walker-dmenu-cancel.sh theme-switch >/dev/null 2>&1 || { echo "FAIL: surviving dmenu suite broke"; exit 1; }; test -f .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-AFTER-waybar.md || { echo "FAIL: after-run artifact not written"; exit 1; }; test -z "$(git status --porcelain)" || { echo "FAIL: uncommitted changes remain"; exit 1; }; echo "PASS: after-run reports 14 PASS / 0 FAIL / 0 SKIP against a retired row; zero blocking references survive"'</automated>
  </verify>
  <done>Every residual reference outside the report domain is scrubbed with its reasoning preserved or relocated to the SUMMARY; the after-run reports fourteen `[PASS]` lines, zero `[FAIL]` and zero `[SKIP]` against a `retired` registry row; `theme-doctor`'s tally is no worse than before the deletion and carries no permanently-skipping gate; the after-run transcript is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan manifest → filesystem deletion | A path list drives irreversible `git rm` and `rm` operations across ~60 files |
| plan → pacman on a live host | A package removal executes against the running system, outside git's reach |
| repo tree → `retirement-check` scan | The script's exit code is the phase's completion proof; anything that weakens the scan weakens the proof |
| deleted surface → surviving consumers | Five files consume the seven contract names; a partial removal leaves a renderer or a consumer dangling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-20-01 | Denial of Service | Mass deletion driven by a stale or over-broad manifest | high | mitigate | Task 1 reconciles every path by content before Task 2 deletes anything, and halts on any drift it cannot attribute to a named prior plan. Task 2 deletes only through `git rm` against tracked files with no recursive glob outside the single package directory, so the entire surface is restorable by reverting one commit. The prohibition against widening the deletion beyond the reconciled manifest is explicit |
| T-18-20-02 | Denial of Service | `pacman` removal cascading to dependent packages | high | mitigate | Task 1 asserts `pactree -r` returns only the package itself and `pacman -Qi` reports `Required By: None` — both verified at plan time on this host — and halts otherwise. Task 2 uses pacman's dependency-respecting removal and is prohibited from any flag that ignores or cascades dependencies |
| T-18-20-03 | Tampering | Partial contract removal leaving a renderer or consumer dangling | high | mitigate | The `files[]`/`theme-apply`/`theme-doctor`/`theme-parity`/`motion.sh` chain is named as a key link and the seven names are removed from all five in one commit. Task 3 gates on `theme-doctor`'s tally being no worse than the pre-deletion tally and on zero permanently-skipping blocks |
| T-18-20-04 | Denial of Service | Stress-test sentinel repointed at a source that is itself retired later this milestone | high | mitigate | The repoint targets `palette.json`, a contract entry no remaining RETIRE requirement removes. Task 1 asserts the old and new sources yield an identical value before the change, so the repoint is proven value-preserving rather than assumed. Repointing at another gtk-css sheet is explicitly forbidden in the action |
| T-18-20-05 | Denial of Service | A live bind or menu entry left pointing at a deleted script | high | mitigate | Super+B is repointed at `bar-orientation.sh` rather than deleted, and the superseded menu entry is removed. Acceptance criteria assert the bind count is unchanged and the new target appears exactly once, so a silently-dead bind cannot pass |
| T-18-20-06 | Elevation of Privilege | Deleting a file outside the repository through an unanchored path | medium | mitigate | Host-side removals are confined to an enumerated list of state and cache files under the user's own directories; no wildcard removal is issued outside `~/.local/state/theme/` and no path is constructed from any external input |
| T-18-20-07 | Repudiation | The completion proof weakened by suppressing rather than removing a reference | medium | mitigate | 18-06 forbids any per-file or per-surface suppression that removes a blocking reference from the tally without printing it; this plan restates that as a prohibition. The after-run additionally asserts zero `[SKIP]` lines, so an unscannable class can never be mistaken for a clean one |
| T-18-20-08 | Information Disclosure | Historical reasoning destroyed while scrubbing comments | low | mitigate | Task 3's scrub rule preserves the reasoning and relocates any fact that cannot survive the rename into the SUMMARY rather than deleting it |
| T-18-20-09 | Denial of Service | The running session losing its bar when the package is uninstalled underneath it | low | accept | The QML bar has been the live bar since wave 1 and does not link against the removed package; the removed binary is not running by this point. Recorded as a backstop truth rather than asserted, since it depends on live session state |
| T-18-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager **install** occurs in this plan — the only package operation is a removal of an already-installed, already-audited official-repository package. The legitimacy gate guards untrusted incoming packages and has no install to guard here, so no `[ASSUMED]`/`[SUS]` checkpoint applies. Recorded rather than omitted so the absence is visibly deliberate |
</threat_model>

<verification>
1. **The gate authorised it.** `18-19-SUMMARY.md` exists and records a GATE-02 pass with B.3
   noted as not demonstrable on this hardware; Task 1 halts the plan otherwise.
2. **Before-run, disarmed.** `retirement-check waybar` against a `pending` row exits 0 with
   zero `[FAIL]` and at least 14 `[REPORT]` lines, diffed against 18-06's committed baseline
   with every difference attributable to a named prior plan.
3. **After-run, armed.** `retirement-check waybar` against a `retired` row exits 0 with
   exactly 14 `[PASS]`, zero `[FAIL]` and zero `[SKIP]`.
4. **One commit.** The config removal, the contract edits and the package uninstall share a
   single commit; `git rev-list --count` confirms it.
5. **Contract arithmetic.** `files[]` is 22, `presence_only_files` is 1,
   `engine_owned_files` is 11, and the file is valid JSON.
6. **Reproducibility.** Neither `install.sh` nor `stow.sh` names the surface, and
   `pacman -Q waybar` exits non-zero on this host.
7. **Nothing dangling.** Super+B reaches `bar-orientation.sh`; the stress-test sentinel
   reaches `palette.json`; `theme-doctor`'s tally is no worse than pre-deletion with no
   permanently-skipping block; the surviving dmenu suite still runs.
8. **Report domain untouched.** `.planning/`, `README.md`, `VERIFICATION.md`, `docs/` and
   `.claude/CLAUDE.md` retain their historical references and are excluded from every
   negative grep.
</verification>

<success_criteria>
- RETIRE-02 is closed: package, config, contract entries, matugen template,
  `waybar-equivalence-check` and `waybar-design-lint` are all gone, in the phase that proved
  the replacement.
- RETIRE-01's second half is delivered: the checklist script ran before and after the
  deletion, and both runs are recorded, with the plan stating what each proves.
- The registry row reads `retired`, flipped in the deletion commit, arming the blocking tier
  for this surface permanently.
- `contract.json` stands at 22 `files[]` entries — the running total RETIRE-08 lands on after
  Phases 19-21 remove the remaining five.
- `~/.local/state/theme/waybar-visibility.css` is gone, not orphaned.
- Zero blocking-domain references survive anywhere outside `.planning/` and repo prose.
- No capability is silently lost: Super+B still opens an orientation picker, the stress test
  still resolves a materialyou sentinel, and the notification bell still reaches its
  Phase-19-owned target.
</success_criteria>

<output>
Create `.planning/phases/18-qml-bar-retirement-machinery/18-20-SUMMARY.md` when done.

Record for downstream phases:
- The final `contract.json` counts (22 / 1 / 11) as RETIRE-08's starting point in Phase 21.
- The registry state after this plan: one row `retired`, six `pending`, one self-test row —
  and the standing warning from 18-06 that `wlogout` and `eww` must not be tidied to
  `retired` while they still carry references (RETIRE-07, Phase 20).
- The scrubbed-history section: every fact that could not survive the comment scrub, moved
  here verbatim rather than destroyed.
- The `theme-stress-test` sentinel repoint, flagged for Phase 19's LEDGER-07 clean run —
  the materialyou branch now reads `palette.json`, and the reason no gtk-css sheet is a
  durable source is that all four definers are retired during this milestone.
- Confirmation that D-18-33's temporary bell binding survived the deletion, since Phase 19
  owns swapping what sits behind it.
</output>
