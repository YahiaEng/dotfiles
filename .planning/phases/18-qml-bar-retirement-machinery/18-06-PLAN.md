---
phase: 18-qml-bar-retirement-machinery
plan: 06
type: execute
wave: 2
depends_on: ["18-03"]
files_modified:
  - hypr/.config/hypr/scripts/retirement-check
  - hypr/.config/hypr/scripts/tests/retirement-fixtures/
  - theme-engine/.config/theme-engine/theme-doctor
  - .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md
autonomous: true
requirements: ["RETIRE-01"]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`retirement-check waybar` exits 0 and prints a per-class reference inventory while waybar is still live (registry status `pending`), so folding it into `theme-doctor` at wave 2 cannot turn the tally red eighteen plans before the deletion lands (D-18-35)."
    - "`retirement-check swaync` exits 0 and prints a non-empty per-class inventory that includes the in-repo systemd drop-in `swaync/.config/systemd/user/swaync.service.d/override.conf` — a file class waybar has no instance of — proving the script is generic and not waybar-shaped (D-18-34)."
    - "`retirement-check notarealsurface` exits 2, writes an error naming the allowed surfaces to stderr, and constructs no filesystem path and no grep pattern from the rejected argument."
    - "`retirement-check --self-test` exits 0, and within it the poisoned fixtures each drive the blocking tier to a `[FAIL]` line with a non-zero exit — the blocking tier is proven able to fail before 18-20 depends on it (motion-lint's D-28 `--self-test` precedent)."
    - "The `poisoned-planning-only` fixture, whose only reference to the surface lives under `.planning/`, produces a `[REPORT]` line and exit 0 — never a `[FAIL]` — proving D-18-37's tier split is enforced in code and not merely documented."
    - "`theme-doctor` invokes `retirement-check --all` and folds only `[PASS]`/`[FAIL]` lines into its pass/fail tally; `[REPORT]` and `[SKIP]` lines pass through unfolded, matching the motion-lint fold's `*)` branch (theme-doctor:698)."
    - "Blocking-domain classes never count hits under `.planning/` or in repo prose (`README.md`, `VERIFICATION.md`, `docs/`, `.claude/CLAUDE.md`); report-domain classes never influence the exit code (D-18-37)."
    - "Reference-class coverage spans all fourteen blocking classes plus the two report classes named in this plan's Reference Class Registry — covering every class RETIRE-01's requirement text enumerates and all four classes D-18-36 adds."
    - "Surface matching is case-insensitive and word-boundary anchored, so `bar` never matches inside `waybar`, while `waybar` correctly matches inside `waybar-style-full.css`; a file attributed to a specific class is not re-counted under `cross-package-refs`."
    - "A scan root that does not exist on this host reports a `[SKIP]` line naming the missing root; it is never reported as a genuine zero and never silently omitted."
    - "Two consecutive runs over an unchanged tree produce byte-identical stdout — class order is the fixed declared order and within-class hits are `LC_ALL=C` sorted — which is what makes 18-20's before/after diff meaningful."
    - "The script writes to stdout and stderr only; it acquires no lock, creates no temp file, and mutates nothing it scans, so concurrent or interrupted runs leave no partial state and cannot corrupt each other."
    - "The pre-deletion waybar baseline produced by this plan is committed at `.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md` and is the artifact 18-20's post-deletion run is compared against."
  prohibitions:
    - "MUST NOT delete, edit, move, or otherwise mutate any file it scans — a stray reference is reported, never auto-removed, even when the fix looks obvious."
    - "MUST NOT report a reference class as clean when that class could not actually be scanned; an unscannable root must be visibly distinguishable in the output from a genuinely-zero root."
    - "MUST NOT provide a per-file or per-surface suppression mechanism that removes a real blocking-domain reference from the tally without printing it — an acknowledged exception must stay visible on every run, never become an invisible permanently-green gate."
  artifacts:
    - hypr/.config/hypr/scripts/retirement-check
    - hypr/.config/hypr/scripts/tests/retirement-fixtures/compliant-clean-surface/
    - hypr/.config/hypr/scripts/tests/retirement-fixtures/poisoned-stray-layer-rule/
    - hypr/.config/hypr/scripts/tests/retirement-fixtures/poisoned-stray-contract-entry/
    - hypr/.config/hypr/scripts/tests/retirement-fixtures/poisoned-stray-cross-script-ref/
    - hypr/.config/hypr/scripts/tests/retirement-fixtures/poisoned-planning-only/
    - theme-engine/.config/theme-engine/theme-doctor
    - .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md
  key_links:
    - "`theme-doctor` -> `retirement-check --all` fold block — the tally connection D-18-35 requires; without it the checklist is a script someone has to remember to run, which is the exact WINDOWS #1 failure mode."
    - "Registry `status` field (`pending` -> `retired`) is the single switch that arms the blocking tier; 18-20 flips waybar's row in the same commit as the deletion."
    - "Allowlist validation gate sits before every path construction and every grep-pattern interpolation — the single control mitigating T-18-06-01/02/03."
    - "`--self-test` fixtures -> the blocking tier's fail path; this is the only proof the blocking tier can fail until 18-20 runs it for real."
---

<objective>
Build `retirement-check` — the generic, reusable retirement checklist script RETIRE-01
requires and RETIRE-02..06 all depend on — then fold its blocking tier into
`theme-doctor` and run it once against `waybar` to capture the pre-deletion baseline
that 18-20 compares against.

The script is **generic from day one** (D-18-34): it takes `<surface-name>`, validates
it against an allowlist before constructing any path or grep pattern, and reports every
reference class for that surface across the repo and the live host. It carries **two
tiers in one script** (D-18-37): a blocking tier over live code, config, fixtures and
checker internals, and a non-failing `[REPORT]` tier over `.planning/` and repo prose.

Purpose: Phase 19 deletes swaync with no soak window and no rollback. That is a poor
moment to be refactoring the tool that verifies the deletion. Phase 18 is also the only
phase that exercises this tool twice against a real deletion before anything else
depends on it, so the generalisation happens here or it happens under time pressure.

Output: `hypr/.config/hypr/scripts/retirement-check`, five committed self-test fixture
trees, a `retirement-check --all` fold in `theme-doctor`, and the committed waybar
pre-deletion baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-qml-bar-retirement-machinery/18-CONTEXT.md
@.planning/phases/18-qml-bar-retirement-machinery/18-PATTERNS.md

@theme-engine/.config/theme-engine/theme-doctor
@hypr/.config/hypr/scripts/motion-lint
@hypr/.config/hypr/scripts/waybar-design-lint
@hypr/.config/hypr/scripts/waybar-visibility.sh
</context>

<scope_facts>
## UI scope

This plan is tooling — a bash checklist script and a `theme-doctor` fold. **No
`## UI Considerations` rows from `18-UI-SPEC.md` are in scope for this plan.** Stated
explicitly rather than silently omitted: `18-UI-SPEC.md` governs the QML bar surfaces
built by 18-01/05/08-16, and nothing in this plan renders a pixel.

## Measured ground truth (verified on this host, 2026-08-10 — do not re-derive)

These counts come from a direct census of the working tree and live host. They are the
numbers the executor should expect to see; a wild divergence means a scan is wrong.

| Fact | Value |
|---|---|
| Files outside `.planning/` referencing `waybar` | 55 |
| `waybar` mentions inside `.planning/` | 3796 |
| `swaync` mentions inside `.planning/` | 1244 |
| Largest single blocking-domain offender | `theme-engine/.config/theme-engine/theme-doctor` — 45 hits |
| `windowrules.lua` waybar layer rules | 2 (`hl.layer_rule` at lines 222 and 401) |
| `autostart.lua` waybar exec lines | 2 (lines 42 and 48) |
| `contract.json` waybar entries | 9 (7 template entries + `waybar-font.css` + `waybar-visibility.css`) |
| `matugen/config.toml` waybar lines | 4 (the `[templates.waybar]` block) |
| `stow.sh` / `install.sh` waybar lines | 21 / 1 |
| Live systemd `--user` waybar unit | `waybar.service` — `disabled`, packaged, never started by this repo |
| In-repo systemd `--user` unit tree that exists today | `swaync/.config/systemd/user/swaync.service.d/override.conf` |
| Repo hits for already-repo-retired `wlogout` / `eww` | 3 / 33 |

The last row is load-bearing and is why **every registry row ships `pending` in this
phase** (see the Registry note in Task 1). `eww` and `wlogout` are repo-retired but not
reference-free; marking either `retired` now would arm the blocking tier against real
surviving hits and turn `theme-doctor` red for the remaining eighteen plans of this
phase. RETIRE-07 closes them in Phase 20; that is the correct moment to flip them.

## Detectors

api-coverage, assumption-delta and schema-gate were all run by the orchestrator against
this plan's scope and all three came back negative. No API surface, no assumption delta,
no schema. Security is ACTIVE — see `<threat_model>`.
</scope_facts>

<artifacts_produced>
## Artifacts This Phase Produces

**Script path:** `hypr/.config/hypr/scripts/retirement-check`
(reachable at `$HOME/.config/hypr/scripts/retirement-check` — that path is a symlink
into this repo, verified, so a new file in the repo is live immediately with no re-stow)

### Invocation surface

| Invocation | Behaviour | Exit code |
|---|---|---|
| `retirement-check <surface-name>` | Full per-class report for one surface | `0` clean / `1` any `[FAIL]` / `2` invalid argument |
| `retirement-check <surface-name> --root <dir>` | Same, treating `<dir>` as the repo root instead of `$DOTFILES_DIR` (motion-lint's `[target-dir]` precedent — a gate that cannot be pointed at a fixture cannot be proven to fail) | as above |
| `retirement-check --all` | One summary line per `pending` surface; full blocking checks for every `retired` surface. This is what `theme-doctor` calls. | `0` / `1` |
| `retirement-check --self-test` | Runs the five committed fixture trees and asserts each expected verdict | `0` all as expected / `1` otherwise |
| `retirement-check --list` | Prints the registry (surface, status, retiring requirement) and exits | `0` |
| `retirement-check` (no argument) | Usage to stderr | `2` |

### Tier labels

| Label | Tier | Folded by `theme-doctor`? | Meaning |
|---|---|---|---|
| `[PASS]` | blocking | yes — increments `PASS` | A blocking-domain class is reference-free for a `retired` surface |
| `[FAIL]` | blocking | yes — increments `FAIL` | A blocking-domain class still carries references to a `retired` surface |
| `[REPORT]` | reported | no — passed through unfolded | An inventory line: a `pending` surface's blocking-domain count, or any report-domain (`.planning/` / prose) count for any surface |
| `[SKIP]` | neither | no — passed through unfolded | A scan root does not exist on this host; explicitly not a zero |

### Reference class names (14 blocking + 2 report)

Blocking domain: `own-tree`, `layer-window-rules`, `autostart`, `keybinds`,
`contract-json`, `matugen-templates`, `checker-internals`, `test-fixtures`,
`cross-package-refs`, `install-stow-lists`, `systemd-units`, `dbus-activation`,
`xdg-autostart`, `host-package`.

Report domain: `planning-archive`, `repo-prose`.

### `theme-doctor` fold identifiers

`theme-doctor`'s three existing folds (`waybar-design-lint` at ~660, `motion-lint` at
~681, `hypr-equivalence-check` at ~705) are **inline blocks, not functions** — there is
no `_theme_doctor_*_fold()` precedent to follow, and inventing one here would break the
copy-the-fold-pattern instruction. This plan therefore names the fold by its two stable
identifiers, which is what 18-20 greps for when it removes the waybar block:

- Section header comment: `# ── retirement-check fold (18-06, RETIRE-01/D-18-35) ───`
- Guard variable: `RETIREMENT_CHECK`
</artifacts_produced>

<reference_class_registry>
## Reference Class Registry — the table the executor implements one scan function per row

`<repo>` is `$DOTFILES_DIR` (or `--root <dir>`). `<S>` is the validated surface token.
Every scan matches `<S>` **case-insensitively with word boundaries on both sides**, and
excludes `.git/`, `node_modules/`, `wallpapers/`, and binary files.

### Blocking domain (14 classes)

| # | Class name | Scan target | Notes |
|---|---|---|---|
| 1 | `own-tree` | The surface's own stow package dir and own-named scripts, from the registry row's own-tree field | For `waybar`: `<repo>/waybar/` plus `<repo>/hypr/.config/hypr/scripts/waybar-*`. Asserts the surface's own files are gone, which is RETIRE-02's deletion list restated as a check |
| 2 | `layer-window-rules` | `<repo>/hypr/.config/hypr/config/windowrules.lua` | Catches `hl.layer_rule`/`hl.window_rule` rows. Expect 2 waybar hits today |
| 3 | `autostart` | `<repo>/hypr/.config/hypr/config/autostart.lua`, `<repo>/hypr/.config/hypr/config/env.lua` | Catches `hl.exec_cmd` launch lines. Expect 2 waybar hits today |
| 4 | `keybinds` | `<repo>/hypr/.config/hypr/config/keybinds.lua` | Expect 2 waybar hits today |
| 5 | `contract-json` | `<repo>/theme-engine/.config/theme-engine/contract.json` | All arrays — templates, `presence_only_files`, `engine_owned_files`. This is WINDOWS #1's exact class: an orphaned contract entry blocked `theme-doctor` for a full milestone because nothing enforced it |
| 6 | `matugen-templates` | `<repo>/matugen/.config/matugen/config.toml`, `<repo>/matugen/.config/matugen/templates/` | Catches both the `[templates.<S>]` block and an orphaned template file |
| 7 | `checker-internals` | `<repo>/theme-engine/.config/theme-engine/{theme-doctor,theme-parity,theme-stress-test}`, `<repo>/hypr/.config/hypr/scripts/{quickshell-doctor,keybind-doctor,keybind-source-equivalence,motion-lint,hypr-equivalence-check}` | D-18-36's "theme-doctor's own internal blocks". **`retirement-check` itself is excluded** — a registry entry naming a surface is not a reference to it |
| 8 | `test-fixtures` | `<repo>/hypr/.config/hypr/scripts/tests/` | D-18-36's "test fixtures and doctor registries" |
| 9 | `cross-package-refs` | Deny-by-default: every top-level stow package dir under `<repo>` **except** the surface's own tree and except files already attributed to classes 2-8 | D-18-36's "cross-script references in unrelated scripts". Deny-by-default (motion-lint's D-23 discipline) so a stow package added in a later phase is covered the moment it exists, with no registry edit |
| 10 | `install-stow-lists` | `<repo>/install.sh`, `<repo>/stow.sh` | Expect 1 + 21 waybar hits today |
| 11 | `systemd-units` | Live: `systemctl --user list-unit-files`. In-repo: `<repo>/*/.config/systemd/user/` | RESEARCH.md line 534 recommends the live check by name. The in-repo half is real today for swaync and is the class that most cleanly proves genericity |
| 12 | `dbus-activation` | `/usr/share/dbus-1/services/`, `$HOME/.local/share/dbus-1/services/` | Filename and `Exec=`/`Name=` content |
| 13 | `xdg-autostart` | `/etc/xdg/autostart/`, `$HOME/.config/autostart/` | Filename and `Exec=` content |
| 14 | `host-package` | `pacman -Q <S>` | RETIRE-07 exists precisely because a repo-retired surface can still be installed. Guarded: no `pacman` on PATH is a `[SKIP]`, never a silent pass |

### Report domain (2 classes, never affect exit code)

| # | Class name | Scan target | Notes |
|---|---|---|---|
| 15 | `planning-archive` | `<repo>/.planning/` | Count only, never per-hit. 3796 waybar hits today — D-18-37's whole rationale. These are shipped milestone archives that must stay |
| 16 | `repo-prose` | `<repo>/README.md`, `<repo>/VERIFICATION.md`, `<repo>/docs/`, `<repo>/.claude/CLAUDE.md`, and any other `*.md` outside `.planning/` | Count only |

### Known precision limit (record, do not silently ignore)

A surface whose name is a short common word can word-boundary-match unrelated prose —
`ags` is the live example. Word boundaries already exclude `tags`/`flags`/`images`, so
the residual exposure is a standalone occurrence of the token in an unrelated sentence,
which lands almost entirely in the report domain. Print this limit in `--list` output so
a future phase reading an `ags` report knows the number carries prose noise.
</reference_class_registry>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "check one surface for one reference class" — the whole path, one class only</name>

  <read_first>
    - `hypr/.config/hypr/scripts/waybar-visibility.sh` lines 296-336 — `main()`'s case statement. This is the T-08-05 allowlist-before-path-construction precedent the `<surface-name>` guard copies verbatim in shape.
    - `hypr/.config/hypr/scripts/motion-lint` lines 1-115 — the header contract block, the `[PASS]`/`[FAIL]` printf helpers at 110-113, and the usage block documenting `[target-dir]` / `--self-test` / `--no-pending`. This is the script skeleton to mirror.
    - `hypr/.config/hypr/scripts/waybar-design-lint` — the shorter, single-purpose lint whose overall file shape (header, CHECK sections, tally, exit) this script is closest in size to.
    - `theme-engine/.config/theme-engine/theme-doctor` lines 1-60 — `check()`, `PASS`/`FAIL` counters, `DOTFILES_DIR`, and the `[PASS]`/`[FAIL]` output format the fold consumes.
    - `theme-engine/.config/theme-engine/contract.json` — the class-5 scan target, read in full so the executor knows exactly what an entry looks like.
  </read_first>

  <files>hypr/.config/hypr/scripts/retirement-check</files>

  <action>
Create `retirement-check` as a `#!/usr/bin/env bash` script with `set -uo pipefail`,
mode `755`, opening with a boxed header comment block in the exact style of
`motion-lint` and `waybar-design-lint`: name the requirement (`RETIRE-01`), the
decisions it implements (`D-18-34` generic from day one, `D-18-35` blocking fold into
`theme-doctor`, `D-18-36` four-class coverage extension, `D-18-37` two tiers in one
script), and state the report-only posture in the same words the two sibling linters use.

Wire the complete vertical path for exactly ONE reference class — `contract-json`
(class 5) — so every layer is proven before any horizontal expansion:

**Argument parsing and validation.** Accept `<surface-name>` as `$1`, plus the optional
`--root <dir>` flag. Before constructing any filesystem path and before interpolating
the argument into any grep pattern, run two guards in this order, mirroring
`waybar-visibility.sh`'s reject-before-use discipline: first a syntactic guard rejecting
anything not matching `^[a-z][a-z0-9-]*$` (this alone excludes every path separator,
every `..` traversal sequence and every regex metacharacter — implements D-18-34's
security requirement and mitigates T-18-06-01, T-18-06-02 and T-18-06-03 in one gate);
then a registry-membership guard rejecting any token that is not a declared registry key.
Both rejections write to stderr naming the allowed surfaces and `exit 2` — an exit code
deliberately distinct from `1`, so a caller can tell "you asked wrong" from "the surface
is dirty". No argument at all prints usage and exits `2`.

**The registry.** Declare `RETIREMENT_REGISTRY` as a newline-delimited here-doc table,
one row per surface, four pipe-separated fields: surface token, status
(`pending` or `retired`), a colon-separated own-tree path list, and the retiring
requirement ID. Ship exactly these eight rows, every one of them `pending`:
`waybar|pending|waybar/:hypr/.config/hypr/scripts/waybar-*|RETIRE-02`,
`swaync|pending|swaync/:hypr/.config/hypr/scripts/swaync-*|RETIRE-03`,
`swayosd|pending|swayosd/|RETIRE-04`,
`wleave|pending|wleave/:hypr/.config/hypr/scripts/wleave.sh|RETIRE-05`,
`ags|pending|ags/|RETIRE-06`,
`wlogout|pending||RETIRE-07`,
`eww|pending||RETIRE-07`, and
`retirement-fixture|retired|hypr/.config/hypr/scripts/tests/retirement-fixtures/|SELF-TEST`.

Carry an inline comment on the `wlogout` and `eww` rows recording the measured reason
they stay `pending`: both are repo-retired but not reference-free (3 and 33 surviving
hits respectively, measured 2026-08-10), and RETIRE-07 in Phase 20 is where they close.
A future reader must not "tidy" them to `retired` and arm the blocking tier against
known-surviving hits.

The `retirement-fixture` row is `retired` by design and is the one row whose blocking
tier is armed in normal operation. Against the real repo it finds nothing (the token is
not a real package), so it emits `[PASS]` lines on every `theme-doctor` run — which
means the blocking tier is continuously exercised and demonstrably green rather than
dormant until 18-20. Exclude `retirement-check` itself from the `checker-internals`
scan for every surface: a checker naming the surfaces it checks is a registry, not a
stray reference.

**Matching contract.** Build the search pattern once, from the validated token only, as
a word-boundary-anchored case-insensitive pattern. Word boundaries are the resolution of
this plan's adjacency contract: `bar` must not match inside `waybar`, while `waybar` must
match inside `waybar-style-full.css`. Sort every within-class hit list under `LC_ALL=C`
and emit classes in the fixed declared order, so two runs over an unchanged tree are
byte-identical — 18-20's before/after diff depends on this and nothing else.

**Tier dispatch.** Implement the status-driven tier rule as a single shared emitter both
this class and every later class calls. For a `retired` surface, a blocking-domain class
emits `[PASS] <S>/<class>: no references` on zero hits and
`[FAIL] <S>/<class>: N reference(s)` followed by the sorted `path:line` list otherwise,
incrementing the failure tally. For a `pending` surface, the same class emits
`[REPORT] <S>/<class>: N reference(s)` plus the sorted list, and never touches the
failure tally. A scan target that does not exist emits
`[SKIP] <S>/<class>: <root> not present` — never a zero, never omitted, which is this
plan's second prohibition made mechanical.

**Exit.** Print a trailing summary line in `theme-doctor`'s idiom, then exit `1` if the
failure tally is non-zero, else `0`.

The script writes to stdout and stderr only. It takes no lock, creates no temp file, and
opens no file for writing — the resolution of this plan's concurrency contract, and the
same report-only posture `motion-lint`'s and `waybar-design-lint`'s headers already
declare.
  </action>

  <acceptance_criteria>
    - `bash hypr/.config/hypr/scripts/retirement-check waybar` exits `0` and its stdout contains a line matching `\[REPORT\] waybar/contract-json: 9 reference` (the measured contract.json count).
    - `bash hypr/.config/hypr/scripts/retirement-check swaync` exits `0` and its stdout contains a line beginning `[REPORT] swaync/contract-json:`.
    - `bash hypr/.config/hypr/scripts/retirement-check retirement-fixture` exits `0` and its stdout contains `[PASS] retirement-fixture/contract-json` — the blocking tier's pass path, live against the real repo.
    - `bash hypr/.config/hypr/scripts/retirement-check notarealsurface; echo $?` prints `2`, and its stderr names the eight allowed surfaces.
    - `bash hypr/.config/hypr/scripts/retirement-check '../../etc'; echo $?` prints `2` — rejected by the syntactic guard, with no path constructed.
    - `bash hypr/.config/hypr/scripts/retirement-check 'way.ar'; echo $?` prints `2` — the regex metacharacter is rejected before pattern interpolation.
    - `bash hypr/.config/hypr/scripts/retirement-check; echo $?` prints `2` and writes usage to stderr.
    - `bash hypr/.config/hypr/scripts/retirement-check --list` exits `0` and prints all eight registry rows with their status.
    - Determinism: `bash hypr/.config/hypr/scripts/retirement-check waybar > /tmp/r1.txt; bash hypr/.config/hypr/scripts/retirement-check waybar > /tmp/r2.txt; diff /tmp/r1.txt /tmp/r2.txt` exits `0` with no output.
    - Read-only posture: `git status --porcelain` reports no change caused by any of the runs above beyond the new script file itself.
    - `test -x hypr/.config/hypr/scripts/retirement-check` exits `0`.
  </acceptance_criteria>

  <verify>
    <automated>bash hypr/.config/hypr/scripts/retirement-check waybar >/dev/null && bash hypr/.config/hypr/scripts/retirement-check retirement-fixture | grep -q '\[PASS\] retirement-fixture/contract-json' && ! bash hypr/.config/hypr/scripts/retirement-check notarealsurface 2>/dev/null; test $? -eq 0 && echo TRACER-OK</automated>
  </verify>

  <done>
`retirement-check <surface-name>` runs end-to-end for one real reference class: the
argument is allowlist-validated before any path or pattern is built, the registry
resolves the surface's status, the `contract-json` class is scanned with the
word-boundary matching contract, and the correct tier label is emitted with the correct
exit code for all three cases — `pending` surface, `retired` surface, rejected argument.
Committed.
  </done>

  <reversibility rating="reversible">
A new standalone report-only script with no callers yet; deleting the file restores the
prior state exactly.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Expand to all sixteen reference classes, add `--all`, and prove the blocking tier can fail with committed fixtures</name>

  <read_first>
    - `hypr/.config/hypr/scripts/retirement-check` — the Task 1 tracer, in full; every new class calls its shared emitter.
    - `hypr/.config/hypr/scripts/motion-lint` lines 1040-1100 — the `--self-test` fixture loop: iterate committed fixtures one at a time in isolation, assert compliant fixtures exit `0` and poisoned fixtures exit non-zero, print `[PASS]`/`[FAIL] self-test:` per fixture. This is the exact shape to copy, and D-28's "a gate that cannot fail is not a gate" is its stated reason for existing.
    - `hypr/.config/hypr/scripts/motion-lint` lines 405-500 — the `EXEMPTIONS`/`LINE_EXEMPTIONS` visibility discipline, read as the negative example this plan's third prohibition forbids: **do not port an exemption mechanism into `retirement-check`.**
    - `hypr/.config/hypr/config/windowrules.lua` lines 216-225 and 395-405 — the two live waybar `hl.layer_rule` calls class 2 must find.
    - `hypr/.config/hypr/config/autostart.lua` lines 40-50 — the two live waybar `hl.exec_cmd` lines class 3 must find.
    - `stow.sh` lines 255-290 and 395-455 — the waybar state-file seeds and the sheet list class 10 must find.
    - This plan's `<reference_class_registry>` table — the authoritative per-class scan-target definition.
  </read_first>

  <files>hypr/.config/hypr/scripts/retirement-check, hypr/.config/hypr/scripts/tests/retirement-fixtures/</files>

  <action>
Implement the remaining fifteen reference classes exactly as the
`<reference_class_registry>` table defines them — thirteen more blocking classes
(`own-tree`, `layer-window-rules`, `autostart`, `keybinds`, `matugen-templates`,
`checker-internals`, `test-fixtures`, `cross-package-refs`, `install-stow-lists`,
`systemd-units`, `dbus-activation`, `xdg-autostart`, `host-package`) and the two
report-domain classes (`planning-archive`, `repo-prose`). Each is one function calling
the Task 1 shared emitter; emit them in the table's declared order.

Three classes carry rules that are not obvious from their names and must be implemented
deliberately:

`cross-package-refs` is **deny-by-default**: enumerate the top-level stow package
directories under the repo root at runtime rather than from a hardcoded list, subtract
the surface's own tree, and subtract every file already attributed to classes 2 through
8 so a single hit is never counted twice. This is motion-lint's D-23 discipline applied
to packages — a stow package added by a later phase is covered the moment it exists,
with no registry edit. It is also the class that will catch `bar-visibility.sh` after
18-15's rename if any of the current file's 27 waybar mentions survive in its comments
and error strings; note that in a comment so 18-15 and 18-20 are not surprised by it.

`systemd-units` has two halves that must both run: the live half shells
`systemctl --user list-unit-files` and matches the surface token against unit names, and
the in-repo half globs `*/.config/systemd/user/` for unit files and drop-in directories.
The in-repo half is not hypothetical — `swaync/.config/systemd/user/swaync.service.d/override.conf`
exists today and is a file class waybar has no instance of, which is what makes the
swaync run a genuine genericity proof rather than a restatement of the waybar run. Guard
the live half: no `systemctl` on PATH or no user bus is a `[SKIP]`, never a silent pass.

`host-package` shells `pacman -Q <S>` and treats a successful query as a hit. Guard it
the same way — no `pacman` on PATH is a `[SKIP]`.

Add `--all`: iterate the registry in declared order. For a `pending` surface emit exactly
**one** `[REPORT]` summary line carrying the surface name, its retiring requirement ID,
and its blocking-domain and report-domain totals — not the per-hit lists, which would
flood `theme-doctor` with thousands of lines on every run. For a `retired` surface run
the full blocking tier and emit its per-class `[PASS]`/`[FAIL]` lines. Exit `1` if any
`[FAIL]` was emitted across the whole registry.

Add `--self-test` following `motion-lint`'s loop shape: run this script against each
committed fixture tree in isolation via `--root`, using surface `retirement-fixture`, and
assert the expected verdict per fixture, printing `[PASS] self-test: <fixture> -> <verdict> as expected`
or the corresponding `[FAIL]` line. Create five fixture trees under
`hypr/.config/hypr/scripts/tests/retirement-fixtures/`, each a miniature repo root
containing only the files its class needs:

- `compliant-clean-surface/` — the class-bearing files present, containing zero
  references to `retirement-fixture`. Expected: blocking tier all `[PASS]`, exit `0`.
- `poisoned-stray-layer-rule/` — identical, except `hypr/.config/hypr/config/windowrules.lua`
  carries an `hl.layer_rule` naming the surface. Expected: `[FAIL]` on
  `layer-window-rules`, exit `1`.
- `poisoned-stray-contract-entry/` — identical, except
  `theme-engine/.config/theme-engine/contract.json` still lists a surface-named `.css`
  entry. Expected: `[FAIL]` on `contract-json`, exit `1`. This reproduces WINDOWS #1's
  exact failure class, which is D-18-35's cited standing precedent.
- `poisoned-stray-cross-script-ref/` — identical, except an unrelated script under
  `hypr/.config/hypr/scripts/` references the surface. Expected: `[FAIL]` on
  `cross-package-refs`, exit `1`. This is D-18-36's cross-script class.
- `poisoned-planning-only/` — identical, except the ONLY reference to the surface lives
  in a file under `.planning/`. Expected: a `[REPORT]` line on `planning-archive`, zero
  `[FAIL]` lines, exit `0`. This is the fixture that makes D-18-37's tier split
  mechanically enforced rather than merely documented, and it is the single most
  important fixture in the set — without it, nothing stops a later edit from quietly
  pulling `.planning/` into the blocking tier and making zero-hits unreachable.

Do not add any exemption, suppression, allowlist-of-permitted-hits or `--ignore`
mechanism. This plan's third prohibition forbids it: `motion-lint`'s own header records
that an invisible carve-out turns a gate green over non-compliant surfaces, and a
retirement checklist with a suppression list is a checklist that cannot fail.
  </action>

  <acceptance_criteria>
    - `bash hypr/.config/hypr/scripts/retirement-check --self-test; echo $?` prints `0`, and its stdout contains five lines beginning `[PASS] self-test:` and zero lines beginning `[FAIL] self-test:`.
    - `bash hypr/.config/hypr/scripts/retirement-check retirement-fixture --root hypr/.config/hypr/scripts/tests/retirement-fixtures/poisoned-stray-contract-entry; echo $?` prints `1`, and its stdout contains a line beginning `[FAIL] retirement-fixture/contract-json:`.
    - `bash hypr/.config/hypr/scripts/retirement-check retirement-fixture --root hypr/.config/hypr/scripts/tests/retirement-fixtures/poisoned-planning-only; echo $?` prints `0`, and the count of lines beginning `[FAIL]` in that output is `0` while at least one line begins `[REPORT] retirement-fixture/planning-archive:`.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar` exits `0` and its stdout contains one line per class for all sixteen class names in the registry table.
    - `bash hypr/.config/hypr/scripts/retirement-check waybar` reports `2` hits under `layer-window-rules`, `2` under `autostart`, `9` under `contract-json`, and a `planning-archive` count above `3000`.
    - `bash hypr/.config/hypr/scripts/retirement-check swaync` exits `0` and its `systemd-units` line reports at least one in-repo hit, naming `swaync/.config/systemd/user/swaync.service.d/override.conf`.
    - `bash hypr/.config/hypr/scripts/retirement-check --all; echo $?` prints `0`, emits exactly seven `[REPORT]` summary lines (one per `pending` surface), and emits `[PASS]` lines for `retirement-fixture`.
    - `bash hypr/.config/hypr/scripts/retirement-check --all` completes in under 60 seconds on this host.
    - Genericity: `bash hypr/.config/hypr/scripts/retirement-check swayosd` and `... wleave` and `... ags` each exit `0` and produce a full sixteen-class report — no surface produces an empty or malformed report.
  </acceptance_criteria>

  <verify>
    <automated>bash hypr/.config/hypr/scripts/retirement-check --self-test && bash hypr/.config/hypr/scripts/retirement-check --all && for s in waybar swaync swayosd wleave ags; do bash hypr/.config/hypr/scripts/retirement-check "$s" >/dev/null || exit 1; done && echo EXPANSION-OK</automated>
  </verify>

  <done>
All sixteen reference classes are implemented and produce a correct report for every
allowlisted surface, not only waybar. `--all` is safe to fold (zero `[FAIL]` while every
real surface is `pending`). `--self-test` passes with five committed fixtures, three of
which drive the blocking tier to a genuine `[FAIL]` and one of which proves a
`.planning/`-only reference stays in the report tier. No suppression mechanism exists.
Committed.
  </done>

  <reversibility rating="reversible">
Additive scan functions plus new fixture files; reverting to the Task 1 tracer restores
the prior state.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: Fold the blocking tier into `theme-doctor` and capture the waybar pre-deletion baseline</name>

  <precondition>
`~/.config/theme-engine/theme-doctor` is a copied file, not a symlink into this repo
(verified 2026-08-10), so an edit to the repo copy is not live until stow re-runs.
Every acceptance criterion below therefore invokes the **repo** copy explicitly at
`theme-engine/.config/theme-engine/theme-doctor`, which resolves its own `lib/` via
`SCRIPT_DIR` and works unstowed. `~/.config/hypr/scripts` IS a symlink into the repo, so
`retirement-check` is already live at the path the fold uses.
  </precondition>

  <read_first>
    - `theme-engine/.config/theme-engine/theme-doctor` lines 655-755 — all three existing fold blocks (`waybar-design-lint` at 660-679, `motion-lint` at 681-703, `hypr-equivalence-check` at 705-738) and the trailing summary/exit at 752-755. Read 18-03's newly-landed hex-lint fold in the same region and place the new fold consistently with whatever shape it took, rather than assuming its text.
    - `theme-engine/.config/theme-engine/theme-doctor` lines 1-40 — `check()`, the `PASS`/`FAIL` counters, and the CLEAN-02 `git status --porcelain` check at 744-750 (see the acceptance-criteria note about it).
    - `hypr/.config/hypr/scripts/retirement-check` — the finished script, specifically its `--all` output shape, so the fold's `case` arms match the labels it actually emits.
  </read_first>

  <files>theme-engine/.config/theme-engine/theme-doctor, .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md</files>

  <action>
Insert the `retirement-check` fold into `theme-doctor` immediately after the
`hypr-equivalence-check` fold and 18-03's hex-lint fold, before the CLEAN-02 git-clean
check. Open it with the section header comment
`# ── retirement-check fold (18-06, RETIRE-01/D-18-35) ───`, guarded on
`RETIREMENT_CHECK="$HOME/.config/hypr/scripts/retirement-check"` being executable, with
an unstowed-package guarded `[SKIP]` degradation exactly as the `waybar-design-lint` and
`motion-lint` folds do — never a hard failure.

Consume `"$RETIREMENT_CHECK" --all` through a `while IFS= read -r` loop with a three-arm
`case`: fold `[PASS]` lines via `check "retirement-check: ..." "0"`, fold `[FAIL]` lines
via `check "retirement-check: ..." "1"`, and pass every other line through unfolded with
a bare indented `echo`, matching the `motion-lint` fold's `*)` arm at theme-doctor:698.
That third arm is what carries `[REPORT]` and `[SKIP]` lines into the output without
counting them, which is D-18-37's tier split expressed at the fold boundary rather than
only inside the script.

Write a fold comment recording why this fold exists in the words the sibling folds use:
the same "green gates, broken surface" gap, applied to retirement. Name WINDOWS #1
explicitly — an orphaned `eww.scss` contract entry blocked `theme-doctor` for a full
milestone because no gate enforced this class — and state that while every real surface
is `pending` the fold contributes only `[REPORT]` lines plus the `retirement-fixture`
row's `[PASS]` lines, so the tally is unchanged until 18-20 flips waybar to `retired`.

Then capture the pre-deletion baseline. Run `retirement-check waybar` and write its
complete output, verbatim and unedited, into
`.planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md`
under a short front matter recording the capture date, the commit SHA the capture was
taken at, the exact command invocation, and one sentence stating that 18-20 re-runs the
identical command after the deletion and requires every blocking-domain class to reach
zero. Include the per-class totals as a summary table above the verbatim block so a
reader does not have to count lines.

This baseline is the artifact the Ordering Constraints call "RETIRE-01 twice" — built and
run here in wave 2 while waybar is fully alive and readable, and compared against in wave
10 after it is gone.
  </action>

  <acceptance_criteria>
    - `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c 'retirement-check'` returns a count of `1` or greater — the fold is wired and producing lines.
    - `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c '\[FAIL\] retirement-check'` returns `0` — no blocking-tier failure while every real surface is `pending`.
    - `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c '\[PASS\] retirement-check: retirement-fixture'` returns a count of `1` or greater — the blocking tier is armed and green, not dormant.
    - `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c 'REPORT.*waybar'` returns a count of `1` or greater — the report tier passes through the fold unfolded.
    - **Do not gate on `theme-doctor`'s overall exit code for this task.** Its CLEAN-02 check at line 744-750 asserts `git status --porcelain` is empty and therefore fails on any dirty working tree during execution. Assert on the `retirement-check` fold lines above, and separately confirm the tally is unchanged by comparing the `Summary: N passed, M failed` line against a run with `RETIREMENT_CHECK` pointed at a nonexistent path: `M` must be identical in both runs.
    - Guarded skip: `RETIREMENT_CHECK=/nonexistent bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c 'SKIP.*retirement-check'` returns a count of `1` or greater, and the run's `failed` count is unchanged from the normal run.
    - `test -f .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md` exits `0`.
    - The baseline file contains a line for every one of the sixteen reference class names and records a `planning-archive` count above `3000`.
    - `diff <(bash hypr/.config/hypr/scripts/retirement-check waybar) <(sed -n '/^```$/,/^```$/p' .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md | sed '1d;$d')` exits `0` — the committed baseline is byte-identical to a fresh run, which is the property 18-20's diff relies on.
  </acceptance_criteria>

  <verify>
    <automated>bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -q '\[PASS\] retirement-check: retirement-fixture' && [ "$(bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -c '\[FAIL\] retirement-check')" -eq 0 ] && test -f .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md && echo FOLD-OK</automated>
  </verify>

  <done>
`theme-doctor` runs `retirement-check --all` on every invocation and folds its blocking
tier into the pass/fail tally, with `[REPORT]` and `[SKIP]` lines passing through
unfolded and the tally unchanged from before the fold. The waybar pre-deletion baseline
is committed and is byte-identical to a fresh run of the same command.
  </done>

  <reversibility rating="costly">
Once `theme-doctor` folds this gate, every later phase's green run depends on
`retirement-check` existing and behaving; RETIRE-03..06 in Phases 19-21 consume it
unmodified. Removing the fold later means re-opening the WINDOWS #1 class D-18-35 exists
to close.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv -> `retirement-check` | `<surface-name>` and `--root <dir>` are attacker-influenceable in any context where this script is invoked with a non-literal argument (a future wrapper, a CI loop over a generated list). Both flow into filesystem paths and grep patterns |
| `retirement-check` -> live host commands | `systemctl --user list-unit-files`, `pacman -Q <S>`, and directory listings of `/usr/share/dbus-1/services/` and `/etc/xdg/autostart/` cross out of the repo into system state |
| `retirement-check` -> `theme-doctor` stdout | Matched file paths and lines are printed into `theme-doctor`'s output, which is read by humans and captured into planning artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06-01 | Tampering | `retirement-check` argv -> command construction | high | mitigate | Two-stage guard before any path or pattern is built: syntactic `^[a-z][a-z0-9-]*$` then registry-membership allowlist, both rejecting to stderr with `exit 2`. Copies `waybar-visibility.sh:326-332`'s reject-before-use case statement (T-08-05 precedent) and `quickshell-doctor:219-221`'s `_qsd_valid_token`. Every scan uses the validated token only, always quoted (Task 1) |
| T-18-06-02 | Tampering | `retirement-check` argv -> grep pattern interpolation | medium | mitigate | The `^[a-z][a-z0-9-]*$` guard admits no regex metacharacter, so an injected `.`/`*`/`[` cannot widen a scan into false hits or narrow it into a false clean. The pattern is built once from the validated token and reused across all sixteen classes (Task 1) |
| T-18-06-03 | Tampering | `retirement-check` argv -> `own-tree` path construction | high | mitigate | The same syntactic guard rejects `/` and `.`, so no `../` traversal sequence survives validation to reach a constructed path. `--root <dir>` is separately confined to `--self-test` fixture use and resolves through the caller's own shell, not through the surface token (Task 1) |
| T-18-06-04 | Information disclosure | stdout — matched paths and lines | low | accept | All scanned content is already repo-visible or user-readable config. RESEARCH.md's Runtime State Inventory records `Secrets/env vars: None` for this stack, so no credential can appear in a match. Accepted rather than mitigated because redacting matched lines would destroy the tool's only useful output |
| T-18-06-05 | Denial of service | deny-by-default recursive scans | low | mitigate | Every scan excludes `.git/`, `node_modules/`, `wallpapers/` and binary files; the `planning-archive` and `repo-prose` classes are count-only and never build a per-hit list. Bounded by the under-60-second acceptance criterion on `--all` (Task 2) |
| T-18-06-06 | Repudiation | committed baseline artifact | low | mitigate | The baseline records the capture date, the exact command invocation, and the commit SHA it was taken at, and is asserted byte-identical to a fresh run — so a later reader can reproduce it rather than trust it (Task 3) |
| T-18-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. RESEARCH.md confirms zero new dependencies for this phase's tooling work; `retirement-check` is bash using only coreutils, `grep`, `systemctl` and `pacman`, all already present. The package-legitimacy gate has no input, so no `[ASSUMED]`/`[SUS]` human checkpoint is required |
</threat_model>

<verification>
Run in order from the repo root:

1. `bash hypr/.config/hypr/scripts/retirement-check --list` — eight registry rows print, all real surfaces `pending`.
2. `bash hypr/.config/hypr/scripts/retirement-check --self-test` — exits `0`, five `[PASS] self-test:` lines, zero `[FAIL] self-test:` lines. This is the proof the blocking tier can fail.
3. `bash hypr/.config/hypr/scripts/retirement-check waybar` — exits `0`, sixteen classes reported, counts match the measured ground truth table (2 layer rules, 2 autostart, 9 contract, 3000+ planning).
4. `bash hypr/.config/hypr/scripts/retirement-check swaync` — exits `0`, sixteen classes reported, `systemd-units` names the in-repo drop-in. **This is the D-18-34 genericity gate**: a script that only worked for waybar would produce an empty or malformed report here even if step 3 passed.
5. `for s in swayosd wleave ags wlogout eww; do bash hypr/.config/hypr/scripts/retirement-check "$s" >/dev/null || echo "BROKE: $s"; done` — no output.
6. Argument rejection: each of `notarealsurface`, `../../etc`, `way.ar`, and no argument exits `2`.
7. `bash hypr/.config/hypr/scripts/retirement-check --all` — exits `0`, seven `[REPORT]` summary lines, `[PASS]` lines for `retirement-fixture`, under 60 seconds.
8. `bash theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep 'retirement-check'` — fold lines present, zero `[FAIL] retirement-check`, at least one `[PASS] retirement-check: retirement-fixture`.
9. Tally-neutrality: the `Summary: N passed, M failed` line's `M` is identical between a normal run and `RETIREMENT_CHECK=/nonexistent bash theme-engine/.config/theme-engine/theme-doctor`.
10. `diff <(bash hypr/.config/hypr/scripts/retirement-check waybar) <(sed -n '/^```$/,/^```$/p' .planning/phases/18-qml-bar-retirement-machinery/18-RETIREMENT-BASELINE-waybar.md | sed '1d;$d')` — exits `0`.

Note on step 8: do not use `theme-doctor`'s overall exit code as a gate. Its CLEAN-02
check requires an empty `git status --porcelain` and will fail on any dirty tree mid-
execution, which is unrelated to this plan.
</verification>

<success_criteria>
- `hypr/.config/hypr/scripts/retirement-check` exists, is executable, and is generic: it produces a complete sixteen-class report for all seven real registry surfaces, not only waybar (D-18-34).
- `<surface-name>` is rejected by a syntactic guard and a registry allowlist before any path is constructed or any grep pattern interpolated; invalid input exits `2`, distinct from a dirty-surface `1` (T-18-06-01/02/03).
- Two tiers exist in the one script (D-18-37): fourteen blocking-domain classes emit `[PASS]`/`[FAIL]` for a `retired` surface and `[REPORT]` for a `pending` one; the two report-domain classes emit `[REPORT]` always and never influence the exit code.
- Coverage spans every class RETIRE-01's requirement text names — window/layer rules, autostart, keybinds, `contract.json`, matugen templates, doctor registries and fixtures, `systemctl --user list-unit-files`, D-Bus activation files, XDG autostart, and `install.sh`/`stow.sh` lists — plus all four D-18-36 classes.
- `--self-test` passes against five committed fixtures; three drive the blocking tier to a real `[FAIL]`, and `poisoned-planning-only` proves a `.planning/`-only reference stays in the report tier.
- The blocking tier is folded into `theme-doctor` (D-18-35) and `theme-doctor`'s failure count is unchanged by the fold while every real surface is `pending`.
- No exemption or suppression mechanism exists anywhere in the script.
- The waybar pre-deletion baseline is committed and byte-identical to a fresh run, ready for 18-20's post-deletion comparison.
</success_criteria>

<output>
Create `.planning/phases/18-qml-bar-retirement-machinery/18-06-SUMMARY.md` when done.

The summary must record, for 18-20's benefit: the final registry row for `waybar`
(status `pending`, own-tree globs as shipped), the exact command 18-20 must re-run, and
the blocking-domain class totals from the baseline that must all reach zero.
</output>
