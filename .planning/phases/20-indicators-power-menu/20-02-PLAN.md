---
phase: 20-indicators-power-menu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/WINDOWS.md
  - .planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md
autonomous: true
requirements: [LEDGER-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The triage works from the LIVE `.planning/WINDOWS.md` frontmatter `open_count` (51 as of 2026-08-13), not from LEDGER-05's or ROADMAP SC-6's stale figure of 16 — D-20-39 records the requirement text itself as wrong."
    - "Every WINDOWS row touching swayosd, wleave, wlogout, eww or one of this phase's own surfaces receives an INDIVIDUAL verdict with its own stated reason (D-20-40). At minimum rows 3, 4, 5, 6, 10 and 74 fall in this set."
    - "Row 74 (Toast.qml, phase 19 — the DND toast's show()/timer/self-dismiss path never exercised interactively) is individually triaged, because this phase reuses that exact mechanism and its own GATE-02 Gate A hover-pause and auto-hide checks can serve as the closure evidence."
    - "The remainder is re-deferred as ONE batch entry with a single stated reason and a NAMED OWNING PHASE — not a second silent backlog (D-20-40)."
    - "LEDGER-05 / adjacency: a row that both names one of this phase's surfaces AND falls in the numeric remainder receives the INDIVIDUAL verdict, not the batch — individual triage wins on tie, and the row appears in exactly one of the two sets, never both."
    - "LEDGER-05 / empty: if the live `open_count` reads 0 at triage time, the triage records that fact and writes NO batch entry — it never fabricates a batch re-defer for an empty remainder."
    - "LEDGER-05 / ordering: rows are triaged in ascending `id` order and the batch entry names its remainder as an explicit id list, so re-running the triage against the same ledger produces a byte-identical verdict set."
    - "After the triage, `.planning/WINDOWS.md`'s frontmatter counters are internally consistent with its own table rows — the frontmatter is regenerated from the table, never hand-adjusted."
  artifacts:
    - .planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md
    - .planning/WINDOWS.md
  key_links:
    - "20-LEDGER-05-TRIAGE.md's individual-verdict set ↔ plans 20-04/20-05/20-06/20-07's own acceptance criteria — a row closed on this phase's evidence must name the plan and criterion that produces that evidence."
    - "The batch entry's named owning phase ↔ ROADMAP.md — the named phase must be a real phase that exists in the roadmap, not a placeholder."
  prohibitions:
    - "MUST NOT batch re-defer any WINDOWS.md row without naming a real owning phase for it — an unowned deferral is the second silent backlog D-20-40 exists to prevent."
    - "MUST NOT mark a row `fixed` on the strength of a plan that has not yet executed — a row closed on this phase's own evidence stays open until the plan producing that evidence has run."
---

<objective>
Triage `.planning/WINDOWS.md` at PHASE START (D-20-41, and the roadmap's own instruction),
against the live ledger rather than the requirement's stale count.

Purpose: LEDGER-05's bar is "none left silently open." D-20-40 sizes how that bar is met without
producing 51 hand-written verdicts inside a phase already carrying two surfaces and three
deletions: the rows this phase actually touches get individual verdicts, and the remainder
becomes one honest, owned batch entry. Doing this at phase start also lets plans 20-04..20-07
know which rows their own acceptance criteria are expected to close.

Output: `20-LEDGER-05-TRIAGE.md` and an updated `.planning/WINDOWS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the live ledger and write the individual verdicts</name>
  <files>.planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md</files>
  <read_first>
    - `.planning/WINDOWS.md` — the full file. Its frontmatter (`open_count: 51`, `fixed_count: 24`, `total_count: 75`, `last_updated: 2026-08-13T12:43:20.122Z`) is authoritative; the `16` in LEDGER-05's requirement text and ROADMAP SC-6 is stale (D-20-39).
    - `.planning/phases/20-indicators-power-menu/20-CONTEXT.md` § "LEDGER-05 — the WINDOWS.md triage" (D-20-39, D-20-40, D-20-41) and § "Motion and layer rules" (D-20-36, which explicitly leaves rows 3 and 4 open and in scope for this triage).
    - `.planning/phases/20-indicators-power-menu/20-RESEARCH.md` § "Priority Research Findings" item 7 — the already-verified row-by-row read of rows 3, 4, 5, 6, 10 and the row-74 find.
    - `.planning/ROADMAP.md` — the phase list, so the batch entry's named owning phase is a phase that actually exists.
  </read_first>
  <action>
Re-read `.planning/WINDOWS.md` fresh (the ledger may have moved since 2026-08-13) and record the
live `open_count` at the top of the triage document as the working figure, with an explicit note
that LEDGER-05's own "16" and ROADMAP SC-6's "16" are stale per D-20-39.

Create `.planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md` with two sections.

**§ Individual verdicts (D-20-40 in-scope set).** Select every row whose `status` is `open` AND
whose `file` or `description` names `swayosd`, `wleave`, `wlogout`, `eww`, or one of this phase's
own surfaces (`Toast.qml`, the OSD, the power menu, `windowrules.lua`'s layer rules,
`ClockActionsCapsule.qml`'s power cell). Select by ascending `id`. RESEARCH.md already verified
this set contains at least rows 3, 4, 5, 6, 10 and 74 — treat that as a floor to confirm, not a
ceiling to stop at; re-derive the set from the live file.

For each selected row write: `id | phase | file | verdict | reason | evidence-or-owner`. The
verdict is one of `fixed`, `waived`, or `re-deferred`. Concrete guidance for the six already
identified:

- **Row 3** (phase 09, `wleave/.config/wleave/style.css`) — the D-10 entrance-vs-hover
  interaction, never exercised live. The file is deleted in plan 20-10, and D-20-36 deliberately
  did NOT serialise grid entrance against input readiness, so the equivalent interaction exists
  on the new power grid. Verdict: `re-deferred` onto the new surface with the new surface named,
  OR `waived` on the ground that the artefact it describes ceases to exist — pick one and say
  which, and if re-deferring, name plan 20-08's Gate B as where the equivalent is checked.
- **Row 4** (phase 09, `09-03-SUMMARY.md`) — hover evidence captured via keyboard focus
  (`wtype` Tab), not literal mouse hover. Same disposition family as row 3.
- **Row 5** (phase 09, `wleave/.config/wleave/layout.json`) — icon glyph size was SVG
  natural/shrink-fit, not the UI-SPEC's literal token. The file is deleted in 20-10 and the new
  tiles pin `Design.sessionTileIconSize` (32) explicitly, so this is a strong `fixed`-by-
  construction candidate — but it may only be marked `fixed` after plan 20-06 has actually run.
  If 20-06 has not run, record the verdict as `re-deferred` with plan 20-06 named as the owner.
- **Row 6** (phase 09, `hypr/.config/hypr/scripts/wleave.sh`) — the wrapper's fault-injection
  gap. `wleave.sh` is deleted in plan 20-10 and D-20-23 deletes the availability probe outright
  because an in-process QML surface has no "missing" state. Verdict: the row's subject ceases to
  exist; record `waived` with that as the reason, or `fixed` after 20-10 runs — pick one and say
  which.
- **Row 10** (phase 13, `windowrules.lua`) — the D-06 boundary correction, layer-surface exit
  animations being client-owned. This phase adds two new namespaces to that same file. Give it
  its own verdict; do not fold it into the batch.
- **Row 74** (phase 19, `Toast.qml`) — the DND toast's `show()`/timer/self-dismiss path was
  never exercised interactively (DND was flipped by editing the state file). This phase reuses
  that exact mechanism, and GATE-02 Gate A criteria 3 and 4 (auto-hide, hover-pause) exercise it
  for real. Verdict: `re-deferred` with plan 20-08's Gate A named as the closing evidence.

**§ Batch re-defer (the remainder).** One entry covering every other `open` row, listing the
remainder as an explicit ascending id list (so the set is reproducible), with ONE stated reason
and ONE named owning phase that exists in `.planning/ROADMAP.md`. Do not write per-row prose for
the batch. If the intersection rule bites — a row both names a phase-20 surface and would fall
in the remainder — it goes in the individual set, never both; state that tie-break rule in the
document.

If the live `open_count` reads 0, record that fact and write no batch entry at all.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md &amp;&amp; grep -q 'D-20-39' .planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md &amp;&amp; for r in 3 4 5 6 10 74; do grep -qE "^\| *$r *\|" .planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md || { echo "MISSING individual verdict for row $r"; exit 1; }; done &amp;&amp; grep -qE '^## +(§ *)?Batch' .planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-LEDGER-05-TRIAGE.md` records the live `open_count` read from `.planning/WINDOWS.md` frontmatter and states that LEDGER-05's/SC-6's `16` is stale, citing `D-20-39`.
    - The individual-verdict table contains a row for each of ids 3, 4, 5, 6, 10 and 74, each with a non-empty verdict from {`fixed`, `waived`, `re-deferred`} and a non-empty reason.
    - Every `re-deferred` individual verdict names either a plan in this phase or a phase in `.planning/ROADMAP.md` as its owner.
    - The batch section lists its remainder as an explicit ascending id list, carries exactly one reason, and names one owning phase that appears as a `### Phase` heading in `.planning/ROADMAP.md`.
    - No id appears in both the individual-verdict table and the batch id list.
  </acceptance_criteria>
  <done>Every open ledger row is accounted for exactly once — individually verdicted or inside one named, owned batch — with the tie-break rule stated in the document.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the verdicts to WINDOWS.md and reconcile its frontmatter</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md` — the frontmatter block and the table header, so the applied edits keep the existing column order and the `gsd-tools windows` verb conventions (`waive <id> "<reason>"`, `fixed <id>`) documented at the top of the file.
    - `.planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md` — the verdicts written in Task 1, which this task transcribes.
  </read_first>
  <action>
Apply each Task 1 verdict to `.planning/WINDOWS.md`.

Use the `gsd-tools windows` verbs where they apply (`waive <id> "<reason>"` for a waiver, and
`fixed <id>` only for a row whose closing evidence already exists) so the tooling stays the
writer of record for those two transitions. For a `re-deferred` verdict there is no verb: edit
the row's `reason` column in place to carry the deferral reason and the named owner, leaving
`status` as `open`. A re-deferred row is still open — that is the point; what changes is that it
is no longer SILENTLY open.

Add the batch re-defer as a single new block immediately beneath the table, headed
`## Batch re-defer (LEDGER-05, D-20-40)`, carrying: the explicit ascending id list, the one
stated reason, the named owning phase, and the date. Do not add 45 individual `reason` strings.

Then reconcile the frontmatter counters (`open_count`, `waived_count`, `fixed_count`,
`total_count`, `last_updated`) so they agree with the table's own rows. Derive them by counting
the table, never by adjusting the previous numbers arithmetically — a hand-adjusted counter that
drifts from its own table is the failure mode this reconciliation exists to prevent.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re,sys
p='.planning/WINDOWS.md'
t=open(p).read()
fm=re.search(r'^---\n(.*?)\n---\n',t,re.S).group(1)
def num(k):
    m=re.search(rf'^{k}:\s*(\d+)',fm,re.M); return int(m.group(1)) if m else None
rows=[l for l in t.splitlines() if re.match(r'^\|\s*\d+\s*\|',l)]
st=[l.split('|')[7].strip() for l in rows]
counts={'open':st.count('open'),'waived':st.count('waived'),'fixed':st.count('fixed')}
exp={'open_count':counts['open'],'waived_count':counts['waived'],'fixed_count':counts['fixed'],'total_count':len(rows)}
bad=[k for k,v in exp.items() if num(k)!=v]
if bad: print('FRONTMATTER MISMATCH',{k:(num(k),exp[k]) for k in bad}); sys.exit(1)
if '## Batch re-defer (LEDGER-05, D-20-40)' not in t and counts['open']>0:
    print('MISSING batch re-defer block'); sys.exit(1)
silent=[l.split('|')[1].strip() for l in rows if l.split('|')[7].strip()=='open' and not l.split('|')[8].strip()]
print('rows',len(rows),'open',counts['open'],'reasonless-open',silent)
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` frontmatter `open_count`, `waived_count`, `fixed_count` and `total_count` each equal the corresponding count derived from the table's own rows (the verify script exits 0).
    - A `## Batch re-defer (LEDGER-05, D-20-40)` block exists beneath the table containing an explicit ascending id list, one reason, one named owning phase and a date — unless the live open count was 0, in which case the triage document records that and no block is written.
    - Every row whose id appears in Task 1's individual-verdict table has either changed `status` or gained a non-empty `reason` — no individually-verdicted row is left byte-identical to its pre-triage state.
    - Rows in the batch id list are collectively covered by the batch block's single reason and named owner.
  </acceptance_criteria>
  <done>`.planning/WINDOWS.md` carries the applied verdicts, a single owned batch entry for the remainder, and frontmatter counters that agree with its own table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger text → planning tooling | `.planning/WINDOWS.md` row descriptions are free text authored across many phases; they are transcribed, never executed or interpolated into a command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02-01 | Repudiation | WINDOWS.md batch re-defer | medium | mitigate | Every batch deferral carries an explicit ascending id list, one stated reason and one named owning phase — a deferral with no owner is unrepudiable debt, which the plan's own prohibition forbids. |
| T-20-02-02 | Tampering | frontmatter counters | low | mitigate | Counters are DERIVED by counting table rows (the verify script re-derives and compares), never hand-adjusted arithmetically. |
| T-20-02-SC | Tampering | package-manager installs | low | accept | No package installs in this plan — documentation-only changes. |
</threat_model>

<verification>
- `.planning/WINDOWS.md` frontmatter counters equal the counts derived from its table (Task 2's verify script).
- Every id in Task 1's individual-verdict set appears exactly once in the ledger with a verdict applied.
- No id appears in both the individual set and the batch id list.
- `git diff --name-only` for this plan touches only `.planning/WINDOWS.md` and the new triage document.
</verification>

<success_criteria>
- The triage was performed at phase start against the live ledger, not the requirement's stale count.
- Rows 3, 4, 5, 6, 10 and 74 each carry an individual, reasoned verdict.
- The remainder is one batch entry with a single reason and a named owning phase that exists in the roadmap.
- No open row is left silently open — every open row carries either an individual reason or membership in the named batch.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files:**
- `.planning/phases/20-indicators-power-menu/20-LEDGER-05-TRIAGE.md`

**New sections in existing files:**
- `.planning/WINDOWS.md` → `## Batch re-defer (LEDGER-05, D-20-40)` block

**Deleted symbols:** none.
</artifacts_produced>

<output>
Create `.planning/phases/20-indicators-power-menu/20-02-SUMMARY.md` when done.
</output>
