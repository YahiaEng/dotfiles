---
phase: 20-indicators-power-menu
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quickshell/.config/quickshell/modules/dashboard/Design.qml
  - quickshell/.config/quickshell/modules/bar/BarRoles.qml
  - hypr/.config/hypr/config/windowrules.lua
autonomous: true
requirements: [QOSD-01, QOSD-04, QPOWER-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every numeric and colour token both halves of this phase consume exists before either half starts building, so plans 20-04..20-07 never reopen `Design.qml`, `BarRoles.qml` or `windowrules.lua` — this is what keeps the OSD half and the power half genuinely parallel (D-20-42)."
    - "`Design.osdWidth` is 380 and is strictly narrower than the existing `Design.notifSurfaceWidth` (430), so the OSD reads as a lighter surface than the popup/centre family it shares chrome with (D-20-10)."
    - "`Design.osdHideDelayMs` is a NEW token distinct from `Design.notifToastDurationMs` (2000) — the OSD's dwell is tunable independently of the DND toast's (D-20-06)."
    - "`BarRoles.onWarn` completes the existing `warn` pair rather than opening a new colour family, mirroring the `danger`/`onDanger` pairing already on that file."
    - "`quickshell-osd` carries its OWN `ignore_alpha` override row, not the family's 0.5 floor — Toast.qml's reused fill is `BarRoles.notifSurface` at alpha 0.38, and 0.38 < 0.5, so the family floor alone silently kills blur (D-20-34, RESEARCH Pitfall 1)."
    - "Both new namespace rows are declared AFTER the `^quickshell-.*` family regex pair, in the file's last-declared block, matching the three `quickshell-notif-*` worked examples — a namespace rule contradicting the family regex loses if declared before it (D-20-33)."
    - "The plan's own acceptance evidence does NOT rely on `hyprctl reload` — that verb silently drops layer-rule edits on this build; only `hyprctl eval` or a full restart exercises them."
    - "Every new numeric token sits on the 4px grid, with no exemption introduced this phase."
  artifacts:
    - quickshell/.config/quickshell/modules/dashboard/Design.qml
    - quickshell/.config/quickshell/modules/bar/BarRoles.qml
    - hypr/.config/hypr/config/windowrules.lua
  key_links:
    - "`Design.osdWidth`/`osdHideDelayMs`/`osdRecencyWindowMs` ↔ plans 20-04 and 20-05 — the OSD half reads these by name and declares none of its own."
    - "`Design.sessionDialogWidth`/`sessionTileWidth`/`sessionTileHeight`/`sessionTileRadius`/`sessionTileIconSize`/`sessionScrimOpacity` + `BarRoles.onWarn` ↔ plans 20-06 and 20-07 — the power half reads these by name and declares none of its own."
    - "`windowrules.lua`'s `quickshell-osd` `ignore_alpha` row ↔ `Toast.qml`'s `BarRoles.notifSurface` fill alpha (0.38) — if either moves, the other must move in the same commit (D-20-34)."
---

<objective>
Declare the complete token, colour-role and layer-namespace surface both halves of this phase
consume, in one commit, before either half starts building.

Purpose: the OSD half and the power-menu half share no backend (D-20-42) but they DO share three
files — `Design.qml`, `BarRoles.qml` and `windowrules.lua`. Declaring that surface up front is
the only thing that keeps the two halves file-disjoint and therefore genuinely parallel. This is
the same move Phase 19 made in `19-01`, and it is a deliberate, named exception to this phase's
tracer-first shape: it is a shared-contention-removal plan, not a horizontal foundation layer.
Neither half's tracer is blocked on anything here beyond a name existing.

Output: nine new `Design.qml` tokens, one new `BarRoles.qml` role, and the `quickshell-osd` /
`quickshell-session` layer-rule rows including the `ignore_alpha` override the OSD's reused fill
alpha requires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-UI-SPEC.md
@.planning/phases/20-indicators-power-menu/20-RESEARCH.md
@.planning/phases/20-indicators-power-menu/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the nine new Design.qml tokens</name>
  <files>quickshell/.config/quickshell/modules/dashboard/Design.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/dashboard/Design.qml` — the whole file's declaration convention (`readonly property int|real <camelCase>: <value>` with a provenance comment citing the UI-SPEC that introduced it). Anchor points already verified: `notifSurfaceWidth: 430` at line 417 and `notifToastDurationMs: 2000` at line 468; the new OSD tokens belong adjacent to those, in the notification/transient-surface region of the file, not appended at the end.
    - `.planning/phases/20-indicators-power-menu/20-UI-SPEC.md` § "New Tokens" — the two token tables with each token's value and its provenance sentence.
    - `quickshell/.config/quickshell/modules/dashboard/QuickToggles.qml` — `chipRadius: 16` at line 79, the existing chip-radius vocabulary `sessionTileRadius` deliberately matches.
  </read_first>
  <action>
Add exactly nine `readonly property` declarations to
`quickshell/.config/quickshell/modules/dashboard/Design.qml`, each with a provenance comment
citing `20-UI-SPEC.md` and the decision id, in the existing declaration convention.

**OSD group** (place adjacent to the existing `notifSurfaceWidth` / `notifToastDurationMs`
declarations, since these are their deliberate siblings):
- `osdWidth: 380` — int. D-20-10, locked. Provenance comment must state it is deliberately
  narrower than `notifSurfaceWidth` (430) so the OSD reads as a lighter surface than the
  popup/centre family it shares chrome with.
- `osdHideDelayMs: 1200` — int. D-20-06, Claude's Discretion resolved by UI-SPEC. Provenance
  comment must state this is independent of `notifToastDurationMs` (2000) on purpose, and why
  1200 rather than SwayOSD's own 1000: `interactive: true` means a drag must be able to complete
  inside the dwell window, and the hover-pause already covers genuinely slow interaction.
- `osdRecencyWindowMs: 1500` — int. D-20-08, Claude's Discretion resolved by UI-SPEC. Provenance
  comment must state this gates SLIDER-ROW MEMBERSHIP (a control earns a row only if its value
  changed within this window), not the dwell.

**Session group:**
- `sessionDialogWidth: 488` — int. D-20-21. The provenance comment must carry the derivation so
  the number is never re-derived wrong: `sessionTileWidth`(136) × 3 + `spacingMd`(16) × 2 gaps +
  `panelPadding`(24) × 2 sides = 408 + 32 + 48 = 488.
- `sessionTileWidth: 136` — int. D-20-21.
- `sessionTileHeight: 104` — int. D-20-21.
- `sessionTileRadius: 16` — int. D-20-21. Provenance comment must state it is deliberately NOT
  `popoutCornerRadius` (20, reserved for the outer frame) and that it matches
  `QuickToggles.qml`'s own `chipRadius: 16` so a session tile reads as a sibling of a
  quick-toggle chip.
- `sessionTileIconSize: 32` — int. D-20-21. Provenance comment must state this is the one
  deliberate departure from the shell's uniform `iconSizeMd` (24) icon discipline, and why.
- `sessionScrimOpacity: 0.55` — real. D-20-21/D-20-34. Provenance comment must state it matches
  `BarRoles.barSurface`'s own 0.55 and therefore already clears the `^quickshell-.*` family's
  `ignore_alpha: 0.5` floor with headroom.

Declare nothing else. In particular do NOT add `lineHeightTight` / `lineHeightNormal` here:
20-UI-SPEC.md's own Step-9.5 planner correction verified those are LOCAL `readonly property real`
declarations in `PanelDialog.qml:149-150` and `Dashboard.qml:228-229`, not `Design.qml` tokens,
and this phase takes the conservative, precedent-matching choice of declaring them locally in the
power dialog rather than promoting them (which would also require repointing three existing
declaration sites — larger than this phase's scope).
  </action>
  <verify>
    <automated>F=quickshell/.config/quickshell/modules/dashboard/Design.qml; for t in "osdWidth: 380" "osdHideDelayMs: 1200" "osdRecencyWindowMs: 1500" "sessionDialogWidth: 488" "sessionTileWidth: 136" "sessionTileHeight: 104" "sessionTileRadius: 16" "sessionTileIconSize: 32" "sessionScrimOpacity: 0.55"; do grep -qF "$t" "$F" || { echo "MISSING token: $t"; exit 1; }; done; grep -vE '^\s*//' "$F" | grep -c 'lineHeight' | grep -qx 0 || { echo "lineHeight must NOT be declared in Design.qml"; exit 1; }; hypr/.config/hypr/scripts/colour-lint >/dev/null; echo "colour-lint exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `Design.qml` contains all nine literal declarations: `osdWidth: 380`, `osdHideDelayMs: 1200`, `osdRecencyWindowMs: 1500`, `sessionDialogWidth: 488`, `sessionTileWidth: 136`, `sessionTileHeight: 104`, `sessionTileRadius: 16`, `sessionTileIconSize: 32`, `sessionScrimOpacity: 0.55`.
    - Each of the nine carries a provenance comment naming `20-UI-SPEC.md` and its decision id.
    - `grep -vE '^\s*//' Design.qml | grep -c lineHeight` returns 0 — no line-height token was promoted into this file.
    - `hypr/.config/hypr/scripts/colour-lint` exits 0.
    - Every new integer token is divisible by 4 (`380`, `488`, `136`, `104`, `16`, `32`) — the two millisecond tokens are durations, not spatial values, and are exempt from the grid by kind.
  </acceptance_criteria>
  <done>Both halves' numeric vocabulary exists in one file, with derivations recorded so no later plan re-derives a number.</done>
</task>

<task type="auto">
  <name>Task 2: Add BarRoles.onWarn — one role completion, not a new colour family</name>
  <files>quickshell/.config/quickshell/modules/bar/BarRoles.qml</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` — the existing role declarations, specifically `accent`/`onAccent` (lines 76-77), `warn: Colours.tertiary` (line 78) and `danger`/`onDanger` (lines 79-80). The `danger`/`onDanger` pairing is the exact shape `warn`/`onWarn` must mirror.
    - `quickshell/.config/quickshell/modules/Colours.qml` — confirm `onTertiary` exists as a `readonly property alias` in the on-roles sibling object (the file's own documented X/onX singleton-collision workaround), so `onWarn` binds to a real role rather than a literal.
    - `.planning/phases/20-indicators-power-menu/20-UI-SPEC.md` § "New Tokens" → "`BarRoles.qml` addition".
  </read_first>
  <action>
Add exactly one new role to `quickshell/.config/quickshell/modules/bar/BarRoles.qml`:
`onWarn`, composed as `Colours.onTertiary`, declared immediately adjacent to the existing
`warn: Colours.tertiary` so the pair reads as a pair.

Its provenance comment must record why it did not exist before: `warn` has existed since Phase
18.1 with no paired foreground colour because nothing needed one until QPOWER-03's warning
banner, and this completes the existing pair rather than opening a new colour family — the same
relationship `danger`/`onDanger` already has on this file.

Add nothing else. Every other colour this phase needs already exists and is consumed by name:
`notifSurface`/`notifSurfaceFg` (the OSD, inherited unchanged through `Toast.qml`),
`accent`/`onAccent` (OSD slider fill and handle, and the power menu's focus ring),
`capsuleTrack` (OSD slider track), `warn` (the warning banner's icon). The power dialog's own
background and tile colours read `Colours.*` DIRECTLY rather than through `BarRoles` — that is
the existing `PanelDialog`-family convention this phase inherits, not a new routing rule, and
D-19-43's route-through-`BarRoles` rule is scoped to the notification family only.
  </action>
  <verify>
    <automated>F=quickshell/.config/quickshell/modules/bar/BarRoles.qml; grep -qE '^\s*readonly property color onWarn:\s*Colours\.onTertiary' "$F" || { echo "MISSING or wrong onWarn declaration"; exit 1; }; N=$(git diff --numstat -- "$F" | awk '{print $1}'); echo "added-lines=$N"; hypr/.config/hypr/scripts/colour-lint >/dev/null; echo "colour-lint exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `BarRoles.qml` declares `readonly property color onWarn: Colours.onTertiary` — bound to a `Colours.*` role, never a literal or an `Qt.rgba(...)` construction.
    - The `onWarn` declaration is adjacent to the existing `warn: Colours.tertiary` declaration (within 3 lines of it, comments excluded).
    - No other role was added to `BarRoles.qml` in this task — the diff adds exactly one `readonly property color` line plus its provenance comment.
    - `hypr/.config/hypr/scripts/colour-lint` exits 0.
  </acceptance_criteria>
  <done>The `warn` role has a paired foreground colour, and the warning banner has a token to read instead of a literal.</done>
</task>

<task type="auto">
  <name>Task 3: Register quickshell-osd and quickshell-session layer rules, with the ignore_alpha override the OSD's fill requires</name>
  <files>hypr/.config/hypr/config/windowrules.lua</files>
  <read_first>
    - `hypr/.config/hypr/config/windowrules.lua` — read the whole layer-rule region. Anchors already verified: the `^quickshell-.*` family `blur = true` rule at line 396 and the family `ignore_alpha = 0.5` rule at line 445; `quickshell-overview`'s own late pair at lines 499-500; the three `quickshell-notif-*` `animation = "slide"` rows at lines 524-526; and the LAST-declared block at lines 560-565 which overrides `ignore_alpha` down to 0.2 for `quickshell-notif-popups` / `quickshell-notif-centre` / `quickshell-notif-toast`, with its own inline comment recording "Round 8 sets those alphas to 0.38 resting / 0.52 hover, both comfortably clear of 0.2". Also read the file's inline ordering finding: `hyprctl reload` silently drops layer-rule edits on this build, and a namespace rule that contradicts the family regex loses if declared BEFORE it.
    - `quickshell/.config/quickshell/modules/bar/BarRoles.qml` line 131 — `notifSurface` is `Qt.rgba(..., 0.38)`. This is the fill `Toast.qml` uses and the OSD instance reuses unchanged, and it is the number the new override row is sized against.
    - `.planning/phases/20-indicators-power-menu/20-PATTERNS.md` § "`hypr/.config/hypr/config/windowrules.lua`" — the worked precedent block and the concrete-numbers statement of the trap.
  </read_first>
  <action>
Append new layer-rule rows to `hypr/.config/hypr/config/windowrules.lua` in the file's
LAST-declared block — after the existing `ignore_alpha = 0.2` notification override block that
currently ends the file's layer-rule region. Placement is load-bearing: an exact-match namespace
rule declared before the `^quickshell-.*` family regex loses to it on this build.

Add for `quickshell-osd`:
- an `animation = "slide"` row, matching the three `quickshell-notif-*` precedent rows;
- a `blur = true` row, restated by name rather than left to the family regex, matching the
  existing override block's own idiom;
- an `ignore_alpha = 0.2` row.

The `ignore_alpha = 0.2` row is the point of this task and must carry an inline comment stating
the reasoning with the real numbers, because a future reader will otherwise "tidy" it away:
`quickshell-osd` is a BRAND-NEW namespace. It does NOT inherit `quickshell-notif-toast`'s 0.2
override just because it renders the same `Toast.qml` frame — a fresh namespace inherits only the
family's own `ignore_alpha = 0.5` floor. `Toast.qml`'s fill is `BarRoles.notifSurface` at alpha
0.38, tuned against the notification family's 0.2, and 0.38 < 0.5. Without this row the OSD
region falls below the active cutoff and blur silently turns off — a symptom visually
indistinguishable from "the layer rule never applied at all", which is a different and unrelated
failure mode this repo has already hit once.

Add for `quickshell-session`:
- an `animation = "slide"` row.
Do NOT add an `ignore_alpha` override for `quickshell-session` speculatively. Its card fill is
`Colours.surface` at `panelSurfaceOpacity` 0.78 and its scrim is `Design.sessionScrimOpacity`
0.55 — both above the family's own 0.5 floor, so the floor alone is predicted sufficient. Record
that prediction as an inline comment naming both numbers, and record that it is VERIFIED at
plan 20-08's Gate B criterion 1 (the dialog reads as part of the panel family — rim, rounded
card, theme-reactive fill, scrim), not assumed here.

Do not modify the family regex rows at lines 396 and 445, and do not modify the existing
notification override block — this task is additive only.
  </action>
  <verify>
    <automated>F=hypr/.config/hypr/config/windowrules.lua; SRC=$(grep -vE '^\s*--' "$F"); printf '%s\n' "$SRC" | grep -qE 'namespace = "quickshell-osd".*ignore_alpha = 0\.2' || { echo "MISSING quickshell-osd ignore_alpha override"; exit 1; }; printf '%s\n' "$SRC" | grep -qE 'namespace = "quickshell-osd".*animation = "slide"' || { echo "MISSING quickshell-osd animation row"; exit 1; }; printf '%s\n' "$SRC" | grep -qE 'namespace = "quickshell-osd".*blur = true' || { echo "MISSING quickshell-osd blur row"; exit 1; }; printf '%s\n' "$SRC" | grep -qE 'namespace = "quickshell-session".*animation = "slide"' || { echo "MISSING quickshell-session animation row"; exit 1; }; FAM=$(printf '%s\n' "$SRC" | grep -n 'namespace = "\^quickshell-\.\*".*ignore_alpha' | tail -1 | cut -d: -f1); OSD=$(printf '%s\n' "$SRC" | grep -n 'namespace = "quickshell-osd".*ignore_alpha' | head -1 | cut -d: -f1); [ "$OSD" -gt "$FAM" ] || { echo "ORDERING: quickshell-osd override must come AFTER the family regex (fam=$FAM osd=$OSD)"; exit 1; }; hypr/.config/hypr/scripts/hypr-lua-harness 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - With comment lines stripped, `windowrules.lua` contains a `quickshell-osd` rule for each of `animation = "slide"`, `blur = true`, and `ignore_alpha = 0.2`.
    - With comment lines stripped, `windowrules.lua` contains a `quickshell-session` rule with `animation = "slide"`.
    - The `quickshell-osd` `ignore_alpha` row's line number is strictly greater than the last `^quickshell-.*` family `ignore_alpha` row's line number (the verify script asserts this numerically).
    - The `quickshell-osd` `ignore_alpha` row carries an inline comment containing both the literal `0.38` (Toast.qml's fill alpha) and the literal `0.5` (the family floor).
    - The `quickshell-session` rows carry an inline comment naming both `0.78` and `0.55` and stating the no-override prediction is verified at Gate B, not assumed here.
    - Lines 396 and 445 (the family regex rows) are unchanged: `git diff -U0 -- hypr/.config/hypr/config/windowrules.lua` shows no hunk overlapping them.
    - `hypr/.config/hypr/scripts/hypr-lua-harness` runs without a parse error against the edited file.
  </acceptance_criteria>
  <done>Both new namespaces have their layer rules declared after the family regex, with the OSD's alpha override sized against the real fill value it will carry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → compositor | `windowrules.lua` is evaluated by Hyprland at session start; a malformed rule degrades the compositor's own rule set for every surface, not just this phase's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03-01 | Denial of Service | `windowrules.lua` layer-rule set | medium | mitigate | Edits are strictly additive (family regex rows at 396/445 untouched, asserted by diff-hunk check) and `hypr-lua-harness` parses the edited file before the change is accepted. |
| T-20-03-02 | Tampering | `Design.qml` / `BarRoles.qml` token surface | low | mitigate | New declarations are `readonly property` only and bind to existing `Colours.*` roles, never literals — `colour-lint` (GATE-04) exits 0 as the mechanical assertion. |
| T-20-03-SC | Tampering | package-manager installs | low | accept | No package installs — config-only edits to three already-tracked files. |
</threat_model>

<verification>
- `hypr/.config/hypr/scripts/colour-lint` exits 0.
- `hypr/.config/hypr/scripts/motion-lint` exits 0 (no motion literal was introduced; the new namespaces animate via the compositor's `animation = "slide"` row, not a hand-authored duration).
- `hypr/.config/hypr/scripts/hypr-lua-harness` parses `windowrules.lua` without error.
- The nine `Design.qml` tokens and the one `BarRoles.qml` role are all present and none is referenced yet — this plan declares, it does not consume.
</verification>

<success_criteria>
- Nine `Design.qml` tokens, one `BarRoles.qml` role, and four new layer-rule rows land in one commit.
- The `quickshell-osd` `ignore_alpha = 0.2` override exists and is declared after the family regex — the single edit that prevents the OSD shipping with silently-dead blur.
- No later plan in this phase needs to reopen any of these three files, keeping the OSD half and the power half file-disjoint.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New `Design.qml` tokens:** `osdWidth` (380), `osdHideDelayMs` (1200), `osdRecencyWindowMs` (1500), `sessionDialogWidth` (488), `sessionTileWidth` (136), `sessionTileHeight` (104), `sessionTileRadius` (16), `sessionTileIconSize` (32), `sessionScrimOpacity` (0.55).

**New `BarRoles.qml` role:** `onWarn` (= `Colours.onTertiary`).

**New layer namespaces registered in `windowrules.lua`:** `quickshell-osd` (animation/blur/ignore_alpha=0.2), `quickshell-session` (animation).

**Deleted symbols:** none.
</artifacts_produced>

<output>
Create `.planning/phases/20-indicators-power-menu/20-03-SUMMARY.md` when done.
</output>
