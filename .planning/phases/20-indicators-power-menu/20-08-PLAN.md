---
phase: 20-indicators-power-menu
plan: 08
type: execute
wave: 5
depends_on: ["20-05", "20-07"]
files_modified:
  - .planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md
  - .planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md
autonomous: false
requirements: [QOSD-01, QOSD-02, QOSD-03, QOSD-04, QPOWER-01, QPOWER-02, QPOWER-03, QPOWER-04, RETIRE-04, RETIRE-05, RETIRE-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TWO independent render gates are run and recorded SEPARATELY — Gate A (OSD) unlocks RETIRE-04, Gate B (power menu) unlocks RETIRE-05 (D-20-42). A stall in one must never hold the other hostage; Phase 19 needed twelve gap-closure rounds on a single gate."
    - "Every criterion passes only when confirmed LIVE — a real key press, a real drag, a real theme switch. 'Looks right in the source' is never a pass."
    - "Each criterion is recorded with one of PASS / FAIL / NOT-DEMONSTRABLE / OVERRIDDEN and a one-line observation. A criterion with no recorded observation is not a pass."
    - "Gate A criterion 3's brightness half is expected NOT-DEMONSTRABLE on this host and is pre-empted rather than discovered mid-gate: `brightnessctl -l` lists zero backlight-class devices, so the brightness row's code path is present-but-inert — the same D-18-39 precedent that made GATE-02 criterion B.3 NOT-DEMONSTRABLE in Phase 18."
    - "Gate A criterion 7 reads plan 20-01's recorded measurements rather than re-taking them, and a `RETIRE-04: BLOCKED` verdict there fails Gate A's own authorisation regardless of every other criterion passing (D-20-18)."
    - "Gate B re-runs Phase 15's 'who owns the prompt' check against the new power menu: a layer-shell overlay sits unconditionally above every XDG toplevel, so a confirm dialog another app raises lands behind it. This is a live-verify item with no existing code fix, recorded as an observation either way."
    - "The deletion authorisation is a `checkpoint:decision` — this phase's one-way door. `pacman -Rns` on this host cannot be undone without reinstalling packages this milestone exists to remove (D-20-42)."
    - "The authorisation records the exact commit sha each gate judged, and states the interlock plans 20-09 and 20-10 must re-assert before deleting: the shell tree must be unchanged since that sha."
    - "RETIRE-07 (`wlogout` + `eww`) rides whichever deletion lands SECOND, as the roadmap's single `pacman -Rns` covering both leftovers (D-20-42)."
  artifacts:
    - .planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md
    - .planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md
  key_links:
    - "Gate A record's verdict ↔ plan 20-09's authorisation to delete `swayosd` — 20-09 reads this file and refuses on anything but a full pass."
    - "Gate B record's verdict ↔ plan 20-10's authorisation to delete `wleave` (and, riding second, `wlogout`/`eww`)."
    - "Each record's judged sha ↔ the `git diff --quiet <sha> -- quickshell/.config/quickshell/` interlock re-asserted in 20-09 and 20-10 — the same interlock Phase 18's own deletion honoured."
  prohibitions:
    - "MUST NOT authorise or execute a package deletion whose own render gate has not passed. Each gate unlocks exactly its own deletion and nothing else."
    - "MUST NOT record a criterion as passing on source inspection. A criterion is confirmed by a real gesture against the running shell, or it is recorded NOT-DEMONSTRABLE with its reason."
---

<objective>
Run the two independent GATE-02 render gates, record each separately, and take the phase's one-way
door decision on package deletion.

Purpose: GATE-01 recorded what the retiring surfaces did. GATE-02 is where a human judges whether
the replacements are at least as good — the milestone's standing rule that no phase closes with
its replacement judged worse, and no old package is deleted before that judgment. D-20-25 makes
these gates mandatory rather than optional: the power menu is a redesign, not a port, so the
old-vs-new equivalence check is forfeit by design and a human verdict is the only remaining
instrument.

Output: `20-GATE-02-A-RECORD.md`, `20-GATE-02-B-RECORD.md`, and a recorded deletion authorisation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-UI-SPEC.md
@.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md
@.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and record GATE-02 Gate A — the OSD (unlocks RETIRE-04)</name>
  <files>.planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-UI-SPEC.md` § "GATE-02 Render-Gate Criteria" → "Gate A — OSD" — the seven criteria, verbatim. Do not paraphrase them into the record; transcribe them.
    - `.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md` — swayosd's recorded behaviour, which is what "at least as good as what it replaced" is judged against.
    - `.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md` — criterion 7 reads its two answers from here rather than re-taking them.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-GATE-02-RECORD.md` — the record format and the `## Deletion Authorisation` section shape this phase reuses, including how a NOT-DEMONSTRABLE verdict was recorded for B.3's brightness half under D-18-39.
    - `.planning/phases/20-indicators-power-menu/20-RESEARCH.md` § "Priority Research Findings" item 4, final paragraph — the live-verified `brightnessctl -l` finding (zero backlight-class devices on this host) that pre-empts criterion 3's brightness half.
  </read_first>
  <action>
Create `.planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md` following
`18-GATE-02-RECORD.md`'s format: a criteria table (`# | criterion | verdict | observation`),
followed by the sha judged and a per-gate authorisation section.

Transcribe UI-SPEC's seven Gate A criteria verbatim into the table, then have the operator run
each live against the shipped shell and record the verdict with a one-line observation. The seven:

1. **Aesthetic** — the OSD frame reads as part of the same family as the popup card, centre and
   toast (rounded corners, same `notifSurface` fill register, theme-reactive) — never SwayOSD's
   flat unthemed pill.
2. **Aesthetic** — a live theme switch re-colours the slider fill and handle within one crossfade,
   with zero literal-hex flash.
3. **Capability** — volume, brightness and mic key presses each show the correct single-row OSD;
   two pressed within `osdRecencyWindowMs` show a two-row column; a control that never moved never
   appears. **Pre-empt the brightness half:** `brightnessctl -l` lists zero backlight-class devices
   on this host, so the brightness row cannot be demonstrated live. Record it NOT-DEMONSTRABLE
   citing the existing D-18-39 precedent (the same reason Phase 18's own criterion B.3 carried
   that verdict) rather than treating it as a new gap discovered mid-gate. Demonstrate the volume
   and mic halves normally.
4. **Capability** — hover pauses the auto-hide timer; leaving RESUMES it, not resets it.
5. **Capability** — drag and scroll both adjust a slider in place, writing through the live
   backend, confirmed by a value shown elsewhere (bar capsule, centre) agreeing immediately.
6. **Capability** — Caps Lock shows the icon+label row ONLY on the ON transition, never on OFF.
   Use a real physical key press; a `wtype`-based check would not exercise the kernel's LED-classdev
   path and would not be a valid pass.
7. **Capability** — the two GATE-01 open questions are answered with evidence, not assumed: does
   the pill render over hyprlock (D-20-19), and does Caps Lock indicate at the SDDM prompt before
   RETIRE-04's `swayosd-libinput-backend.service` removal is authorised (D-20-17/D-20-18). Read
   both from `20-GATE-01-MEASUREMENTS.md`. **If that file records `RETIRE-04: BLOCKED`, criterion 7
   FAILS and Gate A does not authorise anything**, regardless of criteria 1-6.

Record the exact commit sha the gate judged (`git rev-parse HEAD`), and state the interlock plan
20-09 must re-assert before deleting:
`git diff --quiet <sha> -- quickshell/.config/quickshell/` must still hold — a shell tree that
moved after the judgment invalidates the judgment.

**Pass bar:** every criterion PASS or NOT-DEMONSTRABLE-with-recorded-reason. Any FAIL blocks Gate
A's own deletion until fixed and re-checked — and blocks nothing else. Gate B is unaffected.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md; test -f "$R"; N=$(grep -cE '^\| *[1-7] *\|' "$R"); [ "$N" -eq 7 ] || { echo "expected 7 criteria rows, found $N"; exit 1; }; U=$(grep -E '^\| *[1-7] *\|' "$R" | grep -cvE 'PASS|FAIL|NOT-DEMONSTRABLE|OVERRIDDEN'); [ "$U" -eq 0 ] || { echo "$U criteria rows carry no verdict"; exit 1; }; grep -qE '\b[0-9a-f]{7,40}\b' "$R" || { echo "no judged sha recorded"; exit 1; }; grep -qF 'git diff --quiet' "$R" || { echo "interlock command not recorded"; exit 1; }; grep -q 'D-18-39' "$R" || echo "WARN: criterion 3 NOT-DEMONSTRABLE precedent not cited"; echo OK</automated>
    <human-check>Run all seven Gate A criteria live against the shipped shell, in order, and report each verdict with a one-line observation. Before starting, apply layer rules with `hyprctl eval` or restart Hyprland — never `hyprctl reload`, which silently drops layer-rule edits and would make a correct surface fail criterion 1 for the wrong reason. For criterion 2, switch themes with the real theme-switch path (both a static preset and a matugen wallpaper switch) rather than editing a colour file. For criterion 6, press the physical Caps Lock key — not `wtype`.</human-check>
  </verify>
  <acceptance_criteria>
    - `20-GATE-02-A-RECORD.md` contains exactly seven numbered criteria rows, each transcribed from UI-SPEC's Gate A list.
    - Every criteria row carries one of PASS / FAIL / NOT-DEMONSTRABLE / OVERRIDDEN and a non-empty one-line observation.
    - Criterion 3's brightness half is recorded NOT-DEMONSTRABLE citing D-18-39, not FAIL and not silently omitted.
    - Criterion 7's verdict is derived from `20-GATE-01-MEASUREMENTS.md`, and a `RETIRE-04: BLOCKED` verdict there produces a criterion-7 FAIL.
    - The record contains a judged commit sha and the literal interlock command `git diff --quiet <sha> -- quickshell/.config/quickshell/`.
    - The record's authorisation section authorises RETIRE-04 ONLY — it names no other requirement.
  </acceptance_criteria>
  <done>A human has judged the OSD against swayosd's recorded behaviour, criterion by criterion, and the verdict is committed with the sha it applies to.</done>
</task>

<task type="auto">
  <name>Task 2: Run and record GATE-02 Gate B — the power menu (unlocks RETIRE-05)</name>
  <files>.planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-UI-SPEC.md` § "GATE-02 Render-Gate Criteria" → "Gate B — Power menu" — the nine criteria, verbatim.
    - `.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md` — wleave's recorded behaviour, including its six action quadruples and its `show-keybinds: false` dead definition.
    - `.planning/research/PITFALLS.md` around line 295 — the Phase 15 "who owns the prompt" carry-over, quoted verbatim in `20-RESEARCH.md`'s Security Domain table. This is criterion 10 below.
    - `.planning/phases/18-qml-bar-retirement-machinery/18-GATE-02-RECORD.md` — the record and authorisation format.
  </read_first>
  <action>
Create `.planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md` in the same format as
Gate A's, transcribing UI-SPEC's nine Gate B criteria verbatim plus ONE addition (criterion 10)
that the roadmap's own Notes require and UI-SPEC did not carry:

1. **Aesthetic** — the dialog reads as part of the panel family (rim, rounded card, theme-reactive
   fill, scrim) — never wleave's flat six-hue-capsule layout.
2. **Aesthetic** — a live theme switch re-colours tile fill, focus ring and warning banner within
   one crossfade.
3. All six actions present, correctly labelled and glyphed; Lock auto-focused on open.
4. Arrow keys move focus two-dimensionally across the 3×2 grid; Enter activates the focused tile;
   Escape closes with no action taken.
5. Mnemonics `l/e/u/h/r/s` fire their action from any focus state.
6. A live pacman/paru/yay run triggers the warning banner within one poll interval of opening the
   menu with it already running, and clears within one poll interval of it ending — the action
   still fires either way.
7. All three entry points (keybind, walker menu, bar glyph) open the identical in-process surface;
   none still shells to `wleave.sh`.
8. Opening the menu visibly clears any live notification popups, and the OSD is confirmed
   suppressed while the menu is open (a manual key press during the gate produces no OSD).
9. Shutdown and Reboot still route through the graceful compositor exit (QPOWER-04) — confirmed
   against the same mechanism FIX-01 established, not a new one.
10. **Security carry-over from Phase 15 — "who owns the prompt".** With the power menu open, have
    another application raise a confirm dialog (for example a browser's "leave site?" prompt or a
    file manager's overwrite confirmation). Observe and record whether that dialog lands BEHIND the
    layer-shell overlay. A layer-shell overlay sits unconditionally above every XDG toplevel, so
    the expected observation is that it does. Record the observation either way, plus the residual
    mitigations that exist today: the surface is transient (summoned, destroyed on dismiss),
    Escape always closes it with no action taken, and `HyprlandFocusGrab` dismisses it on focus
    loss. There is no code fix for the general case; this criterion records the residual honestly
    rather than pretending it is solved.

Record the judged sha and the same interlock command plan 20-10 must re-assert.

Also record the `quickshell-session` alpha prediction's outcome: plan 20-03 predicted no
`ignore_alpha` override row is needed because the card sits at 0.78 and the scrim at 0.55, both
above the family's own 0.5 floor. Criterion 1 is where that prediction is verified. If the card
reads flat rather than frosted, record it as a criterion-1 FAIL with "needs its own `ignore_alpha`
override row" as the observation — that is a real, fixable defect, not a taste judgment.

**Pass bar:** every criterion PASS or NOT-DEMONSTRABLE-with-recorded-reason. Any FAIL blocks Gate
B's own deletion until fixed and re-checked. Gate A is unaffected — the two halves share no
backend, and D-20-42 exists precisely so a stall in one cannot hold the other hostage.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md; test -f "$R"; N=$(grep -cE '^\| *([1-9]|10) *\|' "$R"); [ "$N" -eq 10 ] || { echo "expected 10 criteria rows, found $N"; exit 1; }; U=$(grep -E '^\| *([1-9]|10) *\|' "$R" | grep -cvE 'PASS|FAIL|NOT-DEMONSTRABLE|OVERRIDDEN'); [ "$U" -eq 0 ] || { echo "$U criteria rows carry no verdict"; exit 1; }; grep -qiE 'who owns the prompt' "$R" || { echo "Phase 15 security carry-over criterion missing"; exit 1; }; grep -qE '\b[0-9a-f]{7,40}\b' "$R"; grep -qF 'git diff --quiet' "$R"; grep -qE 'ignore_alpha' "$R" || echo "WARN: quickshell-session alpha prediction outcome not recorded"; echo OK</automated>
    <human-check>Run all ten Gate B criteria live and report each verdict with a one-line observation. Apply layer rules with `hyprctl eval` or a Hyprland restart first — never `hyprctl reload`. For criterion 6 use a real `pacman -Syuw` (download-only, safe to cancel). For criterion 9, actually reboot or shut down once at the END of the gate run, after every other criterion is recorded — that criterion cannot be observed without ending the session. For criterion 10, raise a genuine confirm dialog from another app while the menu is open and report exactly what you see.</human-check>
  </verify>
  <acceptance_criteria>
    - `20-GATE-02-B-RECORD.md` contains exactly ten numbered criteria rows: UI-SPEC's nine transcribed verbatim plus the Phase 15 "who owns the prompt" carry-over as criterion 10.
    - Every criteria row carries one of PASS / FAIL / NOT-DEMONSTRABLE / OVERRIDDEN and a non-empty one-line observation.
    - Criterion 10's observation records literally what happened to the other application's confirm dialog, and names the three residual mitigations that exist today.
    - Criterion 1's observation records the outcome of plan 20-03's `quickshell-session` no-override alpha prediction.
    - The record contains a judged commit sha and the literal interlock command.
    - The record's authorisation section authorises RETIRE-05 ONLY.
  </acceptance_criteria>
  <done>A human has judged the power menu against wleave's recorded behaviour, including the security carry-over the roadmap named, and the verdict is committed with the sha it applies to.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Authorise the irreversible package deletions this phase's two render gates unlock: `swayosd` (including `swayosd-libinput-backend.service`) under RETIRE-04, `wleave` under RETIRE-05, and `wlogout` + `eww` under RETIRE-07 riding whichever of the first two lands second.</decision>
  <context>
This is the phase's one-way door (D-20-42). `pacman -Rns` on this host is irreversible in the
sense that matters: undoing it means reinstalling packages this milestone exists to remove, and
re-adding the stow trees, contract entries, matugen templates, reload steps, layer rules and
checker references that the same commits delete. The deletions are also the point at which several
green gates stop being green if a reference site was missed — which is why plan 20-01 captured a
pre-deletion baseline with a disposition per site.

What has been established before this door:
- Gate A's record (Task 1) — the OSD judged criterion by criterion, at a named sha.
- Gate B's record (Task 2) — the power menu judged criterion by criterion, at a named sha.
- `20-GATE-01-MEASUREMENTS.md` — the SDDM greeter verdict that decides
  `swayosd-libinput-backend.service`'s fate. A `RETIRE-04: BLOCKED` verdict there means the
  backend reaches contexts the replacement cannot, and D-20-18 requires escalating that as a scope
  decision rather than working around it.
- `20-RETIREMENT-BASELINE.md` — the per-site disposition table plans 20-09 and 20-10 execute from,
  including the checker-internals and test-fixture reference classes CONTEXT.md's own
  canonical_refs list did not carry.

What is NOT established: nothing here proves a fresh clone still reproduces the desktop after the
deletions. That is RETIRE-09's fresh-install container gate in Phase 22, deliberately not this
phase's job.
  </context>
  <options>
    <option id="authorise-both">
      <name>Authorise both deletions (Gate A and Gate B both passed)</name>
      <pros>The phase completes as scoped; four packages leave the host; `contract.json` drops two entries toward its post-migration size; the milestone's retirement sequence stays on schedule.</pros>
      <cons>Irreversible without reinstalling. Any reference site missed by the baseline sweep surfaces as a red gate after the fact rather than before.</cons>
    </option>
    <option id="authorise-one">
      <name>Authorise only the gate that passed; hold the other</name>
      <pros>Honours D-20-42's whole design — the two halves share no backend, so a stall in one must not hold the other hostage. Phase 19 needed twelve gap-closure rounds on a single gate; this option is what keeps that from costing both halves.</pros>
      <cons>RETIRE-07 must ride the held deletion, so `wlogout` and `eww` stay installed until the second gate passes; the phase carries an open deletion into gap closure.</cons>
    </option>
    <option id="authorise-neither">
      <name>Authorise nothing; route both to gap closure</name>
      <pros>Nothing irreversible happens while a replacement is judged worse than what it replaced — the milestone's standing GATE-02 rule.</pros>
      <cons>The phase cannot close; both deletions defer.</cons>
    </option>
    <option id="escalate-retire-04">
      <name>Escalate RETIRE-04 as a scope decision (only if the SDDM measurement showed working greeter feedback)</name>
      <pros>Honours D-20-18 exactly: greeter feedback that genuinely works blocks RETIRE-04 outright, because the libinput backend cannot run without the `swayosd` package. Escalating is the recorded correct move, not working around it.</pros>
      <cons>RETIRE-04 does not complete this phase and the requirement needs re-scoping with the developer.</cons>
    </option>
  </options>
  <resume-signal>Reply with one of `authorise-both`, `authorise-one:<A|B>`, `authorise-neither`, or `escalate-retire-04`. If authorising, confirm the judged sha from each authorised gate's record so plans 20-09 and 20-10 can re-assert the `git diff --quiet <sha> -- quickshell/.config/quickshell/` interlock before deleting.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human judgment → irreversible action | The render-gate verdict is the only thing standing between a working desktop and four uninstalled packages |
| judged sha → deletion commit | A shell tree that moved between judgment and deletion invalidates the judgment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08-01 | Repudiation | gate verdicts | high | mitigate | Every criterion carries an explicit verdict token and a one-line observation; the verify script fails if any row lacks a verdict. A gate cannot pass by silence. |
| T-20-08-02 | Tampering | judged-sha drift | high | mitigate | Each record names the exact sha judged and the literal `git diff --quiet <sha> -- quickshell/.config/quickshell/` interlock, which plans 20-09 and 20-10 re-assert before deleting — the same interlock Phase 18's own deletion honoured. |
| T-20-08-03 | Denial of Service | another app's confirm dialog occluded by the session overlay | medium | mitigate | Gate B criterion 10 is the Phase 15 "who owns the prompt" carry-over, run live and recorded either way. No code fix exists for the general case; the residual mitigations (transient surface, Escape always closes, focus-grab dismissal) are recorded rather than claimed as a solution. |
| T-20-08-04 | Elevation of Privilege | authorising a deletion whose gate did not pass | high | mitigate | The `checkpoint:decision` is blocking, its options enumerate the partial-authorisation path explicitly, and the plan's own prohibition forbids authorising a deletion whose gate did not pass. Plans 20-09 and 20-10 each re-read their own gate's record before acting. |
| T-20-08-SC | Tampering | package-manager removals | high | mitigate | The removals themselves are gated behind this blocking checkpoint and executed in plans 20-09/20-10, each preceded by a `pacman -Rns` impact preview and a reverse-dependency check. No package is installed anywhere in this phase, so no legitimacy audit applies. |
</threat_model>

<verification>
- Both gate records exist, each with every criterion carrying a verdict and an observation.
- Gate A has exactly 7 criteria; Gate B has exactly 10 (UI-SPEC's 9 plus the Phase 15 carry-over).
- Each record names a judged sha and the interlock command.
- The checkpoint's resolution is recorded in the plan summary, including which deletions were authorised and at which shas.
</verification>

<success_criteria>
- Two independent gates were run and recorded separately, so a failure in one does not block the other's deletion.
- Every criterion was confirmed by a live gesture or recorded NOT-DEMONSTRABLE with a reason.
- The one-way door was taken deliberately, by a human, with both records and the SDDM verdict in front of them.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files:** `.planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md`, `.planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md`.

**New sections other plans read by name:**
- `20-GATE-02-A-RECORD.md` § Deletion Authorisation → authorises RETIRE-04 only, at a named sha (read by plan 20-09)
- `20-GATE-02-B-RECORD.md` § Deletion Authorisation → authorises RETIRE-05 only, at a named sha (read by plan 20-10)
- Both records' interlock command `git diff --quiet <sha> -- quickshell/.config/quickshell/`

**Deleted symbols:** none — this plan judges and authorises; it deletes nothing.
</artifacts_produced>

<output>
Create `.planning/phases/20-indicators-power-menu/20-08-SUMMARY.md` when done.
</output>
