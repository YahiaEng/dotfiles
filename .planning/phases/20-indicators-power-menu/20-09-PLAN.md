---
phase: 20-indicators-power-menu
plan: 09
type: execute
wave: 6
depends_on: ["20-08"]
files_modified:
  - swayosd/.config/swayosd/style.css
  - matugen/.config/matugen/config.toml
  - matugen/.config/matugen/templates/swayosd-colors.css
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/theme-doctor
  - theme-engine/.config/theme-engine/lib/reload.sh
  - theme-engine/.config/theme-engine/lib/gtk.sh
  - hypr/.config/hypr/config/autostart.lua
  - hypr/.config/hypr/scripts/motion-lint
  - hypr/.config/hypr/scripts/quickshell-doctor
  - hypr/.config/hypr/scripts/retirement-check
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-hyprctl-binds.txt
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-missing-xf86-binds.txt
  - hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-duplicate-xf86-binds.txt
  - install.sh
  - stow.sh
autonomous: false
requirements: [RETIRE-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Nothing is deleted before Gate A has passed. Plan 20-08's `20-GATE-02-A-RECORD.md` is read first, and its `git diff --quiet <sha> -- quickshell/.config/quickshell/` interlock is re-asserted — a shell tree that moved after the judgment invalidates the judgment."
    - "`swayosd` leaves repo and host, INCLUDING its libinput backend service (RETIRE-04), and the backend's fate is read from `20-GATE-01-MEASUREMENTS.md`'s recorded SDDM observation — never defaulted into (D-20-17, the roadmap's own BAR-02-style requirement)."
    - "Config removal and package removal land in the SAME COMMIT, config-then-package — the standing WINDOWS #1 precedent for every retirement in this milestone."
    - "`retirement-check swayosd` reports zero blocking hits AFTER the deletion, measured against the pre-deletion baseline plan 20-01 committed. Before-and-after is RETIRE-01's own requirement, not a formality."
    - "The checker-internals class is closed, not just the obvious config class: `motion-lint`'s swayosd exemption and deployed-directory entries, `theme-doctor`'s `GTK4_CSS_SHEETS` entry, `quickshell-doctor`'s residual swayosd helpers, and the three `tests/quickshell-fixtures/*binds.txt` fixture files that carry `swayosd-client` bind strings all go in the same commit."
    - "`theme-doctor`, `theme-parity`, `motion-lint`, `colour-lint`, `quickshell-doctor --self-test` and `retirement-check --all` are all green after the deletion — a retirement that reds a gate is not complete."
    - "`contract.json`'s `files` array loses `swayosd.css`, and `theme-doctor`/`theme-parity` agree with the new size with no orphaned entry."
    - "`retirement-check`'s own registry flips `swayosd` from `status=pending` to `status=retired`, so `--all` reports it in the blocking tier from then on."
  artifacts:
    - .planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md
  key_links:
    - "`20-GATE-02-A-RECORD.md` § Deletion Authorisation ↔ this plan's Task 1 pre-flight — the authorisation is read, not assumed."
    - "`20-RETIREMENT-BASELINE.md`'s disposition table ↔ this plan's Task 2 — every swayosd row's disposition is executed and ticked off."
    - "`20-GATE-01-MEASUREMENTS.md` § SDDM greeter ↔ the libinput backend's removal — a `BLOCKED` verdict halts this plan."
  prohibitions:
    - "MUST NOT delete any part of `swayosd` if `20-GATE-02-A-RECORD.md` records a FAIL on any criterion, or if `20-GATE-01-MEASUREMENTS.md` records `RETIRE-04: BLOCKED`."
    - "MUST NOT split config removal and package removal across two commits. WINDOWS #1 is the standing precedent for exactly this failure, and it is why `retirement-check` exists."
---

<objective>
Remove `swayosd` — package, config tree, contract entry, matugen template, reload step, autostart
line, systemd unit enablement, checker exemptions and test fixtures — in one config-then-package
commit, gated behind Gate A's passing verdict.

Purpose: RETIRE-04's bar is not "uninstall the package." It is every reference class cleared,
before and after, with the human render gate having judged the replacement at least as good. The
libinput backend half is the named scope call the roadmap singled out, decided by measurement
rather than by omission.

Output: `swayosd` gone from repo and host, all gates green, and `20-RETIRE-04-RECORD.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md
@.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md
@.planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md
@.planning/phases/20-indicators-power-menu/20-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — re-assert the interlock, read the verdicts, preview the removal</name>
  <precondition>`20-GATE-02-A-RECORD.md` exists and records no FAIL, and `20-GATE-01-MEASUREMENTS.md`'s SDDM section does not record `RETIRE-04: BLOCKED`. If either fails, HALT and report — do not proceed to Task 2.</precondition>
  <files>.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-GATE-02-A-RECORD.md` — the seven criteria verdicts, the judged sha, and the `## Deletion Authorisation` section.
    - `.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md` § SDDM greeter Caps Lock — the recorded verdict token.
    - `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md` — the swayosd rows of the disposition table and the committed pre-deletion `retirement-check swayosd` output.
    - `.planning/phases/20-indicators-power-menu/20-08-SUMMARY.md` — the checkpoint resolution recording which deletions were authorised and at which shas.
  </read_first>
  <action>
Halt-or-proceed pre-flight. Produce `.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md`
with a `## Pre-flight` section recording each of the following, with its literal result:

1. **Gate A verdict** — every one of the seven criteria's recorded verdict. Any FAIL halts.
2. **Interlock** — run `git diff --quiet <judged-sha> -- quickshell/.config/quickshell/` using the
   sha named in the Gate A record. A non-zero exit means the shell tree moved after the judgment,
   which invalidates the judgment: halt and report rather than re-authorising on your own
   authority. This is the same interlock Phase 18's own deletion honoured.
3. **SDDM verdict** — the recorded token from `20-GATE-01-MEASUREMENTS.md`. `RETIRE-04: BLOCKED`
   halts (D-20-18: greeter feedback that genuinely works blocks RETIRE-04 outright and is escalated
   as a scope decision, not worked around). `UNTAKEN` also halts — an untaken measurement is not a
   verdict.
4. **Removal impact preview** — run `pacman -Rns --print swayosd` (or `pacman -Rnsp swayosd`) and
   record the full list of packages that would be removed. If it names anything other than
   `swayosd` itself and packages that exist solely as its dependencies, halt and report: `-s`
   removes now-orphaned dependencies and a shared dependency appearing in that list is a real
   hazard, not a formality.
5. **Reverse-dependency check** — run `pactree -r swayosd` and record the output. Anything
   depending on `swayosd` halts.
6. **Baseline recall** — record the pre-deletion `retirement-check swayosd` hit counts from
   `20-RETIREMENT-BASELINE.md`, so Task 3's after-run has an explicit number to be measured
   against rather than "looks clean".

Make no repository or host change in this task. It reads, records, and either clears the way or
halts.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md; test -f "$R"; grep -q '^## Pre-flight' "$R"; grep -qF 'pacman -Rns' "$R"; grep -qF 'pactree -r' "$R"; grep -qF 'git diff --quiet' "$R"; grep -qiE 'RETIRE-04: (BLOCKED|proceeds)' "$R" || { echo "SDDM verdict not carried into the record"; exit 1; }; git diff --quiet HEAD -- swayosd/ matugen/ theme-engine/ hypr/ install.sh stow.sh || { echo "pre-flight must not modify the tree"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-RETIRE-04-RECORD.md` has a `## Pre-flight` section recording all six items with literal results.
    - The Gate A interlock command was run and its exit code is recorded.
    - The SDDM verdict token is transcribed into the record, and the record states plainly whether it clears or halts RETIRE-04.
    - `pacman -Rns --print swayosd` output and `pactree -r swayosd` output are both recorded verbatim.
    - The pre-deletion `retirement-check swayosd` hit counts are recorded as the number Task 3 measures against.
    - `git diff --quiet HEAD` over the config trees confirms this task changed nothing.
  </acceptance_criteria>
  <done>Either the way is provably clear — gate passed, interlock held, measurement recorded, removal scoped to swayosd alone — or the plan halted with the specific reason recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Execute the concrete `swayosd` removal that Task 1's impact preview just produced.</decision>
  <context>
Plan 20-08 authorised RETIRE-04 in principle, before this preview existed. This checkpoint is not
a re-ask of that decision — it is the last look at information that did not exist when the
authorisation was given: the literal `pacman -Rns --print swayosd` package list, the
`pactree -r swayosd` reverse-dependency output, the Gate A interlock's exit code, and the recorded
SDDM verdict, all captured in `20-RETIRE-04-RECORD.md` § Pre-flight in the preceding task.

`-s` removes now-orphaned dependencies. If that preview names anything beyond `swayosd` and
packages that exist solely to serve it, this is a different removal from the one that was
authorised. This is the point of no return: the next task deletes the stow tree, the contract
entry, the matugen template, the reload step, the autostart line, four checker references and
three test fixtures, and uninstalls the package — all in one commit.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the removal exactly as previewed</name>
      <pros>Preview is scoped to swayosd and its own orphans, nothing depends on it, Gate A passed at an unchanged sha, and the SDDM measurement cleared the libinput backend. RETIRE-04 completes as scoped.</pros>
      <cons>Irreversible without reinstalling the package and re-adding every reference the same commit deletes.</cons>
    </option>
    <option id="halt-scope">
      <name>Halt — the preview names a package beyond swayosd and its orphans</name>
      <pros>Prevents removing a shared dependency some other part of this desktop needs; the exact class of damage `-s` can do quietly.</pros>
      <cons>RETIRE-04 defers until the removal is narrowed (e.g. `-Rn` without `-s`, plus a separate orphan review).</cons>
    </option>
    <option id="halt-gate">
      <name>Halt — the interlock failed or the SDDM verdict is BLOCKED/UNTAKEN</name>
      <pros>A moved shell tree invalidates Gate A's judgment, and D-20-18 requires escalating working greeter feedback as a scope decision rather than working around it.</pros>
      <cons>RETIRE-04 defers pending a re-run gate or a scope conversation.</cons>
    </option>
  </options>
  <resume-signal>Reply `proceed`, `halt-scope`, or `halt-gate`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: One config-then-package commit — every swayosd reference class, then the package</name>
  <reversibility rating="one-way">`pacman -Rns swayosd` on this host cannot be undone without reinstalling a package this milestone exists to remove, together with its stow tree, contract entry, matugen template and every checker reference the same commit deletes. Authorised at plan 20-08's `checkpoint:decision`; the rating is carried here for the record, not re-gated.</reversibility>
  <files>swayosd/.config/swayosd/style.css, matugen/.config/matugen/config.toml, matugen/.config/matugen/templates/swayosd-colors.css, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/theme-doctor, theme-engine/.config/theme-engine/lib/reload.sh, theme-engine/.config/theme-engine/lib/gtk.sh, hypr/.config/hypr/config/autostart.lua, hypr/.config/hypr/scripts/motion-lint, hypr/.config/hypr/scripts/quickshell-doctor, hypr/.config/hypr/scripts/retirement-check, hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-hyprctl-binds.txt, hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-missing-xf86-binds.txt, hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-duplicate-xf86-binds.txt, install.sh, stow.sh</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md` — the swayosd disposition rows. Every row's disposition is executed here; a row left unticked is an unclosed hole.
    - `theme-engine/.config/theme-engine/contract.json` — the `files` array (20 entries) containing `{ "name": "swayosd.css", "format": "gtk-css" }`.
    - `matugen/.config/matugen/config.toml` lines 79-81 — the `[templates.swayosd]` block and the template path it points at.
    - `theme-engine/.config/theme-engine/theme-doctor` — the `GTK4_CSS_SHEETS` array containing `$HOME/.config/swayosd/style.css`.
    - `theme-engine/.config/theme-engine/lib/reload.sh` lines ~100-130 — the swayosd-server kill-and-relaunch block with its `pgrep -x swayosd-server` guard and its explanatory comment. QML hot-reloads natively, so nothing replaces this step.
    - `theme-engine/.config/theme-engine/lib/gtk.sh` around line 311 — the comment referencing the wleave/swayosd sheets. Remove only the swayosd half here; wleave's half belongs to plan 20-10.
    - `hypr/.config/hypr/config/autostart.lua` lines 185-200 — `hl.exec_cmd("uwsm app -- swayosd-server")` at line 192 and the two comments at 185/190 distinguishing the libinput backend from the per-session client.
    - `hypr/.config/hypr/scripts/motion-lint` — the `swayosd/style.css` exemption entry (around line 411) and the `$HOME/.config/swayosd` deployed-directory entry (around line 1154).
    - `hypr/.config/hypr/scripts/quickshell-doctor` — any residual swayosd helper plan 20-04 Task 3 did not remove (its summary records which, if any, survived).
    - `hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-hyprctl-binds.txt`, `poisoned-missing-xf86-binds.txt`, `poisoned-duplicate-xf86-binds.txt` — all three carry `swayosd-client` bind strings as fixture DATA. Read what each fixture is asserting before editing, so the poisoned fixtures still poison the thing they were built to poison.
    - `install.sh` around line 190 (the swayosd package entry) and lines 553-562 (the `systemctl enable --now swayosd-libinput-backend.service` block); `stow.sh` around line 29 (the swayosd stow entry).
    - `hypr/.config/hypr/scripts/retirement-check` — the registry block, where `swayosd` carries `status=pending requirement=RETIRE-04 own-tree=swayosd/`.
  </read_first>
  <action>
Stage every config-side removal, then remove the package, then commit all of it together. Config
then package, one commit — the standing WINDOWS #1 precedent, and the reason `retirement-check`
exists.

**Repo side:**
- Delete the `swayosd/` stow tree (its only file is `.config/swayosd/style.css`).
- Remove `{ "name": "swayosd.css", "format": "gtk-css" }` from `contract.json`'s `files` array
  (20 → 19 entries; plan 20-10 takes it to 18). `swayosd.css` is NOT a `REPRESENTATIVE_FILES`
  entry — that trap belongs to `wleave.css` and plan 20-10.
- Remove `[templates.swayosd]` from `matugen/.config/matugen/config.toml` and delete
  `matugen/.config/matugen/templates/swayosd-colors.css`.
- Remove `$HOME/.config/swayosd/style.css` from `theme-doctor`'s `GTK4_CSS_SHEETS` array.
- Remove `lib/reload.sh`'s entire swayosd-server kill-and-relaunch block. Replace it with a short
  comment recording that the OSD is now an in-process QML surface which hot-reloads on file change
  natively, so the fan-out has no OSD step and needs none — a deleted step with no explanation
  reads as an omission to the next person editing this file.
- Remove the swayosd half of `lib/gtk.sh`'s comment; leave the wleave half for plan 20-10.
- Remove `autostart.lua:192`'s `hl.exec_cmd("uwsm app -- swayosd-server")` and the two surrounding
  comments at 185/190 that only exist to explain it.
- Remove `motion-lint`'s `swayosd/style.css` exemption entry and its `$HOME/.config/swayosd`
  deployed-directory entry.
- Remove any residual swayosd helper still in `quickshell-doctor` (plan 20-04 Task 3's summary
  records whether `_qsd_swayosd_server_reachable` survived and why).
- Edit the three `tests/quickshell-fixtures/*binds.txt` fixtures so their `swayosd-client` bind
  strings become the new exec targets (`wpctl set-volume …` / `wpctl set-mute …` /
  `brightnessctl --class=backlight set …`). These are fixture DATA driving a checker's own
  self-test: `compliant-hyprctl-binds.txt` must still represent a COMPLIANT bind set,
  `poisoned-missing-xf86-binds.txt` must still be missing the same XF86 bind it was built to omit,
  and `poisoned-duplicate-xf86-binds.txt` must still duplicate the same one. Do not blanket-replace
  strings without re-reading what each fixture asserts — a poisoned fixture that stops poisoning
  turns its checker into a rubber stamp.
- Remove `install.sh`'s swayosd package entry AND its
  `systemctl enable --now swayosd-libinput-backend.service` block; remove `stow.sh`'s swayosd entry.
- Flip `retirement-check`'s registry row for `swayosd` from `status=pending` to `status=retired`,
  so `--all` reports it in the blocking tier from now on (the same transition `waybar` and `swaync`
  already carry).

**Host side, in this order:**
1. `systemctl disable --now swayosd-libinput-backend.service` FIRST. Removing the package while the
   unit is still enabled leaves a dangling symlink under `graphical.target.wants/`, which is a
   silent broken window rather than a loud one. This is the libinput backend's removal — the
   named scope call the roadmap required be decided explicitly, cleared by Task 1's SDDM verdict.
2. `pacman -Rns swayosd`, having confirmed in Task 1 that its `--print` preview names nothing
   beyond swayosd and its own orphans.

Then commit everything — the repo edits and the fact of the package removal — as one commit whose
message names RETIRE-04 and cites the Gate A record's sha as the authorising judgment.
  </action>
  <verify>
    <automated>set -e; test ! -d swayosd; test ! -f matugen/.config/matugen/templates/swayosd-colors.css; for f in theme-engine/.config/theme-engine/contract.json matugen/.config/matugen/config.toml theme-engine/.config/theme-engine/theme-doctor theme-engine/.config/theme-engine/lib/reload.sh install.sh stow.sh hypr/.config/hypr/config/autostart.lua; do case "$f" in *reload.sh|*autostart.lua) SRC=$(grep -vE '^\s*(#|--)' "$f");; *) SRC=$(cat "$f");; esac; printf '%s\n' "$SRC" | grep -ci 'swayosd' | grep -qx 0 || { echo "swayosd still referenced in $f"; exit 1; }; done; grep -vE '^\s*#' hypr/.config/hypr/scripts/motion-lint | grep -ci 'swayosd' | grep -qx 0 || { echo "motion-lint still references swayosd"; exit 1; }; for t in hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-hyprctl-binds.txt hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-missing-xf86-binds.txt hypr/.config/hypr/scripts/tests/quickshell-fixtures/poisoned-duplicate-xf86-binds.txt; do grep -c 'swayosd-client' "$t" | grep -qx 0 || { echo "fixture still carries swayosd-client: $t"; exit 1; }; done; grep -q 'swayosd .*status=retired' <(hypr/.config/hypr/scripts/retirement-check --list) || { echo "retirement-check registry not flipped to retired"; exit 1; }; pacman -Q swayosd >/dev/null 2>&1 && { echo "swayosd package still installed"; exit 1; }; systemctl is-enabled swayosd-libinput-backend.service >/dev/null 2>&1 && { echo "libinput backend still enabled"; exit 1; }; python3 -c "import json;d=json.load(open('theme-engine/.config/theme-engine/contract.json'));n=len(d['files']);print('contract files:',n);assert not any(f['name']=='swayosd.css' for f in d['files'])"; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The `swayosd/` directory and `matugen/.config/matugen/templates/swayosd-colors.css` no longer exist.
    - `contract.json` parses as valid JSON, its `files` array no longer contains `swayosd.css`, and its length dropped by exactly one.
    - No non-comment line in `matugen/config.toml`, `theme-doctor`, `lib/reload.sh`, `lib/gtk.sh`, `autostart.lua`, `motion-lint`, `quickshell-doctor`, `install.sh` or `stow.sh` mentions `swayosd`.
    - None of the three `tests/quickshell-fixtures/*binds.txt` files contains `swayosd-client`, and each still asserts what it asserted before (compliant set compliant, missing-bind fixture still missing the same bind, duplicate fixture still duplicating the same one).
    - `retirement-check --list` shows `swayosd status=retired`.
    - `pacman -Q swayosd` fails and `systemctl is-enabled swayosd-libinput-backend.service` fails — the package and its unit are gone from the host.
    - `lib/reload.sh` carries a comment explaining why there is no OSD reload step, rather than a silent gap.
    - `git log -1 --stat` shows the config edits and the deletion in ONE commit, whose message names RETIRE-04 and the Gate A sha.
  </acceptance_criteria>
  <done>`swayosd` and its libinput backend are gone from repo and host, every reference class cleared in the same commit as the package.</done>
</task>

<task type="auto">
  <name>Task 3: Post-deletion zero-hit run and green gates</name>
  <files>.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md` — the pre-deletion `retirement-check swayosd` output and hit counts, which this task's after-run is measured against.
    - `.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md` — the `## Pre-flight` section written by Task 1, which this task appends to.
    - `hypr/.config/hypr/scripts/retirement-check` header — the two-tier `[FAIL]` (blocking) vs `[REPORT]` (`.planning/` and repo prose) split, so a `.planning/` archive mention is correctly not treated as a failure.
  </read_first>
  <action>
Append a `## Post-deletion` section to
`.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md`.

Run `retirement-check swayosd` again and commit its raw output verbatim alongside the pre-deletion
baseline's, with both hit counts side by side. RETIRE-01's requirement is the before-AND-after
pair; a single clean run proves nothing about what was there to begin with. The blocking tier must
be zero. Report-tier hits in `.planning/` and repo prose are expected and correct — the archive
legitimately records thousands of mentions of every retired surface, and D-18-37 split the tiers
for exactly this reason. Record the report-tier count without treating it as a failure.

Then run and record the exit status of each gate:
- `theme-engine/.config/theme-engine/theme-doctor`
- `theme-engine/.config/theme-engine/theme-parity`
- `hypr/.config/hypr/scripts/motion-lint`
- `hypr/.config/hypr/scripts/colour-lint`
- `hypr/.config/hypr/scripts/quickshell-doctor --self-test`
- `hypr/.config/hypr/scripts/quickshell-doctor` (the live run, including the OSD check plan 20-04
  Task 3 re-expressed against `quickshell-osd`)
- `hypr/.config/hypr/scripts/retirement-check --all`
- `hypr/.config/hypr/scripts/keybind-doctor`

Any non-zero exit is a defect this plan introduced and must be fixed here, not deferred — a
retirement that reds a gate is not complete. Record each gate's literal exit code, not a summary
adjective.

Do NOT run `theme-stress-test` in this plan. Its `REPRESENTATIVE_FILES` array still names
`wleave.css`, which plan 20-10 removes; running it here would either pass for the wrong reason or
fail for a reason this plan does not own. Record that deferral explicitly with plan 20-10 named as
the owner, so a reader does not read the omission as an oversight.

Finally, record a one-line honest statement of what this deletion did NOT prove: nothing here
establishes that a fresh clone still reproduces the desktop. That is RETIRE-09's fresh-install
container gate in Phase 22.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md; grep -q '^## Post-deletion' "$R"; grep -qF 'retirement-check swayosd' "$R"; for g in theme-doctor theme-parity motion-lint colour-lint keybind-doctor; do grep -qF "$g" "$R" || { echo "gate not recorded: $g"; exit 1; }; done; grep -qF 'theme-stress-test' "$R" || { echo "stress-test deferral not recorded"; exit 1; }; hypr/.config/hypr/scripts/retirement-check swayosd; echo "retirement-check swayosd exit=$?"; theme-engine/.config/theme-engine/theme-doctor >/dev/null; theme-engine/.config/theme-engine/theme-parity >/dev/null; hypr/.config/hypr/scripts/motion-lint >/dev/null; hypr/.config/hypr/scripts/colour-lint >/dev/null; hypr/.config/hypr/scripts/quickshell-doctor --self-test >/dev/null; hypr/.config/hypr/scripts/retirement-check --all >/dev/null; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-RETIRE-04-RECORD.md` has a `## Post-deletion` section carrying the raw `retirement-check swayosd` output and its blocking-tier hit count of 0, printed beside the pre-deletion baseline count.
    - The report-tier hit count is recorded and explicitly noted as expected under D-18-37's two-tier split, not as a failure.
    - Each of the eight listed gates has its literal exit code recorded, and every one is 0.
    - The `theme-stress-test` deferral is recorded with plan 20-10 named as its owner.
    - The record states plainly that nothing here proves a fresh clone still reproduces the desktop, naming RETIRE-09/Phase 22 as where that is proven.
  </acceptance_criteria>
  <done>The before/after zero-hit pair is committed, every gate is green with its exit code recorded, and the one thing this deletion did not prove is named rather than implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → host package database | `pacman -Rns` mutates system state outside the repo and outside git's ability to revert |
| repo → systemd system-level units | `systemctl disable` touches `graphical.target.wants/`, a host-level directory no stow tree owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-09-01 | Denial of Service | `pacman -Rns` removing a shared dependency | high | mitigate | Task 1 runs `pacman -Rns --print swayosd` and `pactree -r swayosd` and HALTS if the preview names anything beyond swayosd and its own orphans, or if anything depends on it. The preview is recorded verbatim in the record. |
| T-20-09-02 | Tampering | poisoned checker fixtures silently defanged | high | mitigate | The three `tests/quickshell-fixtures/*binds.txt` files are edited with their assertion re-read first, and `quickshell-doctor --self-test` is required to exit 0 afterward — a fixture that stopped poisoning would fail its own self-test rather than pass quietly. |
| T-20-09-03 | Denial of Service | dangling systemd unit symlink | medium | mitigate | `systemctl disable --now` runs BEFORE `pacman -Rns`, so no orphaned symlink is left under `graphical.target.wants/`; `systemctl is-enabled` failing afterward is asserted. |
| T-20-09-04 | Repudiation | claiming a clean retirement without a baseline | medium | mitigate | The post-deletion `retirement-check` output is recorded beside plan 20-01's pre-deletion baseline with both counts printed — RETIRE-01's before-AND-after pair, not a single clean run. |
| T-20-09-05 | Elevation of Privilege | deleting on a failed or stale gate | high | mitigate | Task 1's precondition halts on any Gate A FAIL, on a `RETIRE-04: BLOCKED` or `UNTAKEN` SDDM verdict, and on a non-zero `git diff --quiet <judged-sha>` interlock. |
| T-20-09-SC | Tampering | package-manager removal | high | mitigate | Removal is preceded by a `--print` impact preview and a reverse-dependency check, and gated behind plan 20-08's blocking `checkpoint:decision`. No package is installed, so no legitimacy audit applies (RESEARCH.md records this phase as removals-only). |
</threat_model>

<verification>
- `pacman -Q swayosd` fails; `systemctl is-enabled swayosd-libinput-backend.service` fails.
- `retirement-check swayosd` blocking tier is zero, recorded beside the pre-deletion baseline.
- `theme-doctor`, `theme-parity`, `motion-lint`, `colour-lint`, `quickshell-doctor --self-test`, the live `quickshell-doctor` run, `retirement-check --all` and `keybind-doctor` all exit 0.
- `contract.json` parses and its `files` array dropped exactly one entry.
- The volume/mic keys and the QML OSD still work with swayosd gone — the exec-target swap in plan 20-04 is what made the deletion safe, and this confirms it.

</verification>

<success_criteria>
- `swayosd` and `swayosd-libinput-backend.service` are gone from repo and host, in one config-then-package commit, behind a passed Gate A and a recorded SDDM measurement.
- Every reference class is cleared, including the checker-internals and test-fixture classes CONTEXT.md's canonical_refs did not list.
- Every gate is green with its exit code recorded, and the one deferred gate names its owner.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files:** `.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md` (§ Pre-flight, § Post-deletion).

**Deleted symbols and files:**
- `swayosd/` stow tree (`swayosd/.config/swayosd/style.css`)
- `matugen/.config/matugen/templates/swayosd-colors.css` and `[templates.swayosd]`
- `contract.json` `files` entry `swayosd.css` (20 → 19)
- `theme-doctor`'s `GTK4_CSS_SHEETS` entry `$HOME/.config/swayosd/style.css`
- `lib/reload.sh`'s swayosd-server kill-and-relaunch block
- `lib/gtk.sh`'s swayosd comment half
- `autostart.lua:192`'s `hl.exec_cmd("uwsm app -- swayosd-server")` and its two explanatory comments
- `motion-lint`'s `swayosd/style.css` exemption and `$HOME/.config/swayosd` directory entries
- `quickshell-doctor`'s residual swayosd helpers
- `install.sh`'s swayosd package entry and its `systemctl enable --now swayosd-libinput-backend.service` block; `stow.sh`'s swayosd entry
- host: the `swayosd` package and the enabled `swayosd-libinput-backend.service`

**Changed symbols:** `retirement-check` registry row `swayosd` → `status=retired`; the three `tests/quickshell-fixtures/*binds.txt` fixtures' bind strings repointed to the new exec targets.
</artifacts_produced>

<output>
Create `.planning/phases/20-indicators-power-menu/20-09-SUMMARY.md` when done.
</output>
