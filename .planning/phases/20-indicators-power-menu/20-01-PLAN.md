---
phase: 20-indicators-power-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md
  - .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md
  - .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md
  - .planning/phases/20-indicators-power-menu/COVERAGE.md
autonomous: false
requirements: [RETIRE-04, RETIRE-05, RETIRE-07, QOSD-01, QOSD-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "GATE-01 baseline exists and was written while swayosd and wleave are still installed — the six wleave action command strings, their l/e/u/h/r/s mnemonics, swayosd's anchor/timing/pill geometry, and both packages' unit topology are transcribed off the live implementations (D-20-43 item 2)."
    - "The consumer sweep captures a PRE-deletion `retirement-check` run for all four surfaces (swayosd, wleave, wlogout, eww) as committed raw output, so the post-deletion zero-hit run in plans 20-09/20-10 has a baseline to be measured against (D-20-43 item 3)."
    - "The consumer sweep names every reference site found OUTSIDE 20-CONTEXT.md's own canonical_refs list — in particular the checker-internals class (`quickshell-doctor`'s `panel-swayosd-key-ownership` swayosd instrument, `motion-lint`'s swayosd/wleave exemption and carve-out rules, the `tests/quickshell-fixtures/*binds.txt` and `tests/motion-fixtures/*gtk4.css` fixture trees) — because a reference class discovered at deletion time is the WINDOWS #1 failure mode this sweep exists to prevent."
    - "D-20-17's SDDM-greeter Caps Lock observation is RECORDED either way, with the literal observed outcome, never defaulted into by omission."
    - "D-20-19's hyprlock volume-key observation is RECORDED either way, with the literal observed outcome."
    - "The sysfs Caps Lock `FileView{watchChanges:true}` watch is exercised against a REAL physical Caps Lock key press (not `wtype`, not a root write) and the result — fired / did not fire — is recorded."
    - "If the SDDM observation shows working greeter feedback, the plan records RETIRE-04 as BLOCKED and escalates it as a scope decision rather than working around it (D-20-18)."
    - "`COVERAGE.md` carries a reasoned `No external API integration:` declaration so the seal-time `api-coverage.verify-pre` gate passes without a fabricated matrix."
  artifacts:
    - .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md
    - .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md
    - .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md
    - .planning/phases/20-indicators-power-menu/COVERAGE.md
  key_links:
    - "20-RETIREMENT-BASELINE.md ↔ plans 20-09/20-10's post-deletion zero-hit runs — the before/after pair RETIRE-01 requires."
    - "20-GATE-01-MEASUREMENTS.md § SDDM ↔ plan 20-09's RETIRE-04 authorisation — the libinput backend's fate is read from this file, not re-derived."
    - "20-GATE-01-MEASUREMENTS.md § sysfs-watch ↔ plan 20-05 Task 2's implementation path — a negative result changes the implementation from event-driven to polled and is a scope conversation, not a silent substitution."
  prohibitions:
    - "MUST NOT record a GATE-01 measurement as 'not applicable', 'assumed', or 'expected' when the observation was not actually taken — an untaken observation is recorded as UNTAKEN with the reason, never as a defaulted verdict."
---

<objective>
Take the phase's opening GATE-01 enumeration off the live implementations of `swayosd` and
`wleave` while they still exist, capture the full pre-deletion consumer sweep for all four
retirement targets, and close the three open questions RESEARCH.md could not close from within
a running session.

Purpose: every later plan in this phase reads from these three documents. The behavioural
baseline is what GATE-02 judges the replacements against; the consumer sweep is the "before"
half of RETIRE-01's before/after zero-hit pair; and the three measurements decide two locked
decisions (D-20-17, D-20-19) and one implementation path (D-20-13/14) that would otherwise be
defaulted into.

Output: `20-BEHAVIOUR-BASELINE.md`, `20-RETIREMENT-BASELINE.md`, `20-GATE-01-MEASUREMENTS.md`,
`COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-RESEARCH.md
@.planning/phases/18-qml-bar-retirement-machinery/18-BEHAVIOUR-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate swayosd's and wleave's live behaviour into 20-BEHAVIOUR-BASELINE.md</name>
  <files>.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md</files>
  <read_first>
    - `.planning/phases/18-qml-bar-retirement-machinery/18-BEHAVIOUR-BASELINE.md` § "GATE-01 Recurrence Protocol" (lines 467-512) — the six numbered steps and the per-phase surface table row for Phase 20. This is the protocol, not a suggestion.
    - `swayosd/.config/swayosd/style.css` — the only file in the swayosd stow package.
    - `wleave/.config/wleave/layout.json` — the SOLE source of the six action command strings and their `l/e/u/h/r/s` mnemonics (D-20-26); deleted with the package.
    - `wleave/.config/wleave/style.css` — the six hue capsules being redesigned away.
    - `hypr/.config/hypr/scripts/wleave.sh` — the launcher all three entry points call today.
    - `hypr/.config/hypr/config/autostart.lua` lines 185-200 — `swayosd-server` autostart and the comment distinguishing it from the libinput backend.
    - `.planning/phases/20-indicators-power-menu/20-RESEARCH.md` § "Priority Research Findings" item 6 — the already-verified excerpts, so this task confirms rather than re-derives.
  </read_first>
  <action>
Create `.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md` following the six
steps of 18-BEHAVIOUR-BASELINE.md's GATE-01 Recurrence Protocol, surface-agnostic discipline
carried over unchanged.

Record, per surface:

**swayosd** — the layer-shell anchor and edge margin and the fact that both are SwayOSD
built-ins and NOT CSS-configurable (its `style.css` header says so); `#container` background
`alpha(@background, 0.85)`; the `16px` margin/padding; the `999px` full-pill border radius; icon
scale `1.2x`; label `FiraCode Nerd Font` at 16px; progress track `alpha(@surface_variant, 0.6)`
at `6px` min-height with a solid `@primary` fill. Record its two unit halves separately and by
name: `swayosd-libinput-backend.service` (system-level, `graphical.target.wants/`, enabled,
running) and the per-session `swayosd-server` started at `autostart.lua:192` via
`hl.exec_cmd("uwsm app -- swayosd-server")`. Record the six `swayosd-client` invocations at
`keybinds.lua:297-308` verbatim including their `locked = true` and `repeating = true` flags.

**wleave** — the six `layout.json` entries transcribed VERBATIM as `label` / `action` / `text` /
`keybind` quadruples. These six action strings exist nowhere else in the repo and are deleted
with the package (D-20-26), so this transcription is the migration source plan 20-06 reads.
Record also `buttons-per-row: 6`, `show-keybinds: false`, `margin: 36.4%`,
`column-spacing: 24`, and wleave's three layer rules in `windowrules.lua` (blur,
`animation = "fade"`, `ignore_alpha = 0.25`).

**wlogout / eww** — the `pacman -Qi` facts only: installed version, install date, explicit-vs-
dependency install reason, and whether the package has an install script. Neither has a config
tree in this repo; that absence is itself a recorded finding, not an omission.

Follow protocol step 3: the output is **checkable criteria in gesture-and-observation form**,
never a feature narrative — the consumer is a human running the replacement side by side at
GATE-02. Follow protocol step 6: this is **never a port specification** — record the capability
the user had, not the pixel/colour/duration it had it at. Add a `## Not a Port Specification`
section naming the concrete trap for this phase: wleave's six hue capsules and swayosd's flat
unthemed pill are the exact aesthetics D-20-25 redesigns AWAY from, so nothing in this document
authorises reproducing them.

Add a `## Dead Definitions` section (protocol step 5) for any capability that is a deliberate
prior cut rather than a live capability — at minimum, `show-keybinds: false` (mnemonics work but
are undisplayed today, which D-20-24 deliberately reverses).

Add a `## Unaccounted Keys` section (protocol step 4) asserting completeness MECHANICALLY: a
runnable loop over every top-level key of `layout.json` and every top-level selector of both
`style.css` files, with the unaccounted set shown as explicitly empty. Do not write a sentence
claiming the enumeration felt thorough.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md &amp;&amp; for s in lock logout suspend hibernate reboot shutdown; do grep -qF "\"$s\"" .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md || { echo "MISSING wleave action: $s"; exit 1; }; done &amp;&amp; grep -qF 'swayosd-libinput-backend.service' .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md &amp;&amp; grep -qF 'autostart.lua:192' .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md &amp;&amp; grep -q '^## Unaccounted Keys' .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md &amp;&amp; grep -q '^## Not a Port Specification' .planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-BEHAVIOUR-BASELINE.md` exists and contains all six wleave `label` values (`lock`, `logout`, `suspend`, `hibernate`, `reboot`, `shutdown`) with their verbatim `action` strings.
    - The document contains the literal strings `swayosd-libinput-backend.service` and `autostart.lua:192`, recording the two-unit split.
    - The document contains `## Unaccounted Keys`, `## Dead Definitions` and `## Not a Port Specification` sections.
    - The `## Unaccounted Keys` section shows the output of a runnable key-enumeration loop with an explicitly empty unaccounted set for each of the three source files — not a prose completeness claim.
    - No section of the document instructs reproducing wleave's six hue capsules or swayosd's pill geometry as a target.
  </acceptance_criteria>
  <done>The live behaviour of both retiring surfaces is transcribed in gesture-and-observation form, with the six wleave action strings captured before the file that holds them is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the pre-deletion consumer sweep for all four retirement targets</name>
  <files>.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md</files>
  <read_first>
    - `hypr/.config/hypr/scripts/retirement-check` header (lines 1-60) — the usage block, the two-tier `[FAIL]`/`[REPORT]` split (D-18-37), and the `--root` / `--list` / `--all` verbs.
    - `hypr/.config/hypr/scripts/quickshell-doctor` — the `panel-swayosd-key-ownership` check body and its helpers `_qsd_swayosd_server_reachable` / `_qsd_osd_activation_count`, which instrument the literal layer namespace `swayosd` and invoke `swayosd-client --output-volume raise`.
    - `hypr/.config/hypr/scripts/motion-lint` — its `swayosd/style.css` exemption entry, its `wleave/style.css button:hover,button:focus` carve-out, its wleave `animation-delay` carve-out, and its `$HOME/.config/swayosd` + `$HOME/.config/wleave` deployed-directory list.
    - `theme-engine/.config/theme-engine/theme-stress-test` — the `REPRESENTATIVE_FILES` array, which names `wleave.css` as a representative of the `gtk-css` format.
    - `theme-engine/.config/theme-engine/theme-doctor` — the `GTK4_CSS_SHEETS` array containing `$HOME/.config/swayosd/style.css` and `$HOME/.config/wleave/style.css`.
    - `theme-engine/.config/theme-engine/contract.json` — the 20-entry `files` array containing `wleave.css` and `swayosd.css`.
  </read_first>
  <action>
Create `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md`.

Run `hypr/.config/hypr/scripts/retirement-check <surface>` once for each of `swayosd`, `wleave`,
`wlogout`, `eww` and commit the RAW per-class output for all four, verbatim, under one
`## Pre-deletion retirement-check output` section with a fenced block per surface. Record each
surface's exit code alongside its block. This is the "before" half of the pair; plans 20-09 and
20-10 re-run the identical command for the "after" half.

Then write a `## Reference sites not in 20-CONTEXT.md's canonical_refs` section. CONTEXT.md's
list is authoritative but not exhaustive — this sweep's job is to find what it missed. Confirm
and record at minimum these sites, each with file, line and the reference class it belongs to:

- **checker-internals (swayosd)** — `quickshell-doctor`'s `panel-swayosd-key-ownership` check:
  its `osd-differential` sub-check literally polls `hyprctl layers -j` for a layer whose
  `namespace == "swayosd"` and drives it with `swayosd-client --output-volume raise`, and its
  `source-holds-by-construction` sub-check greps the panel QML dir for `swayosd-client` refs.
  Deleting swayosd without re-instrumenting this check turns a green GATE-03 gate red.
- **checker-internals (swayosd)** — `motion-lint`'s `swayosd/style.css` exemption entry and its
  `$HOME/.config/swayosd` deployed-directory entry.
- **checker-internals (wleave)** — `motion-lint`'s `wleave/style.css button:hover,button:focus`
  carve-out, its wleave `animation-delay` carve-out comment block, and its
  `$HOME/.config/wleave` deployed-directory entry.
- **checker-internals (wleave)** — `theme-stress-test`'s `REPRESENTATIVE_FILES` array. Record
  that the array already contains `gtk-4.0-colors.css` as a second `gtk-css` entry, so the
  format stays represented once `wleave.css` is removed — i.e. the correct fix is a removal, not
  a repoint. Record this explicitly so plan 20-10 does not invent a repoint.
- **test-fixtures (swayosd)** — `hypr/.config/hypr/scripts/tests/quickshell-fixtures/compliant-hyprctl-binds.txt`, `poisoned-missing-xf86-binds.txt`, `poisoned-duplicate-xf86-binds.txt` — all three carry `swayosd-client` bind strings as fixture data.
- **test-fixtures (wleave)** — `hypr/.config/hypr/scripts/tests/motion-fixtures/compliant-gtk4.css`, `poisoned-raw-gtk4.css`, `poisoned-dangling-gtk4.css`.
- **repo-prose (wleave)** — the repository-root `VERIFICATION.md` line naming wleave in the
  themed-surface list. Classify as report-tier, not blocking.
- **contract/theme-engine** — `contract.json`'s `wleave.css` and `swayosd.css` entries (20 → 18
  in the `files` array), `matugen/.config/matugen/config.toml`'s `[templates.wleave]` (lines
  49-51) and `[templates.swayosd]` (lines 79-81) plus the two template files they point at,
  `theme-doctor`'s two `GTK4_CSS_SHEETS` entries, `lib/reload.sh`'s swayosd-server
  kill-and-relaunch block, and `lib/gtk.sh`'s comment referencing both sheets.
- **install/stow** — `install.sh` swayosd entry, wleave entry, and the
  `systemctl enable --now swayosd-libinput-backend.service` block; `stow.sh` swayosd and wleave
  entries.

For each site record: `path:line | reference class | which surface | disposition (delete / edit /
re-instrument / report-only)`. The disposition column is what plans 20-09 and 20-10 execute
from — a site with no disposition is an unclosed hole.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md &amp;&amp; for s in swayosd wleave wlogout eww; do grep -qF "retirement-check $s" .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md || { echo "MISSING baseline run: $s"; exit 1; }; done &amp;&amp; grep -qF 'panel-swayosd-key-ownership' .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md &amp;&amp; grep -qF 'REPRESENTATIVE_FILES' .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md &amp;&amp; grep -qF 'compliant-hyprctl-binds.txt' .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md &amp;&amp; grep -qF 'compliant-gtk4.css' .planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-RETIREMENT-BASELINE.md` contains a committed raw `retirement-check` output block and recorded exit code for each of the four surfaces `swayosd`, `wleave`, `wlogout`, `eww`.
    - The document names `panel-swayosd-key-ownership`, `REPRESENTATIVE_FILES`, `compliant-hyprctl-binds.txt` and `compliant-gtk4.css` as reference sites, each with a disposition.
    - Every listed reference site row carries a non-empty disposition value from the set {delete, edit, re-instrument, report-only}.
    - The `theme-stress-test` row states that removal (not repoint) is correct because `gtk-4.0-colors.css` already represents the `gtk-css` format in the same array.
  </acceptance_criteria>
  <done>A complete, committed pre-deletion reference inventory exists for all four surfaces, with per-site dispositions that plans 20-09 and 20-10 execute from.</done>
</task>

<task type="auto">
  <name>Task 3: Take the three live observations no in-session agent can reach</name>
  <files>none — live observation only; results are reported back and recorded by Task 4</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-CONTEXT.md` D-20-17, D-20-18, D-20-19 — the two open questions and what each verdict authorises or blocks.
    - `.planning/phases/20-indicators-power-menu/20-RESEARCH.md` § "Priority Research Findings" item 2 and § "Open Questions" item 1 — the already-verified node permissions (`644 root:root`, readable unprivileged), the `kbd-capslock` kernel trigger binding, and the honest record that this session's own `select.poll()` test was inconclusive because the write trigger was blocked by missing passwordless sudo.
    - `hypr/.config/hypr/config/autostart.lua` lines 185-200 — the split between `swayosd-libinput-backend.service` (system-level) and `swayosd-server` (started per-session at line 192), which is what makes Observation 1's answer non-obvious.
  </read_first>
  <action>
Nothing is built here. Three observations must be taken on live hardware and at contexts no
in-session agent can reach: the SDDM greeter (pre-session), the hyprlock lock surface, and a real
physical Caps Lock key press. All three were attempted during research and all three were blocked
by the environment, not by reasoning.

Prepare whatever minimal diagnostic each observation needs (for Observation 3, a throwaway
`FileView{watchChanges:true}` watcher pointed at the resolved node, or an equivalent
`inotifywait`-class watcher if one is available), hand the procedure below to the operator, and
record the three literal answers verbatim for Task 4 to consume. Do not interpret them here and do
not proceed past a missing answer by inferring it.
  </action>
  <verify>
    <human-check>
**Observation 1 — SDDM greeter Caps Lock (D-20-17, decides RETIRE-04's libinput-backend half).**
Log out to the SDDM greeter. At the login prompt, press Caps Lock. Record literally: did ANY
on-screen indicator appear? Yes / No / Could-not-determine. Note also whether the keyboard's own
LED lit (that is hardware, not swayosd — do not confuse the two).
Ground truth already established: `sddm.service` is enabled, `swayosd-libinput-backend.service`
is enabled at system level and running, but `swayosd-server` — the thing that renders — is only
started inside the Hyprland session at `autostart.lua:192`. The prediction is that the backend's
pre-session reach is already dead. This observation converts the prediction into evidence.

**Observation 2 — SwayOSD over hyprlock (D-20-19, settles QOSD-01's lock-screen clause).**
Lock the session (`loginctl lock-session` or the existing Lock keybind). While hyprlock is up,
press a volume key. Record literally: did the SwayOSD pill appear over the lock surface?
Yes / No.

**Observation 3 — sysfs Caps Lock watch (RESEARCH Open Question 1, decides QOSD-02's mechanism).**
In a terminal: `ls -l /sys/class/leds/*::capslock/brightness` to resolve the current node (the
`inputNN` index changes across boots — it was `input5` on 2026-08-14 and `input33` on
2026-08-15, which is itself D-20-14's premise re-confirmed). Then run a watcher on that exact
path and press the physical Caps Lock key twice (on, then off). Record literally: did the
watcher observe a change event on each transition? Yes / No / Only-on-one-edge.
Use `wtype` for NOTHING here — a virtual keyboard event does not drive the kernel's real
evdev/LED-classdev path and would produce an untrustworthy answer.

Report all three results back verbatim, in the form `1: yes|no|undetermined`, `2: yes|no`,
`3: yes|no|only-one-edge`, plus the resolved sysfs node path. Do not interpret them; the next task
records them.
    </human-check>
  </verify>
  <acceptance_criteria>
    - All three observations are reported back with a literal outcome token, not an inference.
    - Observation 1 distinguishes an on-screen indicator from the keyboard's own hardware LED lighting — those are different things and confusing them would produce a wrong RETIRE-04 verdict.
    - Observation 3 reports the resolved `/sys/class/leds/*::capslock/brightness` path observed on this boot, and confirms the trigger was a physical key press rather than `wtype` or a root write.
    - Any observation that could not be taken is reported as `undetermined` with its reason, never converted into a verdict.
  </acceptance_criteria>
  <done>Three literal observations are in hand — the SDDM greeter's behaviour, hyprlock's behaviour, and whether the sysfs watch actually fires — each taken rather than inferred.</done>
</task>

<task type="auto">
  <name>Task 4: Record the three measurements and their consequences; write COVERAGE.md</name>
  <files>.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md, .planning/phases/20-indicators-power-menu/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-CONTEXT.md` § "`swayosd-libinput-backend.service` — the named scope call (RETIRE-04)" (D-20-17, D-20-18) and § "QOSD-01's lock-screen clause" (D-20-19, D-20-20).
    - `.planning/phases/20-indicators-power-menu/20-RESEARCH.md` § "Open Questions" items 1 and 3, and § "Assumptions Log" row A1.
    - `hypr/.config/hypr/config/keybinds.lua` lines 293-314 — the `locked = true` media-key binds that QOSD-01's in-session half already rests on.
  </read_first>
  <action>
Create `.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md` with one section
per observation. Each section records: the question, the exact procedure run, the literal
observed outcome, the date, and the consequence.

**§ SDDM greeter Caps Lock (D-20-17).** Record the observed outcome verbatim. Then record the
consequence per D-20-17/D-20-18:
- Observed NO indicator → RETIRE-04 proceeds including `swayosd-libinput-backend.service`, and
  the record states that its pre-session reach was measured dead, not assumed dead. Plan 20-09
  reads this verdict.
- Observed AN indicator → record `RETIRE-04: BLOCKED`. The backend cannot run without the
  `swayosd` package, so this is a scope decision to escalate to the developer, NOT something to
  work around (D-20-18 is explicit). Plan 20-09 must not proceed on a BLOCKED verdict.
- Could-not-determine → record `UNTAKEN` with the reason. Do NOT convert an untaken observation
  into a verdict.

**§ SwayOSD over hyprlock (D-20-19).** Record the observed outcome verbatim. Then:
- Pill did NOT appear → amend QOSD-01 WITH THE EVIDENCE: its real content is "the keys keep
  working while locked," which is already true via the `locked = true` binds at
  `keybinds.lua:297-308`; the clause is settled, not chased. Cite the keybind line range in the
  amendment so the evidence travels with it.
- Pill DID appear → record that the clause is a genuine capability the phase must solve, and
  flag it to the developer as scope this plan set did not size for.
Record that putting the readout inside hyprlock's own config was already considered and rejected
(D-20-20) so the amendment does not reopen it.

**§ sysfs Caps Lock watch (RESEARCH Open Question 1 / Assumption A1).** Record the resolved node
path observed on this boot, the observed watch behaviour, and the consequence:
- Fired on both edges (or at least on the ON edge) → QOSD-02's event-driven mechanism is
  confirmed viable; plan 20-05 Task 2 implements it as specified with no fallback.
- Did NOT fire → record the negative result plainly and flag it to the developer as a SCOPE
  CONVERSATION: the fallback is a polling `Timer`, which costs the phase its zero-idle claim for
  this one indicator. Do not pre-authorise the substitution here.
Record the observed node path alongside the 2026-08-14 (`input5`) and 2026-08-15 (`input33`)
observations as a third data point confirming D-20-14's glob-at-startup / re-glob-on-failure
design, and note explicitly that a stable-path implementation is therefore ruled out by
measurement, not by prediction.

Also create `.planning/phases/20-indicators-power-menu/COVERAGE.md` containing exactly the
reasoned single-line declaration form the API-coverage seal gate accepts:
`No external API integration: <reason>` — the reason being that this phase builds local
Quickshell/QML layer-shell surfaces driving already-installed system binaries (`wpctl`,
`brightnessctl`, `systemctl`, `hyprshutdown`, `uwsm`, `pgrep`) and a world-readable kernel sysfs
read, with no network, no SDK and no third-party service surface. Do not fabricate a capability
matrix for an API that does not exist.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md &amp;&amp; test -f .planning/phases/20-indicators-power-menu/COVERAGE.md &amp;&amp; grep -q 'D-20-17' .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md &amp;&amp; grep -q 'D-20-19' .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md &amp;&amp; grep -qE 'capslock/brightness' .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md &amp;&amp; grep -cE '^(Observed|\*\*Observed)' .planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md | grep -qvE '^0$' &amp;&amp; grep -q '^No external API integration:' .planning/phases/20-indicators-power-menu/COVERAGE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-GATE-01-MEASUREMENTS.md` has three sections, each citing its decision ID (`D-20-17`, `D-20-19`, and RESEARCH Open Question 1 / Assumption A1).
    - Each section records a literal observed outcome and a consequence; no section states an outcome as "expected", "assumed", "should be" or "not applicable".
    - The SDDM section records exactly one of the three verdicts: RETIRE-04 proceeds / `RETIRE-04: BLOCKED` / `UNTAKEN` with a reason.
    - The sysfs section records the resolved `/sys/class/leds/*::capslock/brightness` node path observed on this boot.
    - `COVERAGE.md` begins a line with the literal `No external API integration:` followed by a non-empty reason.
  </acceptance_criteria>
  <done>All three open questions carry a recorded observation and a stated consequence, and the API-coverage seal gate has a reasoned declaration to read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel sysfs → shell | `/sys/class/leds/*::capslock/brightness` is root-owned, world-readable (`644 root:root`); the shell reads it and must never write it |
| greeter/lock context → session | SDDM and hyprlock run outside the Hyprland session; observations taken there cross a context this shell does not own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01-01 | Information Disclosure | GATE-01 measurement record | low | mitigate | The measurement record contains no credentials or host secrets — only device node paths and yes/no observations. Task 4 records the node path only, never `udevadm` vendor/serial output. |
| T-20-01-02 | Tampering | `/sys/class/leds/*::capslock/brightness` | low | mitigate | Observation 3 is a READ-only watch plus a physical key press. No `sudo`, no write to the node, no `wtype` synthetic injection — asserted by the checkpoint's own procedure text. |
| T-20-01-SC | Tampering | package-manager installs | low | accept | This phase installs no packages — RESEARCH.md's Package Legitimacy Audit records the phase as removals-only (`swayosd`, `wleave`, `wlogout`, `eww`), and no npm/pip/cargo ecosystem is involved. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `20-BEHAVIOUR-BASELINE.md`, `20-RETIREMENT-BASELINE.md`, `20-GATE-01-MEASUREMENTS.md` and `COVERAGE.md` all exist and are committed.
- `hypr/.config/hypr/scripts/retirement-check swayosd` re-run reproduces the committed baseline block (the tree has not changed under it).
- No `.qml`, `.lua`, `.sh`, `.css` or `.toml` file in the repo was modified by this plan — it is a read-and-record plan only: `git diff --name-only <base>..HEAD -- ':!.planning/'` is empty.
</verification>

<success_criteria>
- The six wleave action command strings are captured in a committed file before the file that holds them is deleted.
- A raw pre-deletion `retirement-check` run exists for all four surfaces, with per-site dispositions covering the checker-internals and test-fixture classes CONTEXT.md's canonical_refs did not list.
- All three open questions carry a literal recorded observation and a stated consequence; none is defaulted into.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files:**
- `.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md`
- `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md`
- `.planning/phases/20-indicators-power-menu/20-GATE-01-MEASUREMENTS.md`
- `.planning/phases/20-indicators-power-menu/COVERAGE.md`

**New symbols/sections other plans read by name:**
- `20-GATE-01-MEASUREMENTS.md` § SDDM greeter Caps Lock → verdict token one of `RETIRE-04 proceeds` / `RETIRE-04: BLOCKED` / `UNTAKEN` (read by plan 20-09)
- `20-GATE-01-MEASUREMENTS.md` § sysfs Caps Lock watch → verdict token `fired` / `did-not-fire` (read by plan 20-05)
- `20-RETIREMENT-BASELINE.md` § Reference sites — the disposition table (read by plans 20-09, 20-10)

**Deleted symbols:** none.
</artifacts_produced>

<flagged_assumptions>
Edge-probe items surfaced but NOT authored into `must_haves` (probe category `unclassified`
stays `unresolved` and is surfaced here rather than silently auto-resolved):

- **RETIRE-04 / unclassified** — the deterministic probe could not classify an edge for
  "`swayosd` is removed the same way, including its libinput backend service". Planner
  assumption: the "same way" clause inherits RETIRE-01's before/after zero-hit checklist
  semantics verbatim, and the libinput-backend half is gated on D-20-17's measurement rather
  than on any edge condition. Carried forward to plan 20-09.
- **RETIRE-05 / unclassified** and **RETIRE-07 / unclassified** — same shape; carried forward to
  plan 20-10.
</flagged_assumptions>

<output>
Create `.planning/phases/20-indicators-power-menu/20-01-SUMMARY.md` when done.
</output>
