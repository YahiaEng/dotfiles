---
phase: 20-indicators-power-menu
plan: 10
type: execute
wave: 7
depends_on: ["20-08", "20-09"]
files_modified:
  - wleave/.config/wleave/layout.json
  - wleave/.config/wleave/style.css
  - hypr/.config/hypr/scripts/wleave.sh
  - hypr/.config/hypr/config/windowrules.lua
  - hypr/.config/hypr/config/autostart.lua
  - matugen/.config/matugen/config.toml
  - matugen/.config/matugen/templates/wleave-colors.css
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/theme-doctor
  - theme-engine/.config/theme-engine/theme-stress-test
  - theme-engine/.config/theme-engine/lib/gtk.sh
  - hypr/.config/hypr/scripts/motion-lint
  - hypr/.config/hypr/scripts/retirement-check
  - hypr/.config/hypr/scripts/tests/motion-fixtures/compliant-gtk4.css
  - hypr/.config/hypr/scripts/tests/motion-fixtures/poisoned-raw-gtk4.css
  - hypr/.config/hypr/scripts/tests/motion-fixtures/poisoned-dangling-gtk4.css
  - install.sh
  - stow.sh
  - VERIFICATION.md
autonomous: false
requirements: [RETIRE-05, RETIRE-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Nothing is deleted before Gate B has passed. Plan 20-08's `20-GATE-02-B-RECORD.md` is read first and its `git diff --quiet <sha> -- quickshell/.config/quickshell/` interlock is re-asserted."
    - "`wleave` leaves repo and host — package, stow tree, launcher script, contract entry, matugen template, layer rules, checker exemptions and test fixtures — in ONE config-then-package commit (RETIRE-05, WINDOWS #1 precedent)."
    - "`wlogout` and `eww` — repo-retired in earlier milestones but confirmed STILL INSTALLED on this host (`wlogout 1.2.2-0`, `eww 0.6.0-1`, both explicitly installed) — are uninstalled through the same checklist, as the roadmap's single `pacman -Rns` covering both leftovers. They ride this deletion because it lands second (D-20-42, RETIRE-07)."
    - "`theme-stress-test`'s `REPRESENTATIVE_FILES` array loses `wleave.css` by REMOVAL, not repoint: the array already contains `gtk-4.0-colors.css`, so the `gtk-css` format stays represented. Deleting the contract entry without handling this array would break a checker that has nothing to do with the surface."
    - "`retirement-check` reports zero blocking hits AFTER deletion for each of `wleave`, `wlogout` and `eww`, measured against the pre-deletion baselines plan 20-01 committed."
    - "`contract.json` reaches 18 `files` entries after this plan (20 before the phase, 19 after plan 20-09's `swayosd.css`, 18 after `wleave.css`), with `theme-doctor` and `theme-parity` green and no orphaned entry."
    - "`theme-stress-test` reaches a full clean run — the gate plan 20-09 deliberately deferred to this plan because its `REPRESENTATIVE_FILES` array was still mid-change."
    - "`retirement-check`'s registry flips `wleave`, `wlogout` and `eww` from `status=pending` to `status=retired`."
  artifacts:
    - .planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md
  key_links:
    - "`20-GATE-02-B-RECORD.md` § Deletion Authorisation ↔ this plan's Task 1 pre-flight."
    - "`20-RETIREMENT-BASELINE.md`'s disposition table ↔ Tasks 2 and 3 — every wleave/wlogout/eww row's disposition is executed and ticked off."
    - "`theme-stress-test`'s `REPRESENTATIVE_FILES` ↔ `contract.json`'s `gtk-css` entries — removing `wleave.css` from one requires the other still to represent that format, which `gtk-4.0-colors.css` does."
  prohibitions:
    - "MUST NOT delete any part of `wleave` if `20-GATE-02-B-RECORD.md` records a FAIL on any criterion."
    - "MUST NOT split config removal and package removal across two commits, for any of the three packages."
    - "MUST NOT repoint `theme-stress-test`'s `REPRESENTATIVE_FILES` entry to a newly-invented file. The array already carries a second `gtk-css` representative; the correct edit is a removal."
---

<objective>
Remove `wleave` in one config-then-package commit behind Gate B's passing verdict, then uninstall
the `wlogout` and `eww` leftovers through the same checklist, and close the phase with every gate
green.

Purpose: RETIRE-05 is the second of this phase's two gate-unlocked deletions, and RETIRE-07 rides
it because this deletion lands second (D-20-42). `wlogout` is also the direct lineage of the power
menu being replaced — it was the surface `wleave` itself replaced — so closing both here is the
roadmap's own sequencing, not an add-on.

Output: `wleave`, `wlogout` and `eww` gone from repo and host, `contract.json` at 18 entries, and
`20-RETIRE-05-07-RECORD.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md
@.planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md
@.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md
@.planning/phases/20-indicators-power-menu/20-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — Gate B verdict, interlock, and removal previews for all three packages</name>
  <precondition>`20-GATE-02-B-RECORD.md` exists and records no FAIL across its ten criteria. If it records any FAIL, HALT and report — do not proceed to Task 2.</precondition>
  <files>.planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-GATE-02-B-RECORD.md` — the ten criteria verdicts, the judged sha, and the `## Deletion Authorisation` section.
    - `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md` — the wleave, wlogout and eww rows of the disposition table and the three committed pre-deletion `retirement-check` outputs.
    - `.planning/phases/20-indicators-power-menu/20-08-SUMMARY.md` — the checkpoint resolution recording which deletions were authorised and at which shas.
    - `.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md` — plan 20-09's record, in particular its `theme-stress-test` deferral naming this plan as owner.
  </read_first>
  <action>
Halt-or-proceed pre-flight. Create
`.planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md` with a `## Pre-flight`
section recording each item and its literal result:

1. **Gate B verdict** — all ten criteria, including criterion 10 (the Phase 15 "who owns the
   prompt" security carry-over) and criterion 1's `quickshell-session` alpha-prediction outcome.
   Any FAIL halts.
2. **Interlock** — `git diff --quiet <judged-sha> -- quickshell/.config/quickshell/` using the sha
   named in the Gate B record. Non-zero exit means the shell tree moved after the judgment; halt
   and report rather than re-authorising.
3. **Removal impact previews** — run `pacman -Rns --print` for each of `wleave`, `wlogout` and
   `eww` separately, and record each full list. Halt if any preview names a package beyond the
   target and its own orphans. `eww` in particular carries an install script (recorded in plan
   20-01's baseline), so read what its removal will run.
4. **Reverse-dependency checks** — `pactree -r wleave`, `pactree -r wlogout`, `pactree -r eww`.
   Anything depending on any of the three halts.
5. **Baseline recall** — the pre-deletion `retirement-check` hit counts for all three surfaces from
   `20-RETIREMENT-BASELINE.md`, so Task 3's after-runs have explicit numbers to be measured
   against.
6. **Live installed-state confirmation** — `pacman -Qi wlogout` and `pacman -Qi eww` to confirm both
   are still installed on this host. RETIRE-07's whole premise is that they were repo-retired in
   earlier milestones but never uninstalled; if either is already gone, record that and narrow the
   removal accordingly rather than failing on it.

Make no repository or host change in this task.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md; test -f "$R"; grep -q '^## Pre-flight' "$R"; for p in wleave wlogout eww; do grep -qF "pacman -Rns --print $p" "$R" || { echo "missing removal preview for $p"; exit 1; }; grep -qF "pactree -r $p" "$R" || { echo "missing reverse-dep check for $p"; exit 1; }; done; grep -qF 'git diff --quiet' "$R"; git diff --quiet HEAD -- wleave/ matugen/ theme-engine/ hypr/ install.sh stow.sh VERIFICATION.md || { echo "pre-flight must not modify the tree"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-RETIRE-05-07-RECORD.md` has a `## Pre-flight` section recording all six items with literal results.
    - The Gate B interlock command was run and its exit code recorded.
    - A `pacman -Rns --print` preview and a `pactree -r` output are recorded verbatim for each of `wleave`, `wlogout` and `eww`.
    - `eww`'s install-script presence is noted, with what its removal will run.
    - The three pre-deletion `retirement-check` hit counts are recorded as the numbers Task 3 measures against.
    - `git diff --quiet HEAD` over the config trees confirms this task changed nothing.
  </acceptance_criteria>
  <done>Gate B's verdict, the interlock and three scoped removal previews are recorded, or the plan halted with the specific reason.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Execute the concrete `wleave`, `wlogout` and `eww` removals that Task 1's three impact previews just produced.</decision>
  <context>
Plan 20-08 authorised RETIRE-05 and RETIRE-07 in principle, before these previews existed. This
checkpoint is not a re-ask of that decision — it is the last look at information that did not
exist when the authorisation was given: three literal `pacman -Rns --print` package lists, three
`pactree -r` reverse-dependency outputs, `eww`'s install-script removal hook, and the Gate B
interlock's exit code, all captured in `20-RETIRE-05-07-RECORD.md` § Pre-flight.

Two distinct hazards live in those previews. First, `-s` removes now-orphaned dependencies across
three packages at once, so a shared dependency can be pulled by one removal that the other two
would have kept. Second, `eww` carries an install script whose removal hook runs during
uninstall — read what it does before it runs, not after.

This is the point of no return for the second half of this phase: the next tasks delete the
`wleave` stow tree (including `layout.json`, the sole source of the six action command strings),
its launcher script, its contract entry, its matugen template, its three layer rules, four checker
references and three test fixtures, and uninstall all three packages.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with all three removals exactly as previewed</name>
      <pros>All three previews are scoped to their own targets and orphans, nothing depends on any of them, Gate B passed at an unchanged sha, and the six action strings are already migrated into `PowerMenu.qml` and transcribed in the behaviour baseline. RETIRE-05 and RETIRE-07 both complete.</pros>
      <cons>Irreversible without reinstalling three packages and re-adding every reference the same commits delete.</cons>
    </option>
    <option id="proceed-wleave-only">
      <name>Proceed with `wleave` only; hold `wlogout`/`eww`</name>
      <pros>Useful if one of the leftovers' previews names a shared dependency. RETIRE-05 completes on its own gate, which is exactly what D-20-42's per-gate design allows.</pros>
      <cons>RETIRE-07 defers to a follow-up; two packages this milestone intended to remove stay installed.</cons>
    </option>
    <option id="halt-scope">
      <name>Halt — a preview names a package beyond its target and that target's own orphans</name>
      <pros>Prevents `-s` quietly removing something another part of this desktop needs, across a three-package removal where the blast radius is widest.</pros>
      <cons>Both requirements defer until the removals are narrowed.</cons>
    </option>
    <option id="halt-gate">
      <name>Halt — the Gate B interlock failed or a Gate B criterion recorded FAIL</name>
      <pros>A moved shell tree invalidates Gate B's judgment; nothing irreversible happens while a replacement is judged worse than what it replaced.</pros>
      <cons>Both requirements defer pending gap closure and a re-run gate.</cons>
    </option>
  </options>
  <resume-signal>Reply `proceed`, `proceed-wleave-only`, `halt-scope`, or `halt-gate`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: One config-then-package commit — every wleave reference class, then the package</name>
  <reversibility rating="one-way">`pacman -Rns wleave` on this host cannot be undone without reinstalling a package this milestone exists to remove, together with its stow tree, its launcher script, its contract entry, its matugen template, its three layer rules and every checker exemption the same commit deletes — and `layout.json`, the sole source of the six action command strings, goes with it. Authorised at plan 20-08's `checkpoint:decision`; the rating is carried here for the record, not re-gated.</reversibility>
  <files>wleave/.config/wleave/layout.json, wleave/.config/wleave/style.css, hypr/.config/hypr/scripts/wleave.sh, hypr/.config/hypr/config/windowrules.lua, hypr/.config/hypr/config/autostart.lua, matugen/.config/matugen/config.toml, matugen/.config/matugen/templates/wleave-colors.css, theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/theme-doctor, theme-engine/.config/theme-engine/theme-stress-test, theme-engine/.config/theme-engine/lib/gtk.sh, hypr/.config/hypr/scripts/motion-lint, hypr/.config/hypr/scripts/retirement-check, hypr/.config/hypr/scripts/tests/motion-fixtures/compliant-gtk4.css, hypr/.config/hypr/scripts/tests/motion-fixtures/poisoned-raw-gtk4.css, hypr/.config/hypr/scripts/tests/motion-fixtures/poisoned-dangling-gtk4.css, install.sh, stow.sh, VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md` — the wleave disposition rows. Every row's disposition is executed here.
    - `.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md` — confirm the six action quadruples are transcribed there and that plan 20-06 migrated them into `PowerMenu.qml`. `layout.json` is the sole source of those strings and it disappears in this commit; if either the transcription or the migration is missing, HALT.
    - `theme-engine/.config/theme-engine/contract.json` — the `files` array (19 entries after plan 20-09) containing `{ "name": "wleave.css", "format": "gtk-css" }` alongside `gtk-3.0-colors.css` and `gtk-4.0-colors.css`, both also `gtk-css`.
    - `theme-engine/.config/theme-engine/theme-stress-test` around line 314 — `REPRESENTATIVE_FILES=(hyprland-tokens.lua wleave.css gtk-4.0-colors.css kitty.conf)`. Note that `gtk-4.0-colors.css` is ALREADY in the array as a second `gtk-css` representative.
    - `theme-engine/.config/theme-engine/theme-doctor` — the `GTK4_CSS_SHEETS` entry `$HOME/.config/wleave/style.css`.
    - `matugen/.config/matugen/config.toml` lines 49-51 — `[templates.wleave]` and the template path.
    - `hypr/.config/hypr/config/windowrules.lua` lines 216-292 and 340-440 — wleave's three layer rules (blur, `animation = "fade"`, `ignore_alpha = 0.25`) and their recorded tuning history. Read the tuning-history comments before deleting: they document findings about this file's own ordering semantics that outlive wleave and should be preserved as comments even where the rules go.
    - `hypr/.config/hypr/scripts/motion-lint` — the `wleave/style.css button:hover,button:focus` carve-out (around lines 441-451), the wleave `animation-delay` carve-out (around lines 688-692), the CHECK A/B coverage note (around lines 381-383), and the `$HOME/.config/wleave` deployed-directory entry (around line 1156).
    - `hypr/.config/hypr/scripts/tests/motion-fixtures/compliant-gtk4.css`, `poisoned-raw-gtk4.css`, `poisoned-dangling-gtk4.css` — read what each fixture asserts before editing. These are fixture DATA driving `motion-lint`'s self-test.
    - `theme-engine/.config/theme-engine/lib/gtk.sh` around line 311 — the remaining wleave half of the comment plan 20-09 partially edited.
    - `install.sh` around line 317 (the wleave package entry); `stow.sh` around line 36 (the wleave stow entry); `hypr/.config/hypr/config/autostart.lua` (any residual wleave reference); the repository-root `VERIFICATION.md` line naming wleave in the themed-surface list.
    - `hypr/.config/hypr/scripts/retirement-check` — the registry rows for `wleave`, `wlogout` and `eww`.
  </read_first>
  <action>
Stage every config-side removal, then remove the package, then commit all of it together — config
then package, one commit.

**Repo side:**
- Delete the `wleave/` stow tree (`layout.json` and `style.css`) and
  `hypr/.config/hypr/scripts/wleave.sh`. Confirm first that the six action strings are transcribed
  in `20-BEHAVIOUR-BASELINE.md` AND migrated into `PowerMenu.qml`; `layout.json` is the only place
  they exist and this commit is where they stop existing.
- Remove `{ "name": "wleave.css", "format": "gtk-css" }` from `contract.json` (19 → 18 entries).
- Remove `wleave.css` from `theme-stress-test`'s `REPRESENTATIVE_FILES` array — a REMOVAL, not a
  repoint. The array already carries `gtk-4.0-colors.css`, so the `gtk-css` format stays
  represented after the removal and nothing needs inventing. Record that reasoning in a comment
  beside the array so nobody re-adds a placeholder.
- Remove `$HOME/.config/wleave/style.css` from `theme-doctor`'s `GTK4_CSS_SHEETS`.
- Remove `[templates.wleave]` from `matugen/config.toml` and delete
  `matugen/.config/matugen/templates/wleave-colors.css`.
- Remove wleave's three layer rules from `windowrules.lua`. PRESERVE the tuning-history comments'
  findings about this file's own ordering and alpha semantics — those findings are why this phase's
  own `quickshell-osd` override row exists and they outlive the rules they were written beside.
  Where a comment is purely about wleave's own hue capsules, it goes; where it records a
  file-level semantic, keep it and note which rule it was learned from.
- Remove the remaining wleave half of `lib/gtk.sh`'s comment.
- Remove `motion-lint`'s three wleave carve-outs and its `$HOME/.config/wleave` deployed-directory
  entry. The `button:hover,button:focus` carve-out is marked PERMANENT in its own text — that
  permanence was scoped to a file that is about to stop existing, so record in the removal that the
  fence is retired with its subject, not overridden.
- Edit the three `tests/motion-fixtures/*gtk4.css` fixtures so they no longer reference wleave.
  These are fixture DATA: `compliant-gtk4.css` must still represent a COMPLIANT sheet,
  `poisoned-raw-gtk4.css` must still carry the same raw-literal poison, and
  `poisoned-dangling-gtk4.css` must still carry the same dangling reference. Re-read each fixture's
  assertion before editing — a poisoned fixture that stops poisoning turns `motion-lint`'s
  self-test into a rubber stamp.
- Remove `install.sh`'s wleave entry and `stow.sh`'s wleave entry, plus any residual wleave
  reference in `autostart.lua`.
- Update the repository-root `VERIFICATION.md` line that lists wleave among the themed surfaces —
  this is a report-tier (prose) hit, not blocking, but leaving it makes the document assert
  something false.
- Flip `retirement-check`'s registry row for `wleave` from `status=pending` to `status=retired`.

**Host side:** `pacman -Rns wleave`, having confirmed in Task 1 that its `--print` preview names
nothing beyond wleave and its own orphans.

Commit everything as one commit whose message names RETIRE-05 and cites the Gate B record's sha as
the authorising judgment.
  </action>
  <verify>
    <automated>set -e; test ! -d wleave; test ! -f hypr/.config/hypr/scripts/wleave.sh; test ! -f matugen/.config/matugen/templates/wleave-colors.css; for f in theme-engine/.config/theme-engine/contract.json matugen/.config/matugen/config.toml theme-engine/.config/theme-engine/theme-doctor install.sh stow.sh VERIFICATION.md; do grep -ci 'wleave' "$f" | grep -qx 0 || { echo "wleave still referenced in $f"; exit 1; }; done; for f in hypr/.config/hypr/config/windowrules.lua hypr/.config/hypr/config/autostart.lua; do grep -vE '^\s*--' "$f" | grep -ci 'wleave' | grep -qx 0 || { echo "wleave still in source lines of $f"; exit 1; }; done; grep -vE '^\s*#' hypr/.config/hypr/scripts/motion-lint | grep -ci 'wleave' | grep -qx 0 || { echo "motion-lint still references wleave"; exit 1; }; for t in hypr/.config/hypr/scripts/tests/motion-fixtures/compliant-gtk4.css hypr/.config/hypr/scripts/tests/motion-fixtures/poisoned-raw-gtk4.css hypr/.config/hypr/scripts/tests/motion-fixtures/poisoned-dangling-gtk4.css; do grep -ci 'wleave' "$t" | grep -qx 0 || { echo "fixture still references wleave: $t"; exit 1; }; done; grep -q 'wleave.css' theme-engine/.config/theme-engine/theme-stress-test && { echo "REPRESENTATIVE_FILES still names wleave.css"; exit 1; }; grep -q 'gtk-4.0-colors.css' theme-engine/.config/theme-engine/theme-stress-test || { echo "gtk-css format lost its representative"; exit 1; }; hypr/.config/hypr/scripts/retirement-check --list | grep -q 'wleave .*status=retired'; pacman -Q wleave >/dev/null 2>&1 && { echo "wleave package still installed"; exit 1; }; python3 -c "import json;d=json.load(open('theme-engine/.config/theme-engine/contract.json'));n=len(d['files']);print('contract files:',n);assert n==18,n;assert not any(f['name']=='wleave.css' for f in d['files'])"; hypr/.config/hypr/scripts/hypr-lua-harness >/dev/null; hypr/.config/hypr/scripts/motion-lint >/dev/null; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `wleave/`, `hypr/.config/hypr/scripts/wleave.sh` and `matugen/.config/matugen/templates/wleave-colors.css` no longer exist.
    - `contract.json` parses and its `files` array is exactly 18 entries with no `wleave.css`.
    - `theme-stress-test`'s `REPRESENTATIVE_FILES` no longer names `wleave.css` and still names `gtk-4.0-colors.css`, with a comment recording why removal (not repoint) was correct.
    - No file listed in `files_modified` contains `wleave` in a non-comment line, and the three `tests/motion-fixtures/*gtk4.css` fixtures contain no `wleave` at all while each still asserts what it asserted before (`motion-lint` self-test green).
    - `windowrules.lua`'s wleave layer rules are gone, and its file-level ordering/alpha findings are preserved as comments with a note of which rule they were learned from.
    - `retirement-check --list` shows `wleave status=retired`; `pacman -Q wleave` fails.
    - `hypr-lua-harness` and `motion-lint` both exit 0.
    - `git log -1 --stat` shows the config edits and the deletion in ONE commit naming RETIRE-05 and the Gate B sha.
  </acceptance_criteria>
  <done>`wleave` is gone from repo and host, its six action strings live on in `PowerMenu.qml`, and the checker that named its stylesheet as a format representative still has one.</done>
</task>

<task type="auto">
  <name>Task 3: Uninstall the wlogout and eww leftovers; close the phase with the after-runs and green gates</name>
  <reversibility rating="one-way">`pacman -Rns wlogout eww` removes two packages that have had no repo tree since earlier milestones; reinstalling them would mean re-adding surfaces this project already replaced twice over. Authorised at plan 20-08's `checkpoint:decision`.</reversibility>
  <files>hypr/.config/hypr/scripts/retirement-check, .planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md</files>
  <read_first>
    - `.planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md` § Pre-flight — the removal previews, reverse-dependency checks, install-script note for `eww`, and the three baseline hit counts.
    - `.planning/phases/20-indicators-power-menu/20-RETIREMENT-BASELINE.md` — the wlogout and eww rows. Both have `own-tree=<none>` in `retirement-check`'s registry: their repo trees were already retired, and the only remaining reference class is the host package plus any prose mention.
    - `hypr/.config/hypr/scripts/retirement-check` — the `wlogout` and `eww` registry rows, and the header's note that a short common-word surface token (`eww`) can word-boundary-match unrelated prose, with the residual exposure landing almost entirely in the report tier.
    - `.planning/phases/20-indicators-power-menu/20-RETIRE-04-RECORD.md` § Post-deletion — plan 20-09's `theme-stress-test` deferral, which this task discharges.
  </read_first>
  <action>
**(a) RETIRE-07.** Run ONE `pacman -Rns wlogout eww`, as the roadmap specifies — a single removal
covering both leftovers rather than two. Both were confirmed still installed in plan 20-01's
baseline (`wlogout 1.2.2-0`, `eww 0.6.0-1`, both explicitly installed, `eww` carrying an install
script). Neither has a repo tree; both have `own-tree=<none>` in the registry, so there is no
stow-side deletion to pair with the package removal — the config-then-package rule has no config
half here, which is itself worth recording so the asymmetry does not read as a skipped step.

Flip `retirement-check`'s registry rows for `wlogout` and `eww` from `status=pending` to
`status=retired`, and commit that with the removal. Remove any remaining non-`.planning/` prose
reference the pre-deletion baseline recorded for either.

**(b) The after-runs.** Append a `## Post-deletion` section to `20-RETIRE-05-07-RECORD.md`. Run
`retirement-check` once for each of `wleave`, `wlogout` and `eww` and commit each raw output
verbatim beside its pre-deletion baseline, with both hit counts printed side by side. Blocking tier
must be zero for all three. Report-tier hits in `.planning/` and repo prose are expected and
correct under D-18-37's two-tier split — record the counts without treating them as failures, and
note explicitly for `eww` that its short token can word-boundary-match unrelated prose, which the
registry's own header already documents as a known precision limit.

**(c) The green gates.** Run and record the literal exit code of each:
- `theme-engine/.config/theme-engine/theme-doctor`
- `theme-engine/.config/theme-engine/theme-parity`
- `theme-engine/.config/theme-engine/theme-stress-test` — **this is the deferral plan 20-09
  recorded and this task discharges.** It must reach a full clean run now that
  `REPRESENTATIVE_FILES` is settled.
- `hypr/.config/hypr/scripts/motion-lint`
- `hypr/.config/hypr/scripts/colour-lint`
- `hypr/.config/hypr/scripts/quickshell-doctor --self-test`
- `hypr/.config/hypr/scripts/quickshell-doctor` (live run)
- `hypr/.config/hypr/scripts/keybind-doctor`
- `hypr/.config/hypr/scripts/retirement-check --all`
- `hypr/.config/hypr/scripts/hypr-lua-harness`

Any non-zero exit is a defect this plan introduced and is fixed here, not deferred.

**(d) Close the record honestly.** State plainly what this phase's deletions did NOT prove:
nothing here establishes that a fresh clone still reproduces the desktop — that is RETIRE-09's
fresh-install container gate in Phase 22 — and `contract.json` is at 18 entries, not yet the
post-migration ~17 that RETIRE-08 reaches in Phase 21. Name both as other phases' work rather than
implying this phase closed them.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md; grep -q '^## Post-deletion' "$R"; for p in wleave wlogout eww; do grep -qF "retirement-check $p" "$R" || { echo "after-run not recorded for $p"; exit 1; }; pacman -Q "$p" >/dev/null 2>&1 && { echo "$p still installed"; exit 1; }; hypr/.config/hypr/scripts/retirement-check --list | grep -q "$p .*status=retired" || { echo "$p registry row not flipped"; exit 1; }; hypr/.config/hypr/scripts/retirement-check "$p"; done; grep -qF 'theme-stress-test' "$R"; grep -qiE 'RETIRE-09|Phase 22' "$R" || { echo "fresh-install caveat not recorded"; exit 1; }; theme-engine/.config/theme-engine/theme-doctor >/dev/null; theme-engine/.config/theme-engine/theme-parity >/dev/null; hypr/.config/hypr/scripts/motion-lint >/dev/null; hypr/.config/hypr/scripts/colour-lint >/dev/null; hypr/.config/hypr/scripts/quickshell-doctor --self-test >/dev/null; hypr/.config/hypr/scripts/keybind-doctor >/dev/null; hypr/.config/hypr/scripts/retirement-check --all >/dev/null; hypr/.config/hypr/scripts/hypr-lua-harness >/dev/null; python3 -c "import json;d=json.load(open('theme-engine/.config/theme-engine/contract.json'));assert len(d['files'])==18,len(d['files']);print('contract files: 18')"; echo OK</automated>
    <human-check>Run `theme-engine/.config/theme-engine/theme-stress-test` to completion and confirm a full clean run — this is the gate plan 20-09 deferred to here, and it is the one gate whose runtime makes it worth watching rather than only reading an exit code. Then perform one live theme switch (static preset and matugen wallpaper) and confirm the QML OSD and power menu both re-colour, with no surface left referencing a deleted stylesheet.</human-check>
  </verify>
  <acceptance_criteria>
    - `pacman -Q wleave`, `pacman -Q wlogout` and `pacman -Q eww` all fail — all three are gone from the host.
    - `retirement-check --list` shows `wleave`, `wlogout` and `eww` all at `status=retired`.
    - `20-RETIRE-05-07-RECORD.md` § Post-deletion carries a raw after-run and a blocking-tier count of 0 for each of the three, each printed beside its pre-deletion baseline count.
    - The `eww` after-run's report-tier count is annotated with the registry's own documented short-token precision limit.
    - All ten listed gates have their literal exit code recorded and every one is 0, including `theme-stress-test`'s full clean run.
    - `contract.json` has exactly 18 `files` entries.
    - The record names RETIRE-09/Phase 22 for the fresh-install proof and RETIRE-08/Phase 21 for the ~17-entry contract target, as other phases' work.
  </acceptance_criteria>
  <done>All three packages are gone from the host, every retirement's before/after pair is committed, and every gate in the repo is green including the stress test plan 20-09 deferred here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → host package database | `pacman -Rns` mutates system state outside git's ability to revert; `eww` additionally carries an install script whose removal hook runs |
| checker fixtures → checker verdicts | `motion-lint`'s poisoned fixtures are the only proof its checks can fail; editing them can silently defang the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-10-01 | Denial of Service | `pacman -Rns` removing a shared dependency across three packages | high | mitigate | Task 1 runs a separate `--print` preview and `pactree -r` for each of `wleave`, `wlogout`, `eww` and halts if any names a package beyond the target and its own orphans. All three previews are recorded verbatim. |
| T-20-10-02 | Tampering | `eww`'s removal install script | medium | mitigate | Task 1 records that `eww` carries an install script and what its removal hook will run, before the removal happens — a hook discovered after the fact is a hook nobody reviewed. |
| T-20-10-03 | Tampering | `theme-stress-test` losing its `gtk-css` representative | high | mitigate | `wleave.css` is REMOVED rather than repointed, because `gtk-4.0-colors.css` already represents that format in the same array. Asserted by a verify that requires `gtk-4.0-colors.css` still present after `wleave.css` is gone, plus a full clean stress-test run in Task 3. |
| T-20-10-04 | Tampering | poisoned `motion-lint` fixtures silently defanged | high | mitigate | Each fixture's assertion is re-read before editing and `motion-lint` must exit 0 afterward with its self-test intact — a fixture that stopped poisoning would fail rather than pass quietly. |
| T-20-10-05 | Repudiation | claiming a clean retirement without a baseline | medium | mitigate | Three after-runs are recorded beside plan 20-01's three pre-deletion baselines with both counts printed, and the `eww` short-token precision limit is annotated rather than hidden. |
| T-20-10-06 | Elevation of Privilege | deleting on a failed or stale gate | high | mitigate | Task 1's precondition halts on any Gate B FAIL and on a non-zero `git diff --quiet <judged-sha>` interlock. |
| T-20-10-SC | Tampering | package-manager removal | high | mitigate | All three removals are preceded by `--print` impact previews and reverse-dependency checks, and gated behind plan 20-08's blocking `checkpoint:decision`. No package is installed anywhere in this phase, so no legitimacy audit applies. |
</threat_model>

<verification>
- `pacman -Q wleave`, `pacman -Q wlogout`, `pacman -Q eww` all fail.
- `retirement-check` blocking tier is zero for all three, each recorded beside its pre-deletion baseline.
- `theme-doctor`, `theme-parity`, `theme-stress-test`, `motion-lint`, `colour-lint`, `quickshell-doctor --self-test`, the live `quickshell-doctor` run, `keybind-doctor`, `retirement-check --all` and `hypr-lua-harness` all exit 0.
- `contract.json` parses with exactly 18 `files` entries.
- `Super+Shift+Q`, the walker power entry and the bar power glyph all still open the QML power menu with wleave gone.

</verification>

<success_criteria>
- `wleave` is gone from repo and host in one config-then-package commit behind a passed Gate B, with its six action strings preserved in `PowerMenu.qml` and its behaviour preserved in `20-BEHAVIOUR-BASELINE.md`.
- `wlogout` and `eww` are uninstalled in a single `pacman -Rns`, through the same checklist, riding the second deletion as D-20-42 specifies.
- Every retirement has a committed before/after zero-hit pair, and every gate in the repo is green including the deferred stress test.
- What this phase did not prove — the fresh-install reproduction and the final contract size — is named as other phases' work rather than implied closed.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files:** `.planning/phases/20-indicators-power-menu/20-RETIRE-05-07-RECORD.md` (§ Pre-flight, § Post-deletion).

**Deleted symbols and files:**
- `wleave/` stow tree (`layout.json`, `style.css`) and `hypr/.config/hypr/scripts/wleave.sh`
- `matugen/.config/matugen/templates/wleave-colors.css` and `[templates.wleave]`
- `contract.json` `files` entry `wleave.css` (19 → 18)
- `theme-stress-test`'s `REPRESENTATIVE_FILES` entry `wleave.css`
- `theme-doctor`'s `GTK4_CSS_SHEETS` entry `$HOME/.config/wleave/style.css`
- `windowrules.lua`'s three wleave layer rules (blur, `animation = "fade"`, `ignore_alpha = 0.25`)
- `lib/gtk.sh`'s wleave comment half
- `motion-lint`'s three wleave carve-outs and its `$HOME/.config/wleave` directory entry
- `install.sh` and `stow.sh` wleave entries; any residual `autostart.lua` wleave reference; the root `VERIFICATION.md` wleave mention
- host: the `wleave`, `wlogout` and `eww` packages

**Changed symbols:** `retirement-check` registry rows `wleave`, `wlogout`, `eww` → `status=retired`; the three `tests/motion-fixtures/*gtk4.css` fixtures de-wleave'd with their assertions intact.
</artifacts_produced>

<output>
Create `.planning/phases/20-indicators-power-menu/20-10-SUMMARY.md` when done.
</output>
