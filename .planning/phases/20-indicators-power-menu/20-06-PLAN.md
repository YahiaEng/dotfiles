---
phase: 20-indicators-power-menu
plan: 06
type: execute
wave: 3
depends_on: ["20-01", "20-03"]
files_modified:
  - quickshell/.config/quickshell/modules/session/PowerMenu.qml
  - quickshell/.config/quickshell/modules/session/qmldir
  - quickshell/.config/quickshell/shell.qml
  - quickshell/.config/quickshell/shortcuts.json
  - quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml
  - hypr/.config/hypr/config/keybinds.lua
  - elephant/.config/elephant/menus/main.toml
  - hypr/.config/hypr/scripts/quickshell-doctor
  - .planning/phases/20-indicators-power-menu/20-LEDGER-02-RECORD.md
autonomous: true
requirements: [QPOWER-01, QPOWER-02, QPOWER-04, LEDGER-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All six actions are present — Lock, Log Out, Suspend, Hibernate, Reboot, Shut Down — as a 3×2 grid inside a screen-centred card on the `PanelDialog` family's chrome language, with a scrim behind (D-20-21). It is neither end-4's full-screen overlay nor Caelestia's inline right-edge popout, and both rejections are recorded."
    - "The six action command strings are MIGRATED from `wleave/.config/wleave/layout.json` — the sole place they exist — into QML BEFORE that file is deleted (D-20-26). The baseline transcription in `20-BEHAVIOUR-BASELINE.md` is the migration source."
    - "Every `Process.command` element is a fixed literal declared inline; no element is produced by concatenation, template literal, or interpolation of any runtime value."
    - "Shutdown and Reboot still route through `hyprshutdown`'s graceful compositor exit — the mechanism that closed the FIX-01 hang class (QPOWER-04); the wrap is not re-derived, it is carried over verbatim from the baseline."
    - "Lock is auto-focused on open, arrow keys move focus two-dimensionally across the 3×2 grid, Enter activates the focused tile, Escape closes with no action taken (QPOWER-02, D-20-24)."
    - "wleave's existing per-action mnemonics `l/e/u/h/r/s` still fire their action from any focus state, and are now VISIBLY surfaced on the tiles — they work today but are undisplayed (`show-keybinds: false`), which D-20-24 deliberately reverses."
    - "`WlrKeyboardFocus.Exclusive` is used, a deliberate divergence from D-19-18's no-exclusive-focus rule, which was written for a non-modal centre — this surface's actions end the session. `HyprlandFocusGrab` click-outside dismissal must be confirmed to COEXIST with `Exclusive` on this compositor; no surface in this repo has combined the two before, so it is proven live, not assumed."
    - "All three entry points open the identical in-process surface and none still shells to `wleave.sh` — `Super+Shift+Q` in `keybinds.lua`, the walker menu's power entry in `elephant/.config/elephant/menus/main.toml`, and the bar's `powerCell` in `ClockActionsCapsule.qml`. The bar glyph was not in the roadmap's consumer list and is a third consumer (D-20-22)."
    - "`powerAvailabilityProbe`, `powerAvailable` and `powerScriptPath` are deleted outright, not repointed — an in-process QML surface has no 'missing' state to probe for, which is the exact risk that probe's own comment cites (D-20-23)."
    - "The `powerCell`'s own rendering — the `power_settings_new` glyph, its permanent accent tint and its tooltip — is UNCHANGED; only `onClicked`'s target and the deleted probe change, matching the repoint-internals-leave-rendering-untouched discipline Phase 19 applied to the bell in this same file."
    - "LEDGER-02 is recorded as 'Logout wrapped WITHOUT the D-29 measurement being taken'. The hazard remains neither confirmed nor falsified — the same status it has held since the 2026-07-28 waiver (D-20-37)."
    - "The `hyprshutdown --post-cmd 'uwsm stop'` composition is recorded with RESEARCH's live systemd-topology evidence that its marginal effect is likely low, worded as evidence and not as a measurement (D-20-38)."
    - "S4/empty: not applicable — the dialog exists only while summoned; all six actions are always present, there is no 'no actions' state."
    - "S4/loading: not applicable — the first detector poll's result simply has not landed yet and is treated as 'no warning', never as a placeholder banner."
    - { statement: "S4/error — if the compositor-exit mechanism (`hyprshutdown`) is itself unreachable, Shutdown and Reboot fail silently rather than surfacing an in-menu error, matching this shell's convention of degrading external-process failures silently. A completely silent failure on a session-ending action is a real UX question and must be CONFIRMED as the intended failure mode, not assumed.", verification: backstop }
    - "S4/populated: header + 3×2 grid, optionally plus the warning banner."
    - "S4/partial: one detector active renders exactly one warning line — not three lines with two saying 'no issue'. Absent-if-clear, never a three-state checklist."
    - "S4/overflow: a fixed 3×2 grid of exactly six tiles with no growth path; the banner is bounded at three lines, one per detector."
    - "S4/zero-one-many: not applicable to the tile count, which is always six; the banner's 0-3 range is resolved under S4/partial and S6."
    - "S4/long-text: fixed header, six fixed labels, three fixed warning strings — no free or sender-controlled text anywhere on this surface, unlike the notification family."
    - "S5/empty: not applicable — six tiles, always fully populated."
    - "S5/loading: not applicable — glyph, label and mnemonic are static per tile with no async content."
    - "S5/error: not applicable per D-20-28 — no tile is ever disabled, greyed or shown unavailable; warn-only means every tile stays fully interactive."
    - "S5/populated: icon + label + mnemonic per the tile anatomy."
    - "S5/partial: not applicable — a tile has no sub-parts that can be partially present."
    - "S5/overflow: fixed tile size; 'Hibernate' and 'Log Out' are the longest labels and both fit `Design.sessionTileWidth` at Body/DemiBold without wrapping."
    - "S5/zero-one-many: not applicable — exactly six tiles, no dynamic count."
    - "S5/long-text: no ellipsis or truncation mechanism exists or is needed; every label was checked against the tile width."
  artifacts:
    - quickshell/.config/quickshell/modules/session/PowerMenu.qml
    - quickshell/.config/quickshell/modules/session/qmldir
    - .planning/phases/20-indicators-power-menu/20-LEDGER-02-RECORD.md
  key_links:
    - "`PowerMenu.qml`'s six command arrays ↔ `20-BEHAVIOUR-BASELINE.md`'s verbatim `layout.json` transcription — the migration source, captured before deletion."
    - "`quickshell:power-menu` GlobalShortcut ↔ `shortcuts.json` manifest entry ↔ `keybinds.lua`'s `hl.dsp.global(...)` bind — all three land in ONE commit, per the declared-manifest mechanism D-17 established and `keybind-doctor` checks."
    - "`ClockActionsCapsule.qml`'s `powerCell.onClicked` ↔ the `PowerMenu` toggle verb — an in-process call, not a `Process` launch."
    - "`quickshell-session` namespace ↔ 20-03's `animation = \"slide\"` row and its recorded no-override prediction (card 0.78 / scrim 0.55, both above the family's 0.5 floor), verified at Gate B."
  prohibitions:
    - "MUST NOT describe LEDGER-02's Logout wrap as a measurement taken, a hazard confirmed, or a hazard closed by evidence. It is 'wrapped without measurement'; the hazard is neither confirmed nor falsified."
    - "MUST NOT silently upgrade D-20-37's literal target string to `uwsm stop -r`. Adding the `-r` flag changes a locked decision's content and is a new, explicit decision made outside this plan."
---

<objective>
Prove the whole power-menu path end-to-end — keybind to a real session action — then repoint the
remaining two consumers and settle LEDGER-02 honestly.

Purpose: this is the power half's tracer. It wires one real path through every layer the half
touches — Hyprland bind → `GlobalShortcut` → a new `PanelWindow` on the `quickshell-session`
namespace → keyboard-navigable grid → a real `Process` that actually ends the session. If
`WlrKeyboardFocus.Exclusive` and `HyprlandFocusGrab` do not coexist on this compositor — a
combination no surface in this repo has used before — that is discovered on the first commit.

Output: `PowerMenu.qml`, its shell mount and shortcut, all three entry points repointed, the
`powerAvailabilityProbe` deleted, and `20-LEDGER-02-RECORD.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-indicators-power-menu/20-CONTEXT.md
@.planning/phases/20-indicators-power-menu/20-UI-SPEC.md
@.planning/phases/20-indicators-power-menu/20-RESEARCH.md
@.planning/phases/20-indicators-power-menu/20-PATTERNS.md
@.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md
@.planning/phases/20-indicators-power-menu/20-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end power menu — Super+Shift+Q to a real session action</name>
  <files>quickshell/.config/quickshell/modules/session/PowerMenu.qml, quickshell/.config/quickshell/modules/session/qmldir, quickshell/.config/quickshell/shell.qml, quickshell/.config/quickshell/shortcuts.json, hypr/.config/hypr/config/keybinds.lua</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/dashboard/PanelDialog.qml` — the whole file. Specifically: the layer posture block (lines ~122-133) with `exclusiveZone: 0`, `exclusionMode: ExclusionMode.Normal`, `WlrLayershell.layer: WlrLayer.Overlay`, the namespace binding and `WlrKeyboardFocus.OnDemand` (line 131); the background `Rectangle` + `GradientBorder` construction (lines ~139-172) including `panelSurfaceOpacity: 0.78` (line 154) and the corner-radius call sites; `HyprlandFocusGrab { windows: [panelWindow]; active: true; onCleared: panelWindow.requestDismiss() }` (lines 203-208); the `Cascade{}` component with `.bands`/`.armed`/`.run()` (lines 213-219); the Design-derived constants block (lines ~136-158) including the LOCALLY-declared `lineHeightTight: 1.2` / `lineHeightNormal: 1.5` at lines 149-150 and `disabledOpacity: 0.38` at line 287; and `handleEscape()`.
    - `quickshell/.config/quickshell/modules/dashboard/Cascade.qml` — the `bands` / `armed` / `run()` API, reused as-is.
    - `quickshell/.config/quickshell/modules/Overview.qml` — its full-screen catch-region precedent and the way its own scrim base colour is applied, since the power menu's window spans the output for exactly the same reason.
    - `quickshell/.config/quickshell/shell.qml` — the `GlobalShortcut { appid: "quickshell"; name: …; onPressed: … }` blocks (around lines 813-835) and the `LazyLoader { active: false }` summon idiom described in the file header; also the import list at lines 19-29.
    - `quickshell/.config/quickshell/shortcuts.json` — the manifest entry shape (`appid`, `name`, `chord.mods`, `chord.key`, `description`), and the `notif-centre` entry as the most recent worked example.
    - `hypr/.config/hypr/config/keybinds.lua` line 68 (the current `Super+Shift+Q` → `wleave.sh` bind) and lines 208/224/233 (`hl.dsp.global("quickshell:dashboard"|"quickshell:overview"|"quickshell:notif-centre")`) — the in-process-summon idiom that replaces `exec_cmd` against a script.
    - `.planning/phases/20-indicators-power-menu/20-BEHAVIOUR-BASELINE.md` — the verbatim six-quadruple transcription of `wleave/.config/wleave/layout.json`. This is the migration source; do not re-read `layout.json` and do not re-derive the strings.
    - `.planning/phases/20-indicators-power-menu/20-UI-SPEC.md` § "Power Menu — Frame", § "Power Menu — Tile Anatomy", § "Power Menu — Focus Treatment", and ASCII previews 4 and 5.
  </read_first>
  <action>
Build the thinnest complete path: **`Super+Shift+Q` → the menu opens → Lock is focused → Enter
locks the session.** The QPOWER-03 warning banner and its detectors are plan 20-07; the cascade
entrance is plan 20-07. Everything else on this surface lands here.

**(a) Create `quickshell/.config/quickshell/modules/session/PowerMenu.qml`** plus
`modules/session/qmldir` declaring it (a `.qml` type in a `modules/` subdirectory that is not
declared in that subdirectory's own `qmldir` is unresolvable — `modules/qmldir`'s header states
this as a standing rule).

Build it ON `PanelDialog.qml`'s pattern language, not by instantiating it: `PanelDialog` is
anchors-top, bottom-rounded and docked under the bar, built for drawer-style panels, and the
power menu is a screen-centred modal. Copy the construction, diverge on the geometry:

- ONE `PanelWindow` anchored to all four edges of the output, `exclusiveZone: 0`,
  `WlrLayershell.layer: WlrLayer.Overlay`, namespace `"quickshell-session"`.
- A full-bleed scrim filling that window — `Colours.surface` at `Design.sessionScrimOpacity`
  (0.55) — which gives `HyprlandFocusGrab`'s click-outside dismissal a screen-wide catch area
  with no second window to coordinate. This resolves the "scrim as window property vs separate
  layer" discretion item in favour of one window.
- The dialog CARD as a child `Item`, `anchors.centerIn: parent`,
  `implicitWidth: Design.sessionDialogWidth` (488), height content-driven. Background
  `Colours.surface` at `panelSurfaceOpacity` (0.78, `PanelDialog`'s own literal, reused not
  redeclared). Corner radius UNIFORM `Design.popoutCornerRadius` (20) on all four corners — unlike
  `PanelDialog`'s bottom-only rounding, because this card floats clear of every screen edge. Rim
  `GradientBorder` at `Design.borderWidth` (3).
- Header band at `Design.popoutHeaderHeight` (48) — NOT `PanelDialog`'s own 72px `headerHeight`,
  since this dialog carries no Advanced button and no icon+title pairing at that scale. Title
  "Session" at `Design.fontHeading` (20) / `Design.weightEmphasis`, left-inset `Design.panelPadding`
  (24). No close button.
- Declare `lineHeightTight: 1.2` / `lineHeightNormal: 1.5` LOCALLY in this file, in
  `PanelDialog.qml`'s own idiom. 20-UI-SPEC.md's Step-9.5 correction verified these are not
  `Design.qml` tokens; the conservative, precedent-matching choice is a local declaration, and
  plan 20-03 deliberately did not promote them. Do not invent a third declaration site elsewhere.

**(b) The 3×2 grid.** Six tiles, `Design.sessionTileWidth` (136) × `Design.sessionTileHeight`
(104), radius `Design.sessionTileRadius` (16), `Design.spacingMd` (16) gap on both axes,
`Design.panelPadding` (24) inset from the card edge. Fill `Colours.surfaceVariant` at rest — the
same source as `PanelDialog`'s own Advanced button. Icon `Design.sessionTileIconSize` (32),
centred, `Colours.onSurfaceVariant`. Label at `Design.fontBody` (16) / `Design.weightEmphasis`,
one line, `Colours.onSurfaceVariant`. Mnemonic letter at `Design.fontLabel` (12),
`Colours.onSurfaceVariant` at the muted-but-present 0.38 opacity register, bottom-right corner,
inset `Design.spacingXs` (4).

Row order exactly as the selected mockup names it: **Lock / Log Out / Suspend** on the top row,
**Hibernate / Reboot / Shut Down** on the bottom. Glyphs: `lock`, `logout`, `bedtime`, `ac_unit`,
`restart_alt`, `power_settings_new`. Labels exactly: "Lock", "Log Out", "Suspend", "Hibernate",
"Reboot", "Shut Down" — Title Case, matching the dashboard tab labels' casing convention.
`power_settings_new` is deliberately the same glyph the bar's own `powerCell` already uses, so the
action and the trigger that opens the menu share one icon identity.

Hover (pointer only) lifts the tile fill toward a lighter blend using the same +0.1-alpha-step
idiom `BarRoles.capsule`→`capsuleHover` establishes (0.85→0.95), applied locally here since this
dialog reads `Colours.*` directly. No per-tile destructive styling exists anywhere on this surface
(D-20-28): all six tiles render identically at rest and stay fully interactive.

**(c) The six actions.** Migrate the command strings from `20-BEHAVIOUR-BASELINE.md`'s verbatim
transcription:

| tile | command | note |
|---|---|---|
| Lock | `uwsm app -- hyprlock` | unchanged |
| Log Out | `cliphist wipe; hyprshutdown --post-cmd 'uwsm stop'` | **CHANGED** — the current action is the bare `cliphist wipe; uwsm stop` with no hyprshutdown wrapper at all. This is a genuine addition of the wrapper (D-20-37), not a re-composition. See Task 3. |
| Suspend | `systemctl suspend` | unchanged |
| Hibernate | `systemctl hibernate` | unchanged |
| Reboot | `cliphist wipe; hyprshutdown --post-cmd 'systemctl reboot'` | unchanged — this is QPOWER-04's graceful-exit mechanism, carried over verbatim, not re-derived |
| Shut Down | `cliphist wipe; hyprshutdown --post-cmd 'systemctl poweroff'` | unchanged — same |

Four of the six compose two commands with `;` and therefore need a shell. Express each as a fixed
literal array whose shell-string element is written out inline in the action table — never built
by concatenation, template literal, or interpolation of any runtime value. This matches
`PanelDialog.qml`'s own `advancedCommand` discipline (a `Process.command` bound to a declared
property, never joined into a string) and `BrightnessBackend.qml`'s documented reasoning about
never handing a constructed string to an interpreter. A reader must be able to see every byte
that will reach a shell by reading this one table.

**(d) Focus and keyboard.** `WlrKeyboardFocus.Exclusive` (D-20-24) — a deliberate divergence from
D-19-18's no-exclusive-focus rule, which was written for the non-modal notification centre; this
surface's actions end the session. Record that reasoning in a comment so the divergence does not
read as an oversight. Keep `HyprlandFocusGrab { windows: [powerWindow]; active: true; onCleared:
… }` copied verbatim from `PanelDialog.qml` for click-outside dismissal. **No surface in this repo
has combined `Exclusive` with a focus grab before** — the human-check below proves the combination
live rather than assuming it from the `OnDemand` case.

Lock (top-left) is auto-focused on open — the least destructive action, and the one action
QPOWER-03 never warns about (D-20-29). Arrow keys move focus two-dimensionally across the grid:
up/down between rows, left/right within a row, matching visual adjacency, no wraparound. Enter
activates the focused tile. Escape closes with no action taken, using the same key-handling idiom
`PanelDialog.qml`'s `handleEscape()` establishes. Mnemonics `l`/`e`/`u`/`h`/`r`/`s` fire their
matching action directly from any focus state — unchanged behaviour from wleave, now visibly
surfaced on the tiles.

Visible focus is a RING, never a fill swap: `BarRoles.accent` at `Design.borderWidth` (3), drawn
OUTSIDE the tile's `Design.sessionTileRadius` (16) boundary so the ring reads as an addition to
the tile rather than a recolour of it. This keeps the six tiles uniform at rest and makes the ring
the single unambiguous "this is where a keypress goes" signal QPOWER-02 requires.

**(e) Mount and summon.** Add `import "modules/session"` to `shell.qml`'s import list and mount
the surface behind the same `LazyLoader { active: false }` summon idiom every other summonable
surface in this file uses, plus a `GlobalShortcut { appid: "quickshell"; name: "power-menu" }`
whose `onPressed` toggles it. Add the matching `shortcuts.json` entry (`appid: "quickshell"`,
`name: "power-menu"`, chord `SUPER SHIFT` + `Q`, with a description citing QPOWER-01/D-20-22).
Repoint `keybinds.lua:68` from `hl.dsp.exec_cmd("~/.config/hypr/scripts/wleave.sh")` to
`hl.dsp.global("quickshell:power-menu")`, keeping the trailing `-- Open power menu` comment.

All three of these — the manifest entry, the `GlobalShortcut` and the bind — land in ONE commit.
That is the declared-manifest mechanism D-17 established and what `keybind-doctor` checks; a
manifest entry with no claimant, or a bind with no manifest entry, reds that gate.
  </action>
  <verify>
    <automated>set -e; S=quickshell/.config/quickshell/modules/session; test -f "$S/PowerMenu.qml"; test -f "$S/qmldir"; grep -q 'PowerMenu .*1\.0 .*PowerMenu\.qml' "$S/qmldir"; grep -q 'import "modules/session"' quickshell/.config/quickshell/shell.qml; grep -q '"name": "power-menu"' quickshell/.config/quickshell/shortcuts.json; grep -qE 'name:\s*"power-menu"' quickshell/.config/quickshell/shell.qml; grep -q 'hl.dsp.global("quickshell:power-menu")' hypr/.config/hypr/config/keybinds.lua; grep -vE '^\s*--' hypr/.config/hypr/config/keybinds.lua | grep -c 'wleave.sh' | grep -qx 0 || { echo "keybinds.lua still references wleave.sh"; exit 1; }; Q=$(grep -vE '^\s*//' "$S/PowerMenu.qml"); for c in "uwsm app -- hyprlock" "cliphist wipe; hyprshutdown --post-cmd 'uwsm stop'" "systemctl suspend" "systemctl hibernate" "cliphist wipe; hyprshutdown --post-cmd 'systemctl reboot'" "cliphist wipe; hyprshutdown --post-cmd 'systemctl poweroff'"; do printf '%s\n' "$Q" | grep -qF "$c" || { echo "MISSING action command: $c"; exit 1; }; done; printf '%s\n' "$Q" | grep -q "uwsm stop -r" && { echo "D-20-37's literal target string was upgraded to -r"; exit 1; }; printf '%s\n' "$Q" | grep -q 'WlrKeyboardFocus.Exclusive'; printf '%s\n' "$Q" | grep -q 'HyprlandFocusGrab'; printf '%s\n' "$Q" | grep -q 'quickshell-session'; hypr/.config/hypr/scripts/colour-lint >/dev/null; hypr/.config/hypr/scripts/motion-lint >/dev/null; hypr/.config/hypr/scripts/keybind-doctor >/dev/null 2>&1; echo "keybind-doctor exit=$?"</automated>
    <human-check>Restart the shell and apply layer rules with `hyprctl eval` or a full Hyprland restart — NEVER `hyprctl reload`. Then: (1) press `Super+Shift+Q` and confirm the centred card appears over a dimmed scrim with a visible rim and a rounded card on all four corners; (2) confirm `hyprctl layers -j` shows namespace `quickshell-session` at level 3 and the card reads as frosted, not flat — 20-03 predicted no `ignore_alpha` override is needed here (card 0.78, scrim 0.55, both above the family's 0.5 floor); if it reads flat, that prediction was wrong and needs its own override row; (3) confirm Lock is focused on open, with a visible accent RING and no fill change; (4) press arrow keys and confirm focus moves two-dimensionally (right from Lock reaches Log Out; down from Lock reaches Hibernate); (5) **the combination check no surface in this repo has done before** — with `WlrKeyboardFocus.Exclusive` active, click OUTSIDE the card on the scrim and confirm the menu dismisses via `HyprlandFocusGrab`; then reopen and confirm Escape also closes with no action taken; (6) press `s` from any focus state and confirm the Shut Down tile's action is what fires (cancel before it completes, or run this check last); (7) with Lock focused, press Enter and confirm hyprlock actually starts — this is the tracer's real end-to-end proof, not a simulated one.</human-check>
  </verify>
  <acceptance_criteria>
    - `modules/session/PowerMenu.qml` and `modules/session/qmldir` exist; the `qmldir` declares the type and `shell.qml` carries `import "modules/session"`.
    - `shortcuts.json` has a `power-menu` entry, `shell.qml` has a matching `GlobalShortcut`, and `keybinds.lua` binds `hl.dsp.global("quickshell:power-menu")` — all three in one commit; `keybind-doctor` exits 0.
    - `grep -vE '^\s*--' keybinds.lua | grep -c wleave.sh` returns 0.
    - All six action command strings from the table appear verbatim in `PowerMenu.qml`, and `uwsm stop -r` appears nowhere.
    - No `Process.command` element in `PowerMenu.qml` is produced by `+` concatenation, a template literal, or interpolation of a runtime value — every element is an inline literal.
    - `PowerMenu.qml` declares `WlrKeyboardFocus.Exclusive`, an `HyprlandFocusGrab` with `onCleared` dismissal, and namespace `quickshell-session`.
    - `PowerMenu.qml` declares `lineHeightTight` and `lineHeightNormal` locally; `Design.qml` still declares neither.
    - The focus treatment is a ring using `BarRoles.accent` at `Design.borderWidth`; no tile's fill colour changes on focus.
    - `colour-lint` and `motion-lint` both exit 0.
    - The human-check confirms all seven observations, including the `Exclusive` + `HyprlandFocusGrab` coexistence and a real hyprlock start from Enter on Lock.
  </acceptance_criteria>
  <done>`Super+Shift+Q` opens a themed, keyboard-navigable, click-outside-dismissible session dialog whose six actions carry the real command strings, and Enter on Lock actually locks the machine.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repoint the walker menu and the bar glyph; delete the availability probe</name>
  <files>quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml, elephant/.config/elephant/menus/main.toml, hypr/.config/hypr/scripts/quickshell-doctor</files>
  <read_first>
    - `quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml` lines 567-580 — `powerScriptPath` (567), `powerAvailable` (568), the `powerAvailabilityProbe` `Process` running `test -x` (571-575) and `powerLaunchProcess` (577-579). Also lines ~1002-1019 — the `powerCell` `ActionCell` with glyph `power_settings_new` and the operator decision recorded at 1007-1017 that it is permanently accent-tinted. Also the file's own bell/`NotificationSource` block from Phase 19, which is the worked precedent for repointing internals while leaving rendering untouched.
    - `elephant/.config/elephant/menus/main.toml` around line 35 — the power entry's `actions = { "open" = … }` and its comment at line 33 stating it "delegates to the ONE existing power surface (same as Super+Shift+Q)", plus the D-09 note that the target is invoked bare, never through `uwsm app --`.
    - `hypr/.config/hypr/scripts/quickshell-doctor` — `QSD_BAR_SURFACE_ROWS`, its row format comment, and `QSD_KNOWN_NONBAR_FRAMES`. Read the namespace-discipline check body to determine whether a new frame declaring `WlrLayershell.namespace` must be registered or waved through.
    - `quickshell/.config/quickshell/modules/session/PowerMenu.qml` — as written by Task 1, so the repoints call the real toggle verb rather than a guessed name.
  </read_first>
  <action>
**(a) The bar glyph (the third consumer D-20-22 names, which the roadmap's own list missed).** In
`ClockActionsCapsule.qml`, delete `powerScriptPath`, `powerAvailable` and the entire
`powerAvailabilityProbe` `Process` block. D-20-23 is explicit that these are removed OUTRIGHT, not
repointed: the menu becomes an in-process QML surface, so "the power menu is missing" stops being
a reachable state — which is the exact risk that probe's own comment cites as its reason to exist.

Repoint `powerCell`'s `onClicked` to call the `PowerMenu` surface's toggle verb IN-PROCESS. There
is no `Process` launch left on this path, so `powerLaunchProcess` goes too unless the file has
another consumer of it — check, and if it is now unused, delete it.

Leave the `powerCell`'s own rendering completely untouched: the `power_settings_new` glyph, the
permanent accent tint recorded by operator decision at lines 1007-1017, and the tooltip. Any
`powerAvailable` binding that gated rendering must be removed along with the property rather than
replaced by a new always-true condition — a probe deleted but its binding left as
`available: true` is the same dead machinery under a new name. This is the identical discipline
Phase 19 applied to the bell in this same file.

**(b) The walker menu.** In `elephant/.config/elephant/menus/main.toml`, repoint the power entry's
`actions.open` from `~/.config/hypr/scripts/wleave.sh` to the same target the keybind now uses.
The entry's own comment says it delegates to the ONE existing power surface, same as
`Super+Shift+Q` — keep that invariant true by using the identical dispatch, and update the comment
to name the new surface. The label and icon are unchanged. Honour the file's D-09 note: the target
is invoked bare, never wrapped in `uwsm app --`.

**(c) Register the namespace.** Add the `quickshell-session` surface to `quickshell-doctor`'s
registry so GATE-03's namespace-discipline check resolves it, following the same reasoning plan
20-04 Task 3 recorded for `quickshell-osd`: read the check body first to establish whether the
registry resolves namespace→row or file→row, write the row where that direction requires, and
record the direction in an inline comment. The row's fields are
`session/PowerMenu.qml | quickshell-session | exact | 3 | noreserve | transient` — level 3 because
it declares `WlrLayer.Overlay`, `noreserve` because `exclusiveZone: 0` (the bar remains the single
reserving surface this file's header insists on), and `transient` because it is summoned per
invocation and destroyed on dismiss. Do NOT add it to `QSD_KNOWN_NONBAR_FRAMES` — that set is
explicitly for frames that PREDATE the registry, and this one does not; Phase 19 closed its own
`unregistered=3` debt by registering rather than exempting, and this follows that discipline.

Verify with `quickshell-doctor --self-test`, and do not introduce a `<producer> | grep -q`
construct — under `pipefail` a producer piped into `grep -q` can exit 141 on a match and flip a
gate's verdict by file length.
  </action>
  <verify>
    <automated>set -e; C=quickshell/.config/quickshell/modules/bar/ClockActionsCapsule.qml; SRC=$(grep -vE '^\s*//' "$C"); for sym in powerScriptPath powerAvailable powerAvailabilityProbe; do printf '%s\n' "$SRC" | grep -c "$sym" | grep -qx 0 || { echo "$sym still present in ClockActionsCapsule.qml source"; exit 1; }; done; printf '%s\n' "$SRC" | grep -q 'power_settings_new' || { echo "powerCell glyph was removed - rendering must be untouched"; exit 1; }; M=elephant/.config/elephant/menus/main.toml; grep -vE '^\s*#' "$M" | grep -c 'wleave.sh' | grep -qx 0 || { echo "walker menu still points at wleave.sh"; exit 1; }; grep -vE '^\s*#' "$M" | grep -q 'power-menu' || { echo "walker menu not repointed at the new surface"; exit 1; }; D=hypr/.config/hypr/scripts/quickshell-doctor; grep -vE '^\s*#' "$D" | grep -q 'quickshell-session' || { echo "quickshell-session not registered"; exit 1; }; grep -vE '^\s*#' "$D" | grep -q 'PowerMenu.qml' || { echo "PowerMenu.qml not in the surface registry"; exit 1; }; bash -n "$D"; "$D" --self-test >/dev/null; hypr/.config/hypr/scripts/colour-lint >/dev/null; echo OK</automated>
    <human-check>(1) Click the bar's power glyph and confirm the in-process menu opens — the glyph itself must look byte-identical to before (same icon, same permanent accent tint, same tooltip text and delay). (2) Open walker, pick the Power entry, and confirm the identical surface opens. (3) Confirm all three entry points produce the SAME surface instance — open via keybind, dismiss, open via bar glyph, dismiss, open via walker: one surface each time, never two stacked.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*//' ClockActionsCapsule.qml` contains zero occurrences of `powerScriptPath`, `powerAvailable` and `powerAvailabilityProbe`.
    - `power_settings_new` is still present in `ClockActionsCapsule.qml` and the `powerCell`'s glyph, tint and tooltip lines are unchanged in the diff.
    - No `available:`-style always-true replacement binding was introduced in place of the deleted `powerAvailable`.
    - `grep -vE '^\s*#' main.toml` contains zero occurrences of `wleave.sh` and does contain the new dispatch target; the entry's label and icon are unchanged in the diff.
    - `quickshell-doctor` registers `quickshell-session` against `PowerMenu.qml` in the surface registry, not in `QSD_KNOWN_NONBAR_FRAMES`, with an inline comment recording the registry's resolution direction.
    - `bash -n quickshell-doctor` parses clean, `quickshell-doctor --self-test` exits 0, and no `<producer> | grep -q` construct was introduced.
    - The human-check confirms all three entry points open one identical surface and the bar glyph's rendering is unchanged.
  </acceptance_criteria>
  <done>All three entry points open the same in-process surface, the availability probe is gone with no residue, and the new namespace is registered rather than exempted.</done>
</task>

<task type="auto">
  <name>Task 3: Record LEDGER-02 honestly — wrapped, unmeasured</name>
  <files>.planning/phases/20-indicators-power-menu/20-LEDGER-02-RECORD.md</files>
  <read_first>
    - `.planning/milestones/v3.0-phases/13-motion-retrofit-existing-surface-sweep/13-03-PLAN.md` Task 2 — the verbatim D-29 reproduction steps. Read to understand exactly WHAT is being skipped; the measurement is not being run.
    - `.planning/phases/20-indicators-power-menu/20-CONTEXT.md` D-20-37 and D-20-38 — the locked decision and the open question it made blocking.
    - `.planning/phases/20-indicators-power-menu/20-RESEARCH.md` § "Priority Research Findings" item 1 in full — the live `hyprshutdown --help` and `uwsm stop --help` output, the `systemctl --user list-units --all` topology showing `wayland-wm@hyprland.desktop.service` and `wayland-session-bindpid@1542.service`, and the explicit contradiction flag; plus § "Common Pitfalls" Pitfall 2 and § "Assumptions Log" row A3.
    - `.planning/REQUIREMENTS.md` LEDGER-02's own text and `.planning/ROADMAP.md` Phase 20 Success Criterion 4 — both of which ask for the measurement to be *taken*.
  </read_first>
  <action>
Create `.planning/phases/20-indicators-power-menu/20-LEDGER-02-RECORD.md`.

State the outcome plainly in the first sentence: **the D-29 teardown measurement was NOT taken;
Logout was wrapped anyway.** Record that this satisfies the OUTCOME of ROADMAP SC-4 (the hazard is
closed by construction) but not its LETTER (SC-4 asks for the measurement to be taken), and that
the hazard remains neither confirmed nor falsified — the same status it has held since the
2026-07-28 waiver.

Record the concrete change: the previous Logout action was the bare `cliphist wipe; uwsm stop`
with no `hyprshutdown` wrapper at all, and the new action is
`cliphist wipe; hyprshutdown --post-cmd 'uwsm stop'`. This is a genuine ADDITION of the wrapper,
mirroring Reboot and Shutdown, with the `cliphist wipe` deliberately BEFORE teardown because
cliphist's daemon may already be gone once a graceful exit begins.

Record D-20-38's resolution as EVIDENCE, never as a measurement. RESEARCH established from live
inspection of this host's actual systemd unit topology that `hyprshutdown --post-cmd` runs its
command strictly AFTER Hyprland's own process has exited, and that uwsm binds the session's unit
graph to the compositor's PID (`wayland-session-bindpid@…`) up through `graphical-session.target`,
with a dedicated `wayland-session-shutdown.target`. The reasoned conclusion is that `uwsm stop`
most likely finds the compositor already stopped, and that its "removes generated units" half is
gated behind a `-r` flag D-20-37's literal string does not pass. Word this as: *evidence from live
unit topology suggests the composition has low marginal effect; it was not empirically exercised,
because doing so ends the session that would observe it.*

Write the honesty constraint into the document as a standing instruction for whoever summarises
this phase: **no summary sentence may say Logout's teardown hazard is "closed", "resolved",
"fixed" or "measured".** The permitted phrasing is "wrapped, unmeasured, evidence suggests low
marginal risk". Name the trap explicitly — `hyprshutdown --post-cmd 'uwsm stop'` sounds
authoritative and mirrors the Reboot/Shutdown pattern, inviting the same confidence, but
Reboot/Shutdown's post-cmd does something unambiguous while Logout's very likely finds nothing
left to do.

Record what a future phase would need to actually take the measurement: the verbatim steps remain
in `13-03-PLAN.md` Task 2, and the real unkillable-client detector that would characterise the
hazard is explicitly deferred to v5.0+ (CONTEXT.md's deferred list) because nothing on this stack
detects it reliably.

Do not change the action string in `PowerMenu.qml`. In particular do not upgrade it to
`uwsm stop -r` — that changes a locked decision's literal content and would be a new, explicit
decision made outside this plan.
  </action>
  <verify>
    <automated>set -e; R=.planning/phases/20-indicators-power-menu/20-LEDGER-02-RECORD.md; test -f "$R"; grep -qi 'not taken' "$R"; grep -qF "cliphist wipe; hyprshutdown --post-cmd 'uwsm stop'" "$R"; grep -qF 'cliphist wipe; uwsm stop' "$R"; grep -q 'D-20-37' "$R"; grep -q 'D-20-38' "$R"; grep -qiE 'neither confirmed nor falsified' "$R"; grep -qiE '\b(hazard is (closed|resolved|fixed))\b' "$R" && { echo "record contains a forbidden closure claim"; exit 1; }; Q=quickshell/.config/quickshell/modules/session/PowerMenu.qml; grep -vE '^\s*//' "$Q" | grep -q 'uwsm stop -r' && { echo "action string was upgraded to -r"; exit 1; }; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `20-LEDGER-02-RECORD.md` exists and states in its opening that the D-29 measurement was not taken.
    - The document contains both the previous action string (`cliphist wipe; uwsm stop`) and the new one (`cliphist wipe; hyprshutdown --post-cmd 'uwsm stop'`), and identifies the change as an addition of the wrapper rather than a re-composition.
    - The document contains the phrase "neither confirmed nor falsified" and cites both `D-20-37` and `D-20-38`.
    - The document contains no sentence asserting the hazard is closed, resolved or fixed (the verify grep rejects those forms).
    - The D-20-38 discussion is worded as evidence from live unit topology, explicitly stating it was not empirically exercised and why.
    - `PowerMenu.qml` contains no `uwsm stop -r`.
  </acceptance_criteria>
  <done>LEDGER-02 is settled by construction and recorded truthfully, with the exact phrasing a later summary must use written into the record itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → shell interpreter | Four of the six actions compose two commands with `;` and therefore reach `sh -c`; the string that reaches it must be a compile-time literal |
| layer-shell overlay → every XDG toplevel | A layer-shell overlay renders unconditionally above every application window, including another app's confirm dialog |
| shell → systemd-logind / compositor | Session-ending actions delegate the actual state transition to `systemctl`, `hyprshutdown` and `uwsm`, which own it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06-01 | Tampering | `PowerMenu.qml` action command arrays | high | mitigate | Every `Process.command` element is an inline literal declared in the action table; no element is produced by concatenation, template literal or interpolation of a runtime value. Asserted by an acceptance criterion and by the verify's verbatim-string greps. Mirrors `PanelDialog.qml`'s `advancedCommand` discipline. |
| T-20-06-02 | Denial of Service | another application's confirm dialog | medium | mitigate | **The Phase 15 "who owns the prompt" carry-over, quoted from `research/PITFALLS.md`.** A `quickshell-session` overlay sits above every XDG toplevel, so a confirm dialog another app raises while the menu is open lands behind it. Mitigations: the surface is transient (summoned, destroyed on dismiss — it does not persist), Escape always closes it with no action taken, and `HyprlandFocusGrab` dismisses it on focus loss. No code fix currently exists for the general case; the residual is a live-verify item at plan 20-08's Gate B, not a pre-solved concern. |
| T-20-06-03 | Elevation of Privilege | `WlrKeyboardFocus.Exclusive` | medium | mitigate | Exclusive focus swallows every keypress while the menu is open, which is the point (a session-ending surface must not lose a keystroke) but also a trap-the-user risk. Mitigated by Escape always closing with no action taken and by `HyprlandFocusGrab` click-outside dismissal — the coexistence of the two is proven live in Task 1's human-check, not assumed, since no surface in this repo has combined them before. |
| T-20-06-04 | Spoofing | layer namespace `quickshell-session` | low | accept | Any local process can create a layer surface under any namespace; a spoofed session dialog could mimic this one. Out of this shell's control by protocol design, and an imitation gains no privilege it did not already have as a local process. Accepted at ASVS L1. |
| T-20-06-05 | Repudiation | LEDGER-02 record | medium | mitigate | The record states the measurement was NOT taken in its opening sentence and writes the permitted summary phrasing into itself, so a later summary cannot silently upgrade "wrapped" to "measured". The verify rejects closure-claim phrasings mechanically. |
| T-20-06-SC | Tampering | package-manager installs | low | accept | No package installs. Every binary invoked (`uwsm`, `hyprlock`, `systemctl`, `hyprshutdown`, `cliphist`) is already present and already invoked by the surface being replaced. |
</threat_model>

<verification>
- `colour-lint`, `motion-lint`, `keybind-doctor` and `quickshell-doctor --self-test` all exit 0.
- `grep -vE '^\s*--' keybinds.lua | grep -c wleave.sh` and `grep -vE '^\s*#' main.toml | grep -c wleave.sh` both return 0.
- `hyprctl layers -j` reports `quickshell-session` at level 3 while the menu is open, and nothing at all when it is dismissed (the summon-on-demand posture).
- `ClockActionsCapsule.qml` contains no `powerScriptPath` / `powerAvailable` / `powerAvailabilityProbe` in source lines, and its `powerCell` rendering block is unchanged in the diff.
</verification>

<success_criteria>
- All six actions are present, keyboard-navigable with a visible focus ring, mnemonic-addressable, and carry the real migrated command strings.
- All three entry points — keybind, walker menu, bar glyph — open one identical in-process surface, and none shells to `wleave.sh`.
- Shutdown and Reboot still route through `hyprshutdown`'s graceful compositor exit.
- LEDGER-02 is closed by construction and recorded as wrapped-without-measurement, with the permitted summary phrasing written into the record.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files:** `quickshell/.config/quickshell/modules/session/PowerMenu.qml`, `quickshell/.config/quickshell/modules/session/qmldir`, `.planning/phases/20-indicators-power-menu/20-LEDGER-02-RECORD.md`.

**New symbols:** the `PowerMenu` surface with a toggle verb called in-process by three consumers; local `lineHeightTight` (1.2) / `lineHeightNormal` (1.5) declarations inside `PowerMenu.qml`; a six-entry action table whose command arrays are inline literals.

**New shortcut/manifest entries:** `shortcuts.json` `power-menu` (chord `SUPER SHIFT` + `Q`); `shell.qml` `GlobalShortcut { name: "power-menu" }` and `import "modules/session"`; `keybinds.lua` `hl.dsp.global("quickshell:power-menu")`.

**New layer namespace in live use:** `quickshell-session` (declared in 20-03, first rendered here), plus its `quickshell-doctor` surface-registry row `session/PowerMenu.qml | quickshell-session | exact | 3 | noreserve | transient`.

**New glyphs introduced to this shell:** `logout`, `bedtime`, `ac_unit`, `restart_alt` (`lock` and `power_settings_new` already exist elsewhere).

**Deleted symbols:** `ClockActionsCapsule.qml`'s `powerScriptPath`, `powerAvailable`, `powerAvailabilityProbe` (and `powerLaunchProcess` if it becomes unused); `keybinds.lua:68`'s `exec_cmd` against `wleave.sh`; `main.toml`'s `wleave.sh` dispatch target.
</artifacts_produced>

<flagged_assumptions>
Edge-probe items surfaced but NOT authored into `must_haves` (probe category `unclassified`
stays `unresolved` and is surfaced here, never auto-resolved with a backstop):

- **QPOWER-01 / unclassified** — no edge derived for "Six actions available — Shutdown, Reboot,
  Suspend, Hibernate, Logout, Lock." Planner assumption: the action set is fixed at exactly six
  with no dynamic membership, so the usual zero-one-many edge does not arise; the UI-SPEC's S4
  and S5 rows carry that reasoning and are authored as truths above.
- **QPOWER-02 / unclassified** — no edge derived for "Fully keyboard-navigable with visible focus;
  Enter activates, Escape closes, first action auto-focused on open." Planner assumption: the
  interesting edge is grid-boundary behaviour, which this plan pins as NO wraparound (right from
  Suspend stays on Suspend, down from Hibernate stays on Hibernate). Recorded here because the
  probe did not derive it; if wraparound is later wanted it is a new decision, not a bug fix.
- **QPOWER-04 / unclassified** — no edge derived for "Shutdown and Reboot keep the graceful
  compositor exit." Planner assumption: the mechanism is carried over verbatim from the baseline
  transcription rather than re-derived, so there is no new edge to classify; the failure-mode
  question (what happens if `hyprshutdown` is unreachable) is captured as the S4/error backstop
  truth above.
- **LEDGER-02 / unclassified** — no edge derived for "MAINT-02 is settled by taking the D-29
  teardown measurement." Planner assumption: the requirement's own edge is moot because D-20-37
  declines the measurement; what is verifiable instead is the honesty of the record, which Task 3
  makes mechanically checkable.
</flagged_assumptions>

<output>
Create `.planning/phases/20-indicators-power-menu/20-06-SUMMARY.md` when done.
</output>
