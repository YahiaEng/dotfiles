---
phase: 11-quickshell-viability-gate
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - quickshell/.config/quickshell/shell.qml
  - quickshell/.config/quickshell/modules/ScreencopyProbe.qml
  - quickshell/.config/quickshell/shortcuts.json
  - hypr/.config/hypr/config/keybinds.conf
  - hypr/.config/hypr/config/permissions.conf
  - hypr/.config/hypr/hyprland.conf
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md
autonomous: false
requirements: [QS-02, QS-05]

must_haves:
  truths:
    - "A second Quickshell GlobalShortcut is declared, registered, manifested and bound — and adding it required editing exactly one manifest file plus one keybind line, which is the concrete benefit D-17 claimed the declared manifest would give Phases 14 and 16"
    - "`keybind-doctor` exits 0 after the second shortcut is added, with both manifest entries showing as registered and neither colliding"
    - "`quickshell-doctor` exits 0 after the second surface is added, with the reserved-array diff still identical across summoning BOTH surfaces"
    - "A live `ScreencopyView` capture of multiple real windows renders actual window content — not blank, black or placeholder tiles — confirmed by a human looking at it"
    - "The exact screencopy permission mechanics for Hyprland 0.56.0 are recorded verbatim in the evidence artifact from a run against the installed compositor, not copied from documentation: the config keyword form, the enforcement toggle, the three mode values, the exact verified quickshell binary path, and the fact that a grant requires a full Hyprland restart rather than `hyprctl reload`"
    - "`hypr/.config/hypr/config/permissions.conf` exists, is sourced from `hyprland.conf`, ships with enforcement OFF, and is documented as deliberately inert until Phase 16 flips it"
    - "With enforcement temporarily ON and the allow entries in place, every pre-existing screencopy consumer on this machine still works — screenshot capture and the desktop portal's screen-share path both verified before enforcement is reverted"
    - "ROADMAP Phase 11 criterion 5 no longer names a standalone Hyprland permissions config file that does not exist on this build"
    - "PROJECT.md's Quickshell adoption Key Decisions row no longer reads Pending — it carries the Phase 11 verdict and points at the evidence artifact"
    - "The evidence artifact carries a single top-level Verdict line, a filled gate table with all seven requirement rows resolved, and a Reproduce section whose commands actually rerun"
    - "No frame-rate, CPU or memory measurement of screencopy is performed or recorded — that budget is OVER-04's requirement in Phase 16 and is deliberately not pulled forward (D-12)"
  artifacts:
    - quickshell/.config/quickshell/modules/ScreencopyProbe.qml
    - hypr/.config/hypr/config/permissions.conf
    - .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md
  key_links:
    - "shell.qml GlobalShortcut (appid quickshell, name screencopy-probe) -> keybinds.conf `global, quickshell:screencopy-probe` -> shortcuts.json second manifest entry -> keybind-doctor's registered + collision checks"
    - "permissions.conf -> `source =` line in hyprland.conf -> Hyprland's permission manager (read once at startup, NOT on reload) -> the screencopy protocol grant for the exact verified binary path"
    - "ScreencopyProbe.qml `ScreencopyView` -> hyprland-toplevel-export / screencopy protocol -> live window content -> the Phase 16 feasibility answer"
    - "The evidence artifact Verdict line -> PROJECT.md Key Decisions outcome -> whether Phases 14-17 stand or v3.0 rescopes (D-09)"
  prohibitions:
    - "No file named after the roadmap's imagined standalone permissions config is created or referenced anywhere under hypr/ or quickshell/ — that name is not a real Hyprland 0.56.0 concept"
    - "No wildcard, loose prefix, or broad regex in the `permission =` client identifier — it names the exact binary path verified by `pacman -Ql`, so a grant intended for one binary cannot be inherited by an unrelated one"
    - "Enforcement is NOT left enabled in the committed config — the file ships inert and Phase 16 flips it"
    - "No `ask`-mode permission entry is used — `allow` sidesteps the unresolved `hyprland-guiutils` popup-UI dependency and matches D-12's feasibility-only framing"
    - "No frame/CPU/memory budget is measured or recorded — that is OVER-04's requirement in Phase 16 (D-12)"
    - "No commitment is made here to Phase 16's fallback approach (D-12 explicitly forbids pre-committing it)"
    - "No permission change is chained directly after an `hyprctl reload` and assumed to have taken effect — grants are read once at compositor startup"
    - "No colour, hex literal or style value in ScreencopyProbe.qml (D-04 applies to every repo-authored QML this phase produces)"
    - "No wholesale rewrite of ROADMAP.md, REQUIREMENTS.md or PROJECT.md — scoped Edit calls only"
---

<objective>
Answer the workspace-overview feasibility question on this build, record the screencopy permission mechanics as verified fact rather than as researched assumption, and write the phase's final verdict.

Purpose: ROADMAP criterion 5 exists so that "Phase 16's scope is a decision, not a discovery." Two things stand in the way of that today. First, the criterion names a Hyprland config file that does not exist on 0.56.0 — the real mechanism is a config keyword plus a category block in an ordinary sourced `.conf` module. Second, research could only reach that mechanism through web sources and tagged it `[ASSUMED — LOW confidence]`, including the binary path and the restart requirement. Copying an unverified stanza into an evidence artifact and calling the question answered is precisely the failure mode PROJECT.md already records once: Phase 7's research claimed a walker flag "already ships and works" from reading a config file, never running it, and the resulting plan was wrong.

This plan also closes the phase: the second Quickshell shortcut proves the manifest mechanism scales the way D-17 promised, and the verdict block turns seven requirement rows into one decision.

Output: a screencopy probe surface, a verified and deliberately-inert `permissions.conf`, an amended criterion 5, an updated PROJECT.md decision row, and a complete evidence artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-quickshell-viability-gate/11-CONTEXT.md
@.planning/phases/11-quickshell-viability-gate/11-RESEARCH.md
@.planning/phases/11-quickshell-viability-gate/11-PATTERNS.md
@.planning/phases/11-quickshell-viability-gate/11-01-SUMMARY.md
@.planning/phases/11-quickshell-viability-gate/11-02-SUMMARY.md
@.planning/phases/11-quickshell-viability-gate/11-04-SUMMARY.md
</context>

## Artifacts this phase produces

**Bold** = created or changed by this plan.

| Kind | Symbol / path |
|---|---|
| Stow package | `quickshell/` (plan 01) |
| QML root | `quickshell/.config/quickshell/shell.qml` — **gains a second GlobalShortcut and loader** |
| QML module | `quickshell/.config/quickshell/modules/Probe.qml` (plan 01) |
| **QML module (new)** | **`quickshell/.config/quickshell/modules/ScreencopyProbe.qml`** |
| **QML type name (new)** | **`ScreencopyProbe`** |
| Shortcut manifest | `quickshell/.config/quickshell/shortcuts.json` — **gains a second entry** |
| **GlobalShortcut id (new)** | **`quickshell:screencopy-probe`** |
| **Layer namespace (new)** | **`quickshell-screencopy-probe`** |
| **Keybind (new)** | **`$mainMod SHIFT, K` → `global, quickshell:screencopy-probe`** (verified free against all 78 existing binds — `K` is unused under every modifier combination) |
| **Hyprland config (new)** | **`hypr/.config/hypr/config/permissions.conf`**, sourced from `hyprland.conf` alongside the existing six `source =` lines |
| Launcher / gates | `quickshell-launch.sh`, `keybind-doctor`, `quickshell-doctor` (plans 01-04) |
| State dir / files | `~/.local/state/quickshell/`, `probe.json`, `doctor-baseline.json` |
| Log file | `~/.cache/quickshell.log` |
| Evidence artifact | `.planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md` — **completed: all seven gate rows resolved, verdict written, reproduce section final** |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the screencopy probe surface and prove the shortcut manifest scales</name>
  <precondition>Plan 01's shell is running and plan 02's `keybind-doctor` and plan 03's `quickshell-doctor` both exit 0 — this task's whole point is that they still do afterwards.</precondition>
  <files>quickshell/.config/quickshell/shell.qml, quickshell/.config/quickshell/modules/ScreencopyProbe.qml, quickshell/.config/quickshell/shortcuts.json, hypr/.config/hypr/config/keybinds.conf</files>
  <read_first>
    - `quickshell/.config/quickshell/shell.qml` and `quickshell/.config/quickshell/modules/Probe.qml` — the existing GlobalShortcut declaration, the loader/summon mechanism, and the exact `PanelWindow` + `WlrLayershell` + `HyprlandFocusGrab` property set to mirror
    - `quickshell/.config/quickshell/shortcuts.json` — the manifest entry shape (`appid`, `name`, `chord.mods`, `chord.key`, `description`) the second entry must match
    - `hypr/.config/hypr/config/keybinds.conf` — the existing `global, quickshell:probe` line and the trailing-`#`-description convention every bind carries
    - `.planning/phases/11-quickshell-viability-gate/11-RESEARCH.md` — §"Sources (Secondary)" the `ScreencopyView` documentation link (capture-source requirements, properties, methods) and §"Common Pitfalls" Pitfall 4 (duplicate `appid`+`name` crashes rather than rejecting)
    - `.planning/phases/11-quickshell-viability-gate/11-CONTEXT.md` — D-12 (feasibility-only scope), D-17 (the manifest's stated benefit for Phases 14 and 16), D-19 (minimal structure), D-21 (layer-shell convention)
  </read_first>
  <action>
Create `quickshell/.config/quickshell/modules/ScreencopyProbe.qml` as a second summonable surface. It stays inside D-19's minimal shape: one more file under the existing `modules/` directory, no new subdirectories.

It carries a `ScreencopyView` (from `Quickshell.Wayland`) rendering a live capture of **multiple** real toplevel windows simultaneously — criterion 5 says "multi-window", and a single-window capture would not answer the question Phase 16 needs answered. Lay them out as a plain grid of tiles at whatever size is convenient. Add a visible per-tile caption naming the window each tile is supposed to be showing, so a blank tile is unambiguously distinguishable from a tile that captured a genuinely blank window.

Its `PanelWindow` inherits D-21's convention exactly as `Probe.qml` does: overlay layer, `exclusiveZone: 0`, `WlrLayershell.keyboardFocus` set explicitly, and a distinct namespace `quickshell-screencopy-probe` that no existing client uses. Give it the same `HyprlandFocusGrab` click-outside dismiss. It stays unstyled — QtQuick defaults only, no authored colour of any kind (D-04). Do not add any frame-rate counter, timing instrumentation, or resource readout: that measurement is OVER-04's requirement in Phase 16 and D-12 explicitly forbids pulling it forward.

Declare a second `GlobalShortcut` in `shell.qml` with appid `quickshell` and name `screencopy-probe`, toggling this surface the same way the first shortcut toggles the probe. The appid+name pair must be distinct from the first — duplicates can crash Quickshell rather than reject cleanly.

Add the matching manifest entry to `shortcuts.json` and the matching bind to `keybinds.conf`: `$mainMod SHIFT, K` dispatching `global, quickshell:screencopy-probe`, with a trailing `#` description. `K` was verified free against all 78 existing binds under every modifier combination.

Then rerun both gates and treat their results as the real deliverable of this task. Adding a Quickshell-owned chord should have required editing exactly one manifest file and one keybind line — that is the concrete benefit D-17 claimed the declared manifest would give Phases 14 and 16, and this is the first and only chance to test that claim inside the phase that made it. `keybind-doctor` must exit 0 with both entries registered and neither colliding. `quickshell-doctor` must exit 0 with the reserved-array diff still identical when *both* surfaces are summoned, and with the namespace-discipline check now covering two namespaces.

Record in the evidence artifact what adding the second shortcut actually cost, in files touched and gate reruns — a plain sentence Phases 14 and 16 can rely on or contradict.
  </action>
  <acceptance_criteria>
    - `quickshell/.config/quickshell/modules/ScreencopyProbe.qml` exists and `find quickshell/.config/quickshell -mindepth 1 -type d` still lists only `.../modules` (D-19 holds)
    - `jq 'length' quickshell/.config/quickshell/shortcuts.json` returns 2, and `jq -r '[.[] | .appid + ":" + .name] | unique | length'` also returns 2 (no duplicate pair)
    - `hyprctl globalshortcuts` lists both `quickshell:probe` and `quickshell:screencopy-probe`
    - `hypr/.config/hypr/scripts/keybind-doctor` exits 0
    - `hypr/.config/hypr/scripts/quickshell-doctor` exits 0
    - With both surfaces summoned, `hyprctl layers -j` level 3 lists exactly the two `quickshell-` namespaces and nothing else, and `hyprctl monitors -j | jq -c '[.[].reserved]'` is identical to the pre-summon reading
    - `grep -riE '#[0-9a-f]{3,8}\b' quickshell/.config/quickshell/` returns no matches
    - `grep -icE 'fps|frame.?rate|elapsed|benchmark' quickshell/.config/quickshell/modules/ScreencopyProbe.qml` returns 0 (D-12: no budget measurement pulled forward)
    - `grep -c 'quickshell:screencopy-probe' hypr/.config/hypr/config/keybinds.conf` returns 1
    - `git status --porcelain` is clean after the commit
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test "$(jq length quickshell/.config/quickshell/shortcuts.json)" = "2"; test "$(jq -r "[.[] | .appid + \":\" + .name] | unique | length" quickshell/.config/quickshell/shortcuts.json)" = "2"; hyprctl globalshortcuts | grep -q "screencopy-probe"; hypr/.config/hypr/scripts/keybind-doctor >/dev/null; hypr/.config/hypr/scripts/quickshell-doctor >/dev/null; ! grep -riqE "#[0-9a-f]{3,8}\b" quickshell/.config/quickshell/; echo SC-PROBE-OK'</automated>
  </verify>
  <done>A second Quickshell-owned chord exists, both gates are still green with two surfaces and two manifest entries, and the cost of adding one is recorded rather than assumed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify the screencopy permission mechanics against the installed compositor (D-12)</name>
  <precondition>The operator can restart the Hyprland session twice during this task and is at the keyboard. Permission grants are read once at compositor startup — `hyprctl reload` does not pick them up — so there is no way to verify the mechanics without restarts. Nothing else in this phase depends on the session's continuity, so this is the right task to spend them in.</precondition>
  <reversibility rating="reversible">`permissions.conf` ships with enforcement OFF, so the committed state is a documented no-op. Reverting the whole task is deleting one file and one `source =` line.</reversibility>
  <files>hypr/.config/hypr/config/permissions.conf, hypr/.config/hypr/hyprland.conf, .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md</files>
  <read_first>
    - `hypr/.config/hypr/hyprland.conf` — the six existing `source = ~/.config/hypr/config/*.conf` lines (7-12) and the generated-theme source at line 15, so the new source line lands among the config-module sources and not after the generated one
    - `hypr/.config/hypr/config/windowrules.conf` — an existing sourced module, for the file-header comment style this repo uses on config modules
    - `.planning/phases/11-quickshell-viability-gate/11-RESEARCH.md` — §"Code Examples" the screencopy permission stanza in full (including every `[ASSUMED]` tag on it), §"Common Pitfalls" Pitfall 3 (restart required, not reload), §"Open Questions" 2 and 3, §"Security Domain" the V4 Access Control row on client-identifier scoping
    - `.planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md` — the `## Verified binary contract` section plan 01 wrote, which holds the `pacman -Ql` output and therefore the exact binary path this task's client identifier must use
    - `install.sh` — lines around 78-79 and 142 (`grim`, `slurp`, `hyprshot`) and 58-59 (`xdg-desktop-portal-hyprland`, `xdg-desktop-portal-gtk`), the pre-existing screencopy consumers on this machine
    - `hypr/.config/hypr/scripts/capture-region.sh` and `capture-window.sh` — the live screenshot paths that must still work after enforcement is toggled
  </read_first>
  <action>
Sequence matters here more than anything else in this plan. Do the zero-risk proof first, the mechanics verification second, and leave the machine in the safe state.

**Step A — capture with enforcement at its default.** Hyprland's enforcement toggle defaults to off, meaning screencopy is unrestricted today. Open three or four visibly-distinct real windows (a terminal with text, a file manager, a browser). Summon the screencopy probe with `$mainMod SHIFT, K`. This proves the `ScreencopyView` protocol path works on this build with zero blast radius and no config change at all. Capture what you see.

**Step B — verify the permission mechanics, bounded.** Only now write `hypr/.config/hypr/config/permissions.conf` and add its `source =` line to `hyprland.conf` among the existing config-module sources.

The file contains the enforcement toggle inside its config category block, plus one `permission =` line per client. Use `allow` mode only — never `ask`, which depends on an unresolved popup-UI package question and is irrelevant to a feasibility probe. The client identifier is a **path**, and it must be the exact binary path verified by `pacman -Ql` and recorded in the evidence artifact's `## Verified binary contract` section. Do not use a wildcard, a loose prefix, or a broad regex: a permission intended for one binary must not be inheritable by an unrelated binary that happens to match a sloppy pattern.

Before enabling enforcement, enumerate the machine's **existing** screencopy consumers and give each one its own allow entry. This repo already ships `grim`, `slurp`, `hyprshot` and `xdg-desktop-portal-hyprland`, all installed by `install.sh`, and two live capture scripts bound to keys. Turning on a global enforcement switch without allow entries for them would break screenshots and screen sharing — that is a regression, and standing constraint 4 makes this milestone additive-only. Resolve each one's real binary path with `pacman -Ql` rather than assuming; note that `hyprshot` is a wrapper script, so the process actually issuing the capture request is the tool it invokes, and the allow entry must name that.

Set enforcement ON, restart the Hyprland session fully (not `hyprctl reload` — grants are read once at startup, and a capture that returns blank tiles despite a syntactically correct permission line is almost always this and not a `ScreencopyView` bug). Then verify three things: the screencopy probe still captures real content; the region and window capture keybinds still produce a real screenshot; and the desktop portal's screen-share path still offers and delivers a stream.

**Step C — leave it inert and commit that state.** Set enforcement back OFF in the committed file, leave every allow entry present but commented with a header explaining that they are pre-verified and that Phase 16 activates the whole file by flipping one value. Restart Hyprland once more and confirm the desktop is back to its pre-task behaviour. The committed file must be a documented, verified, deliberately-inert module — not a live global enforcement switch this phase never had a mandate to throw.

The header comment on `permissions.conf` states plainly: the exact keyword syntax, the three mode values, the enforcement key's default, the fact that a change requires a full compositor restart, and that the file is inert until Phase 16 (OVER-04) enables it.

Record in the evidence artifact, under a `## Criterion 5 — screencopy feasibility` section: the Step A result, the exact verified syntax of both the category block and the permission line as they were actually loaded by the compositor, the exact binary path used, the full list of allow entries needed for existing consumers, the observed restart-vs-reload behaviour, and the Step B verification results for the probe, the screenshot keybinds and the portal. Record explicitly that no frame/CPU budget was measured and that OVER-04 in Phase 16 still owns it.
  </action>
  <acceptance_criteria>
    - `hypr/.config/hypr/config/permissions.conf` exists and `grep -c 'permissions.conf' hypr/.config/hypr/hyprland.conf` returns 1
    - The committed `permissions.conf` has enforcement set to its OFF value, and its header states the file is inert until Phase 16
    - Every `permission =` line names an absolute binary path containing no `*`, `?`, `.*`, `[` or `|` — `grep -E '^permission' hypr/.config/hypr/config/permissions.conf | grep -cE '\*|\?|\[|\|'` returns 0
    - The quickshell client identifier string is byte-identical to the path recorded in the evidence artifact's `## Verified binary contract` section
    - Allow entries exist for the pre-existing screencopy consumers (grim and the desktop portal at minimum), each with a path resolved by `pacman -Ql`
    - `grep -ril 'ecosystem\.conf' hypr/ quickshell/` returns nothing — the roadmap's imagined standalone permissions file is not created or referenced anywhere in the config tree
    - The evidence artifact contains a `## Criterion 5 — screencopy feasibility` section holding the verbatim loaded syntax, the verified binary path, the allow-entry list and the restart-vs-reload finding
    - The evidence artifact records the human confirmation that the multi-window capture showed real window content, naming the windows captured
    - The evidence artifact records that the screenshot keybinds and the portal screen-share still worked with enforcement ON
    - The evidence artifact states that no frame/CPU/memory budget was measured and that OVER-04 retains it
    - `grep -icE 'fps|frames per second|cpu %|benchmark' .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md` returns 0 outside the sentence explicitly disclaiming the measurement
    - After Step C's restart: `hypr/.config/hypr/scripts/quickshell-doctor` exits 0, `hypr/.config/hypr/scripts/keybind-doctor` exits 0, and `theme-engine/.config/theme-engine/theme-doctor` exits 0
    - `git status --porcelain` is clean after the commit
  </acceptance_criteria>
  <verify>
    <human-check>
1. Open three or four visibly-distinct windows. Press Super+Shift+K. Confirm the tiles show real window content — actual text, actual UI — not blank, black or placeholder rectangles. Name which windows you saw.
2. After the enforcement-ON restart: press Super+Shift+K again and confirm the capture still shows real content.
3. Still with enforcement ON: take a region screenshot and a window screenshot with their keybinds and confirm both produce a real image file.
4. Still with enforcement ON: start a screen share through an app that uses the desktop portal and confirm a real stream appears.
5. After the enforcement-OFF restart: confirm the desktop behaves exactly as it did before this task.
Report each result.
    </human-check>
    <automated>bash -c 'set -e; test -f hypr/.config/hypr/config/permissions.conf; test "$(grep -c "permissions.conf" hypr/.config/hypr/hyprland.conf)" = "1"; test "$(grep -E "^permission" hypr/.config/hypr/config/permissions.conf | grep -cE "\*|\?|\[|\|")" = "0"; test -z "$(grep -ril "ecosystem\.conf" hypr/ quickshell/ || true)"; grep -q "Criterion 5" .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md; hypr/.config/hypr/scripts/quickshell-doctor >/dev/null; hypr/.config/hypr/scripts/keybind-doctor >/dev/null; theme-engine/.config/theme-engine/theme-doctor >/dev/null; echo PERM-OK'</automated>
  </verify>
  <done>The screencopy mechanics are recorded as verified fact from a run against the installed compositor — including the client-identifier path, the restart requirement, and the allow entries every existing consumer needs — and the committed config is a documented no-op that Phase 16 activates with one edit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Amend criterion 5, record the decision outcome, and write the phase verdict</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — §"Phase 11: Quickshell Viability Gate" success criterion 5 and the **Open questions owned** line, both of which name a Hyprland permissions file that does not exist; also the `**Plans**: TBD` line
    - `.planning/PROJECT.md` — §"Key Decisions", the row reading "Adopt Quickshell as the shell toolkit, reversing the v2.0 Out-of-Scope entry" whose Outcome column currently reads "— Pending (v3.0 Phase 11 viability gate decides)"; and §"Out of Scope", the reversed Quickshell entry
    - `.planning/REQUIREMENTS.md` — the QS-01..QS-06 and MAINT-01 checkbox lines and the Traceability rows, to confirm every ID this phase owns is accounted for
    - `.planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md` — the whole file as built up by plans 01-04 and this plan's Tasks 1-2
    - `.planning/milestones/v2.0-phases/08-waybar-evolution/08-BAR-02-EVIDENCE.md` — lines 52-75 (the gate table and the "which gate fired" narrative) and lines 149-166 (the Reproduce section), the structural precedent to finish matching
  </read_first>
  <action>
**Amend criterion 5.** D-15 established that fixing roadmap wording the machine cannot satisfy is in-scope phase work, not a deviation; extend that treatment here. Criterion 5 and the Open-questions-owned line both name a standalone Hyprland permissions configuration file. Verified against 0.56.0: no such file exists as a distinct concept. The real mechanism is a config keyword plus a category block, both written in ordinary sourced `.conf` files. Replace the phrasing in both places with the mechanism this phase actually verified, and add a short parenthetical pointing at the evidence artifact. Do not restate the non-existent filename in the parenthetical — the evidence artifact and `permissions.conf`'s own header are where the full explanation lives, and restating it would make the amendment's own acceptance check self-invalidating. Use scoped `Edit` calls; the Phase 12-17 sections must be untouched.

Also fill in the `**Plans**: TBD` line for Phase 11 with the five plans and their one-line objectives, and mark this phase's plan checkboxes.

**Record the decision outcome.** PROJECT.md's Key Decisions row for the Quickshell adoption reversal currently reads Pending, waiting on this exact gate. Update its Outcome column with the verdict, a one-line reason, and a pointer to the evidence artifact — following the style of the neighbouring rows, which read as short judgements with a phase reference. If the verdict is STOP, the row records the reversal-back and the Out of Scope entry is restored with the evidence artifact as the stated reason (D-09); do not perform that rewrite silently, and surface it to the operator before applying it.

**Write the verdict.** Finish the evidence artifact so it stands alone for a reader who has none of this context:
- One `Verdict:` line at the very top, from the vocabulary this phase's decisions already name — `PASS` or `STOP` for QS-02, with everything else as record-and-continue findings. Invent no new verdict words.
- A gate table with all seven rows (QS-01..QS-06, MAINT-01) resolved: criterion quoted verbatim, method, instrument, raw result, PASS/FAIL/SKIP. No blank cells.
- A "which gate fired" narrative distinguishing the genuinely decisive result (QS-02 — the sole stop-trigger) from the record-and-continue findings, so a later reader does not mistake a recorded limitation for a near-miss failure.
- A consolidated **findings and caveats** section pulling together everything this phase recorded rather than resolved: the virtual-output caveat (D-07), the abandonment of structured bind parsing and why (D-14), the absence of Quickshell-side shortcut introspection on 0.3.0 (D-17), the answer to open question #1 (D-13 branch taken or not), the brightness criterion skipped for lack of a backlight device, the zero-outputs and surface-ordering edges left unverified, the screencopy restart requirement, and the note that OVER-04's budget stays Phase 16's.
- A final `## Reproduce` section listing every command needed to rerun the phase's mechanical claims from scratch.

Confirm the whole set is consistent before finishing: every QS and MAINT ID resolved, `keybind-doctor` exit 0, `quickshell-doctor` exit 0, `theme-doctor` exit 0, `theme-parity` exit 0, and `git status --porcelain` empty.
  </action>
  <acceptance_criteria>
    - `grep -ci 'ecosystem\.conf' .planning/ROADMAP.md` returns 0
    - ROADMAP Phase 11 criterion 5 and the Open-questions-owned line both name the verified mechanism and carry a pointer to the evidence artifact
    - `git diff --stat .planning/ROADMAP.md` shows a scoped diff — the Phase 12 through Phase 17 sections are unchanged
    - ROADMAP Phase 11's `**Plans**` line lists five plans with one-line objectives, and no `TBD` remains in the Phase 11 section
    - PROJECT.md's Quickshell adoption Key Decisions row's Outcome column no longer contains the word `Pending` and names the verdict plus the evidence artifact
    - `grep -cE '^Verdict:\s+(PASS|STOP)' .planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md` returns 1
    - The evidence artifact's gate table has seven rows and no empty `Raw result` or `PASS/FAIL` cell
    - The evidence artifact contains a consolidated findings-and-caveats section naming, at minimum: the virtual-output caveat, the structured-parsing abandonment, the missing 0.3.0 shortcut introspection, the open-question-#1 answer, the brightness skip, and the screencopy restart requirement
    - The `## Reproduce` section lists at least eight exact runnable commands
    - `hypr/.config/hypr/scripts/keybind-doctor` exits 0
    - `hypr/.config/hypr/scripts/quickshell-doctor` exits 0
    - `theme-engine/.config/theme-engine/theme-doctor` exits 0 (baseline was 136 passed / 0 failed before this phase)
    - `theme-engine/.config/theme-engine/theme-parity` exits 0 (baseline was 1542 passed / 0 failed)
    - `git status --porcelain` is empty
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; F=.planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md; test "$(grep -ci "ecosystem\.conf" .planning/ROADMAP.md)" = "0"; test "$(grep -cE "^Verdict:\s+(PASS|STOP)" "$F")" = "1"; ! grep -q "Pending (v3.0 Phase 11" .planning/PROJECT.md; hypr/.config/hypr/scripts/keybind-doctor >/dev/null; hypr/.config/hypr/scripts/quickshell-doctor >/dev/null; theme-engine/.config/theme-engine/theme-doctor >/dev/null; theme-engine/.config/theme-engine/theme-parity >/dev/null; test -z "$(git status --porcelain)"; echo VERDICT-OK'</automated>
  </verify>
  <done>The roadmap no longer asserts a mechanism that does not exist, PROJECT.md's Pending decision has an answer, and the evidence artifact is a self-contained document that a reader with no context can use to understand the verdict and rerun every claim behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hyprland permission manager → screencopy protocol clients | A `permission =` grant decides which binaries may capture the contents of the user's screen |
| Compositor screencopy buffers → Quickshell process | Live window content, including anything on screen, flows into a QML client |
| Enforcement toggle → every existing screencopy consumer | Flipping one global value changes the capture behaviour of grim, hyprshot, the desktop portal and any future client at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-20 | Elevation of Privilege | The `permission =` client identifier | high | mitigate | ASVS V4 Access Control. The identifier is a path and accepts a regex, so a loose pattern would let an unrelated binary inherit a screen-capture grant. Scope it to the exact binary path verified by `pacman -Ql` and recorded in the evidence artifact, with no `*`, `?`, `[`, `|` or `.*`. Asserted by a grep in Task 2's acceptance criteria. |
| T-11-21 | Denial of Service | Global enforcement toggle vs. existing screencopy consumers | high | mitigate | This machine already runs four screencopy consumers (`grim`, `slurp`, `hyprshot`, `xdg-desktop-portal-hyprland`) plus two keybound capture scripts. Enabling enforcement without allow entries breaks screenshots and screen sharing — a real regression against standing constraint 4's additive-only rule. Mitigated by enumerating every consumer and granting each one explicitly *before* enforcement is enabled, verifying all of them with enforcement ON, and then shipping the file inert with enforcement OFF so the committed state cannot regress anyone. |
| T-11-22 | Information Disclosure | `ScreencopyView` rendering live window content | medium | accept | Screen capture on a single-user desktop is the intended capability, and the probe renders only to the local session's own overlay surface — nothing is written to disk, transmitted, or logged. Accepted for a feasibility probe. Phase 16, where captures persist in a live overview surface, should revisit whether any capture path can outlive its window. |
| T-11-23 | Repudiation | Recording researched assumptions as verified findings | high | mitigate | RESEARCH.md tagged the permission syntax, the binary path and the restart requirement `[ASSUMED — LOW confidence]`. PROJECT.md already records one instance of this exact failure costing a phase (Phase 7's walker flag, believed from reading a config file rather than running it). Mitigated by Task 2's ordering: exercise the mechanics against the running compositor first, then record only what was observed, with the verbatim loaded syntax quoted. |
| T-11-24 | Denial of Service | A permission change assumed to apply on reload | medium | mitigate | Grants are read once at compositor startup, so `hyprctl reload` leaves them inert and a capture returns blank tiles that look like a client bug. Mitigated by explicitly budgeting two full session restarts into the task and by recording the observed reload-vs-restart behaviour in the evidence artifact so Phase 16 does not rediscover it. |
</threat_model>

<verification>
1. Both Quickshell shortcuts registered; `keybind-doctor` and `quickshell-doctor` both exit 0 with two surfaces and two manifest entries.
2. Reserved arrays identical with both surfaces summoned.
3. Human-confirmed: a live multi-window capture shows real window content.
4. `permissions.conf` exists, is sourced, ships inert, and every client identifier is an exact wildcard-free path.
5. With enforcement temporarily ON, the probe, both screenshot keybinds and the portal screen-share all still work.
6. No standalone-permissions-file name is referenced anywhere in `hypr/` or `quickshell/`, and none remains in ROADMAP.md.
7. PROJECT.md's Quickshell decision row carries the verdict, not `Pending`.
8. Evidence artifact: one Verdict line, seven resolved gate rows, a findings-and-caveats section, and a Reproduce section.
9. All four gates green: `keybind-doctor`, `quickshell-doctor`, `theme-doctor`, `theme-parity`. `git status --porcelain` empty.
</verification>

<success_criteria>
- Criterion 5 answered: the workspace-overview feasibility question is decided rather than discovered, with the permission mechanics recorded as verified fact and the frame/CPU budget deliberately left to OVER-04.
- QS-05 extended: a second registration mechanism claim is added, cross-checked and proven non-colliding — the manifest's promised benefit tested inside the phase that promised it.
- QS-02 closed: the phase's verdict is written, dated and consistent across ROADMAP.md, PROJECT.md and the evidence artifact.
- The desktop is left in exactly the state it started in, minus a headless shell root that renders nothing.
</success_criteria>

<output>
Create `.planning/phases/11-quickshell-viability-gate/11-05-SUMMARY.md` when done.
</output>
