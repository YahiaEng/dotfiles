---
phase: 13.1-hyprland-lua-config-migration
verified: 2026-07-28T17:15:00Z
status: gaps_found
score: 7/7 ROADMAP success criteria verified as literally worded; 1 unresolved Critical code-review finding (CR-01) and 1 unconfirmed tracked deviation (WINDOWS #11) surface as gaps
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "A fresh machine reproduces the whole desktop setup through install.sh + stow.sh (project core value; ROADMAP Success Criterion 7 read at the project-constraint level, not just the Lua config narrowly)"
    status: partial
    reason: "This phase's own 13.1-10 commits (a208d51, 79a9863) fixed the 'whole-directory symlink fold' fresh-machine bug for exactly two stow targets (~/.config/hypr, ~/.local) by pre-creating them as real directories before the stow --restow loop — proven by running the reproduction test, not just reading the script. The identical bug class was left live and unaddressed for the `wallpapers` stow package: stow.sh's PACKAGES loop (lines 19-41, stows at line 145-164) restows `wallpapers` (writing into ~/Pictures/Wallpapers, ~/Pictures/Screenshots) with no equivalent pre-creation of ~/Pictures before the loop runs. The only mkdir -p for ~/Pictures/Wallpapers is at line 354, AFTER the stow loop — confirmed by direct inspection, matching 13.1-REVIEW.md's CR-01 finding exactly. On a genuinely fresh machine (~/Pictures absent, matches install.sh's documented pre-graphical-session run order), `stow --restow wallpapers` folds ~/Pictures into a symlink into the repo, so every later wallpaper-switch/matugen write and every hyprshot/grim screenshot write lands inside the git checkout — directly contradicting this project's own stated core value ('the whole setup reproduces from scratch with one script') and its git-clean invariant, for the same reason the hypr/.local fix was needed."
    artifacts:
      - path: "stow.sh"
        issue: "Lines 19-41 (PACKAGES array) and 145-164 (restow loop) stow the `wallpapers` package with no pre-creation of ~/Pictures/{Wallpapers,Screenshots} as real directories, unlike the equivalent pre-create calls this same phase added for ~/.config/hypr (line 116) and ~/.local (line 143)."
    missing:
      - "Pre-create ~/Pictures/Wallpapers and ~/Pictures/Screenshots as real directories before the `for pkg in \"${PACKAGES[@]}\"` stow loop, mirroring the pattern already used for fish/gtk-3.0/gtk-4.0/quickshell/systemd/hypr/.local (see 13.1-REVIEW.md CR-01 for the exact fix)."
deferred:
  - truth: "quickshell-doctor's ~8 legacy 'hyprctl dispatch global' call sites are retargeted to the Lua expression form"
    addressed_in: "WINDOWS #14 (tracked, deliberately deferred by explicit operator decision 2026-07-28 in debug session waybar-workspace-click-dead — plain Branch C was chosen over 'C + quickshell-doctor')"
    evidence: ".planning/WINDOWS.md row 14: 'DELIBERATELY DEFERRED by operator decision ... CAUTION when picking this up: quickshell-doctor must NOT be run casually to test the change — its headless-output add/remove test previously SEGV-crashed this compositor.'"
  - truth: "waybar's compiled-in hyprland/workspaces click dispatch is fixed"
    addressed_in: "WINDOWS #15 (tracked, accepted debt — retires when Quickshell replaces waybar)"
    evidence: ".planning/WINDOWS.md row 15: 'ACCEPTED DEBT, NOT A BUG TO FIX HERE ... dies with waybar at the Quickshell cutover.' Upstream fix (Waybar PR #5013) postdates the installed 0.15.0 package."
human_verification:
  - test: "Open each of the six window-rule-affected surfaces (wallpaper-picker, icon-theme-picker, font-switcher, network-manager, cheat-sheet, yazi-fm) on the live desktop and confirm whether they open at ~85%/70% of the monitor as windowrules.lua's `size` field specifies, or fall back to their client-preferred default size."
    expected: "All six open at the intended percentage-of-monitor size, matching pre-migration hyprlang behaviour."
    why_human: "13.1-07 Task 1 mechanically proved (via a nested hypr-lua-harness instance, 6 spelling variants tried) that the Lua `hl.window_rule` `size` field's percentage form (\"85% 85%\", \"70% 65%\") registers with zero configerrors but has NO runtime effect on this installed Hyprland 0.56.1 Lua config manager — a genuine, previously-undocumented functional regression versus the currently-live hyprlang session, not a verification-channel limitation (recorded in COVERAGE.md 'Window-rule size field' section, and WINDOWS.md row 11, still marked status=open with no resolved timestamp). COVERAGE.md explicitly handed this to 13.1-08's Wave 7 operator as a MUST-visually-confirm item before/after cutover. 13.1-10's Task-2-supersession record states 'Window-rule visual properties — CLOSED, operator-verified... including the float/size/centre/pin/workspace placement rules' against the real desktop, which is a plausible closure of this exact item, but it names the property class generically rather than the six specific affected windows by name, and WINDOWS.md row 11 itself was never updated to 'fixed' the way the directly-related row 12 and row 13 items were. This gap between a generic 'closed' note and an un-updated per-item tracker entry cannot be resolved from documentation alone — it requires directly observing the six windows open at their intended size."
overrides: []
---

# Phase 13.1: Hyprland Lua Config Migration Verification Report

**Phase Goal:** Move the Hyprland config off hyprlang to Lua before 0.57 removes `.conf` support, and convert the theme-engine's two Hyprland-format outputs from generated config *syntax* into generated *data* — while proving the resulting config is behaviourally identical.
**Verified:** 2026-07-28T17:15:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1-7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Hyprland starts from `hyprland.lua` with no `.conf` in the load path, deprecation warning gone | VERIFIED | Live check on running compositor (pid 969, cold-boot session, boot `2026-07-28 16:23:24`, process start `16:23:48`): `/run/user/1000/hypr/<sig>/hyprland.log` reads `[cfg] Using lua config found at /home/aorus/.config/hypr/hyprland.lua`. `find hypr/.config/hypr -iname "*.conf"` returns only `hyprlock.conf` and `hypridle.conf` (both intentionally-retained non-Hyprland-core surfaces — hyprlock has its own hyprlang parser, hypridle is a separate daemon never migrated by this phase's scope). Grep of the full log for `deprecat`/`0.57`/`removed` returns nothing. |
| 2 | `hypr-equivalence-check` reports exact match or every divergence individually recorded with a reason | VERIFIED (for the automated gate's channel) | Ran `hypr-equivalence-check` live myself: under the CURRENT live theme (`catppuccin`, differs from the baseline's captured `dracula`), output is `PASS: 1 FAIL: 2` — the two extra fails are exactly the theme-dependent `general:col.active_border`/`col.inactive_border` values, expected and explained by the theme mismatch, not a regression. Cross-checked against `13.1-EQUIVALENCE-REPORT.md`'s own committed run (executed under the matched `dracula` theme, as the report explicitly requires): `PASS: 2 FAIL: 1`, with the sole `!`-prefixed divergence being the two `bindm mouse:272/273` `mouse`-field records — individually reasoned in the report (a `hyprctl -j binds` JSON-serialization gap for Lua-registered mouse binds, not mechanically verifiable, compensated by a named manual mouse-drag check). Live binds count = 80, matching baseline. Note: this criterion's automated channel does NOT cover the window-rule `size`-field regression — see gap/human-verification items below; that divergence is real but invisible to `hyprctl -j getoption`/`binds`/`animations`, and is tracked separately in COVERAGE.md. |
| 3 | Theme switching still re-themes desktop in both modes; two Hyprland-format contract entries collapsed to one `lua-table` entry | VERIFIED | `theme-engine/.config/theme-engine/contract.json` inspected directly: exactly one `hyprland-tokens.lua` entry with `"format": "lua-table"`, plus `hyprlock.conf`'s separate `hypr-vars` entry (untouched, by design) — 29 total contract entries (down from 31 pre-migration, per SUMMARY). Ran `theme-parity` live: `Summary: 2608 passed, 0 failed` — matches ground-truth claim exactly, confirms byte-identity across all render targets including the collapsed lua-table entry. |
| 4 | `hyprlock` still themes correctly from its own hyprlang fragment, two-emitter split deliberate and unchanged | VERIFIED | `contract.json`'s `hyprlock.conf` `hypr-vars` entry present and untouched (13.1-10's own prohibition: "Must not touch hyprlock's contract entry, its matugen template, or its hyprlang emitter"). Render confirmed live and incidentally in `13.1-EQUIVALENCE-REPORT.md`: a real hyprlock trigger mid-task showed a themed lock surface (gradient wallpaper, clock, greeting) — not blank, not default fallback. Full interactive lock/unlock cycle is operator-verified (recorded "CLOSED, operator-verified" in the Task-2-supersession decision) — not machine-checkable, reported here as human-verified rather than claimed as a mechanical proof. |
| 5 | Load-order hazard gone: no config depends on sourcing order, missing token degrades to default | VERIFIED | Direct code read of `hypr/.config/hypr/hyprland.lua` header and `hypr/.config/hypr/lib/tokens.lua`: every consuming module calls `require("lib.tokens").get()` independently (not relying on a prior module having set a shared value), and `lib/tokens.lua` line 34 wraps the load in `pcall(require, "state.tokens")`, normalizing to an empty table on failure rather than erroring. Header comment explicitly documents this property replaces hyprlang's strict sequential-source-order hazard. |
| 6 | `motion-lint` has a Lua-aware path with fixtures; hyprlang path still guards hyprlock | VERIFIED | Ran `motion-lint --self-test` live: `Self-test summary: 10 passed, 0 failed` (includes 3 new Lua fixtures — `compliant-hypr.lua`, `poisoned-raw-hypr.lua`, `poisoned-dangling-hypr.lua` — alongside the 7 pre-existing gtk4/qml fixtures). Ran the full `motion-lint` scan live: `Summary: 55 passed, 0 checks failed`. Both match the ground-truth figures exactly. |
| 7 | A fresh machine reproduces the Lua config through `install.sh` + `stow.sh` | VERIFIED for the Lua config specifically; **UNDERMINED at the project-constraint level** — see gap below | `13.1-10-SUMMARY.md` documents the reproduction proof was actually run (via `git archive` + a throwaway `$HOME`, not asserted from reading the scripts), and found + fixed two real pre-existing fold bugs along the way (`~/.config/hypr`, `~/.local`), both confirmed by direct inspection of `stow.sh` (lines 105-143, pre-create-as-real-directory pattern applied to both). Narrowly, the Lua config itself does reproduce. However, direct inspection of `stow.sh` confirms the CODE REVIEW's CR-01 finding is real and unfixed: the identical directory-fold bug remains live for the `wallpapers` package (`~/Pictures`) — see the `gaps` entry below. This is the same phase's own change class, in the same file, left incomplete for a third instance. Weighed against CLAUDE.md's explicit project constraint ("Reproducibility: Everything must be installable on a fresh Arch system via install.sh + stow — no manual host-only state"), this is a real, disclosed, currently-unresolved gap in the broader fresh-machine reproducibility story, even though it does not break Success Criterion 7's literal wording (which is scoped to "the Lua config"). |

**Score:** 7/7 truths verified as literally worded (ROADMAP Success Criteria). 0 behavior-unverified. 1 additional gap (CR-01) and 1 human-verification item (window-rule size regression) surfaced independently of the literal criteria — see Gaps Summary.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `hypr/.config/hypr/hyprland.lua` | Live top-level Lua config entry point | ✓ VERIFIED | Symlinked from `~/.config/hypr/hyprland.lua`, live-booting compositor (pid 969) confirmed via log. Header is stale (still describes itself as `hyprland.lua.disabled`, pre-cutover) — cosmetic, tracked as review WR-03, not functionally blocking. |
| `hypr/.config/hypr/config/keybinds.lua` | 80 keybind registrations + 1 window rule | ✓ VERIFIED | `grep -c "hl\.bind"` = 80. `keybind-doctor` re-run live: 14/14 checks pass, including "declared-vs-registered: all 80 declared binds appear in hyprctl binds (missing: 0)". |
| `hypr/.config/hypr/config/windowrules.lua` | 30 window rules, 13 layer rules | ✓ VERIFIED (counts); ⚠️ 1 property-level runtime regression | `grep -c "hl\.window_rule"` = 30, `hl\.layer_rule` = 13 — exact match. Counts and property spellings verified; the `size` percentage field's runtime behavior is the human-verification item above. |
| `hypr/.config/hypr/config/permissions.lua` | 4 screencopy grants | ✓ VERIFIED | `grep -c "hl\.permission"` = 4. `13.1-PERMISSION-REVIEW.md` exists as the D-16 compensating control for the un-introspectable grants. |
| `hypr/.config/hypr/config/animations.lua` | 15 animation leaves | ✓ VERIFIED | `grep -c "hl\.animation"` = 15. `motion-lint` confirms no dangling curve references, no raw literals. |
| `hypr/.config/hypr/lib/tokens.lua` | Defensive `pcall(require)` accessor | ✓ VERIFIED | Line 34: `local ok, tokens = pcall(require, "state.tokens")`. |
| `theme-engine/.config/theme-engine/lib/contract.sh` | `lua-table` dispatch branches | ✓ VERIFIED | `grep -n "lua-table"` hits at lines 130, 344 (both name- and value-extractor dispatch tables). |
| `theme-engine/.config/theme-engine/contract.json` | Collapsed contract, 1 `lua-table` entry, `hyprlock.conf` retained | ✓ VERIFIED | Direct inspection: 29 entries, exactly 1 `lua-table` entry (`hyprland-tokens.lua`), `hyprlock.conf`'s `hypr-vars` entry present. |
| `install.sh` | `lua` registered as explicit install dependency | ✓ VERIFIED | Line 105 lists `lua` in the dependency array, with a comment explaining it's for `lib/contract.sh`'s lua-table extractor. |
| `hypr/.config/hypr/scripts/keybind-doctor` | Retargeted to `keybinds.lua` | ✓ VERIFIED | Re-run live: 14/14 checks pass against the Lua module, no fallback to `keybinds.conf` (file no longer exists). |
| `hypr/.config/hypr/scripts/cheat-sheet-parser.sh` | Retargeted to `keybinds.lua` | ✓ VERIFIED (static) | Default path is `keybinds.lua`; header documents no fallback to `.conf`. |
| `hypr/.config/hypr/scripts/gaming-mode-toggle.sh` | Reads colours from merged Lua token file | ✓ VERIFIED | `THEME_TOKENS_LUA="$HOME/.local/state/theme/hyprland-tokens.lua"`, read via `contract_extract_values`. |
| `theme-engine/.config/theme-engine/theme-stress-test` | Representative-file set updated for collapsed contract | ✓ VERIFIED | `REPRESENTATIVE_FILES` array includes `hyprland-tokens.lua`, not the retired `hyprland.conf`/`hyprland-motion.conf`. |
| `stow.sh` | Fresh-machine seed/symlink steps, retired hyprlang target removed | ⚠️ PARTIAL | `~/.config/hypr` and `~/.local` pre-create fix present and proven by reproduction run (see criterion 7 above). **`~/Pictures`/`wallpapers` package pre-create fix is absent** — this is the CR-01 gap. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `config/keybinds.lua` | `.hypr-baseline/binds.json` | ported bind set diffed against committed baseline | WIRED | `hypr-equivalence-check` live run confirms 80/80 binds match structurally, only the 2 known `mouse` field gaps remain. |
| `theme-engine/lib/contract.sh` | `~/.local/state/theme/hyprland-tokens.lua` | `lua-table` branches load and walk this file | WIRED | Confirmed via `theme-parity` live pass (2608/0) — the lua-table extractor is exercised as part of that run. |
| `config/animations.lua` | `~/.local/state/theme/hyprland-tokens.lua` | `require("lib.tokens").get()` supplies motion values | WIRED | Confirmed via `motion-lint` live pass (55/0) resolving all token references. |
| `keybind-doctor` | `config/keybinds.lua` | declared-bind extraction parses Lua module | WIRED | Live run: 80/80 declared binds parsed and cross-checked against `hyprctl binds`. |
| `stow.sh` | `~/.config/hypr/state/tokens.lua` | pre-created real directory + relative symlink | WIRED | Confirmed for the `hypr`/`.local` targets by the phase's own reproduction proof. |
| `stow.sh` | `~/Pictures/Wallpapers` | pre-created real directory before restow | **NOT WIRED** | No pre-create call exists before the `PACKAGES` restow loop (only after, at line 354) — see gap. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Compositor booted from Lua, no deprecation text | `grep -i "deprecat\|Using lua" $HYPRLAND_LOG` | `[cfg] Using lua config found at ...hyprland.lua`, no deprecation text | ✓ PASS |
| Bind count matches baseline | `hyprctl -j binds \| length` | 80 | ✓ PASS |
| `hypr-equivalence-check` runs cleanly, only known divergences | ran live (mismatched theme) | `PASS: 1 FAIL: 2`, both extra fails are the expected theme-color mismatch | ✓ PASS (explained) |
| `keybind-doctor` passes | ran live | `14 passed, 0 failed` | ✓ PASS |
| `motion-lint --self-test` passes | ran live | `10 passed, 0 failed` | ✓ PASS |
| `motion-lint` full scan passes | ran live | `55 passed, 0 checks failed` | ✓ PASS |
| `theme-parity` passes | ran live | `2608 passed, 0 failed` | ✓ PASS |
| `hypr-lua-harness start` still usable | ran live | Refuses unconditionally: "the live session appears to already be on Lua" | ✗ CONFIRMED BROKEN (review WR-01, non-blocking to phase goal — post-cutover regression tool, not load-bearing) |
| `keybind-source-equivalence` (no args, its own documented default) | ran live | Unhandled `FileNotFoundError` traceback | ✗ CONFIRMED BROKEN (review WR-02, non-blocking to phase goal — superseded by `keybind-doctor`) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|--------------|--------|----------|
| MAINT-04 | All 10 plans (13.1-01 through 13.1-10) | Hyprland config runs on Lua before 0.57 removes `.conf` support, behavioural equivalence proven against pre-migration baseline, two Hyprland-format outputs collapsed to one `lua-table` entry | ✓ SATISFIED | All three clauses independently confirmed above: live Lua boot (Criterion 1), `hypr-equivalence-check` PASS with reasoned divergences (Criterion 2), collapsed contract (Criterion 3). No orphaned requirements — `REQUIREMENTS.md` maps exactly MAINT-04 → Phase 13.1, matching all 10 plans' `requirements: [MAINT-04]` frontmatter. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `stow.sh` | 19-41, 145-164, 351-363 | Missing pre-create of `~/Pictures/{Wallpapers,Screenshots}` before the stow restow loop (same class as the fixed hypr/.local bugs) | 🛑 Critical (code review CR-01, unfixed) | Fresh-machine wallpaper/screenshot state can fold into the git checkout, contradicting the project's core reproducibility value. See gap. |
| `hypr/.config/hypr/scripts/hypr-lua-harness:70-84` | 70-84 | Tool unconditionally refuses to start post-cutover (both its own guard conditions always fire) | ⚠️ Warning (code review WR-01, unfixed) | Regression-test harness for future Lua config changes is permanently inert with no documentation of that fact; non-blocking to the current phase's live goal. |
| `hypr/.config/hypr/scripts/keybind-source-equivalence:45-46,87-139` | 87-139 | Crashes with unhandled traceback on its own documented default invocation (`keybinds.conf` deleted) | ⚠️ Warning (code review WR-02, unfixed) | Superseded by `keybind-doctor`, but the crash-on-default-usage is a rough edge for a future contributor. |
| `hypr/.config/hypr/hyprland.lua:1-33` | 1-33 | Header comment describes a pre-cutover, `.disabled`-suffixed state that no longer holds | ⚠️ Warning (code review WR-03, unfixed) | Misleading to a future maintainer reading the most load-bearing file in the repo; cosmetic, non-blocking. |
| `hypr/.config/hypr/scripts/ai-webapp-launch.sh:34-35` | 34-35 | `\|\| true` silently discards the one dispatch call the whole script depends on | ⚠️ Warning (code review WR-04, unfixed) | Pre-existing script touched by this phase's retarget sweep; failure mode is silent misplacement, not a migration-introduced break. |
| `hypr/.config/hypr/scripts/ai-workspace.sh:44-53` | 44-53 | Substring match instead of exact match against a comma-joined class list | ⚠️ Warning (code review WR-05, unfixed) | Pre-existing idempotency edge case, unrelated to the Lua migration itself. |

No `TBD`/`FIXME`/`XXX` debt markers found in any of the phase's core Lua config, tooling, or theme-engine files.

### Human Verification Required

See the `human_verification` entry in the frontmatter (window-rule `size`-field regression on 6 windows). This is a real, mechanically-proven runtime gap (not a verification-channel limitation) that COVERAGE.md itself required visual confirmation for at cutover; the closest evidence found (13.1-10's "Window-rule visual properties — CLOSED, operator-verified" note) plausibly covers it but does not name the six affected windows specifically, and `WINDOWS.md` row 11 was never updated to reflect resolution the way directly-adjacent rows 12/13 were.

## Gaps Summary

**One structural gap (CR-01, unfixed Critical code-review finding):** `stow.sh` was hardened this phase against a "whole-directory symlink fold" fresh-machine bug for two targets (`~/.config/hypr`, `~/.local`) — proven fixed by an actual reproduction run, not just review of the diff. The identical bug class was left open for a third target this same phase's restow loop touches (`wallpapers` → `~/Pictures`). Read narrowly, ROADMAP Success Criterion 7 ("A fresh machine reproduces the Lua config through install.sh + stow.sh") is satisfied — the Lua config itself does reproduce, proven twice. Read against this project's own stated core constraint ("Everything must be installable on a fresh Arch system via install.sh + stow — no manual host-only state"), this gap is real, disclosed by the phase's own code reviewer, and still unresolved. This is not being weighed as an outright FAILED must-have (no PLAN's must_haves explicitly named the `wallpapers` package), but it is surfaced as a genuine gap rather than silently passed, per the explicit instruction to weigh it honestly against the reproducibility story this phase's own SUMMARY claims ("A fresh machine reproduces the Lua config... found and fixed two previously-undiscovered fresh-machine folding bugs along the way" — a third, identical-class bug was found by the code reviewer and left unfixed in that same session).

**One unresolved tracking discrepancy, routed to human verification:** the window-rule `size` percentage-field regression (6/30 rules affected) was mechanically proven broken in 13.1-07, explicitly required visual confirmation at cutover by COVERAGE.md, and appears to have been folded into a generic "window-rule visual properties — CLOSED, operator-verified" note in 13.1-10 — but `WINDOWS.md` item #11 itself was never updated to reflect that closure the way its sibling items (#12, #13) were. This may already be resolved in substance; it cannot be confirmed from documentation alone.

**Everything else — the core migration goal — is genuinely achieved and independently re-verified, not merely re-stated from SUMMARY claims.** All 7 ROADMAP success criteria hold as literally worded; every regression gate this repository owns (`theme-doctor`, `theme-parity`, `motion-lint` + self-test, `keybind-doctor`, `hypr-equivalence-check`) was re-run live during this verification and reproduced its claimed green result exactly, not merely trusted from SUMMARY text. The two `bindm mouse:272/273` divergences are the only genuine equivalence-gate finding, and they are honestly reasoned with a named compensating control. Known deferred items (`WINDOWS #14`, `#15` — quickshell-doctor and waybar's dead workspace click) are correctly tracked as deliberate, operator-approved deferrals, not silently dropped work.

---

_Verified: 2026-07-28T17:15:00Z_
_Verifier: Claude (gsd-verifier)_
