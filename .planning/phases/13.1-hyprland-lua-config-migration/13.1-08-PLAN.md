---
phase: 13.1-hyprland-lua-config-migration
plan: 08
type: execute
wave: 7
depends_on: ["13.1-07"]
files_modified:
  - hypr/.config/hypr/hyprland.lua
  - .planning/phases/13.1-hyprland-lua-config-migration/13.1-EQUIVALENCE-REPORT.md
  - .planning/phases/13.1-hyprland-lua-config-migration/13.1-LUA-FINDINGS.md
autonomous: false
requirements: [MAINT-04]

must_haves:
  truths:
    - "The live session starts from hyprland.lua and the hyprlang deprecation warning is absent from the compositor's own startup log"
    - "hypr-equivalence-check run against the live Lua session either reports an exact match with the committed pre-migration baseline, or every divergence is individually recorded in 13.1-EQUIVALENCE-REPORT.md as intended, with a reason"
    - "Theme switching still re-themes the whole desktop in both static-preset and matugen dynamic modes while the compositor runs on Lua"
    - "hyprlock still themes correctly from its own hyprlang fragment, which is unchanged"
    - "The legacy hyprlang tree is still present on disk and still bootable, so rollback remains one rename plus a restart at every point in this plan"
    - "The Hyprland release that removes hyprlang support is re-read from the installed binary at cutover time and recorded, rather than carried forward as a design-time assumption"
    - "Every regression gate this repository already owns is green on the Lua session, or its failure is a pre-existing one carried in with an explicit reference"
    - statement: "The window-rule properties with no runtime projection behave as they did before the migration, confirmed by the named per-property visual checks recorded in 13.1-07"
      verification: backstop
    - statement: "Repeated real theme switches over a soak period do not accumulate compositor registrations"
      verification: backstop
  prohibitions:
    - "Must not delete, move or stop generating any part of the legacy hyprlang tree or its generated inputs in this plan — rollback must stay one rename away until the soak completes"
    - "Must not record a divergence without a reason, and must not adjust the committed baseline to make a divergence disappear"
    - "Must not declare the gate green while any regression gate this repository already owns is newly red"
  artifacts:
    - path: "hypr/.config/hypr/hyprland.lua"
      provides: "the active top-level Lua config — the renamed entry point"
    - path: ".planning/phases/13.1-hyprland-lua-config-migration/13.1-EQUIVALENCE-REPORT.md"
      provides: "the D-10 record: gate output plus an individually reasoned entry per divergence"
  key_links:
    - from: "hypr/.config/hypr/hyprland.lua"
      to: ".planning/phases/13.1-hyprland-lua-config-migration/.hypr-baseline"
      via: "hypr-equivalence-check diffs the live Lua session against the committed pre-migration capture"
      pattern: "hypr-baseline"
---

<objective>
Flip the live session onto Lua and run the gate the whole phase exists to satisfy.

Purpose: every proof so far has been against a nested instance. Success criterion 2 is about the
real session — the one with the real monitor, the real daemon set and the real theme-engine
attached to it. This plan is where the migration either is or is not behaviourally identical, and
where every remaining difference gets a name and a reason instead of a shrug.

Output: an active `hyprland.lua`, and `13.1-EQUIVALENCE-REPORT.md`.

The legacy tree stays untouched throughout. Rollback is one rename plus a restart at every point in
this plan, and remains so until plan `13.1-10`'s explicit decision gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.1-hyprland-lua-config-migration/13.1-CONTEXT.md
@.planning/phases/13.1-hyprland-lua-config-migration/13.1-LUA-FINDINGS.md
@.planning/phases/13.1-hyprland-lua-config-migration/13.1-PERMISSION-REVIEW.md
@.planning/phases/13.1-hyprland-lua-config-migration/13.1-07-SUMMARY.md
</context>

## Artifacts this phase produces

**New files**
- `hypr/.config/hypr/scripts/hypr-equivalence-check`
- `hypr/.config/hypr/scripts/hypr-lua-harness`
- `.planning/phases/13.1-hyprland-lua-config-migration/.hypr-baseline/{binds.json,animations.json,options.jsonl,uncovered.txt,MANIFEST.md}`
- `.planning/phases/13.1-hyprland-lua-config-migration/COVERAGE.md`
- `.planning/phases/13.1-hyprland-lua-config-migration/13.1-LUA-FINDINGS.md`
- `.planning/phases/13.1-hyprland-lua-config-migration/13.1-PERMISSION-REVIEW.md`
- `.planning/phases/13.1-hyprland-lua-config-migration/13.1-EQUIVALENCE-REPORT.md`
- `hypr/.config/hypr/hyprland.lua` (carried as `hyprland.lua.disabled` until this plan renames it)
- `hypr/.config/hypr/lib/tokens.lua`
- `hypr/.config/hypr/config/env.lua`, `monitors.lua`, `autostart.lua`, `animations.lua`,
  `keybinds.lua`, `windowrules.lua`, `permissions.lua`
- `~/.config/hypr/state/tokens.lua` (relative symlink created by `stow.sh`, not a repo file)
- `~/.local/state/theme/hyprland-tokens.lua` (generated; the single merged token table)
- `matugen/.config/matugen/templates/hyprland-colors.lua`
- `hypr/.config/hypr/scripts/tests/motion-fixtures/compliant-hypr.lua`,
  `poisoned-raw-hypr.lua`, `poisoned-dangling-hypr.lua`

**New CLI surface**
- `hypr-equivalence-check --snapshot <outdir>` · `--uncovered` · `--baseline <dir>` · default check mode
- `hypr-lua-harness start [--with-autostart]` · `hyprctl <args…>` · `status` · `stop`

**New contract.json format name / entry**
- format `lua-table`; entry `hyprland-tokens.lua`

**New shell functions**
- `theme_engine_render_hypr_tokens` in `theme-engine/.config/theme-engine/lib/motion.sh`
- `lua-table` case arms added to `contract_extract_names` and `contract_extract_values` in `lib/contract.sh`
- a Lua-aware extractor branch in `hypr/.config/hypr/scripts/motion-lint`

**Retired in plan 13.1-10**
- `matugen/.config/matugen/templates/hyprland-colors.conf`
- `contract.json` entries `hyprland.conf` (`hypr-vars`) and `hyprland-motion.conf` (`hypr-motion`)
- `hypr/.config/hypr/hyprland.conf` and the seven-file `hypr/.config/hypr/config/*.conf` tree

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight every gate, then activate the Lua entry point without restarting</name>
  <precondition>All seven `hypr/.config/hypr/config/*.lua` modules and `hypr/.config/hypr/lib/tokens.lua` exist and pass `luac -p`, and `~/.local/state/theme/hyprland-tokens.lua` exists and is non-empty.</precondition>
  <files>hypr/.config/hypr/hyprland.lua, .planning/phases/13.1-hyprland-lua-config-migration/13.1-LUA-FINDINGS.md</files>
  <read_first>
    - `.planning/phases/13.1-hyprland-lua-config-migration/13.1-LUA-FINDINGS.md`
      §"Rollback procedure (verified)" — the activate/deactivate command pair recorded in 13.1-02.
    - `stow.sh` — how the `hypr` package is deployed and what the token seed and symlink steps do.
    - `hypr/.config/hypr/hyprland.lua.disabled` and every `hypr/.config/hypr/config/*.lua`.
    - `.planning/ROADMAP.md` §"Standing constraints" — constraint 1 (human sign-off on visual
      surfaces), constraint 2 (verify against the installed binary), constraint 3 (same-commit stow
      registration).
  </read_first>
  <action>
Run every gate this repository already owns **before** touching the entry point's name, so any
failure found afterwards is attributable to the cutover rather than to pre-existing state. Record
the pre-cutover result of each: `theme-doctor`, `theme-parity`, `motion-lint`,
`motion-lint --self-test`, `keybind-doctor`, `quickshell-doctor`, and `luac -p` over every
repo-authored `.lua` file under `hypr/.config/hypr/`. A gate that is already red for a reason
recorded in `.planning/STATE.md` stays red — note the reference; a gate that is green now must be
green after.

Re-read the deprecation warning from the installed binary and record it in
`13.1-LUA-FINDINGS.md` under a `## Removal version reconfirmed at cutover` heading: the current
`hyprctl version` output, the exact wording of the startup warning as it appears in the
compositor's own log, and the release it names. This closes the backstop opened in `13.1-01`. If
the installed version has moved on since research, record that too — it does not change the plan,
but an assumption carried this far should not be the one thing nobody re-read.

Then activate: rename `hypr/.config/hypr/hyprland.lua.disabled` to
`hypr/.config/hypr/hyprland.lua` in the repository (a tracked rename, so the change is visible in
history rather than being a working-tree artefact), re-run `stow.sh`, and confirm the deployed
state: `~/.config/hypr/hyprland.lua` exists and resolves, `~/.config/hypr/hyprland.conf` still
exists and still resolves, `~/.config/hypr/state/tokens.lua` is still a resolving relative symlink,
and `~/.local/state/theme/hyprland-tokens.lua` is still present and non-empty.

**Do not restart the compositor in this task.** The running session is still the hyprlang one and
must stay that way until the operator performs the restart in Task 2 — the executor cannot restart
the session it is running inside without terminating itself mid-plan. Commit the rename and the
recorded pre-flight results.

State plainly in the commit message that rollback at this point is renaming the file back and
re-running `stow.sh`, and that the legacy tree is untouched.
  </action>
  <verify>
    <automated>bash -c 'set -e; test -f hypr/.config/hypr/hyprland.lua; test ! -e hypr/.config/hypr/hyprland.lua.disabled; luac -p hypr/.config/hypr/hyprland.lua hypr/.config/hypr/lib/tokens.lua hypr/.config/hypr/config/*.lua; test -e ~/.config/hypr/hyprland.lua; test -e ~/.config/hypr/hyprland.conf; test -e ~/.config/hypr/state/tokens.lua; test -s ~/.local/state/theme/hyprland-tokens.lua'</automated>
  </verify>
  <acceptance_criteria>
    - `luac -p` exits 0 for `hypr/.config/hypr/hyprland.lua`, `hypr/.config/hypr/lib/tokens.lua` and all seven `hypr/.config/hypr/config/*.lua` files.
    - `hypr/.config/hypr/hyprland.lua` exists in the repository and `hypr/.config/hypr/hyprland.lua.disabled` does not; `git log --diff-filter=R` shows the rename.
    - After `stow.sh`: `~/.config/hypr/hyprland.lua` resolves, `~/.config/hypr/hyprland.conf` still resolves, `~/.config/hypr/state/tokens.lua` is a symlink whose target begins with `../` and resolves, and `~/.local/state/theme/hyprland-tokens.lua` is non-empty.
    - The pre-cutover result of each of `theme-doctor`, `theme-parity`, `motion-lint`, `motion-lint --self-test`, `keybind-doctor` and `quickshell-doctor` is recorded with its exit code and pass/fail counts.
    - `13.1-LUA-FINDINGS.md` contains a `## Removal version reconfirmed at cutover` section with the current `hyprctl version` output and the verbatim startup-warning text naming the removal release.
    - The compositor was not restarted in this task: `hyprctl version` still reports the hyprlang session, and the deprecation warning is still present in its log.
  </acceptance_criteria>
  <done>Every gate's pre-cutover state is recorded, the removal version is reconfirmed from the binary, the entry point is renamed and deployed, and the running session is still hyprlang with rollback one rename away.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Restart the session so the compositor comes up on Lua</name>
  <files>(none — this task performs no file change)</files>
  <action>
The compositor selects its configuration format once, at startup. Everything is now in place for it
to select Lua, but the change cannot take effect without a compositor restart — and the executor is
running inside the session it would be restarting, so it cannot perform the restart and then
continue. This is the one step in the phase that genuinely has no CLI path.
  </action>
  <instructions>
**What is already in place:** `hypr/.config/hypr/hyprland.lua` is deployed at
`~/.config/hypr/hyprland.lua`, all seven config modules are ported and `luac`-clean, the merged
token file is generated and reachable through the `state/tokens.lua` symlink, and every existing
regression gate's pre-cutover state is recorded. The legacy hyprlang tree is untouched and still
on disk.

**What to do:**

1. Save any open work. This will end the current graphical session.
2. Log out (or run the session's normal stop path). Log back in.
3. The desktop should come up looking and behaving exactly as it did before — same bar, same
   wallpaper, same window rules, same keybinds.
4. If the compositor does **not** start, or comes up visibly broken: switch to a TTY
   (Ctrl+Alt+F2), run
   `mv ~/.config/hypr/hyprland.lua ~/.config/hypr/hyprland.lua.disabled`, then log in again — the
   hyprlang tree is untouched and will load. Report what happened; do not attempt to repair the
   Lua config from the TTY.
5. Once logged back in, run `hyprctl version` to confirm a compositor is running, then resume.
  </instructions>
  <verification>
`hyprctl version` returns successfully, and the compositor's startup log no longer contains the
hyprlang deprecation warning — check with `hyprctl rollinglog | head -40` or the session's journal.
A desktop that comes up looking and behaving as it did before is the human-side signal; the
mechanical proof follows in Task 3.
  </verification>
  <resume-signal>Type "resumed on lua" if the desktop came up on the Lua config, "rolled back" plus what went wrong if you had to roll back, or describe any visible difference from before.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the equivalence gate on the live session and reason about every divergence</name>
  <precondition>The running compositor was started from `~/.config/hypr/hyprland.lua` — the hyprlang deprecation warning is absent from the compositor's own startup log.</precondition>
  <files>.planning/phases/13.1-hyprland-lua-config-migration/13.1-EQUIVALENCE-REPORT.md</files>
  <read_first>
    - `hypr/.config/hypr/scripts/hypr-equivalence-check` — the gate's modes and its D-10 semantics.
    - `.planning/phases/13.1-hyprland-lua-config-migration/.hypr-baseline/MANIFEST.md` — the
      documented coverage limits the gate cannot reach, and their named compensating controls.
    - `.planning/phases/13.1-hyprland-lua-config-migration/13.1-LUA-FINDINGS.md` — the recorded
      option readback divergences from `13.1-04` and the non-introspectable window-rule properties
      from `13.1-07`, each with its compensating visual check.
    - `.planning/ROADMAP.md` §"Phase 13.1" — the seven success criteria, verbatim.
  </read_first>
  <action>
Run `hypr-equivalence-check` with no arguments against the live Lua session and capture its full
output into `.planning/phases/13.1-hyprland-lua-config-migration/13.1-EQUIVALENCE-REPORT.md`.

D-10 is the standard: the gate exits 0 only on exact match. Where it does not, **every** divergence
gets its own entry in the report with four fields — the key or bind or animation leaf, the baseline
value, the live Lua value, and a reason that explains why the difference is intended. A divergence
whose reason is "the two formats represent this differently" must say *how*, with the two
representations quoted. Never adjust the committed baseline to make a divergence disappear; the
baseline is the record of what the machine did before, and editing it destroys the only evidence
the phase has.

Then verify the remaining ROADMAP success criteria that only the live session can show:

- **Criterion 1** — the compositor started from `hyprland.lua`, and the hyprlang deprecation warning
  is absent from its startup log. Quote the relevant log lines both ways.
- **Criterion 3** — theme switching still re-themes the whole desktop in both modes. Apply a static
  preset, then apply `materialyou`, then apply the original theme back. After each, confirm the
  compositor's border colours changed (read `general:col.inactive_border` back and compare against
  the token file), and confirm the other surfaces re-themed as they always have — waybar, kitty,
  swaync, walker, GTK. Record the observed values.
- **Criterion 4** — `hyprlock` still themes correctly from its own unchanged hyprlang fragment.
  Confirm `~/.local/state/theme/hyprlock.conf` is still generated and its `hypr-vars` contract entry
  is untouched, then lock and unlock once and record the result.
- **Criterion 6** — `motion-lint` passes with its Lua path active on the live tree, and
  `--self-test` passes.

Re-run the full regression set and compare against the pre-cutover results recorded in Task 1:
`theme-doctor`, `theme-parity`, `motion-lint`, `keybind-doctor`, `quickshell-doctor`. Any gate that
was green before and is red now is a cutover regression and must be fixed in this plan, not carried
forward.

Finally, start the soak clock: record the timestamp at which the Lua config became the live
configuration, and note in the report that plan `13.1-09`'s decision gate depends on this clock plus
the operator's own judgement of normal use. Do not delete anything.
  </action>
  <verify>
    <automated>bash -c 'set -e; hypr/.config/hypr/scripts/hypr-equivalence-check > /tmp/hypr-eq.out 2>&1; echo "gate-exit=$?"; cat /tmp/hypr-eq.out; test -s .planning/phases/13.1-hyprland-lua-config-migration/13.1-EQUIVALENCE-REPORT.md; hypr/.config/hypr/scripts/motion-lint --self-test; hypr/.config/hypr/scripts/motion-lint'</automated>
  </verify>
  <acceptance_criteria>
    - `13.1-EQUIVALENCE-REPORT.md` contains the gate's full verbatim output plus its exit code.
    - The gate exits 0, **or** every divergence it reported has an entry in the report carrying the key, the baseline value, the live value and a reason — with the count of entries equal to the count of divergences the gate printed.
    - `.hypr-baseline/` is byte-unchanged since `13.1-01`: `git diff --stat` for that directory shows no modification.
    - Criterion 1: the compositor's startup log contains no hyprlang deprecation warning, and the report quotes both the pre-cutover warning line and its post-cutover absence.
    - Criterion 3: three theme applications (static preset → materialyou → back) each complete without error, `general:col.inactive_border` read back after each matches the `outline` value in `~/.local/state/theme/hyprland-tokens.lua` at that moment, and the report records all three value pairs.
    - Criterion 4: `~/.local/state/theme/hyprlock.conf` is generated and non-empty, its `contract.json` entry still declares format `hypr-vars`, and a lock/unlock cycle is recorded as successful.
    - Criterion 6: `motion-lint` and `motion-lint --self-test` both exit 0 on the live Lua tree.
    - Every gate that was green in Task 1's pre-cutover record is green again, with exit codes and counts recorded side by side; any newly red gate is fixed within this plan.
    - The legacy tree is intact: `hypr/.config/hypr/hyprland.conf` and all eight `hypr/.config/hypr/config/*.conf` files still exist, and `~/.local/state/theme/hyprland.conf` and `hyprland-motion.conf` are still generated.
    - The soak start timestamp is recorded in the report.
  </acceptance_criteria>
  <done>The live Lua session is measured against the committed pre-migration baseline with every divergence individually reasoned, criteria 1, 3, 4 and 6 are demonstrated on the real desktop, no existing gate regressed, and the rollback path is still intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config tree → the live desktop session | this is the first plan where a mistake takes down the operator's actual working environment rather than a nested throwaway |
| committed baseline → equivalence verdict | the baseline is the only evidence of pre-migration behaviour; editing it to resolve a divergence would erase the phase's entire correctness argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13.1-24 | Denial of Service | the operator's live session | high | mitigate | The legacy hyprlang tree and both of its generated inputs remain untouched and generating, so the rollback is a single rename executable from a TTY. The restart is a blocking human-action checkpoint carrying the verbatim TTY rollback command, so the operator is not left improvising if the compositor does not come up. No deletion happens in this plan. |
| T-13.1-05 | Repudiation | the committed baseline | high | mitigate | `.hypr-baseline/` is asserted byte-unchanged since `13.1-01`. Every divergence is recorded with baseline value, live value and reason, and the entry count must equal the gate's reported divergence count so a difference cannot be quietly dropped. |
| T-13.1-25 | Tampering | regression-gate interpretation | medium | mitigate | Every gate's result is captured **before** the cutover as well as after, so a pre-existing failure cannot be mistaken for a cutover regression and, more importantly, a cutover regression cannot be excused as pre-existing. |
| T-13.1-02 | Elevation of Privilege | permission grants on the live session | high | mitigate | The grants are not `getoption`-introspectable, so the gate cannot cover them; `13.1-PERMISSION-REVIEW.md` from `13.1-07` is the standing compensating control, and this plan changes nothing about the grants. |
| T-13.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
- All gates' pre-cutover state recorded; removal version reconfirmed from the installed binary.
- Entry point renamed, deployed, and the compositor restarted by the operator onto Lua.
- `hypr-equivalence-check` run on the live session with output and exit code captured, and every divergence individually reasoned.
- Baseline directory byte-unchanged.
- ROADMAP criteria 1, 3, 4 and 6 demonstrated on the real desktop with recorded evidence.
- No gate regressed; legacy tree and its generated inputs intact.
- Soak clock started.
</verification>

<success_criteria>
The real desktop runs on the Lua config and is provably the same desktop it was before — or every
way in which it differs has a name, a measurement and a reason. Rollback is still one rename away.
</success_criteria>

<output>
Create `.planning/phases/13.1-hyprland-lua-config-migration/13.1-08-SUMMARY.md` when done.
</output>
