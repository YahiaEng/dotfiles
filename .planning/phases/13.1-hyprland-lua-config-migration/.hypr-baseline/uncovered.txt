permission-grant:/usr/bin/quickshell, screencopy, allow	permission grants are read once at Hyprland startup and are not exposed via hyprctl getoption — no per-grant introspection key exists; compensating check: recorded side-by-side textual review in plan 13.1-07
permission-grant:/usr/bin/grim, screencopy, allow	permission grants are read once at Hyprland startup and are not exposed via hyprctl getoption — no per-grant introspection key exists; compensating check: recorded side-by-side textual review in plan 13.1-07
permission-grant:/usr/bin/hyprpicker, screencopy, allow	permission grants are read once at Hyprland startup and are not exposed via hyprctl getoption — no per-grant introspection key exists; compensating check: recorded side-by-side textual review in plan 13.1-07
permission-grant:/usr/lib/xdg-desktop-portal-hyprland, screencopy, allow	permission grants are read once at Hyprland startup and are not exposed via hyprctl getoption — no per-grant introspection key exists; compensating check: recorded side-by-side textual review in plan 13.1-07
binds.json:dispatcher	Lua-registered binds report dispatcher="__lua" via hyprctl -j binds, not the real dispatcher name (an opaque closure reference in the C++ layer, found live in 13.1-03) — no computable mapping back to the real dispatcher exists through this JSON surface. hypr-equivalence-check's binds.json comparison excludes this field entirely (structural-only, D-16). Compensating check: hypr/.config/hypr/scripts/keybind-source-equivalence proves (mods,key,dispatcher,arg) equivalence at the source level (keybinds.conf vs keybinds.lua), self-tested in 13.1-04 Task 3 to catch three corruption classes (changed arg, dropped bind, dropped modifier).
binds.json:arg	Lua-registered binds report arg as an opaque small-integer internal index (e.g. "5"), not the real command/argument text — same opacity mechanism as binds.json:dispatcher above. Compensating check: hypr/.config/hypr/scripts/keybind-source-equivalence (see binds.json:dispatcher entry for the self-test evidence).
binds.json:keycode (code:NNN physical-keycode binds only)	A Lua-registered physical-keycode bind (the 4 code:107 screenshot/record binds) reads back keycode:0 in hyprctl -j binds, not the real value (baseline: 107) — found live in 13.1-03, reconfirmed in 13.1-04. hypr-equivalence-check's structural binds.json comparison tolerates ONLY this specific 107-vs-0 shape, nothing else. Compensating check: physically press all four Print-key variants (bare/SHIFT/CTRL/ALT) at the end-of-phase human verification.
binds.json:mouse (bindm binds only)	The two bindm-ported binds (mouse:272 drag-move, mouse:273 drag-resize) read back mouse:false in hyprctl -j binds under Lua, not the real value (baseline: true) — HL.BindOptions (the typed stub) does not list "mouse" as a settable field at all, and hl.bind's opts table has no field validator to confirm whether { mouse = true } (the exact vendor-example shape, used verbatim) has any real effect. NOT MECHANICALLY VERIFIABLE with tooling available in this environment (no evdev/uinput mouse-button injection tool). 14-10 TASK 3 UPDATE (2026-07-30): a narrow, named forgiveness for exactly this field on exactly these two records was added to hypr-equivalence-check's structural comparator (MOUSE_FORGIVEN_KEYS), poison-tested (a different field changed on the same records still FAILs; the same field changed on an unrelated record still FAILs). This forgiveness is PROVISIONAL, not a resolution: it stands entirely on the compensating check named below, added to 14-10-PLAN.md's own Task 4 blocking render gate. AS OF THIS TASK'S CLOSE THE COMPENSATING CHECK HAS NOT YET BEEN RUN — Task 3 is an unattended auto task and cannot perform a physical mouse-drag itself; Task 4 is the human checkpoint that runs it. If Task 4 reports either gesture does not work, this forgiveness is reverted in the same session and the divergence is recorded as a genuine functional regression from the Lua migration — a significant finding, not a gate-tuning detail. Compensating check: physically drag-move and drag-resize a window with Super+LMB/Super+RMB — PENDING, scheduled at 14-10-PLAN.md Task 4.
