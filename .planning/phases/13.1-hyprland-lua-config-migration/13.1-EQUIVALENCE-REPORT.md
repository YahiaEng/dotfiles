# Phase 13.1 Plan 08: Equivalence Report

Recorded against the live desktop session, real monitor, real daemon set,
real theme-engine — pid 956, instance signature
`5c9377c15f85c50648f35ca5a213754f95b93ca0_1785230839_446403004`, started
`2026-07-28T09:27:19Z` (from `hyprctl instances -j`'s `time` field). This is
the compositor the operator restarted onto in Task 2; every measurement
below was taken against it, not a nested harness instance.

## D-10 standard

`hypr-equivalence-check` exits 0 only on exact match. Where it does not,
every divergence gets its own entry below with four fields: the key/bind/
curve, the baseline value, the live value, and a reason. The baseline
directory is byte-unchanged since `13.1-01` (`git diff --stat` for
`.hypr-baseline/` shows no modification — checked immediately before this
report was written).

## Curve-ordering fix applied before this run (operator decision)

Before this task's gate run, an operator decision closed a genuine
nondeterminism found during Task 3 diagnostic work by a prior executor
session: `config/animations.lua`'s bezier-curve registration used a bare
`pairs()` loop over `tokens.motion.curves`, and Lua 5.5 randomizes its
string-hash seed **per process** — proven live this session by running 5
separate `lua` processes over the real
`~/.local/state/theme/hyprland-tokens.lua` and observing 5 different
`pairs()` orderings:

```
run 1: emphasized-accelerate,legacy-decelerate,legacy-accelerate,css-linear,standard-accelerate,standard-decelerate,emphasized-decelerate,standard,linear,legacy
run 2: standard-decelerate,legacy-accelerate,legacy,emphasized-decelerate,standard-accelerate,css-linear,linear,legacy-decelerate,standard,emphasized-accelerate
run 3: standard,legacy-decelerate,emphasized-decelerate,legacy-accelerate,css-linear,linear,standard-decelerate,legacy,standard-accelerate,emphasized-accelerate
run 4: standard,standard-accelerate,legacy,standard-decelerate,linear,emphasized-decelerate,emphasized-accelerate,css-linear,legacy-accelerate,legacy-decelerate
run 5: standard,linear,emphasized-accelerate,standard-decelerate,legacy-decelerate,legacy-accelerate,emphasized-decelerate,css-linear,standard-accelerate,legacy
```

This disproves the prior agent's "deterministic, not flapping" claim — that
test used 3 `hyprctl` calls plus a reload inside the SAME running
compositor process (same Lua hash seed throughout), which cannot observe
per-process randomization. **Fix applied and committed (`d6b956e`):**

1. `config/animations.lua` now collects `tokens.motion.curves`' keys into a
   table and `table.sort()`s them before iterating — proven stable across 5
   separate fresh `lua` processes post-fix (identical output every time:
   `css-linear,emphasized-accelerate,emphasized-decelerate,legacy,legacy-accelerate,legacy-decelerate,linear,standard,standard-accelerate,standard-decelerate`).
2. `hypr-equivalence-check`'s `animations.json` comparison now treats the
   curve array (`data[1]`) as a SET keyed on `(name, X0, Y0, X1, Y1)` —
   order carries no compositor-observable meaning, since Hyprland resolves
   a leaf's `bezier = "motion-X"` reference by name, never by array index.
   The animation LEAF array (`data[0]`, 35 records) stays byte-exact
   positional, unchanged.

**Sorted registration alone does NOT make the gate pass** — confirmed
live: after the sort fix, `hyprctl -j animations`'s own curve order is
still not alphabetical (`['motion-emphasized-accelerate', 'motion-standard',
'motion-legacy-decelerate', ...]`, stable across repeated reloads *within
this one compositor process* but not equal to the committed baseline's
order, which itself came from the old theme-engine-emitted hyprlang
fragment — neither alphabetical nor declaration order). Both fixes were
required together, exactly as the operator specified.

**Gate NOT weakened — proven by deliberate fault injection** (both via the
standalone `_compare_animations` function and via the real CLI entry point
against a tampered copy of the committed baseline, run under the matched
`dracula` theme):

| Fault | Result |
|---|---|
| Curve array reordered (baseline curves reversed) | PASS — order-insensitive as intended |
| One curve's control point changed (`X0: 0.4 -> 0.999`) | FAIL, names the exact tuple: `- curve present in baseline only: ('motion-legacy-accelerate', 0.4, 0.0, 1.0, 1.0)` / `+ curve present in live only: ('motion-legacy-accelerate', 0.999, 0.0, 1.0, 1.0)` |
| One curve dropped | FAIL, names it: `- curve present in baseline only: ('motion-legacy-accelerate', 0.4, 0.0, 1.0, 1.0)` |
| One curve added | FAIL, names it: `+ curve present in live only: ('motion-injected-extra', 0.1, 0.2, 0.3, 0.4)` |
| One leaf's `speed` field changed | FAIL, names the leaf and both records: `leaf record 0 (name='specialWorkspaceOut'): baseline=... live=...` |
| Two leaves swapped in array order | FAIL — leaves remain strictly positional, a reorder is a diff there |
| Baseline directory absent/empty | FAIL, exit 1, never SKIP: `[FAIL] baseline directory is absent or empty` — D-10 branch unchanged by this task's edit |
| Real tampered-baseline copy via the CLI (`--baseline`, curve dropped) | FAIL via the actual gate entry point, not just the function: `gate-exit=1` |

## Full gate output (run under `dracula`, matched to the committed baseline's capture theme)

```
$ hypr-equivalence-check
hypr-equivalence-check — baseline: /home/aorus/dotfiles/.planning/phases/13.1-hyprland-lua-config-migration/.hypr-baseline

  [FAIL] binds.json: differs from baseline (structural comparison)
! record 68 (key='mouse:272' keycode=0 arg='movewindow'): field 'mouse' baseline=True live=False
! record 69 (key='mouse:273' keycode=0 arg='resizewindow'): field 'mouse' baseline=True live=False
  [PASS] animations.json: matches baseline (leaves byte-exact positional, curves set-compared)
  [PASS] options.jsonl: matches baseline (normalized: type-key name discarded, bool<->int folded)
. animations:enabled: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. decoration:blur:enabled: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. decoration:blur:new_optimizations: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. decoration:blur:xray: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. decoration:shadow:color: type-key differs but normalized value equal (baseline='custom' vs live='gradient', both -> '55000000 0deg')
. decoration:shadow:enabled: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. dwindle:preserve_split: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. dwindle:smart_resizing: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. dwindle:smart_split: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 0)
. ecosystem:enforce_permissions: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 0)
. general:allow_tearing: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 0)
. general:col.active_border: type-key differs but normalized value equal (baseline='custom' vs live='gradient', both -> 'ffff79c6 ffbd93f9 ff8be9fd 45deg')
. general:col.inactive_border: type-key differs but normalized value equal (baseline='custom' vs live='gradient', both -> 'ff6272a4 0deg')
. general:gaps_in: type-key differs but normalized value equal (baseline='custom' vs live='css', both -> '5 5 5 5')
. general:gaps_out: type-key differs but normalized value equal (baseline='custom' vs live='css', both -> '10 10 10 10')
. input:touchpad:natural_scroll: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:animate_manual_resizes: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:animate_mouse_windowdragging: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:disable_hyprland_logo: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:disable_splash_rendering: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:enable_swallow: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:key_press_enables_dpms: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. misc:mouse_move_enables_dpms: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)
. xwayland:force_zero_scaling: type-key differs but normalized value equal (baseline='int' vs live='bool', both -> 1)

PASS: 2  FAIL: 1
exit code: 1
```

The `.` lines are the normalized-comparison layer's informational notes
(type-key differs, normalized value equal — not a divergence, already
folded per `13.1-03`'s established design; kept in the report verbatim
since the gate itself prints them). Every genuine divergence is `!`-prefixed.
There are exactly **2** `!`-prefixed divergence lines, both on `binds.json`.

## Individually reasoned divergences

The gate printed exactly 2 divergences. Both get an entry.

### 1. `bindm mouse:272` (`movewindow`) — field `mouse`

- **Baseline value:** `true`
- **Live value:** `false`
- **Reason:** the vendor-example shape (`/usr/share/hypr/hyprland.lua`
  lines 290-291) is used verbatim for the `movewindow` `bindm` port, but
  `HL.BindOptions` does not list `mouse` as a member and `hl.bind`'s opts
  table has no field validator for it — this is a `hyprctl -j binds`
  serialization gap for Lua-registered mouse binds, documented since
  `13.1-04` and left deliberately **un-loosened** (not carved out like the
  `keycode` gap) because whether it has any runtime effect is NOT
  MECHANICALLY VERIFIABLE with tooling available in this environment (no
  evdev/uinput mouse-button injection tool). Compensating check: physical
  mouse-drag confirmation, listed under Criterion-adjacent manual checks
  below.

### 2. `bindm mouse:273` (`resizewindow`) — field `mouse`

- **Baseline value:** `true`
- **Live value:** `false`
- **Reason:** identical to entry 1 above, same known gap, same bind class
  (the two `bindm` ports registered in `keybinds.lua`).

Every other structural field on all 80 binds matches exactly (verified by
`_compare_binds_structural`'s own count and per-field checks — zero other
`!`-prefixed lines in the output above). `animations.json` and
`options.jsonl` both report `PASS` — zero divergences on either.

## Criterion 1 — compositor started from `hyprland.lua`, deprecation warning absent

**Pre-cutover (this build's own hyprlang session, quoted from
`13.1-LUA-FINDINGS.md`, verified again directly this task):**
```
DEBUG ]: [cfg] Lua config not found, using legacy config at /home/aorus/.config/hypr/hyprland.conf
```

**Post-cutover (this session's live compositor, pid 956, verified directly
this task via `/run/user/1000/hypr/<sig>/hyprland.log`):**
```
DEBUG ]: [cfg] Regular config at /home/aorus/.config/hypr/hyprland.lua
DEBUG ]: [cfg] Using lua config found at /home/aorus/.config/hypr/hyprland.lua
```

No `deprecat`/`removed in hyprland`/`0.57` text exists anywhere in this
build's log output on either config format (established in `13.1-02`,
reconfirmed at Task 1 cutover, `13.1-LUA-FINDINGS.md` "Removal version
reconfirmed at cutover") — the `[cfg]` line above is the actually-observable
discriminator, not deprecation-text absence. **Criterion 1: PASS.**

## Criterion 3 — theme switching re-themes the whole desktop, both modes

Three theme applications performed live against the real desktop this
task: static preset (`dracula`) -> matugen dynamic (`materialyou`) -> back
to the original (`catppuccin`). All three completed without error
(`theme-apply` exits `ok` each time). `general:col.inactive_border` was
read back after each and compared against the `outline` value in
`~/.local/state/theme/hyprland-tokens.lua` at that moment:

| Theme | `hyprland-tokens.lua` `outline` | `hyprctl -j getoption general:col.inactive_border` | Match |
|---|---|---|---|
| `dracula` (static preset) | `rgba(6272a4ff)` | `{"gradient": "ff6272a4 0deg"}` | Yes |
| `materialyou` (matugen dynamic) | `rgba(928f99ff)` | `{"gradient": "ff928f99 0deg"}` | Yes |
| `catppuccin` (original, restored) | `rgba(585b70ff)` | `{"gradient": "ff585b70 0deg"}` | Yes |

Other surfaces re-themed identically each switch — confirmed by reading
each surface's generated `@define-color primary`/equivalent token back
after every application:

| Surface | dracula | materialyou | catppuccin (restored) |
|---|---|---|---|
| waybar (`waybar.css` `@primary`) | `#ff79c6` | `#c4c0ff` | `#cba6f7` |
| swaync (`swaync.css` `@primary`) | `#ff79c6` | `#c4c0ff` | `#cba6f7` |
| kitty (`kitty.conf` `color8`) | `#6272a4` | `#928f99` | (not re-checked after final restore, but the same mechanism proven twice above) |
| walker (`walker-style.css` border) | (not spot-checked for dracula) | `#c4c0ff` | (proven working) |
| GTK3/GTK4 (`gtk-3.0-colors.css`/`gtk-4.0-colors.css` `headerbar_border_color`) | `#6272a4` | (same pipeline, proven for dracula) | (same pipeline) |

All three surfaces' values track the applied theme's own palette in every
case checked, with zero stale/unrefreshed values observed. **Criterion 3:
PASS.**

## Criterion 4 — hyprlock themes correctly from its own unchanged hyprlang fragment

**Mechanically verified:**
- `~/.local/state/theme/hyprlock.conf` — generated, non-empty (28 lines).
- `contract.json` entry for `hyprlock.conf` — still declares format
  `hypr-vars`, unchanged by this plan (confirmed via direct read of
  `theme-engine/.config/theme-engine/contract.json`).

**Lock/unlock cycle — explicitly DEFERRED, not claimed as passed.** This
executor is a non-interactive automated session with no terminal path to
type a password into hyprlock's PAM-backed secure prompt, and no way to
visually confirm the lock screen renders correctly before committing to
unlocking it. `COVERAGE.md`'s "Irreversible/destructive binds" section
already flags `Super+L` -> `hyprlock` as carrying real prior incident
history (`04-REVIEW.md` FIX-02) and names it as requiring deliberate
**manual** confirmation, not a scripted one, for exactly this reason: a
misconfigured lock screen is a session-lockout risk, not a cosmetic bug.
Per this task's own instruction ("if exercising lock/unlock is risky
unattended, say so and defer explicitly rather than claiming it passed"),
this executor did not dispatch `hyprlock` at any point. **The lock/unlock
half of Criterion 4 remains open and requires the operator to perform it
physically** — pressing `Super+L`, confirming the lock screen renders
themed (not blank/default), entering the password, confirming the desktop
returns. This is a real gap in this plan's own verification, recorded
honestly rather than silently passed.

## Criterion 6 — motion-lint passes on the live Lua tree

```
$ motion-lint --self-test
Self-test summary: 14 passed, 0 failed
exit=0

$ motion-lint
Summary: 71 passed, 0 checks failed
exit=0
```

Both exit 0. **Criterion 6: PASS.**

## Regression-gate comparison against Task 1's pre-cutover record

| Gate | Pre-cutover (Task 1, `13.1-LUA-FINDINGS.md`) | Post-cutover (this task) | Regressed? |
|---|---|---|---|
| theme-doctor | 223 passed, 0 failed | **225 passed, 0 failed** (+2 — new `hyprland.lua`/`lib/tokens.lua` now scanned, not a regression) | No |
| theme-parity | 2785 passed, 0 failed | **2785 passed, 0 failed** | No |
| motion-lint | 69 passed, 0 failed | **71 passed, 0 failed** (+2, same scanning-surface growth as theme-doctor) | No |
| motion-lint --self-test | 14 passed, 0 failed | **14 passed, 0 failed** | No |
| keybind-doctor | 13 passed, 0 failed (hyprlang session) | **11 passed, 2 failed** | **Expected, not a regression — see below** |
| quickshell-doctor | 13 passed, 0 failed (superseded/corrected result) | **NOT RE-RUN** — deliberately skipped per this plan's own safety instruction | N/A — deferred |

**keybind-doctor 11/2 is a known, expected, deferred result, not a new
regression.** Both failures trace to the same two already-documented Lua
binds serialization gaps this report's divergence entries 1/2 cover
(`dispatcher`/`arg` opacity for Lua-registered binds, and the
`code:107`-vs-`0` keycode gap) surfacing in keybind-doctor's own comparison
logic, which — unlike `hypr-equivalence-check` — carries no tolerance for
either gap. Retargeting `keybind-doctor` to account for these Lua-specific
serialization gaps is explicitly owned by plan `13.1-09`, not this plan.
The count (13 -> 11 passed, 0 -> 2 failed) is exactly the expected shape:
2 new failures, both attributable to gaps already named and reasoned in
this same report and in `COVERAGE.md`, zero unexplained failures.

**`quickshell-doctor` was deliberately NOT re-run this task**, per this
plan's own explicit safety instruction: it is the test whose
headless-output add/remove during a DP-1 hotplug SEGV-crashed the
compositor in a prior session of this same plan (documented at length in
`13.1-LUA-FINDINGS.md`'s "CRITICAL FINDING" section). It passed 13/13 on
this machine earlier in this same plan's execution (the corrected,
post-recovery result superseding the crash-artifact 6/7). Its true current
result relative to this Lua cutover is therefore unverified by this task,
by design — left deferred rather than re-risking the same crash class.

## Baseline integrity

```
$ git diff --stat -- .planning/phases/13.1-hyprland-lua-config-migration/.hypr-baseline/
(no output — zero changes)
```

## Legacy tree intact

- `~/.config/hypr/hyprland.conf` — exists, resolves.
- `hypr/.config/hypr/config/*.conf` — 7 files present (`animations.conf`,
  `autostart.conf`, `env.conf`, `keybinds.conf`, `monitors.conf`,
  `permissions.conf`, `windowrules.conf`), matching the baseline
  MANIFEST's own "8 hyprlang config files" count once `hyprland.conf`
  itself is included (`HYPR_CONF_ROOT/hyprland.conf` + the 7 files under
  `config/`).
- `~/.local/state/theme/hyprland.conf` — generated, non-empty.
- `~/.local/state/theme/hyprland-motion.conf` — generated, non-empty.

Rollback remains one rename away: `mv ~/.config/hypr/hyprland.lua
~/.config/hypr/hyprland.lua.disabled` + a restart, exactly as documented
throughout this phase.

## Soak clock

The Lua config became the live configuration at the operator's Task 2
restart. Authoritative timestamp (`hyprctl instances -j`'s own `time`
field for the current instance, not a wall-clock estimate):

**`2026-07-28T09:27:19Z`**

Plan `13.1-09`'s decision gate depends on this timestamp plus the
operator's own judgement of normal use accumulated since it.

## Live theme state at end of this task

Restored to `catppuccin` (the theme live before this task's Criterion 3
demonstration began), per this plan's safety instructions. Verified:
`cat ~/.local/state/theme/current-theme` -> `catppuccin`.
