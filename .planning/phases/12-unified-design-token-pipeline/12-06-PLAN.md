---
phase: 12-unified-design-token-pipeline
plan: 06
type: execute
wave: 4
depends_on: ["12-02", "12-04"]
files_modified:
  - matugen/.config/matugen/templates/qml-palette.json
  - matugen/.config/matugen/config.toml
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/theme-stress-test
  - quickshell/.config/quickshell/modules/qmldir
  - quickshell/.config/quickshell/modules/Colours.qml
  - quickshell/.config/quickshell/modules/Motion.qml
  - quickshell/.config/quickshell/modules/Probe.qml
  - quickshell/.config/quickshell/shell.qml
autonomous: false
requirements: [TOKEN-01, TOKEN-02, TOKEN-05]

must_haves:
  truths:
    - "QML surfaces read theme colours from `~/.local/state/theme/`, rendered by matugen as a `contract.json`-listed target — no copied palette file exists anywhere under `quickshell/`, and the only hex literals in repo-authored QML are the declared debug-magenta fallbacks in `Colours.qml`. (TOKEN-01, criterion 1)"
    - "A theme switch re-colours the live summoned inspector without restarting it: the swatch fills change while `pgrep -x quickshell` reports the same PID before and after. (TOKEN-02, criterion 1)"
    - "No quickshell step is added to `theme-apply`'s reload fan-out — quickshell is the first surface in the repo needing none, and a reload would rebuild the surface and destroy the crossfade the JSON transport was chosen to enable. (D-18)"
    - "Live re-colour is proven across `theme-stress-test`'s 10 consecutive switches, not by a single switch — `commit.sh` replaces files by temp-then-rename, which changes the inode, and a path-based watcher that stops firing afterwards would pass a single-switch test by construction. (D-17)"
    - "A missing matugen key renders a debug-magenta chip with an `(unmapped)` caption — never a blank swatch and never a plausible-looking black or white default. (ui: empty/E1)"
    - "A `FileView` read taken before the palette JSON exists takes the identical unmapped-token fallback path; it is a local-disk read, so no spinner or in-flight state exists or is needed. (ui: loading/E1)"
    - "Malformed palette content resolves to the same debug-magenta path rather than a plausible default. (ui: error/E1)"
    - "All 17 colour roles render as a chip plus a Label-role caption, grouped, and crossfade on theme switch via `Behavior on color`. (ui: populated/E1)"
    - "A truncated or unparseable palette read, possible mid-render, neither crashes nor blanks the `Colours` singleton: the parse path has an explicit failure branch yielding the per-key fallback, and the subsequent watcher event self-heals. (ui: partial/E1)"
    - "Token-name captions are a fixed known set whose longest member is `onSecondaryContainer`; the Label role is sized against that, so no dynamic truncation logic is required. (ui: long-text/E1)"
    - "A missing or semantic-pair-less `motion.json` renders one explicit row reading that no motion tokens loaded, never an empty gap. (ui: empty/E2)"
    - "The motion rows use the same local-disk `FileView` read as the swatches; there is no in-flight state. (ui: loading/E2)"
    - "An unresolvable easing or duration renders the row with its name plus an undefined marker, and the same condition is a motion-lint FAIL. (ui: error/E2)"
    - "Three motion rows render — `standard`, `emphasized-in`, `emphasized-out` — each showing the token name, its resolved duration in milliseconds, and its resolved easing name. (ui: populated/E2)"
    - "A pair whose duration resolves but whose easing does not renders the resolved field and marks only the unresolved one; the row is never hidden. (ui: partial/E2)"
    - "Two things animate, and both are token-driven: the theme-switch crossfade, which proves tokens govern real behaviour rather than a demo, and a replayable motion row fired from a button, which makes the human render gate repeatable in seconds instead of costing a full theme switch. (D-16)"
    - "Clicking Replay mid-replay restarts from frame 0 across all three pairs simultaneously — stop then start, never disabled and never a silent no-op, because the surface exists for rapid repeat comparison at the render gate. (ui: loading/E4)"
    - "Control labels are fixed literals; no dynamic text path exists on the replay controls. (ui: long-text/E4)"
    - statement: "17 colour roles plus the motion rows may exceed the probe's current fixed 360x260 footprint; the panel must size from content via Column-driven implicit dimensions rather than clip. (ui: overflow/E1)"
      verification: backstop
    - statement: "The motion row count grows as Phase 13 wires further semantic pairs; the section must not clip as rows are added. (ui: overflow/E2)"
      verification: backstop
    - statement: "The header banner must not push panel width when the screen name and the theme name are both long. (ui: overflow/E3)"
      verification: backstop
    - statement: "Theme names vary in length; the header banner must elide with an ellipsis rather than wrap or widen the panel. (ui: long-text/E3)"
      verification: backstop
  artifacts:
    - quickshell/.config/quickshell/modules/Colours.qml
    - quickshell/.config/quickshell/modules/Motion.qml
    - matugen/.config/matugen/templates/qml-palette.json
  key_links:
    - "matugen [templates.qml] -> ~/.local/state/theme/palette.json -> Colours.qml FileView/JsonAdapter -> Colours.<role> aliases -> inspector swatch fills"
    - "motion.sh -> ~/.local/state/theme/motion.json -> Motion.qml FileView/JsonAdapter -> Motion.<token> -> Behavior easing.bezierCurve"
    - "commit.sh atomic rsync (inode replace) -> FileView watch -> theme-stress-test's 10-switch live re-colour assertion (D-17)"
    - "modules/qmldir -> must list Colours and Motion, because an explicit qmldir disables directory scanning for the whole directory"
  flagged_assumptions:
    - "Edge probe row TOKEN-02 / category `unclassified` is UNRESOLVED. The deterministic probe could not classify TOKEN-02 into any of its eight shape categories, which is expected for a requirement about live propagation rather than a data transformation. Surfaced here rather than auto-backstopped, per the probe protocol's rule that an unclassified row never becomes a backstop. Reviewer action: confirm that D-17's 10-switch assertion plus the unchanged-PID check are the complete proof TOKEN-02 needs, or name the missing one."
    - "UI-SPEC §RESEARCH carry-over, unchecked: does matugen write its render targets atomically (temp plus rename) or in place? 12-RESEARCH.md verified matugen's own write is NOT atomic (same inode across two renders to the same path), but also that this does not matter for the live pipeline because matugen writes into a fresh mktemp tree every run and the file FileView actually watches is published by commit.sh's atomic rsync. The residual question is therefore only whether a one-frame magenta flicker is ever OBSERVED at the render gate. If Task 3's human gate reports one, restructuring contract.json's render targets to write-then-rename becomes a Phase 13 item with evidence behind it; if not, E1's partial consideration dissolves. Do not build for it speculatively."
---

<objective>
Give QML a colour and motion palette read live from the state dir, and turn the Phase 11 probe
into the token inspector that carries criterion 1 — then judge it with a blocking human
render-and-look gate.

Purpose: D-10 makes the summoned probe the surface that proves live re-colour, keeping the root
headless and leaving Phase 14 its "first real QML surface" billing. D-15 keeps the probe
unmistakable as an instrument through its *content* rather than by refusing colour — the "never
being pretty is a feature" intent survives as diagnostic content. D-11's JSON transport is
chosen specifically so a theme switch can crossfade rather than hard-cut, which is the fidelity
the milestone's subject demands.

Output: a matugen QML palette target, two Quickshell singletons, a rewritten inspector, D-17's
stress-test assertion, and a recorded human verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unified-design-token-pipeline/12-CONTEXT.md
@.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md
@.planning/phases/12-unified-design-token-pipeline/12-01-SUMMARY.md
@.planning/phases/12-unified-design-token-pipeline/12-04-SUMMARY.md
@quickshell/.config/quickshell/modules/Probe.qml
@quickshell/.config/quickshell/shell.qml
</context>

<artifacts_this_phase_produces>
See `12-01-PLAN.md` §"Artifacts this phase produces" for the complete phase-level list of
new symbols, files, contract keys, CLI surfaces, Hyprland variables, GTK4 custom properties,
QML types and gate checks. Nothing in that list is pre-existing; a source-grounding pass must
not treat any of those names as drift.

This plan specifically creates: `matugen/.config/matugen/templates/qml-palette.json`; the
matugen `[templates.qml]` entry; the rendered path `~/.local/state/theme/palette.json` and its
`contract.json` `files[]` entry with format `json`;
`quickshell/.config/quickshell/modules/Colours.qml` exposing 19 readonly role aliases
(`primary`, `onPrimary`, `primaryContainer`, `onPrimaryContainer`, `secondary`, `onSecondary`,
`secondaryContainer`, `onSecondaryContainer`, `tertiary`, `onTertiary`, `surface`, `onSurface`,
`surfaceVariant`, `onSurfaceVariant`, `background`, `onBackground`, `outline`, `error`,
`onError`); `quickshell/.config/quickshell/modules/Motion.qml` exposing `motionEnabled`,
`motionScale`, `standardDuration`, `standardBezier`, `emphasizedInDuration`,
`emphasizedInBezier`, `emphasizedOutDuration`, `emphasizedOutBezier`; the `Probe.qml` inspector
members `replayAll()` and its section structure; and the `theme-stress-test` live re-colour
assertion.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: matugen QML palette target and the two Quickshell singletons</name>

  <precondition>`quickshell/.config/quickshell/modules/qmldir` exists and is the file plan 12-01 created — an explicit qmldir disables quickshell's directory scanner for the whole directory, so any type not listed in it becomes unresolvable. If the file is absent, halt: adding `Colours.qml` and `Motion.qml` without listing them would break `import "modules"` in a way that looks like a QML syntax error.</precondition>

  <files>matugen/.config/matugen/templates/qml-palette.json, matugen/.config/matugen/config.toml, theme-engine/.config/theme-engine/contract.json, quickshell/.config/quickshell/modules/qmldir, quickshell/.config/quickshell/modules/Colours.qml, quickshell/.config/quickshell/modules/Motion.qml</files>

  <read_first>
    - `matugen/.config/matugen/templates/vscodium-colors.json` — the closest existing JSON-format template, showing matugen's `{{colors.<key>.default.hex}}` interpolation syntax and the exact key spellings (`on_surface`, `surface_variant`, `primary_container`, `on_primary_container`, and so on) that the QML template must map from.
    - `matugen/.config/matugen/config.toml` lines 40-100 — the `[templates.<name>]` block shape with its `input_path` / `output_path` pair and the `# ── <Name> colors ──` banner comment convention. The new entry copies it exactly.
    - `theme-engine/.config/theme-engine/contract.json` — the `files[]` array as amended by plan 12-03; the `vscodium.json` entry shows the `json` format already in use for a matugen JSON target, which is the handler `palette.json` reuses unchanged.
    - `quickshell/.config/quickshell/modules/Probe.qml` lines 50-65 — **the working `FileView`/`JsonAdapter` precedent already proven live in Phase 11**: `path`, `watchChanges: true`, `onFileChanged: reload()`, `onAdapterUpdated: writeAdapter()`, and a `JsonAdapter` with declared property defaults.
    - `.planning/phases/12-unified-design-token-pipeline/12-RESEARCH.md` §"Pattern 2" — the proposed `Colours.qml` and `Motion.qml` shapes, the `[VERIFIED]` note that Quickshell's `Singleton` type is exported as `Quickshell/Singleton 0.0` by the installed qmltypes (so no `pragma Singleton` and no qmldir singleton declaration is needed), and the `easing.bezierCurve` list ordering.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"QML palette contract (D-11)" lines 101-113 — the exact 19 camelCase property names and the debug-magenta fallback rule.
    - `quickshell/.config/quickshell/modules/qmldir` as created by plan 12-01 — the entry grammar to extend.
    - `theme-engine/.config/theme-engine/lib/reload.sh` — confirm for yourself that it has no quickshell branch and leave it that way (D-18).
  </read_first>

  <behavior>
    - After one `theme-apply` run, `~/.local/state/theme/palette.json` exists and holds one hex string per matugen role.
    - `Colours.primary` in QML resolves to the current theme's primary hex.
    - With `palette.json` absent, every `Colours.*` alias resolves to the declared fallback rather than to an empty string or a plausible default.
    - With `palette.json` holding malformed JSON, the same fallback path is taken and the shell does not crash.
    - After `palette.json` is replaced on disk, `Colours.*` values update in place without a document reload.
    - `Motion.standardDuration` and `Motion.standardBezier` resolve from the rendered `motion.json`.
  </behavior>

  <action>
**Create `matugen/.config/matugen/templates/qml-palette.json`** emitting a flat JSON object with
one camelCase key per Material You role, mapped from matugen's snake_case keys through its
`{{colors.<key>.default.hex}}` interpolation. The 19 keys are fixed by UI-SPEC's QML palette
contract: `primary`, `onPrimary`, `primaryContainer`, `onPrimaryContainer`, `secondary`,
`onSecondary`, `secondaryContainer`, `onSecondaryContainer`, `tertiary`, `onTertiary`,
`surface`, `onSurface`, `surfaceVariant`, `onSurfaceVariant`, `background`, `onBackground`,
`outline`, `error`, `onError`. No renaming beyond snake_case-to-camelCase — the one cost D-11
accepted is that property names track matugen's key names, and any consumer breaks on a key
rename regardless, so an extra translation layer would only add a second place to break.

**Add `[templates.qml]` to `matugen/.config/matugen/config.toml`** with the file's banner-comment
convention, `input_path` pointing at the new template and `output_path` at
`~/.local/state/theme/palette.json`. Motion targets are deliberately NOT added here — `motion.sh`
renders those, per D-01's explicit rejection of the matugen route.

**Add `palette.json` to `contract.json`'s `files[]`** with format `json`. TOKEN-01's wording is
specific: the palette must be a `contract.json`-listed matugen render target. An unlisted target
is exactly the drift vector Phase 2's lesson named.

**Create `quickshell/.config/quickshell/modules/Colours.qml`** rooted at Quickshell's `Singleton`
type, holding a `FileView` on `~/.local/state/theme/palette.json` with `watchChanges: true` and
`onFileChanged: reload()`, a `JsonAdapter` declaring one property per role with the debug-magenta
default `#FF00FF`, and one `readonly property alias` per role so consumers write `Colours.primary`.

Two deliberate divergences from `Probe.qml`'s precedent, both worth a comment: omit
`onAdapterUpdated: writeAdapter()` — this singleton is a read-only consumer of matugen's output
and must never write back into the state dir, where `commit.sh` is the sole writer; and root at
`Singleton` rather than `PanelWindow`.

The fallback rule is the load-bearing part and must be stated in a comment: a missing or
malformed key renders debug magenta, never a silent black or white. A broken token mapping must
be visually loud, not visually plausible — the same lesson D-24 applies to motion-reference
resolution, applied here to colour. Handle the malformed-content and truncated-read cases
explicitly rather than relying on the adapter's implicit behaviour, so the failure branch is
inspectable in source (this is what downgraded UI-SPEC's E1 `partial` consideration from
backstop to covered).

**Create `quickshell/.config/quickshell/modules/Motion.qml`** as a sibling singleton on the same
pattern, reading `~/.local/state/theme/motion.json` and exposing `motionEnabled`, `motionScale`,
and per-semantic-pair duration and bezier properties. Emit the bezier as the six-element list
Qt's `easing.bezierCurve` expects — the four control points plus the trailing endpoint — which
`motion.sh` already renders in that order, so `Motion.qml` passes it straight through rather than
re-appending anything.

Two verified traps to avoid, both recorded in RESEARCH: the base `Animation` /
`PropertyAnimation` type has **no** `enabled` property on this Qt build — only `Behavior` does —
so `motionEnabled` is consumed as `Behavior.enabled`, and any imperatively-triggered animation
needs a different gate (Task 2 handles that). And `SpringAnimation`'s stiffness parameter is
named `spring`, not `stiffness` — relevant to plan 12-08, noted here so `Motion.qml`'s property
naming does not invent a Compose-style vocabulary the toolkit does not have.

**Extend `modules/qmldir`** with entries for both new types. This is not optional bookkeeping:
plan 12-01 checked in an explicit qmldir precisely to stop quickshell scanning that directory, so
a type absent from the file is unresolvable and `shell.qml` fails to load with what looks like a
syntax error. Verify the entries against a `-v -v` run before considering the task done.

**Verify `Easing.BezierSpline` behaves as documented on the installed build before relying on
it** — UI-SPEC flags this explicitly as unverified and standing constraint 2 applies. A minimal
QML that animates a property with an emitted bezier and renders visibly is sufficient; record the
result.

Add no quickshell step to `theme-apply`'s reload fan-out (D-18). Quickshell becomes the first
surface in the repo needing none — state that explicitly in a comment in `Colours.qml` so nobody
adds one later for symmetry. Beyond being unnecessary, a reload would rebuild the surface and
destroy the crossfade this transport was chosen to enable.
  </action>

  <reversibility rating="reversible">Two new QML files, one new template, two additive config entries; removing them leaves the colour pipeline and the probe exactly as Phase 11 left them.</reversibility>

  <verify>
    <automated>~/.config/theme-engine/theme-apply catppuccin && jq -e 'keys | length == 19' ~/.local/state/theme/palette.json && ~/.config/theme-engine/theme-doctor >/dev/null 2>&1; echo "doctor=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `jq -e 'keys | length == 19 and (to_entries | all(.value | test("^#[0-9a-fA-F]{6}$")))' ~/.local/state/theme/palette.json` exits 0 after a `theme-apply` run.
    - `jq -e '.files | map(select(.name == "palette.json")) | length == 1' theme-engine/.config/theme-engine/contract.json` exits 0.
    - `grep -cE '^(Colours|Motion) ' quickshell/.config/quickshell/modules/qmldir` returns `2`.
    - No copied palette file exists under the quickshell package: `find quickshell/ -name '*.json' | wc -l` returns `0`, and no QML file reads a palette path outside `~/.local/state/theme/`.
    - Hex literals in repo-authored QML are confined to the declared fallbacks: `grep -rlE '#[0-9a-fA-F]{6}' quickshell/.config/quickshell/` lists `Colours.qml` and nothing else.
    - The shell loads with the two new types present: 5 consecutive `pkill -x quickshell` plus `quickshell-launch.sh` cycles produce zero `is not a type` and zero `Failed to load configuration` occurrences in `~/.cache/quickshell.log`.
    - Fallback path proven: move `~/.local/state/theme/palette.json` aside, restart the shell, summon the probe, and confirm the surface renders with the fallback rather than crashing or blanking. Restore the file and confirm normal colours return.
    - Malformed-content path proven: write invalid JSON into a copy of the palette path, confirm the shell does not crash and takes the same fallback, then restore via one `theme-apply` run.
    - `theme-apply`'s reload fan-out is unchanged: `grep -ci quickshell theme-engine/.config/theme-engine/lib/reload.sh` returns `0`.
    - `~/.config/theme-engine/theme-doctor` and `~/.config/theme-engine/theme-parity` both exit 0.
  </acceptance_criteria>

  <done>QML reads both colour and motion tokens live from `contract.json`-listed state-dir targets through two singletons, with a loud fallback and no second palette copy anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Probe becomes the token inspector, and theme-stress-test proves live re-colour across 10 switches</name>

  <files>quickshell/.config/quickshell/modules/Probe.qml, quickshell/.config/quickshell/shell.qml, theme-engine/.config/theme-engine/theme-stress-test</files>

  <read_first>
    - `quickshell/.config/quickshell/modules/Probe.qml` (current state, post-12-01) — the four Phase 11 instruments that must survive unchanged underneath the new content: the counter button, the text field, the hand-edited JSON state label bound through `probeAdapter.label`, and the screen label. Also `implicitWidth: 360` / `implicitHeight: 260`, the `WlrLayershell` namespace and keyboard-focus settings, the `HyprlandFocusGrab`, and the `Column { spacing: 12 }` / `Row { spacing: 8 }` layout.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"Token Inspector — Surface Contract" lines 241-273 — the six required content blocks top to bottom, and the explicit instruction that Phase 11's instruments are retained unchanged underneath.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"Spacing Scale" and §"Typography" lines 43-84 — the four type roles with explicit `font.pixelSize` values and weights, and the spacing tokens, including the note that the existing `spacing: 12` and `spacing: 8` are superseded by the `md` and `sm` tokens.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"Color" lines 86-113 — the 60/30/10 role mapping onto the inspector's surfaces, and the reservation of accent roles for the crossfade target, the currently-animating replay element and the header banner only.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"UI Considerations" — the E1, E2 and E4 covered rows this task implements literally, and the four overflow/long-text backstop rows it must satisfy by content-driven sizing and header elision.
    - `.planning/phases/12-unified-design-token-pipeline/12-RESEARCH.md` §"Pitfall 4" — **the imperative-animation gap.** `Behavior.enabled` gates an implicit transition; a standalone or imperatively-triggered animation has no `enabled` toggle at all on this Qt build. The replay row's `.start()` calls therefore need an explicit `Motion.motionEnabled` check at the trigger, or a zero duration for that run — this is a real implementation decision, not something that falls out of one property binding.
    - `theme-engine/.config/theme-engine/theme-stress-test` lines 100-160 (the log file, `PASS`/`FAIL`, `check_or_abort` and the failure-time diagnostic dump) and lines 330-400 (the `for (( i = 1; i <= SWITCHES; i++ ))` loop, the per-switch `theme-doctor` gate, and the postcondition block).
    - `theme-engine/.config/theme-engine/lib/commit.sh` line ~94 — `rsync -a --delete` **without** `--inplace`, which is why each published file is a new inode and why a path-based watcher could stop firing after the first switch.
  </read_first>

  <behavior>
    - Summoning the inspector shows a header banner, a colour-role section, a motion section and a replay control, with Phase 11's four instruments below.
    - Switching theme while the inspector is open crossfades the swatch fills rather than hard-cutting, with the daemon PID unchanged.
    - Clicking Replay re-triggers all three semantic pairs' animations; clicking again mid-replay restarts them from the beginning.
    - At motion-scale `off`, the crossfade does not animate and the replay control produces no motion.
    - At motion-scale `reduced`, the same animations visibly complete sooner than at `normal`.
    - A palette or motion file with a missing key renders the loud fallback in the affected swatch or row while every other element renders normally.
    - `theme-stress-test` reports live re-colour holding on every one of its 10 switches, not only the first.
  </behavior>

  <action>
**Rewrite `Probe.qml` into the token inspector** exactly as UI-SPEC's Surface Contract specifies,
top to bottom: a header banner in the Display role showing the screen name, the current theme and
the mode; a "Colour Roles" section with one chip plus Label-role caption per matugen role reading
live from `Colours`; a "Motion — Semantic Pairs" section with one row per semantic token showing
its name, resolved duration in milliseconds and resolved easing name; a "Replay" section with a
button labelled exactly as the Copywriting Contract specifies; and the theme-switch crossfade
implemented as `Behavior on color { ColorAnimation { … } }` on the swatch fills, taking its
duration and `easing.bezierCurve` from `Motion`.

Keep Phase 11's four instruments — counter button, text field, hand-edited JSON state label,
screen label — unchanged underneath. This phase does not remove working instrumentation, and
QS-02's proof surface stays intact.

Apply UI-SPEC's spacing and typography tokens: replace the existing `spacing: 12` and
`spacing: 8` with the `md` and `sm` values, use `lg` for panel padding and `xl` between the
header band and the swatch grid, and declare explicit `font.pixelSize` and weight for each of the
four type roles. Exactly two weights exist — regular for body and labels, semibold for headings
and the banner.

**Size from content, not from the fixed footprint.** Seventeen roles plus three motion rows plus
a replay control will not fit the inherited 360x260. Drive the panel's implicit dimensions from
the `Column` rather than clipping, and elide the header banner's text with an ellipsis rather
than letting a long theme name widen or wrap the panel. These are UI-SPEC's four overflow and
long-text backstop rows; content-driven sizing plus elision satisfies all four with one
structural choice.

**Keep it reading as an instrument (D-15).** It is styled fully — it must consume real palette
roles across surface, background, text and accent for criterion 1 to mean anything — but every
element is labelled with the token it consumes. Phase 11's "the probe never being pretty is a
feature" intent survives through *content*: a labelled diagnostic grid cannot be mistaken for a
shipped surface even in full colour.

**Implement the fallback and partial-resolution behaviours literally**, because they are the
covered UI considerations this plan is accountable for: an unmapped colour role renders the
debug-magenta chip with an `(unmapped)` caption; a `motion.json` with no semantic pairs renders
one explicit row saying so rather than an empty gap; a row whose duration resolves but whose
easing does not renders the resolved field and marks only the unresolved one, and is never
hidden.

**Handle the imperative-animation gap explicitly.** The crossfade is implicit and takes
`Behavior.enabled: Motion.motionEnabled` directly. The replay row is imperative — its trigger
code must check `Motion.motionEnabled` before calling `start()`, or set the animation's duration
to zero for that run. Choose one, do it in one place, and comment why: the base animation type has
no `enabled` property on this Qt build, so the two paths genuinely need different gates. Replay
clicked mid-replay stops then starts across all three pairs simultaneously — never disabled,
never a silent no-op, because the surface exists for rapid repeat comparison at the render gate.

Touch `shell.qml` only if the inspector's structure requires it. Plan 12-01 already settled the
per-screen fan-out; do not restructure it here.

**Add D-17's live re-colour assertion to `theme-stress-test`.** Inside the existing 10-switch
loop, after each switch, assert that the QML palette actually propagated: compare the hex the
shell is currently rendering for a known role against the value in `~/.local/state/theme/palette.json`
for that same switch, and assert the quickshell PID is unchanged from the run's start. Reach the
rendered value without a human — writing the observed value out through the existing
`FileView`/`JsonAdapter` write path the probe already owns is the mechanism already proven in
Phase 11; use it rather than inventing a screenshot comparison.

Say in a comment why one switch is not enough, because that is the entire point of putting it
here: `commit.sh` uses `rsync -a --delete` without `--inplace`, so every published file is a new
inode, and a path-based watcher that detaches on replace would work on the first switch and
silently stop — which a single-switch test passes by construction. `theme-stress-test` exists for
exactly this works-once-then-degrades class. If the assertion fails from switch two onward, the
fix is re-arming the watch on replace via `FileView.reload()` — but that is a finding to record,
not something to build speculatively.

Guard the assertion so a context with no running quickshell degrades to a named SKIP rather than
a FAIL, matching the file's existing guarded-check discipline.
  </action>

  <reversibility rating="reversible">`Probe.qml` is fully replaced but `git checkout` restores it; the stress-test assertion is additive and report-only. The one sticky choice is the transport format, and D-11 already fixed it.</reversibility>

  <verify>
    <automated>~/.config/theme-engine/theme-doctor >/dev/null 2>&1; echo "doctor=$?"; grep -c 'is not a type\|Failed to load configuration' ~/.cache/quickshell.log</automated>
  </verify>

  <acceptance_criteria>
    - The shell loads cleanly with the rewritten inspector: 5 consecutive `pkill -x quickshell` plus `quickshell-launch.sh` cycles produce zero `is not a type` and zero `Failed to load configuration` occurrences in `~/.cache/quickshell.log`.
    - The inspector renders all 17 roles: summon it and confirm via the shell log or a `Colours`-driven count property that the swatch repeater's model length is 17.
    - Live re-colour with an unchanged PID, mechanically: capture `pgrep -x quickshell`, run `theme-apply` with a different palette, confirm the inspector's rendered role value matches the new `~/.local/state/theme/palette.json` value, and confirm `pgrep -x quickshell` returns the same PID.
    - `~/.config/theme-engine/theme-stress-test` completes 10 switches with zero failures and its output contains a per-switch live re-colour `[PASS]` line for every switch — not only switch 1. Record the full summary line in the SUMMARY.
    - The stress-test assertion is proven to fail: run it once with the quickshell daemon deliberately stopped and confirm the assertion reports a named SKIP (not a false PASS), then run it again with the daemon up and confirm PASS.
    - Unmapped-role behaviour: render a palette missing one key into a throwaway copy of the state path, confirm the affected chip shows the fallback and its caption carries the unmapped marker while every other chip renders normally.
    - Empty-motion behaviour: point `Motion.qml`'s path at a `motion.json` whose `semantic` object is empty and confirm one explicit "no motion tokens loaded" row renders rather than an empty gap. Restore afterwards.
    - Motion gating: at motion-scale `off`, `jq -e '.motion_enabled == false' ~/.local/state/theme/motion.json` exits 0 and the replay control produces no animation; return the axis to `normal`.
    - Panel sizes from content: the rendered surface's height reported by `hyprctl layers -j` for the `quickshell-probe` namespace exceeds the inherited 260 pixels, confirming the fixed footprint was replaced by content-driven sizing.
    - `~/.config/hypr/scripts/motion-lint` exits 0 — the inspector's QML consumes motion tokens through `Motion.*` rather than raw literals, and the lint now covers it.
    - `~/.config/theme-engine/theme-doctor` and `~/.config/theme-engine/theme-parity` both exit 0.
  </acceptance_criteria>

  <done>The probe is a labelled token inspector reading colour and motion live from the state dir, crossfading on theme switch with the daemon PID unchanged, and live re-colour is asserted on all 10 stress-test switches rather than one.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking human render-and-look gate — the token inspector (D-27)</name>

  <what-built>
    The Phase 11 probe is now a token inspector. It reads the 17 Material You colour roles live
    from `~/.local/state/theme/palette.json` (a new matugen render target) and the three motion
    semantic pairs from `~/.local/state/theme/motion.json`, through two Quickshell singletons.
    Its swatch fills crossfade on a theme switch using the emitted motion tokens, and a "Replay
    motion" button re-triggers each semantic pair's animation on demand so this gate is
    repeatable in seconds rather than costing a full theme switch each time.

    D-27 makes this a blocking per-plan gate rather than deferring it to phase end, overriding
    `config.json`'s `human_verify_mode`, per ROADMAP standing constraint 1 — which was written
    directly out of the Phase 6 and Phase 8 failures, where fully green mechanical gates shipped
    visibly broken surfaces.
  </what-built>

  <how-to-verify>
    Three things are being judged: that the palette maps correctly across themes, that motion
    tokens visibly differ from one another, and that reduced motion visibly shortens.

    **A — palette maps correctly across themes.**
    1. Press Super+Shift+G to summon the inspector.
    2. Read the header banner: it should name the screen, the current theme and the mode.
    3. Look at the colour grid. Every chip should be captioned with the token it consumes.
       Check three specifically: `primary` should read as the theme's accent; `surface` should
       read as the panel's own background family; `onSurface` should be legible against it.
    4. Report: is any chip magenta? A magenta chip means that token did not map — say which.
    5. With the inspector still open, run in a terminal:
       `~/.config/theme-engine/theme-apply catppuccin-latte`
       Watch the swatches. Report: did they **crossfade** smoothly, or hard-cut with a flash?
       Did the panel stay open the whole time (it must not disappear and reappear)?
    6. Repeat with a couple more themes of your choosing, including at least one light and one
       dark. Report whether any theme produces an obviously wrong or unreadable pairing.

    **B — motion tokens visibly differ.**
    7. Click "Replay motion". Watch the three semantic pairs animate.
    8. Report: can you see the difference between them? `emphasized-in` should read as slower
       and settling; `emphasized-out` should read as quicker and departing; `standard` sits
       between. If all three look identical, the tokens are not reaching the animations.
    9. Click "Replay motion" again while they are still animating. Report: did they restart
       from the beginning, or did the click do nothing?

    **C — reduced motion visibly shortens.**
    10. Run: `~/.config/hypr/scripts/motion-switch.sh reduced`
    11. Summon the inspector again and click "Replay motion". Report: are the animations
        visibly shorter than before?
    12. Run: `~/.config/hypr/scripts/motion-switch.sh off`
    13. Summon and click "Replay motion", then switch theme once with the inspector open.
        Report: is there **no** animation at all now — no replay motion and no crossfade?
    14. Run: `~/.config/hypr/scripts/motion-switch.sh normal` to restore.

    **D — does it read as an instrument?**
    15. Final judgement: does this panel read unmistakably as a diagnostic instrument rather
        than as a shipped surface? D-15 requires that the "never pretty" intent survives through
        labelled diagnostic content. If it looks like a product, say so.
  </how-to-verify>

  <resume-signal>Report A (5 items), B (2 items), C (3 items) and D. Type "approved" if all four groups pass, or describe exactly what looked wrong — including anything that merely felt off, since that is the class of problem this gate exists to catch and mechanical gates cannot.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.local/state/theme/palette.json` -> quickshell daemon | A generated file on disk is read and parsed by the always-on shell process; malformed content must not take the shell down |
| `Colours.qml` / `Motion.qml` -> state dir | Both singletons read a directory whose sole writer is `commit.sh`; a QML write-back would create a second writer |
| `theme-stress-test` -> live desktop | The stress test performs 10 real theme switches against the running session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-20 | Denial of Service | malformed or truncated `palette.json` parsed by the always-on shell | high | mitigate | Both singletons declare per-key defaults and an explicit parse-failure branch yielding the loud fallback; the acceptance criteria prove both the absent-file and malformed-content paths leave the shell running. The watcher self-heals on the next valid write. |
| T-12-21 | Tampering | a QML singleton writing back into `$STATE_DIR` | high | mitigate | `Colours.qml` and `Motion.qml` deliberately omit `onAdapterUpdated: writeAdapter()`, with a comment stating they are read-only consumers and `commit.sh` is the sole writer. `Probe.qml` retains its own write path only for its separate `~/.local/state/quickshell/probe.json` instrument, which is outside the theme state dir. |
| T-12-22 | Information Disclosure | `$STATE_DIR` read by the shell process | low | accept | `commit.sh` already enforces `chmod 700` on the state dir and the shell runs as the same user. No new exposure; the palette contains colour values only. |
| T-12-23 | Denial of Service | `theme-stress-test`'s 10 live switches | medium | accept | The script is the repo's established phase-gate mechanism, already gated on a strict `theme-doctor` exit 0 per switch, with failure-time diagnostic dumps. This plan adds one report-only assertion inside the existing loop and changes no switch behaviour. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs anywhere in Phase 12 — `12-RESEARCH.md` §Package Legitimacy Audit records zero new packages, every dependency already installed and `pacman -Q`-verified. |
</threat_model>

<verification>
- `~/.config/theme-engine/theme-doctor`, `~/.config/theme-engine/theme-parity` and `~/.config/hypr/scripts/motion-lint` all exit 0.
- `~/.config/theme-engine/theme-stress-test` completes 10 switches with zero failures and a per-switch live re-colour PASS.
- 5 consecutive clean daemon restarts with no QML load failure.
- The blocking human render gate is recorded as approved, with the reported observations captured verbatim in the SUMMARY.
- The motion-scale axis is returned to `normal` and the state dir restored before the plan closes.
</verification>

<success_criteria>
Criterion 1 is proven: a theme switch re-colours a live Quickshell surface without restarting
it, with the palette read from a `contract.json`-listed matugen render target, no copied palette
file and no stray hex literal in repo-authored QML — and a human has confirmed by eye that the
palette maps correctly, that motion tokens visibly differ, and that reduced motion visibly
shortens.
</success_criteria>

<output>
Create `.planning/phases/12-unified-design-token-pipeline/12-06-SUMMARY.md` when done.
Capture the human gate's reported observations verbatim, and record whether a one-frame
fallback flicker was observed during a theme switch — that answers UI-SPEC's RESEARCH carry-over
question and decides whether it becomes a Phase 13 item.
</output>
