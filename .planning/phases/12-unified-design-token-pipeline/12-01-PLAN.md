---
phase: 12-unified-design-token-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickshell/.config/quickshell/modules/qmldir
  - quickshell/.config/quickshell/shell.qml
  - quickshell/.config/quickshell/modules/Probe.qml
  - .planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md
autonomous: false
requirements: [QS-03]

must_haves:
  truths:
    - "quickshell-doctor reports 14 passed / 0 failed and exits 0, with the per-screen surface creation check (QS-03) among the passes — or, on the STOP branch, the check still FAILs honestly and a written STOP record exists naming every arrangement tried."
    - "The proof is taken against the always-on autostart daemon (the process started by hypr/.config/hypr/scripts/quickshell-launch.sh), not a foreground `quickshell -p` run, and is re-taken after a real session restart."
    - "quickshell/.config/quickshell/modules/qmldir exists on disk and lists every .qml type in that directory, so quickshell's directory scanner is never invoked for modules/ (the FM1 root cause recorded in 11-QUICKSHELL-EVIDENCE.md)."
    - "Two Quickshell.screens entries yield two distinct probe surfaces, never one shared: hyprctl layers -j reports exactly one quickshell-probe address under DP-1 and exactly one under the hotplugged output, and the two address sets are disjoint. (edge: QS-03/adjacency)"
    - "Variants delegate instantiation order over Quickshell.screens is never relied on: each delegate binds its own PanelWindow.screen from modelData and Probe.qml's screenLabel reads probeWindow.screen.name, so hotplug order cannot change which surface lands on which output. (edge: QS-03/ordering)"
    - statement: "With zero entries in Quickshell.screens the shell root survives without crashing and creates no surface; with exactly one entry it creates exactly one. Zero-output survival is physically untestable on this single-monitor host (STATE.md Blockers/Concerns item 3). (edge: QS-03/empty)"
      verification: backstop
  artifacts:
    - quickshell/.config/quickshell/modules/qmldir
    - .planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md
  key_links:
    - "shell.qml Variants(model: Quickshell.screens) -> per-screen LazyLoader -> Probe(screen: modelData)"
    - "modules/qmldir -> disables quickshell's synthesised-qmldir scanner for modules/ (FM1 fix)"
    - "quickshell-doctor per-screen surface creation check -> hyprctl layers -j per-monitor address sets"
  flagged_assumptions:
    - "RESEARCH finding 4 ([CITED: DeepWiki quickshell-mirror docs], MEDIUM confidence, not binary-verified) claims an explicit qmldir disables Quickshell's auto-generation scanner for that directory entirely. If Task 1's `-v -v` trace still shows a `Scanning directory` line for modules/ after the qmldir is checked in, that citation is wrong on this build and arrangement A's premise is void — record it and move to arrangement B rather than iterating on A."
---

<objective>
Close the carried-forward QS-03 per-screen surface fan-out with the bounded targeted fix
D-12 specifies, and prove it against the always-on autostart daemon across a real session
restart.

Purpose: D-32 sequences QS-03 first because it and the token inspector both modify
`shell.qml` and `modules/Probe.qml`, and because debugging an intermittent scanner race
while adding swatches and a replay row to the same files is a compound problem. Settling
per-screen structure against the *current minimal* probe keeps only the two already-recorded
failure modes in play. It also means that if QS-03 ends in D-13 acceptance, that is known
before the inspector is built — so the inspector is built once, in its final structure.

Output: an explicit `modules/qmldir`, a `Variants` + per-screen `LazyLoader` fan-out in
`shell.qml`, and a dated evidence record — green or STOP, both fully written down.

This plan deliberately does **not** cross the D-13 one-way door. Plan 12-02 opens with the
blocking `checkpoint:decision` that gates it, reading this plan's evidence record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unified-design-token-pipeline/12-CONTEXT.md
@.planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md
@quickshell/.config/quickshell/shell.qml
@quickshell/.config/quickshell/modules/Probe.qml
</context>

<artifacts_this_phase_produces>
The complete set of symbols Phase 12 creates. Any name below is **new in this phase** and
must not be treated as pre-existing drift by a source-grounding pass.

**Shell functions (new):** `theme_engine_read_motion_scale`,
`theme_engine_render_motion_files`, `theme_engine_validate_motion_values` (all in
`theme-engine/.config/theme-engine/lib/motion.sh`); a repo-standard `check()` helper inside
the new `motion-lint` script.

**New repo-tracked files:** `theme-engine/.config/theme-engine/motion.json`;
`theme-engine/.config/theme-engine/lib/motion.sh`;
`hypr/.config/hypr/scripts/motion-lint`; `hypr/.config/hypr/scripts/motion-switch.sh`;
`hypr/.config/hypr/scripts/tests/motion-fixtures/` containing
`compliant-gtk4.css`, `poisoned-raw-gtk4.css`, `poisoned-dangling-gtk4.css`,
`compliant-hypr.conf`, `poisoned-raw-hypr.conf`, `poisoned-dangling-hypr.conf`,
`compliant-qml.qml`, `poisoned-raw-qml.qml`, `poisoned-dangling-qml.qml`;
`quickshell/.config/quickshell/modules/qmldir`;
`quickshell/.config/quickshell/modules/Colours.qml`;
`quickshell/.config/quickshell/modules/Motion.qml`;
`matugen/.config/matugen/templates/qml-palette.json`;
`.planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md`;
`.planning/phases/12-unified-design-token-pipeline/12-MOTION-VERDICT.md`.

**New rendered/runtime paths (never git-tracked):** `~/.local/state/theme/motion.json`,
`~/.local/state/theme/gtk-4.0-motion.css`, `~/.local/state/theme/hyprland-motion.conf`,
`~/.local/state/theme/palette.json`, `~/.local/state/theme/motion-scale`.

**New `contract.json` keys:** top-level `engine_owned_files` array; `files[]` entries
`motion.json` (format `json`), `gtk-4.0-motion.css` (format `css-vars`),
`hyprland-motion.conf` (format `hypr-motion`), `palette.json` (format `json`); two new
format tags `css-vars` and `hypr-motion` dispatched in `lib/contract.sh`.

**New CLI surfaces:** `motion-switch.sh [off|reduced|normal|lively] | --get | --list`;
`motion-lint [target-dir]` (path argument, D-28 self-test hook).

**New Hyprland variables and curve names:** `$motion_enabled`; beziers `motion-standard`,
`motion-emphasized-decelerate`, `motion-emphasized-accelerate`, `motion-linear`.

**New GTK4 CSS custom properties:** `--motion-duration-standard`,
`--motion-duration-emphasized-in`, `--motion-duration-emphasized-out`,
`--motion-easing-standard`, `--motion-easing-emphasized-decelerate`,
`--motion-easing-emphasized-accelerate`.

**New QML types, singletons and properties:** `Colours` (Quickshell `Singleton`) exposing
`primary`, `onPrimary`, `primaryContainer`, `onPrimaryContainer`, `secondary`,
`onSecondary`, `secondaryContainer`, `onSecondaryContainer`, `tertiary`, `onTertiary`,
`surface`, `onSurface`, `surfaceVariant`, `onSurfaceVariant`, `background`, `onBackground`,
`outline`, `error`, `onError`; `Motion` (Quickshell `Singleton`) exposing `motionEnabled`,
`motionScale`, `standardDuration`, `standardBezier`, `emphasizedInDuration`,
`emphasizedInBezier`, `emphasizedOutDuration`, `emphasizedOutBezier`; `shell.qml`'s
`root.probeActive`; `Probe.qml`'s `replayAll()` and `springMode`.

**New gate checks:** `theme-doctor` state-manifest gate, `hyprctl animations -j` readback,
motion-lint fold; `theme-parity` motion byte-identity assertion plus `css-vars`/`hypr-motion`
in its `enforce_emptiness` set; `theme-stress-test` live re-colour assertion;
`quickshell-doctor` D-14 documented-SKIP branch; `motion-lint` CHECK A / CHECK B / CHECK C.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bounded targeted QS-03 fix — explicit qmldir plus Variants and per-screen LazyLoader</name>

  <precondition>The always-on quickshell daemon is running under this session (`pgrep -x quickshell` returns a PID) and `~/.config/hypr/scripts/quickshell-doctor` currently reports 13 passed / 1 failed, exit 1, with the single failure being the per-screen surface creation check. If it reports anything else, halt and report — the baseline this task measures against is not the one recorded in 11-VERIFICATION.md.</precondition>

  <files>quickshell/.config/quickshell/modules/qmldir, quickshell/.config/quickshell/shell.qml, quickshell/.config/quickshell/modules/Probe.qml, .planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md</files>

  <read_first>
    - `.planning/phases/11-quickshell-viability-gate/11-QUICKSHELL-EVIDENCE.md` lines ~560-630 — the verbatim record of the three arrangements already tried and the TWO reproducible failure modes (FM1 intermittent `Probe is not a type` from an empty synthesised `modules/qmldir` with no `Scanning directory` log line; FM2 post-hotplug visibility break under the always-instantiated `visible:` binding). Do not re-derive these; do not retry a listed arrangement unchanged.
    - `quickshell/.config/quickshell/shell.qml` (61 lines, current state) — the `ShellRoot` with two `LazyLoader`s and two `GlobalShortcut`s that this task restructures.
    - `quickshell/.config/quickshell/modules/Probe.qml` (103 lines, current state) — note `implicitWidth: 360`, `WlrLayershell.namespace: "quickshell-probe"`, `HyprlandFocusGrab { windows: [ probeWindow ] }`, and `screenLabel` reading `probeWindow.screen`.
    - `quickshell/.config/quickshell/modules/ScreencopyProbe.qml` — the second local type that an explicit `qmldir` must also declare, since `shell.qml` imports it from the same directory.
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines ~384-470 — the exact per-screen surface creation check this task must turn green: it summons the probe, then asserts exactly one `quickshell-probe` address under `DP-1`, exactly one under the newly created headless output, and disjoint address sets.
    - `hypr/.config/hypr/scripts/quickshell-launch.sh` — the autostart path the proof must use (`exec quickshell -p "$CONFIG_DIR"` with logging to `~/.cache/quickshell.log`); the roadmap fixes the proof protocol to the always-on daemon, not a foreground run.
    - `.planning/phases/12-unified-design-token-pipeline/12-RESEARCH.md` finding 4 and Pitfall 7 — the `[CITED]` claim that an explicit `qmldir` disables the scanner, and the standing reminder that a web claim about this stack is not evidence until the binary agrees.
  </read_first>

  <behavior>
    - Arrangement A, clean start: 10 consecutive `quickshell-launch.sh` restarts all load `shell.qml` without `Probe is not a type`.
    - Arrangement A, `-v -v` trace: no `Scanning directory ".../modules"` line appears, because the checked-in `qmldir` replaced synthesis.
    - Arrangement A, single screen: pressing Super+Shift+G creates exactly one `quickshell-probe` surface on `DP-1`; pressing again destroys it.
    - Arrangement A, two screens: with a headless output added after startup, pressing Super+Shift+G creates exactly two surfaces with distinct addresses, one per output — and pressing again destroys both. This is FM2's exact failure point; the LazyLoader-per-screen shape exists to close it.
  </behavior>

  <action>
Restructure the per-screen fan-out under an explicit, countable budget. Stop conditions are
part of the task, not advice.

**Create `quickshell/.config/quickshell/modules/qmldir`** declaring every `.qml` type in
that directory so quickshell never runs its directory scanner there (D-12, targeting FM1's
documented root cause). It is a directory-listing `qmldir` reached through `shell.qml`'s
relative `import "modules"`, so it carries type entries only and no `module` header line.
Entries needed today: `Probe` at version `1.0` mapping to `Probe.qml`, and
`ScreencopyProbe` at version `1.0` mapping to `ScreencopyProbe.qml`. Before finalising the
line shape, run `quickshell -p ~/.config/quickshell -v -v` once against the CURRENT
unmodified tree and read the synthesised registration lines back out of the trace — match
that exact grammar rather than trusting the doc form. Later plans add `Colours` and `Motion`
entries here; an explicit `qmldir` disables scanning for the whole directory, so any type
missing from this file becomes unresolvable — state that consequence in a comment at the top
of the file.

**Arrangement A — `shell.qml`.** Replace the single `LazyLoader { id: probeLoader }` +
`Probe` pair with: a `property bool probeActive: false` on `root`; a
`Variants { model: Quickshell.screens }` whose delegate `Component` wraps a `LazyLoader`
whose `active` binds to `root.probeActive` and whose `Probe` child sets
`screen: modelData` and `onDismissRequested: root.probeActive = false`. `probeShortcut`'s
`onPressed` toggles `root.probeActive`. This is deliberately the LazyLoader-per-screen shape,
NOT the always-instantiated `visible:` binding that produced FM2 — record that distinction in
a comment. Leave `screencopyProbeLoader` and `screencopyProbeShortcut` structurally
untouched: this plan changes the probe's fan-out only, and keeping the second surface on the
old shape preserves a control.

In `Probe.qml`, the only change permitted here is whatever the `screen: modelData` binding
requires (the file already reads `probeWindow.screen` for `screenLabel`, so it may need
none). Do not restyle it and do not add content — D-32 reserves the inspector rewrite for
plan 12-06, and mixing the two is the compound problem this ordering exists to avoid.

**Proof for arrangement A, in this exact order.** (1) `pkill -x quickshell`, wait for a clean
`pgrep -x quickshell` miss, relaunch via `~/.config/hypr/scripts/quickshell-launch.sh` —
repeat 10 times, and count clean loads by grepping `~/.cache/quickshell.log` for
`is not a type` and for `Failed to load configuration`. (2) Run
`~/.config/hypr/scripts/quickshell-doctor` and capture its full output and summary line.
(3) Leave the daemon running and healthy.

**Budget (D-12, hard stop conditions — these are the task's contract).**
- Stage A: at most TWO arrangements. Arrangement A is above. Arrangement B, only if A fails,
  is the `Variants` rooted INSIDE `Probe.qml` (root type `Variants` rather than
  `PanelWindow`, `shell.qml` touching the local `Probe` type exactly once) combined with the
  same explicit `qmldir` — 11-QUICKSHELL-EVIDENCE.md records that arrangement tried WITHOUT
  a checked-in `qmldir`, so the combination is new. Each arrangement gets exactly ONE
  10-restart proof run.
- Stage B, only if both A and B fail: ONE further escape-hatch spike hypothesis of your
  choosing, hard stop 45 minutes.
- Overall hard stop: 90 minutes of implement-and-test wall time across Stage A, plus the
  45-minute Stage B allowance. On exhaustion, STOP. Do not try a third arrangement, do not
  iterate, do not open-endedly hunt a workaround — that is the house rule this budget
  encodes.

**Write `.planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md` either way.**
On the green branch it records the arrangement that worked, the 10-restart tally, the
`quickshell-doctor` summary line, and the `-v -v` trace evidence about the scanner. On the
STOP branch it records every arrangement tried, the exact failure signature of each, which
budget clause was hit, and — per the `flagged_assumptions` note above — whether the
`[CITED]` qmldir-disables-the-scanner claim held on this build. Use
`11-QUICKSHELL-EVIDENCE.md`'s record-why voice: what was tried, what was observed verbatim,
what it implies.
  </action>

  <reversibility rating="reversible">Both arrangements are confined to `shell.qml`, `Probe.qml` and one new `qmldir`; `git checkout` restores the Phase 11 state exactly, as 11-04 already demonstrated when it reverted its own attempt.</reversibility>

  <verify>
    <automated>~/.config/hypr/scripts/quickshell-doctor 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `quickshell/.config/quickshell/modules/qmldir` exists, is tracked by git, and declares both `Probe` and `ScreencopyProbe`: `grep -cE '^(Probe|ScreencopyProbe) ' quickshell/.config/quickshell/modules/qmldir` returns `2`.
    - GREEN branch: `~/.config/hypr/scripts/quickshell-doctor` prints `Summary: 14 passed, 0 failed` and exits 0, with the per-screen surface creation (QS-03) line reporting `[PASS]`.
    - GREEN branch: 10 consecutive `pkill -x quickshell` + `quickshell-launch.sh` cycles produce zero occurrences of `is not a type` and zero occurrences of `Failed to load configuration` in `~/.cache/quickshell.log` for that window.
    - GREEN branch: with a headless output present, `hyprctl layers -j` reports exactly one `quickshell-probe` namespace entry under `DP-1` and exactly one under the headless output name, with different `address` values.
    - STOP branch: `quickshell-doctor` still exits 1 with exactly one failure (the per-screen check) — no NEW check regressed — and `git status --porcelain quickshell/` shows only the intended `qmldir` addition plus whatever the chosen arrangement left in place, with nothing half-applied.
    - Either branch: `.planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md` exists, names every arrangement attempted, and states which budget clause ended the task.
    - Either branch: the daemon is left running (`pgrep -x quickshell` returns a PID) and a Super+Shift+G summon still produces a surface on `DP-1`.
  </acceptance_criteria>

  <done>Either QS-03's mechanical gate is green at 14/0 exit 0, or the bounded budget is exhausted with a complete written STOP record and no regression in the other 13 checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Session-restart re-proof against the autostart daemon</name>

  <what-built>
    Task 1 restructured the Quickshell shell root to fan a probe surface out over every
    `Quickshell.screens` entry, using an explicit `modules/qmldir` plus a `Variants` +
    per-screen `LazyLoader`, and proved it mechanically across 10 in-session daemon
    restarts. The roadmap's criterion 6 fixes the proof protocol as re-proven "against the
    always-on autostart daemon across a real session restart, not only in a foreground test
    run" — an in-session `pkill`/relaunch loop is not that. Only a human can log out and back
    in; an executor doing it would kill its own session.
  </what-built>

  <how-to-verify>
    1. Log out of the Hyprland session completely and log back in (do not merely restart the
       quickshell process — the point is a real session restart, so the autostart path in
       `hypr/.config/hypr/config/autostart.conf` re-runs `quickshell-launch.sh` from scratch).
    2. Open a terminal and run: `~/.config/hypr/scripts/quickshell-doctor`
    3. Report back the final `Summary: N passed, M failed` line and, if any check failed, the
       full `[FAIL]` line(s).
    4. Also run `tail -40 ~/.cache/quickshell.log` and report whether it contains
       `is not a type` or `Failed to load configuration` anywhere after the login timestamp.
    5. Press Super+Shift+G once and confirm a probe panel appears; press it again and confirm
       it disappears. Report yes/no.

    Expected on the GREEN branch: `Summary: 14 passed, 0 failed`, a clean log, and the
    summon/dismiss toggle working.

    If Task 1 ended on the STOP branch instead, the expected result is
    `Summary: 13 passed, 1 failed` with the single failure being the per-screen surface
    creation check — confirm that is exactly what you see, and that nothing else regressed.
  </how-to-verify>

  <resume-signal>Paste the `Summary:` line, the log finding, and the summon/dismiss result. Type "approved" if it matches the expected outcome for whichever branch Task 1 ended on, or describe the discrepancy.</resume-signal>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML config tree -> quickshell daemon | `shell.qml` and `modules/*.qml` are parsed and executed by the always-on daemon; a malformed document takes the shell down for the whole session |
| gate script -> live compositor topology | `quickshell-doctor` creates and removes a headless output on the running compositor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Denial of Service | `shell.qml` / `modules/qmldir` | high | mitigate | An explicit `qmldir` disables directory scanning for `modules/`, so any type omitted from it becomes unresolvable and the shell fails to load. Task 1 asserts both existing types are declared (`grep -c` == 2) and records the consequence in a file-header comment so later plans adding `Colours`/`Motion` cannot forget. The 10-restart proof is exactly the check that catches an unresolvable type. |
| T-12-02 | Denial of Service | `quickshell-doctor` headless-output group | medium | accept | The gate already arms its removal trap before the create call, refuses to touch a pre-existing `HEADLESS*` output, and restores topology on EXIT/INT/TERM. This plan adds no new mutation; the existing self-restoring design is the mitigation and needs no change. |
| T-12-03 | Tampering | `.planning/*` planning artifacts | medium | mitigate | The D-13 acceptance branch rewrites `REQUIREMENTS.md`, `PROJECT.md` and `ROADMAP.md`. No task in this plan edits those files; plan 12-02's opening blocking `checkpoint:decision` is the gate, and its remaining tasks execute only on an explicit `accept-permanent` selection. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs anywhere in Phase 12 — `12-RESEARCH.md` §Package Legitimacy Audit records zero new packages, every dependency already installed and `pacman -Q`-verified. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `~/.config/hypr/scripts/quickshell-doctor` runs to completion and its `Summary:` line is recorded in the SUMMARY.
- `git status --porcelain quickshell/` shows only intended additions/modifications.
- `.planning/phases/12-unified-design-token-pipeline/12-QS03-EVIDENCE.md` exists and is committed.
- The branch reached (GREEN or STOP) is stated explicitly in the SUMMARY — plan 12-02's decision checkpoint reads it.
</verification>

<success_criteria>
QS-03 is either mechanically closed at 14 passed / 0 failed exit 0 with session-restart
evidence, or the bounded budget is exhausted with a complete written record of every
arrangement tried — with the one-way-door decision left to plan 12-02's opening checkpoint.
</success_criteria>

<output>
Create `.planning/phases/12-unified-design-token-pipeline/12-01-SUMMARY.md` when done.
State the branch reached (GREEN or STOP) in the first line and record the
`quickshell-doctor` summary line verbatim — plan 12-02's decision checkpoint reads both.
</output>
