---
phase: 12-unified-design-token-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - hypr/.config/hypr/scripts/motion-switch.sh
  - theme-engine/.config/theme-engine/lib/gtk.sh
  - hypr/.config/hypr/hyprland.conf
  - hypr/.config/hypr/config/animations.conf
  - stow.sh
  - theme-engine/.config/theme-engine/theme-doctor
  - theme-engine/.config/theme-engine/theme-parity
autonomous: true
requirements: [TOKEN-03, TOKEN-05]

must_haves:
  truths:
    - "A motion-scale preset is selected through a CLI and a single state file; changing it and running `theme-apply` once changes the emitted values for all three targets in that one run. (TOKEN-05, D-06/D-07)"
    - "`off` disables at the toolkit level rather than shrinking durations: Hyprland's `animations { enabled }` resolves false, and `org.gnome.desktop.interface enable-animations` is set false; every other preset sets both true. (D-08/D-20)"
    - "The generated Hyprland fragment is sourced BEFORE `config/animations.conf` in `hyprland.conf`, so `$motion_enabled` is defined before `animations.conf` reads it and emitted curves are registered before any future `animation =` line references them. (D-22)"
    - "`stow.sh` seeds both the `motion-scale` state file and the three rendered motion files when absent, generating the latter by invoking `motion.sh` rather than writing a hand-authored stub — a missing sourced file and an undefined `$motion_enabled` are both hard Hyprland config errors, so a fresh install must never reach first login without them. (D-30)"
    - "`hyprctl animations -j` confirms every emitted `motion-*` curve holds its generated control points rather than a silently substituted default, and `theme-doctor` FAILs if any emitted curve is absent or altered. (D-02b, criterion 2)"
    - "Emitted curve names and the 12 hand-authored `animations.conf` curve names coexist in one registry without collision: `hyprctl animations -j` lists both sets, every emitted name carries the `motion-` prefix, and no name appears twice. (edge: TOKEN-03/adjacency)"
    - "The boundary between the smallest non-zero emitted duration and zero is never crossed: every emitted duration is at or above the 40ms floor, and `off` is expressed as a toolkit disable rather than as a zero duration — `speed = 0` is a hard Hyprland parse error, so this is a correctness boundary, not a perceptual one. (edge: TOKEN-05/adjacency)"
    - "An absent `motion-scale` file or an unrecognised value resolves to `normal` through the reader's closed `case`, and `stow.sh` seeds `normal` when absent so a fresh install never leaves `$motion_enabled` undefined. (edge: TOKEN-05/empty)"
    - "The four presets are a closed set and the mapping from preset to output is total and deterministic: the same preset always yields byte-identical emitted files, and where two presets would collapse a token to the same floored value the emitter WARNs rather than changing its output. (edge: TOKEN-05/ordering)"
    - "The motion files are byte-identical across all 22 palettes, both modes, and both the static-preset and materialyou render branches at a fixed motion-scale — proving `motion.sh` is wired into both branches and that no motion token has accidentally been made to depend on a colour or on light/dark mode. (D-31)"
    - "At motion-scale `off` the absence of animation is the intended state: `theme-doctor` reports it as such and does not treat `enabled` resolving false as a defect. (ui: empty/E7)"
    - "At motion-scale `normal` a window open or close animates with the emitted `motion-*` curves, with the curve values confirmed by `hyprctl animations -j` readback. (ui: populated/E7)"
    - statement: "Hyprland may silently substitute a default curve for a malformed emitted bezier with no visible failure; every emitted curve must be confirmed to hold its generated values. (ui: error/E7)"
      verification: backstop
  artifacts:
    - hypr/.config/hypr/scripts/motion-switch.sh
  key_links:
    - "motion-switch.sh -> ~/.local/state/theme/motion-scale -> theme_engine_read_motion_scale -> emitted values in all three targets"
    - "hyprland.conf source order: hyprland-motion.conf BEFORE config/animations.conf -> $motion_enabled defined before animations.conf's `enabled =` reads it"
    - "gtk.sh GSettings block -> org.gnome.desktop.interface enable-animations -> third-party GTK/libadwaita apps via the portal"
    - "stow.sh seed-when-absent -> motion-scale + three rendered motion files -> Hyprland starts at all on a fresh install"
    - "theme-doctor hyprctl animations -j readback -> emitted control points (criterion 2's proof)"
  prohibitions:
    - requirement_id: TOKEN-05
      category: accessibility
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship a reduced-motion setting that only affects repo-authored surfaces while leaving the system-wide `org.gnome.desktop.interface enable-animations` signal untouched. A setting labelled reduced-motion that third-party GTK and libadwaita applications do not honour is a false accessibility claim, not a partial implementation — the difference between 'our surfaces animate less' and reduced motion actually working."
---

<objective>
Give the motion pipeline its runtime axis and its compositor wiring: a named-preset
motion-scale with a CLI, the GSettings signal that makes reduced-motion real beyond this
repo's own surfaces, the Hyprland source-order wiring D-22 fixes, fresh-install seeding, and
the two readback gates that prove emitted curves survive into the compositor unchanged.

Purpose: D-32 places the Hyprland target and its readback immediately after the foundation.
This plan is what makes criterion 2 provable end to end (curves emit AND `hyprctl` reads back
their generated control points) and what makes criterion 4's mechanism exist before the two
surfaces that demonstrate it are built.

Output: `motion-switch.sh`, a `$motion_enabled`-driven `animations.conf`, a correctly-ordered
`hyprland.conf`, fresh-install seeding, D-02b's readback check, and D-31's byte-identity
assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unified-design-token-pipeline/12-CONTEXT.md
@.planning/phases/12-unified-design-token-pipeline/12-03-SUMMARY.md
@theme-engine/.config/theme-engine/lib/motion.sh
@theme-engine/.config/theme-engine/motion.json
</context>

<artifacts_this_phase_produces>
See `12-01-PLAN.md` §"Artifacts this phase produces" for the complete phase-level list of
new symbols, files, contract keys, CLI surfaces, Hyprland variables, GTK4 custom properties,
QML types and gate checks. Nothing in that list is pre-existing; a source-grounding pass must
not treat any of those names as drift.

This plan specifically creates: `hypr/.config/hypr/scripts/motion-switch.sh` (with subcommands
`off`, `reduced`, `normal`, `lively`, `--get`, `--list`); the runtime path
`~/.local/state/theme/motion-scale`; the `theme-doctor` `hyprctl animations -j` readback check
and its motion-scale-`off` awareness; and the `theme-parity` motion byte-identity assertion.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: motion-scale axis — CLI, state file, and the GSettings signal that makes reduced-motion real</name>

  <precondition>`theme_engine_read_motion_scale` and `theme_engine_render_motion_files` exist in `theme-engine/.config/theme-engine/lib/motion.sh` and a `theme-apply` run emits all three motion targets (plan 12-03 Task 1). If `~/.local/state/theme/motion.json` is absent, halt — this task has nothing to switch.</precondition>

  <files>hypr/.config/hypr/scripts/motion-switch.sh, theme-engine/.config/theme-engine/lib/gtk.sh</files>

  <read_first>
    - `hypr/.config/hypr/scripts/font-switch.sh` and `hypr/.config/hypr/scripts/icon-theme-switch.sh` — the two existing theme-orthogonal-axis switch entrypoints, for naming and shape. Note both are thin launchers over an fzf picker; `motion-switch.sh` deliberately is NOT that, because D-07 ships this axis as **state file + CLI only** this phase and defers the graphical picker.
    - `theme-engine/.config/theme-engine/theme-apply` lines 40-62 — the argument-validation precedent: reject an unknown value outright before it is interpolated anywhere, notify, and exit non-zero leaving the desktop unchanged. `motion-switch.sh` follows this exactly.
    - `theme-engine/.config/theme-engine/lib/motion.sh` — `theme_engine_read_motion_scale`'s closed `case` and `MOTION_STATE_FILE`; the CLI writes the same file the reader reads, and must validate against the same closed set rather than a second copy of it.
    - `theme-engine/.config/theme-engine/motion.json` — the `scales` table is the source of truth for which preset names exist and what each does; the CLI's `--list` should read it rather than hardcode a fifth copy of the name set.
    - `theme-engine/.config/theme-engine/lib/gtk.sh` lines 30-45 and 250-260 — the existing GSettings block that already sets `color-scheme`, `gtk-theme` (twice, with the deliberate empty-then-real reassert), `accent-color` and `icon-theme`, each `2>/dev/null || true`-guarded. The new key joins this block; it does not get a second writer.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"Copywriting Contract" — preset display names are Title Case wherever presented to a human, while the state file stays lowercase.
  </read_first>

  <behavior>
    - `motion-switch.sh normal` writes `normal` to the state file and triggers one re-render.
    - `motion-switch.sh bogus` writes nothing, prints a usage error naming the four valid presets, and exits non-zero.
    - `motion-switch.sh --get` prints the current preset, or `normal` when the state file is absent.
    - `motion-switch.sh --list` prints the four presets in Title Case with their multipliers.
    - After `motion-switch.sh off` + one `theme-apply` run, `gsettings get org.gnome.desktop.interface enable-animations` reports false.
    - After `motion-switch.sh reduced` + one `theme-apply` run, that same key reports true, and emitted durations are half the `normal` values above the floor.
  </behavior>

  <action>
**Create `hypr/.config/hypr/scripts/motion-switch.sh`** as the CLI half of D-07's
"state file + CLI only" axis. It accepts one preset name, or `--get`, or `--list`, or `--help`.

Validate the preset against `motion.json`'s `scales` keys — read them, do not hardcode a
second copy — and reject anything else with a usage message naming the valid presets and a
non-zero exit, writing nothing. This is the same ASVS V5 posture `theme-apply` already takes
with palette names, and it matters more here because the value ends up inside a file the
compositor parses.

On a valid preset: write the lowercase name to `~/.local/state/theme/motion-scale` using the
temp-file-plus-`mv` idiom `commit.sh` already uses for `current-theme`, so a concurrent reader
sees the old value or the new one and never a truncated file. Then trigger exactly one
re-render by invoking `theme-apply` with the currently active theme (read from
`~/.local/state/theme/current-theme`, defaulting the way `theme-init.sh` already does) — one
entrypoint, per TOKEN-05's "driven through `theme-apply`'s existing single entrypoint". Do not
write any rendered file directly from this script; the axis is a state input to the existing
pipeline, not a second writer.

`--list` prints the four presets in Title Case with their multipliers and a one-line note that
`off` disables at the toolkit level rather than scaling. `--get` prints the current lowercase
value, defaulting to `normal` when the file is absent.

Make it executable and register it in `stow.sh`'s existing package set if the `hypr` package
needs no change (it will not — the script lands inside an already-stowed directory), but do
confirm the file lands at `~/.config/hypr/scripts/motion-switch.sh` after a `stow.sh` run.

Do **not** add a Super-key menu entry or a walker picker. D-07 defers both, deliberately, to
keep a GTK surface out of a phase whose subject is the token pipeline and to avoid adding a
surface Phase 13 would then have to retrofit for motion.

**Extend `gtk.sh`'s existing GSettings block with `enable-animations`** (D-20). Read the
current motion-scale through `theme_engine_read_motion_scale` and set
`org.gnome.desktop.interface enable-animations` to false for `off` and true for every other
preset — the mapping is total over the closed preset set, so no default branch can silently
leave the key stale. Use the same `2>/dev/null || true` guard the five sibling lines already
carry, and place it inside the existing block rather than creating a second GSettings writer
(the Pitfall-6 discipline `font.sh`'s header already records).

State the reason in a comment, because it is the point of the whole line: TOKEN-05 says "across
every surface", and third-party GTK and libadwaita applications read this key live through the
portal without a restart. Without it, this axis would only affect the handful of surfaces this
repo authors — which is a false accessibility claim rather than a partial implementation.
  </action>

  <reversibility rating="reversible">One new standalone script plus one line in an existing GSettings block; both revert cleanly and neither changes an existing code path's behaviour when the axis sits at `normal`.</reversibility>

  <verify>
    <automated>~/.config/hypr/scripts/motion-switch.sh --list && ~/.config/hypr/scripts/motion-switch.sh --get && ~/.config/hypr/scripts/motion-switch.sh bogus; test $? -ne 0</automated>
  </verify>

  <acceptance_criteria>
    - `~/.config/hypr/scripts/motion-switch.sh bogus` exits non-zero, prints a usage error, and leaves `~/.local/state/theme/motion-scale` byte-unchanged (compare before/after with `cmp`).
    - `~/.config/hypr/scripts/motion-switch.sh --list` prints exactly four presets and its output contains `Off`, `Reduced`, `Normal` and `Lively` in Title Case.
    - `~/.config/hypr/scripts/motion-switch.sh off` then `gsettings get org.gnome.desktop.interface enable-animations` prints `false`; `~/.config/hypr/scripts/motion-switch.sh normal` then the same command prints `true`.
    - At `reduced`, an emitted duration above the floor is exactly half its `normal` value: compare `jq -r '.semantic["emphasized-in"].duration_ms' ~/.local/state/theme/motion.json` across the two presets and confirm the ratio.
    - At `off`, `jq -e '.motion_enabled == false' ~/.local/state/theme/motion.json` exits 0 and `grep -c '^\$motion_enabled = false' ~/.local/state/theme/hyprland-motion.conf` returns `1`.
    - The reader's default holds: with `~/.local/state/theme/motion-scale` temporarily moved aside, `~/.config/hypr/scripts/motion-switch.sh --get` prints `normal`. Restore the file afterwards.
    - `bash -n hypr/.config/hypr/scripts/motion-switch.sh` exits 0 and `shellcheck` reports no error-level finding.
    - `~/.config/theme-engine/theme-doctor` exits 0 with the axis left at `normal`.
  </acceptance_criteria>

  <done>A named preset is selectable from a CLI, drives all three emitted targets through one `theme-apply` run, and carries the system-wide GSettings signal that makes reduced-motion real outside this repo.</done>
</task>

<task type="auto">
  <name>Task 2: Hyprland source-order wiring and fresh-install seeding</name>

  <files>hypr/.config/hypr/hyprland.conf, hypr/.config/hypr/config/animations.conf, stow.sh</files>

  <read_first>
    - `hypr/.config/hypr/hyprland.conf` lines 6-27 — the seven `source =` lines (line 10 is `config/animations.conf`), the theme-colours `source =` at line 16 with its D-06 comment, and `general { }` at line 19 consuming `$primary`/`$secondary`/`$tertiary`/`$outline` from that line-16 source. That define-before-use ordering is the exact precedent this task follows.
    - `hypr/.config/hypr/config/animations.conf` lines 1-20 — `animations {` at line 6, `enabled = true` at line 7, then the 12 hand-authored `bezier =` lines. Only line 7 changes; D-04 leaves the 12 beziers and the 14 `animation =` lines untouched.
    - `.planning/phases/12-unified-design-token-pipeline/12-CONTEXT.md` `<code_context>` verified-facts table — eight binary-verified Hyprland config semantics. The load-bearing ones here: `source =` inside a category block FAILS; a top-level `source =` of a fragment wrapping its own `animations { }` is CLEAN; `enabled = $var` with the var defined earlier is CLEAN; `enabled = $undefined_var` FAILS with "cannot parse as an int"; `source =` of a missing file FAILS with a globbing error. Do not re-derive these.
    - `stow.sh` lines 95-140 — three seed-when-absent examples (`current-waybar-layout`, `gaming-mode`, `waybar-visibility.css`), each `[[ -f ... ]] ||` guarded with a comment explaining why unconditional would clobber live state; the `mkdir -p "$HOME/.local/state/theme"` at line ~119; and the first-boot `theme-apply` seed at line ~135 which is `|| true`-guarded and therefore cannot be relied on.
    - `.planning/phases/12-unified-design-token-pipeline/12-RESEARCH.md` §"Pattern 4" — the binary-verified fragment and source-order shape.
  </read_first>

  <behavior>
    - `Hyprland --verify-config` passes against the live config tree with the motion fragment present.
    - `Hyprland --verify-config` FAILS with a globbing error against a throwaway copy of the config tree in which the motion fragment is absent — proving the seed is load-bearing, not decorative.
    - A fresh-install simulation (state dir emptied, `stow.sh` seed logic run) produces a `motion-scale` file and all three rendered motion files before any Hyprland start.
    - Re-running the seed logic with a live `motion-scale` value present does not overwrite it.
  </behavior>

  <action>
**`hyprland.conf`:** insert `source = ~/.local/state/theme/hyprland-motion.conf` **before**
line 10's `config/animations.conf` — between `autostart.conf` and `animations.conf`. Not
beside the colours at line 16. D-22 forces this: curves must be registered before any
`animation =` references them, and `animations.conf` reads `$motion_enabled`, which this
fragment defines. Phase 12 does not strictly need the ordering (its curves are new names
nobody references yet), but placing it correctly now avoids a Phase 13 reshuffle. Give the
line a comment in the style of line 15's, naming D-22 and stating the two ordering reasons.

**`animations.conf`:** change line 7's `enabled = true` to `enabled = $motion_enabled`. Nothing
else in the file changes — the 12 `bezier =` lines and the 14 `animation =` lines stay
hand-authored per D-04. Add a short comment noting that the variable is defined by the
generated fragment sourced immediately before this file, and that this block deliberately owns
`enabled` while the generated block does not, because whichever `animations { }` block comes
last would otherwise clobber it.

**`stow.sh`:** add two seeds beside the existing three, using the same `[[ -f ... ]] ||` guard
and the same comment discipline (state what breaks without it, and why unconditional is wrong).

1. `motion-scale` — a plain single-value seed of `normal`, mirroring the `gaming-mode` shape at
   line ~110.
2. The three rendered motion files — seeded by **invoking `motion.sh`** (source
   `theme-engine/.config/theme-engine/lib/motion.sh`, set `STATE_DIR`, call
   `theme_engine_render_motion_files` into a tmp tree and copy the three files across), not by
   writing a hand-authored stub. D-30's reason is one source of truth: a stub goes stale the
   moment Phase 13 points assignments at generated curves. Guard it so a failure prints a
   warning and continues rather than aborting `stow.sh` — but say in the comment that this is
   the one seed whose absence means the compositor does not start at all, because a missing
   sourced file and an undefined `$motion_enabled` are both hard config errors, debugged from a
   TTY. The existing `theme-apply` seed at line ~135 is `|| true`-guarded and therefore cannot
   be the thing this depends on.

Place both seeds before line ~135's `theme-apply` call, so the ordering reads as
"guarantee the floor, then try the full render".
  </action>

  <reversibility rating="costly">Reverting `animations.conf`'s `enabled = $motion_enabled` back to a literal is trivial, but any session started against a config tree missing the sourced fragment fails to start Hyprland at all — the cost is paid at boot, not at edit time, which is exactly why the seeding is in the same task as the wiring.</reversibility>

  <verify>
    <automated>Hyprland --verify-config 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Source order is correct: `grep -n 'source = ' hypr/.config/hypr/hyprland.conf` shows the `hyprland-motion.conf` line at a lower line number than the `config/animations.conf` line.
    - `grep -c '^\s*enabled = \$motion_enabled' hypr/.config/hypr/config/animations.conf` returns `1`, and `grep -c '^\s*bezier = ' hypr/.config/hypr/config/animations.conf` still returns `12` (D-04 fence intact).
    - `grep -c '^\s*animation = ' hypr/.config/hypr/config/animations.conf` returns `14` — unchanged.
    - `Hyprland --verify-config` against the live tree exits 0.
    - The seed is proven load-bearing: copy the config tree to a throwaway directory, delete the motion fragment it sources, run `Hyprland --verify-config -c <throwaway>/hyprland.conf`, and confirm it fails with a globbing error. Non-destructive — never point `--verify-config` at a mutated live path.
    - The seed is proven idempotent: write a sentinel preset into `~/.local/state/theme/motion-scale`, re-run `stow.sh`'s seed logic, and confirm the file still holds the sentinel.
    - The seed is proven to fire: move all three rendered motion files and `motion-scale` aside, run `stow.sh`'s seed logic, and confirm all four are recreated with valid content (`jq -e . ~/.local/state/theme/motion.json` exits 0 and the Hyprland fragment passes `--verify-config` via a throwaway sourcing conf). Restore afterwards.
    - `bash -n stow.sh` exits 0 and `shellcheck stow.sh` reports no new error-level finding versus the pre-edit baseline.
    - `~/.config/theme-engine/theme-doctor` exits 0.
  </acceptance_criteria>

  <done>The compositor reads the generated fragment in the right order, `animations.conf` takes its enable state from the axis, and a fresh install cannot reach first login without the files Hyprland hard-requires.</done>
</task>

<task type="auto">
  <name>Task 3: hyprctl readback, the speed-unit reconciliation, and D-31's byte-identity assertion</name>

  <files>theme-engine/.config/theme-engine/theme-doctor, theme-engine/.config/theme-engine/theme-parity</files>

  <read_first>
    - `theme-engine/.config/theme-engine/theme-doctor` lines 1-35 (`STATE_DIR`, `source lib/contract.sh`, `check()`) and lines ~474-496 (the `waybar-design-lint` guarded fold, the shape any new externally-dependent check copies).
    - `.planning/phases/12-unified-design-token-pipeline/12-CONTEXT.md` `<code_context>` — `hyprctl animations -j` is well-formed and `jq`-parses cleanly on 0.56.0 (unlike `hyprctl binds -j`, which Phase 11 D-14 abandoned as field-misaligned — record that the `-j` distrust is query-specific, not blanket). Element `[1]` carries `name, X0, Y0, X1, Y1`; element `[0]` lists all 35 animation slots with `overridden`, `bezier`, `speed`, `style`, `enabled`. Overshoot survives intact: control points outside the unit interval are not clamped.
    - `.planning/phases/12-unified-design-token-pipeline/12-RESEARCH.md` §"Pitfall 2" and §"Code Examples" — the verified `speed = 0` / `speed = -1` hard-error result, the clean fractional-speed result, and the `[CITED: wiki.hypr.land]` decisecond claim that is **not** binary-verified.
    - `theme-engine/.config/theme-engine/theme-parity` lines 26-115 (the palette enumeration, `contract_files`, and the refusal to proceed on an empty contract list) and lines 143-175 (the mode-fixture assertions, for the shape a new fixed-value assertion should copy).
    - `theme-engine/.config/theme-engine/theme-parity` lines 176-222 — Layers 1 and 2, so the new byte-identity assertion is placed as a distinct layer rather than folded into a name-set comparison it is not.
  </read_first>

  <behavior>
    - With the desktop at `normal`, `theme-doctor`'s readback check PASSes and names the number of curves confirmed.
    - Corrupting one control point in the rendered fragment and reloading makes the readback check FAIL, naming the curve.
    - With the axis at `off`, the readback check still PASSes on curve values and does not report the disabled state as a defect.
    - `theme-parity` reports the three motion files byte-identical across every palette, both modes, and both render branches.
    - Changing the motion-scale between two `theme-parity` runs changes the recorded hash but keeps it identical across palettes within a run.
  </behavior>

  <action>
**Add the D-02b readback check to `theme-doctor`.** For every `bezier = motion-<name>` line in
`~/.local/state/theme/hyprland-motion.conf`, find the matching entry in element `[1]` of
`hyprctl animations -j` and assert the four control points match the emitted values. Emit one
`check()` line reporting how many curves were confirmed, and on failure name the specific curve
and the emitted-versus-readback values — a bare count mismatch is unactionable.

This is the only guard that catches a value Hyprland accepts and then substitutes. Record in a
comment what it is NOT for, so it does not get "simplified" into a presence check later: a
*missing* bezier is not silent — Hyprland errors loudly with "no such bezier" at config parse
(binary-verified). The narrow risk this closes is malformed control-point **values**, not
missing names.

Guard it the way the file already guards externally-dependent checks: degrade to a named
`[SKIP]` if `hyprctl` or `jq` is unavailable, or if no compositor is running (the container
gate has no Hyprland), never to a `[FAIL]`.

**Make the check motion-scale-aware.** At `off`, `hyprctl animations -j` element `[0]` will
report animations disabled. That is the intended state, not a defect — the check asserts curve
*values* only and must not fold `enabled` into its verdict. Emit an informational line naming
the active preset so a human reading the output at `off` is not left wondering.

**Record the speed-unit reconciliation, in a comment beside the readback check.** State
plainly: `speed` is a float that reads back with two decimals where the config wrote an
integer; `speed = 0` and `speed = -1` are hard `invalid speed` parse errors and fractional
values parse cleanly (both binary-verified this phase); the decisecond interpretation
(1 unit = 100ms) is `[CITED: wiki.hypr.land]` and was **not** independently binary-verified.
Then state why that residual gap is not this phase's problem and cannot be deferred silently:
**Phase 12 emits no `speed` value at all** — D-04 fences it to curves only, and the 14
`animation =` assignment lines stay hand-authored — so no ms-to-speed conversion exists in this
codebase yet. Phase 13, which retargets those assignment lines, is where the conversion gets
written and is therefore where the unit must be confirmed against the binary before use. Add
the same note to the SUMMARY so it reaches Phase 13's planner rather than living only in a
comment.

**Add D-31's byte-identity assertion to `theme-parity`** as its own layer. With the
motion-scale pinned to a fixed value for the duration of the run, hash each of the three
rendered motion files in every palette's render directory — all 22 palettes, both modes, and
both the static-preset and materialyou branches — and assert one distinct hash per file across
the whole set. A hash compare, not a structural walk: it is the strongest and cheapest option
simultaneously.

Say in a comment what the three things it proves are, because two are not available anywhere
else: that `motion.sh` is wired into *both* render branches; that each emitted file is
well-formed; and — the one nothing else guards — that no motion token has accidentally been
made to depend on a colour or on light/dark mode, which is the premise D-01's whole design
rests on. On failure, name which palette or mode diverged and print the differing hashes.
  </action>

  <reversibility rating="reversible">Both are report-only gate additions; removing either restores the previous gate behaviour with no effect on rendered output.</reversibility>

  <verify>
    <automated>~/.config/theme-engine/theme-doctor 2>&1 | grep -i 'readback\|motion' ; ~/.config/theme-engine/theme-parity 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `~/.config/theme-engine/theme-doctor` exits 0 and its output contains a `[PASS]` line for the `hyprctl animations` readback naming the number of curves confirmed.
    - The readback is proven to fail before it is trusted: copy the rendered fragment aside, alter one control point in `~/.local/state/theme/hyprland-motion.conf`, run `hyprctl reload`, run `theme-doctor`, and confirm the readback line reports `[FAIL]` and names that curve. Restore the file and `hyprctl reload` afterwards, and confirm `theme-doctor` returns to exit 0.
    - Both curve sets coexist without collision: `hyprctl animations -j | jq -r '.[1][].name' | sort | uniq -d | wc -l` returns `0`, and `hyprctl animations -j | jq -r '.[1][].name' | grep -c '^motion-'` returns the number of `easings` entries in `motion.json`.
    - At motion-scale `off`, `theme-doctor` still exits 0 and the readback line still reports `[PASS]` — the disabled state is not reported as a defect. Return the axis to `normal` afterwards.
    - `~/.config/theme-engine/theme-parity` exits 0 and its output contains a `[PASS]` line for motion byte-identity naming the number of render directories compared.
    - The byte-identity assertion is proven to fail: point one palette's rendered `gtk-4.0-motion.css` at a one-character-altered copy inside a throwaway parity run and confirm the assertion FAILs naming that palette. Use a throwaway render tree; never mutate `~/.local/state/theme/` for this.
    - Guarded degradation holds: with `PATH` temporarily stripped of `hyprctl`, `theme-doctor` prints a named `[SKIP]` for the readback and still exits 0.
    - `bash -n` and `shellcheck` are clean on both edited scripts.
  </acceptance_criteria>

  <done>Criterion 2's readback proof exists and has been demonstrated failing; D-31's byte-identity assertion proves motion is theme-orthogonal across both render branches; and the residual `speed`-unit gap is recorded as Phase 13's precondition rather than silently inherited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `motion-switch.sh` argv -> `motion-scale` state file -> generated compositor config | A CLI argument ultimately selects values inside a file Hyprland parses at session start |
| `stow.sh` seed -> first-boot Hyprland config | A fresh install's very first compositor start depends on files this task seeds |
| gate scripts -> live compositor (`hyprctl reload`) | The readback proof deliberately reloads the running compositor's config during its fail-first demonstration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Denial of Service | fresh-install Hyprland start with a missing sourced fragment or undefined `$motion_enabled` | high | mitigate | `stow.sh` seeds both the state file and the three rendered files before the `\|\| true`-guarded `theme-apply` call, generating the rendered files by invoking `motion.sh` rather than a stub. Task 2's acceptance criteria prove the failure exists (a throwaway tree without the fragment fails `--verify-config` with a globbing error) and that the seed closes it. |
| T-12-13 | Elevation of Privilege | `motion-switch.sh` argv reaching a file the compositor parses | medium | mitigate | The argument is validated against `motion.json`'s `scales` keys and rejected outright before any write or interpolation, mirroring `theme-apply`'s palette-name validation. The state file is written via temp-plus-`mv`, so no reader ever sees a partial value. |
| T-12-14 | Tampering | readback fail-first demonstration mutating the live rendered fragment | medium | mitigate | The demonstration copies the fragment aside first, and the acceptance criteria require restoring it plus a confirming `hyprctl reload` and a return to `theme-doctor` exit 0. The byte-identity fail-first demonstration is required to use a throwaway render tree and never `~/.local/state/theme/`. |
| T-12-15 | Spoofing | `enable-animations` set false without the rest of the axis following | low | mitigate | The preset-to-key mapping is total over the closed four-name set and lives inside `gtk.sh`'s single existing GSettings block, so there is no second writer that could set the key independently of the emitted values. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs anywhere in Phase 12 — `12-RESEARCH.md` §Package Legitimacy Audit records zero new packages, every dependency already installed and `pacman -Q`-verified. |
</threat_model>

<verification>
- `Hyprland --verify-config` exits 0 against the live tree, and fails as expected against a throwaway tree missing the fragment.
- `~/.config/theme-engine/theme-doctor` exits 0; the readback check is demonstrated failing and recovering.
- `~/.config/theme-engine/theme-parity` exits 0; the byte-identity assertion is demonstrated failing on a throwaway tree.
- `gsettings get org.gnome.desktop.interface enable-animations` tracks the preset in both directions.
- The axis is left at `normal` and the state dir restored before the plan closes.
</verification>

<success_criteria>
The motion-scale axis exists as a validated CLI over one state file, drives all three targets
and the system-wide GSettings signal through a single `theme-apply` run, the compositor reads
the generated fragment in the order D-22 requires, a fresh install cannot start without the
files Hyprland hard-requires, and both new gates have been proven to fail before being trusted
to pass.
</success_criteria>

<output>
Create `.planning/phases/12-unified-design-token-pipeline/12-04-SUMMARY.md` when done.
Carry the `speed`-unit reconciliation note forward explicitly — Phase 13 owns the conversion.
</output>
