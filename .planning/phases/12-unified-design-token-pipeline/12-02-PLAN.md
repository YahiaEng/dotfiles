---
phase: 12-unified-design-token-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - hypr/.config/hypr/scripts/quickshell-doctor
autonomous: false
requirements: [QS-03]

must_haves:
  truths:
    - "This plan opens with the blocking D-13 decision checkpoint. Its remaining tasks execute ONLY IF that checkpoint returns `accept-permanent`; on `close-qs03` they are documented no-ops and the SUMMARY records why."
    - "On the acceptance branch, QS-03 appears in REQUIREMENTS.md's Out of Scope with a reason, in PROJECT.md's Out of Scope with a reason, and ROADMAP criterion 6 is amended in place with an amendment note naming this phase — the same D-15 amendment precedent Phase 11 set, not a silent edit."
    - "On the acceptance branch, `quickshell-doctor` reports the per-screen surface creation check as a documented SKIP that prints its reason AND an evidence pointer, and the script exits 0 — a gate everyone knows is permanently red is a gate nobody reads (D-14)."
    - "On the acceptance branch, the SKIP branch is guarded by an explicit acceptance marker, never by a bare unconditional skip: a future build on which the fan-out starts working must be able to remove one guard and get the real check back."
  artifacts:
    - hypr/.config/hypr/scripts/quickshell-doctor
  key_links:
    - "12-01 evidence record -> this plan's opening checkpoint:decision -> the acceptance tasks execute or no-op"
    - "quickshell-doctor SKIP line -> 11-VERIFICATION.md overrides[0] and 12-CONTEXT.md D-13 (evidence pointer)"
    - "REQUIREMENTS.md Out of Scope <-> PROJECT.md Out of Scope <-> ROADMAP criterion 6 (three artifacts, one amendment)"
---

<objective>
Execute the D-13 acceptance branch: formally move QS-03 to Out of Scope across
`REQUIREMENTS.md`, `PROJECT.md` and `ROADMAP.md`, and convert `quickshell-doctor`'s
per-screen check into D-14's documented SKIP so the script exits 0 again.

Purpose: D-13 is a one-way door, so it gets its own blocking decision checkpoint immediately
before the tasks that walk through it. The acceptance branch is planned explicitly rather
than left as an implicit fallback, because a limitation that is
"accepted" without the three artifacts being rewritten is exactly the deferred-twice state
D-13 forbids. The ROADMAP amendment is in-scope work for this phase under the D-15
precedent Phase 11 set, not a deviation.

Output: three amended planning artifacts and a `quickshell-doctor` that exits 0 with an
honest, self-explaining SKIP.

**CONDITIONAL PLAN.** Task 1 is the gate. If it returns `close-qs03`, do not edit any file:
write the SUMMARY recording "no-op — QS-03 closed green in 12-01, one-way door not crossed"
and stop. Do not invent work to justify the plan's existence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unified-design-token-pipeline/12-01-SUMMARY.md
@.planning/phases/12-unified-design-token-pipeline/12-CONTEXT.md
</context>

<artifacts_this_phase_produces>
See `12-01-PLAN.md` §"Artifacts this phase produces" for the complete phase-level list of
new symbols, files, contract keys, CLI surfaces, Hyprland variables, GTK4 custom properties,
QML types and gate checks. Nothing in that list is pre-existing; a source-grounding pass must
not treat any of those names as drift.

This plan itself produces no new symbol. It amends `quickshell-doctor` with a
D-14 documented-SKIP branch and rewrites three planning artifacts.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-13 one-way door — close QS-03 or accept it as a permanent limitation</name>

  <decision>
    Whether QS-03 is CLOSED (the fan-out works and is proven across a real session restart),
    or ACCEPTED as a permanent limitation under D-13 — which is a one-way door: it moves the
    requirement to Out of Scope in `REQUIREMENTS.md` and `PROJECT.md`, amends ROADMAP
    criterion 6, and leaves Phase 16's per-monitor overview inheriting a root that cannot fan
    out.
  </decision>

  <context>
    D-13 is rated **one-way** in `12-CONTEXT.md`. Dropping QS-03 rewrites three planning
    artifacts and is explicitly "dropped, not deferred a second time" — it cannot be walked
    back by re-adding a line. That is why this checkpoint opens this plan: every remaining
    task here is the acceptance work, and none of it runs until this is answered.

    Plan 12-01 has already spent D-12's budget and taken the session-restart evidence. Read
    `12-01-SUMMARY.md` and `12-QS03-EVIDENCE.md` before presenting this — it is a decision on a
    completed body of evidence, not a request for more work.

    **If 12-01 came back `14 passed, 0 failed`, this checkpoint is a formality:** record
    `close-qs03`, note that no one-way door was crossed, and treat Tasks 2 and 3 as documented
    no-ops. Do not prompt for a judgement call that the evidence has already made.

    Relevant standing facts, already verified, that should not be re-argued here: quickshell
    0.3.0-2 is the latest in `extra` and is what is installed, so "wait for upstream" is not
    an available option; the host has one physical monitor (`DP-1`), so the fan-out is
    unexercised in daily use; and Phase 11 already accepted this gap once via a recorded
    override rather than dropping it.
  </context>

  <options>
    <option id="close-qs03">
      <name>Close QS-03 — the fix holds</name>
      <pros>The requirement is met as written; Phases 14 and 16 inherit a root that fans out; no planning artifact is rewritten; the rest of this plan becomes a documented no-op.</pros>
      <cons>None, if plan 12-01's evidence is 14/0 across a real session restart.</cons>
    </option>
    <option id="accept-permanent">
      <name>Accept QS-03 as a permanent limitation (D-13, ONE-WAY)</name>
      <pros>Unblocks phase close on honest evidence rather than an open-ended workaround hunt; `quickshell-doctor` regains a usable exit code via D-14's documented SKIP instead of a permanently-red check nobody reads.</pros>
      <cons>ONE-WAY. Rewrites `REQUIREMENTS.md`, `PROJECT.md` and ROADMAP criterion 6. Phase 16's full-screen per-monitor overview inherits a root that cannot fan out, and will have to solve this itself on a build that has already resisted three arrangements plus a spike.</cons>
    </option>
    <option id="extend-budget">
      <name>Extend the budget for one more arrangement</name>
      <pros>Keeps the requirement alive if you have a specific new hypothesis the evidence record suggests.</pros>
      <cons>Directly contradicts D-12's "bounded, with a spike as escape hatch" and the house rule against open-ended workaround hunting. Choose this only with a named hypothesis and a new hard stop — not as a default.</cons>
    </option>
  </options>

  <resume-signal>Select: close-qs03, accept-permanent, or extend-budget. If accept-permanent, confirm explicitly that you understand this is one-way and that Phase 16 inherits the limitation.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Move QS-03 to Out of Scope across REQUIREMENTS.md, PROJECT.md and ROADMAP.md</name>

  <precondition>Task 1's decision checkpoint returned `accept-permanent`. If it returned `close-qs03`, this task is a no-op — record that in the SUMMARY and stop without editing any file.</precondition>

  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>

  <read_first>
    - `.planning/phases/12-unified-design-token-pipeline/12-01-SUMMARY.md` and `12-QS03-EVIDENCE.md` — the branch plan 12-01 reached, every arrangement it tried, and which budget clause ended it. These are the reason text this task writes into three artifacts; do not paraphrase them from memory.
    - `.planning/REQUIREMENTS.md` — the QS-03 row at line ~18, the Traceability row at line ~129, the `## Future Requirements` / Out-of-Scope structure, and the Traceability footer note at line ~181 that already documents the Phase 11 -> Phase 12 ownership move. All three locations must change together or the counts stop reconciling.
    - `.planning/PROJECT.md` §"Out of Scope" (line ~95) — the established row format, including the strikethrough-plus-REVERSED convention used for a reversal; a drop uses the plain form with a reason, not a strikethrough.
    - `.planning/ROADMAP.md` §"Phase 12" criterion 6 (line ~124) and §"Carried-in maintenance placement" (the QS-03 paragraph at line ~72) — both state the requirement as live and must be amended in place.
    - `.planning/phases/11-quickshell-viability-gate/11-CONTEXT.md` D-15 — the ROADMAP-amendment precedent this task follows: amend in place with a dated note naming the phase and the evidence, never a silent rewrite.
    - `.planning/phases/11-quickshell-viability-gate/11-VERIFICATION.md` frontmatter `overrides` — the Phase 11 acceptance this supersedes; the new text must point at it, not erase it.
  </read_first>

  <action>
Amend the three artifacts as one coherent change. Use `Edit` with scoped replacements
throughout — never `Write` on any of these files; a whole-file rewrite destroys content
outside the diff window.

**`.planning/REQUIREMENTS.md`.** Change the QS-03 checkbox row so it reads as dropped rather
than carried: keep the ID and the original text, append a dated Out-of-Scope marker naming
`12-CONTEXT.md D-13` and `12-QS03-EVIDENCE.md` as the reason and the evidence. Update the
Traceability row for QS-03 from "Deferred (carried forward from Phase 11)" to an
Out-of-Scope status naming Phase 12 as the phase that dropped it. Update the Traceability
footer note that currently explains the Phase 11 -> Phase 12 ownership move so it also
records the drop, and re-state the requirement total honestly: the row moves out of the
active set, so say what the new active count is rather than leaving a stale 38.

**`.planning/PROJECT.md` §"Out of Scope".** Add a QS-03 row in the section's existing format,
with the reason stated in the project's own terms: the fan-out was re-attempted in Phase 12
with a checked-in `qmldir` and a per-screen `LazyLoader` under an explicit bounded budget
plus an escape-hatch spike, all recorded in `12-QS03-EVIDENCE.md`; quickshell 0.3.0-2 is the
latest in `extra` so "wait for upstream" was not available; the host has one physical monitor
(`DP-1`) so the fan-out is unexercised in daily use. Also add the consequence, because it is
the part that costs later: **Phase 16's full-screen per-monitor overview inherits a shell root
that cannot fan out and must solve this itself.**

**`.planning/ROADMAP.md`.** Amend Phase 12's criterion 6 in place, following the exact shape
Phase 11's criterion 2 already uses for an amended criterion — keep the original sentence,
then append an italicised amendment note stating that the criterion was amended in Phase 12
per D-13, that QS-03 is now Out of Scope in `REQUIREMENTS.md` and `PROJECT.md`, and pointing
at `12-QS03-EVIDENCE.md` and `12-CONTEXT.md` D-13. Do the same to the QS-03 paragraph under
§"Carried-in maintenance placement". Do not delete either passage: the history of a
twice-attempted requirement is the record that stops a third uninformed attempt.
  </action>

  <reversibility rating="one-way">This is the one-way door itself. Gated by plan 12-01's Task 3 blocking decision checkpoint, which is the only authorisation for this task to run.</reversibility>

  <verify>
    <automated>grep -n "QS-03" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "QS-03" .planning/PROJECT.md` returns at least 1, and the matching line sits under the `## Out of Scope` heading (confirm by `awk` range or by reading the section back).
    - `.planning/REQUIREMENTS.md`'s QS-03 Traceability row no longer contains the word `Deferred`; it names Phase 12 and an Out-of-Scope status.
    - `.planning/ROADMAP.md` Phase 12 criterion 6 still contains its original sentence AND a new amendment note containing the string `12-QS03-EVIDENCE.md`.
    - All three files reference `12-QS03-EVIDENCE.md` so the evidence is reachable from every artifact: `grep -l "12-QS03-EVIDENCE" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` lists all three.
    - No pre-existing phase entry outside Phase 12 changed: `git diff --stat .planning/ROADMAP.md` shows a small edit, and `git diff .planning/ROADMAP.md` contains no removal of any other phase's `### Phase` heading or criteria.
  </acceptance_criteria>

  <done>QS-03 is recorded as Out of Scope in all three artifacts, with a stated reason, a reachable evidence pointer, and the Phase 16 consequence written down.</done>
</task>

<task type="auto">
  <name>Task 3: quickshell-doctor documented SKIP for the per-screen check, exit 0 restored</name>

  <precondition>Task 2 completed on the acceptance branch. If Task 1's checkpoint returned `close-qs03`, this task is a no-op and `quickshell-doctor` is left exactly as plan 12-01 left it.</precondition>

  <files>hypr/.config/hypr/scripts/quickshell-doctor</files>

  <read_first>
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines ~384-470 — the QS-03 headless-output group. Note that it ALREADY has three guarded-SKIP branches for this exact group (`--no-headless-output`, missing `hyprctl`/`jq`, and a pre-existing `HEADLESS*` monitor), each printing four named `[SKIP]` lines. The new branch copies that shape; it does not invent a mechanism.
    - `hypr/.config/hypr/scripts/quickshell-doctor` lines 1-40 — the header banner and the `Usage:` block listing `--no-summon` and `--no-headless-output`, and the record-why comment convention this file already uses for a design correction.
    - `hypr/.config/hypr/scripts/quickshell-doctor` final lines — `echo "Summary: ${PASS} passed, ${FAIL} failed"` followed by `[[ "$FAIL" -eq 0 ]]; exit $?`. A `[SKIP]` line is a bare `echo` and touches neither counter, which is precisely why the SKIP restores exit 0 without faking a pass.
    - `.planning/phases/12-unified-design-token-pipeline/12-UI-SPEC.md` §"Copywriting Contract" — the exact SKIP copy is specified there verbatim; use it, do not paraphrase.
    - `theme-engine/.config/theme-engine/theme-doctor` lines ~484-494 — the sibling guarded-SKIP shape, for consistency of voice across the gate family.
  </read_first>

  <action>
Convert ONLY the per-screen surface creation check into a documented SKIP. The other three
checks in the headless-output group — output add, reserved-space unchanged across hotplug,
and output remove — stay live: they still pass today and they still prove hotplug survival,
which is a different fact from fan-out and was never the accepted gap.

Add a single guard variable near the top of the file, `QS03_ACCEPTED_PERMANENT=1`, with a
record-why comment block in this file's established voice: state that per-screen surface
fan-out was accepted as a permanent limitation under `12-CONTEXT.md` D-13 after a bounded
re-attempt recorded in `12-QS03-EVIDENCE.md`, that the flag is deliberately a one-line guard
so a future quickshell build on which the fan-out works can be re-tested by flipping it to 0
and getting the real check back, and that leaving the check permanently red instead was
rejected because a gate everyone knows is red is a gate nobody reads and its non-zero exit
stops being a usable signal for the other checks.

Inside the per-screen block, branch on that flag BEFORE the summon: when set, print the
SKIP line and do not dispatch `quickshell:probe` at all (skipping the summon is the point —
an accepted limitation should not keep paying for a probe cycle), leaving
`PROBE_SUMMONED_FOR_HEADLESS_TEST` untouched. The SKIP text is fixed by UI-SPEC's
Copywriting Contract and must print both its reason and its evidence pointer, naming
`11-VERIFICATION.md` overrides[0] and `12-CONTEXT.md` D-13.

Extend the file's `Usage:` header block with a short note that the per-screen check is a
documented SKIP under D-13 and where to look to re-enable it, so someone reading the header
sees the caveat without reading 450 lines.

Do not add a `--qs03-accepted` command-line flag. The acceptance is a property of this repo
on this quickshell version, not a per-invocation choice; a flag would let a future run
silently produce a different verdict from the same tree.
  </action>

  <reversibility rating="reversible">A single guard variable and one SKIP branch; setting `QS03_ACCEPTED_PERMANENT=0` restores the live check verbatim. The one-way part is Task 1's artifact rewrite, not this.</reversibility>

  <verify>
    <automated>~/.config/hypr/scripts/quickshell-doctor 2>&1 | tail -3; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `~/.config/hypr/scripts/quickshell-doctor` exits 0 and prints `Summary: 13 passed, 0 failed`.
    - Its output contains a `[SKIP]` line for the per-screen surface creation check that includes both the string `11-VERIFICATION.md` and the string `D-13`.
    - The other three headless-output checks still run and PASS: the output-add, reserved-unchanged and output-remove lines are `[PASS]`, not `[SKIP]`.
    - `bash -n hypr/.config/hypr/scripts/quickshell-doctor` exits 0, and `shellcheck hypr/.config/hypr/scripts/quickshell-doctor` reports no new error-level finding versus the pre-edit baseline.
    - Setting the guard back off restores the failing check: `QS03_ACCEPTED_PERMANENT=0` (edited in-file, then reverted) makes the script report the per-screen check again — confirm once during the task and revert.
    - `grep -c 'QS03_ACCEPTED_PERMANENT' hypr/.config/hypr/scripts/quickshell-doctor` returns at least 2 (the assignment plus the guard).
  </acceptance_criteria>

  <done>`quickshell-doctor` exits 0 with an honest, self-explaining SKIP that carries its reason and its evidence pointer, and no check was faked into a PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts -> future phases | `REQUIREMENTS.md`, `PROJECT.md` and `ROADMAP.md` are the inputs every later phase plans from; a wrong or partial edit misinforms Phases 13-17 |
| gate exit code -> operator trust | `quickshell-doctor`'s exit code is consumed as a signal; a SKIP that reads as a PASS would silently launder a known defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | `.planning/ROADMAP.md` whole-file write | high | mitigate | Task 2 mandates scoped `Edit` only, explicitly forbids `Write`, and its acceptance criteria require `git diff .planning/ROADMAP.md` to contain no removal of any other phase's heading or criteria. |
| T-12-05 | Repudiation | `quickshell-doctor` SKIP branch | high | mitigate | The SKIP prints its reason AND a pointer to two evidence artifacts, is guarded by a named variable rather than a bare skip, and leaves `PASS`/`FAIL` untouched so it can never be mistaken for a passing check. The other three hotplug checks stay live so the group cannot go silently dark. |
| T-12-06 | Information Disclosure | `12-QS03-EVIDENCE.md` content | low | accept | The evidence file records QML arrangements, log excerpts and monitor names from a single-user local desktop. No credential, token or network surface is involved. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs anywhere in Phase 12 — `12-RESEARCH.md` §Package Legitimacy Audit records zero new packages, every dependency already installed and `pacman -Q`-verified. |
</threat_model>

<verification>
- On the acceptance branch: `quickshell-doctor` exits 0; all three planning artifacts reference `12-QS03-EVIDENCE.md`; `git diff --stat` shows only the four intended files.
- On the `close-qs03` branch: `git status --porcelain` is clean for all four files and the SUMMARY states the no-op and its reason.
- Task 1's selected option is recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
Either QS-03's permanent-limitation acceptance is recorded consistently across all three
planning artifacts with `quickshell-doctor` back to exit 0 on an honest SKIP, or this plan
is a documented no-op because 12-01 closed QS-03 green.
</success_criteria>

<output>
Create `.planning/phases/12-unified-design-token-pipeline/12-02-SUMMARY.md` when done.
State the branch taken (`accept-permanent` or `close-qs03` no-op) in the first line.
</output>
