---
phase: 01-root-cause-fix-consolidated-theme-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md
  - install.sh
autonomous: true
requirements: [SCAN-01, SCAN-02, THEME-02]
user_setup: []

must_haves:
  truths:
    - "A single AUDIT.md exists in the phase dir with findings grouped by component (theme pipeline, hyprland/keybinds, uwsm, stow, install scripts), each carrying severity (critical/major/minor), evidence as file:line, and a disposition (fix-in-phase-1 / fix-in-phase-2 / fix-in-phase-3 / wontfix)."
    - "`pacman -Q adw-gtk-theme` returns 0 (the theme package that every config already references by name is installed on this machine) — the stuck-white Thunar root cause is eliminated at the package level."
    - "install.sh installs adw-gtk-theme from the official extra repo via its pacman array, and the nonexistent AUR package name is gone from install.sh."
    - "elephant/walker functional health is recorded in AUDIT.md: `elephant listproviders` output is compared against walker config.toml's configured provider set, and every configured provider that lacks an installed elephant-* package is listed as a finding with a disposition."
    - "AUDIT.md records the four phase-level findings surfaced by research (wallpaper-picker.sh triple apply/reload duplication; elephant provider-package gap; matugen [config.wallpaper]+json panic; GTK_THEME triplication) plus generated-files-tracked-in-git, each with a disposition."
  artifacts:
    - ".planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md"
    - "install.sh (adw-gtk-theme in the pacman array; verified elephant provider packages in the AUR array; wrong AUR name removed)"
  key_links:
    - "walker/.config/walker/config.toml [providers] set ↔ `elephant listproviders` runtime output ↔ installed elephant-* packages (the three must agree, or a gap is a finding)"
    - "install.sh package arrays ↔ the theme/launcher names every stowed config already references by name (adw-gtk3-dark theme, walker providers)"
  prohibitions:
    - statement: "install.sh must not reference the nonexistent AUR package name adw-gtk3 as an install target."
      status: resolved
      verification: "grep -vE '^\\s*#' install.sh | grep -cw 'adw-gtk3' returns 0 (the correct package adw-gtk-theme does not match this word-boundary pattern)."
    - statement: "The audit must not silently drop any finding as unowned — every finding carries a phase disposition."
      status: resolved
      verification: "Every finding row/entry in AUDIT.md contains one of: fix-in-phase-1, fix-in-phase-2, fix-in-phase-3, wontfix."
---

<objective>
Break the patch-without-diagnosing loop (8+ prior failed fix commits) by producing a single documented full-repo audit BEFORE any engine work, and land the one trivially-verified root-cause fix immediately alongside it (D-24): install the real GTK theme package.

This plan covers SCAN-01 (full-repo audit), SCAN-02 (walker/elephant functional health), and the install half of THEME-02 (adw-gtk-theme installed). It is audit-first per D-24; the engine (Plan 01-02) reads this AUDIT.md before it is built so findings inform the design.

Purpose: Every finding gets an owner (D-22/D-23) so nothing sits known-broken, and the verified stuck-white root cause (the `adw-gtk3` package name in install.sh does not exist; the real package `adw-gtk-theme` lives in the official `extra` repo and is not installed) is fixed at once.
Output: `AUDIT.md` in the phase directory; corrected `install.sh` package arrays; `adw-gtk-theme` installed on this machine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-root-cause-fix-consolidated-theme-engine/01-CONTEXT.md
@.planning/phases/01-root-cause-fix-consolidated-theme-engine/01-RESEARCH.md
@.planning/phases/01-root-cause-fix-consolidated-theme-engine/01-PATTERNS.md
</context>

## Phase Goal (MVP user story)

**As a** desktop user, **I want to** switch to any theme (static preset or matugen dynamic) and have every visible app re-theme live from one shared engine with the stuck-white bug gone, **so that** my whole desktop stays consistent without relogins and theming fixes stop looping.

> After this plan, a real user gains: Thunar (and every GTK3 app) stops falling back to stock white — the theme package it already references is finally installed — and there is a written audit that tells every later plan exactly what to fix and where.

## Artifacts this phase produces

Symbols/files this PLAN creates or modifies (source-grounding allowlist — these are new, do not flag as drift):
- `AUDIT.md` (new file, phase directory) — component-grouped findings with severity + `file:line` evidence + disposition
- `install.sh` — `adw-gtk-theme` added to the pacman package array; `elephant-runner`, `elephant-websearch`, `elephant-files` added to the AUR array (only the names that resolve via `paru -Si`); the wrong `adw-gtk3` install target removed

<tasks>

<task type="auto">
  <name>Task 1: Full-repo bug audit → AUDIT.md (SCAN-01, SCAN-02)</name>
  <files>.planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md</files>
  <read_first>
    - .planning/phases/01-root-cause-fix-consolidated-theme-engine/01-RESEARCH.md (Pitfalls W1/W2/G1/G2, Runtime State Inventory, Package Legitimacy Audit, Anti-Patterns — the source of the new findings)
    - .planning/phases/01-root-cause-fix-consolidated-theme-engine/01-PATTERNS.md (file-by-file analog map + line references)
    - .planning/phases/01-root-cause-fix-consolidated-theme-engine/01-CONTEXT.md (D-22 audit format, D-23 fix policy, D-24 sequencing)
    - hypr/.config/hypr/scripts/theme-switch.sh, theme-init.sh, gtk-reload.sh, walker-restart.sh, walker-theme-gen.sh, wallpaper-picker.sh, vscodium-theme.sh
    - matugen/.config/matugen/config.toml
    - walker/.config/walker/config.toml
    - uwsm/.config/uwsm/env, hypr/.config/hypr/config/env.conf
    - stow.sh, .stow-local-ignore, install.sh
  </read_first>
  <action>
    Produce a single AUDIT.md in the phase directory following D-22's format exactly: findings grouped under five component headings — theme pipeline, hyprland/keybinds, uwsm, stow, install scripts — where each finding carries severity (critical|major|minor), evidence as a `file:line` reference, and a disposition of exactly one of fix-in-phase-1 / fix-in-phase-2 / fix-in-phase-3 / wontfix (D-23).

    Apply D-23 fix policy when assigning dispositions: fix in Phase 1 everything found EXCEPT items explicitly owned by Phase 2 (parity/stress → PIPE-04/PIPE-06) or Phase 3 (install.sh hardening beyond the root cause → INST-01/02/03; dead-config/wofi removal → CLEAN-01; future-switch git-clean guarantee → CLEAN-02). Deferred findings MUST carry their target-phase disposition so nothing is unowned.

    Record at minimum these research-surfaced findings, each with evidence and disposition:
    1. Reload/apply logic is duplicated THREE ways, not two: theme-switch.sh, theme-init.sh, AND wallpaper-picker.sh (evidence wallpaper-picker.sh Material-You regeneration block ~lines 132-157). Disposition fix-in-phase-1 (PIPE-01, resolved by Plan 01-02).
    2. Reload fan-out fires twice in dynamic mode: matugen post-render hooks in config.toml AND reload_all() in the switcher (evidence matugen config.toml hook lines; theme-switch.sh:43-50). Disposition fix-in-phase-1 (PIPE-02).
    3. GTK theme env var is hardcoded in three places: uwsm/.config/uwsm/env, hypr/.config/hypr/config/env.conf, and re-exported at runtime in gtk-reload.sh. Disposition fix-in-phase-1 (PIPE-05).
    4. Generated color files are still git-tracked at their old in-repo paths (evidence `git log` on gtk-3.0/colors.css, walker/themes/rice/style.css). Disposition fix-in-phase-1 (the contract changes this phase, so the pre-existing tracked artifacts are this phase's cleanup, per RESEARCH Runtime State Inventory), with future-switch cleanliness noted as CLEAN-02/Phase 3.
    5. matugen crashes (unhandled panic, helpers.rs:262) when `matugen json` runs while [config.wallpaper] set=true (evidence matugen config.toml:8-16). Disposition fix-in-phase-1 (blocking prerequisite for Plan 01-02, D-19).
    6. Walker's assumed hot-reload theme key does not exist in walker 2.16.2 source — reload must be restart-based (RESEARCH Pitfall W1). Disposition fix-in-phase-1 (informs Plan 01-03).
    7. SCAN-02 provider gap: run `elephant listproviders` and diff against walker config.toml's [providers] default + prefix sets; record every configured provider with no installed elephant-* package. Resolve RESEARCH assumption A2 by running `paru -Si elephant-runner elephant-websearch elephant-files` and record which names actually resolve. Note elephant-providerlist is listed in install.sh AUR array but not installed. Disposition fix-in-phase-1 for the install.sh package additions (Task 2), matching the adw-gtk-theme exception shape.

    Also note (one line each, appropriate disposition) any wofi dead-config presence (D-11, physical removal → fix-in-phase-3/CLEAN-01), stow.sh unguarded operations (→ fix-in-phase-3/INST-02), and install.sh blind AUR install hygiene (→ fix-in-phase-3/INST-01). Do NOT fix Phase 2/3 items here — only record and dispose them.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md && grep -qiE 'fix-in-phase-1|fix-in-phase-2|fix-in-phase-3|wontfix' .planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md && grep -qiE 'severity' .planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT.md exists at .planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md
    - AUDIT.md contains the five component headings: theme pipeline, hyprland/keybinds, uwsm, stow, install scripts
    - Every finding has a severity token (critical|major|minor), a `file:line` evidence reference, and exactly one disposition token (fix-in-phase-1|fix-in-phase-2|fix-in-phase-3|wontfix)
    - AUDIT.md contains a SCAN-02 section comparing `elephant listproviders` output to walker config.toml's configured providers, listing each provider with no installed package
    - AUDIT.md records the three-way duplication finding naming wallpaper-picker.sh explicitly
  </acceptance_criteria>
  <done>SCAN-01 and SCAN-02 are documented: a single AUDIT.md groups all findings by component with severity, evidence, and owned dispositions; walker/elephant health is empirically verified and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Root-cause package fix — install adw-gtk-theme + elephant providers (THEME-02 install, SCAN-02 fix)</name>
  <files>install.sh</files>
  <read_first>
    - install.sh (the PACMAN package array and the AUR package array — see PATTERNS.md install.sh section for the exact array locations: pacman array near line 41, AUR array near lines 146-185 with the wrong name near line 150)
    - .planning/phases/01-root-cause-fix-consolidated-theme-engine/01-RESEARCH.md (Standard Stack "The one package this phase installs" + "Correction to install.sh placement" + Package Legitimacy Audit)
    - .planning/phases/01-root-cause-fix-consolidated-theme-engine/AUDIT.md (Task 1's SCAN-02 findings — which elephant provider names resolved via paru -Si)
  </read_first>
  <action>
    Fix the verified root cause and the SCAN-02 provider gap in install.sh, then install the theme package on this machine so THEME-02's "installed" half is true.

    In install.sh: remove the install target adw-gtk3 from the AUR package array (it does not exist under that name in AUR or official repos — it is the actual root cause).
    <!-- planner-discipline-allow: adw-gtk3 -->
    Add `adw-gtk-theme` to the PACMAN package array (it is in the official `extra` repo — pacman, NOT AUR; do not put it in the AUR array). This provides the `adw-gtk3-dark` theme name that gtk-3.0/settings.ini and gsettings already reference.

    For the SCAN-02 provider gap: add ONLY the elephant provider package names that resolved via `paru -Si` in Task 1 (candidates: elephant-runner, elephant-websearch, elephant-files) to the AUR package array, alongside the existing elephant-* block. If a candidate name did not resolve, do not add it — instead leave a Phase-3 note in AUDIT.md with the corrected name TBD. Keep this change minimal and same-shape as the adw-gtk-theme fix; broader install.sh hardening (post-install verification loop, --noconfirm review) stays Phase 3 (INST-01).

    Then install the theme package now: run `sudo pacman -S --needed adw-gtk-theme` (this is a real system change; if it prompts for a sudo password, that is an expected interactive auth gate). Confirm the theme resolves on disk afterward.
  </action>
  <verify>
    <automated>pacman -Q adw-gtk-theme && grep -q 'adw-gtk-theme' install.sh && [ "$(grep -vE '^\s*#' install.sh | grep -cw 'adw-gtk3')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `pacman -Q adw-gtk-theme` exits 0 (package installed on this machine)
    - install.sh contains the string adw-gtk-theme
    - `grep -vE '^\s*#' install.sh | grep -cw 'adw-gtk3'` returns 0 (the wrong install target is gone; adw-gtk-theme does not match the word-boundary pattern)
    - Any elephant provider package added to install.sh resolved via `paru -Si` (verified in Task 1); unresolved candidates are NOT added and are noted in AUDIT.md instead
    - The theme name resolves on disk: `find /usr/share/themes ~/.local/share/themes -maxdepth 1 -iname 'adw-gtk3*' 2>/dev/null` returns at least one entry
  </acceptance_criteria>
  <done>The stuck-white root cause is eliminated: adw-gtk-theme is installed and install.sh installs it from the correct repo; the wrong AUR name is removed; verified elephant providers are added per the audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry (official extra + AUR) → local system | Install targets pulled from remote repos cross into the local machine via pacman/paru |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pacman/AUR installs (adw-gtk-theme, elephant-*) | medium | mitigate | adw-gtk-theme is verified official `extra` repo (Package Legitimacy Audit verdict OK) — install via pacman, not AUR. Elephant provider names verified with `paru -Si` before adding to install.sh (RESEARCH assumption A2); unresolved names are not added. Not npm/pip/cargo, so no automated legitimacy seam applies; verification is the direct pacman/AUR check recorded in AUDIT.md. Severity below the `high` block threshold — no blocking-human gate required. |
| T-01-01 | Information disclosure | AUDIT.md content | low | accept | AUDIT.md records only local file:line evidence and package names — no secrets exist in this pipeline (RESEARCH Security Domain: no SOPS keys, no credentials). |
</threat_model>

<verification>
- AUDIT.md exists with five component sections, severity + evidence + disposition on every finding, and a SCAN-02 provider comparison.
- `pacman -Q adw-gtk-theme` exits 0; install.sh references adw-gtk-theme and no longer lists the wrong AUR name.
- No Phase 2/3 fixes were performed here (only recorded + disposed) — the engine build stays in Plan 01-02.
</verification>

<success_criteria>
- SCAN-01: single documented full-repo audit with owned dispositions.
- SCAN-02: walker/elephant functional health verified and recorded.
- THEME-02 (install half): adw-gtk-theme installed and installable from install.sh.
</success_criteria>

<output>
Create `.planning/phases/01-root-cause-fix-consolidated-theme-engine/01-01-SUMMARY.md` when done.
</output>
