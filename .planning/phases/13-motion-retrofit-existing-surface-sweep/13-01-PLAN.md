---
phase: 13-motion-retrofit-existing-surface-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - theme-engine/.config/theme-engine/motion.json
  - theme-engine/.config/theme-engine/lib/motion.sh
  - hypr/.config/hypr/scripts/motion-switch.sh
  - hypr/.config/hypr/scripts/motion-lint
  - hypr/.config/hypr/config/animations.conf
  - hypr/.config/hypr/config/windowrules.conf
autonomous: false
requirements: [MOTION-01, MOTION-03]

must_haves:
  truths:
    - "Every `animation =` line in animations.conf resolves its speed and its curve from an engine-emitted variable — no raw duration and no hand-authored bezier name remains in the file (D-09, MOTION-01)"
    - "The easing scale carries the full MD3 set before the retrofit consumes it, with every control point taken from the primary specification sources reproduced in 13-RESEARCH.md rather than from another rice's config file (D-10)"
    - "`hyprctl animations -j` after a theme-apply reports every retrofitted slot holding its generated curve values rather than a silently substituted Hyprland default (12-04 precedent: Hyprland DROPS a malformed bezier rather than erroring)"
    - "`hyprland.conf` sources `hyprland-motion.conf` before `config/animations.conf`, and `hyprctl animations -j` confirms every retrofitted slot holds its generated curve rather than a Hyprland default"
    - "`animations.conf` referencing a curve or speed name absent from `hyprland-motion.conf` makes `Hyprland --verify-config` fail loudly rather than silently substituting — proven against a poisoned copy before the real config is trusted"
    - "A single `motion-switch.sh --curves legacy|md3` invocation flips the whole window/fade/workspace feel through exactly one theme-apply re-render, with no second render path (D-21, D-36)"
    - "The Hyprland speed unit is confirmed by a human-observed extreme-value run before any duration is converted (D-13) — the documented ds claim is falsified against a stopwatch, not trusted"
    - "The default motion vocabulary is pure MD3: no `animation =` line references an `x-*` curve unless a D-22 retune adopted it (D-09/D-11/D-12)"
    - statement: "With `layers`, `layersIn` and `layersOut` all declared, `hyprctl animations -j` reports layersIn/layersOut holding their own speed and curve rather than inheriting the parent `layers` values"
      verification: backstop
  artifacts:
    - path: "theme-engine/.config/theme-engine/motion.json"
      provides: "Full primary-sourced MD3 easing scale, D-11 marked non-MD3 `x-*` extension, D-15 semantic and indicator additions, A/B `curve_sets`"
      contains: "standard-decelerate"
    - path: "hypr/.config/hypr/config/animations.conf"
      provides: "Fully tokenized window/workspace/layer animation set with layersIn/layersOut split"
      contains: "layersOut"
    - path: "theme-engine/.config/theme-engine/lib/motion.sh"
      provides: "Hyprland speed/curve variable writer + motion-curves closed-set reader"
      contains: "motion_speed_"
  key_links:
    - from: "hypr/.config/hypr/config/animations.conf"
      to: "~/.local/state/theme/hyprland-motion.conf"
      via: "hyprlang variable substitution of $motion_speed_* / $motion_curve_* and motion-* bezier names"
      pattern: "\\$motion_(speed|curve)_"
    - from: "hypr/.config/hypr/scripts/motion-switch.sh"
      to: "theme-engine/.config/theme-engine/lib/motion.sh"
      via: "~/.local/state/theme/motion-curves state file read by the closed-set reader"
      pattern: "motion-curves"
  prohibitions:
    - statement: "MUST NOT let this plan's render gate stand in for the soak verdict — the gate asks fidelity only (does the same named token render the same here as in the QML inspector's replay row); a gate that also asks 'does this look good?' at plan close is a rubber stamp, because taste needs time and only the per-motion soak table may record it"
      category: values
      status: unverified
      flagged: true
    - statement: "MUST NOT emit motion that ignores the active motion-scale — `off` must still disable animation at the toolkit level and `reduced` must still shorten every retrofitted Hyprland slot after the tokenization, not only the pre-retrofit ones"
      category: safety
      status: unverified
      flagged: true
---

<objective>
Retrofit every Hyprland window, workspace and layer animation onto the shared motion
tokens, and ship the temporary A/B comparison toggle that makes the soak verdict
comparative rather than a memory test.

This is the phase's **tracer**: it wires one motion path end-to-end through every layer
the phase touches — `motion.json` → `lib/motion.sh` → `~/.local/state/theme/hyprland-motion.conf`
→ `hypr/config/animations.conf` → the live compositor — and proves it with a real
`hyprctl animations -j` readback before any expansion.

It is also, per D-18/D-37, the **first** plan: the Hyprland change lands here so the soak
clock starts now and accrues while plans 02-06 proceed on files this plan does not touch.

Purpose: MOTION-01. Also starts MOTION-03's gate sequence and D-19's incompressible soak.
Output: a fully tokenized `animations.conf`, a grown `motion.json`, a Hyprland
speed/curve variable writer, and `motion-switch.sh --curves`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-motion-retrofit-existing-surface-sweep/13-CONTEXT.md
@.planning/phases/13-motion-retrofit-existing-surface-sweep/13-RESEARCH.md
@.planning/phases/13-motion-retrofit-existing-surface-sweep/13-PATTERNS.md
</context>

<!-- planner-discipline-allow: bezier = -->
<!-- planner-discipline-allow: md3_decel -->
<!-- planner-discipline-allow: wofi -->

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing, exclude from drift verification):

| Artifact | Kind | Location |
|---|---|---|
| `standard-decelerate`, `standard-accelerate`, `legacy`, `legacy-decelerate`, `legacy-accelerate`, `css-linear` | new `motion.json` `.easings` keys | `theme-engine/.config/theme-engine/motion.json` |
| `x-overshoot`, `x-overshoot-in`, `x-undershoot-out`, `x-smooth-in`, `x-smooth-out` | new `motion.json` `.easings` keys (D-11 marked non-MD3 namespace; also the A/B `legacy` set) | same |
| `standard-slow`, `ambient` | new `motion.json` `.semantic` keys | same |
| `indicators` (`blink-slow`, `blink-fast`, `border-rotate`) | new `motion.json` top-level object | same |
| `curve_sets` (`md3`, `legacy`) | new `motion.json` top-level object (temporary, removed in 13-07) | same |
| `theme_engine_read_motion_curves` | new shell function | `theme-engine/.config/theme-engine/lib/motion.sh` |
| `$motion_speed_<token>`, `$motion_curve_<slot>`, `$motion_speed_indicator_<name>` | new emitted hyprlang variables | `~/.local/state/theme/hyprland-motion.conf` |
| `~/.local/state/theme/motion-curves` | new state file (temporary, removed in 13-07) | state dir |
| `motion-switch.sh --curves` | new CLI flag | `hypr/.config/hypr/scripts/motion-switch.sh` |
| `animation = layersIn`, `animation = layersOut` | new Hyprland animation slots | `hypr/.config/hypr/config/animations.conf` |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: One-way door — MD3 purity (D-09 costly, D-12 one-way in spirit)</name>
  <reversibility rating="costly">D-09 deletes five hand-authored character curves from the tree and D-12's escape policy forecloses restoring them; undoing means re-sourcing curves that only exist in git history.</reversibility>
  <decision>Confirm the MD3-purity retrofit before the five character curves leave `animations.conf`</decision>
  <context>
    CONTEXT.md D-09 records the user choosing MD3 purity over the safer recommendation
    (promote the character curves verbatim, zero perceptual change). D-12 then forecloses
    reverting a rejected motion to a hand-authored curve — a soak rejection re-tunes WITHIN
    MD3 or lands on D-11's marked `x-*` extension.

    13-RESEARCH.md confirmed D-11's working hypothesis with primary sources: every MD3
    bezier control point is inside [0,1]. There is no overshoot anywhere in the MD3 bezier
    vocabulary — the overshoot in MD3 Expressive is spring physics, which TOKEN-06 already
    evaluated and rejected. So this retrofit deliberately removes overshoot/undershoot from
    window open/close/move and fades on a daily-driver desktop.

    Undo cost: the five curves (`0.05,0.9,0.1,1.05`, `0.1,1.1,0.1,1.1`, `0.3,-0.3,0,1`,
    `0.25,1,0.5,1`, `0.36,0,0.66,-0.56`) survive only in git history after this plan's first
    commit. This plan mitigates that by single-sourcing them into `motion.json` under the
    marked `x-*` namespace for the duration of the phase — that is D-11's pre-authorized
    landing spot and the A/B toggle's `legacy` set, not a reversion path.

    This gate is the last moment before the deletion lands. It is not re-asking the choice;
    it is confirming the door immediately before walking through it.
  </context>
  <options>
    <option id="proceed-md3-pure">
      <name>Proceed as decided — pure MD3 default, `x-*` retained only for the A/B toggle and D-22 retunes</name>
      <pros>Delivers D-09/D-12 exactly as recorded; criterion 1 satisfied; the A/B toggle and the soak are the instruments that judge the feel change</pros>
      <cons>Window open/close/move and fades lose their overshoot character from this commit until the soak verdict says otherwise</cons>
    </option>
    <option id="proceed-x-default">
      <name>Proceed, but map the five feel-changing slots to `x-*` by default and treat MD3 as the A/B alternative</name>
      <pros>Zero day-one perceptual change; the soak still runs comparatively</pros>
      <cons>Contradicts D-09's recorded choice; the default vocabulary would no longer be pure MD3, so criterion 1's "shared curve set" reads as the legacy set</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-CONTEXT.md (D-09, D-11, D-12, D-21)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-RESEARCH.md (State of the Art — the full MD3 easing table and D-11's answer)
    - hypr/.config/hypr/config/animations.conf
  </read_first>
  <acceptance_criteria>
    - The operator's selection (`proceed-md3-pure` or `proceed-x-default`) is recorded verbatim in the plan SUMMARY before Task 3 edits `motion.json`
    - Tasks 3 and 4 implement the selected option; if `proceed-x-default` is selected, the `curve_sets` default in `motion.json` is `legacy` and every other instruction in this plan is unchanged
  </acceptance_criteria>
  <resume-signal>Select: proceed-md3-pure or proceed-x-default</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-13 — confirm Hyprland's speed unit by extreme-value observation</name>
  <precondition>A live Hyprland 0.56.0 session is running and `hyprctl animations -j` returns JSON (this gate cannot run headless).</precondition>
  <what-built>
    Nothing is edited. The executor stages a **runtime-only, instantly revertible** probe:

    `hyprctl keyword animation "layers,1,500,md3_decel,popin 80%"`

    This changes one animation slot in the running compositor with no file edit. 13-RESEARCH.md
    cites the Hyprland wiki verbatim — "`speed` is the amount of ds (1ds = 100ms)" — but that
    page documents the newer Lua surface, not the legacy `hyprlang` parser this repo runs, and
    CONTEXT.md's own finding is that no string in the Hyprland binary states the unit. A
    readback assertion would prove only that Hyprland stored what we wrote; the gate would be
    green while every duration is off by 10x. D-13 requires the smallest test that can actually
    fail: a human with a stopwatch.

    Expected discrimination at speed 500: **~50 s** if the unit is deciseconds (the documented
    claim), **~5 s** if centiseconds, **~0.5 s** if raw milliseconds.
  </what-built>
  <how-to-verify>
    1. The executor runs the `hyprctl keyword` line above and confirms it returns `ok`.
    2. Press the walker launcher keybind (or any keybind that opens a layer-shell surface) ONCE
       and time the entrance animation with a phone stopwatch — not a script; the point is a
       human watching a single observable event.
    3. Report the observed duration in seconds.
    4. The executor restores the live config with `hyprctl reload` and confirms
       `hyprctl animations -j` reports the `layers` slot back at its pre-probe speed.

    If the observed duration is ~50 s, the ds unit is confirmed and Task 4 converts durations
    as `speed = resolved_ms / 100`. If it is ~5 s or ~0.5 s, report the number — Task 4's
    divisor changes to 10 or 1 accordingly and the plan does NOT proceed on the wiki's claim.
  </how-to-verify>
  <read_first>
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-RESEARCH.md (Code Examples — D-13's extreme-value speed observation; State of the Art — the ds citation)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-CONTEXT.md (D-13, and the live animation readback table in `<code_context>`)
    - hypr/.config/hypr/config/animations.conf
  </read_first>
  <acceptance_criteria>
    - The observed wall-clock duration is recorded as a number in the plan SUMMARY, with the derived ms-per-speed-unit stated explicitly
    - `hyprctl animations -j` after the restore reports the `layers` slot at speed `4` (its pre-probe value), proving the probe left no residue
    - Task 4's speed values are computed with the divisor this observation established, not with the divisor the wiki claims
  </acceptance_criteria>
  <resume-signal>Report the observed duration in seconds, then type "approved"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Grow the token source and add the A/B curve-set axis</name>
  <precondition>`~/.local/state/theme/hyprland-motion.conf` exists and carries `$motion_enabled` plus the Phase 12 curve block (a prior-phase artifact this plan extends rather than creates).</precondition>
  <files>theme-engine/.config/theme-engine/motion.json, theme-engine/.config/theme-engine/lib/motion.sh, hypr/.config/hypr/scripts/motion-switch.sh</files>
  <read_first>
    - theme-engine/.config/theme-engine/motion.json (current 4-easing / 3-semantic / 4-scale shape)
    - theme-engine/.config/theme-engine/lib/motion.sh (the three existing writers, `theme_engine_read_motion_scale`'s closed-`case` shape, `theme_engine_validate_motion_values`'s validate-before-write discipline, the shared `$resolved` TSV)
    - hypr/.config/hypr/scripts/motion-switch.sh (the one-entrypoint contract at line 118, the closed-set validation against `.scales` keys, the temp-file+mv atomic write)
    - theme-engine/.config/theme-engine/lib/font.sh (the canonical minimal theme-orthogonal-axis shape)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-RESEARCH.md (State of the Art — the primary-sourced MD3 easing table with per-token citations)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-PATTERNS.md (motion.sh writer-shape pattern; theme-orthogonal axis contract)
  </read_first>
  <action>
Grow `motion.json` and teach `lib/motion.sh` to emit Hyprland speed and curve variables, so
`animations.conf` can reference tokens instead of literals in Task 4.

**A. `motion.json` — `.easings` additions.** Add the six single-bezier MD3 easings missing
from the scale, using the control points from 13-RESEARCH.md's primary-sourced table
verbatim (kebab-case keys, matching the existing convention):

| key | control points |
|---|---|
| `standard-decelerate` | `[0, 0, 0, 1]` |
| `standard-accelerate` | `[0.3, 0, 1, 1]` |
| `legacy` | `[0.4, 0, 0.2, 1]` |
| `legacy-decelerate` | `[0, 0, 0.2, 1]` |
| `legacy-accelerate` | `[0.4, 0, 1, 1]` |
| `css-linear` | `[0, 0, 1, 1]` |

`css-linear` coexists with the existing `linear` `[1,1,1,1]`, which is Hyprland's own
convention and NOT CSS/MD3 linear (D-11's corroborating finding, confirmed by the primary
table). Do not change `linear`. Do NOT add the compound `emphasized` curve — it is a
two-segment `ThreePointCubic` that the four-number schema cannot carry and no decision in
CONTEXT.md calls for it (13-RESEARCH.md Open Question 1).

**B. `motion.json` — D-11's marked non-MD3 extension.** Add five `x-`-prefixed easings
carrying the pre-retrofit character curves, single-sourced here rather than left as one-off
literals. Add a sibling `_comment` on the object stating that these are non-MD3, are NOT
referenced by any default token, exist as D-11's pre-authorized landing spot and as the A/B
toggle's `legacy` set, and are deleted in plan 13-07 unless a D-22 retune adopted one:

| key | control points | replaces |
|---|---|---|
| `x-overshoot` | `[0.05, 0.9, 0.1, 1.05]` | the `wind` curve |
| `x-overshoot-in` | `[0.1, 1.1, 0.1, 1.1]` | the `winIn` curve |
| `x-undershoot-out` | `[0.3, -0.3, 0, 1]` | the `winOut` curve |
| `x-smooth-in` | `[0.25, 1, 0.5, 1]` | the `smoothIn` curve |
| `x-smooth-out` | `[0.36, 0, 0.66, -0.56]` | the `smoothOut` curve |

**C. `motion.json` — `.semantic` additions (D-15).** Add exactly two pairs, both filling a
genuine gap rather than naming a number:
- `standard-slow`: `{ "duration": "medium2", "easing": "standard" }` — the neutral 300 ms
  transition pair waybar's dominant value maps to. `emphasized-in` is also 300 ms but pairs
  with a decelerate curve, so it is not a neutral substitute.
- `ambient`: `{ "duration": "extra-long4", "easing": "linear" }` — the slow ambient
  border-colour transition, preserving today's 1000 ms feel rather than silently shortening
  a slot the render gate does not judge.

**D. `motion.json` — `indicators` (D-15's non-semantic category).** Add a new top-level
`indicators` object for infinite alternating/looping state indicators. These are not
transitions, have no cross-target semantic meaning, and carry an explicit `duration_ms`
integer because two of them exceed the MD3 duration scale:
- `blink-slow`: `{ "duration_ms": 1000, "easing": "css-linear" }`
- `blink-fast`: `{ "duration_ms": 500, "easing": "css-linear" }`
- `border-rotate`: `{ "duration_ms": 10000, "easing": "linear" }`

`blink-*` use `css-linear` because their consumer is GTK CSS; `border-rotate` uses `linear`
because its consumer is Hyprland's parser.

**E. `motion.json` — `curve_sets` (D-21's A/B toggle).** Add a temporary top-level
`curve_sets` object with a `_comment` naming plan 13-07 as its removal owner. Two sets, each
mapping the six slots whose feel D-09 changes to an easing name:

| slot | `md3` | `legacy` |
|---|---|---|
| `windows-in` | `emphasized-decelerate` | `x-overshoot-in` |
| `windows-out` | `emphasized-accelerate` | `x-undershoot-out` |
| `windows-move` | `standard` | `x-overshoot` |
| `fade-in` | `standard-decelerate` | `x-smooth-in` |
| `fade-out` | `standard-accelerate` | `x-smooth-out` |
| `workspaces` | `emphasized-decelerate` | `x-overshoot` |

**F. `lib/motion.sh` — `theme_engine_read_motion_curves`.** Add a closed-`case` reader for a
new state file `$HOME/.local/state/theme/motion-curves`, accepting only `md3` and `legacy`
and defaulting to `md3`. Copy `theme_engine_read_motion_scale`'s exact shape — this value
flows into a file Hyprland's config parser consumes, so an arbitrary string must never pass
through (ASVS V5).

**G. `lib/motion.sh` — extend `theme_engine_validate_motion_values`.** Before any write,
also assert: every `indicators` entry has a positive finite `duration_ms` and an `easing`
that resolves in `.easings`; every `curve_sets` set resolves every one of its slot values in
`.easings`; and the active `motion-curves` value names a real `curve_sets` key. Diagnose the
offending token to stderr and return non-zero, matching the existing failure shape exactly.

**H. `lib/motion.sh` — extend the Hyprland writer (writer 1).** Above the existing
`animations {}` block in `hyprland-motion.conf`, and alongside the existing
`$motion_enabled` assignment, emit three new families of top-level hyprlang variables. Names
use underscores (hyprlang variables cannot carry the kebab-case the JSON keys use), so
convert `-` to `_` on emit:

- One `$motion_speed_<token> = <ds>` per `.semantic` entry, where `<ds>` is the already-
  resolved, already-clamped milliseconds from the shared `$resolved` TSV divided by the
  divisor Task 2's observation established (100 if deciseconds is confirmed). Emit with two
  decimal places so `150 ms` renders as `1.50`, not `1`.
- One `$motion_speed_indicator_<name> = <ds>` per `indicators` entry, scaled by the same
  multiplier and clamped by the same `floor_ms` as the semantic pairs, so `reduced` and
  `lively` reach indicators too.
- One `$motion_curve_<slot> = motion-<easing>` per slot in the ACTIVE `curve_sets` set,
  resolved through `theme_engine_read_motion_curves`.

The existing `.easings | to_entries[]` loop that emits the curve declarations needs no
change — it already emits every declared easing unconditionally (the WR-02 comment), so the
new MD3 and `x-*` entries appear automatically as `motion-`-prefixed names.

**I. `motion-switch.sh` — `--curves`.** Add `--curves <md3|legacy>` and `--curves --get`,
mirroring the existing preset path exactly: validate the argument against `motion.json`'s
real `curve_sets` keys (never a hardcoded enum), write `motion-curves` with the temp-file+mv
atomic idiom, then trigger exactly ONE `theme-apply "$CURRENT_THEME"` re-render. Do not add
a second render path — line 118's one-entrypoint contract is unchanged and the sass compile
plan 13-02 adds will ride the same single re-render automatically (D-36). Extend `usage()`
and `--list` to mention the curve-set axis.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && jq -e '.easings | has("standard-decelerate") and has("standard-accelerate") and has("legacy") and has("legacy-decelerate") and has("legacy-accelerate") and has("css-linear") and has("x-overshoot") and has("x-overshoot-in") and has("x-undershoot-out") and has("x-smooth-in") and has("x-smooth-out")' theme-engine/.config/theme-engine/motion.json && jq -e '.semantic | has("standard-slow") and has("ambient")' theme-engine/.config/theme-engine/motion.json && jq -e '.indicators | has("blink-slow") and has("blink-fast") and has("border-rotate")' theme-engine/.config/theme-engine/motion.json && jq -e '.curve_sets | has("md3") and has("legacy")' theme-engine/.config/theme-engine/motion.json</automated>
    <automated>cd /home/aorus/dotfiles && jq -e '[.easings | to_entries[] | select((.value|length) != 4 or ([.value[] | type != "number"] | any))] | length == 0' theme-engine/.config/theme-engine/motion.json && jq -e '.easings as $E | [.curve_sets | to_entries[] | .value | to_entries[] | select($E[.value] == null)] | length == 0' theme-engine/.config/theme-engine/motion.json</automated>
    <automated>~/.config/theme-engine/theme-apply "$(cat ~/.local/state/theme/current-theme)" && grep -cE '^\$motion_speed_' ~/.local/state/theme/hyprland-motion.conf | grep -qvx 0 && grep -cE '^\$motion_curve_' ~/.local/state/theme/hyprland-motion.conf | grep -qvx 0</automated>
    <automated>~/.config/hypr/scripts/motion-switch.sh --curves legacy && grep -E '^\$motion_curve_windows_in' ~/.local/state/theme/hyprland-motion.conf | grep -q 'motion-x-overshoot-in' && ~/.config/hypr/scripts/motion-switch.sh --curves md3 && grep -E '^\$motion_curve_windows_in' ~/.local/state/theme/hyprland-motion.conf | grep -q 'motion-emphasized-decelerate'</automated>
    <automated>printf 'nope\n' > ~/.local/state/theme/motion-curves && ~/.config/theme-engine/theme-apply "$(cat ~/.local/state/theme/current-theme)" && grep -E '^\$motion_curve_windows_in' ~/.local/state/theme/hyprland-motion.conf | grep -q 'motion-emphasized-decelerate' && printf 'md3\n' > ~/.local/state/theme/motion-curves</automated>
  </verify>
  <acceptance_criteria>
    - `theme-apply` exits 0 and `~/.local/state/theme/hyprland-motion.conf` carries one `$motion_speed_<token>` line per `.semantic` key, one `$motion_speed_indicator_<name>` line per `indicators` key, and one `$motion_curve_<slot>` line per `curve_sets` slot
    - `$motion_speed_emphasized_out` renders with a fractional part (150 ms at the confirmed divisor is not an integer number of speed units) — a truncated integer here is the failure this criterion exists to catch
    - `motion-switch.sh --curves legacy` then `--curves md3` flips `$motion_curve_windows_in` between `motion-x-overshoot-in` and `motion-emphasized-decelerate`, each through exactly one `theme-apply` invocation
    - An out-of-set value written directly into `~/.local/state/theme/motion-curves` falls back to `md3` rather than reaching the emitted file (closed-set read proven, not assumed)
    - A deliberately broken `motion.json` (an `indicators` entry whose `easing` names a non-existent key, or a `curve_sets` slot naming a non-existent easing) makes `theme_engine_render_motion_files` return non-zero and leaves `~/.local/state/theme/` byte-unchanged — the gate is proven able to fail before it is trusted to pass, then reverted
    - `motion-switch.sh` still contains exactly one `theme-apply` invocation (D-36's one-entrypoint contract intact)
  </acceptance_criteria>
  <done>`motion.json` carries the full MD3 easing scale, the marked `x-*` extension, two new semantic pairs, the indicator category and both curve sets; `hyprland-motion.conf` emits speed and curve variables; `motion-switch.sh --curves` flips them through one re-render.</done>
</task>

<task type="tracer" tdd="false">
  <name>Task 4: Tracer — retrofit animations.conf end-to-end onto the emitted tokens</name>
  <precondition>Task 2's observation established the ms-per-speed-unit divisor, and `~/.local/state/theme/hyprland-motion.conf` carries `$motion_speed_*` and `$motion_curve_*` variables (Task 3).</precondition>
  <files>hypr/.config/hypr/config/animations.conf, hypr/.config/hypr/config/windowrules.conf, hypr/.config/hypr/scripts/motion-lint</files>
  <read_first>
    - hypr/.config/hypr/config/animations.conf (all 24 lines — the 12 curve declarations and 13 `animation =` lines this task rewrites, plus the comment block at lines 7-12 explaining why `enabled = $motion_enabled` is owned here)
    - ~/.local/state/theme/hyprland-motion.conf (the freshly emitted variables Task 3 added — read the real file, not an assumed shape)
    - hypr/.config/hypr/config/windowrules.conf (lines 187 and 263 are the dead rules to delete; line 254's wleave rule is the per-namespace precedent that STAYS)
    - hypr/.config/hypr/scripts/motion-lint (the `EXEMPTIONS` list at line ~355, `load_hypr_defs`, and the reference-scanning logic that must learn `$motion_*` variables)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-CONTEXT.md (D-07, D-08, D-09, D-14; the verified `layerrule` field-grammar table in `<code_context>`)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-RESEARCH.md (State of the Art — the animation-tree inheritance citation; Assumptions Log A2)
  </read_first>
  <action>
Wire ONE motion path all the way through the stack first, prove it live, then expand across
the file. Do the thin slice and its verification before touching the other slots — if
hyprlang does not substitute variables inside an `animation =` value list, that must surface
after one line, not after thirteen.

**Thin slice (do this first, verify, then continue).** Leave every existing curve
declaration and `animation =` line untouched, and add exactly two new lines below the
existing `layers` entry:

```
animation = layersIn,  1, $motion_speed_emphasized_in,  motion-emphasized-decelerate, popin 80%
animation = layersOut, 1, $motion_speed_emphasized_out, motion-emphasized-accelerate, popin 80%
```

Then run `Hyprland --verify-config` (clean), `hyprctl reload`, and `hyprctl animations -j`.
The readback must show `layersIn` at the resolved emphasized-in speed and `layersOut` at the
resolved emphasized-out speed, each holding its own curve rather than inheriting the parent
`layers` values. Open walker once and confirm the entrance visibly animates. This is the
whole vertical slice: `motion.json` → `motion.sh` → `hyprland-motion.conf` →
`animations.conf` → compositor. Only after this readback passes, continue.

**Expansion — retrofit the remaining slots.** Rewrite every `animation =` line so its speed
field is a `$motion_speed_*` variable and its curve field is either a `$motion_curve_*`
variable (the six slots the A/B toggle covers) or a `motion-`-prefixed name. Style keywords
stay hand-authored verbatim (D-14 — they select a spatial transform, not a duration or
curve, and have no GTK4/QML counterpart). Target state:

| slot | speed | curve | style |
|---|---|---|---|
| `windowsIn` | `$motion_speed_emphasized_in` | `$motion_curve_windows_in` | `popin 60%` |
| `windowsOut` | `$motion_speed_emphasized_out` | `$motion_curve_windows_out` | `popin 60%` |
| `windowsMove` | `$motion_speed_standard` | `$motion_curve_windows_move` | `slide` |
| `fadeIn` | `$motion_speed_emphasized_in` | `$motion_curve_fade_in` | — |
| `fadeOut` | `$motion_speed_emphasized_out` | `$motion_curve_fade_out` | — |
| `fadeSwitch` | `$motion_speed_standard` | `motion-standard` | — |
| `fadeShadow` | `$motion_speed_standard` | `motion-standard` | — |
| `fadeDim` | `$motion_speed_standard` | `motion-standard` | — |
| `border` | `$motion_speed_ambient` | `motion-linear` | — |
| `borderangle` | `$motion_speed_indicator_border_rotate` | `motion-linear` | `loop` |
| `workspaces` | `$motion_speed_emphasized_in` | `$motion_curve_workspaces` | `slide` |
| `specialWorkspace` | `$motion_speed_emphasized_in` | `motion-emphasized-decelerate` | `slidevert` |
| `layers` | `$motion_speed_standard` | `motion-standard` | `popin 80%` |
| `layersIn` | `$motion_speed_emphasized_in` | `motion-emphasized-decelerate` | `popin 80%` |
| `layersOut` | `$motion_speed_emphasized_out` | `motion-emphasized-accelerate` | `popin 80%` |

Keep the parent `layers` entry declared even though `layersIn`/`layersOut` now cover every
real case — the animation tree's stated inheritance semantics expect the parent to exist,
and it costs nothing (13-RESEARCH.md Open Question 2's recommendation).

**Delete every curve declaration from the file.** All twelve go: the four byte-identical
duplicates now resolve from the engine-emitted block, the three declared-but-never-referenced
ones are dead, and the five character curves are replaced per D-09 (they survive
single-sourced in `motion.json`'s `x-*` namespace for the A/B toggle). `enabled =
$motion_enabled` STAYS in this file — the comment block at lines 7-12 records why (whichever
`animations {}` block loads last would clobber it).

**Delete the two dead layerrules** in `windowrules.conf` at lines 187 and 263 (the launcher
they targeted was removed in v1.0). Leave line 254's namespace-scoped rule for the logout
overlay in place — it is D-07's per-namespace precedent, not debt.

**Teach `motion-lint` the new reference shape.** `load_hypr_defs` currently collects the
emitted curve names; extend it to also collect every `^\s*\$(motion_[A-Za-z0-9_]+)\s*=`
assignment from `hyprland-motion.conf`, and extend the `.conf` reference scanner to treat
`$motion_*` as a token reference that must resolve against that set (CHECK A) rather than as
an unrecognised literal. Then remove the `hypr/config/animations.conf` entry from
`EXEMPTIONS` — the file is now compliant and D-32's end state has no entry for it. Confirm
CHECK B does not flag the `1` in each line's on/off field.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && Hyprland --verify-config 2>&1 | tee /tmp/hv.log; grep -qiE 'error|no such bezier|invalid' /tmp/hv.log && exit 1 || exit 0</automated>
    <automated>cd /home/aorus/dotfiles && test "$(grep -cE '^[[:space:]]*bezier[[:space:]]*=' hypr/.config/hypr/config/animations.conf)" = "0"</automated>
    <automated>cd /home/aorus/dotfiles && test "$(grep -cE '^[[:space:]]*animation[[:space:]]*=' hypr/.config/hypr/config/animations.conf)" = "15"</automated>
    <automated>cd /home/aorus/dotfiles && grep -vE '^\s*#' hypr/.config/hypr/config/animations.conf | grep -E '^[[:space:]]*animation[[:space:]]*=' | grep -vE '\$motion_speed_' | wc -l | grep -qx 0</automated>
    <automated>hyprctl reload && hyprctl animations -j | jq -e '[.[0][] | select(.name=="layersIn" or .name=="layersOut")] | length == 2'</automated>
    <automated>hyprctl animations -j | jq -e '[.[0][] | select(.name=="layersIn")][0].speed != [.[0][] | select(.name=="layers")][0].speed'</automated>
    <automated>~/.config/hypr/scripts/motion-lint; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `Hyprland --verify-config` produces no error line, and `hyprctl reload` succeeds against the live session
    - `hyprctl animations -j` reports 15 configured slots including `layersIn` and `layersOut`, each holding its own speed distinct from the parent `layers` slot — the 12-04 finding is that Hyprland silently DROPS a malformed curve rather than erroring, so this readback (not the verify-config pass) is the real proof
    - Every `animation =` line's speed field is a `$motion_speed_*` variable: filtering comment lines out and grepping for lines without that prefix returns zero
    - The file contains zero curve declaration lines — all curves resolve from `~/.local/state/theme/hyprland-motion.conf`
    - A poisoned copy of `animations.conf` referencing a curve name the engine does not emit makes `Hyprland --verify-config` fail with a "no such bezier" diagnostic, and a poisoned copy referencing an undefined `$motion_speed_*` variable also fails — both proven, then discarded, before the real file is trusted
    - `windowrules.conf` no longer carries either dead launcher layerrule, and still carries the namespace-scoped rule for the logout overlay at its original position
    - `~/.config/hypr/scripts/motion-lint` exits 0 with `hypr/config/animations.conf` no longer present in `EXEMPTIONS`, and a temporarily poisoned `animations.conf` carrying a raw millisecond literal makes it exit 1
    - `motion-switch.sh reduced` visibly shortens, and `motion-switch.sh off` disables, the retrofitted Hyprland animations — confirmed by `hyprctl animations -j` speed values and the `$motion_enabled` readback, then restored to `normal`
  </acceptance_criteria>
  <done>Every Hyprland window, workspace, fade, border and layer animation resolves both its duration and its curve from the shared token source; the layer motion is split into entrance and exit; the dead launcher layerrules are gone; and `motion-lint` verifies the file with no exemption.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: D-17 render gate — Hyprland fidelity, and the soak clock starts</name>
  <what-built>
    Hyprland's window, workspace, fade, border and layer animations now resolve every
    duration and curve from `motion.json` through `hyprland-motion.conf`. Layer motion is
    split into `layersIn` (emphasized-in, 300 ms, decelerate) and `layersOut` (emphasized-out,
    150 ms, accelerate) — which is what walker, SwayOSD, swaync's popup and the AGS media card
    animate with, since their motion has always been compositor-owned (D-06). A temporary
    `motion-switch.sh --curves md3|legacy` toggle flips window/fade/workspace curves between
    the new MD3 set and the pre-retrofit character curves.
  </what-built>
  <how-to-verify>
    **This gate asks the FIDELITY question only. It does not ask whether you like it.**
    Taste is the soak's question (D-17) — asked "does this look OK?" at plan close the honest
    answer is always "yes, I guess", which is how a gate becomes a rubber stamp.

    The instrument already exists: Phase 12's token inspector
    (`quickshell/.config/quickshell/modules/Probe.qml`) has a replayable motion row fired
    from a button.

    1. Open the Quickshell token inspector and replay the `emphasized-in` motion row. Note how
       it accelerates and settles.
    2. Open walker (a layer surface). **Question: does `emphasized-in` visibly render the same
       on this surface as in the inspector's replay row?** Same settle character, same rough
       duration — not "does it look nice".
    3. Dismiss walker and watch the exit. **Question: is `layersOut` visibly shorter than
       `layersIn` was?** (150 ms vs 300 ms — MD3 argues exits should be shorter than entrances,
       and a single `layers` entry could not express that.)
    4. Replay the inspector's `standard` row, then move a window (`windowsMove` uses
       `standard`). **Same question: same token, same visible render?**
    5. Run `~/.config/hypr/scripts/motion-switch.sh --curves legacy`, open and close a window,
       then run `--curves md3` and do the same. **Confirm the two feel audibly different** —
       this is not a taste verdict, it is proof that the measuring instrument actually
       measures something. If both feel identical, the toggle is not wired and the soak would
       be worthless.

    Approve if every fidelity answer is yes. Describe the specific mismatch if not — a fitted
    curve that renders differently in Hyprland than in QML is exactly what this catches, and
    D-07's per-namespace `layerrule` style override is the available remedy if one surface
    reads wrong.

    **On approval the soak clock starts.** From this point, use the desktop normally and keep
    a running note per motion (window open, window close, window move, workspace switch,
    notification appearance, layer entrance, fade). Plan 13-07 collects these into the D-20
    verdict table. The D-19 floor is stated there and is incompressible.
  </how-to-verify>
  <read_first>
    - quickshell/.config/quickshell/modules/Probe.qml (the replayable motion row this gate compares against)
    - .planning/phases/13-motion-retrofit-existing-surface-sweep/13-CONTEXT.md (D-17's fidelity/taste split; D-06; D-07; D-19)
  </read_first>
  <acceptance_criteria>
    - Each of the five numbered checks is answered individually in the plan SUMMARY — not one aggregate "looks fine"
    - Check 5's A/B comparison is recorded as a stated difference, proving the toggle is live before the soak depends on it
    - Any fidelity mismatch is recorded with the surface and token named, and either fixed in this plan or filed as a `layerrule` style override candidate under D-07
    - The SUMMARY records the date/time the soak clock started
  </acceptance_criteria>
  <resume-signal>Answer checks 1-5 individually, then type "approved" or describe the mismatch</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.local/state/theme/motion-curves` → `lib/motion.sh` | User-writable runtime state read by a renderer whose output Hyprland's config parser executes |
| `~/.local/state/theme/hyprland-motion.conf` → Hyprland | Generated text handed to a config parser with no readback for malformed input |
| `motion-switch.sh` argv → state file | CLI argument written to a file that reaches the compositor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | `theme_engine_read_motion_curves` | high | mitigate | Closed-`case` read accepting only `md3`/`legacy`, defaulting to `md3` — identical shape to `theme_engine_read_motion_scale`, proven by writing an out-of-set value and confirming it never reaches the emitted file (Task 3 acceptance criteria) |
| T-13-02 | Tampering | `motion-switch.sh --curves` argv | high | mitigate | Argument validated against `motion.json`'s real `curve_sets` keys with `grep -qx` before any write — same posture as the existing preset path, never a hardcoded enum and never raw interpolation |
| T-13-03 | Denial of Service | `hyprland-motion.conf` → Hyprland parser | high | mitigate | `theme_engine_validate_motion_values` extended to cover `indicators` and `curve_sets` and run BEFORE any write; a failed validation returns non-zero and leaves the live state dir byte-unchanged (atomic render-then-commit), proven by a deliberately broken `motion.json` |
| T-13-04 | Denial of Service | `animations.conf` referencing an unemitted name | high | mitigate | `Hyprland --verify-config` on a poisoned copy proven to fail loudly for both an unknown curve name and an undefined `$motion_speed_*` variable before the real file is trusted (Task 4 acceptance criteria) |
| T-13-05 | Information Disclosure | `GENERATE_LOG` / notification channel | low | accept | The new writer emits no user-controlled or secret data; the existing `head -c 200 | tr -d` sanitisation on the clamp WARN path is unchanged. Rationale: no new data crosses this boundary. |
</threat_model>

<verification>
- `Hyprland --verify-config` clean; `hyprctl reload` succeeds
- `hyprctl animations -j` reports 15 slots with `layersIn`/`layersOut` holding distinct speeds
- `~/.config/hypr/scripts/motion-lint` exits 0 with `animations.conf` unexempted
- `~/.config/theme-engine/theme-doctor` exits 0
- `~/.config/theme-engine/theme-parity` exits 0
- `motion-switch.sh --curves` flips the emitted curve aliases through one re-render
- Blocking human render gate (Task 5) approved with all five checks answered individually
</verification>

<success_criteria>
- MOTION-01 satisfied for the Hyprland surface: no hand-tuned one-off bezier and no raw duration remains in `animations.conf`
- The temporary A/B toggle is live and demonstrably changes the feel, so the soak verdict can be comparative
- The soak clock has started, with its start time recorded
- Plans 13-02, 13-03 and 13-04 can proceed immediately on files this plan does not touch
</success_criteria>

<output>
Create `.planning/phases/13-motion-retrofit-existing-surface-sweep/13-01-SUMMARY.md` when done
</output>
