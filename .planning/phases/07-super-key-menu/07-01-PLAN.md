---
phase: 07-super-key-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - elephant/.config/elephant/menus/main.toml
  - walker/.config/walker/config.toml
  - hypr/.config/hypr/scripts/elephant-restart.sh
  - stow.sh
autonomous: false
requirements: [MENU-01]
user_setup: []

must_haves:
  truths:
    - "D-05: The elephant `menus` provider is PROVEN on the installed elephant 2.21.0 binary to express submenus, leaf actions, and Nerd Font glyph-as-text BEFORE any real menu content is authored — or the bash + `walker --dmenu` fallback is adopted with evidence."
    - "D-06: Drill-down in place works — selecting a parent entry swaps the same walker window's list to the child menu, and Esc/Backspace goes back exactly one level (the mechanism for back-nav is empirically determined, not assumed)."
    - "D-07: A Nerd Font glyph rendered as plain entry `text` displays correctly in walker's list (no `icon` field, no icon-theme coupling)."
    - "D-08: The menu renders through the existing `walker-style.css` (contract target #18) with ZERO new contract targets and ZERO new parity fixtures — `theme-parity` is untouched and still green."
    - "D-34: Menu definitions are stow-managed repo content; `~/.config/elephant/menus/` exists only as a stow symlink tree, no host-only state."
    - "RESEARCH Pitfall 3 is handled: an explicit elephant-restart step exists, because elephant scans its menus dir ONCE at daemon startup and has no hot-reload."
  artifacts:
    - elephant/.config/elephant/menus/main.toml
    - hypr/.config/hypr/scripts/elephant-restart.sh
    - walker/.config/walker/config.toml
    - stow.sh
  key_links:
    - "walker's `[placeholders]` table ↔ the `menus` provider — carries 07-UI-SPEC's locked root placeholder copy now that `[sets.menu]` is dead (see SPIKE CORRECTION). The `menus:<name>` provider identifiers elephant registers are targeted directly by `walker -m menus:<name>`; there is no set allowlist to enumerate."
    - "`elephant-restart.sh` ↔ the running elephant daemon started by `autostart.conf`'s `exec-once = uwsm app -- elephant` — restart must not orphan `walker --gapplication-service`."
    - "`stow.sh` PACKAGES array ↔ the new `elephant/` stow package — without the array entry the menus dir is never created on a fresh install and elephant silently loads zero menus."
---

<objective>
Prove the elephant `menus` provider actually works on the installed binary (D-05's mandatory spike), then lay the engine foundation every other Phase 7 plan hangs off: a stow-managed `elephant/` package, the walker-side wiring to reach it, and an explicit elephant-restart step.

Purpose: D-05 exists because this project shipped dead hyprlock config in Phase 4 by trusting an installed binary's documented options without verifying them. Six submenus and the cheat-sheet all hang off this one choice. RESEARCH.md resolved the schema live but left two questions open — this plan closes them empirically before any content is written.
Output: A working `walker -m menus:main` surface showing a two-level test menu, plus the stow package and the restart script.
</objective>

<spike_correction status="RESOLVED — supersedes any conflicting text below">
**Task 1's D-05 spike ran and returned GO on the provider, but disproved the plan's chosen invocation flag. This block overrides every `[sets.menu]` / `walker -s menu` instruction in this file and in 07-RESEARCH.md / 07-PATTERNS.md.**

Proven live against walker 2.16.2 + elephant 2.21.0 on this machine (screenshot + PID evidence, reproduced 5×, then independently re-confirmed by the orchestrator):

- **GO — the `menus` provider works.** Submenus, leaf actions, in-place drill-down, and Nerd Font glyph-as-text all render correctly. D-05's `--dmenu` fallback is NOT needed. D-06 (Esc = back exactly one level; second Esc closes) and D-07 (glyph, not tofu) are both confirmed with no extra walker keybind config.
- **NO-GO — `walker -s <set>` is broken on this binary.** It panics the walker gapplication-service (`can't find specified set`, `src/data.rs:566`) and kills it. This is a latent walker 2.16.2 bug, NOT a defect in the spike's content: it reproduces against the pre-existing, untouched `[sets.runner]` block that this repo already ships. **`walker -s runner` — bound to Super+R in production today — is therefore already broken.** (07-02 fixes that bind; this plan removes the dead block.)
- **The replacement, verified working:** `-m/--provider` (exclusive-provider mode). `walker -m menus:main` opens a specific menu; `walker -m runner` is the working Super+R equivalent. The qualified `menus:<name>` form was verified to register (`elephant listproviders` → `menus:<name>`) and to run without crashing walker.

**Consequences for this plan:** there is no `[sets.menu]` block and no `providers` allowlist to populate — `-m` targets the provider directly, so RESEARCH Open Question 1 ("must the set enumerate every submenu?") is moot. `menus` is already present in `[providers] default` (config.toml line 20), so no provider registration is needed either. Task 3 keeps `elephant-restart.sh` unchanged and swaps its walker-config deliverable (see Task 3's amended body). No locked D-xx decision changes; the menu tree, keybinds, and UX are untouched.
</spike_correction>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-super-key-menu/07-CONTEXT.md
@.planning/phases/07-super-key-menu/07-RESEARCH.md
@.planning/phases/07-super-key-menu/07-PATTERNS.md
@.planning/phases/07-super-key-menu/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike the elephant menus provider and close RESEARCH Open Questions 1 and 2</name>
  <files>elephant/.config/elephant/menus/ (throwaway spike TOMLs, removed at task end), walker/.config/walker/config.toml (temporary [sets.spike] block, removed at task end)</files>
  <read_first>
    - `.planning/phases/07-super-key-menu/07-RESEARCH.md` — read "Pattern 2: elephant `menus` provider" (the live-verified TOML schema), "Common Pitfalls" Pitfall 3 (startup-only menu scan), and "Open Questions" 1 and 2 verbatim. These are the two questions this task must answer.
    - `.planning/phases/07-super-key-menu/07-PATTERNS.md` — the `elephant/.config/elephant/menus/*.toml` section (schema + directory-creation caveat + restart requirement).
    - `walker/.config/walker/config.toml` — the existing `[sets.runner]` block (the exact set pattern to mirror) and the `[providers] default` array which already lists `menus`.
    - `.planning/phases/05-light-mode-pipeline-theme-presets/05-CONTEXT.md` — the walker-dmenu exit-code-130 cancel pattern, needed only if the spike fails and the fallback is adopted.
  </read_first>
  <action>
Write a THROWAWAY two-level test menu directly into `~/.config/elephant/menus/` (not into the repo — this is a spike, and the real stow package lands in Task 2). Create `spike-root.toml` (`name = "spike-root"`, `name_pretty = "Spike"`, two entries: one leaf with an `actions` map running `notify-send`, one with `submenu = "spike-child"`) and `spike-child.toml` (`name = "spike-child"`, `parent = "spike-root"`, one leaf entry). Put a Nerd Font glyph codepoint from the 07-UI-SPEC table (e.g. U+F0AD `nf-fa-wrench`) in the leaf entry's `text` field as plain text — no `icon` key — to prove D-07's glyph-as-text approach renders.

Restart elephant (`pkill -x elephant` then `uwsm app -- elephant`, or via the script from Task 3 once it exists) and confirm `elephant listproviders` prints BOTH `menus:spike-root` and `menus:spike-child`.

Then answer the two open questions by direct observation, running walker against a temporary `[sets.spike]` block whose `providers = ["menus:spike-root"]` ONLY:
  - **Open Question 1:** select the `submenu` entry. Does walker's list swap to the child menu's entries even though `menus:spike-child` is NOT in the set's `providers` allowlist? If NO, retry with `providers = ["menus:spike-root", "menus:spike-child"]` and record that every submenu must be enumerated explicitly.
  - **Open Question 2:** from the child menu, press Esc, then Backspace. Does either navigate back to `spike-root` (the `menus:parent` action firing), or does Esc close walker entirely? Record which gesture, if any, gives D-06's one-level-back behaviour, and whether a walker keybind config addition is required.

Record BOTH answers as explicit findings in the plan SUMMARY. ~~carry the Open-Question-1 answer directly into Task 3's `[sets.menu]` `providers` array~~ — **superseded: Open Question 1 is MOOT (`-m` has no provider allowlist), and Task 3 no longer builds a `[sets.menu]` block. See `<spike_correction>`.**

If the spike shows the provider CANNOT express submenus or actions at all (contradicting RESEARCH.md's live verification), STOP and escalate: D-05's documented fallback is the bash + `walker --dmenu` chain using Phase 5's exit-code-130 cancel pattern, and adopting it changes the shape of plans 05/06/07/08. Do not silently improvise a hybrid.

Delete both spike TOML files and the temporary `[sets.spike]` block before finishing the task — the spike leaves no artifact except its recorded findings.
  </action>
  <verify>
    <automated>elephant listproviders | grep -q 'menus:spike-root' &amp;&amp; elephant listproviders | grep -q 'menus:spike-child'</automated>
  </verify>
  <acceptance_criteria>
    - `elephant listproviders` lists `menus:spike-root` and `menus:spike-child` after an elephant restart.
    - The SUMMARY records Open Question 1 as MOOT, with the reason: `walker -s <set>` panics walker 2.16.2, so there is no set and no `providers` allowlist; `-m menus:<name>` targets the provider directly. Drill-down was nonetheless observed working under `-m`.
    - The SUMMARY records a definitive answer to Open Question 2 naming the exact gesture (Esc / Backspace / an explicit walker keybind) that performs one-level-back navigation, or states that none does and names the workaround.
    - The SUMMARY states whether the Nerd Font glyph in the `text` field rendered as a glyph (not a tofu box) in walker.
    - `ls ~/.config/elephant/menus/` contains no file whose name starts with `spike-` after the task completes.
  </acceptance_criteria>
  <done>Both RESEARCH Open Questions are closed with observed evidence, D-05 is discharged GO (or the fallback is escalated), and no spike artifact remains.</done>
</task>

<task type="auto">
  <name>Task 2: Create the elephant stow package and register it in stow.sh</name>
  <files>elephant/.config/elephant/menus/main.toml, stow.sh</files>
  <read_first>
    - `stow.sh` — the `PACKAGES` array (lines 19-38) and the `chmod +x` block (line 81) which only targets `*.sh`.
    - `.planning/phases/07-super-key-menu/07-PATTERNS.md` — the directory-creation caveat: elephant's `ConfigDirs()` only scans `$XDG_CONFIG_HOME/elephant` if the directory ALREADY EXISTS (`os.Stat`); an absent dir yields zero menus loaded with no error.
    - `walker/` — the existing stow package layout, as the structural model for the new `elephant/` package.
  </read_first>
  <action>
Create a new stow package rooted at `elephant/` with the tree `elephant/.config/elephant/menus/`. Author `elephant/.config/elephant/menus/main.toml` as a MINIMAL placeholder root menu at this stage — `name = "main"`, `name_pretty = "Menu"`, and a single leaf entry pointing at `~/.config/hypr/scripts/wlogout.sh` (the D-19 Power delegation, the one root entry whose target already exists). The full six-entry root menu is authored in plan 07-05; this plan only proves the wiring end-to-end, so `main.toml` must exist and load but need not be complete.

Add `elephant` to `stow.sh`'s `PACKAGES` array, keeping the array's existing alphabetical ordering (between `theme-engine`'s neighbours — place it so the list stays sorted).

Do NOT add a `mkdir -p ~/.config/elephant` anywhere: stow creates the directory tree from the package's own symlink targets, and adding a second creation site would be exactly the duplicated-owner drift this project keeps removing.
  </action>
  <verify>
    <automated>test -f elephant/.config/elephant/menus/main.toml &amp;&amp; grep -qE '^\s*elephant\s*$' stow.sh &amp;&amp; bash -n stow.sh</automated>
  </verify>
  <acceptance_criteria>
    - `elephant/.config/elephant/menus/main.toml` exists in the repo and declares `name = "main"`.
    - `stow.sh`'s `PACKAGES` array contains an `elephant` entry (`grep -qE '^\s*elephant\s*$' stow.sh` succeeds).
    - After `./stow.sh`, `readlink -f ~/.config/elephant/menus/main.toml` resolves into the repo working tree (proving the symlink, not a host-only copy — D-34).
    - `elephant listproviders` prints `menus:main`.
    - `git status --porcelain` shows no untracked files under `~/.config/` (the state stays repo-owned).
  </acceptance_criteria>
  <done>A stow-managed `elephant/` package exists, stow creates `~/.config/elephant/menus/`, and elephant registers `menus:main`.</done>
</task>

<task type="auto">
  <name>Task 3: Author elephant-restart.sh, wire the menus placeholder, and remove the dead [sets.runner] block</name>
  <files>hypr/.config/hypr/scripts/elephant-restart.sh, walker/.config/walker/config.toml</files>
  <read_first>
    - **`<spike_correction>` at the top of this plan — READ IT FIRST. It overrides this task's original `[sets.menu]` deliverable. `walker -s <set>` panics walker 2.16.2; `[sets.*]` is a dead mechanism.**
    - `theme-engine/.config/theme-engine/lib/reload.sh` — the `pgrep`-guarded restart / `pkill -SIGUSR<n> <proc> 2>/dev/null || true` fan-out idiom this script must copy. This is the house pattern; there is no existing `walker-restart.sh` in the repo despite CONTEXT.md naming one.
    - `walker/.config/walker/config.toml` — the `[placeholders]` table (lines 10-12) is the structure to mirror; it is keyed by provider name. The `[sets.runner]` block (lines 50-52) is the DEAD block to delete.
    - `hypr/.config/hypr/config/autostart.conf` — lines 21-23 show elephant and `walker --gapplication-service` are BOTH started via `exec-once = uwsm app -- …`. Restarting elephant must not leave walker's service pointing at a dead socket.
    - `.planning/phases/07-super-key-menu/07-RESEARCH.md` — the CLAUDE.md/PROJECT.md warning that walker and elephant speak a private socket protocol and must be treated as two processes, not one.
  </read_first>
  <action>
Create `hypr/.config/hypr/scripts/elephant-restart.sh` (executable bit set in git). It must: `pkill -x elephant` guarded by `pgrep -x elephant` (best-effort, `2>/dev/null || true` per the house idiom), relaunch with `uwsm app -- elephant`, then restart `walker --gapplication-service` the same way so walker reconnects to the new elephant socket rather than a stale one. Add a short bounded readiness wait (poll `elephant listproviders` for success, capped at a few seconds) before returning, and exit nonzero with a `notify-send` error if elephant never comes back. Do NOT hardcode a bare `sleep` as the only readiness signal.

**AMENDED (see `<spike_correction>`): do NOT add a `[sets.menu]` block.** `-m menus:main` targets the provider directly, and `menus` is already listed in `[providers] default` (line 20), so no set and no provider allowlist are needed. Instead, make these two edits to `walker/.config/walker/config.toml`:

1. **Preserve 07-UI-SPEC's locked root-menu placeholder copy.** The `[sets.menu]` block was the original home for `placeholder = { input = "Search or select...", list = "No entries" }` (the exact strings locked by 07-UI-SPEC's Copywriting Contract — they are user-facing and must not be improvised). Their new home is walker's existing `[placeholders]` table (lines 10-12), which is keyed by provider name. **Verify the exact key form against the running binary before committing** — try `"menus"` and, if the qualified form is honoured, `"menus:main"` — and use whichever actually renders the copy. Confirm by launching `walker -m menus:main` and *reading the placeholder text that appears*. Do not assume; this is the same class of error the D-05 spike exists to catch. If NEITHER key form renders the locked copy, do not improvise different text and do not silently drop it — record the finding and escalate.

2. **Delete the now-dead `[sets.runner]` block (lines 49-52) and its `# ── Sets (launched via walker -s <name>) ──` banner.** `walker -s` panics this binary, so the block is not merely unused — it is a live trap for the next reader. Replace it with a short `#` comment recording that `[sets.*]`/`-s` is broken on walker 2.16.2 (panic at `src/data.rs:566`) and that exclusive-provider `-m` is the supported path, so nobody "restores" it later. (Plan 07-02 repoints Super+R's `$app_launcher_drun` from `walker -s runner` to `walker -m runner`; until it does, Super+R stays exactly as broken as it already is — this deletion introduces no new regression.)

Do not add any new matugen template, contract.json target, or walker stylesheet (D-08).
  </action>
  <verify>
    <automated>bash -n hypr/.config/hypr/scripts/elephant-restart.sh &amp;&amp; test -x hypr/.config/hypr/scripts/elephant-restart.sh &amp;&amp; ! grep -q '\[sets\.' walker/.config/walker/config.toml</automated>
  </verify>
  <acceptance_criteria>
    - `hypr/.config/hypr/scripts/elephant-restart.sh` exists, passes `bash -n`, passes `shellcheck`, and is committed with the executable bit set (`git ls-files -s` shows mode `100755`).
    - Running `~/.config/hypr/scripts/elephant-restart.sh` leaves exactly one `elephant` process running (`pgrep -c -x elephant` outputs `1`) and one `walker` service process.
    - After the restart, `elephant listproviders` succeeds and prints `menus:main`.
    - `walker/.config/walker/config.toml` contains NO `[sets.` block (`grep -c '\[sets\.'` outputs `0`) and carries the comment explaining why.
    - `walker -m menus:main` opens the menu AND displays the locked placeholder copy `Search or select...` — confirmed by looking at the running window, not by grepping the config file. The walker service is still alive afterwards (`pgrep -x walker` non-empty).
    - `theme-engine/.config/theme-engine/contract.json` is UNCHANGED (`git diff --exit-code theme-engine/.config/theme-engine/contract.json` succeeds) — D-08 mandates zero new render targets.
  </acceptance_criteria>
  <done>`elephant-restart.sh` reliably cycles the daemon pair, `walker -m menus:main` renders the menu with its locked placeholder copy, and the dead `[sets.*]` trap is gone from the config.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the menu engine renders live</name>
  <files>(none — human verification only; this task edits no files)</files>
  <action>Present the <what-built> summary to the human, walk them through every numbered step in <how-to-verify>, and BLOCK until the <resume-signal> is given. Do not edit any file in this task. Do not self-approve.</action>
  <what-built>
    A stow-managed `elephant/` package with a loading `menus:main` provider, an `elephant-restart.sh` daemon-cycling script, and the walker-side wiring to reach it (placeholder copy preserved; dead `[sets.*]` block removed). The D-05 spike returned GO on the `menus` provider and disproved `walker -s` — see this plan's `<spike_correction>`.
  </what-built>
  <how-to-verify>
    1. Run `~/.config/hypr/scripts/elephant-restart.sh` and confirm it exits 0.
    2. Run `walker -m menus:main` from a terminal. The walker window must open showing the placeholder `Search or select...` and the single placeholder root entry. (NOT `walker -s menu` — that flag panics walker 2.16.2; see `<spike_correction>`.)
    3. Confirm the entry's Nerd Font glyph renders as an actual glyph, not a tofu box (□).
    4. Confirm the menu window is themed identically to the normal walker launcher (same background, same selection accent) — this proves D-08's zero-new-stylesheet claim.
    5. Select the entry: wlogout should open. Press Esc there to dismiss it.
    6. Confirm `pgrep -x walker` still returns a PID after all of the above — i.e. the walker service survived, which is the whole point of moving off `-s`.
    7. Read the SUMMARY and confirm it records the spike's findings as definitive observed statements — including that `-s` panics the service and `-m` does not.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what rendered incorrectly (tofu glyphs, unthemed window, provider not found, walker died).</resume-signal>
  <verify><human-check>Human confirmed every step in <how-to-verify> and gave the resume signal.</human-check></verify>
  <done>The human explicitly approved via the resume signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo TOML → elephant `sh -c` | Menu action strings authored in-repo are executed by the elephant daemon via `sh -c`. The strings are static repo content under version control, not runtime user input. |
| elephant daemon ↔ walker frontend | A private Unix-socket protocol between two independently-versioned packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | elephant menu action `sh -c` execution | medium | mitigate | Every action string is static, repo-authored, version-controlled content. No action interpolates runtime input. No action contains `sudo`. elephant itself runs unprivileged under `uwsm app --` (confirmed via `autostart.conf`). |
| T-07-02 | Tampering | `~/.config/elephant/menus/` | low | mitigate | The directory is a stow symlink tree into the git-tracked repo — a host-side modification is visible as a broken/replaced symlink, and `git status` in the repo is the audit trail. No host-only menu state (D-34). |
| T-07-03 | Denial of Service | `elephant-restart.sh` killing the daemon pair | low | accept | Requires local shell access, which already implies full session control on a single-user desktop. The script's bounded readiness wait plus nonzero-exit-on-failure means a failed restart is loud, not silent. |
</threat_model>

<verification>
- `elephant listproviders` prints `menus:main`.
- `walker -m menus:main` opens a themed, glyph-rendering menu window AND the walker service is still alive afterwards.
- `walker/.config/walker/config.toml` contains no `[sets.` block.
- `theme-engine/.config/theme-engine/theme-parity` still passes across all 22 palettes (must be unaffected — D-08).
- `git diff --exit-code theme-engine/.config/theme-engine/contract.json` succeeds (zero new render targets).
- `shellcheck hypr/.config/hypr/scripts/elephant-restart.sh` reports no errors.
</verification>

<success_criteria>
- D-05 is closed: the `menus` provider is proven with recorded evidence, not assumed. (Closed GO — see `<spike_correction>`.)
- RESEARCH Open Question 2 (back-nav gesture) has a definitive recorded answer. Open Question 1 (set provider allowlist) is recorded as MOOT — `-m` has no allowlist.
- `walker -m menus:main` is a working, themed invocation backed by a stow-managed menu tree, and it does not kill the walker service.
- `elephant-restart.sh` exists and is the single owner of the daemon-cycling step every later plan will call.
- Zero new contract targets, zero new parity fixtures, zero new stylesheets.
</success_criteria>

## Artifacts this phase produces

Symbols created by THIS plan (new — exclude from drift/source-grounding checks):

| Artifact | Kind | Notes |
|---|---|---|
| `elephant/` | stow package | New package root; creates `~/.config/elephant/` |
| `elephant/.config/elephant/menus/main.toml` | menu TOML | Placeholder root; completed in 07-05 |
| `menus:main` | elephant provider id | Registered by the TOML's `name = "main"` |
| `hypr/.config/hypr/scripts/elephant-restart.sh` | script | New; no prior analog in repo |
| `[placeholders]` `menus` entry | walker config row | New row in the EXISTING `[placeholders]` table; carries 07-UI-SPEC's locked root placeholder copy |
| `walker -m menus:main` | invocation | Exclusive-provider mode. Replaces the planned `[sets.menu]`/`walker -s menu`, which panics walker 2.16.2 — see `<spike_correction>` |
| `elephant` (PACKAGES entry) | stow.sh array entry | New |

<output>
Create `.planning/phases/07-super-key-menu/07-01-SUMMARY.md` when done. It MUST record the Open Question 1 and Open Question 2 answers verbatim — plans 07-05, 07-06, 07-07 and 07-08 all read them.
</output>
