---
phase: 07-super-key-menu
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
autonomous: false
requirements: [MENU-03, MENU-04]
user_setup: []

must_haves:
  truths:
    - "D-25: Steam installs on a genuinely fresh system — `install.sh` explicitly and idempotently enables the `[multilib]` repo in `/etc/pacman.conf` before the pacman install runs. This is real new territory: the repo has ZERO existing pacman.conf handling."
    - "D-33: All ~10 new packages land in `install.sh`. Official repos are used wherever they exist. Every AUR package passes a human package-legitimacy check BEFORE `install.sh` gains it. RESEARCH corrects CONTEXT.md: `protonup-qt` is AUR-only, not official-repo — it falls under the human gate too."
    - "D-23: `install.sh` installs and ENABLES ollama but pulls NO model. A multi-GB model download would wreck the unattended container gate and fresh-install time."
    - "D-22: Local models stop at ollama + a TUI client. open-webui is not installed. `aichat` (official `extra` repo, Rust) is the chosen TUI client over AUR-only `oterm`, per D-33's official-repo preference."
    - "D-18: The bluetooth manager is `blueman` (official `extra`) rather than AUR-only `overskride` — D-33's official-repo preference is decisive. `windowrules.conf` already carries a `float-blueman` rule for `blueman-manager`."
    - "D-34: The unattended container gate stays green. Nothing added here is interactive or network-heavy: no model pull, no prompts, every pacman/paru call carries `--noconfirm`."
  artifacts:
    - install.sh
  key_links:
    - "`[multilib]` in `/etc/pacman.conf` ↔ the `steam` entry in `PACMAN_PKGS` — without multilib enabled BEFORE the pacman install line, `sudo pacman -S steam` fails and the whole `--core-only` run aborts under `set -e`."
    - "`install.sh`'s `VERIFY_PKGS` post-install check (line ~446) ↔ every package added here — it asserts each package is actually installed, so a package name typo fails the gate loudly rather than silently."
    - "ollama's default listen address (127.0.0.1:11434) ↔ `aichat`'s OpenAI-compatible endpoint config — the service must stay localhost-bound; `OLLAMA_HOST` must NOT be set to 0.0.0.0."
---

<objective>
Make every package this phase's AI dashboard (MENU-03) and Game center (MENU-04) depends on reproducible from a fresh `install.sh` run — including the multilib enablement Steam requires, which the repo has never handled.

Purpose: The menu entries authored in plans 07-06 and 07-07 launch programs that do not exist on this machine today. Reproducibility is a hard project constraint ("everything must be installable on a fresh Arch system via install.sh + stow — no manual host-only state"). RESEARCH.md confirmed multilib is entirely absent from `install.sh` while being ALREADY enabled on this dev box — the exact configuration that silently masks the gap during local testing and breaks every fresh install.
Output: `install.sh` gains 8 official-repo packages, 2 human-verified AUR packages, an idempotent multilib enablement step, and an ollama service enable with no model pull.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-super-key-menu/07-CONTEXT.md
@.planning/phases/07-super-key-menu/07-RESEARCH.md
@.planning/phases/07-super-key-menu/07-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Human package-legitimacy gate for the two AUR packages (D-33)</name>
  <files>(none — human verification only; this task edits no files)</files>
  <action>Present the <what-built> summary to the human, walk them through every numbered step in <how-to-verify>, and BLOCK until the <resume-signal> is given. Do not edit any file in this task. Do not self-approve.</action>
  <what-built>
    NOTHING YET — this is D-33's mandatory package-legitimacy gate, and it must clear BEFORE `install.sh` gains either AUR package. This is the established Phase 4 / Phase 6 precedent (Phase 6 gated `tela-icon-theme`, `colloid-icon-theme-git`, `papirus-folders` the same way).

    RESEARCH.md's Package Legitimacy Audit already did the verification legwork and returned `OK (AUR)` for both, with **zero [SUS] and zero [SLOP] verdicts**. The human gate remains required by project policy regardless — it is a blanket policy, not a response to a red flag.

    **Two AUR packages requiring approval:**

    | Package | Version | AUR upstream | Project homepage |
    |---|---|---|---|
    | `heroic-games-launcher-bin` | 2.22.0-1 | github.com/Heroic-Games-Launcher/HeroicGamesLauncher | heroicgameslauncher.com |
    | `protonup-qt` | 2.15.1-1 | github.com/DavidoTek/ProtonUp-Qt | davidotek.github.io/protonup-qt |

    **Correction to CONTEXT.md that you should know about:** D-25/D-33 assumed `protonup-qt` was an official-repo package. RESEARCH.md verified live via `paru -Si` that it is **AUR-only**. It therefore falls under this human gate, which the original decision did not anticipate.

    All 8 other new packages (`steam`, `lutris`, `ollama`, `aichat`, `gamemode`, `mangohud`, `nwg-displays`, `blueman`) are official-repo (`extra`/`multilib`) and need no gate.
  </what-built>
  <how-to-verify>
    1. Visit `https://aur.archlinux.org/packages/heroic-games-launcher-bin`. Confirm: the package exists, the upstream URL matches `heroicgameslauncher.com`, the maintainer is not obviously abandoned, and the vote count / last-updated date look like a real, maintained package rather than a typosquat.
    2. Visit `https://aur.archlinux.org/packages/protonup-qt`. Confirm the same against `github.com/DavidoTek/ProtonUp-Qt`.
    3. Optionally inspect the PKGBUILDs (`paru -Gp heroic-games-launcher-bin` / `paru -Gp protonup-qt`) and confirm the `source=` URLs point at those upstreams and nowhere else.
    4. Confirm you accept both as dependencies of your fresh-install script.
  </how-to-verify>
  <resume-signal>Type "approved" to allow both AUR packages into `install.sh`, or name any package you want dropped (dropping Heroic removes the Heroic menu entry from 07-07; dropping ProtonUp-Qt removes that entry — both are Game Center leaf entries, not load-bearing for the phase). This checkpoint is never auto-approvable regardless of `auto_advance`.</resume-signal>
  <verify><human-check>Human confirmed every step in <how-to-verify> and gave the resume signal.</human-check></verify>
  <done>The human explicitly approved via the resume signal.</done>
</task>

<task type="auto">
  <name>Task 1: Enable multilib idempotently in section_core_rice (D-25, Pitfall 2)</name>
  <files>install.sh</files>
  <read_first>
    - `install.sh` — read `section_core_rice()` in full (starts line 244). Note the ordering: `reflector` mirror sync → `pacman -Syu` → AUR-helper bootstrap → `sudo pacman -Sy --needed --noconfirm "${PACMAN_PKGS[@]}"` (line 285) → AUR installs (line 289). The multilib step must land BEFORE line 285.
    - `install.sh` — the D-59 comment block at lines 252-255: "strictly zero prompts — every pacman/paru invocation in this script must carry `--noconfirm`". Your new code must honour this.
    - `.planning/phases/07-super-key-menu/07-RESEARCH.md` — "Pitfall 2: Steam installed without multilib enabled" in full, including the note that multilib IS already enabled on this dev machine and will therefore silently mask the gap during local testing.
    - `.planning/phases/07-super-key-menu/07-PATTERNS.md` — the `install.sh` section's "Multilib enablement (Pitfall 2, no existing analog — new territory)" paragraph. There is ZERO existing pacman.conf-editing code anywhere in this repo; author fresh, following the general check-before-write discipline, not a literal copy.
    - `verify/container-run.sh` — confirm how `install.sh --core-only` is invoked (as `su - builder`, so `sudo` is available). This is the gate that must stay green (D-34).
  </read_first>
  <action>
Add an idempotent multilib-enablement step to `section_core_rice()`, placed AFTER the `pacman -Syu` mirror-sync block and BEFORE the `sudo pacman -Sy --needed --noconfirm "${PACMAN_PKGS[@]}"` line. Put it in `section_core_rice` and NOT `section_hardware` — the container gate runs `--core-only`, and this is a config-file-state gap that WILL reproduce in a container, so it must be caught there (Pitfall 2's explicit mandate).

Behaviour:
- Check first whether `[multilib]` is already an ACTIVE (uncommented) section in `/etc/pacman.conf`. Detect this by testing for an uncommented `[multilib]` header, e.g. `grep -q '^\[multilib\]' /etc/pacman.conf`. If it is already active, print a short "already enabled" line and skip — this makes re-running `install.sh` a no-op, which matters because this dev machine already has it on.
- If it is present but commented out (the Arch default: `#[multilib]` followed by `#Include = /etc/pacman.d/mirrorlist`), uncomment both lines in place with `sudo sed -i`. Anchor the sed to the commented multilib header and its immediately-following Include line — do NOT blanket-uncomment every `#Include` in the file, which would also enable any other commented repo.
- If neither an active nor a commented `[multilib]` section exists at all, append the two-line stanza to the end of `/etc/pacman.conf` with `sudo tee -a`.
- Then run `sudo pacman -Sy --noconfirm` so the newly-enabled repo's database is synced before the package install reads it. Without this sync, `pacman -S steam` still fails with "target not found".

Everything must be non-interactive (`--noconfirm`, no prompts) — D-34 and install.sh's own D-59 zero-prompts rule.

Add a comment block above the step recording WHY it exists: multilib is required for Steam, the repo had zero pacman.conf handling before this, and this dev machine already has multilib enabled which is exactly what would mask the gap in local testing. A future reader must not delete this as redundant.

Do not touch `/etc/pacman.conf`'s `ParallelDownloads`, `Color`, or any other option — this step owns the `[multilib]` section and nothing else.
  </action>
  <verify>
    <automated>bash -n install.sh &amp;&amp; shellcheck -S error install.sh &amp;&amp; grep -q 'multilib' install.sh</automated>
  </verify>
  <acceptance_criteria>
    - `install.sh` passes `bash -n` and `shellcheck -S error`.
    - The multilib block appears in `section_core_rice()` at a line number LESS than the `sudo pacman -Sy --needed --noconfirm "${PACMAN_PKGS[@]}"` line. Verify with: `awk '/multilib/{m=NR} /pacman -Sy --needed --noconfirm "\$\{PACMAN_PKGS/{p=NR} END{exit !(m>0 && m<p)}' install.sh`.
    - Idempotency: running the multilib block's logic twice against a `/etc/pacman.conf` that already has an active `[multilib]` produces no second `[multilib]` stanza. Verify by counting: after two runs, `grep -c '^\[multilib\]' /etc/pacman.conf` outputs `1`.
    - The sed does not blanket-uncomment other repos: after the step, any `[testing]`/`[multilib-testing]` sections that were commented in `/etc/pacman.conf` remain commented.
    - `sudo pacman -Sy --noconfirm` follows the enablement in the same block.
    - No prompt is emitted: every pacman invocation added carries `--noconfirm`.
  </acceptance_criteria>
  <done>A fresh Arch system running `install.sh --core-only` has multilib enabled and synced before `steam` is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 10 packages to PACMAN_PKGS / AUR_PKGS and enable ollama with no model pull</name>
  <files>install.sh</files>
  <read_first>
    - `install.sh` — the `PACMAN_PKGS` array (lines 52-177). Note `pavucontrol` is ALREADY present at line 97 — do NOT re-add it. Note the existing grouped `# comment` banner convention (e.g. line 139's "Screenshots, screen recording, OSD, utilities (D-03/D-16/D-18/D-23 — …)" banner).
    - `install.sh` — the `AUR_PKGS` array (lines 188-237), and specifically the icon-theme block's comment convention at lines 231-236 which records the human-verify checkpoint and a package-name gotcha. Copy that comment style.
    - `install.sh` — the services block around lines 324-342, particularly the swayosd precedent: `sudo systemctl enable --now swayosd-libinput-backend.service || echo "  ⚠ swayosd-libinput-backend enable failed" >&2` — a non-silenced, non-fatal enable. This is the exact shape the ollama enable must copy so the container gate (where systemd is not PID 1) does not abort under `set -e`.
    - `.planning/phases/07-super-key-menu/07-RESEARCH.md` — the "Supporting (new packages this phase)" table with every verified version and repo, and the Package Legitimacy Audit table.
    - `.planning/phases/07-super-key-menu/07-PATTERNS.md` — the `install.sh` section's package-placement pattern.
  </read_first>
  <action>
**PACMAN_PKGS additions (8 packages, all official repo, all live-verified by RESEARCH.md via `pacman -Si`).** Append a new grouped block with a `# ── … ──` banner in the file's own style, citing the decisions: `steam` (multilib — add an inline comment pointing at the multilib enablement step from Task 1), `lutris`, `ollama`, `aichat`, `gamemode`, `mangohud`, `nwg-displays`, `blueman`.

Do NOT add `pavucontrol` — it is already at line 97. Re-adding it would be a duplicate array entry.

Record the discretionary picks in the banner comment so a future reader knows they were deliberate: `aichat` chosen over AUR-only `oterm` for D-22 (official repo, Rust, multi-provider, speaks Ollama's OpenAI-compatible endpoint at localhost:11434/v1); `blueman` chosen over AUR-only `overskride` for D-18 (official repo, and `windowrules.conf` already carries a `float-blueman` rule for the `blueman-manager` class).

**AUR_PKGS additions (2 packages, both approved at the checkpoint above).** Append `heroic-games-launcher-bin` and `protonup-qt` with a comment block in the icon-theme block's style, recording: the D-33 human-verify checkpoint was cleared, and that `protonup-qt` is AUR-only — correcting CONTEXT.md D-25's assumption that it was an official-repo package. That correction note matters: without it, a future reader may "fix" it back into PACMAN_PKGS where it does not exist and the install will break.

**Ollama service enable (D-23).** Add `sudo systemctl enable --now ollama.service` to the services block, following the swayosd precedent's exact non-fatal, non-silenced shape (`|| echo "  ⚠ ollama enable failed" >&2`). This keeps the container gate green when systemd is not PID 1, without hiding a real failure on bare metal.

**Pull NO model.** There must be no `ollama pull`, no `ollama run`, and no model name anywhere in `install.sh` (D-23/D-34 — a multi-GB download would wreck the unattended gate and fresh-install time). Model acquisition is a manual user step, surfaced to the user by the UI-SPEC's "No Model Installed" notification copy which plan 07-06 implements.

**Do not set `OLLAMA_HOST`.** Ollama's default bind is 127.0.0.1:11434 (localhost-only). Setting it to `0.0.0.0` would expose an unauthenticated model-inference API to the local network. Leave the default; this is the T-07-09 mitigation below.
  </action>
  <verify>
    <automated>bash -n install.sh &amp;&amp; for p in steam lutris ollama aichat gamemode mangohud nwg-displays blueman heroic-games-launcher-bin protonup-qt; do grep -qE "^[[:space:]]+${p}([[:space:]]*#.*)?$" install.sh || { echo "MISSING: $p"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 10 package names appear as array entries in `install.sh` — the verify loop above asserts each one individually and names any that is missing.
    - `steam`, `lutris`, `ollama`, `aichat`, `gamemode`, `mangohud`, `nwg-displays`, `blueman` are in `PACMAN_PKGS`; `heroic-games-launcher-bin` and `protonup-qt` are in `AUR_PKGS`. Verify each is in the correct array by checking its line number falls within that array's line range.
    - `pavucontrol` appears EXACTLY ONCE in `install.sh` (`grep -c '^\s*pavucontrol\s*$' install.sh` outputs `1`) — no duplicate entry was introduced.
    - No model pull: `grep -nE 'ollama (pull|run)' install.sh` returns no match.
    - No network exposure: `grep -n 'OLLAMA_HOST' install.sh` returns no match.
    - The ollama enable is non-fatal: the line contains `systemctl enable --now ollama.service` followed by a `|| echo` fallback, matching the swayosd precedent. Confirm the script does not abort under `set -e` when the enable fails.
    - `install.sh` passes `bash -n` and `shellcheck -S error`.
  </acceptance_criteria>
  <done>All 10 packages are reproducibly installed by `install.sh`, ollama is enabled and localhost-bound, and no model is pulled.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the container gate stays green with the new packages and multilib (D-34)</name>
  <files>(no file edits — runs verify/container-run.sh against the changes from Tasks 1-2)</files>
  <read_first>
    - `verify/container-run.sh` — the full gate. Note it clones from a real remote and runs `install.sh --core-only` as `su - builder`, then `stow.sh`, then `theme-doctor`. Exit 0 only if all three succeed.
    - `install.sh` — the `VERIFY_PKGS=("${PACMAN_PKGS[@]}" "${AUR_PKGS[@]}")` post-install verification block (line ~446). This asserts every listed package is actually installed, so a wrong package name fails loudly here rather than silently.
    - `.planning/phases/07-super-key-menu/07-RESEARCH.md` — Pitfall 2's closing note: this is a config-file-state gap, not a hardware gap, so it WILL reproduce in a container — catch it there.
  </read_first>
  <action>
Run the existing container gate end-to-end: `verify/container-run.sh`. It must exit 0 with the new packages and the multilib step in place.

This is the D-34 proof. The gate is what distinguishes "works on this dev box, where multilib happened to already be enabled" from "works on a fresh Arch system". RESEARCH.md flagged exactly this: multilib IS already on in this machine's `/etc/pacman.conf`, which would silently mask the missing enablement during any local-only test.

If the gate fails, diagnose from `verify/logs/<run>/03-install.log` — the two most likely failures are (a) `steam` reporting "target not found", meaning the multilib enablement ran too late or the `pacman -Sy` after it was omitted; (b) a package-name typo caught by the `VERIFY_PKGS` check.

Do NOT weaken the gate, add a skip, or move a package out of `--core-only` to make it pass. If a package genuinely cannot install unattended, that is a finding to surface, not to route around — D-34 is a locked decision.

Record the passing run's log directory in the SUMMARY.
  </action>
  <verify>
    <automated>verify/container-run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `verify/container-run.sh` exits 0.
    - Its log shows `install.sh --core-only` completing, including the multilib enablement line and a successful `steam` install.
    - The `VERIFY_PKGS` post-install check passes for all 10 new packages.
    - The gate ran fully unattended — no prompt appears anywhere in `verify/logs/<run>/03-install.log` (grep the log for `[Y/n]` and `[y/N]` and confirm no match).
    - No `ollama pull` ran and no model was downloaded (the gate's runtime does not balloon; compare wall-clock against the previous recorded run).
  </acceptance_criteria>
  <done>A fresh, unattended container install succeeds with multilib, Steam, and all 10 new packages — reproducibility is proven, not assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AUR (third-party PKGBUILDs) → local system | AUR packages are arbitrary user-submitted build scripts executed with build-time privileges. This is the phase's single genuine supply-chain surface. |
| `install.sh` → `/etc/pacman.conf` (root-owned system config) | A privileged, system-wide mutation via `sudo`. New territory for this repo — no existing pacman.conf handling exists to model on. |
| ollama daemon → local network | The service listens on a TCP port and serves an unauthenticated inference API to whatever address it binds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | AUR packages `heroic-games-launcher-bin`, `protonup-qt` | high | mitigate | Blocking human package-legitimacy checkpoint (D-33) BEFORE `install.sh` gains either package — never auto-approvable regardless of `auto_advance`. RESEARCH.md's Package Legitimacy Audit verified both against their upstreams via `paru -Si`: zero [SUS], zero [SLOP]. The human still verifies AUR page + upstream URL match before approval. |
| T-07-08 | Tampering / Elevation of Privilege | `install.sh` editing `/etc/pacman.conf` with `sudo` | high | mitigate | The edit is narrowly scoped to the `[multilib]` stanza and its single `Include` line, anchored by an explicit sed pattern — never a blanket `#Include` uncomment, which would silently enable `[testing]`/`[multilib-testing]` too. Check-before-write makes it idempotent. No user-supplied or runtime-derived value is interpolated into the sed. Acceptance criteria assert both the idempotency and the no-other-repos-enabled property. |
| T-07-09 | Information Disclosure | ollama service listening on a network port | medium | mitigate | Ollama's default bind is 127.0.0.1:11434 (loopback only). `OLLAMA_HOST` is explicitly NOT set anywhere in `install.sh` — setting it to `0.0.0.0` would expose an unauthenticated inference API to the LAN. Asserted by a grep acceptance criterion. D-23's no-model-pull also means the service has nothing to serve until the user deliberately pulls one. |
| T-07-10 | Denial of Service | unattended container gate broken by an interactive/heavy install step | medium | mitigate | D-34 is enforced by running the real gate as a task, not by assertion. Every added pacman/paru call carries `--noconfirm` (install.sh's own D-59 rule). No model pull. The prompt-free property is asserted by grepping the gate's install log for confirmation prompts. |
</threat_model>

<verification>
- `verify/container-run.sh` exits 0 — a fresh unattended install succeeds with all 10 packages and multilib.
- `install.sh` passes `bash -n` and `shellcheck -S error`.
- `grep -cE 'ollama (pull|run)' install.sh` finds nothing (D-23).
- `grep -c 'OLLAMA_HOST' install.sh` finds nothing (T-07-09).
- `pavucontrol` appears exactly once (no duplicate).
- The multilib block precedes the `PACMAN_PKGS` install line in `section_core_rice()`.
</verification>

<success_criteria>
- MENU-03's and MENU-04's programs are reproducible from a fresh `install.sh` run.
- Steam installs on a fresh system because multilib is explicitly, idempotently enabled — closing a real gap that this dev machine's pre-enabled multilib was masking.
- Both AUR packages passed a blocking human legitimacy check before entering `install.sh`.
- ollama is enabled, localhost-bound, and pulls no model.
- The unattended container gate is green.
</success_criteria>

## Artifacts this phase produces

Symbols created by THIS plan (new — exclude from drift/source-grounding checks):

| Artifact | Kind | Notes |
|---|---|---|
| `steam`, `lutris`, `ollama`, `aichat`, `gamemode`, `mangohud`, `nwg-displays`, `blueman` | PACMAN_PKGS entries | New official-repo package entries |
| `heroic-games-launcher-bin`, `protonup-qt` | AUR_PKGS entries | New; human-verified per D-33 |
| multilib enablement block | install.sh code | New; zero prior pacman.conf handling in repo |
| `sudo systemctl enable --now ollama.service` | install.sh service enable | New |

<output>
Create `.planning/phases/07-super-key-menu/07-03-SUMMARY.md` when done. Record the container-gate log directory and the human checkpoint's approval outcome.
</output>