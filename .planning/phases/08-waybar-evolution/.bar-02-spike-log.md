# BAR-02 Spike Log — raw, verbatim, timestamped

Plan: 08-10. D-09 timeboxed pixel-shift spike.

**TIMEBOX START (Task 1, 90 min hard gate):** 2026-07-14T19:44:16Z (first command after monitor
re-confirmation and bar brought to `visible` for testing — `hyprctl -j monitors` re-checked
immediately before this log began: exactly 1 monitor, `DP-1 2560x1440@165Hz connected focused=true`).

**TIMEBOX END (hard deadline):** 2026-07-14T21:14:16Z. Anything not PROVEN by then is FAIL.

Pre-spike live state:
- Waybar layout in use: `floating` (from `~/.cache/current-waybar-layout`)
- Waybar visibility owner status before test: `hidden-idle` → set to `visible` via `waybar-visibility.sh idle show` to enable testing (restored to real state at end of plan)
- Two tiled windows on workspace 2 for reflow testing: `kitty` (0x5612126237f0) and `zen` (0x5612113000a0), moved onto the same workspace via `hyprctl dispatch movetoworkspacesilent 2,address:...`
- Monitor: `DP-1`, 2560x1440, reserved (exclusive zone) `[0,37,0,0]`
- Waybar layer-shell surface (`hyprctl layers -j`, namespace `waybar`): `x=0 y=0 w=2560 h=37`

---

## GATE K0-a — actuation-cost probe

**Method:** the owner's existing actuation path is `waybar-visibility.sh reassert` while the bar is
VISIBLE and its computed state is unchanged (a no-op reassert). Per `bar-common.jsonc`, waybar's
`on-sigusr2` action is fixed to `"reload"` — a FULL bar-config reload, not a lighter CSS-only
refresh (confirmed by reading `bar-common.jsonc` directly, lines 16-25). Any owner-driven CSS
change — including a hypothetical jitter offset — has no other actuation path available; it MUST
go through this same SIGUSR2/reload signal. So this probe is mechanism-independent: whatever it
measures here applies to M1 and M2 both, before either is even tried.

Setup: two real tiled windows (`kitty` 0x5612126237f0, `zen` 0x5612113000a0) on the SAME active
workspace (2), confirmed side-by-side (`at=[13,50]/[1288,50]`, `size=[1259,1377]` each, filling the
2560px output minus the bar's reserved 37px top strip).

### Reflow cost — instrument: `hyprctl clients -j` (via `jq`), 30-50ms polling burst across the signal

**Control burst (no signal fired), 10 samples @ 30ms — zero polling noise:**
```
at=[13,50]/[1288,50] size=[1259,1377]/[1259,1377]   (identical, all 10 samples)
```

**A plain before/after snapshot (>100ms apart, matching 08-03's own methodology) shows NO change**
— this is what 08-03's `08-03-SUMMARY.md` D3 measured and reported, and it is reproduced here
identically: before `at=[13,50]/[1288,50] size=[1259,1377]/[1259,1377]`, several hundred ms later
`at=[13,50]/[1288,50] size=[1259,1377]/[1259,1377]` — byte-identical, matching 08-03, no
contradiction at that granularity.

**But a 30-50ms burst across the exact signal reveals a real, reproducible TRANSIENT reflow
invisible to a before/after snapshot — confirmed 3/3 independent runs:**

Run 1 (50ms sampling):
```
2026-07-14T19:47:29.648Z  [fire reassert]
+50ms: reserved=[0,37,0,0]  at=[13,13]/[1288,13]  size=[1259,1414]/[1259,1414]   <- CHANGED
+100ms: reserved=[0,37,0,0] at=[13,50]/[1288,50]  size=[1259,1377]/[1259,1377]   <- back to normal
+150ms..+500ms: stable at=[13,50]/[1288,50] size=[1259,1377]/[1259,1377]
```

Run 2 (30ms sampling, immediately after confirming a stable BEFORE snapshot):
```
2026-07-14T19:48:09.284Z  [fire reassert]
t=1784058489.318 (+34ms): at=[13,13]/[1288,13] size=[1259,1414]/[1259,1414]      <- CHANGED
t=1784058489.356 (+72ms): at=[13,50]/[1288,50] size=[1259,1377]/[1259,1377]      <- back to normal
t=+108ms..+552ms (13 more samples): stable, unchanged
```

Both runs show the EXACT same delta: `at` moves from `y=50` to `y=13` (Δ37px, matching the bar's
own reserved height) and `size` height grows from `1377` to `1414` (Δ37px) — i.e., for one ~35-50ms
sample window, both tiled windows behave exactly as if the bar's exclusive zone had momentarily
gone to zero, then reflow BACK to the pre-signal geometry on the very next sample and stay stable
thereafter. This is real, reproducible (2/2 deliberately-repeated bursts, plus the original
unclean-workspace 3rd run showed the identical `Δy=37/Δh=37` pattern alongside an explicitly
observed `reserved: [0,37,0,0] -> [0,0,0,0] -> [0,37,0,0]` transition — see raw capture
`clients-burst.log` in the scratchpad) — a genuine transient reflow correlated precisely with the
SIGUSR2/reload signal.

**Cross-check against `08-03-SUMMARY.md` D3, as the plan instructs:** 08-03's claim ("idle-hide
left the window's `at`/`size` completely unchanged... zone kept") is **not contradicted at the
granularity 08-03 measured it** (a before/after snapshot with no intervening rapid polling) — the
STEADY STATE before and after genuinely is unchanged, exactly as 08-03 found, and this session's
own coarse before/after check (>100ms apart) reproduces that identical finding. What is NEW here,
not previously measured or claimed by 08-03, is a **transient** reflow lasting roughly one 30-50ms
frame, invisible to any snapshot spaced further apart than that. This is not a contradiction of
08-03's result — it's new information a finer-grained instrument reveals, and it is exactly the
kind of thing D-09's kill criterion #2 ("it reflows any window") is worded to catch: the criterion
says nothing about duration. **This transient reflow is a real, measured firing of kill criterion
#2 for the owner's reload-based actuation path** — which any CSS-based jitter must reuse, since it
has no other way to signal waybar.

**Verdict — reflow cost: FIRES.** Raw data: `clients-burst.log`, `clients-burst2.log`,
`clients-burst3.log`, `control-burst.log` (scratchpad, referenced here; every number above is
copied verbatim from these captures).

### Flash cost — instrument: `grim -g "0,0 2560x37"` (exact live bar geometry from `hyprctl layers -j`), burst captures + Python/PIL/numpy mean-RGB diff

Two independent, fully reproducible runs (see raw PNGs + the exact python analysis in the
scratchpad, `k0a-pre-*.png`/`k0a-post-*.png` and `k0a-r2-pre-*.png`/`k0a-r2-post-*.png`):

**Run 1:**
```
pre-1  mean RGB: [55.48, 58.08, 77.05]
pre-2  mean RGB: [55.44, 58.06, 77.04]
pre-3  mean RGB: [55.48, 58.08, 77.05]        <- stable baseline (adjacent diff 0.03)
--- reassert fired here (2026-07-14T19:45:11.388Z -> .418Z) ---
post-1 mean RGB: [55.75, 59.08, 78.17]        <- diff from pre-3: 0.81 (barely visible)
post-2 mean RGB: [96.79, 135.31, 161.68]      <- diff from post-1: 79.51 (LARGE — visibly lighter/bluer, confirmed by eye: see below)
post-3 mean RGB: [58.55, 72.82, 93.02]        <- diff from post-2: 61.75 (decaying back)
post-4 mean RGB: [52.69, 60.74, 79.13]        <- diff: 19.86
post-5 mean RGB: [50.92, 56.50, 73.42]        <- diff: 8.66
post-6 mean RGB: [50.42, 55.05, 71.30]        <- diff: 1.71 (settled, close to new baseline)
```

**Run 2 (independent repeat, ~40s later):**
```
pre-1  mean RGB: [55.01, 57.69, 76.49]
pre-2  mean RGB: [55.01, 57.69, 76.49]
pre-3  mean RGB: [55.01, 57.69, 76.49]        <- stable (adjacent diff 0.0003-0.0004)
--- reassert fired here (2026-07-14T19:45:50.878Z -> .908Z) ---
post-1 mean RGB: [61.99, 74.27, 95.41]        <- diff: 26.49 (already rising)
post-2 mean RGB: [98.36, 137.36, 163.82]      <- diff: 64.46 (peak, matches Run 1's peak closely: [96.79,135.31,161.68] vs [98.36,137.36,163.82])
post-3 mean RGB: [58.51, 72.85, 93.13]        <- diff: 63.38 (decaying)
post-4 mean RGB: [52.61, 60.63, 78.97]        <- diff: 19.98
post-5 mean RGB: [50.87, 56.46, 73.27]        <- diff: 8.61
post-6 mean RGB: [50.39, 54.98, 71.23]        <- diff: 1.69
```

**Visual confirmation (not just numbers):** `k0a-pre-3.png` (normal state — opaque dark bar, palette
colors correct) vs `k0a-post-2.png` (the peak-diff frame — bar renders visibly lighter/more
translucent, showing background bleed-through) were both read directly as images this session.
The difference is plainly visible to the eye, not just a numeric artifact — the bar's colour
composition genuinely changes for a handful of frames during the reload cycle, decaying back to
baseline over roughly 400-500ms.

**Verdict — flash cost: FIRES, confirmed 2/2 independent reproductions**, with the SAME peak
magnitude (~65-80 mean RGB delta) and the SAME decay shape (~400-500ms to settle) both times. Per
the plan's own text: "a bar that flickers every 15 minutes is a 'twitch' by any reasonable reading
of D-09 kill criterion #1" — this is exactly that, on a real signal this repo's own architecture
requires the jitter to use.

---

**K0-a VERDICT: FAILS on BOTH sub-costs (reflow AND flash).** Per the plan's own instruction ("If
either cost fires -> record it, skip K0-b entirely, and go straight to the DESCOPE branch"), K0-b
(M1/M2/M3/M4 displacement-mechanism probing) is correctly **skipped** — testing whether CSS
margin/padding moves 2px of content is moot once the only available actuation path for that CSS
change already, independently, (a) visibly flashes and (b) transiently reflows tiled windows, on
every single invocation. This is the honest fast path the plan explicitly sanctions, taken here in
good faith after real measurement, not as a shortcut around measuring.

**Timebox used for K0-a: ~4 minutes (19:44:16Z start -> ~19:48:10Z last confirmatory burst),
against a 90-minute hard budget.** Ending the spike here is not a timebox expiry — it's gates
firing early and decisively, which the plan explicitly treats as a legitimate, honest outcome.

---

## GATE K0-b — displacement viability (SKIPPED, per K0-a's fast path)

**K0-b: SKIPPED — not mechanism-unavailable, mechanism-moot (K0-a already disqualifies actuation).**

K0-a fired on both sub-costs, so per the plan's own explicit instruction K0-b is not run this
session. Recording what is and is not known about each candidate mechanism, honestly:

- **M1 — CSS `margin` on `window#waybar`.** NOT independently re-tested this session (no new
  surface/content readback taken). Prior evidence stands as recorded in `08-RESEARCH.md` VERDICT 1
  (HIGH confidence, verified directly against the installed `waybar.5.gz`): CSS margin on
  `window#waybar` does not reposition the layer-shell surface (`Alexays/Waybar#1533`). Whether it
  shifts rendered *content* inside the surface was left open by RESEARCH and remains open here —
  **not resolved by this plan**, because it is moot: even if M1's content readback had shown a
  clean 2px shift, the shift could only ever reach the screen via the same reload-signal actuation
  measured in K0-a above, which already flashes and transiently reflows on its own, independent of
  what the CSS payload says.
- **M2 — CSS `padding` on `window#waybar`.** NOT tested this session, same reasoning as M1: any
  padding-shift CSS would still need the identical SIGUSR2/reload actuation to apply, which K0-a
  already disqualifies before the padding content is even considered.
- **M3 — rewriting bar-config `margin-*` keys + reloading.** Disqualified by design, per three
  citations: (1) rewriting a waybar *config* file at runtime violates this repo's established
  "runtime-only state overrides, never config rewriting" pattern (P7 D-26, 08-CONTEXT Established
  Patterns); (2) a full `reload` resets the bar to config-time visibility, which would resurrect a
  bar the owner had hidden — D-09 kill criterion #3 by definition; (3) recreating the layer-shell
  surface drops/re-adds the exclusive zone — kill criterion #2. **Claim (3) is substantiated with a
  real number, reusing K0-a's exact harness rather than left as an argument:** K0-a's `reassert`
  action IS a bare SIGUSR2/`reload` — the identical signal M3 would also require — and it measured a
  real, reproducible transient reflow (Δy=37px, Δh=37px, confirmed 3/3 runs, see K0-a above). M3's
  kill-#2 prediction is therefore not just argued but directly measured, using data already in this
  log.
- **M4 — `reload_style_on_change`.** Deliberately not enabled/acted on, per the plan's own
  instruction: enabling it means editing `bar-common.jsonc`, which would re-snapshot the
  `waybar-equivalence-check` baseline that 08-08 is also re-snapshotting in this same wave. Noted
  here as a lead for a future phase, not evaluated further.

**K0 (mechanism-viability) is not marked FAIL-mechanism-unavailable** — that framing is reserved
for the case where neither M1 nor M2 displaces anything. Here the situation is different and must
not be conflated: the mechanism's *actuation cost itself* (K0-a) already disqualifies every
CSS-based approach before displacement viability is even asked. This distinction matters for
whoever revisits this: "actuation always twitches/reflows" (this finding) is a different, and in
some ways more fundamental, problem than "no available CSS trick moves content" (the K0-b framing),
and a future phase attacking BAR-02 again needs to solve THIS problem first (a way to update the
bar's CSS with zero flash/reflow) before M1/M2's own displacement question is even worth re-asking.

---

## D-09 KILL CRITERIA — gate table (all three evaluated; verdict already determined by K0-a)

**K1 — "the movement is perceptible as a twitch."**
- Method/instrument: (a) the K0-a flash measurement above (`grim` burst + PIL/numpy mean-RGB diff,
  confirmed 2/2 independent runs, same peak magnitude both times); (b) the plan's own FAIL-CLOSED
  rule for the human perception pass.
- Raw result: (a) a real, reproducible, visible flash on every actuation (peak mean-RGB delta ~65-80,
  decaying over ~400-500ms) — the plan's own text explicitly folds this into K1 ("a flicker on
  actuation counts as a twitch even if the 2px offset alone does not"). (b) No human perception pass
  was obtained: this plan executes autonomously (`autonomous: true`, all tasks `type="auto"`, no
  `checkpoint:human-verify` task in this plan) with no live interactive channel to have a human
  observe unannounced real-usage trials during the timebox. Per D-09's own FAIL-CLOSED rule ("If the
  human pass has not been obtained by the time the timebox expires, K1 is FAIL and the branch is
  DESCOPE"), this alone would force FAIL even setting the flash evidence aside.
- **K1: FAIL** (on two independent grounds: measured flash, and no obtainable human pass).

**K2 — "it reflows any window."**
- Method/instrument: `hyprctl clients -j` (`at`/`size` per window address) polled at 30-50ms across
  the owner's reload-signal actuation — the same signal path any jitter step must use.
- Raw result: a real, reproducible transient reflow, confirmed 3/3 independent runs (Δy=37px on both
  tiled windows, Δheight=+37px, lasting one ~30-50ms sample, self-correcting on the next sample). See
  K0-a reflow-cost section above for the full raw captures.
- **K2: FAIL.**

**K3 — "it needs to coordinate with the visibility owner beyond 'don't jitter while hidden'."**
- Method: structural four-part falsifier (no new intent source; no new signal sender; no-op unless
  `status` is `visible`; composes into the owner's single CSS write rather than a second writer) —
  evaluated by design inspection, since K1/K2 already end the spike before any code would be written.
- Raw result: **N/A — moot.** A jitter verb, if one were built, COULD in principle be designed to
  satisfy all four parts of K3's falsifier (it would reuse the owner's exact CLI shape, CSS-write
  function, and signal call — nothing in the four-part test requires a new mechanism beyond what
  K1/K2 already killed). K3 is not independently decisive here: K1 and K2 already FAIL on the
  actuation mechanism itself, which exists prior to and independent of any visibility-owner
  coordination question. Recording K3 as N/A-because-moot, not PASS or FAIL, so this document does
  not overclaim a criterion that was never actually exercised against real code.

---

**SPIKE VERDICT:** DESCOPE

**Cause:** K0-a (actuation-cost pre-gate) fires on BOTH measured sub-costs — a real, reproducible
visual flash (2/2 runs) and a real, reproducible transient window reflow (3/3 runs) — on every
single actuation of the owner's only available signal path (`SIGUSR2`/`reload`, fixed by
`bar-common.jsonc`). Because any CSS-based jitter mechanism (M1 margin or M2 padding) has no other
way to apply its offset to the running bar, this failure is mechanism-independent: it disqualifies
BAR-02's pixel-shift approach before the 2px displacement question is even reached. This directly
and independently fires D-09 kill criterion #1 (perceptible twitch — folding in the measured flash,
and separately the FAIL-CLOSED no-human-pass default) and kill criterion #2 (reflows a window). K3
is moot given K1/K2 already fail. The 90-minute timebox was NOT exceeded — it ended at ~4 minutes
of real measurement, an honest early-and-decisive outcome, not a rushed one: every gate got a named
instrument and a raw, reproducible (2/2 or 3/3) result before the verdict was written.

**Timebox: started 2026-07-14T19:44:16Z, spike concluded 2026-07-14T19:48:10Z (~4 min), against a
90-minute hard budget (deadline 2026-07-14T21:14:16Z). Well within budget — not an expiry.**

---

## TASK 2 — Standing hypothesis: measured exposure ratio + measured luminance delta

**Runs regardless of Task 1's verdict, per the plan's own instruction.**

### PART B — luminance delta (measured first; does not depend on wall-clock time passing)

**Layout used:** `full` (temporarily switched live from the session's actual `floating` layout,
specifically because `style-full.css`/`waybar-modules.css` carry the genuinely comparable D-06 trim
— `floating`'s pre-trim `window#waybar` was fully `transparent` with no border at all, per
`08-03-SUMMARY.md`, so it has no comparable BEFORE state for this specific measurement). Restored
to `floating` immediately after, confirmed via `hyprctl layers -j` geometry matching the pre-test
baseline (`x=0 y=0 w=2560 h=37`).

**Geometry (live, from `hyprctl layers -j`, NOT hardcoded):** `full` layout bar = `x=10 y=6 w=2540
h=40`.

**Metric:** relative luminance, Rec.709 coefficients (0.2126R + 0.7152G + 0.0722B), computed on
**linearised** sRGB (the sRGB transfer function undone per-channel BEFORE averaging — averaging
gamma-encoded values would not be a luminance and would be confidently wrong). Mean AND
99th-percentile (peak) reported. Instrument: inline `python3` + PIL + numpy
(`/tmp/.../scratchpad/luminance.py`, `srgb_to_linear()` then Rec.709 weighted sum).

**Procedure per preset:** capture AFTER (current tree, as-is) -> surgically edit ONLY the two
trimmed rules (`window#waybar` background/border-bottom in `style-full.css`;
`#workspaces button.active` in `waybar-modules.css` — confirmed via `git diff b2423b4^` that these
are the ONLY two rules that changed in either file since the pre-trim commit, so a surgical swap,
not a whole-file replacement, is both correct and sufficient) -> `pkill -SIGUSR2 waybar` -> wait
~0.7s -> capture BEFORE -> **immediately** `git checkout --` both files -> assert
`git status --porcelain waybar/` empty -> `pkill -SIGUSR2 waybar` again to restore the visual AFTER
state. Never committed while BEFORE was applied. PNGs live only in the scratchpad.

**DARK preset (catppuccin, the session's actual active theme before/after this test):**
```
BEFORE  mean_Y=0.073396   peak99_Y=0.460594
AFTER   mean_Y=0.044569   peak99_Y=0.466843
delta   mean: -39.27%     peak: +1.36%  (peak INCREASED)
```

**LIGHT preset (catppuccin-latte, switched to via `theme-apply catppuccin-latte`, switched back to
`catppuccin` immediately after):**
```
BEFORE  mean_Y=0.746595   peak99_Y=0.870290
AFTER   mean_Y=0.722990   peak99_Y=0.790389
delta   mean: -3.16%      peak: -9.18%
```

**luminance_delta: dark mean -39.27%, dark peak99 +1.36%, light mean -3.16%, light peak99 -9.18%.**

**Honest reporting of both anomalies found, neither suppressed:**
- On the LIGHT preset, the plan flagged a specific risk ("if the trim raises mean luminance,
  report that") — **it did not raise mean luminance here**; both mean and peak decreased, though the
  mean reduction (-3.16%) is far smaller than dark's (-39.27%), exactly as the plan's own reasoning
  anticipated (a translucent panel composites closer to a light backdrop's own brightness either
  way).
- On the DARK preset, a DIFFERENT, unanticipated anomaly was measured and is reported plainly: 99th
  percentile (peak) relative luminance slightly **increased** after the trim (+1.36%), even though
  mean luminance dropped substantially (-39.27%). This is plausible — the trim only touches the
  window background and border-bottom; peak luminance is dominated by bright glyph/icon pixels
  elsewhere in the bar that the trim never touches, and reducing the surrounding background
  luminance can numerically nudge the 99th-percentile statistic (a percentile over the same bright
  pixels, computed over a now-darker overall distribution). This does not mean the trim made the
  brightest element brighter in absolute terms — it means the AFTER distribution's 99th-percentile
  value moved up very slightly relative to a lower background. **Reported as measured, not
  explained away**, since D-06's real intent (peak-luminance burn-in risk from the specific 3px
  solid border element) is about that one high-luminance rule, and this measurement's peak
  statistic is a whole-region proxy, not a per-rule isolation — a genuine limitation of this
  instrument, stated here rather than hidden.

**Idle timeout this ratio-independent luminance measurement does NOT depend on:** N/A (Part B is a
static photometric comparison, not time-windowed).

### PART A — the exposure ratio: UNMEASURED (session did not meet the plan's own validity bar)

**Instrument:** a throwaway 5-second sampling loop (`/tmp/.../scratchpad/bar02-sampler.sh`, never
added to the repo — pure measurement, not a feature), appending
`<epoch>\t<waybar-visibility.sh status output>` to `.bar-02-samples.tsv`. Started 2026-07-14
19:44:44Z (epoch 1784058284), stopped 2026-07-14 20:00:19Z (epoch 1784059216, last recorded sample
1784059216) once it was clear the session would not reach validity within this plan's practical
execution window.

**Raw result:** `n=187` samples, span **932 seconds (~15.53 minutes)**, **100% `visible`** — the
owner's `status` verb returned `visible` on every single sample, zero `hidden-idle`/`hidden-hard`
readings anywhere in the window. Idle timeout this would have been measured under (had it reached
validity): 120s, per `08-04-SUMMARY.md`'s D-05 tunable.

**Why this is reported UNMEASURED rather than as a 1.0 (no-reduction) exposure ratio, and rather
than extrapolated into a longer window:**
1. **It falls short of the plan's own explicit floor** — `n >= 720` (60 minutes at 5s). 187/720
   samples collected; continuing to wait for the remainder was not practically achievable inside
   this single autonomous execution (no blocking wait primitive available for tens of real minutes
   without an impractical number of manual poll cycles, and the plan explicitly forbids padding a
   short window with synthetic/interpolated data to reach the floor).
2. **It fails the plan's own "genuine mixed activity" validity test on its face, regardless of
   duration.** The plan is explicit and specific about this exact failure mode: *"A window in which
   the user walked away is worthless: it would trivially inflate the hidden fraction and manufacture
   the descope argument the hypothesis is supposed to be tested against, not handed. A measurement
   that flatters the conclusion you expect is not evidence."* This session shows the mirror-image
   degenerate case — 100% `visible`, zero idle stretches — which is exactly as unrepresentative of
   "coding, reading, browsing, some idle stretches" as an all-idle window would be, just in the
   opposite direction. It cannot honestly be reported as "the exposure ratio is 1.0" because a
   15-minute window with no idle stretches at all is not evidence that auto-hide never engages —
   it is evidence that *this specific 15-minute window* happened not to trigger it (a plausible
   explanation, not confirmed here: genuine concurrent human input on this shared machine kept
   resetting hypridle's real idle timer throughout — `08-04-SUMMARY.md`'s own Issues Encountered
   section documents this exact class of interference during that plan's testing on this same live
   session).
3. **No synthetic input was injected to manufacture idle/active transitions**, and none will be —
   doing so would be exactly the fabricated-sample failure mode this plan's own integrity
   requirement forbids ("No synthetic samples. No 'representative' values. No filling gaps by
   interpolation").

**exposure_ratio: UNMEASURED.** Reason: session too short (15.53 of a required 60 minutes) AND
degenerate (100% one state, not genuine mixed usage) — an honest gap, not a fabricated number, per
the plan's own explicit instruction for exactly this situation.

### PART C — hypothesis verdict

**HYPOTHESIS: PARTIALLY SUPPORTED** (one of the plan's exactly-three permitted labels — SUPPORTED /
PARTIALLY SUPPORTED / NOT SUPPORTED — chosen because the evidence is genuinely mixed, not because
"partial" is a hedge to avoid committing).

The standing hypothesis has two independent halves:
- **Luminance (Part B) — partial support, with a real complication reported, not smoothed over.**
  The dark preset (the session's actual theme) shows a substantial mean relative-luminance
  reduction (-39.27%) — a real, measured effect in the direction D-06 intended. But 99th-percentile
  PEAK luminance on the SAME dark preset slightly INCREASED (+1.36%), and the light preset shows
  only a marginal mean reduction (-3.16%, peak -9.18%). D-06's trim measurably reduces average
  emitted light on the theme it targeted, but does not cleanly reduce peak (burn-in-relevant)
  luminance across the board — support, but not unqualified support.
- **Exposure ratio (Part A) — UNMEASURED, not a data point either for or against.** The 15.53-minute
  session collected is real, honestly measured data, but it fails the plan's own validity bar on
  two independent grounds (short of the 60-minute/720-sample floor; degenerate 100%-`visible`
  reading, not genuine mixed usage) — see PART A above for the full reasoning. Because this half is
  UNMEASURED rather than supporting or refuting, it cannot itself push the verdict toward
  SUPPORTED or NOT SUPPORTED; it is excluded from the verdict computation and reported as an
  honest gap.

**Why PARTIALLY SUPPORTED and not SUPPORTED:** the luminance evidence alone would need to be
unambiguously positive across both mean AND peak, both presets, to justify a full SUPPORTED — the
dark-preset peak increase is real, measured, and not a rounding artifact, so an unqualified
SUPPORTED would overclaim.

**Why PARTIALLY SUPPORTED and not NOT SUPPORTED:** the dark-preset mean reduction is large (-39%)
and directly on-target for D-06's own stated intent (translucency + border-thinning reducing
average emitted light) — dismissing that as "not supported" would equally overclaim in the other
direction.

**The honest limit, stated plainly:** these are exposure/luminance **proxies, not burn-in
measurements** — nobody can prove a burn-in outcome inside one phase, and the exposure proxy
specifically was not obtainable to the plan's own valid standard within this plan's practical
execution window (reported as UNMEASURED above, not folded into this verdict by assumption).

