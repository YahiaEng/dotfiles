---
phase: 10-ags-media-applet
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - ags/.config/ags/tsconfig.json
  - ags/.config/ags/env.d.ts
  - ags/.config/ags/style.scss
  - ags/.config/ags/app.tsx
  - ags/.config/ags/widget/MediaWindow.tsx
autonomous: false
requirements: [MEDIA-01]

must_haves:
  truths:
    - "`ags request toggle-media` makes a centered dark card appear on screen, and a second call hides it."
    - "Clicking the test button prints `AGS TEST BUTTON CLICKED` to the AGS terminal — proving AGS delivers pointer input to its widgets on this exact Hyprland (the thing eww could not do)."
    - "Clicking outside the card hides the window (click-away works)."
    - "Pressing Esc while the window is open hides it."
  artifacts:
    - "ags/.config/ags/app.tsx — app.start with instanceName 'media', css bootstrap, and a requestHandler handling 'toggle-media'."
    - "ags/.config/ags/widget/MediaWindow.tsx — the full-screen Astal.Window named 'media', namespace 'ags-media', keymode ON_DEMAND, layer TOP, with a centered card, a test button, click-away, and Esc-to-close."
    - "ags/.config/ags/tsconfig.json and env.d.ts (from `ags init`)."
    - "ags/.config/ags/style.scss — minimal card + test-button styling."
  key_links:
    - "waybar (later) ↔ `ags request toggle-media` ↔ app.tsx requestHandler ↔ app.get_window('media').visible."
    - "Gtk.GestureClick onPressed ↔ card.compute_bounds(win) ↔ win.hide() (click-away)."
    - "Gtk.EventControllerKey onKeyPressed ↔ Gdk.KEY_Escape ↔ win.hide() (Esc)."
---

<objective>
Scaffold the AGS v3 project as a new stow package and prove — with a single test button — that AGS delivers pointer clicks to its widgets on this exact eww-0.6.0-failing / Hyprland 0.55.4 machine. This is the phase's FAIL-FAST INPUT-VIABILITY GATE (MEDIA-01): if the AGS test button does not respond to a user click, the entire AGS approach is void and execution STOPS before any further investment.

Purpose: eww rendered the media popup correctly but no interactive widget ever received a pointer click (see `.planning/debug/resolved/eww-media-popup-clicks-dead.md`). Everything downstream assumes AGS fixes that. Prove it here, first, with the smallest possible surface.

Output: A running AGS instance named `media`; a centered clickable card window with click-away + Esc dismiss; the `ags request toggle-media` verb wired; a human-confirmed click on the test button.

Artifacts this plan produces: AGS window name `media`; namespace `ags-media`; request verb `toggle-media`; new files `ags/.config/ags/{app.tsx,style.scss,tsconfig.json,env.d.ts}` and `ags/.config/ags/widget/MediaWindow.tsx`; the component `MediaWindow()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ags-media-applet/10-CONTEXT.md
@.planning/debug/resolved/eww-media-popup-clicks-dead.md
@docs/superpowers/plans/2026-07-15-ags-media-applet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold AGS into a temp dir and pin the reactive/JSX primitive names</name>
  <files>ags/.config/ags/tsconfig.json, ags/.config/ags/env.d.ts</files>
  <read_first>
    - `docs/superpowers/plans/2026-07-15-ags-media-applet.md` — Task 2, Steps 1-2 (exact scaffold + copy commands).
    - `.planning/phases/10-ags-media-applet/10-CONTEXT.md` — "Verified AGS v3 API facts" (the import shapes to expect).
  </read_first>
  <action>
    Run `ags init --gtk 4 --directory /tmp/ags-scaffold` into a temp dir (rm -rf it first), then inspect the generated `app.tsx`/`app.ts` to PIN the exact reactive/JSX primitive names this installed 3.1.2 uses (e.g. `createState` vs `Variable`, `createBinding`, `With`, `For`). Record the pinned names in the SUMMARY — every later plan (10-03 lib/media.ts, 10-04 lib/cava.ts, 10-05 hot reload) depends on them. Then `mkdir -p /home/aorus/dotfiles/ags/.config/ags/widget` and copy ONLY the generated `tsconfig.json` and `env.d.ts` into `ags/.config/ags/`. Do NOT copy the scaffold's demo `app.tsx` or `style.scss` — those are authored in Task 2. This establishes `ags/` as a new stow package (per CLAUDE.md reproducibility: every new file lives in a stow package).
  </action>
  <verify>
    <automated>test -f /home/aorus/dotfiles/ags/.config/ags/tsconfig.json && test -f /home/aorus/dotfiles/ags/.config/ags/env.d.ts && test -d /home/aorus/dotfiles/ags/.config/ags/widget && echo OK</automated>
    Expected: prints `OK`. SUMMARY records the pinned reactive-primitive names observed in the scaffold.
  </verify>
  <acceptance_criteria>
    - `ags/.config/ags/tsconfig.json` and `ags/.config/ags/env.d.ts` exist (copied from the scaffold, not authored).
    - `ags/.config/ags/widget/` directory exists.
    - The exact reactive/JSX primitive names for the installed AGS 3.1.2 are recorded in the SUMMARY for downstream plans.
  </acceptance_criteria>
  <done>The stow package skeleton exists with the scaffold's support files, and the reactive primitives are pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Author the clickable centered window (test button + click-away + Esc + toggle verb)</name>
  <files>ags/.config/ags/widget/MediaWindow.tsx, ags/.config/ags/app.tsx, ags/.config/ags/style.scss</files>
  <read_first>
    - `docs/superpowers/plans/2026-07-15-ags-media-applet.md` — Task 2, Steps 3-6. Steps 3/4/5 contain the VERBATIM, API-pinned TSX/SCSS for `MediaWindow.tsx`, `app.tsx`, and `style.scss`. Copy that code exactly, adjusting only import names that differ from what Task 1 pinned.
    - `.planning/phases/10-ags-media-applet/10-CONTEXT.md` — window/keymode/anchor/click-away/Esc API facts.
  </read_first>
  <action>
    Write the three files exactly as specified in the approved plan's Task 2 Steps 3-5. `widget/MediaWindow.tsx` exports `MediaWindow()` returning an `Astal.Window` with `name="media"`, `namespace="ags-media"`, `visible={false}`, `keymode={Astal.Keymode.ON_DEMAND}`, `anchor={TOP|BOTTOM|LEFT|RIGHT}` (full-screen), `exclusivity={Astal.Exclusivity.IGNORE}`, `layer={Astal.Layer.TOP}`, `application={app}`; it captures `win` and `card` refs via the `$` prop; a `Gtk.EventControllerKey` `onKeyPressed` hides on `Gdk.KEY_Escape`; a `Gtk.GestureClick` `onPressed` calls an `onClickOutside(x, y, win, card)` helper that uses `card.compute_bounds(win)` + `rect.contains_point(new Graphene.Point(...))` to hide when the click is outside the card; the centered `box` has `class="media-card"` and holds one `button` `class="test-btn"` whose `onClicked` calls `print("AGS TEST BUTTON CLICKED")`. Import `Graphene from "gi://Graphene"` and `{ Astal, Gdk, Gtk }` from `ags/gtk4`. `app.tsx` calls `app.start({ instanceName: "media", css: style, main() { MediaWindow() }, requestHandler(argv, res) {...} })` where the handler flips `app.get_window("media").visible` on the `toggle-media` request and replies `res("ok")` (else `res("unknown request: ...")`). `style.scss` gives `.media-card` a min-width/min-height/padding/dark rgba background/border-radius and `.test-btn` padding + radius so the card is visibly on screen. Use the exact reactive-import names pinned in Task 1. Then run the app from Task 2 Step 6: `ags run --directory /home/aorus/dotfiles/ags/.config/ags &`, `sleep 2`, `ags request toggle-media`. If `ags run` errors on TS, correct the import names to match the scaffold and re-run. NOTE: if `ags request toggle-media` targets the wrong instance, disambiguate with `ags request -i media toggle-media` and record which form works in the SUMMARY (consistency for 10-06's waybar wiring).
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles/ags/.config/ags && node -e "process.exit(0)"; grep -q 'name="media"' widget/MediaWindow.tsx && grep -q 'namespace="ags-media"' widget/MediaWindow.tsx && grep -q 'toggle-media' app.tsx && grep -q 'AGS TEST BUTTON CLICKED' widget/MediaWindow.tsx && echo STRUCT_OK</automated>
    Then run `ags run --directory /home/aorus/dotfiles/ags/.config/ags &`, `sleep 2`, `ags request toggle-media`, and read a `grim` screenshot to confirm a centered dark card with the test button renders. Expected: `STRUCT_OK` and a visible centered card.
  </verify>
  <acceptance_criteria>
    - The three files exist with the window named `media`, namespace `ags-media`, keymode ON_DEMAND, full-screen anchor, layer TOP.
    - `ags run` starts without a TS error; `ags request toggle-media` toggles the window's visibility.
    - A screenshot shows the centered dark card with the test button (render confirmed by the agent before the human gate).
  </acceptance_criteria>
  <done>A running AGS `media` instance shows a centered clickable card and toggles via `ags request toggle-media`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HARD FAIL-FAST INPUT-VIABILITY GATE — user click test</name>
  <files>ags/.config/ags/widget/MediaWindow.tsx</files>
  <action>
Halt and present the checkpoint below to the human. This is the phase's fail-fast gate: there is NO agent-side pointer injection, so the executor CANNOT self-verify that a click registers — a human must click the test button. Do NOT self-approve on a green structure/screenshot check; a rendered button that never receives a click is exactly the eww failure this phase exists to escape. If the click produces no terminal output, STOP the entire phase and reassess — do not proceed to 10-03.
  </action>
  <what-built>A running AGS v3 window named `media` (namespace `ags-media`), centered on screen, containing a single test button, with click-away and Esc dismiss. This is the input-viability gate: it proves AGS delivers pointer clicks to widgets on this machine — the exact capability eww lacked.</what-built>
  <how-to-verify>
    1. Ensure the applet is open: run `ags request toggle-media` (the centered card should appear). If it does not appear, re-run and check the AGS terminal for errors first.
    2. CLICK THE TEST BUTTON. Watch the terminal running `ags run` — it MUST print `AGS TEST BUTTON CLICKED`.
    3. Click anywhere OUTSIDE the card — the window MUST close (click-away).
    4. Re-open with `ags request toggle-media`, then press `Esc` — the window MUST close.
    HARD FAIL-FAST GATE: If step 2 produces NO terminal output (the button click does nothing), STOP. AGS also cannot deliver clicks on this setup, the whole AGS approach is void, and this phase must be reassessed — do NOT proceed to 10-03. Report the failure with the AGS terminal output and `hyprctl layers` for the `ags-media` namespace.
  </how-to-verify>
  <resume-signal>Type "approved" only if the test button click printed to the terminal AND click-away AND Esc all closed the window. Otherwise describe exactly what happened (especially whether the click printed anything) so the approach can be reassessed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| waybar/CLI → AGS requestHandler | `ags request <verb>` strings arrive over the AGS instance socket; this plan only handles the fixed literal `toggle-media`, no metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | app.tsx requestHandler | low | mitigate | The handler matches only the exact literal `toggle-media`; any other request returns an inert `unknown request` string. No request value is ever passed to a shell or command. |
| T-10-02-02 | Denial of Service | full-screen transparent layer window | low | accept | The window is `visible={false}` by default and `exclusivity=IGNORE`; when hidden it does not capture input. This plan introduces no MPRIS/metadata surface (that arrives in 10-03). |
</threat_model>

<verification>
- Structural greps confirm window name `media`, namespace `ags-media`, `toggle-media` verb, and the test-button print string.
- `ags run` starts cleanly; `ags request toggle-media` toggles visibility; a screenshot shows the centered card.
- HUMAN GATE: test button click prints to terminal; click-away closes; Esc closes. This gate is a hard STOP on failure.
</verification>

<success_criteria>
- AGS delivers a pointer click to a widget on this machine (human-confirmed) — the input-viability gate passes.
- `ags request toggle-media` toggles a centered `media` window with working click-away and Esc dismiss (MEDIA-01 foundation).
</success_criteria>

<output>
Create `.planning/phases/10-ags-media-applet/10-02-SUMMARY.md` when done. Record the pinned reactive-primitive names and the working `ags request` form (with or without `-i media`).
</output>
