# Phase 15 Plan 01 — API Probe Report

Live measurement against the running build (quickshell 0.3.0-2, Hyprland 0.56.1, this host) plus a source read of the two reference shells. Every command and harness cited below actually ran during this session; no answer here is inferred from a type signature alone. A question the probe could not settle reads `unresolved` with the reason — none of the eight did.

All probe harnesses lived under `/tmp/gsd-probe-Sws01q/` (a `mktemp -d` scratch directory), deleted at the end of the executing session. No file under `quickshell/`, `hypr/`, `waybar/`, `matugen/`, `gtk/` or `install.sh` was touched; `quickshell/.config/quickshell/modules/Probe.qml` is byte-identical to its pre-plan state (`git diff HEAD -- quickshell/.config/quickshell/modules/Probe.qml` empty throughout).

## A3 — UntypedObjectModel accessor shape

**Verdict:** measured
**Evidence:** `harness_a3.qml` and `harness_a3b.qml` run via `qs -p` against the live `Pipewire`, `Networking` and `Bluetooth` singletons, plus a direct `Repeater { model: Pipewire.nodes }` instantiation test.

`UntypedObjectModel` (backing `Pipewire.nodes`, `Networking.devices`, `Bluetooth.devices`/`adapters`) has **no `count` property and no `get(index)` method** — both `'count' in model` and `'get' in model` evaluate `false` on all three live singletons, confirming the type file's declared shape and contradicting 15-RESEARCH.md Pattern 2's example code and 15-PATTERNS.md's backend snippet, which both reach for `.count`/`.get(i)`.

The model's readonly `values` property is the correct accessor. `typeof model.values` is `"object"`; `Array.isArray(model.values)` is **`false`** (it is not a real JS Array), but it fully supports `.length`, numeric index access (`vals[i]`), a plain `for` loop, and standard `Array.prototype` methods (`.map`, `.filter` both proven live — `vals.map(n => n.id)` and `vals.filter(n => n.isSink)` both returned correct results against 11 live PipeWire nodes, 4 of them sinks).

`Object.keys(model)` on all three singletons returns the full `QAbstractListModel` surface (`rowCount`, `data`, `insertRows`, …) plus `values`/`valuesChanged`/`indexOf` — confirming the `QAbstractListModel` prototype declared in the type file is real and live, not merely declared.

A `Repeater { model: Pipewire.nodes }` bound **directly to the `UntypedObjectModel` object itself (not `.values`)** instantiates exactly one delegate per object — proven live: 11 delegates instantiated for 11 real PipeWire nodes, each exposing the real node object as `modelData` (`modelData.id`, `modelData.isSink` both read correctly per delegate).

**Reactivity, proven two ways:**
1. A property bound to `Pipewire.nodes.values.length` at `Item` construction re-evaluated live 11 times over ~3 seconds as PipeWire's registry populated asynchronously after process start (0 → 1 → 2 → … → 11), each transition firing the property's change handler.
2. `Bluetooth.defaultAdapter` (same singleton-property mechanism) transitioned live from `null` → a real adapter object → `enabled: false, state: Blocked` while `rfkill` reported the adapter soft-blocked, then to `enabled: true, state: Enabled` within 3s of `rfkill unblock bluetooth`, confirming the reactive mechanism is not `Pipewire`-specific.

**Corrective finding on `PwNodeType` flags (load-bearing for A1 and any backend filter):** the `Flag` enum's named "convenience" members (`AudioSink`, `AudioSource`, `AudioOutStream`, `AudioInStream`, …) are **pre-combined composite values**, not independent bits — e.g. `AudioOutStream = Audio(1)|Stream(4)|Sink(16) = 21`, `AudioInStream = Audio(1)|Stream(4)|Source(8) = 13`. Because `AudioOutStream` and `AudioInStream` share the `Audio` and `Stream` bits, a bitwise-AND membership test (`node.type & PwNodeType.AudioInStream`) produces a **false positive** on an `AudioOutStream` node (measured live: `21 & 13 = 5`, truthy). The correct test is **exact equality** (`node.type === PwNodeType.AudioOutStream`), which was verified correct against both live playback-stream nodes measured for A1 (raw type `21`, exactly `AudioOutStream`).

**Accessor expression for a backend adapter (the one unambiguous sentence this section owes):** iterate any `UntypedObjectModel` (`Pipewire.nodes`, `Networking.devices`, `Bluetooth.devices`/`.adapters`) via its `.values` property using index access or `Array.prototype` methods for JS-side filtering/mapping, and bind `Repeater`/`Instantiator` `model:` directly to the `UntypedObjectModel` object (not `.values`) for one-delegate-per-object UI lists; for reactivity, bind on `<Model>.values.length` (or `.values` itself) — both re-evaluate live as the underlying registry changes, no `Connections`/manual refresh needed.

## A6 — PwObjectTracker requirement

**Verdict:** measured
**Evidence:** `harness_a6_notracker.qml` and `harness_a6_tracker.qml`, both run via `qs -p` against the same two live nodes (a real `pw-play` stream, node id 93; the live default sink, node id 54).

**Without any `PwObjectTracker` in the scene**, both nodes read: `ready: false`, `audio !== null` (the `PwNodeAudioIface` wrapper object exists) but `audio.volume: 0` and `audio.muted: false` for both — stub/default values, not the real live volume — and `Object.keys(node.properties).length: 0` for both (properties dictionary entirely empty).

**With a `PwObjectTracker { objects: [streamNode, sinkNode] }` mounted and given ~1.5s to settle**, the same two nodes read: `ready: true` for both; `audio.volume: 1` (stream, unmuted, matches `pw-play`'s stream-level gain) and `audio.volume: 0.5799...` (sink, matches the real live system volume); `properties` populated with **28 real keys** on the stream node and **53 real keys** on the sink node (verbatim key lists in the A1 section below).

**Tracker is required, unambiguously.** A node's `ready`, `audio.volume`/`audio.muted` and `properties` are all inert placeholders until that node is inside a `PwObjectTracker.objects` list — this confirms 15-RESEARCH.md's undocumented gap exactly as the planning-time read predicted. For `AudioBackend`: mount **one `PwObjectTracker` whose `objects` binds to the live, filtered node set the UI actually needs** (e.g. `Pipewire.nodes.values.filter(n => n.isStream || n.id === Pipewire.defaultAudioSink.id || n.id === Pipewire.defaultAudioSource.id)`) — a static list will not track streams that start/stop after mount, so the `objects` binding must itself be reactive on `Pipewire.nodes.values` (proven possible per A3's reactivity findings) so newly-appeared streams are tracked and departed ones are dropped automatically as `PwObjectTracker.objects` is itself a plain read/write property, not an append-only list.

## A1 — PipeWire node property keys

**Verdict:** measured
**Evidence:** `harness_a1.qml`, tracked via `PwObjectTracker`, dumping `JSON.stringify(node.properties)` for two real `AudioOutStream` nodes (a `pw-play` CLI stream and a live Zen browser tab stream), the live default sink, and a live input source. Cross-checked against `pw-dump` output for the same node ids — property sets match exactly, no Quickshell-side filtering artifact.

**Verbatim key list, real playing stream (`pw-play`, node id 93, type `AudioOutStream`, confirmed via `node.type === PwNodeType.AudioOutStream`):**
`adapt.follower.spa-node, application.name, client.id, clock.quantum-limit, factory.id, library.name, media.category, media.class, media.filename, media.format, media.name, media.role, media.software, media.type, node.async, node.autoconnect, node.driver-id, node.latency, node.loop.class, node.loop.name, node.name, node.rate, node.want-driver, object.id, object.register, object.serial, port.group, stream.is-live` (28 keys)

A second real `AudioOutStream` node (Zen browser, id 87, also confirmed `type === AudioOutStream`) carries the same shape plus GUI-app-only keys: `application.language, application.process.binary, application.process.host, application.process.id, application.process.machine-id, application.process.session-id, application.process.user, client.api, pulse.attr.*, pulse.corked, pulse.server.type, window.x11.display` (33 keys total) — confirming the key set is **not fixed per stream type**; a CLI producer (`pw-play`) and a GUI producer (Zen) both satisfy `AudioOutStream` but expose materially different property sets.

**(a) Display-name fallback chain, ordered, each link's observed non-empty candidates stated:**
1. `node.properties["application.name"]` — non-empty on **both** observed stream nodes (`"pw-play"`, `"Zen"`). Primary candidate.
2. `node.properties["application.process.binary"]` — present and non-empty on the GUI stream (`"zen-bin"`), **absent** (`undefined`) on the CLI stream. Fallback for when `application.name` itself is missing (not observed on these two, but a real gap the chain must cover — CLI tools frequently omit `application.name`).
3. `node.name` (top-level PwNode property, not inside `properties`) — always present on every observed node (`"pw-play"`, `"Zen"`, plus sink/source node names), never empty. Terminal fallback.
4. `node.nickname` / `node.description` — **empty string on both stream nodes** (not `undefined`, but `""`), the opposite of the plan's assumption; these ARE populated on sink/source nodes (`node.nickname: "ALCS1200A Analog"`, `node.description: "Starship/Matisse HD Audio Controller Analog Stereo"` for the sink; `"PRO X"` / `"Logitech G PRO X Gaming Headset Mono"` for the source) — **so `nickname`/`description` are a device-node fallback, not a stream-node fallback**, a corrective distinction downstream plans must respect.

**(b) Icon fallback chain:** `node.properties["application.icon-name"]` was **`undefined` on every node observed** (both stream nodes, the sink, the source) — no observed key ever supplied a real icon name. The icon chain therefore has **no live candidate at all** on this measurement; a generic Material Symbol (e.g. `volume_up` for a stream, `speaker`/`mic` for sink/source) must be the icon for every row unconditionally, not a fallback-of-last-resort.

**(c) Distinguishing two concurrent streams from the same app:** no properties key does this — `client.id` is shared per-process (both streams from the same app share the same `client.id`), and `application.name`/`media.name` are app-chosen strings with no per-stream uniqueness guarantee. **The node's own top-level `id` property (e.g. `93`, `87`) is the only guaranteed-unique per-stream identity** and is what 15-04's per-row row-identity truth must key on — recorded per the plan's own naming: the node id property name is `id` (`PwNodeIface.id`, a `uint`, `isReadonly: true`, `isPropertyConstant: true`).

## A4 — Call path: invokable methods vs request signals

**Verdict:** measured
**Evidence:** `harness_a4.qml`, a full live connect/forget cycle against a real, unsaved, WPA2-secured neighboring AP (`WE_1D03C9`, security `Wpa2Psk`; the plan's originally-recorded target `TONLY_TAP_9943D1C` had scrolled out of range by execution time — normal AP-visibility churn, not an environment defect, see Corrections), cross-checked at every step against `nmcli -t -f NAME,TYPE connection show`.

**Pre-state:** `nmcli connection show` listed only `Fiber:802-3-ethernet` and `lo:loopback` — zero wireless profiles, matching the plan's recorded pre-count.

**Plain `connect()` (no PSK) reached NetworkManager, unambiguously:** immediately after calling `targetNetwork.connect()`, `nmcli connection show` gained a new profile — `WE_1D03C9:802-11-wireless` — and the network's own `state` property read `1` (`ConnectionState.Connecting`) with `stateChanging: true`, `known: true`. **NetworkManager creates and persists a connection profile even with no PSK supplied**, live-confirming D-15-17's premise (the profile appearing is itself the proof 15-05's `forget` reasoning needs). No `connectionFailed` signal fired within the ~10s observation window — the connection stayed in `Connecting`/`stateChanging` the whole time, consistent with NM still negotiating (or waiting to time out on) missing secrets rather than failing fast.

**Emitting the bare `requestConnect` signal afterward produced no additional observable effect** — `state`, `stateChanging`, `known` were all unchanged from the post-`connect()` reading, and `nmcli connection show` was unchanged. With nothing subscribed to `requestConnect`, it is inert — it does not itself call NetworkManager. This settles the call-path question: **the plain invokable method (`connect()`/`forget()`) is the real actor; the `request*` signals are a request/intent channel meant for an external controller to listen to and translate into the plain-method call — they carry no default wiring of their own.**

**Plain `forget()` also reached NetworkManager and removed the profile:** calling `targetNetwork.forget()` after the connect attempt caused `WE_1D03C9` to disappear from `nmcli connection show` within the observation window, restoring the wireless-profile count to the pre-state's zero. `requestForget` was not exercised on this same object — after `forget()` succeeded, the harness's held reference to the (now-forgotten, ephemeral, never-fully-connected) network object was invalidated by the underlying model (a subsequent `.requestForget()` call threw `TypeError: Cannot call method 'requestForget' of null`, i.e. the QML wrapper object itself became `null`). **Corrective finding, load-bearing for Open Q1 and 15-05:** a `WifiNetwork` object returned from a device's `networks` model is **not guaranteed to stay alive** across a `forget()` on an unsaved/ephemeral profile — code holding a reference across such a call must re-resolve the network from the model rather than assume object identity survives.

**One-sentence conclusion:** call the plain invokable method (`connect()`, `disconnect()`, `forget()`, `connectWithPsk()`) to actually act on NetworkManager — the `request*` signals require an application-level listener to have any effect and are not a substitute action path.

## A2 — Default-sink write semantics

**Verdict:** measured
**Evidence:** `harness_a2_sink.qml` and `harness_a2_source.qml`, run against a genuine second real output sink (`alsa_output.usb-Logitech_PRO_X_000000000000-00.analog-stereo`, id 45) and a genuine second real input source (`alsa_input.pci-0000_09_00.4.analog-stereo`, id 70), with a single continuous non-restarting stream held open throughout via `pw-play` (a 10-minute generated sine tone, low volume, `media.name=GSD-A1-Probe-Tone`, node id 93) for the sink half, and a continuous `pw-cat --record` capture (node id 100) for the source half. Cross-checked against `pactl get-default-sink`/`get-default-source`, `pw-dump` link records, and `pactl list short sink-inputs`.

**Output side (`preferredDefaultAudioSink`): re-routes EVERYTHING, not just new streams.** Writing `Pipewire.preferredDefaultAudioSink = <node 45>` while the tone stream was already playing on the old default (node 54) caused, within 0.5s: (1) the read-only `Pipewire.defaultAudioSink.id` followed the write, settling at `45`; (2) the **already-playing** stream's PipeWire links moved from `in=54` to `in=45` (`pw-dump` link records `out=93 in=45`), and `pactl list short sink-inputs` showed the same stream's sink-input now targeting the Logitech sink index, not the onboard one; (3) `pactl get-default-sink` reported the Logitech sink **after the harness process exited**, confirming the change is not scoped to the writing process's lifetime; (4) a second, freshly-started stream (`pw-play`, node id 100) also landed on the new default (`in=45`) immediately. All four observations agree: on this wireplumber configuration, a `preferredDefaultAudioSink` write moves live audio, not merely future streams.

**Input side (`preferredDefaultAudioSource`): measured no effect within the observation window — a genuine divergence from the output side.** Writing `Pipewire.preferredDefaultAudioSource = <node 70>` while a `pw-cat --record` capture was actively running on the old default source (node 69) produced **no change** to `Pipewire.defaultAudioSource.id` (stayed `69`) across a 10-second observation window (checked at t+0.5s, +2s, +5s, +10s — reproduced identically across two separate harness runs), **no change** to `pactl get-default-source` (stayed the Logitech mic), and **no change** to the live recording stream's PipeWire link (stayed `out=69 in=100` throughout). This is recorded as measured-no-effect, not `unresolved` — the same write mechanism, same wait discipline, and a real second device were used on both sides, and the sink half of the identical test responded within 0.5s; the source half simply did not respond within 10x that window. The root cause was not isolated further (out of this plan's scope — the finding itself, not its cause, is what 15-04 needs); a plausible contributor is that PipeWire had suspended node 70 (`SUSPENDED` state at probe start, unlike the RUNNING default sink) and wireplumber's session-manager default-node policy may treat sink and source promotion differently, but this is not verified and is not asserted as fact.

**Consequence for the checkpoint that follows:** PANEL-02's output half can honestly claim the strongest reading of the requirement (picking a new output device moves what's already playing). The **input half cannot** make the same claim on this measurement — the write had no observed effect at all, not even a "new captures only" partial win. This asymmetry, not a uniform "new streams only" limitation, is what Task 3's checkpoint disposes.

If a per-stream re-route alternative had been needed (it was not, for the output side): `PwNodeLinkTracker` exposes a `node` property and a live `linkGroups` list; `PwLinkGroup` exposes the per-link-group object a QML binding could iterate to redirect one stream's links individually. Both are present in the installed type surface (confirmed by direct qmltypes read, `quickshell-service-pipewire.qmltypes` lines 472-513 for `PwNodeLinkTracker`), so the escape hatch is real and available if the source-side asymmetry ever needs the same treatment PANEL-02's checkpoint considers for the general case.

### Disposition

**Decided 2026-08-02, by the human at Task 3's checkpoint: bounded re-probe of the input-side gap, then option (a) (accept and document, per side).** Option (b) (explicit per-stream re-route via `PwNodeLinkTracker`/`PwLinkGroup`) was considered and **rejected** — it is the standard remedy for a "moves new streams only" gap, and the output side provably has no such gap (it already moves everything live); building link-tracking machinery there fixes nothing. For the input side, the measured failure mode ("no observed effect at all") is a different shape than "new streams only," and there was no evidence link-tracking is its remedy — building it blind, before the cause was understood, was rejected as premature.

**Bounded re-probe of the input-side gap (run before writing this disposition, per the human's condition that "no effect at all" not be shipped as an unexplained gap):** `harness_a2_reprobe.qml` tested the single discriminating question — did the write ever land on the QML binding, or was it silently rejected? — by reading `Pipewire.preferredDefaultAudioSource` back after the write, not just `Pipewire.defaultAudioSource` (the earlier A2 measurement only checked the latter). Result, reproduced against a same-mechanism sink control write in the same harness run:

- `Pipewire.preferredDefaultAudioSource` (the write-target property itself) read back the **written value** (`70`, the target node) from t+1s through t+4s — the assignment stuck on the QML binding. This rules out "read-only / silently non-assignable at the binding layer."
- `Pipewire.defaultAudioSource` (the read-only, actually-in-effect property) stayed at the **old value** (`69`) for the entire same window — the live default never moved.
- The sink-side control write, performed identically in the same harness run, showed **both** properties converge on the new value (`45`) within 1s — confirming the harness/methodology is sound and the divergence is source-specific, not a re-probe artifact.

**This discriminates cleanly: the write is accepted and ignored, not a harness or binding fault.** Quickshell's `preferredDefaultAudioSource` write reaches and updates the intended QML property; something downstream (wireplumber's session-manager policy, most plausibly — not independently confirmed, and this plan does not chase the root cause further, per the human's own time-box) does not act on it to change the actual default source, at least not within a multi-second window. This is a real, disclosed platform gap on this build — not an unexplained one.

**What ships, per side:**

- **Output:** `AudioBackend.preferredDefaultAudioSink` ships as a single property write. Document it as delivering full live re-route — the measured, stronger claim (moves already-playing streams, not just new ones, confirmed to persist past the writing process's lifetime).
- **Input:** `AudioBackend.preferredDefaultAudioSource` ships as a single property write, matching the output side's implementation shape (no new `AudioBackend` surface, no link-tracking machinery). Documented claim: **the write is accepted by the binding but has not been shown to change the live default source on this build** — selecting a new input device updates the stored preference; whether or when it actually redirects capture is unverified beyond this plan's observation windows. This is a named, understood residual, not a silent gap.
- **Carried forward to 15-04's render gate, explicitly:** 15-04 must re-verify input-device selection against a real recording/call application at its own render gate, and if the live default still does not follow the write there, the picker's input row must surface this honestly in its own copy (e.g. "may require reopening your recording app") rather than implying parity with the output row's instant, confirmed behaviour.

## Open Q1 — scannerEnabled cadence

**Verdict:** measured
**Evidence:** `harness_openq1.qml`, a single continuous 95-second scan window followed by a 62-second post-stop quiet window, both against the real `wlan0` device, with every `networks.valuesChanged` emission timestamped and the full ordered network list logged at each emission.

**Rescan interval is NOT constant — it widens over the session, consistent with NetworkManager's own scan backoff, not a fixed Quickshell-side poll.** Measured gaps between successive list-settling bursts, in order: an initial sub-second burst (10 networks appearing individually as the device warmed up), then bursts at roughly t=2.5s, 5.2s, 9.0s, 14.0s, 21.0s, 43.5s, 56.0s, 65.0s, 90.0s — the spacing roughly doubles across the window (≈2.5s → 5s → 9s → 14s → 21s → 43s → 56s+ → 90s). **No single concrete number can honestly be named as "the" rescan interval** — the accurate statement is a widening/backoff cadence, starting near 2.5–5s and stretching past 20s within the first half-minute.

**List order is NOT stable across rescans, and network object identity does NOT survive a rescan.** Across the 29 recorded change events, the same SSID appears, disappears, and reappears at different positions (e.g. `WE_1D03C9` was position 1 at t=0, absent by t=43.5s, reappeared at position 9 by t=90s before disappearing again at scanner-stop) — **this directly contradicts 15-05's stable-ordering truth as an unconditional guarantee**: a rescan that changes only signal strength was not isolated as a distinct case here (the visible AP set itself churned throughout, being a real neighborhood), but the observed mechanism (objects appearing/disappearing rather than being updated in place) means row identity is not guaranteed to survive any rescan in general — 15-05 must design for react-to-model-diff (add/remove), not assume in-place mutation of existing rows.

**Disabling `scannerEnabled` clears the network list entirely, it does not merely freeze it.** Setting `scannerEnabled = false` triggered a rapid sequence of `valuesChanged` emissions shrinking the list from 8 entries to 0 within the same instant (all logged at `t=95.0s`) — the previously-seen networks are not retained as a stale-but-visible list; the model goes empty. A wifi panel's empty state must be designed for this (the list is briefly non-empty then empties, not "frozen at last known content").

**Scanning genuinely stops when disabled:** the 62-second post-stop quiet window recorded **zero** further `valuesChanged` emissions — confirming D-15-15's zero-idle-on-dismiss doctrine holds: no further scan activity once `scannerEnabled` is false.

## A5 — Reference-shell source study

**Verdict:** measured
**Evidence:** shallow clones of both reference repos, read directly (not from recollection or screenshots): `end-4/dots-hyprland` at commit `aed4d1ec63f584905c28d2a678db5845579fdafc`, `caelestia-dots/shell` at commit `046dd3c6c3b1782f27284d5fc0e181b6021dd7c7`, both resolved via `git ls-remote HEAD` before cloning and re-confirmed via `git rev-parse HEAD` inside each clone. Both clones lived under a `mktemp -d` scratch directory, were read-only, never executed, never copied into this repository, and were deleted at task close.

### Adaptation table

| Surface | Repo | File : lines | What the reference does | Adopt/Skip | Reason |
|---|---|---|---|---|---|
| Quick-settings tile grid | caelestia-dots/shell | `modules/utilities/cards/Toggles.qml:14-163` | Icon-only square tiles (`IconButton`, `implicitWidth: implicitHeight`), **no text label at all**. Two rows, split at `Math.ceil(len/2)` when >6 toggles. A `DelegateChooser` maps a role id (`"wifi"`, `"bluetooth"`, `"mic"`, `"settings"`, `"gameMode"`, `"dnd"`, `"vpn"`) to a per-type `Toggle` delegate. | **Skip** the icon-only convention | Our own D-15-21 requires a visible, wrapped two-word label ("Do Not Disturb") — Caelestia sidesteps the two-word-label problem entirely by never showing a label, so it has no precedent to adopt on the hard question. The `DelegateChooser`-by-role-id pattern itself (mapping a toggle-id string to a typed delegate) **is** adopted — it is the same shape 15-07 already needs for its own toggle roster. |
| Quick-settings tile grid | end-4/dots-hyprland | `modules/ii/sidebarRight/quickToggles/androidStyle/AndroidQuickToggleButton.qml:9-247` | Android-style tile: icon in its own `MouseArea` sub-region (`iconMouseArea`) plus, when `expandedSize`, a two-line text `Column` (`name` bold/small, `statusText` lighter/smaller) filling the rest of the button. Both text lines use `elide: Text.ElideRight` — **single-line elide, not wrap** — for the name. Split hit-region: `iconMouseArea.onClicked → mainAction` (toggle) fires only for clicks on the icon; `root.onClicked` (the outer button) fires `altAction` (open detail panel) when `expandedSize && altAction`, else falls back to `mainAction` — i.e. icon = toggle, body/label area = open panel, exactly the "both toggle and open a panel" split D-15-21 asks about. | **Adopt** the split-hit-region mechanism (icon sub-region toggles, body opens panel); **skip** the elide-not-wrap label treatment | The hit-region split is the concrete, source-proven answer to "how does a tile split toggle vs. open-panel." The label elide, however, is the opposite of D-15-21's binding requirement — **neither reference shell wraps a two-word label to two lines; this is a genuine gap with no reference precedent**, and 15-07 must design its own wrap solution rather than adapt one. |
| Quick-settings tile grid — empty/off-state split | end-4/dots-hyprland | `services/BluetoothStatus.qml:12-13` | `available: Bluetooth.adapters.values.length > 0` (hardware presence) is a **separate** property from `enabled: Bluetooth.defaultAdapter?.enabled ?? false` (software state) — the toggle's `available` flag is read independently in `BluetoothToggle.qml:17`. | **Adopt** | This is exactly D-15-26 case 3's distinction (adapter present-but-disabled vs. hardware absent) sourced from a real, working implementation — `AudioBackend`/`BluetoothBackend` should expose the same two-property split rather than collapsing both into one boolean. |
| Wifi panel layout | caelestia-dots/shell | `modules/bar/popouts/Network.qml:1-397` | Compact bar-popout (not the full "Nexus" settings page). Row: signal-strength `MaterialIcon` (colour = primary if active) + lock glyph if secured + SSID (`elide: Text.ElideRight`, fills width, medium weight + primary colour if active) + a circular link/link-off icon-button on the right that fills with primary colour and shows a `CircularIndicator` spinner while `isConnecting`. List: `[...Nmcli.networks].sort(active desc, then strength desc).slice(0, 8)`. Password entry is **not inline** — clicking an unsaved secured network switches the whole popout's `currentName` to a separate `"wirelesspassword"` view. Scan control is a persistent full-width pill button at the bottom ("Rescan networks", spinning icon + `CircularIndicator` overlay while `Nmcli.scanning`), not a top-line status text. | **Adopt** active-first+strength sort, circular connect/disconnect affordance, and separate-view (not inline) password entry; **note** the persistent rescan-button pattern as a stronger alternative to a bare status line | Directly informs D-15-16 (active connection sorts first, confirming what our own row-identity design needs to preserve across the resort) and settles the "inline vs popup" password question this plan's own read_first flagged as open — Caelestia's answer is a distinct view, not an inline expand, matching D-15-15's structure of dedicated password UI. |
| Wifi panel layout — empty/off state | caelestia-dots/shell | `modules/bar/popouts/Network.qml:35-41` | The "Enabled" toggle switch is the only off-state affordance; there is **no distinct empty state for zero networks found** and **no distinct "no wifi hardware" state** — `Nmcli.wifiEnabled` false just leaves the list empty with the toggle off. | **Skip / absence noted** | Neither reference file in this popout distinguishes "off," "no hardware," and "on but nothing found" as three visually different states — this is a genuine reference gap, consistent with end-4's `available`/`enabled` split being the stronger precedent to follow instead (see the row above). |
| Bluetooth panel layout | caelestia-dots/shell | `modules/bar/popouts/Bluetooth.qml:1-205` | Two toggles at top (`Enabled`, `Discovering`). Row: device-type `MaterialIcon` (`Icons.getBluetoothIcon`) + name (elide right, fills width) + battery glyph (**visible only when connected**; `battery_alert` if `!batteryAvailable`, error colour if `<20%`) + circular connect/disconnect icon-button (same visual language as the wifi row) + a `Loader`-gated forget ("delete" icon) button that is **only present when `bonded`**. Sort: `connected desc, then paired desc, then name.localeCompare`. Pairing/connecting-in-progress: the same `CircularIndicator` spinner overlaying the connect button, driven by `state === Connecting \|\| Disconnecting`. Bottom: a labelled `IconTextButton` ("Open settings", `settings` icon) that calls `popouts.detachRequested("bluetooth")`. | **Adopt** row composition, connected>paired>alphabetical sort, conditional forget-button visibility, and the labelled full-app-link button | Directly answers this surface's open questions: contextual verb is a same-icon-language connect/disconnect toggle (not per-device varying verb text), expanded detail is NOT a separate expand-in-place — battery is shown inline, address/forget are the only "detail" actions and forget is a plain icon button gated on `bonded`, not a drill-down panel. |
| Empty/off states (general) | end-4/dots-hyprland | `services/BluetoothStatus.qml` (see row above) | Same `available` vs `enabled` split generalizes to any adapter/device-backed toggle. | **Adopt** (cross-reference, not a new row) | Repeated here because D-15-26 explicitly spans wifi, bluetooth, and audio — the same two-property pattern (`hardwarePresent` / `softwareEnabled`) should be the shared shape across `AudioBackend`, `WifiBackend`, `BluetoothBackend`, not reinvented per-backend. |
| Per-app volume row | caelestia-dots/shell | `modules/nexus/pages/audio/AppVolumes.qml:1-71`, `services/Audio.qml:87-106` | `SliderRow` delegate: `icon` (`Icons.getVolumeIcon(volume, muted)`), `label` (`Audio.getStreamName(node)`), `valueLabel` (`Math.round(value*100)+"%"`), slider bound to `audio.volume`, row `enabled: !audio.muted` (the **whole row dims** when muted — not a separate mute badge). `getStreamName()`: `stream.properties["application.name"] \|\| stream.description \|\| stream.name \|\| "Unknown Application"`. Empty state: `ItemList` with `placeholderIcon: "music_off"`, `placeholderText: "No apps playing audio"`. | **Adopt everything** | `getStreamName()`'s exact fallback order (`application.name` → `description` → `name` → literal fallback string) **independently matches this plan's own A1 measurement** of the real property keys — direct cross-validation that the fallback chain A1 derived from live PipeWire data is also what a shipping reference shell already relies on in production. The whole-row-dims-when-muted treatment answers this surface's "is muted shown once or more than once" question: once, via `enabled`, not a duplicated icon+text indicator. |
| Full-app link treatment | caelestia-dots/shell | `modules/bar/popouts/Bluetooth.qml:174-184` (bluetooth); `modules/bar/popouts/Network.qml` (network — **absent**, see below) | Bluetooth's popout has a labelled `IconTextButton` ("Open settings" + `settings` glyph) at the bottom, always present, that hands off to the full Nexus settings page. The wifi popout (`Network.qml`) has **no equivalent full-app link at all** — its rescan button is the only bottom affordance, and reaching full network settings (`AddNetworkPage`, `SavedNetworksPage`, etc.) is not exposed from the compact popout in the reviewed file. | **Adopt** the labelled-button pattern (bluetooth) for our own panels' full-app links; **absence noted** for wifi | D-15-06/D-15-22 ask whether the link is labelled or a bare glyph — bluetooth's answer is unambiguous (labelled). The wifi popout's *absence* of any such link is itself the finding: it is not that Caelestia hides the link behind a glyph, it simply does not surface one from this specific compact view — an explicit absence, not an omission from this table. |

**Two-word label handling (D-15-21's hard question), stated plainly:** neither reference wraps a multi-word toggle label across two lines. Caelestia shows no label at all; end-4's Android style elides a too-long label to one line. 15-07's two-line-wrap requirement for "Do Not Disturb" has **no adoptable precedent in either reference shell** and must be designed from scratch.

**Stream-with-no-name/icon (per-app volume row's E1 `partial` backstop):** Caelestia's own `getStreamName()` fallback chain terminates in a literal `"Unknown Application"` string — there is no icon fallback shown in the reviewed file (`Icons.getVolumeIcon` keys off volume/muted state, not app identity), consistent with this plan's own A1 finding that no icon-name property was ever observed on a real stream node.

## Open Q2 — QtQuick Popup inside a layer-shell surface (record only)

**Verdict:** measured
**Evidence:** source grep across both clones (`grep -rEn '^\s*(Popup|Menu|ComboBox|Drawer|Dialog)\s*\{'`) plus a direct harness (`harness_openq2.qml`) building a real `PanelWindow` with the panels' own layer posture (`WlrLayershell.layer: WlrLayer.Overlay`, `exclusiveZone: 0`, `WlrLayershell.keyboardFocus: WlrKeyboardFocus.OnDemand`, distinct namespace `quickshell-gsd-openq2-probe`) containing a `QtQuick.Controls.Popup`, checked against `hyprctl layers -j`, `hyprctl clients -j`, and a `grim` screenshot of the live compositor output. Qt version: `6.11.1-1` (`qt6-base`/`qt6-declarative`, confirmed via `pacman -Q`).

**Source evidence — both reference shells' actual usage differs:** `caelestia-dots/shell` avoids bare `Popup` entirely — grepping every file that imports `QtQuick.Controls` (7 files) found exactly one `Menu { }` instantiation (`components/controls/SplitButton.qml:143`, a `Popup` subclass) and zero bare `Popup {}` instances anywhere in the tree. `end-4/dots-hyprland` **does** use bare `Popup {}` — found in `modules/waffle/startMenu/startPage/{AppCategoryGrid,StartUserButton}.qml`, both descendants of `modules/waffle/startMenu/WaffleStartMenu.qml`, which has a `PanelWindow` root (confirmed via `grep -rln PanelWindow` across the `waffle/` module tree) — i.e. a real Popup living inside a real layer-shell surface, in a shipping reference shell.

**Direct measurement, three sub-questions:**
1. **Does the popup's content render at all?** Yes — confirmed two ways: the QML-side `popup.visible`/`popup.opened` both read `true` from t+3s onward (immediately `false` at `Component.onCompleted`, since `open()` takes effect on the next event-loop tick, not synchronously), and a `grim` screenshot taken while the harness was alive shows the popup's actual pixel content ("POPUP CONTENT VISIBLE" text on a blue fill with a yellow border) rendered at its expected screen position.
2. **Does it render inside the parent surface, or spawn a separate window?** **Inside the parent surface.** `hyprctl layers -j` showed exactly **one** layer entry for the probe's namespace (`quickshell-gsd-openq2-probe`, level 3/overlay, `w: 400, h: 300` — the host `PanelWindow`'s own declared size, not the popup's smaller 250×150), and `hyprctl clients -j` showed **no** additional XDG-toplevel client for the popup. The popup composites as a QML item inside the one layer surface the host window already owns — there is no second layer-shell surface or toplevel window per popup.
3. **Does it accept pointer input?** **Unresolved — genuinely not settled, not inferred.** No synthetic-pointer tool is installed on this host (`ydotool`, `wlrctl`, `dotool` all absent; `wtype` exists but is keyboard-only) to dispatch a real click at the popup's `MouseArea` without installing new software, which is out of scope for a record-only measurement. The harness's own `MouseArea.onClicked` handler never fired during the observation window because no click was ever actually sent — this is an absence of a test, not a negative result, and is recorded as such rather than presented as "popups don't accept input."

**`grabToImage` does not work directly on a `PanelWindow` root** (`TypeError: Property 'grabToImage' of object qs::wayland::layershell::WaylandPanelInterface … is not a function`) — this is a genuinely new correction beyond 14-02's Window-root rule: 14-02 established that `grabToImage` needs a `Window` root rather than a bare `Item`, but a Quickshell `PanelWindow` is not itself a plain `Window` for this purpose either. `grim` (external compositor-level screenshot) was the working substitute for visual verification against a real `PanelWindow`.

**Popup stays out of scope for this phase regardless of this measurement's outcome.** D-15-12 and D-15-19 route around `QtQuick.Controls.Popup` by decision, not by ignorance of whether it can render — this measurement does not change either decision. It is recorded so that if a future surface ever needs to revisit the avoidance, the "does it even render/composite correctly here" question is already answered rather than needing to be asked again from zero, while the pointer-input half stays honestly open until a session with synthetic-input tooling available can close it.

## Live mutations and restoration ledger

Every live mutation this plan performed, arm-before-mutate, each re-checked true at the time this section was written (Tasks 1 and 2 combined; Task 4 adds none — it is read-only source/harness work per its own action spec).

- RESTORED bluetooth-rfkill-soft: yes -> yes (toggled via `rfkill unblock bluetooth` to exercise the A3/A5 adapter-enabled reading, then `rfkill block bluetooth` restored it; re-checked live: `rfkill list bluetooth` reports `Soft blocked: yes`, matching pre-state)
- RESTORED wifi-scanner-enabled: false -> false (the A4 harness process that set `scannerEnabled = true` terminated — via its own `timeout` bound — before its final cleanup step ran, due to a harness bug reaching a `TypeError` on an invalidated network object per A4's finding; `scannerEnabled` is a per-process client-side scan request, not persisted NetworkManager state, and the owning process no longer exists. Re-checked live: `wlan0` state is `disconnected`, no active scan; Open Q1's own `harness_openq1.qml` run later in this same session independently set and cleanly restored `scannerEnabled` to `false` with a verified 62s post-stop quiet window, standing as the compensating clean restoration for this key)
- RESTORED nm-wireless-profile-count: 0 -> 0 (the one wireless profile the A4 test created, `WE_1D03C9`, was removed by the plain `forget()` call within the same harness run; re-checked live: `nmcli -t -f TYPE connection show | grep -cx 802-11-wireless` returns `0`)
- RESTORED default-audio-sink: alsa_output.pci-0000_09_00.4.analog-stereo -> alsa_output.pci-0000_09_00.4.analog-stereo (moved live to the Logitech USB sink during A2's measurement, restored via `pactl set-default-sink`; re-checked live: `pactl get-default-sink` reports the onboard sink, matching pre-state)
- RESTORED default-audio-source: alsa_input.usb-Logitech_PRO_X_000000000000-00.mono-fallback -> alsa_input.usb-Logitech_PRO_X_000000000000-00.mono-fallback (A2's source-side write measured no effect at all — the source never actually moved — but `pactl set-default-source` was run regardless as a defensive restore; re-checked live: `pactl get-default-source` reports the Logitech mic, matching pre-state)

All probe-only PipeWire clients (`pw-play` GSD-A1-Probe-Tone, the second transient test stream, `pw-cat --record` GSD-A2-RecordProbe) were killed at the end of their respective measurements; none remain running. The scratch WAV files and all harness `.qml` files lived under `/tmp/gsd-probe-Sws01q/` and are deleted at the end of the executing session (before this plan's final commit).

## Corrections to 15-RESEARCH.md / 15-PATTERNS.md

- **Pattern 2's accessor snippet — CONTRADICTED.** The `.count`/`.get(i)` shape it assumes does not exist on any `UntypedObjectModel` (`Pipewire.nodes`, `Networking.devices`, `Bluetooth.devices`). The measured shape is `.values` (array-like: `.length`, index access, `.map`/`.filter`, but `Array.isArray()` is `false`) for JS-side iteration, and `Repeater { model: <the UntypedObjectModel itself> }` (not `.values`) for one-delegate-per-object UI lists. See A3 above for the full measurement.
- **Pitfall 3 (default-sink write semantics) — PARTIALLY CONTRADICTED, and found to diverge by direction.** The measured behavior on the output side is stronger than the pessimistic reading Pitfall 3 worried about: `preferredDefaultAudioSink` re-routes an already-playing stream live, not merely future ones. But the input side (`preferredDefaultAudioSource`) showed no observable effect at all within a 10-second window against a real second device and a real active capture — a divergence Pitfall 3 did not anticipate in either direction. See A2 above.
- **Pitfall 4 and Pitfall 5 — see A3 and A6 above respectively** (their content is the accessor shape and the tracker requirement measured in this plan; both are CONFIRMED as real gaps RESEARCH.md correctly flagged as unverified, now measured and settled).
- **`PwNodeType` Flag semantics — a genuinely NEW correction, not previously flagged as a risk by RESEARCH.md or PATTERNS.md.** The convenience flag members (`AudioOutStream`, `AudioInStream`, etc.) are pre-combined composites sharing bits with each other; a bitwise-AND membership test against them produces false positives. Any node-type filtering logic in a backend adapter must use exact equality against the composite constant, not bitwise AND. See A3 above.

### Environment divergences from the "Environment facts measured on this host during planning" table

- **Visible APs churned relative to the planning-time snapshot**, as expected of a real neighborhood: the planning-time target `TONLY_TAP_9943D1C` was not present in this session's first scan (a fallback target, `WE_1D03C9`, was used for A4 instead — itself absent from the original planning-time list, appearing only during this session). Several other SSIDs appeared during the 95-second Open Q1 scan window that were not visible at session start (`Younis`, `MONIR`, `WE_BMNNB`, `Ahmed atef 1`, `Vodafone_VDSL_6A60`). This is ordinary AP-visibility churn, not an environment defect, and does not affect any measurement's validity — every measurement re-resolved its target from the live scan rather than assuming a fixed SSID would still be present.
- All other planning-time environment facts (quickshell version `0.3.0-2`, 3 real output sinks, 2 real input sources, `wlan0` present, zero saved wifi profiles, bluetooth adapter `hci0` soft-blocked, full tooling roster present) were re-confirmed live at the start of this session and matched exactly — no other divergence found.
