---
phase: 03-repo-cleanup-fresh-install-reproducibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - theme-engine/.config/theme-engine/theme-doctor
  - theme-engine/.config/theme-engine/theme-stress-test
autonomous: true
requirements: [INST-01, CLEAN-02]
user_setup: []

must_haves:
  truths:
    - "After installing the four genuinely-missing elephant packages, `elephant listproviders` covers every provider walker/config.toml configures (files, providerlist, runner, websearch now active)"
    - "theme-doctor's provider-parity check treats the data-driven `menus` provider correctly: satisfied when elephant-menus is installed, without requiring v2 menu content"
    - "theme-doctor exits 0 (all checks pass) on the dev machine after the elephant fix — no accepted-gap carve-out remains"
    - "theme-doctor asserts `git status --porcelain` is empty (the CLEAN-02 permanent invariant, D-50)"
    - "theme-stress-test requires a clean theme-doctor pass per switch — it no longer tolerates the elephant-provider FAIL"
    - "The Phase-2 false-pass guards still hold: extraction fails loudly on zero results and walker-style.css template-leftover coverage is present (D-67)"
  artifacts:
    - "theme-doctor with a git-status invariant check and a prefix/package-aware menus provider comparison"
    - "theme-stress-test with a strict check_theme_doctor (exit-0 required)"
  key_links:
    - "walker/config.toml configured providers ↔ elephant listproviders active providers (the parity check that proves the launcher backend is whole)"
    - "theme-doctor exit code ↔ theme-stress-test check_theme_doctor (the stress test's per-switch health gate)"
  prohibitions:
    - statement: "Verification tools MUST NOT carry an accepted-gap carve-out that masks a real elephant-provider regression (D-66)"
      status: resolved
      verification: "The 'expected/known gap' comment block is gone from theme-doctor; check_theme_doctor requires theme-doctor exit 0, not 'no failures beyond the accepted gap'"
    - statement: "A generated theme output or a stray repo change MUST NOT leave git dirty undetected (CLEAN-02)"
      status: resolved
      verification: "theme-doctor runs git status --porcelain and FAILs when non-empty"
---

<objective>
Make the reused verification tools strict so they are trustworthy fresh-install evidence: root-cause and close the deferred elephant provider gap (install the four genuinely-missing packages + fix the data-driven `menus` comparison), remove the accepted-gap carve-outs from theme-doctor and theme-stress-test, add the CLEAN-02 git-clean invariant as a permanent theme-doctor check, and confirm the Phase-2 false-pass guards still hold.

Purpose: Satisfies the provider-health half of INST-01 (all configured launcher providers actually register) and the permanent-invariant half of CLEAN-02 (git-clean assertion, D-50). These tools become the machine-readable pass/fail evidence the 03-04 reproduction gate parses.
Output: A strict theme-doctor (git-clean check + correct provider parity, exit 0 clean) and a strict theme-stress-test (no tolerated FAIL), with the elephant provider gap actually closed on the dev machine.
</objective>

## Phase Goal (MVP user story)

**As a** developer reproducing this desktop on a fresh Arch system, **I want to** run install.sh + stow.sh unattended and get a fully themed desktop from a clean repo, **so that** the entire rice reproduces from scratch with one workflow and no manual fixups.

This plan delivers the slice that makes the verification tools honest: they now catch a missing launcher provider and a dirty repo instead of tolerating them, so a green run in the container/VM gate genuinely means the desktop is whole.

## Artifacts this phase produces

New symbols/behaviors introduced by this plan (excluded from drift verification):
- theme-doctor: a new `git status --porcelain` invariant check; a prefix/package-aware `menus` branch in the provider-parity loop; removal of the "expected/known gap" comment block
- theme-stress-test: a strict `check_theme_doctor` (requires theme-doctor exit 0); removal of the accepted-gap tolerance and its explanatory block
- Runtime: the four elephant packages (elephant-files, elephant-providerlist, elephant-runner, elephant-websearch) installed on the dev machine so parity passes

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-CONTEXT.md
@.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-RESEARCH.md
@.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-PATTERNS.md
@walker/.config/walker/config.toml
</context>

<planner_note>
Resolved Open Question (RESEARCH Open Q1, D-66 intent): the `menus` provider is data-driven — `elephant listproviders` prints `menus:<file>` only when a menu file exists, and MENU-01/02/03 are v2-deferred (zero menu files in v1). D-66 says "all installed providers register." The elephant-menus PACKAGE is installed, so the correct, cheapest reading is: treat `menus` as satisfied when the elephant-menus package is installed OR any `menus:*` entry is active — NO placeholder menu file is seeded. This keeps the check strict (fails if the package is missing) without demanding v2 content. Surfaced here so the executor and verifier see the decision; if the user intended stricter (a seeded placeholder menu), flag it before executing.
</planner_note>

<tasks>

<task type="auto">
  <name>Task 1: Close the elephant provider gap and harden theme-doctor (provider parity + git-clean invariant)</name>
  <files>theme-engine/.config/theme-engine/theme-doctor</files>
  <read_first>
    - theme-engine/.config/theme-engine/theme-doctor (check() accumulator lines 20-30; the "expected/known gap" comment block lines 119-125; the exact-match provider-parity loop lines 126-146; the stow-conflict guarded-skip shape lines 74-79 to mirror for the git check)
    - walker/.config/walker/config.toml (configured providers: default array = desktopapplications, calc, runner, websearch, menus; prefixes add calc, files, clipboard, symbols, providerlist, websearch → the full configured set is calc, clipboard, desktopapplications, files, menus, providerlist, runner, symbols, websearch)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-RESEARCH.md (Pitfall 3 two-independent-bugs; Pattern 3 prefix-aware menus match; the four missing packages: elephant-files/elephant-providerlist/elephant-runner/elephant-websearch)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-CONTEXT.md (D-50 git-clean invariant location = theme-doctor; D-66 remove carve-outs, checks become strict)
  </read_first>
  <action>
    Install the four genuinely-missing elephant packages on the dev machine so their providers activate: `paru -S --needed --noconfirm elephant-files elephant-providerlist elephant-runner elephant-websearch`. These are already listed in install.sh AUR_PKGS (they were simply never (re)installed here); after this, `elephant listproviders` covers files/providerlist/runner/websearch.
    Fix the provider-parity comparison (Pattern 3, D-66) at the loop around lines 136-140: replace the flat exact-match with a per-provider branch — for the configured provider `menus`, treat it as satisfied when `elephant listproviders` output contains a line matching `^menus:` OR when `pacman -Q elephant-menus` succeeds (data-driven, registers as `menus:<file>` only once a v2 menu file exists; the installed package is the correct v1 signal). For every other configured provider, keep the exact-line match (`grep -qx`). Accumulate genuinely-missing providers into MISSING as today.
    Remove the "expected/known gap" carve-out comment block (lines 119-125) that says the non-empty gap is expected until Phase 3 — the check is strict now.
    Add the CLEAN-02 git-clean invariant (D-50) as a new `check`: when `git` is available and `$DOTFILES_DIR/.git` exists, compute `git -C "$DOTFILES_DIR" status --porcelain` and pass only when the output is empty; otherwise print a `[SKIP]` line (mirror the stow-conflict guarded-skip shape at lines 74-79). This makes "repo clean after a theme switch" a permanent, rerunnable assertion.
    After the edits, run theme-doctor and confirm it exits 0 (all checks pass) on the seeded, provider-complete dev machine.
  </action>
  <verify>
    <automated>cd ~/dotfiles && D=theme-engine/.config/theme-engine/theme-doctor && bash -n "$D" && shellcheck -S error "$D" && grep -q 'status --porcelain' "$D" && grep -q 'menus:' "$D" && grep -q 'elephant-menus' "$D" && ! grep -qE 'expected/known gap|deferred to Phase 3' "$D" && "$D" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` and `shellcheck -S error` pass on theme-doctor
    - `elephant listproviders` covers all providers configured in walker/config.toml (files, providerlist, runner, websearch active after the install)
    - The provider-parity loop has a `menus` branch matching `^menus:` OR `pacman -Q elephant-menus`; all other providers use exact-line match
    - The "expected/known gap"/"deferred to Phase 3" carve-out comment is gone
    - theme-doctor contains a `git ... status --porcelain` check that FAILs on a dirty repo and SKIPs cleanly when git/.git is absent
    - Running `theme-doctor` exits 0 on the current machine after the fix
  </acceptance_criteria>
  <done>All configured launcher providers register, theme-doctor treats menus correctly without a placeholder, the accepted-gap carve-out is gone, the git-clean invariant is a permanent check, and theme-doctor exits 0 clean.</done>
</task>

<task type="auto">
  <name>Task 2: Make theme-stress-test strict and confirm the Phase-2 false-pass guards hold</name>
  <files>theme-engine/.config/theme-engine/theme-stress-test</files>
  <read_first>
    - theme-engine/.config/theme-engine/theme-stress-test (the accepted-gap tolerance in check_theme_doctor lines 206-231 and its explanatory comment; THEME_DOCTOR_OUTPUT capture used by abort_with_diagnostics at lines 140-146)
    - theme-engine/.config/theme-engine/theme-doctor (now strict after Task 1 — exit 0 means fully healthy)
    - theme-engine/.config/theme-engine/theme-parity (D-67: confirm extraction already fails loudly on zero results and walker-style.css `{{...}}` template-leftover coverage exists — these were fixed in Phase 2 per RESEARCH; add a guard only if a gap is found)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-CONTEXT.md (D-66 remove the stress-test carve-out; D-67 harden/verify false-pass paths)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-RESEARCH.md (Summary paragraph 2: CR-01/WR-01..05 already fixed in Phase 2 — D-67 is verify-and-regression-guard, not re-implement)
  </read_first>
  <action>
    Rewrite `check_theme_doctor` (lines 206-231) to require a strict pass now that Task 1 removed the gap: capture `THEME_DOCTOR_OUTPUT="$("$ENGINE_DIR/theme-doctor" 2>&1)"` (preserve it for abort_with_diagnostics), capture theme-doctor's exit code, and return success only when that exit code is 0. Delete the "already-accepted elephant listproviders gap" tolerance logic and its explanatory comment block (the `unexpected`/`accepted` FAIL-count comparison) — a clean theme-doctor is now the bar, and any [FAIL] aborts the switch as a real regression.
    Update the per-switch check description string (line 356) so it no longer says "no new failures beyond the accepted elephant-provider gap" — it now asserts a clean theme-doctor pass.
    Confirm the D-67 Phase-2 guards still hold by reading theme-parity: (a) contract extraction fails loudly (nonzero / explicit FAIL) when it yields zero results rather than silently passing, and (b) walker-style.css `{{...}}` unrendered-template leftovers are covered by a check. If both hold, record that verification in the SUMMARY and add nothing. Only if a gap is found, add the minimal guard to restore loud failure — do not re-implement what Phase 2 already fixed.
  </action>
  <verify>
    <automated>cd ~/dotfiles && S=theme-engine/.config/theme-engine/theme-stress-test && bash -n "$S" && shellcheck -S error "$S" && ! grep -qE 'accepted elephant-provider gap|already-accepted' "$S" && grep -q 'check_theme_doctor' "$S" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` and `shellcheck -S error` pass on theme-stress-test
    - `check_theme_doctor` returns success only when theme-doctor exits 0; the accepted-gap tolerance logic and comment are removed
    - No occurrence of "accepted elephant-provider gap"/"already-accepted" remains in theme-stress-test
    - The per-switch theme-doctor check description no longer references an accepted gap
    - A SUMMARY note records that theme-parity's zero-result loud-failure and walker-style.css template-leftover coverage (D-67) were verified present (or the minimal guard added if a gap was found)
  </acceptance_criteria>
  <done>theme-stress-test requires a clean theme-doctor pass every switch with no tolerated failures, and the Phase-2 false-pass guards are confirmed live before these tools serve as fresh-install evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification tool → pass/fail verdict | A carve-out that tolerates a known gap can mask a real regression, producing false green evidence for the reproduction gate |
| walker config → elephant runtime | Configured providers must actually register; a data-driven provider (menus) needs a comparison that reflects how it registers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-FALSE | Repudiation (false evidence) | accepted-gap carve-outs in theme-doctor/theme-stress-test | medium | mitigate | Remove the carve-outs; theme-doctor must exit 0 and the stress test requires that (Tasks 1-2) — green now means genuinely healthy |
| T-03-03-INTEG | Tampering (data hygiene) | undetected dirty repo after a switch | low | mitigate | git-status --porcelain invariant added to theme-doctor (D-50), FAILs on any tracked change |
| T-03-SC | Tampering | elephant AUR packages | medium | accept | The four elephant packages are already in install.sh AUR_PKGS and were re-verified via paru -Si in RESEARCH (Phase-1 T-01-SC precedent); no new/unknown package names introduced; pacman keyring GPG chain enforces integrity |
</threat_model>

<verification>
- `bash -n` and `shellcheck -S error` pass on both tools
- `theme-doctor` exits 0 on the dev machine after installing the four elephant packages
- theme-doctor contains the git-status invariant and the prefix/package-aware menus branch; no accepted-gap comment remains
- theme-stress-test's check_theme_doctor requires exit 0; no accepted-gap tolerance remains
- theme-parity's zero-result loud-failure and template-leftover coverage confirmed present (D-67)
</verification>

<success_criteria>
The reused verification tools are strict and trustworthy: every configured launcher provider registers, the data-driven menus provider is handled correctly without a placeholder, the repo-clean invariant is asserted permanently, and no tolerated failure can mask a regression in the fresh-install evidence.
</success_criteria>

<output>
Create `.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-03-SUMMARY.md` when done. Record the D-67 verification result and the menus-provider decision.
</output>
