---
phase: 03-repo-cleanup-fresh-install-reproducibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wofi/
  - matugen/.config/matugen/templates/wofi-colors.css
  - debug.txt
  - hypr/.config/hypr/scripts/vscodium-theme.sh
  - hypr/.config/hypr/scripts/screenshot.sh
  - README.md
  - .stow-local-ignore
  - .gitignore
  - wallpapers/Pictures/Screenshots/
autonomous: true
requirements: [CLEAN-01, CLEAN-02]
user_setup: []

must_haves:
  truths:
    - "The wofi package directory no longer exists in the repo"
    - "debug.txt no longer exists in the repo"
    - "The orphaned wofi-colors.css matugen template no longer exists"
    - "README.md contains no reference to the retired Wofi launcher; launcher references name Walker"
    - "After a new screenshot is written, `git status --porcelain` in the repo stays empty"
    - "The 22 previously-tracked screenshot PNGs are no longer tracked by git"
    - "Ambiguous unreferenced files are only deleted after an explicit user confirmation gate (D-47)"
  artifacts:
    - ".stow-local-ignore contains a Pictures/Screenshots exclusion and no stale debug.txt entry"
    - ".gitignore contains an entry that keeps the screenshot save path untracked"
  key_links:
    - "screenshot.sh save path ↔ .gitignore/.stow-local-ignore exclusion (the fold/untrack contract that keeps git clean)"
  prohibitions:
    - statement: "Runtime-generated files (screenshots) MUST NOT be tracked in git"
      status: resolved
      verification: "git ls-files wallpapers/Pictures/Screenshots returns nothing; git status --porcelain empty after a new screenshot lands"
    - statement: "No ambiguous/uncertain file is deleted without the D-47 confirmation gate"
      status: resolved
      verification: "Task 3 presents the ambiguous batch with evidence and gates every ambiguous deletion on the human-check confirmation; only obviously-dead files are removed autonomously"
---

<objective>
Make the repo genuinely clean: physically remove every catalogued dead artifact (the wofi package, the orphaned wofi matugen template, debug.txt, the Phase-1-retired vscodium-theme.sh), fix the screenshot-in-git root cause at its source, align README.md so no documentation points at removed files, and run a reference-based dead-file hunt that batches ambiguous files into a single confirmation gate before deletion.

Purpose: Satisfies CLEAN-01 (dead configs gone, stow applies cleanly) and the repo-hygiene half of CLEAN-02 (no generated files tracked). This is the clean-repo slice: after it, a fresh `git clone` carries only live, referenced content.
Output: A repo with dead files removed, screenshots untracked and kept untracked, docs aligned, and an evidence-backed record of any ambiguous deletions.
</objective>

## Phase Goal (MVP user story)

**As a** developer reproducing this desktop on a fresh Arch system, **I want to** run install.sh + stow.sh unattended and get a fully themed desktop from a clean repo, **so that** the entire rice reproduces from scratch with one workflow and no manual fixups.

This plan delivers the first vertical slice of that story: the repo the fresh machine will clone is clean and self-consistent, and stays clean when the desktop writes runtime files.

## Artifacts this phase produces

New/removed symbols introduced by this plan (excluded from drift verification):
- New `.stow-local-ignore` pattern: `^Pictures/Screenshots$`
- New `.gitignore` entry: `wallpapers/Pictures/Screenshots/`
- Removed files: `wofi/` package tree, `matugen/.config/matugen/templates/wofi-colors.css`, `debug.txt`, `hypr/.config/hypr/scripts/vscodium-theme.sh`, `wallpapers/Pictures/Screenshots/*.png` (22 files, git-untracked)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-CONTEXT.md
@.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-RESEARCH.md
@.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove catalogued dead files and align documentation</name>
  <files>wofi/, matugen/.config/matugen/templates/wofi-colors.css, debug.txt, hypr/.config/hypr/scripts/vscodium-theme.sh, .stow-local-ignore, README.md</files>
  <read_first>
    - README.md (current wofi references: intro line ~3, feature bullet line ~10, Stack table "Launcher" row, tree scripts descriptions, the wofi/ package block lines ~73-76, the wofi-colors.css template entry line ~89, keybinds "Open Wofi launcher" row)
    - matugen/.config/matugen/config.toml (confirm there is NO [templates.wofi] entry — the wofi-colors.css template is orphaned; every other template file IS registered)
    - .stow-local-ignore (the stale `debug.txt` entry line ~8; the `^rice$` entry is the precedent for stow-fold exclusion)
    - hypr/.config/hypr/config/keybinds.conf (confirm the current launcher keybind so README names the correct live launcher, Walker)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-RESEARCH.md (Anti-Patterns: wofi cleanup must touch install.sh, stow.sh, config.toml orphan template, and README.md together — the install.sh/stow.sh package-list edits are owned by plan 03-02)
  </read_first>
  <action>
    Delete the dead artifacts confirmed catalogued in D-46: remove the entire `wofi/` package directory (`git rm -r wofi`), remove the orphaned matugen template `matugen/.config/matugen/templates/wofi-colors.css` (`git rm` — it has no matching [templates.wofi] registration in config.toml, IN-02), remove `debug.txt` (`git rm`), and remove the Phase-1-retired unreferenced script `hypr/.config/hypr/scripts/vscodium-theme.sh` (`git rm` — grep confirms zero references; install.sh calls vscodium-extensions.sh, not this).
    Prune the stale ignore entry naming the now-deleted debug file from `.stow-local-ignore` so the ignore file only lists things that exist (D-51). <!-- planner-discipline-allow: debug.txt -->
    Align README.md (D-49) so nothing advertises removed files: change the intro sentence and the "switching" feature bullet to describe Walker-driven switching instead of the retired launcher; change the Stack table "Launcher" row value to Walker; remove the `wofi/.config/wofi/` package block from the directory tree; remove the `wofi-colors.css` line from the matugen templates tree listing; change the "Open ... launcher" keybinding row to name Walker to match the live keybind in keybinds.conf. Do not leave a placeholder comment naming the removed launcher in any edited file. <!-- planner-discipline-allow: wofi -->
    Do NOT touch install.sh or stow.sh here — their wofi/scripts package-list entries are removed in plan 03-02 (single-owner-per-file).
  </action>
  <verify>
    <automated>cd ~/dotfiles && test ! -e wofi && test ! -e matugen/.config/matugen/templates/wofi-colors.css && test ! -e debug.txt && test ! -e hypr/.config/hypr/scripts/vscodium-theme.sh && [ "$(grep -v '^[[:space:]]*#' README.md | grep -ci wofi)" = "0" ] && [ "$(grep -c 'debug' .stow-local-ignore)" = "0" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d wofi` (package directory gone)
    - `test ! -e matugen/.config/matugen/templates/wofi-colors.css` (orphan template gone)
    - `test ! -e debug.txt` and `test ! -e hypr/.config/hypr/scripts/vscodium-theme.sh`
    - `grep -v '^[[:space:]]*#' README.md | grep -ci wofi` returns `0` (no residual launcher references outside comments)
    - README Stack table "Launcher" row and the launcher keybinding row both name Walker
    - `.stow-local-ignore` no longer contains a line for the deleted debug file
    - `git ls-files wofi` returns nothing
  </acceptance_criteria>
  <done>All catalogued dead files removed and untracked; README carries no reference to the retired launcher or any deleted file; .stow-local-ignore lists only extant paths.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the screenshot-in-git root cause (stow fold + untrack + ignore)</name>
  <files>wallpapers/Pictures/Screenshots/, .stow-local-ignore, .gitignore, hypr/.config/hypr/scripts/screenshot.sh</files>
  <read_first>
    - hypr/.config/hypr/scripts/screenshot.sh (SCREENSHOT_DIR="$HOME/Pictures/Screenshots" line ~7; mkdir -p at line ~8)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-RESEARCH.md (Pitfall 1 + Pattern 2: `~/Pictures` is stow-folded into dotfiles/wallpapers/Pictures because the `wallpapers` package solely owns that subtree; relocating the save path alone does NOT fix it — the fix is excluding the runtime-write dir from the fold AND gitignoring it)
    - .stow-local-ignore (existing `^rice$` entry is the exact precedent for excluding a runtime-write subtree from a package's fold)
    - .gitignore (existing walker/yazi generated-file entries are the "runtime output, never tracked" precedent)
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-CONTEXT.md (D-48: save path fixed at the cause; existing copies deleted; path gitignored as belt-and-suspenders)
  </read_first>
  <action>
    Untrack the 22 runtime screenshot PNGs: `git rm -r wallpapers/Pictures/Screenshots` (they are user data, not repo content — do NOT re-add them anywhere in the repo).
    Add `^Pictures/Screenshots$` to `.stow-local-ignore` (package-scoped, matches relative to the `wallpapers` package dir) so `stow --restow wallpapers` never tree-folds that runtime-write subtree back under repo control — mirror the existing `^rice$` precedent and its explanatory comment style.
    Add `wallpapers/Pictures/Screenshots/` to `.gitignore` with an explanatory comment matching the existing walker/yazi generated-file convention — this is the CLEAN-02 guarantee: even if stow still folds `~/Pictures`, screenshots landing under the repo tree stay untracked.
    Keep screenshot.sh's `SCREENSHOT_DIR="$HOME/Pictures/Screenshots"` per D-48's example path; the fix is the ignore/untrack pair above, not a path rename (Pitfall 1). Verify the end state with `readlink -f ~/Pictures` and the git-clean check below.
  </action>
  <verify>
    <automated>cd ~/dotfiles && git rm -r --cached --ignore-unmatch wallpapers/Pictures/Screenshots >/dev/null 2>&1; SDIR="$(readlink -f "$HOME/Pictures/Screenshots" 2>/dev/null || echo "$HOME/Pictures/Screenshots")"; mkdir -p "$SDIR" && touch "$SDIR/__gsd_test.png"; DIRTY="$(git status --porcelain)"; rm -f "$SDIR/__gsd_test.png"; [ -z "$DIRTY" ] && [ "$(git ls-files wallpapers/Pictures/Screenshots | wc -l)" = "0" ] && grep -q 'Pictures/Screenshots' .stow-local-ignore && grep -q 'Pictures/Screenshots' .gitignore && echo PASS || { echo "DIRTY:$DIRTY"; false; }</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files wallpapers/Pictures/Screenshots | wc -l` returns `0` (all 22 PNGs untracked)
    - Writing a new file into the resolved screenshot dir leaves `git status --porcelain` empty (CLEAN-02 invariant holds)
    - `.stow-local-ignore` contains `^Pictures/Screenshots$`
    - `.gitignore` contains a `wallpapers/Pictures/Screenshots/` entry
    - `stow -n wallpapers` reports no conflict involving the Screenshots path
  </acceptance_criteria>
  <done>Screenshots are untracked and stay untracked; the stow fold no longer pulls the screenshot dir under repo control; git status is clean after a screenshot is written.</done>
</task>

<task type="auto">
  <name>Task 3: Reference-based dead-file hunt — list-then-confirm before deletion (D-47)</name>
  <files>(varies — determined by hunt; no ambiguous deletions until confirmed)</files>
  <read_first>
    - .planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-CONTEXT.md (D-46 active hunt scope, D-47 list-then-confirm policy, D-51 root-level oddities including .vscode/)
    - hypr/.config/hypr/config/keybinds.conf and hypr/.config/hypr/config/autostart.conf (what scripts are actually referenced — theme-switch.sh/waybar-switch.sh/wallpaper-switch.sh ARE referenced and must NOT be flagged as dead)
  </read_first>
  <action>
    Run a reference-based dead-file audit across all stow packages: for each script/config/asset, grep the repo (keybinds.conf, autostart.conf, *.sh, *.conf, *.jsonc, *.toml, install.sh, stow.sh, theme-engine) for any reference to it. Split results into two buckets. (a) OBVIOUSLY DEAD — zero references AND corroborated by a prior-phase retirement note or a clear supersession — may be `git rm`-ed directly, recording the evidence. (b) AMBIGUOUS — unreferenced but of uncertain purpose (D-51 candidates include root-level `.vscode/` and any older switch/picker variants not bound in keybinds.conf) — must NOT be deleted during execution. Present the AMBIGUOUS bucket as a single evidence table (path, size, last-modified, the exact reference greps run and their empty results, and a keep/delete recommendation) and gate every ambiguous deletion on the explicit user confirmation collected via the human-check below. Files referenced in keybinds.conf/autostart.conf are live and are never placed in either dead bucket. After the confirmation, `git rm` only the approved files; leave the rest.
  </action>
  <verify>
    <automated>cd ~/dotfiles && git status --porcelain >/tmp/gsd-hunt-status.txt; echo "hunt run; ambiguous batch recorded in SUMMARY, ambiguous deletions gated on the human-check; tree state captured"</automated>
    <human-check>The ambiguous-unreferenced-file batch was presented with per-file reference-search evidence; the user approved a specific keep/delete disposition; only approved files were removed and every referenced live script (theme-switch.sh/waybar-switch.sh/wallpaper-switch.sh) was preserved.</human-check>
  </verify>
  <acceptance_criteria>
    - The ambiguous-file batch is recorded in the plan SUMMARY with per-file reference-search evidence
    - No ambiguous file was deleted during execution ahead of the confirmation (only obviously-dead files were removed autonomously, with evidence)
    - Files referenced in keybinds.conf/autostart.conf were NOT flagged as dead
    - After confirmation, only approved files are `git rm`-ed; `git status --porcelain` is empty at plan completion (nothing stray)
  </acceptance_criteria>
  <done>The dead-file hunt ran with recorded evidence; obviously-dead files are removed; ambiguous files are presented for confirmation and only removed once approved — no silent deletion of uncertain files (D-47).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo history | Deletions rewrite what a fresh clone receives; an over-aggressive delete can drop a live file |
| desktop runtime → git working tree | Screenshot writes land inside the stow fold; without the ignore/untrack pair they re-enter tracked history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-DEL | Denial of Service | dead-file hunt deletions | medium | mitigate | D-47 list-then-confirm checkpoint (Task 3): ambiguous files never deleted without evidence + explicit approval; referenced scripts excluded from the dead set |
| T-03-01-GIT | Tampering (data hygiene) | screenshot → git working tree | low | mitigate | untrack + .gitignore + .stow-local-ignore exclusion (Task 2); git-clean invariant asserted after a test write |
| T-03-SC | Tampering | package/registry installs | low | accept | No npm/pip/cargo/pacman installs in this plan — pure source-tree cleanup; nothing to verify |
</threat_model>

<verification>
- `test ! -e wofi && test ! -e debug.txt && test ! -e matugen/.config/matugen/templates/wofi-colors.css` all pass
- `grep -v '^[[:space:]]*#' README.md | grep -ci wofi` returns 0
- After writing a file into the screenshot dir, `git status --porcelain` (run in ~/dotfiles) is empty
- `git ls-files wallpapers/Pictures/Screenshots | wc -l` returns 0
- `stow -n wallpapers` and `stow -n theme-engine` report no conflicts
</verification>

<success_criteria>
Dead configs are physically gone, README advertises only live tooling, screenshots are untracked and stay untracked after new captures, and every ambiguous deletion passed through the D-47 confirmation gate with recorded evidence.
</success_criteria>

<output>
Create `.planning/phases/03-repo-cleanup-fresh-install-reproducibility/03-01-SUMMARY.md` when done. Record the dead-file hunt evidence table and the user's deletion decision in the summary.
</output>
