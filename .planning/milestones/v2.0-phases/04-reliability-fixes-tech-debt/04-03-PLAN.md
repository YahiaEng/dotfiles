---
phase: 04-reliability-fixes-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - zshell/.zshrc
  - zshell/.config/oh-my-posh/catppuccin.omp.json
  - fastfetch/.config/fastfetch/config.jsonc
autonomous: true
requirements: [FIX-03]
user_setup: []

must_haves:
  truths:
    - "Opening a new kitty terminal reaches a usable prompt fast — warm time-to-usable-prompt is measurably reduced versus the captured baseline, with before/after hyperfine numbers recorded (D-21, ROADMAP SC-3)"
    - "oh-my-posh initializes from a locally vendored theme JSON — no remote GitHub URL is fetched at shell startup (D-03)"
    - "node tooling (nvm/bun) is lazy-loaded — not sourced synchronously on every interactive shell start (D-04)"
    - "fastfetch still greets every new interactive terminal, trimmed only for modules proven slow by fastfetch --stat evidence (D-02)"
    - "If zprof implicates zinit plugin loading, plugins load via turbo mode (wait/lucid) without being dropped (D-05)"
  artifacts:
    - "zshell/.config/oh-my-posh/catppuccin.omp.json"
    - "zshell/.zshrc"
    - "fastfetch/.config/fastfetch/config.jsonc"
    - ".planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md"
  key_links:
    - "zshrc oh-my-posh init -> local $HOME/.config/oh-my-posh/catppuccin.omp.json (no network fetch)"
    - "zshrc lazy shim -> nvm.sh sourced on first node/npm/npx/bun invocation"
  prohibitions:
    - statement: "fastfetch box-frame and module grouping must stay visually identical; only the disk and gpu modules are removal candidates and only with fastfetch --stat evidence; os/kernel/packages/uptime/wm/terminal/shell/terminalfont/cpu/memory/display/colors must NOT be removed (D-02)"
      status: resolved
      verification: "config.jsonc still contains the box-frame custom modules and the retained module keys; any removal has a --stat number recorded in the SUMMARY"
    - statement: "kitty.conf repaint_delay/input_delay/sync_to_monitor must NOT be touched — they are rendering-latency knobs, not shell-startup knobs (Pitfall 3)"
      status: resolved
      verification: "kitty/.config/kitty/kitty.conf is not in this plan's files_modified and git diff shows it unchanged"
    - statement: "No zinit plugins or snippets may be dropped — turbo-mode async loading only, not removal (D-05)"
      status: resolved
      verification: "the set of zinit plugin/snippet names in .zshrc is unchanged (only wrapped in wait/lucid if applicable)"
    - statement: "fastfetch must NOT be removed from the shell greeting — trim modules only (D-02, project decision: it is part of the experience)"
      status: resolved
      verification: ".zshrc still invokes fastfetch under the interactive guard"
---

<objective>
Profile kitty/shell startup, identify the real cost centers with evidence, and fix them (FIX-03): vendor the oh-my-posh theme locally so no remote GitHub URL is fetched at shell start (D-03), lazy-load nvm/bun (D-04), move zinit plugins to turbo mode if profiling implicates them (D-05), and trim fastfetch modules that fastfetch --stat proves slow (D-02) — without removing fastfetch or masking the cost with kitty single-instance tricks (D-06).

Purpose: Opening a new terminal must feel instant. Every named cost center is confirmed present in the current .zshrc; the fix goes wherever profiling points (D-01). This plan delivers the optimized zsh baseline that Plan 04 benchmarks fish against.

Output: An optimized .zshrc, a vendored oh-my-posh theme JSON, an evidence-trimmed fastfetch config, and before/after hyperfine numbers proving the regression is gone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md
@.planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md
@.planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md
@zshell/.zshrc
@fastfetch/.config/fastfetch/config.jsonc
</context>

## Artifacts this phase produces (Plan 03)

New or changed symbols introduced by this plan (excluded from drift verification):

- `zshell/.config/oh-my-posh/catppuccin.omp.json` — NEW vendored oh-my-posh catppuccin theme file inside the zshell stow package (maps to ~/.config/oh-my-posh/catppuccin.omp.json)
- `lazy_load_nvm` shell function + the `nvm node npm npx bun` shim loop in `zshell/.zshrc` (D-04 lazy-load pattern)
- `zinit wait lucid for` turbo block(s) in `zshell/.zshrc` (D-05, conditional on zprof evidence)
- Changed oh-my-posh init line in `zshell/.zshrc` (remote URL -> local path)
- Evidence-gated module removals in `fastfetch/.config/fastfetch/config.jsonc` (disk/gpu candidates only)
- `.planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md` — before/after profile numbers (D-21/D-25)

<tasks>

<task type="auto">
  <name>Task 1: Capture the startup profiling baseline (zprof + hyperfine + fastfetch --stat)</name>
  <files>.planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md (Standard Stack profiling tools; Pattern 3 fastfetch --stat; Anti-Patterns "benchmarking kitty startup by measuring only process spawn time"; Code Examples "Benchmark: kitty time-to-usable-prompt"; Don't Hand-Roll table)
    - .planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md (D-01, D-02, D-21 concrete budget ~400ms warm, Claude's Discretion on methodology)
    - .planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md (zshell/.zshrc cost centers with exact line refs)
    - zshell/.zshrc (fastfetch guard lines 4-7; oh-my-posh remote line 46; zinit lines 24-37; nvm/bun lines 102-111)
    - fastfetch/.config/fastfetch/config.jsonc (module list — disk stats /, gpu on NVIDIA)
  </read_first>
  <action>
    Install the diagnostic tool if missing: `sudo pacman -S --needed --noconfirm hyperfine` (official `extra` repo, Approved in RESEARCH.md Package Legitimacy Audit — no checkpoint needed). Do NOT add hyperfine to install.sh — it is a one-time diagnostic tool, not a runtime dependency (D-24, no permanent gate).

    Capture the BEFORE baseline and record every number in 04-03-SUMMARY.md:
    1. Shell-init cost in isolation: `hyperfine --warmup 3 --min-runs 10 'zsh -i -c exit'` — record mean/stddev/min/max.
    2. Per-plugin/per-line breakdown: temporarily add `zmodload zsh/zprof` as the first line and `zprof` as the last line of a COPY of .zshrc (or set them via a throwaway session), open one interactive shell, and record the top zprof entries (which of oh-my-posh / zinit plugins / nvm / bun / compinit dominate). Remove the temporary zprof lines afterward — do not commit them.
    3. fastfetch module timing: `fastfetch --stat` — record the per-module timings, specifically for `disk` (stats /) and `gpu` (shells out on NVIDIA), which RESEARCH.md names as the concrete present-and-plausible slow candidates (the DisplayManager/battery/power modules named generically in D-02 are NOT in this repo's config).

    Do not modify .zshrc, the oh-my-posh config, or fastfetch config in this task — measure and record only. The baseline is the anchor for the D-21/ROADMAP-SC-3 before/after comparison.
  </action>
  <verify>
    <automated>command -v hyperfine >/dev/null && test -f .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md && grep -qiE "hyperfine|zprof|--stat|baseline|before" .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - hyperfine is installed and on PATH
    - 04-03-SUMMARY.md contains a "Baseline (before)" section with the `zsh -i -c exit` warm mean, the top zprof cost centers, and the fastfetch --stat per-module timings
    - The dominant cost centers are named explicitly (which of oh-my-posh remote fetch / zinit / nvm / bun / fastfetch modules are slow)
    - No temporary zprof lines remain committed in .zshrc; source files unchanged in this task
  </acceptance_criteria>
  <done>The before-baseline profile is captured with concrete numbers and the dominant cost centers are identified from evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply the profile-driven optimizations to .zshrc and fastfetch</name>
  <files>zshell/.zshrc, zshell/.config/oh-my-posh/catppuccin.omp.json, fastfetch/.config/fastfetch/config.jsonc, .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md (the baseline + named cost centers from Task 1)
    - .planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md (zshell/.zshrc "Pattern to apply" for oh-my-posh local vendor (D-03), zinit turbo (D-05), nvm/bun lazy shim (D-04); "Untouched lines" list; "Guard-before-cost shell pattern")
    - .planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md (Pattern 4 zinit turbo; Pattern 5 nvm/bun lazy shim — copy the exact code from these patterns; Pattern 3 fastfetch trim; Deprecated: remote oh-my-posh init)
    - zshell/.zshrc (all cost-center lines), fastfetch/.config/fastfetch/config.jsonc (full module list)
  </read_first>
  <action>
    Apply the fixes for whichever cost centers Task 1's baseline implicated (D-01: follow the profile). All four named mechanisms are pre-approved by CONTEXT.md; apply the ones the profile confirms are slow:

    D-03 oh-my-posh local vendor: create zshell/.config/oh-my-posh/catppuccin.omp.json by fetching the catppuccin theme JSON that the current remote URL points to and saving it inside the zshell stow package (so it maps to ~/.config/oh-my-posh/catppuccin.omp.json via stow, offline-safe). Change the .zshrc oh-my-posh init line to point at the local path `$HOME/.config/oh-my-posh/catppuccin.omp.json` instead of the https GitHub URL. No remote fetch at shell start.

    D-04 nvm/bun lazy-load: replace the synchronous nvm.sh + bun sourcing with the lazy shim from RESEARCH.md Pattern 5 — keep the NVM_DIR/BUN_INSTALL/PATH exports, but defer sourcing nvm.sh and _bun until the first nvm/node/npm/npx/bun invocation via the self-unsetting shim function. Preserve the existing eager fzf/zoxide evals and PATH lines (do not change lines RESEARCH/PATTERNS mark as untouched).

    D-05 zinit turbo (only if zprof implicated zinit): wrap the `zinit light`/`zinit snippet` calls in `zinit wait lucid for` turbo blocks per RESEARCH.md Pattern 4. Do NOT drop any plugin or snippet — every current plugin/snippet name must still be present.

    D-02 fastfetch trim (evidence-gated): remove ONLY the modules that fastfetch --stat proved slow (disk and/or gpu are the candidates). Keep the box-frame custom modules and the retained module set (os/kernel/packages/uptime/wm/terminal/shell/terminalfont/cpu/memory/display/colors) so the greeting stays visually the same shape. If --stat showed disk/gpu are cheap on this hardware, leave them and record that in the SUMMARY — do not trim blindly. Do NOT remove fastfetch from .zshrc.

    Do NOT touch kitty.conf (Pitfall 3 — rendering knobs, not startup). Record which fixes were applied and why in the SUMMARY.
  </action>
  <verify>
    <automated>grep -Eq 'oh-my-posh init zsh --config .*\$HOME/\.config/oh-my-posh/catppuccin\.omp\.json' zshell/.zshrc && ! grep -Eq 'oh-my-posh init.*https?://' zshell/.zshrc && test -f zshell/.config/oh-my-posh/catppuccin.omp.json && grep -q 'lazy_load_nvm' zshell/.zshrc && grep -qE '(if \[ -t 0 \]|status is-interactive).*|fastfetch' zshell/.zshrc && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - zshell/.config/oh-my-posh/catppuccin.omp.json exists and is valid JSON (`jq . zshell/.config/oh-my-posh/catppuccin.omp.json` exits 0)
    - .zshrc oh-my-posh init points at `$HOME/.config/oh-my-posh/catppuccin.omp.json` and no longer contains an `http`/`https` URL on the oh-my-posh line
    - .zshrc defines `lazy_load_nvm` and shims nvm/node/npm/npx/bun; NVM_DIR and BUN_INSTALL exports retained
    - If zinit was implicated, plugin/snippet names are wrapped in `zinit wait lucid` and none are removed (same names present as before)
    - fastfetch config.jsonc retains the box-frame and the non-trimmed module keys; any removed module has a --stat number in the SUMMARY
    - fastfetch is still invoked in .zshrc under the interactive guard; kitty.conf untouched (git diff)
  </acceptance_criteria>
  <done>The profile-driven optimizations are applied: local oh-my-posh, lazy nvm/bun, optional zinit turbo, evidence-gated fastfetch trim — fastfetch retained, kitty.conf untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure and prove the regression is gone (before/after)</name>
  <files>.planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md (the baseline numbers from Task 1 and the applied fixes from Task 2)
    - .planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md (D-21 ~400ms warm starting target; D-25 documentation)
  </read_first>
  <action>
    Re-run the same measurements as Task 1's baseline under identical conditions and record the AFTER numbers next to the BEFORE numbers in 04-03-SUMMARY.md: `hyperfine --warmup 3 --min-runs 10 'zsh -i -c exit'` for shell-init, and `fastfetch --stat` (or `hyperfine 'fastfetch'`) for the greeting. Compute the delta. The hard gate is that the AFTER warm mean is lower than the BEFORE warm mean (the regression is measurably reduced, ROADMAP SC-3). Note whether the combined shell-init + fastfetch warm time meets the ~400ms starting target from D-21; if it does not, record the residual cost and the remaining candidate (this feeds Plan 04's fish benchmark, where "speed + feel, user judges" per D-08). Write the before/after table and the conclusion into the SUMMARY.
  </action>
  <verify>
    <automated>grep -qiE "after|optimi[sz]ed" .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md && grep -qiE "before|baseline" .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md && hyperfine --warmup 3 --min-runs 10 'zsh -i -c exit' >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 04-03-SUMMARY.md contains a before/after comparison table with concrete hyperfine warm means for both
    - The AFTER shell-init warm mean is lower than the BEFORE warm mean (delta recorded)
    - The conclusion states whether the ~400ms warm target is met and names any residual cost
    - `hyperfine --warmup 3 --min-runs 10 'zsh -i -c exit'` runs cleanly (optimized .zshrc has no init errors)
  </acceptance_criteria>
  <done>Before/after numbers prove the startup regression is measurably reduced, and the residual (if any) is documented for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell startup -> network | The current oh-my-posh init fetches a config from a remote GitHub URL at every interactive shell start |
| lazy shim eval -> shell | The nvm/bun lazy shim uses `eval`/function definitions during shell init |
| fastfetch modules -> system probes | fastfetch modules shell out to probe disk/gpu/etc. at greeting time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure / Tampering | oh-my-posh remote-URL fetch at shell start | medium | mitigate | Vendor the theme JSON locally (D-03) — removes the per-shell network fetch of remotely-controlled config, making shell startup offline-safe and reproducible |
| T-04-08 | Tampering | nvm/bun lazy-load shim (eval of function definitions) | low | accept | The shim only wraps fixed command names (nvm/node/npm/npx/bun) with no untrusted input; standard community pattern copied verbatim from RESEARCH.md Pattern 5 |
| T-04-09 | Denial of Service (self) | slow synchronous shell startup | medium | mitigate | Profile-driven fixes (lazy-load, turbo, local prompt) reduce time-to-usable-prompt; verified by before/after hyperfine numbers |
</threat_model>

<verification>
- FIX-03: before/after hyperfine warm means recorded; AFTER < BEFORE (ROADMAP SC-3, D-21) — one-time documented protocol in 04-03-SUMMARY.md (D-24, no permanent gate)
- oh-my-posh init points at a local path with no http(s) URL (D-03); vendored JSON is valid (`jq .` exits 0)
- nvm/bun lazy shim present; fzf/zoxide/PATH lines preserved (D-04)
- zinit plugins wrapped in turbo (if implicated) with none dropped (D-05)
- fastfetch retained and only evidence-trimmed; box-frame + retained modules intact; kitty.conf untouched (D-02, Pitfall 3)
</verification>

<success_criteria>
- New kitty terminal reaches a usable prompt measurably faster than baseline, profiled before/after (regression gone)
- No remote URL fetched at shell start; node tooling lazy-loaded; plugins async (not dropped); fastfetch trimmed only with evidence and never removed
- Optimized zsh baseline is documented and ready for Plan 04's fish benchmark
</success_criteria>

<output>
Create `.planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md` when done — including the baseline profile, the applied fixes with rationale, and the before/after comparison table.
</output>
