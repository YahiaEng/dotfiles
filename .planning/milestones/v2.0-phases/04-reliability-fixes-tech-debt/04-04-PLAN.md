---
phase: 04-reliability-fixes-tech-debt
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-03]
files_modified:
  - fish/.config/fish/config.fish
  - fish/.config/fish/functions/y.fish
  - kitty/.config/kitty/kitty.conf
  - install.sh
  - stow.sh
autonomous: false
requirements: [FIX-03]
user_setup: []

must_haves:
  truths:
    - "Optimized zsh and fish are benchmarked side-by-side (cold + warm time-to-usable-prompt via hyperfine) with a documented trade-off note (D-07/D-08)"
    - "The user selects the winning shell at a decision checkpoint on speed + feel (D-08)"
    - "If fish is selected, kitty launches fish via the kitty.conf shell directive (login shell stays zsh, no chsh), install.sh installs fish from PACMAN_PKGS, and fish is added to stow.sh (D-09/D-12)"
    - "If fish is selected, fish reaches day-one parity: vendored oh-my-posh prompt, fzf, zoxide, trimmed fastfetch greeting, working node tooling (D-10)"
    - "The zshell stow package is retained as an installable fallback regardless of the outcome (D-11)"
  artifacts:
    - "fish/.config/fish/config.fish"
    - ".planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md"
  key_links:
    - "kitty.conf shell directive -> fish -> config.fish parity init"
    - "install.sh PACMAN_PKGS fish -> stow.sh PACKAGES fish -> fish/.config/fish/config.fish"
  prohibitions:
    - statement: "Must NOT chsh the system login shell — the shell is set via kitty.conf only; the login shell stays zsh so the TTY recovery path keeps the proven zsh setup (D-12)"
      status: resolved
      verification: "no chsh call added for fish; stow.sh's existing `sudo chsh -s $(which zsh)` line is unchanged; git grep for chsh shows no fish target"
    - statement: "Must NOT remove or retire the zshell stow package — it stays as a working recovery/fallback shell (D-11)"
      status: resolved
      verification: "zshell remains in stow.sh PACKAGES and the zshell/ directory is intact"
    - statement: "Must NOT evaluate nushell or any shell other than fish — fish is the only candidate; nushell is a deferred idea (D-07)"
      status: resolved
      verification: "the benchmark compares only optimized zsh and fish; no nushell install or config appears"
    - statement: "If zsh wins, fish must NOT be wired into kitty.conf/install.sh/stow.sh — no shell switch happens (D-08)"
      status: resolved
      verification: "when the decision is zsh, git diff shows kitty.conf/install.sh/stow.sh unchanged"
---

<objective>
Benchmark the optimized zsh (from Plan 03) against fish side-by-side (D-07 — regardless of whether optimized zsh already meets target), let the user pick the winner on speed + feel at a decision checkpoint (D-08), and — only if fish wins — do a full, parity-complete switch via kitty.conf (not chsh, D-12), install.sh, and stow.sh (D-09). The zshell package is retained as a fallback either way (D-11).

Purpose: Close FIX-03 with a data-backed shell choice. The switch is declarative (stow + kitty config), reproducible via install.sh, and keeps zsh as the TTY recovery path.

Output: A zsh-vs-fish benchmark comparison, a recorded user decision, and — conditionally — an adopted fish shell at day-one parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md
@.planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md
@.planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md
@.planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md
@zshell/.zshrc
@kitty/.config/kitty/kitty.conf
@stow.sh
</context>

## Artifacts this phase produces (Plan 04)

New or changed symbols introduced by this plan (excluded from drift verification):

- `fish/.config/fish/config.fish` — NEW fish shell init, cross-ported from zshell/.zshrc for the benchmark and (if adopted) daily use
- `fish/.config/fish/functions/y.fish` — NEW fish port of the `y()` yazi-cd function (only if the parity build needs it)
- `.planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md` — benchmark comparison table, trade-off note, and the recorded decision (D-25)
- Conditional on fish winning: `shell fish` directive in `kitty/.config/kitty/kitty.conf`; `fish` in `install.sh` PACMAN_PKGS; `fish` in `stow.sh` PACKAGES

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Package-legitimacy gate for nvm.fish (fisher plugin, [ASSUMED])</name>
  <action>Human checkpoint — the executor pauses before any fisher/nvm.fish install until the resume-signal. This gate is NOT auto-approvable (package legitimacy). The human verifies the nvm.fish plugin and fisher, or approves the native fallback, before node tooling is set up in Task 2's build.</action>
  <what-built>The plan needs to build a fish parity config to benchmark it, and D-10's node-tooling parity uses the `nvm.fish` fisher plugin, which RESEARCH.md flags as [ASSUMED] (discovered via WebSearch, not independently verified). Per the package-legitimacy gate, an [ASSUMED] package must be human-verified before install. This checkpoint is not auto-approvable.</what-built>
  <how-to-verify>
    1. Confirm `fish` is the official `extra`-repo package (Approved in RESEARCH.md): `pacman -Si fish` shows Repository : extra, version ~4.8.0. Official-repo, no further vetting needed.
    2. Verify the `nvm.fish` fisher plugin (github.com/jorgebucaran/nvm.fish) before any `fisher install`: open the GitHub repo, confirm it is the maintained jorgebucaran project (stars, recent commits, real README), and that fisher itself (github.com/jorgebucaran/fisher) is legitimate. Confirm you are comfortable installing it into the fish config.
    3. If you prefer NOT to use nvm.fish, approve the fallback: port the same lazy-load-on-first-invocation node-tooling pattern natively in config.fish without a third-party plugin.
  </how-to-verify>
  <resume-signal>Type "approved: nvm.fish" to allow the fisher plugin, or "approved: native fallback" to build node tooling without it, or "reject" to skip node tooling in the benchmark.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install fish, build the parity config, and benchmark it against optimized zsh</name>
  <files>fish/.config/fish/config.fish, fish/.config/fish/functions/y.fish, .planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-03-SUMMARY.md (optimized-zsh AFTER numbers — the baseline fish must beat/match)
    - .planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md (fish/.config/fish/config.fish analog — the D-10-parity mapping table: oh-my-posh `... | source`, `fzf --fish | source`, `zoxide init --cmd cd fish | source`, `status is-interactive` guard, nvm.fish; NEW stow package structure)
    - .planning/phases/04-reliability-fixes-tech-debt/04-RESEARCH.md (Standard Stack fish + nvm.fish rows; Assumptions Log A1; Anti-Patterns "benchmarking kitty startup by measuring only process spawn time")
    - .planning/phases/04-reliability-fixes-tech-debt/04-CONTEXT.md (D-07 benchmark regardless, D-08 decision rule, D-10 parity checklist non-negotiable items, Claude's Discretion: fish config organization + benchmark methodology)
    - zshell/.zshrc (the source config each parity item ports from), zshell/.config/oh-my-posh/catppuccin.omp.json (shared vendored prompt from Plan 03)
  </read_first>
  <action>
    Install fish (official `extra`, Approved): `sudo pacman -S --needed --noconfirm fish`. After the checkpoint's decision, set up node tooling accordingly (fisher + nvm.fish if approved, or the native fallback).

    Build the fish parity config in a NEW fish stow package following the PATTERNS.md mapping table — every D-10 non-negotiable parity item maps 1:1 from .zshrc:
    - oh-my-posh prompt from the SAME locally vendored JSON Plan 03 created (`oh-my-posh init fish --config ~/.config/oh-my-posh/catppuccin.omp.json | source`) — no remote URL.
    - fzf via the fish-native flag (`fzf --fish | source`, NOT --zsh).
    - zoxide (`zoxide init --cmd cd fish | source`).
    - trimmed fastfetch greeting under fish's `status is-interactive` guard (equivalent to the zsh `[ -t 0 ]` guard).
    - working node tooling per the checkpoint decision (nvm.fish, or native lazy fallback).
    - port the `y()` yazi-cd helper to fish/.config/fish/functions/y.fish if needed for parity.

    Then benchmark both shells under identical conditions and record in 04-04-SUMMARY.md: `hyperfine --warmup 3 --min-runs 10 'zsh -i -c exit'` vs `hyperfine --warmup 3 --min-runs 10 'fish -i -c exit'` (cold and warm). Measure time-to-usable-prompt, not raw process spawn (per the anti-pattern). Write a comparison table (cold/warm mean for each) plus a short trade-off note (parity completeness, feel, any missing niceties) for the user's decision. Do NOT wire kitty.conf/install.sh/stow.sh in this task — the switch is gated on the decision checkpoint.
  </action>
  <verify>
    <automated>command -v fish >/dev/null && test -f fish/.config/fish/config.fish && grep -q 'oh-my-posh init fish' fish/.config/fish/config.fish && grep -q 'status is-interactive' fish/.config/fish/config.fish && grep -q 'fzf --fish' fish/.config/fish/config.fish && grep -qiE "hyperfine|cold|warm|comparison" .planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - fish is installed and on PATH
    - fish/.config/fish/config.fish exists and covers all D-10 parity items: oh-my-posh (local JSON, `init fish`), `fzf --fish`, `zoxide init ... fish`, `status is-interactive` fastfetch guard, node tooling per the checkpoint decision
    - config.fish references the SAME `~/.config/oh-my-posh/catppuccin.omp.json` vendored by Plan 03 — no remote URL
    - 04-04-SUMMARY.md has a zsh-vs-fish comparison table (cold + warm hyperfine means) and a trade-off note
    - kitty.conf, install.sh, stow.sh are NOT modified in this task (git diff)
  </acceptance_criteria>
  <done>fish is installed with a day-one-parity config, and a side-by-side zsh-vs-fish benchmark with a trade-off note is recorded for the decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide the shell — optimized zsh or fish (D-08)</name>
  <action>Human decision checkpoint — the executor pauses until the user selects a shell. The choice is made on speed + feel from the benchmark; the executor records the selection and deciding numbers, then Task 4's wiring applies it. No implementation happens until the selection is given.</action>
  <decision>Which interactive shell does kitty launch going forward — optimized zsh or fish?</decision>
  <context>
    Both shells are now benchmarked side-by-side (see 04-04-SUMMARY.md cold/warm hyperfine means and the trade-off note). Per D-08 the rule is speed + feel, and the user makes the final call. Per D-11 the zshell package stays as a fallback regardless, and per D-12 the choice is made via kitty.conf, not chsh — the system login shell stays zsh either way, so the TTY recovery path is unaffected.
  </context>
  <options>
    <option id="fish">
      <name>Adopt fish</name>
      <pros>Whichever is faster/nicer per the benchmark; full switch wired declaratively via kitty.conf + install.sh + stow</pros>
      <cons>New stow package to maintain; day-one parity must hold; nvm.fish/native node tooling to keep working</cons>
    </option>
    <option id="zsh">
      <name>Keep optimized zsh</name>
      <pros>Already optimized in Plan 03; no new package; zero migration risk</pros>
      <cons>Forgoes any fish speed/feel advantage the benchmark showed</cons>
    </option>
  </options>
  <resume-signal>Select: fish or zsh (state the winning cold/warm number that decided it).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Apply the shell decision (conditional wiring)</name>
  <files>kitty/.config/kitty/kitty.conf, install.sh, stow.sh, .planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md (the recorded decision from the checkpoint)
    - .planning/phases/04-reliability-fixes-tech-debt/04-PATTERNS.md (kitty.conf "Pattern to apply" — add `shell fish` in a banner-comment section, D-12; install.sh PACMAN_PKGS "If D-09's fish branch is taken" — fish goes in PACMAN_PKGS NOT AUR_PKGS; fish/ stow package + stow.sh PACKAGES addition)
    - kitty/.config/kitty/kitty.conf (banner-comment section style; do NOT touch repaint_delay/input_delay/sync_to_monitor per Pitfall 3)
    - install.sh (PACMAN_PKGS array), stow.sh (PACKAGES array — fish added alphabetically near fastfetch/gtk)
  </read_first>
  <action>
    Read the decision from the checkpoint. Then:

    IF fish won: add a `shell fish` directive to kitty/.config/kitty/kitty.conf in a new banner-comment section near the top (matching the existing `# ── Section ─` style) — this is the ONLY switch point (D-12); do NOT add any chsh and do NOT touch kitty's rendering knobs. Add `fish` to install.sh PACMAN_PKGS (official `extra` repo — NOT AUR_PKGS; place under a `# Shell` group or near the existing utilities, one per line, no trailing comma). Add `fish` to stow.sh's PACKAGES array so `stow fish` runs on setup. Leave zshell in stow.sh untouched (D-11 fallback) and leave the existing `sudo chsh -s $(which zsh)` line in stow.sh unchanged (login shell stays zsh). Record the adoption in the SUMMARY.

    IF zsh won: make no changes to kitty.conf, install.sh, or stow.sh. Record the decision and the deciding benchmark numbers in the SUMMARY, and note the fish package files remain in the repo unused (not stowed/installed) or are removed per the user's preference. zshell stays the active shell.

    Either way, confirm in the SUMMARY that the zshell stow package is retained as the fallback (D-11) and that no nushell evaluation occurred (D-07).
  </action>
  <verify>
    <automated>D=$(grep -oiE 'decision:?[[:space:]]*(fish|zsh)' .planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md | grep -oiE '(fish|zsh)$' | head -1); if [ "$D" = "fish" ]; then grep -Eq '^[[:space:]]*shell[[:space:]]+fish' kitty/.config/kitty/kitty.conf && grep -Eq '^[[:space:]]*fish([[:space:]]|$)' install.sh && grep -Eq '^[[:space:]]*fish$' stow.sh && echo PASS; else grep -qi 'decision' .planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md && ! grep -Eq '^[[:space:]]*shell[[:space:]]+fish' kitty/.config/kitty/kitty.conf && echo PASS; fi; grep -q 'zshell' stow.sh && echo ZSHELL_RETAINED</automated>
  </verify>
  <acceptance_criteria>
    - The decision (fish or zsh) is recorded in 04-04-SUMMARY.md with the deciding benchmark number
    - If fish: kitty.conf contains `shell fish`; install.sh PACMAN_PKGS contains an uncommented `fish` line; stow.sh PACKAGES contains `fish`; no chsh for fish added
    - If zsh: kitty.conf/install.sh/stow.sh unchanged (no `shell fish` directive)
    - zshell remains in stow.sh PACKAGES (D-11 fallback retained) in both branches
    - kitty.conf repaint_delay/input_delay/sync_to_monitor untouched (Pitfall 3)
  </acceptance_criteria>
  <done>The user's shell decision is applied declaratively (kitty.conf/install.sh/stow.sh) or recorded as a no-op, with zsh retained as fallback and login shell unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh -> pacman/AUR/fisher | Package + plugin manifest drives what a fresh system installs (supply chain); nvm.fish comes from a fisher plugin, not an official repo |
| kitty.conf shell directive -> interactive shell | The terminal launches whichever shell is named; a broken config.fish could degrade the interactive shell |
| system login shell (PAM/TTY) | Unchanged by design (D-12) — kitty override only, so a broken fish never blocks TTY recovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering (supply chain) | nvm.fish fisher plugin ([ASSUMED]) + fish install | high | mitigate | Blocking, non-auto-approvable human-verify checkpoint before any fisher/nvm.fish install; fish itself is official `extra` (Approved); native fallback offered if the plugin is rejected |
| T-04-10 | Denial of Service | switching kitty to a broken fish config | medium | mitigate | D-12 keeps the system login shell as zsh (no chsh), so a broken config.fish only affects kitty and the TTY recovery path stays on proven zsh; zshell retained (D-11) |
| T-04-11 | Repudiation | shell switch made without recorded rationale | low | mitigate | Decision checkpoint records the deciding benchmark numbers and trade-off note in 04-04-SUMMARY.md (D-25) |
</threat_model>

<verification>
- Optimized zsh and fish benchmarked side-by-side (cold+warm hyperfine), comparison + trade-off note in 04-04-SUMMARY.md (D-07) — one-time documented protocol (D-24)
- User decision recorded with the deciding number (D-08)
- If fish: kitty.conf `shell fish`, install.sh PACMAN_PKGS fish, stow.sh fish — declarative, no chsh (D-09/D-12)
- If fish: day-one parity items all present in config.fish, prompt from the shared vendored JSON (D-10)
- zshell retained as fallback in both branches (D-11); no nushell evaluated (D-07); nvm.fish install gated by non-auto-approvable checkpoint (package legitimacy)
</verification>

<success_criteria>
- A data-backed shell choice is made: optimized zsh vs fish benchmarked, user picks the winner on speed + feel
- If fish wins, the switch is complete and declarative (kitty.conf + install.sh + stow.sh), parity holds, login shell stays zsh, zshell retained as fallback
- If zsh wins, no switch is wired and the decision is documented — FIX-03 is closed either way
</success_criteria>

<output>
Create `.planning/phases/04-reliability-fixes-tech-debt/04-04-SUMMARY.md` when done — including the zsh-vs-fish benchmark table, the trade-off note, the recorded decision, the applied wiring (or no-op), and confirmation that zshell is retained as the fallback.
</output>
