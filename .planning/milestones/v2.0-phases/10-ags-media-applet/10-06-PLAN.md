---
phase: 10-ags-media-applet
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - waybar/.config/waybar/modules.jsonc
  - waybar/.config/waybar/config-vertical.jsonc
  - hypr/.config/hypr/config/autostart.conf
  - eww/.config/eww/eww.yuck
  - hypr/.config/hypr/scripts/media-popup-open.sh
  - hypr/.config/hypr/scripts/media-popup-close.sh
  - matugen/.config/matugen/config.toml
autonomous: false
requirements: [MEDIA-01, MEDIA-04]

must_haves:
  truths:
    - "Clicking the waybar media segment opens the centered AGS applet (via `ags request toggle-media`); the dead eww popup no longer appears."
    - "The AGS daemon autostarts on login (uwsm app -- ags run ...), so the applet is available after a fresh login."
    - "The retired eww media-popup / media-backdrop windows and the media-popup-open/close scripts are gone; eww is dropped from autostart if it had no other consumer."
    - "The full flow works end-to-end via stow on a reproducible setup: waybar click → applet → controls → cava → click-away/Esc → theme recolor."
  artifacts:
    - "waybar/.config/waybar/modules.jsonc + config-vertical.jsonc — every `custom/media`/mpris `on-click` repointed to `ags request toggle-media`."
    - "hypr/.config/hypr/config/autostart.conf — an exec-once starting the AGS daemon (and eww daemon removed if unused)."
    - "eww/.config/eww/eww.yuck — media-popup + media-backdrop defwindows removed."
  key_links:
    - "waybar media segment on-click ↔ `ags request toggle-media` ↔ app.tsx requestHandler ↔ media window visibility."
    - "autostart.conf exec-once ↔ `ags run --directory ~/.config/ags` (daemon present at login)."
    - "stow ags ↔ ~/.config/ags symlinks ↔ the live applet."
---

<objective>
Wire the finished AGS applet into the live desktop and retire the dead eww media popup: repoint every waybar media `on-click` to `ags request toggle-media`, autostart the AGS daemon, remove the eww media-popup/backdrop windows and the media-popup-open/close scripts (dropping eww entirely if it has no other consumer), and prove the whole flow reproducibly via stow (MEDIA-01, MEDIA-04).

Purpose: This is the switchover — the moment the media segment stops opening the click-dead eww popup and starts opening the working AGS applet — plus the cleanup that removes the abandoned eww surface so the repo stays reproducible and free of dead code.

Output: repointed waybar segments, AGS autostart, retired eww windows/scripts, an updated config.toml (eww template removed only if eww is fully retired), and a human-confirmed end-to-end test.

Artifacts this plan produces: the live waybar→AGS wiring; AGS autostart entry; removal of the eww media surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ags-media-applet/10-CONTEXT.md
@docs/superpowers/plans/2026-07-15-ags-media-applet.md
@hypr/.config/hypr/config/autostart.conf
@eww/.config/eww/eww.yuck
@waybar/.config/waybar/modules.jsonc
@waybar/.config/waybar/config-vertical.jsonc
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repoint waybar media on-click to AGS + autostart the AGS daemon</name>
  <files>waybar/.config/waybar/modules.jsonc, waybar/.config/waybar/config-vertical.jsonc, hypr/.config/hypr/config/autostart.conf</files>
  <read_first>
    - `docs/superpowers/plans/2026-07-15-ags-media-applet.md` — Task 6, Steps 1-2.
    - `hypr/.config/hypr/config/autostart.conf` — the existing `uwsm app --` / `exec-once` convention (and the current `exec-once = uwsm app -- eww daemon` line around line 50).
    - The 10-02 SUMMARY — whether the working request form is `ags request toggle-media` or `ags request -i media toggle-media`.
  </read_first>
  <action>
    Repoint EVERY waybar media `on-click` that currently runs `~/.config/hypr/scripts/media-popup-open.sh` to `ags request toggle-media` (use the exact form pinned in 10-02 — add `-i media` if that was required). There are three such sites confirmed by `grep -rn "media-popup-open.sh" waybar/`: `modules.jsonc` (the mpris module ~line 59 AND the `custom/media` module ~line 278) and `config-vertical.jsonc` (~line 91). Re-grep to catch all occurrences and repoint each. Add an AGS autostart entry to `autostart.conf` following the file's existing `uwsm app --` convention, e.g. `exec-once = uwsm app -- ags run --directory ~/.config/ags`, placed consistently with the other daemon exec-once lines. Do not remove the eww daemon line yet — that is Task 2's decision.
  </action>
  <verify>
    <automated>! grep -rq 'media-popup-open.sh' /home/aorus/dotfiles/waybar/ && grep -rq 'ags request toggle-media' /home/aorus/dotfiles/waybar/ && grep -q 'ags run --directory' /home/aorus/dotfiles/hypr/.config/hypr/config/autostart.conf && echo OK</automated>
    Expected: `OK` — no `media-popup-open.sh` reference remains in waybar/, `ags request toggle-media` present, and the AGS autostart exec-once present.
  </verify>
  <acceptance_criteria>
    - All three waybar media on-click sites now run `ags request toggle-media` (zero `media-popup-open.sh` references left in waybar/).
    - `autostart.conf` has an `exec-once` starting the AGS daemon via `uwsm app -- ags run --directory ~/.config/ags`.
  </acceptance_criteria>
  <done>The waybar media segment opens the AGS applet, and AGS starts at login.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Retire the eww media popup (check consumers, remove windows/scripts/template)</name>
  <files>eww/.config/eww/eww.yuck, hypr/.config/hypr/config/autostart.conf, hypr/.config/hypr/scripts/media-popup-open.sh, hypr/.config/hypr/scripts/media-popup-close.sh, matugen/.config/matugen/config.toml</files>
  <read_first>
    - `docs/superpowers/plans/2026-07-15-ags-media-applet.md` — Task 6, Steps 3-4.
    - `eww/.config/eww/eww.yuck` — the `media-backdrop` (~line 138) and `media-popup` (~line 155) defwindows; confirm these are eww's ONLY two defwindows.
  </read_first>
  <action>
    First check for other eww consumers: run `grep -rn 'eww ' hypr/ waybar/ --include='*.sh' --include='*.jsonc' --include='*.conf' | grep -v media-popup` and `grep -c 'defwindow' eww/.config/eww/eww.yuck`. Decision rule (Step 3): if `media-popup`/`media-backdrop` are eww's only windows (confirmed: exactly 2 defwindows) and nothing else invokes `eww`, then eww is fully retired — remove the `exec-once = uwsm app -- eww daemon` line from `autostart.conf`, drop the `[templates.eww]` block from `matugen/.config/matugen/config.toml`, and note in the SUMMARY that eww can later be dropped from `install.sh` (leave the package installed — harmless — but stop autostarting it). If eww DOES have another consumer, keep the eww daemon and `[templates.eww]`, and remove only the two media windows. Then (Step 4): delete the `defwindow media-popup` and `defwindow media-backdrop` blocks from `eww.yuck` (plus any now-orphaned media-center/backdrop widgets/deflisten referenced by nothing else), and `git rm hypr/.config/hypr/scripts/media-popup-open.sh hypr/.config/hypr/scripts/media-popup-close.sh`. Verify eww.yuck still parses (no dangling references to removed widgets).
  </action>
  <verify>
    <automated>! test -f /home/aorus/dotfiles/hypr/.config/hypr/scripts/media-popup-open.sh && ! test -f /home/aorus/dotfiles/hypr/.config/hypr/scripts/media-popup-close.sh && ! grep -q 'defwindow media-popup' /home/aorus/dotfiles/eww/.config/eww/eww.yuck && ! grep -q 'defwindow media-backdrop' /home/aorus/dotfiles/eww/.config/eww/eww.yuck && echo OK</automated>
    Expected: `OK`. If eww fully retired, also confirm `! grep -q 'eww daemon' autostart.conf` and `! grep -q '\[templates.eww\]' config.toml`. Record the consumer-check result in the SUMMARY.
  </verify>
  <acceptance_criteria>
    - The two media-popup-*.sh scripts are git-removed.
    - The `media-popup` and `media-backdrop` defwindows are gone from eww.yuck and it still parses.
    - eww daemon + `[templates.eww]` removed IF and ONLY IF the consumer check found no other eww user (documented in SUMMARY).
  </acceptance_criteria>
  <done>The dead eww media surface is retired; eww is dropped from autostart if it had no other consumer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end user test via stow — full waybar-click flow</name>
  <files>waybar/.config/waybar/modules.jsonc</files>
  <action>
Halt and present the checkpoint below to the human. There is no agent-side pointer injection, so the full waybar-click → applet → controls → cava → theming flow must be exercised live by a human after deploying via stow. Do not self-approve on green structural greps alone.
  </action>
  <what-built>The full switchover, deployed via stow: the waybar media segment now opens the AGS applet, the AGS daemon autostarts, and the old eww media popup is retired. This is the end-to-end reproducibility gate.</what-built>
  <how-to-verify>
    Deploy and exercise the real flow (per Task 6 Step 5): `cd /home/aorus/dotfiles && stow ags`; restart the applet daemon (`ags quit -i media 2>/dev/null; uwsm app -- ags run --directory ~/.config/ags & sleep 2`); reload waybar so the new on-click is live (`pkill -SIGUSR2 waybar`). Then, with a player active:
    1. Click the waybar media segment → the AGS applet opens CENTERED (not the old eww popup).
    2. Transport / seek / volume / switcher all work.
    3. cava bars animate.
    4. Click-away and Esc both close it.
    5. Switch theme (static and dynamic) → the applet recolors with no manual step.
    6. Confirm the OLD eww popup never appears for the media segment anymore.
    (No agent-side pointer injection exists — this must be a live user test.)
  </how-to-verify>
  <resume-signal>Type "approved" once the waybar click opens the working AGS applet, all controls + cava + theming work, and the old eww popup is gone. Otherwise describe which step failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| waybar on-click → AGS instance | The on-click runs the fixed literal command `ags request toggle-media`; no metadata is interpolated into it. |
| MPRIS metadata → applet → backend | Unchanged from 10-03: the sole untrusted surface (player bus name) is `_valid_id`-validated inside `media-players.sh`; no metadata reaches a command string. The integrated applet inherits that contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06-01 | Tampering | waybar on-click → ags request | low | mitigate | The on-click is a fixed literal verb; the AGS requestHandler only matches `toggle-media`/`reload-css` and passes nothing to a shell. |
| T-10-06-02 | Tampering (command injection) | integrated applet → MPRIS backend | medium | mitigate | Inherits the 08-07 contract end-to-end: validated ids + numeric args only, argv-form, no metadata near a command. Low residual risk. |
| T-10-06-03 | Denial of Service | retiring eww daemon | low | accept | eww is dropped from autostart only after a consumer check proves the media popup was its sole user; the package stays installed, so a mistaken removal is trivially reversible. |
</threat_model>

<verification>
- Structural greps: zero `media-popup-open.sh` in waybar/, `ags request toggle-media` present, AGS autostart present, both media-popup-*.sh removed, both eww media defwindows gone.
- Consumer-check result (eww fully retired vs kept) recorded in SUMMARY; `[templates.eww]` / eww daemon removed only if unused.
- HUMAN GATE: waybar click opens the working AGS applet (not eww), all controls + cava + theming work, old popup gone — deployed via stow for reproducibility.
</verification>

<success_criteria>
- Clicking the waybar media segment opens the working centered AGS applet; the dead eww popup is retired (MEDIA-01).
- The applet is reproducible via `install.sh` + `stow ags` and autostarts at login (MEDIA-04, spec success criterion 6).
</success_criteria>

<output>
Create `.planning/phases/10-ags-media-applet/10-06-SUMMARY.md` when done. Record the eww consumer-check result and whether eww was fully retired.
</output>
