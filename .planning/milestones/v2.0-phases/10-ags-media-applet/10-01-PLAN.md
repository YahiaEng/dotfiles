---
phase: 10-ags-media-applet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
autonomous: true
requirements: [MEDIA-04]
user_setup:
  - service: local-terminal
    why: "Package install requires sudo (pacman) and paru (AUR) — both prompt for the user's password in the terminal running this plan."
    dashboard_config:
      - task: "Be present at the terminal to enter the sudo password and confirm the paru AUR build for aylurs-gtk-shell (paru shows the PKGBUILD before building)."
        location: "The terminal executing this plan"

must_haves:
  truths:
    - "`ags --version` prints a 3.x version (AGS v3 toolchain present)."
    - "`cava -v` prints 0.10.x (audio visualizer present)."
    - "`gjs --version` prints a version (AGS runtime present, pulled in by aylurs-gtk-shell)."
    - "A fresh `install.sh` run would install both deps (cava in the pacman/official list, aylurs-gtk-shell in the AUR list)."
  artifacts:
    - "install.sh containing `cava` in its official-repo package list and `aylurs-gtk-shell` in its AUR package list."
  key_links:
    - "install.sh AUR list ↔ aylurs-gtk-shell (the AGS v3 toolkit the whole phase depends on)."
    - "install.sh pacman list ↔ cava (the underlay visualizer 10-04 consumes)."
---

<objective>
Install the two new dependencies for the AGS media applet — `aylurs-gtk-shell` (AGS v3, AUR) and `cava` 0.10.7 (official `extra`) — on this machine, and register both in `install.sh` so a fresh Arch system reproduces them via `install.sh` + stow (MEDIA-04, D-Deps in 10-CONTEXT.md).

Purpose: Every later plan in this phase (scaffold, backend, cava underlay, theming, integration) is inert without the AGS toolchain and cava installed and reproducibly declared. This is the foundation wave.

Output: `install.sh` edited to declare both packages in their correct package lists; both packages installed and their binaries verified on this machine.

Artifacts this plan produces: `cava` added to the official/pacman package list in `install.sh`; `aylurs-gtk-shell` added to the AUR package list in `install.sh`; working `ags`, `cava`, and `gjs` binaries on this host.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ags-media-applet/10-CONTEXT.md
@docs/superpowers/plans/2026-07-15-ags-media-applet.md
@install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register cava (pacman) and aylurs-gtk-shell (AUR) in install.sh</name>
  <files>install.sh</files>
  <read_first>
    - `docs/superpowers/plans/2026-07-15-ags-media-applet.md` — Task 1, Steps 1-2 (the source of truth for this task).
    - `install.sh` — locate the package arrays with `grep -nE 'AUR_PKGS|PACMAN_PKGS|pacman -S|paru -S|packages=\(' install.sh | head` (CLAUDE.md references an `AUR_PKGS` list around line 150). Read the surrounding array to match its exact quoting/formatting style.
  </read_first>
  <action>
    Add `cava` to the official-repo (pacman/PACMAN_PKGS) package list alongside the other `extra`-repo packages, and add `aylurs-gtk-shell` to the AUR package list alongside the other AUR packages. Match the existing array element formatting, quoting, and alphabetical/grouping convention exactly — do not reformat neighboring entries. cava is `extra`-repo (pacman), aylurs-gtk-shell is AUR (paru); do not swap the two lists. Do NOT install anything in this task — this task only edits the declaration. Follow the CLAUDE.md package-list conventions (this repo has established install.sh package-array patterns across Phases 6/7/8).
  </action>
  <verify>
    <automated>grep -nE 'cava' install.sh && grep -nE 'aylurs-gtk-shell' install.sh</automated>
    Expected: `cava` appears in the pacman/official list and `aylurs-gtk-shell` appears in the AUR list (confirm by inspecting each match's surrounding array).
  </verify>
  <acceptance_criteria>
    - `cava` is present in install.sh's official/pacman package array.
    - `aylurs-gtk-shell` is present in install.sh's AUR package array.
    - No other install.sh package entry was reformatted or removed.
  </acceptance_criteria>
  <done>install.sh declares both new deps in their correct lists.</done>
</task>

<task type="auto">
  <name>Task 2: Install both deps on this machine and verify the toolchain</name>
  <files>install.sh</files>
  <read_first>
    - `docs/superpowers/plans/2026-07-15-ags-media-applet.md` — Task 1, Steps 3-4 (exact install + verify commands).
  </read_first>
  <action>
    Install cava from the official repo with `sudo pacman -S --needed --noconfirm cava` and aylurs-gtk-shell from the AUR with `paru -S --needed --noconfirm aylurs-gtk-shell`. `gjs` is pulled in automatically as a dependency of aylurs-gtk-shell — do not add it separately. The terminal will prompt for the user's sudo password and paru will show the PKGBUILD for the AUR package; this is expected (see user_setup). aylurs-gtk-shell is the well-known AGS (Aylur's GTK Shell) v3 package — the user visually confirms the paru legitimacy/PKGBUILD prompt before the build proceeds (supply-chain mitigation, see threat model). Do not use `--noconfirm` as a reason to skip the human being present for the sudo password.
  </action>
  <verify>
    <automated>ags --version && cava -v | head -1 && gjs --version</automated>
    Expected: `ags` prints a 3.x version; `cava` prints 0.10.x; `gjs` prints a version. All three commands exit 0.
  </verify>
  <acceptance_criteria>
    - `ags --version` returns a 3.x version string, exit 0.
    - `cava -v` returns 0.10.x, exit 0.
    - `gjs --version` returns a version, exit 0.
  </acceptance_criteria>
  <done>ags (3.x), cava (0.10.x), and gjs are installed and runnable on this host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AUR package source → local build | `aylurs-gtk-shell` is built from an AUR PKGBUILD (community-submitted); this is the one untrusted-source surface this plan crosses. |
| official `extra` repo → local install | `cava` comes from Arch's signed `extra` repo — trusted, package-signed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-SC | Tampering | paru AUR build of aylurs-gtk-shell | medium | mitigate | aylurs-gtk-shell is the well-known upstream AGS v3 project (verify against `aur.archlinux.org/packages/aylurs-gtk-shell`); the user is present at the terminal and reviews paru's PKGBUILD prompt before the build runs. Repo precedent: Phases 6/7/8 gated every AUR package through a human legitimacy check. |
| T-10-01-01 | Elevation of Privilege | sudo pacman invocation | low | accept | Install requires sudo by nature; the human enters the password interactively, no passwordless sudoers rule is added, and cava is package-signed from `extra`. |
</threat_model>

<verification>
- `grep -nE 'cava' install.sh` and `grep -nE 'aylurs-gtk-shell' install.sh` both match in their correct lists.
- `ags --version` (3.x), `cava -v` (0.10.x), `gjs --version` all succeed.
</verification>

<success_criteria>
- Both packages installed on this machine; `ags`, `cava`, `gjs` runnable.
- Both packages declared in install.sh (cava = pacman list, aylurs-gtk-shell = AUR list), making the applet reproducible on a fresh Arch install (MEDIA-04).
</success_criteria>

<output>
Create `.planning/phases/10-ags-media-applet/10-01-SUMMARY.md` when done.
</output>
