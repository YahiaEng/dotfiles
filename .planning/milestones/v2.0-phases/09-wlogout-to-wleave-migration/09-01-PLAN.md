---
phase: 09-wlogout-to-wleave-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [WLOG-01]

user_setup:
  - service: aur-wleave
    why: "wleave 0.7.1-1 is an AUR build-from-source package (D-13). paru needs an interactive sudo/PGP/build-review TTY that a non-interactive agent session cannot drive — Phase 10-01 precedent: the identical call hung indefinitely and the plan had to degrade to verify-only."
    env_vars: []
    dashboard_config:
      - task: "Review the PKGBUILD at aur.archlinux.org/packages/wleave (confirm pkgver=0.7.1, source pinned to git+https://github.com/AMNatty/wleave#tag=0.7.1, no pre_install/post_install/pre_upgrade hooks), then run: paru -S wleave"
        location: "your own interactive terminal — NOT the agent session"

must_haves:
  truths:
    - "`command -v wleave` resolves and `pacman -Q wleave` reports 0.7.1-1 (the pinned release, never wleave-git) — per D-13"
    - "The wleave config filename, the accepted top-level config keys, and the accepted per-button keys are recorded from the man pages shipped by the INSTALLED 0.7.1 package, not from upstream main-branch docs (RESEARCH Pitfall 6)"
    - "Whether wleave 0.7.1 exposes a second text slot for a hover-revealed action name (D-08) is recorded as an explicit yes/no finding, sourced from the installed man pages and --help output"
    - "All six chosen Nerd Font codepoints (U+F023 lock, U+F08B logout, U+F186 suspend, U+F2DC hibernate, U+F021 reboot, U+F011 shutdown) are present in the installed FiraCode Nerd Font cmap and none of them is one of the Phase 6 six (U+F033E, U+F0343, U+F04B2, U+F02CA, U+F0425, U+F0709) — per D-09"
    - "A scratch render of the wleave colour template resolves all four never-before-exercised M3 container roles: the rendered output contains no literal `{{` token and no empty hex value for tertiary_container, on_tertiary_container, error_container, or on_error_container (RESEARCH Assumption A1 / Pitfall 5; UI-SPEC UI-Consideration 21)"
  artifacts:
    - path: ".planning/phases/09-wlogout-to-wleave-migration/09-01-SUMMARY.md"
      provides: "Recorded pre-flight facts consumed by 09-02: installed version, config filename, top-level/per-button key names, spacing-key availability, second-text-slot finding, the six verified codepoints, container-role dry-run result"
      contains: "U+F011"
  key_links:
    - from: "matugen/.config/matugen/templates/wlogout-colors.css"
      to: "~/.local/state/theme/ (scratch render target)"
      via: "theme-engine lib/generate.sh renders a scratch copy carrying the four added container-role @define-color lines"
      pattern: "tertiary_container|error_container"
  prohibitions: []
---

<objective>
Discharge every pre-flight fact 09-02's atomic cutover depends on, and put the `wleave` binary on the machine behind the D-13 human package-legitimacy gate.

Purpose: 09-02 is an atomic single-cutover plan (D-11) — once it starts, wlogout is gone and there is no fallback engine. Three of its assumptions are currently unverified against the installed artefact: the config filename/key names (RESEARCH read source at the 0.7.1 tag, not the installed man pages), whether the four M3 container roles matugen has never rendered in this repo actually resolve (Assumption A1), and whether wleave 0.7.1 has any second text slot capable of carrying D-08's hover-revealed action name. Discovering any of those mid-cutover means a half-migrated desktop with no power menu.

Output: no repo file changes. This plan produces recorded facts (in its SUMMARY) plus an installed, human-approved `wleave` 0.7.1-1.

**This plan intentionally contains no `tracer` task.** The phase's tracer is 09-02 Task 1 — there is nothing to wire end-to-end until the binary exists, and the binary cannot be installed without the blocking human gate below. This is the pre-flight that makes the tracer possible.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-wlogout-to-wleave-migration/09-CONTEXT.md
@.planning/phases/09-wlogout-to-wleave-migration/09-RESEARCH.md
@.planning/phases/09-wlogout-to-wleave-migration/09-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: AUR package-legitimacy gate + install wleave 0.7.1-1 (D-13)</name>
  <what-built>Nothing yet — this is the pre-install gate. `09-RESEARCH.md` §Package Legitimacy Audit already reviewed the PKGBUILD manually (verdict OK: build source is a pinned `git+https://github.com/AMNatty/wleave#tag=0.7.1` fetch; `package()` is `install -Dm` calls only; zero `pre_install`/`post_install`/`pre_upgrade` hooks; no out-of-tree writes or network calls in any build phase). That written review does NOT substitute for this live gate — D-13 and this repo's Phase 4/6/8 precedent (eww approved, eww-git permanently refused) require a human to approve an AUR install at execution time.</what-built>
  <how-to-verify>
    1. Open https://aur.archlinux.org/packages/wleave and confirm: `pkgver=0.7.1`, `pkgrel=1`, maintainer listed, votes/popularity non-zero, no out-of-date flag.
    2. Read the PKGBUILD `source=()` line and confirm it pins a TAG (`#tag=0.7.1`), not a branch. If it points at a moving branch or the package is now `wleave-git`, STOP — D-13 forbids the -git variant outright.
    3. Confirm the PKGBUILD contains no `pre_install`, `post_install`, `pre_upgrade`, or `.install` file.
    4. In YOUR OWN terminal (not the agent session — paru is interactive and will hang here, per Phase 10-01), run: `paru -S wleave`
    5. Confirm both: `pacman -Q wleave` prints `wleave 0.7.1-1`, and `command -v wleave` prints a path.
    Do NOT run `pacman -Rns wlogout`. Retiring wlogout from this repo is a repo/config change (D-11), not a machine-uninstall; Phase 10-06 precedent left the retired eww package installed.
  </how-to-verify>
  <resume-signal>Paste the output of `pacman -Q wleave`, or describe what blocked you.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Probe the INSTALLED wleave 0.7.1 config surface and re-verify the six glyph codepoints</name>
  <precondition>`command -v wleave` resolves and `pacman -Q wleave` reports 0.7.1-1 (installed by the human at Task 1's blocking gate)</precondition>
  <files>none — read-only probe; findings land in .planning/phases/09-wlogout-to-wleave-migration/09-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/09-wlogout-to-wleave-migration/09-RESEARCH.md` — Pattern 1 (the wrapped config schema RESEARCH read from source at the 0.7.1 tag), Pitfall 1 (`yalign` defaults to 0.9), Pitfall 6 (main-branch docs describe an unreleased 0.8.0 — do not trust them)
    - `.planning/phases/09-wlogout-to-wleave-migration/09-UI-SPEC.md` — Icon library row (the six candidate glyph families), Motion & Interaction Contract → Hover / keyboard focus (the action-name reveal this task must determine feasibility for)
    - `wlogout/.config/wlogout/layout` — the six action strings and keybind letters that must carry over (D-17/D-19)
  </read_first>
  <action>
Read the man pages shipped by the installed package (`man 5 wleave.json`, `man 1 wleave`, plus `wleave --help`) and record, verbatim, four things into this plan's SUMMARY:

(a) **Config filename and search path.** RESEARCH proposes `~/.config/wleave/layout.json`. Confirm the exact filename the installed 0.7.1 reads (it may be `layout.json` or bare `layout`), and the stylesheet filename (RESEARCH proposes `style.css`). 09-02 creates the stow package at those exact names — a wrong filename means a silently-ignored config.

(b) **Accepted top-level config keys.** Confirm `buttons-per-row`, `close-on-lost-focus`, `show-keybinds`, `margin`, `delay-command-ms`, and `protocol` exist and record their accepted value types (RESEARCH's example passes `buttons-per-row` as the STRING `"6"`, not the number 6 — confirm which the installed schema accepts). Also record whether ANY inter-button spacing key exists (a `column-spacing` / `row-spacing` / `spacing` equivalent). 09-02 needs a definitive answer: if a native spacing key exists it owns D-02's 24px gap; if not, the gap comes from CSS `button { margin: 12px; }` and the two mechanisms must not both be applied (that was Phase 6's double-gap bug in a new engine).

(c) **Accepted per-button keys.** Confirm `label`, `action`, `text`, `keybind`, `width`, `height`, and `icon`; confirm `height` is a 0.0–1.0 alignment fraction (NOT a pixel size) and that its default is 0.9. Confirm there is NO `button-defaults` block at 0.7.1 (it is a main-branch/0.8.0 feature — if it IS present, the installed package is not 0.7.1 and this task must halt).

(d) **The D-08 second-text-slot finding — record an explicit yes/no.** D-08 requires the action NAME to fade/slide in below the capsule on hover/focus, in addition to the always-visible glyph. RESEARCH's widget-tree read says an icon-less button renders exactly ONE `GtkLabel` (holding the `text` field) positioned by an `Overlay`, and GTK4 CSS has no generated-content mechanism — so a second text slot may not exist at all. Determine from the man pages and `--help` whether 0.7.1 offers any of: a separate name/description/tooltip field distinct from `text`; pango-markup support in `text`; or an `icon` + `text` split where `icon` renders a picture and `text` renders a separate label BELOW it. Record which of these exist. 09-03 Task 1 is a `checkpoint:decision` that consumes this finding directly — an unrecorded or hedged answer forces the human to re-derive it.

Then re-verify the six chosen codepoints against the installed font. Resolve the font file with `fc-match -f '%{file}' "FiraCode Nerd Font"`, then use python3 + fontTools (`fontTools` 4.63.0 is installed) to load `getBestCmap()` and assert membership for all six: U+F023 (nf-fa-lock → lock), U+F08B (nf-fa-sign_out → logout), U+F186 (nf-fa-moon_o → suspend), U+F2DC (nf-fa-snowflake_o → hibernate), U+F021 (nf-fa-refresh → reboot), U+F011 (nf-fa-power_off → shutdown). Assert the six are pairwise distinct and that none is a member of the Phase 6 set {U+F033E, U+F0343, U+F04B2, U+F02CA, U+F0425, U+F0709} — D-09 requires a refreshed set, not the carried six. If any candidate is absent, substitute from the same semantic family, re-verify, and record the substitution as a deviation (Phase 08-05 precedent). Record the final six as `U+XXXX` codepoint notation in the SUMMARY — never paste the rendered glyph characters into the SUMMARY, since 08-16's durable finding is that PUA glyphs written through the edit tool silently become empty strings.
  </action>
  <verify>
    <automated>python3 -c "import subprocess;from fontTools.ttLib import TTFont;p=subprocess.run(['fc-match','-f','%{file}','FiraCode Nerd Font'],capture_output=True,text=True).stdout.strip();cm=TTFont(p).getBestCmap();new=[0xF023,0xF08B,0xF186,0xF2DC,0xF021,0xF011];old={0xF033E,0xF0343,0xF04B2,0xF02CA,0xF0425,0xF0709};assert len(set(new))==6;assert all(c in cm for c in new),[hex(c) for c in new if c not in cm];assert not (set(new)&old);print('cmap OK',[hex(c) for c in new])"</automated>
    <automated>pacman -Q wleave | grep -qx 'wleave 0.7.1-1' && man 5 wleave.json >/dev/null 2>&1 || man 1 wleave >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `pacman -Q wleave` outputs exactly `wleave 0.7.1-1`
    - The cmap assertion command above exits 0 and prints six hex codepoints
    - `09-01-SUMMARY.md` contains the literal string `U+F011` and a six-row codepoint table mapping each action to its `U+XXXX` value
    - `09-01-SUMMARY.md` records the exact config filename and stylesheet filename the installed binary reads
    - `09-01-SUMMARY.md` records whether an inter-button spacing config key exists, as an explicit yes/no plus the key name if yes
    - `09-01-SUMMARY.md` records the D-08 second-text-slot finding as an explicit yes/no, naming which of (separate name field / pango markup in text / icon+text split) exist
    - `09-01-SUMMARY.md` records that no `button-defaults` key exists at the installed version
  </acceptance_criteria>
  <done>Every schema fact 09-02 and 09-03 depend on is recorded from the installed artefact, and the six refreshed glyph codepoints are proven present in the installed font cmap.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run the four never-before-rendered M3 container roles (Assumption A1)</name>
  <files>none in the repo — a scratch copy of the colour template and its rendered output live outside the working tree; findings land in 09-01-SUMMARY.md</files>
  <read_first>
    - `matugen/.config/matugen/templates/wlogout-colors.css` — the current 19 `@define-color` lines; the four container-role pairs being added are absent from every template in this repo
    - `theme-engine/.config/theme-engine/lib/generate.sh` — how a template is rendered, and what a render failure looks like
    - `.planning/phases/09-wlogout-to-wleave-migration/09-RESEARCH.md` — Pitfall 5 and Assumption A1 (this is the verification step RESEARCH explicitly prescribes)
  </read_first>
  <action>
Prove that matugen resolves `tertiary_container`, `on_tertiary_container`, `error_container`, and `on_error_container` before 09-02 commits a template that depends on them. Work entirely in a scratch location — this task changes no repo file.

Copy the existing colour template to a scratch path outside the repo (use the session scratch dir, not `/tmp` directly if a scratch dir is provided). Append four `@define-color` lines to the scratch copy following the exact idiom every existing line uses — name, then `{{colors.<role>.default.hex}}` — for the four roles: `tertiary_container`, `on_tertiary_container`, `error_container`, `on_error_container`. Then render the scratch template through the pipeline: either point a temporary matugen `[templates.*]` entry at it in a scratch config, or invoke matugen directly with the scratch input/output paths, whichever the existing `generate.sh` idiom supports without mutating `matugen/.config/matugen/config.toml`.

Inspect the rendered scratch output for both documented failure modes from Pitfall 5: a literal unresolved mustache token (a `{{` sequence surviving into the output), and a resolved-but-empty value (a `#` immediately followed by a `;` or whitespace, i.e. a colour name with no hex digits). Assert exactly 23 `@define-color` lines and 23 well-formed six-hex-digit values.

Run the dry-run for BOTH modes — once against a static preset (e.g. `tokyonight`) and once against the Material You dynamic path — because the two take different code paths through `generate.sh` and a role that resolves in one is not proven in the other. Record both results plus the resolved hex values for the four new roles in the SUMMARY. If any of the four fails to resolve in either mode, do NOT improvise a literal fallback hex — record it as a blocking finding and stop; 09-03's derived-hue design depends on these roles and the repo's zero-literal-hex convention forbids the workaround.
  </action>
  <verify>
    <automated>test "$(grep -c '^@define-color' "$SCRATCH/wleave-colors.rendered.css")" = 23 && ! grep -q '{{' "$SCRATCH/wleave-colors.rendered.css" && test "$(grep -cE '^@define-color [a-z_]+ #[0-9a-fA-F]{6};$' "$SCRATCH/wleave-colors.rendered.css")" = 23</automated>
    <automated>grep -E '^@define-color (tertiary_container|on_tertiary_container|error_container|on_error_container) #[0-9a-fA-F]{6};$' "$SCRATCH/wleave-colors.rendered.css" | wc -l | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - The rendered scratch output has exactly 23 `@define-color` lines and 23 lines matching `^@define-color [a-z_]+ #[0-9a-fA-F]{6};$`
    - Zero occurrences of an unresolved mustache token in the rendered scratch output
    - All four of `tertiary_container`, `on_tertiary_container`, `error_container`, `on_error_container` resolve to a six-hex-digit value
    - Both a static preset and the Material You dynamic path were rendered, and both results are recorded in `09-01-SUMMARY.md`
    - `matugen/.config/matugen/config.toml` and `matugen/.config/matugen/templates/` are unchanged: `git diff --exit-code matugen/` returns 0
  </acceptance_criteria>
  <done>Assumption A1 is discharged empirically in both theme modes, and 09-02 can commit the 23-key template knowing every key resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AUR → local machine | Build-from-source of third-party code executes arbitrary `build()`/`package()` shell on this machine |
| upstream git tag → local build | The pinned tag is the integrity anchor; a moving branch would let content change under a fixed version string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering | AUR `wleave` build-from-source (`paru -S wleave`) | high | mitigate | PKGBUILD reviewed in `09-RESEARCH.md` (pinned `#tag=0.7.1`, no `.install` hooks, `install -Dm`-only `package()`); Task 1 is a blocking-human gate that CANNOT be auto-approved, and the `-git` variant is refused outright per D-13 |
| T-09-05 | Tampering | Version-string confusion (installed package is not the reviewed 0.7.1) | medium | mitigate | Task 2 asserts `pacman -Q wleave` is exactly `wleave 0.7.1-1` and halts if a `button-defaults` key (a 0.8.0-only feature) is present in the installed man pages |
| T-09-06 | Information Disclosure | Scratch render of the colour template writes resolved palette hex values outside the repo | low | accept | Palette hex values are non-secret and already committed to `theme-engine/palettes/*.json`; the scratch file is session-local |
</threat_model>

## Artifacts this phase produces

New symbols/paths created by Phase 09 as a whole (this section exists so the plan-review source-grounding pass does not flag them as drift — none of these exist in the codebase before this phase):

- `wleave/` — new stow package
- `wleave/.config/wleave/layout.json` — new file (replaces `wlogout/.config/wlogout/layout`)
- `wleave/.config/wleave/style.css` — new file (replaces `wlogout/.config/wlogout/style.css`)
- `hypr/.config/hypr/scripts/wleave.sh` — new script (replaces `hypr/.config/hypr/scripts/wlogout.sh`)
- `matugen/.config/matugen/templates/wleave-colors.css` — new template (replaces `wlogout-colors.css`)
- `[templates.wleave]` — new matugen config.toml section key (replaces `[templates.wlogout]`)
- `wleave.css` — new theme-engine contract entry + new render target under `~/.local/state/theme/` (replaces `wlogout.css`)
- `wleave` — new Hyprland `layerrule` match namespace (replaces `logout_dialog`)
- `@define-color tertiary_container` / `on_tertiary_container` / `error_container` / `on_error_container` — four new palette role names in the colour template
- `@vhue-teal`, `@vhue-purple` (container-level derived hues) — new `@define-color` names in `wleave/.config/wleave/style.css`
- `.planning/phases/09-wlogout-to-wleave-migration/evidence/` — new directory holding the D-14 render-gate screenshots
- `.planning/phases/09-wlogout-to-wleave-migration/COVERAGE.md` — new file (API-coverage declaration)

## Explicit Flagged Assumptions (probe-surfaced, never silently dropped)

The deterministic edge probe over WLOG-01 returned exactly one row, `unclassified` / `unresolved` — it is carried as an explicit flagged assumption in `09-02-PLAN.md` (the plan that delivers WLOG-01), not resolved here and not dropped. See that plan's matching section.

<verification>
- `pacman -Q wleave` → `wleave 0.7.1-1`
- The cmap assertion in Task 2's `<verify>` exits 0
- The 23-key / zero-unresolved-token / four-container-role assertions in Task 3's `<verify>` exit 0 in BOTH theme modes
- `git status --porcelain` shows no modification to any tracked file (this plan is verification-only)
</verification>

<success_criteria>
- wleave 0.7.1-1 is installed, approved by a human at a blocking gate that was not auto-advanced
- The installed config surface (filenames, top-level keys, per-button keys, spacing-key availability, second-text-slot finding) is recorded in the SUMMARY as fact, not inference
- Six refreshed, pairwise-distinct, cmap-present codepoints are recorded in `U+XXXX` notation, none of them a Phase 6 carry-over
- Assumption A1 is discharged: all four new M3 container roles resolve to real six-digit hex in both static-preset and Material You modes
- Zero repo files modified
</success_criteria>

<output>
Create `.planning/phases/09-wlogout-to-wleave-migration/09-01-SUMMARY.md` when done.
</output>
