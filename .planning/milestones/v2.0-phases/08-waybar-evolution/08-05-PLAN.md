---
phase: 08-waybar-evolution
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - waybar/.config/waybar/modules.jsonc
  - waybar/.config/waybar/waybar-modules.css
  - waybar/.config/waybar/config-full.jsonc
  - waybar/.config/waybar/config-minimal.jsonc
  - waybar/.config/waybar/config-floating.jsonc
  - waybar/.config/waybar/config-vertical.jsonc
  - waybar/.config/waybar/style-vertical.css
  - theme-engine/.config/theme-engine/theme-doctor
  - .planning/phases/08-waybar-evolution/.waybar-config-baseline/
autonomous: true
requirements: [BAR-03, BAR-05]

must_haves:
  truths:
    - "A 4th waybar layout — a narrow left column — exists, is selectable from the picker, launches at login, and re-themes through a theme switch like the other three (D-11, D-14, BAR-03)."
    - "Every module in the column is glyph-only; the detail that cannot fit (now-playing text, per-stat values, notification count) lives in tooltips (D-13)."
    - "The vertical bar draws on every monitor, exactly like the other three — no `output` key anywhere (D-15)."
    - "Vertical does NOT become the default; `full` is still what a fresh install and the container gate land on (D-16)."
    - "The `custom/gaming-mode` indicator reads the SAME `~/.cache/gaming-mode` state file the Super-key menu reads — a read-only consumer, never a second copy of the logic (D-35)."
    - "The `custom/notification` bell is present in EVERY layout, closing the live floating parity bug (D-26, BAR-05)."
    - "Every module and the bell were defined ONCE in modules.jsonc and reached four layouts — the D-31 payoff, cashed."
    - "`theme-doctor` mechanically proves every module in every layout's modules-* arrays has a reachable CSS rule AND that every colour token those rules reference is @define-color'd — an unresolved colour is a HARD FAIL (D-17)."
    - "`theme-doctor` discovers waybar stylesheets by glob, so a 5th layout can never silently escape the CSS-parse guard (D-31's copy-paste-drift class, killed in its 4th hiding place)."
  artifacts:
    - waybar/.config/waybar/config-vertical.jsonc
    - waybar/.config/waybar/style-vertical.css
    - "waybar/.config/waybar/modules.jsonc (gains the custom/gaming-mode definition)"
    - "waybar/.config/waybar/waybar-modules.css (gains the #custom-gaming-mode rules)"
    - "theme-engine/.config/theme-engine/theme-doctor (gains the D-17 per-module colour-resolution gate + glob-discovered waybar sheets)"
    - .planning/phases/08-waybar-evolution/.waybar-config-baseline/vertical.json
  key_links:
    - "config-vertical.jsonc includes modules.jsonc + bar-common.jsonc (08-01's mechanism, 08-03's signal keys) and redefines a module in FULL where the column genuinely diverges — never a partial patch (D-31)."
    - "style-vertical.css imports the theme sheet, then waybar-font.css, then waybar-modules.css, then waybar-visibility.css LAST — the exact order 08-01/08-03 established."
    - "theme-doctor reuses waybar-equivalence-check --resolve (08-01) as its config resolver — one JSONC/include parser in the repo, not two."
    - "theme-doctor's colour gate reads the flattened GTK3 CssProvider output, whose @define-color set arrives via the import chain from the matugen-rendered ~/.local/state/theme/waybar.css."
    - "waybar-switch.sh / waybar-launch.sh need ZERO edits — 08-02 already made layout discovery a disk glob (D-32)."
---

<objective>
Author the vertical (left) waybar layout through 08-01's shared-include mechanism (D-11, D-12, D-13, D-14, D-15, D-16), add the `custom/gaming-mode` indicator Phase 7 deferred here (D-35), land the `custom/notification` parity fix in floating (D-26), and fold a rerunnable per-module colour-resolution gate into `theme-doctor` so D-17's "full module re-test" is mechanical rather than eyeballed.

Purpose: ROADMAP success criterion 2 says the vertical layout must re-theme correctly through a theme switch — **"full module re-test, not copy-paste."** That parenthetical is the requirement. A human clicking through four bars under 22 palettes is not a re-test; it is a hope. This plan discharges it as a gate that fails loudly, forever, on every future layout too.

The gate is not theoretical. **Verified live this session:** GTK3 raises *no* parsing error and yields a *non-empty* CssProvider for a rule referencing an undefined named colour — the token survives verbatim into the resolved sheet and the module simply renders unstyled. `theme-doctor`'s existing fatal-error/empty-provider guard is structurally blind to it. That is the exact Phase-1 battery/backlight silent-unstyled bug class, and it is precisely what D-17 exists to catch.

Output: a 4th layout (config + stylesheet), one new shared module definition reaching all four bars, the bell in all four bars, and a `theme-doctor` that discovers waybar sheets by glob and asserts per-module colour resolution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-waybar-evolution/08-CONTEXT.md
@.planning/phases/08-waybar-evolution/08-RESEARCH.md
@.planning/phases/08-waybar-evolution/08-PATTERNS.md
@.planning/phases/08-waybar-evolution/08-UI-SPEC.md
@.planning/phases/08-waybar-evolution/08-01-SUMMARY.md
@.planning/phases/08-waybar-evolution/08-02-SUMMARY.md
@.planning/phases/08-waybar-evolution/08-03-SUMMARY.md
</context>

<inherited_contracts>
**Read this before touching any file. Four upstream plans already decided things this plan may not relitigate.**

1. **08-01 owns the include mechanism.** `modules.jsonc` holds ONE canonical definition per module key and **zero** bar-level or `modules-*` keys. waybar's `include` is **whole-key, first-defined-wins — NOT a deep merge**. A layout that needs a variant of a module **redefines that key in FULL** in its own file. Never partial-patch. Never copy-paste a module into four config files. The exact `include` path form (bare/tilde/`$HOME`) and the two-element array form were both proven against the installed binary — **read `08-01-SUMMARY.md` and `08-03-SUMMARY.md` and reuse what they recorded. Do not re-derive it.**

2. **08-02 already made layout discovery dynamic (D-32).** Dropping `config-vertical.jsonc` + `style-vertical.css` on disk makes the layout selectable and launchable with **ZERO script edits**. Do NOT add a "vertical" case to `waybar-switch.sh` or `waybar-launch.sh`. Do NOT author a `"Vertical"` display string — 08-02's picker derives the label from the filename (its SUMMARY records the exact derivation rule). `full` stays the hardcoded *fallback constant* in `waybar-launch.sh` (D-16); that is one constant, not a list, and it must survive.

3. **08-03 owns waybar visibility.** `on-sigusr1` is a FIXED `hide`; `on-sigusr2` is `reload`. Both live in `bar-common.jsonc`. **Nothing in this plan may send waybar a raw SIGUSR1/SIGUSR2.** `config-vertical.jsonc` includes `bar-common.jsonc` and inherits the signal contract for free — it does not redeclare it.

4. **`waybar-equivalence-check` (08-01) is a LIVE DRIFT DETECTOR, not a one-shot.** This plan intentionally changes the effective config of all four layouts, so the gate WILL diff. The required move — established by 08-03 — is: run the gate, **read the printed diff, confirm it contains ONLY the intended change**, then re-snapshot (`--snapshot .planning/phases/08-waybar-evolution/.waybar-config-baseline/`). **NEVER hand-edit the baseline.** A hand-edited baseline is a gate that cannot fail.

5. **Housekeeping invariants** (inherited, non-negotiable): stow-package layout, never host-only state · no `font-family` declaration in any waybar stylesheet — the rendered `waybar-font.css` fragment owns it exclusively (Phase 6 CR-01) · zero hex-literal colours in any pipeline stylesheet · glyphs are cmap-verified against the installed font before use, never copied from a cheat-sheet (Phase 6/7 D-10).
<!-- planner-discipline-allow: font-family -->
</inherited_contracts>

<tasks>

<task type="auto">
  <name>Task 1: Verify the glyph set, define custom/gaming-mode once, land the bell in floating, re-baseline the drift gate</name>
  <files>waybar/.config/waybar/modules.jsonc, waybar/.config/waybar/waybar-modules.css, waybar/.config/waybar/config-full.jsonc, waybar/.config/waybar/config-minimal.jsonc, waybar/.config/waybar/config-floating.jsonc, .planning/phases/08-waybar-evolution/.waybar-config-baseline/</files>

  <read_first>
    - waybar/.config/waybar/modules.jsonc (post-08-01 — the canonical definitions; `custom/notification` already lives here, extracted from full)
    - waybar/.config/waybar/waybar-modules.css (post-08-01/08-03 — the shared module rules, already carrying the OLED-trimmed `#workspaces button.active`)
    - waybar/.config/waybar/config-full.jsonc (post-refactor — its `modules-right` tail is `tray`, `custom/notification`, `custom/power`)
    - waybar/.config/waybar/config-minimal.jsonc (post-refactor — `modules-right` is `custom/notification`, `tray`)
    - waybar/.config/waybar/config-floating.jsonc (post-refactor — **`custom/notification` is ABSENT from `modules-right`; this is the live D-26 parity bug** and the whole reason D-31 exists)
    - hypr/.config/hypr/scripts/gaming-mode-toggle.sh (lines 1-52 — the boxed header, and `_write_state`/`_read_state`: the state file is `$HOME/.cache/gaming-mode` and its ONLY two values are the literals `on` and `off`)
    - .planning/phases/08-waybar-evolution/08-PATTERNS.md ("Gaming-mode indicator module" — the `custom/notification` exec/return-type/format shape this module is modeled on)
    - .planning/phases/08-waybar-evolution/08-UI-SPEC.md (Typography → gaming-mode glyph; Color → "the gaming-mode glyph's ON/OFF colour swap (`@primary` when ON, `@outline` when OFF)" — BINDING; Copywriting → the tooltip copy is `Gaming Mode: ON` / `Gaming Mode: OFF`, **identical wording to the Phase 7 Super-key menu entry**, so the two surfaces reading the same state file also read the same to the user)
    - waybar/.config/waybar/config-floating.jsonc `custom/updates` (its `exec`/`exec-if` are inline shell strings — the in-repo precedent that an inline `exec` in a waybar config is house-legal, so this module needs no new script)
  </read_first>

  <action>
**A — cmap-verify the two new glyphs BEFORE writing them anywhere (Phase 6/7 D-10 discipline).**

The UI-SPEC's gaming-mode glyph candidates are placeholders it explicitly marks *"unverified — cmap-check required before use."* Resolve them properly. Run a `python3` + `fontTools` check against the installed `/usr/share/fonts/TTF/FiraCodeNerdFont-Regular.ttf` that asserts, for each candidate codepoint, BOTH:
  1. presence in `TTFont(...).getBestCmap()`, and
  2. an `hmtx` advance width equal to the advance of `M` (a 1.00-cell glyph).

Criterion 2 is not optional and it is not hygiene: this is the **non-Mono** FiraCode build, and Phase 7 (07-08-SUMMARY) recorded that a double-width glyph silently shifts everything aligned next to it. In a **48px column** a 2-cell glyph does not shift the layout — it overflows it.

The two codepoints to verify and adopt:
  - **ON → `U+F04FE`** (`nf-md-controller`)
  - **OFF → `U+F04FF`** (`nf-md-controller_off`)

Both were pre-checked during planning against this exact TTF (present; advance 1200 == `M`'s 1200; 1.00 cells). **Re-run the check anyway and record the output in the SUMMARY** — the discipline is "resolve glyphs from the installed font," and a plan-time claim is not a run-time verification. If either is absent on the executing machine, pick a present 1.00-cell replacement from the same MDI controller/gamepad family (`U+F02B4`, `U+F0EBB`, and `U+F11B` are also pre-checked present at 1.00 cells) and record the substitution.

`U+F04FF` (a *semantically* off controller) is deliberately chosen over the UI-SPEC's looser "same glyph at reduced opacity" alternative — the UI-SPEC offers both, and a distinct OFF glyph plus the mandated `@outline` colour is the stronger, less-ambiguous signal.

**B — define `custom/gaming-mode` ONCE, in `modules.jsonc` (D-35, D-31).**

Keys:
  - `return-type`: `json`
  - `interval`: `2` — a 2-second `cat` of a ~3-byte cached file is free, and it bounds the indicator's worst-case staleness after a toggle to 2s. (PATTERNS suggested `5`; `2` is the same mechanism, tuned. Record the deviation.) Do **not** wire a `signal`-based refresh: that would require editing `gaming-mode-toggle.sh`, which is plan 08-04's file this wave.
  - `exec`: a single fixed shell string that reads `$HOME/.cache/gaming-mode`, compares it against the **literal** `on`, and prints **one of exactly two hardcoded JSON payloads** — `{"alt":"on","class":"on","tooltip":"Gaming Mode: ON"}` or `{"alt":"off","class":"off","tooltip":"Gaming Mode: OFF"}`. **The file's bytes must NEVER be interpolated into the emitted payload.** Compare-and-map, never echo. See the threat model: waybar renders the `tooltip` field as Pango markup, so echoing an attacker-controlled state file straight through would be a markup/format injection primitive. A two-branch literal map has no such surface, and it also makes an unexpected state-file value fail safe to OFF. Match the missing-file case to `gaming-mode-toggle.sh`'s own `_read_state` default (absent ⇒ `off`).
  - `format`: `{icon}`
  - `format-icons`: an object mapping `on` → the U+F04FE glyph and `off` → the U+F04FF glyph. With `return-type: json`, waybar selects the icon from the payload's `alt` field and applies the payload's `class` to the widget — so the glyph and the CSS class both derive from the one state read. This is the same JSON-driven `format-icons`/`class` shape `custom/notification` already uses in this file.

This module is a **read-only consumer** of the state file `gaming-mode-toggle.sh` already owns. It writes nothing, and it re-derives nothing — which is exactly D-28's anti-drift constraint applied to a surface D-28 does not even name. The waybar indicator, the Phase 7 menu entry, and (later) 08-09's swaync toggle all read one file, so they can never disagree.

**C — add the `#custom-gaming-mode` rules to `waybar-modules.css`** (shared, so all four layouts get them from one place — the D-31 payoff):
  - base rule on `#custom-gaming-mode`: `color: @outline`, a transparent background, and padding/margin/border-radius/transition matching the neighbouring `#custom-theme` / `#custom-waybar-layout` block already in this file.
  - a `#custom-gaming-mode.on` rule setting `color: @primary`.

Colours are exactly the UI-SPEC's binding mapping (`@primary` ON, `@outline` OFF) and mirror the Phase 7 menu's own ON/OFF colour swap for the same feature. **No filled background in either state** — a permanently-lit accent pill is precisely what D-06's luminance trim just removed from the workspace pills. Zero hex literals; both tokens must exist in the rendered palette (Task 4's gate will assert exactly that).

**D — compose the module into the three existing layouts, and fix the floating bell (D-26).**

Uniform, mechanical placement rule — apply it identically everywhere so there is nothing to remember:
  - **`custom/gaming-mode` goes immediately BEFORE `custom/notification`** in every layout's `modules-right`.
  - `config-full.jsonc` — `modules-right` tail becomes `tray`, `custom/gaming-mode`, `custom/notification`, `custom/power`.
  - `config-minimal.jsonc` — `modules-right` becomes `custom/gaming-mode`, `custom/notification`, `tray`.
  - `config-floating.jsonc` — **insert BOTH** `custom/gaming-mode` and `custom/notification`, in that order, immediately before the trailing `custom/power`. The bell insertion is the D-26 parity fix: the module has been defined and working in full/minimal for months and was simply never copy-pasted into the fourth file. That is the drift D-31 exists to kill, and this is it being cashed out — **one definition, already in `modules.jsonc`, referenced by name.** Do not redefine the bell in floating.

Touch **only** the `modules-right` arrays in these three files. No bar keys, no module redefinitions, no reordering of anything else.

**E — review the diff, THEN re-baseline (inherited contract 4).**

Run `waybar-equivalence-check`. It will diff — that is correct and expected. **Read the diff.** It must contain ONLY:
  - a new top-level `custom/gaming-mode` key in every layout's resolved config (it comes from `modules.jsonc`, so it lands in all of them — including any layout that does not display it; that is normal under whole-key include resolution and is not a defect),
  - `custom/gaming-mode` appended into `modules-right` for full / minimal / floating,
  - `custom/notification` inserted into floating's `modules-right`.

Anything else in that diff is an accident you just made. Fix the config — **never the baseline**. Once the diff is confirmed clean, re-snapshot with `--snapshot .planning/phases/08-waybar-evolution/.waybar-config-baseline/` and commit the updated snapshots. The gate must stay armed for 08-08, which re-points `mpris`'s `on-click` next wave.
  </action>

  <verify>
    <automated>python3 -c "
from fontTools.ttLib import TTFont
f = TTFont('/usr/share/fonts/TTF/FiraCodeNerdFont-Regular.ttf')
cmap, hmtx = f.getBestCmap(), f['hmtx']
m = hmtx['M'][0]
bad = []
for cp in (0xF04FE, 0xF04FF):
    if cp not in cmap: bad.append(('ABSENT', hex(cp))); continue
    a = hmtx[cmap[cp]][0]
    print(f'U+{cp:04X} present adv={a} ({a/m:.2f}x M)')
    if a != m: bad.append(('WIDE', hex(cp)))
raise SystemExit(1 if bad else 0)
"</automated>
    <automated>grep -q 'custom/gaming-mode' waybar/.config/waybar/modules.jsonc && echo "OK: module defined once"</automated>
    <automated>test $(grep -c 'custom/gaming-mode' waybar/.config/waybar/modules.jsonc) -eq 1</automated>
    <automated>grep -q 'custom-gaming-mode' waybar/.config/waybar/waybar-modules.css && grep -q 'custom-gaming-mode.on' waybar/.config/waybar/waybar-modules.css && echo "OK: base + .on rules present"</automated>
    <automated>for f in config-full config-minimal config-floating; do grep -q 'custom/notification' "waybar/.config/waybar/$f.jsonc" || { echo "FAIL: bell missing from $f"; exit 1; }; done; echo "OK: bell in all three horizontal layouts"</automated>
    <automated>hypr/.config/hypr/scripts/waybar-equivalence-check waybar/.config/waybar</automated>
    <automated>jq -e '.["modules-right"] | index("custom/notification") != null' .planning/phases/08-waybar-evolution/.waybar-config-baseline/floating.json</automated>
  </verify>

  <acceptance_criteria>
    - **Glyphs:** the fontTools check above exits 0 — `U+F04FE` and `U+F04FF` are both present in the installed `FiraCodeNerdFont-Regular.ttf` cmap AND both have an `hmtx` advance exactly equal to `M`'s (1.00 cell). The measured advances are recorded in the SUMMARY. No codepoint is written into any config before this check passes.
    - `waybar/.config/waybar/modules.jsonc` contains exactly ONE `custom/gaming-mode` key (`grep -c` returns 1), and the module carries `return-type: json`, `interval: 2`, `format: "{icon}"`, and a `format-icons` object with `on`/`off` entries.
    - **Injection guard (source assertion):** the `exec` string in `custom/gaming-mode` contains the literal token `Gaming Mode: ON` and the literal token `Gaming Mode: OFF` as two separate hardcoded payloads, and the state file's content appears ONLY on the left-hand side of a comparison — never inside a payload string. Verify by reading the line: no shell expansion of the file's content may appear between the quotes of any emitted `tooltip`/`alt`/`class` value.
    - `waybar/.config/waybar/waybar-modules.css` contains a `#custom-gaming-mode` rule with `color: @outline` and a `#custom-gaming-mode.on` rule with `color: @primary`. Neither rule sets a filled background.
    - `grep -cE '#[0-9a-fA-F]{3,8}\b' waybar/.config/waybar/waybar-modules.css` returns 0 — no hex literal was introduced.
    - **D-26 parity closed:** `custom/notification` appears in the `modules-right` array of all three of `config-full.jsonc`, `config-minimal.jsonc`, and `config-floating.jsonc`. `jq -e '.["modules-right"] | index("custom/notification") != null'` on the re-snapshotted `floating.json` exits 0 — inverting the exact assertion 08-01 used to *prove the bug existed*.
    - **`custom/notification` is NOT redefined in `config-floating.jsonc`** — it is referenced by name only, resolving from `modules.jsonc`. `grep -c '"custom/notification"[[:space:]]*:' waybar/.config/waybar/config-floating.jsonc` returns 0. (A redefinition here would be the copy-paste D-31 exists to kill, re-committed in the very act of fixing it.)
    - **Reviewed re-baseline:** `waybar-equivalence-check waybar/.config/waybar` exits 0 against the re-snapshotted baseline, and the SUMMARY records the pre-re-snapshot diff verbatim, showing it contained ONLY the gaming-mode key + the three array insertions + floating's bell. No baseline file was hand-edited.
    - `custom/gaming-mode` immediately precedes `custom/notification` in the `modules-right` array of every layout that lists both — the uniform placement rule holds with zero exceptions.
  </acceptance_criteria>

  <done>Two cmap-and-advance-verified glyphs back a `custom/gaming-mode` module defined once in `modules.jsonc`, styled once in `waybar-modules.css`, and composed into three layouts; the bell finally exists in floating by reference, not by copy; and the drift gate is green against a re-snapshotted baseline whose diff was read line by line before it was accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Author config-vertical.jsonc — the column composition</name>
  <files>waybar/.config/waybar/config-vertical.jsonc</files>

  <read_first>
    - waybar/.config/waybar/modules.jsonc (post-Task-1 — the canonical definitions this layout composes from, including the brand-new `custom/gaming-mode`)
    - waybar/.config/waybar/bar-common.jsonc (08-03 — the `on-sigusr1: hide` / `on-sigusr2: reload` signal contract this layout inherits by including it)
    - waybar/.config/waybar/config-full.jsonc (post-refactor — the bar-level key shape and the `include`-first composition shape to mirror)
    - waybar/.config/waybar/config-floating.jsonc (post-refactor — its `network` `format-icons` (`󰤯 󰤟 󰤢 󰤥 󰤨`, disconnected `󰤮`) and its `battery` `format-icons` are ALREADY LIVE glyph sets; reuse them verbatim rather than sourcing new codepoints)
    - .planning/phases/08-waybar-evolution/08-01-SUMMARY.md (**the exact `include` path form that resolved on the installed waybar 0.15.0 — reuse it, do not re-derive**)
    - .planning/phases/08-waybar-evolution/08-03-SUMMARY.md (**whether a two-element `include` array resolved** — this layout needs `modules.jsonc` + `bar-common.jsonc`; if the two-element array did NOT resolve, follow whatever fallback 08-03 recorded)
    - .planning/phases/08-waybar-evolution/08-RESEARCH.md ("Vertical Layout Module Set" — `position: "left"` is a first-class documented bar option; `width` replaces `height`; waybar stacks modules-left/-center/-right top-to-bottom automatically for a vertical bar)
    - .planning/phases/08-waybar-evolution/08-UI-SPEC.md (Spacing → "Vertical layout" table: 48px column, 8px module gap, 8px flush-left margin, 24×24 glyph box — BINDING; Typography → 20px glyphs, tooltips inherit the existing `tooltip label` rule with zero new CSS)
    - .planning/phases/08-waybar-evolution/08-CONTEXT.md (D-11 through D-16 — read all six)
  </read_first>

  <action>
Create `waybar/.config/waybar/config-vertical.jsonc`. This is a **composition file**: `include`, bar keys, three module arrays, and full redefinitions of only the modules the column genuinely changes.

**A — `include` first.** Include `modules.jsonc` AND `bar-common.jsonc`, using the exact path form and array form 08-01/08-03 proved against the installed binary. Including `bar-common.jsonc` is what gives this layout the `on-sigusr1: hide` / `on-sigusr2: reload` contract — the visibility owner (08-03) must be able to hide the vertical bar exactly as it hides the other three, and it gets that for free here. **Do not redeclare either signal key.**

**B — bar-level keys** (UI-SPEC's Vertical Layout spacing table is binding):
  - `layer`: `top`
  - `position`: `left` — D-14's flush-left column. This is a documented first-class option; no rotation hack.
  - `width`: `48` — the column width (24px glyph box + 12px padding each side). Note it is `width`, **not** `height`: the axis flips with `position`.
  - `margin-left`: `8`, `margin-top`: `8`, `margin-bottom`: `8` — the flush-left inset (D-14: "flush left with a margin", not a floating island). **No `margin-right`.**
  - `spacing`: `0`
  - **NO `output` key** (D-15) — its absence is what makes waybar draw the bar on every monitor, exactly as all three existing layouts already do. Adding one would introduce the per-monitor config the CONTEXT explicitly rejected.

**C — module arrays.** For a `position: left` bar, waybar maps `modules-left` → top, `modules-center` → middle, `modules-right` → bottom.
  - `modules-left`: `hyprland/workspaces`
  - `modules-center`: `clock`, `mpris`
  - `modules-right`: `cpu`, `memory`, `temperature`, `pulseaudio`, `network`, `battery`, `custom/gaming-mode`, `custom/notification`, `tray`, `custom/power`

That is exactly D-12's named set (workspaces · stacked clock · volume · network · battery · notification button · media trigger · cpu/memory/temperature stacked · tray · power) plus the D-35 indicator, and it preserves Task 1's uniform rule that `custom/gaming-mode` immediately precedes `custom/notification`.

**`hyprland/window` is deliberately absent.** It is not in D-12's module set, and the UI-SPEC's own 24×24 glyph-box list omits it. A window title has no glanceable column form and no natural glyph, so a `hyprland/window` module here would be an invisible hover target — worse than nothing. D-13's "you hover" is satisfied for the *now-playing* text, which keeps a real glyph to hover (the mpris player icon). Record this reading in the SUMMARY.

**D — redefine, in FULL, only the modules the column changes** (whole-key; a partial patch is silently discarded under first-defined-wins):

*Glyph-only (D-13 — the module renders a bare Nerd Font glyph, and every dropped string reappears in `tooltip-format`):*
  - `mpris` — `format` becomes `{player_icon}` alone; keep `format-paused` as `{status_icon}` and `format-stopped` as the empty string (D-25's no-player-no-segment behaviour is already encoded there and must survive). **Keep the existing `on-click` / `on-click-right` / `on-scroll-up` / `on-scroll-down` bindings and the existing `tooltip-format` byte-identical to the canonical definition** — plan 08-08 re-points `on-click` to the media popup next wave and needs one predictable place per layout to do it. Do not pre-empt it. Drop `max-length` (meaningless with no text).
  - `pulseaudio` — `format` becomes `{icon}` alone; `format-muted` becomes the muted glyph alone (strip the ` muted` text). Reuse the canonical `format-icons` array verbatim. Add a `tooltip-format` carrying `{volume}%` and `{desc}`.
  - `network` — glyph-only formats. **Take `format-icons`, the disconnected glyph, and the wifi/ethernet glyphs verbatim from `config-floating.jsonc`'s already-live network definition** — those codepoints are rendering on this machine today, so they are verified by use. `format-wifi` → `{icon}`, `format-ethernet` → floating's ethernet glyph, `format-disconnected` → floating's disconnected glyph. Keep a `tooltip-format` with `{ifname}`/`{ipaddr}` and the wifi/ethernet bandwidth tooltips.
  - `battery` — reuse the canonical (floating-sourced) `format-icons` and `states`; `format` becomes `{icon}` alone, `format-charging` / `format-plugged` become their existing glyphs alone. Add a `tooltip-format` carrying `{capacity}%` and `{time}`.
  - `custom/notification` — `format` becomes `{icon}` alone (drop the trailing `{}` count text). Keep the full canonical `format-icons` map, `return-type`, `exec-if`, `exec`, `on-click`, `on-click-right`, and `escape: true` byte-identical — the bell's behaviour must not change, only its width.

*Stacked (D-12 names these three explicitly as "stacked readouts", so they keep a value):*
  - `clock` — the stacked clock. `format` renders hours and minutes on two lines (a `\n` between `%H` and `%M`), no glyph, no seconds. Keep the canonical `tooltip-format` calendar block and the canonical `actions` verbatim — the hover calendar is exactly the D-13 "detail goes to tooltips" pattern, already built.
  - `cpu`, `memory`, `temperature` — each renders its existing glyph on line 1 and its value on line 2, with the value wrapped in a Pango `<small>` tag so it de-emphasises against the 20px glyph without needing a second CSS rule (waybar enables Pango markup on these modules by default; `<small>` is the same primitive the existing configs' `<span>` usage already relies on). Keep each module's canonical `interval` and `tooltip`/`tooltip-format`. Keep `temperature`'s `critical-threshold` and its `format-icons` — the `#temperature.critical` CSS rule already in `waybar-modules.css` must keep firing.

  **Recorded conflict, resolved:** UI-SPEC's Typography section says every column module is "a bare Nerd Font glyph at 20px — no text label, ever." D-12 is a **locked user decision** and says cpu/memory/temperature are "stacked readouts." A readout with no value is not a readout. Locked decisions outrank the spec, so these three keep a stacked numeric value; every *other* module in the column is bare-glyph, honouring the spec everywhere it does not collide. Record this in the SUMMARY.

*Not redefined — the canonical definition is already correct for a column:*
  - `hyprland/workspaces` (already `{icon}`-only with numeric/active glyphs), `custom/gaming-mode` (already `{icon}`-only), `custom/power` (already a bare glyph), `tray`. Referencing them by name is the point of D-31; redefining them would be the copy-paste it exists to prevent.

**E — the picker needs nothing.** 08-02 already globs `config-*.jsonc` and derives the menu label from the filename. Do not author a `"Vertical"` string anywhere; do not touch `waybar-switch.sh` or `waybar-launch.sh`. `full` remains the launcher's fallback constant (D-16) — vertical is a 4th *valid* value in `~/.cache/current-waybar-layout`, never the default.
  </action>

  <verify>
    <automated>test -f waybar/.config/waybar/config-vertical.jsonc</automated>
    <automated>hypr/.config/hypr/scripts/waybar-equivalence-check --resolve waybar/.config/waybar/config-vertical.jsonc | jq -e '.position == "left" and .width == 48 and (has("height") | not) and (has("output") | not)'</automated>
    <automated>hypr/.config/hypr/scripts/waybar-equivalence-check --resolve waybar/.config/waybar/config-vertical.jsonc | jq -e '.["on-sigusr1"] == "hide" and .["on-sigusr2"] == "reload"'</automated>
    <automated>hypr/.config/hypr/scripts/waybar-equivalence-check --resolve waybar/.config/waybar/config-vertical.jsonc | jq -e '([.["modules-left"],.["modules-center"],.["modules-right"]] | flatten) == ["hyprland/workspaces","clock","mpris","cpu","memory","temperature","pulseaudio","network","battery","custom/gaming-mode","custom/notification","tray","custom/power"]'</automated>
    <automated>grep -cE '"(hyprland/workspaces|custom/gaming-mode|custom/power|tray)"[[:space:]]*:[[:space:]]*\{' waybar/.config/waybar/config-vertical.jsonc | grep -qx 0 && echo "OK: no needless redefinitions"</automated>
    <automated>grep -q 'include' waybar/.config/waybar/config-vertical.jsonc && grep -q 'bar-common' waybar/.config/waybar/config-vertical.jsonc && echo "OK: includes modules + bar-common"</automated>
    <automated>grep -rn 'vertical' hypr/.config/hypr/scripts/waybar-switch.sh hypr/.config/hypr/scripts/waybar-launch.sh; test $(grep -c 'vertical' hypr/.config/hypr/scripts/waybar-switch.sh hypr/.config/hypr/scripts/waybar-launch.sh | awk -F: '{s+=$2} END {print s}') -eq 0 && echo "OK: zero script edits (D-32 holds)"</automated>
  </verify>

  <acceptance_criteria>
    - `config-vertical.jsonc` exists and its first key is `include`, naming BOTH `modules.jsonc` and `bar-common.jsonc` in the exact path/array form 08-01 and 08-03 recorded as resolving on the installed binary.
    - **Resolved bar keys:** `position == "left"`, `width == 48`, `height` is ABSENT, `output` is ABSENT (D-15 — the bar draws on every monitor precisely because no `output` key exists), `margin-left == 8`, `margin-top == 8`, `margin-bottom == 8`, `margin-right` is ABSENT.
    - **Signal contract inherited, not redeclared:** the resolved config has `on-sigusr1 == "hide"` and `on-sigusr2 == "reload"`, AND `grep -c 'on-sigusr' waybar/.config/waybar/config-vertical.jsonc` returns 0 — the values arrive from `bar-common.jsonc` (08-03's owner contract), they are not copy-pasted here.
    - **Module set (D-12 + D-35):** the resolved concatenation of `modules-left` + `modules-center` + `modules-right` is exactly `hyprland/workspaces`, `clock`, `mpris`, `cpu`, `memory`, `temperature`, `pulseaudio`, `network`, `battery`, `custom/gaming-mode`, `custom/notification`, `tray`, `custom/power` — and `hyprland/window` does not appear.
    - **No needless redefinition (D-31):** `config-vertical.jsonc` does NOT redefine `hyprland/workspaces`, `custom/gaming-mode`, `custom/power`, or `tray` — the canonical definitions are used by reference.
    - **Glyph-only (D-13):** in the resolved config, the `format` of `mpris`, `pulseaudio`, `network` (`format-wifi`), `battery`, and `custom/notification` each contain **no literal text characters** — only a placeholder (`{icon}` / `{player_icon}`) and optional whitespace. Each of these five modules has a non-empty `tooltip-format` (the dropped detail must land somewhere).
    - **Stacked (D-12):** `clock.format` contains a newline; `cpu.format`, `memory.format`, and `temperature.format` each contain a newline and a `<small>`-wrapped value placeholder.
    - **Every glyph in this file is already live elsewhere in the repo** (workspaces/power/tray/notification/pulseaudio glyphs from `modules.jsonc`; network + battery glyph sets from `config-floating.jsonc`) **or is one of Task 1's two cmap-verified codepoints.** No codepoint enters this file from a cheat-sheet. Confirm by diffing the file's non-ASCII codepoint set against the union of those two sources plus `U+F04FE`/`U+F04FF`; the difference must be empty.
    - **Zero script edits (D-32/D-16):** `grep -c 'vertical'` across `waybar-switch.sh` and `waybar-launch.sh` returns 0. The layout is discoverable purely because the file exists on disk. This is the criterion that actually proves 08-02 paid off. <!-- planner-discipline-allow: vertical -->
    - `waybar -c waybar/.config/waybar/config-vertical.jsonc -s waybar/.config/waybar/style-vertical.css --log-level error` starts and logs **no parse error** (kill it after a couple of seconds; a clean startup is the signal — this runs after Task 3 creates the stylesheet).
  </acceptance_criteria>

  <done>A 4th layout exists as a pure composition: it includes the shared modules and the shared signal contract, declares a 48px left column on every monitor, lists D-12's module set plus the gaming indicator, redefines in full only what the column genuinely changes, and required not one character of script change to become selectable.</done>
</task>

<task type="auto">
  <name>Task 3: Author style-vertical.css — the column stylesheet</name>
  <files>waybar/.config/waybar/style-vertical.css</files>

  <read_first>
    - waybar/.config/waybar/style-full.css (post-08-01/08-03 — the layout-owned `*` reset and the OLED-trimmed `window#waybar` block; and lines 1-4, the exact `@import` order every layout sheet must follow)
    - waybar/.config/waybar/waybar-modules.css (post-08-01/08-03/Task-1 — every shared module rule this sheet inherits, including `#workspaces button`'s `border-bottom` treatment and the new `#custom-gaming-mode` rules)
    - waybar/.config/waybar/style-floating.css (the one existing sheet that deviates structurally — skim it to see how a layout expresses property-level overrides on shared selectors without redeclaring whole blocks)
    - .planning/phases/08-waybar-evolution/08-03-SUMMARY.md (**the `waybar-visibility.css` import must be LAST so the owner's idle-dim rule wins the cascade** — and the file is seeded by `stow.sh`, so the import always resolves)
    - .planning/phases/08-waybar-evolution/08-UI-SPEC.md (Spacing → Vertical Layout table (48px column · 8px module gap · 24×24 glyph box · 12px side padding); Typography → 20px glyphs; Color → OLED Styling Deltas — every value stays a palette token, true-black is REJECTED not deferred)
  </read_first>

  <action>
Create `waybar/.config/waybar/style-vertical.css`. It is a **thin per-layout sheet** — the shared module rules already do most of the work. Author only what the column genuinely changes.

**A — imports, in exactly this order** (the order is load-bearing; it is 08-01's + 08-03's contract):
  1. the rendered theme sheet (`../../.local/state/theme/waybar.css`) — the named colours must be defined before any rule references them
  2. the rendered font fragment (`../../.local/state/theme/waybar-font.css`) — **Phase 6 CR-01: this fragment owns the typeface declaration exclusively. Do not declare a typeface anywhere in this file.**
  3. `waybar-modules.css` — the shared module rules
  4. `../../.local/state/theme/waybar-visibility.css` — **LAST**, so 08-03's owner-written idle-dim rule wins the cascade

<!-- planner-discipline-allow: font-family -->

**B — the layout-owned block** (copy the shape from `style-full.css`, adapting the axis):
  - the `*` reset: `min-height: 0`, no border, no border-radius — but set the base `font-size` to **20px**, the UI-SPEC's glyph-emphasis size for the column, rather than the horizontal bars' 13px body size. Every module in this column is a glyph; 20px IS the body size here. The `<small>`-wrapped values in cpu/memory/temperature scale down from it automatically via Pango, needing no rule of their own.
  - `window#waybar`: `background: alpha(@background, 0.90)` and `color: @on_surface` and `border-radius: 14px` — the same OLED-trimmed treatment 08-03 applied to the other three (D-06). **Never a hex literal, never a black keyword.**
  - **the accent edge flips axis.** The horizontal bars carry `border-bottom: 1px solid alpha(@primary, 0.4)` — the edge facing the desktop. For a left-anchored column that edge is the **right** edge, so this sheet sets `border-right: 1px solid alpha(@primary, 0.4)` and explicitly zeroes the bottom border. Same token, same 1px weight, same 0.4 alpha — the trim is preserved, only the geometry rotates. Record this adaptation in the SUMMARY.

**C — column-specific property-level overrides on the shared selectors.** CSS cascade is later-wins *at the property level*, which is strictly more granular than the JSONC side's whole-key rule — so override individual properties here; do not redeclare whole blocks (that is the whole reason the CSS split works differently from the config split, per 08-01 Task 3).
  - **Glyph box (UI-SPEC binding):** give every module widget in the column a `min-width` of 24px and `padding: 4px 12px` — 24 + 12 + 12 = the 48px column. Apply as one grouped selector over the column's module ids so there is a single place to change it.
  - **Module gap (UI-SPEC binding: 8px):** `margin: 4px 0` on each module — adjacent 4px margins compose to the 8px vertical gap. The shared sheet's horizontal `margin: 4px 1px` / `margin: 4px 2px` values are meaningless in a column and must be overridden to a 0 horizontal margin, or the modules will not centre in a 48px track.
  - **Workspaces:** the shared `#workspaces button` rule carries a `border-bottom` (transparent base, `@primary` when `.active` — 08-03's OLED trim). In a column that reads as an underline. Override it to a **left** border on `#workspaces button` (transparent) and `#workspaces button.active` (`2px solid @primary`), and zero the bottom border on both. Same accent token, correct axis. Keep `background: transparent` on the active state — do NOT reintroduce the filled pill 08-03 just removed.
  - **cpu / memory / temperature:** the shared group rule gives them a `@surface_variant` pill with a coloured `border-bottom`. In a column, rotate the accent to `border-left` and zero the bottom, matching the workspaces treatment. Leave the `@surface_variant` background and every colour token as-is.
  - **Clock:** the shared `#clock` rule sets a larger font-size; override it to a size that fits two stacked lines of digits inside 48px (the UI-SPEC's 16px heading size is the on-scale choice) and centre the text.
  - **Tray:** ensure the tray icons stack rather than run off the column — no colour change, geometry only.

**D — what this sheet must NOT do:**
  - **No typeface declaration** (Phase 6 CR-01 — the rendered font fragment owns it).
  - **No hex literal, no `black`, no `rgb(0,0,0)`** — D-06 rejects a true-black bar outright: it would opt the column out of the palette and break the five Phase-5 light presets. Every colour is a palette token, optionally wrapped in `alpha()`.
  - **No new named colour.** Every `@token` this sheet references must already be one of the 19 keys the matugen template renders. Task 4's gate turns this from a convention into an assertion.
  - **No `#custom-gaming-mode` rules** — Task 1 put them in the shared sheet precisely so all four layouts get them from one place.
  </action>

  <verify>
    <automated>test -f waybar/.config/waybar/style-vertical.css</automated>
    <automated>grep -cE '^\s*font-family\s*:' waybar/.config/waybar/style-vertical.css | grep -qx 0 && echo "OK: CR-01 invariant holds"</automated>
    <automated>grep -cE '#[0-9a-fA-F]{3,8}\b|[^-a-z]black\b|rgb\(0, *0, *0\)' waybar/.config/waybar/style-vertical.css | grep -qx 0 && echo "OK: zero literal colours"</automated>
    <automated>grep -q 'alpha(@background' waybar/.config/waybar/style-vertical.css && grep -q 'border-right' waybar/.config/waybar/style-vertical.css && echo "OK: OLED trim, accent edge rotated"</automated>
    <automated>python3 -c "
import re,sys
s=open('waybar/.config/waybar/style-vertical.css').read()
order=[m.group(1) for m in re.finditer(r'@import url\(\"([^\"]+)\"\)', s)]
print('import order:', order)
assert order[-1].endswith('waybar-visibility.css'), 'visibility import must be LAST'
assert any(o.endswith('waybar-modules.css') for o in order), 'must import shared module rules'
assert order.index([o for o in order if o.endswith('waybar-modules.css')][0]) > order.index([o for o in order if o.endswith('waybar.css')][0]), 'theme must import before modules'
print('OK: import order correct')
"</automated>
    <automated>./stow.sh >/dev/null 2>&1; theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -i 'style-vertical'</automated>
  </verify>

  <acceptance_criteria>
    - `style-vertical.css` exists and its `@import` order is: rendered theme sheet → rendered font fragment → `waybar-modules.css` → `waybar-visibility.css` **last** (asserted mechanically by the python check above, not by eye).
    - `grep -cE '^\s*font-family\s*:'` returns 0 — the Phase 6 CR-01 invariant (the typeface is owned exclusively by the rendered font fragment) survives its 4th stylesheet.
    - `grep -cE '#[0-9a-fA-F]{3,8}\b|[^-a-z]black\b|rgb\(0, *0, *0\)'` returns 0 — every colour is a palette token. (The `#` in `#clock` is an ID selector, not a colour; the regex requires hex digits.)
    - `window#waybar` sets `background: alpha(@background, 0.90)` and `border-right: 1px solid alpha(@primary, 0.4)`, and does **not** set a bottom border — the D-06 trim, rotated to the column's outward-facing edge.
    - `#workspaces button.active` resolves to `background: transparent` with a **left** accent border — the filled high-luminance pill 08-03 removed does not reappear in the 4th layout.
    - The column's module widgets resolve to a 24px `min-width` with 12px horizontal padding (24+12+12 = the 48px bar width) and a 4px vertical / 0px horizontal margin (composing the UI-SPEC's binding 8px inter-module gap).
    - This sheet contains **no** `#custom-gaming-mode` rule — it inherits it from the shared sheet (`grep -c 'custom-gaming-mode' style-vertical.css` returns 0).
    - After `./stow.sh`, `theme-doctor` reports **PASS** for `waybar/style-vertical.css` — a non-empty CssProvider with no fatal parse error, proving GTK3 resolved the relative `@import` chain from the deployed path and did not silently discard the sheet. (This is the failure mode that survived four PASSing verification rounds in Phase 6 — the sheet being discarded *in full*.)
  </acceptance_criteria>

  <done>The column has a thin stylesheet that inherits the shared module rules, carries the OLED trim with its accent edge rotated to the right, hits the UI-SPEC's 48px/24px/8px geometry exactly, references no colour that is not a palette token, and is confirmed by a real GTK3 parse to load with a non-empty provider from its deployed path.</done>
</task>

<task type="auto">
  <name>Task 4: Fold D-17's "full module re-test" into theme-doctor as a rerunnable gate</name>
  <files>theme-engine/.config/theme-engine/theme-doctor</files>

  <read_first>
    - theme-engine/.config/theme-engine/theme-doctor (lines 178-292 — the GTK CSS-parse guard: its `GTK3_CSS_SHEETS` array **hardcodes the three per-layout waybar sheets**, and `_theme_doctor_css_parse` already runs a REAL `Gtk.CssProvider` load per sheet and connects `parsing-error`. Also lines 17-30 — the `check()` helper and the PASS/FAIL counters every new assertion must report through; and lines 8, 306-309 — `set -uo pipefail` (deliberately no `-e`: a gate must survive an individual check failing) and the exit contract)
    - hypr/.config/hypr/scripts/waybar-equivalence-check (08-01 — its `--resolve <config>` mode already emits a layout's fully-resolved effective config as JSON, having stripped JSONC comments and applied waybar's first-defined-wins include semantics. **Reuse it. Do not write a second JSONC/include parser in this repo.**)
    - waybar/.config/waybar/waybar-modules.css (the shared rules — the source of most module selectors the gate will look for)
    - matugen/.config/matugen/templates/waybar-colors.css (the 19 `@define-color` keys that reach `~/.local/state/theme/waybar.css` — the universe of legal colour tokens)
    - .planning/phases/08-waybar-evolution/08-CONTEXT.md (D-17 — "the exact bug class Phase 1 hit with battery/backlight silently rendering unstyled"; fold into theme-doctor is the stated preference)
    - .planning/phases/08-waybar-evolution/08-RESEARCH.md (line ~203 — "theme-doctor's existing GTK CSS-parse guard currently hardcodes its 3-file list... a small, easy-to-miss 4th occurrence of the exact copy-paste-drift class D-31 exists to fix")
  </read_first>

  <action>
**A — kill the 4th hardcoded list (D-31/D-32's disk-truth principle, applied where RESEARCH found it hiding).**

`GTK3_CSS_SHEETS` names the waybar layout stylesheets one by one. That is the same copy-paste-drift pattern D-32 killed in `waybar-switch.sh` and `waybar-launch.sh`, sitting in a third script — and this plan is about to prove it, because a 4th layout that silently escapes the CSS-parse guard is exactly how a stylesheet gets discarded in full without anyone noticing.

Replace the per-layout waybar entries with a **glob** over the deployed `$HOME/.config/waybar/style-*.css`. Build the array so the glob's results are appended to the non-waybar sheets (wlogout, gtk-3.0, swaync), which stay explicit — they are not a family and have no enumeration to derive. Guard the glob against the nullglob/no-match case so an unstowed waybar package degrades to a SKIP, never to a literal unexpanded pattern being handed to the parser.

**B — add the D-17 gate: per-module CSS reachability + colour resolution.**

Add a new section after the existing CSS-parse guard, reporting through the same `check()` helper so it lands in the same PASS/FAIL summary and the same exit code. Structure it as a bash driver + one `python3` heredoc (mirroring `_theme_doctor_css_parse`'s existing shape: emit `STATUS<TAB>context<TAB>reason` lines on stdout, swallow GTK's own stderr chatter, never call `Gtk.init()` — CssProvider needs no display and `init()` would break the headless container gate).

For each deployed `$HOME/.config/waybar/config-*.jsonc`:

  1. **Resolve the layout's effective config** by invoking `$HOME/.config/hypr/scripts/waybar-equivalence-check --resolve <config>` and parsing its stdout with `json.load`. **Never `eval`, never a shell string.** If that script is not deployed, emit a SKIP for the whole gate with a clear reason — do not FAIL (the container gate must stay green and unattended, P7 D-34) and do not fall back to a hand-rolled JSONC parser.

  2. **Collect the module names** from the resolved `modules-left` + `modules-center` + `modules-right` arrays.

  3. **Map each module name to the widget id waybar actually assigns it.** The rule: for a compositor-namespaced module (`hyprland/…`, `sway/…`, `river/…`, `wlr/…`) the id is the LAST segment — so `hyprland/workspaces` → `#workspaces` and `hyprland/window` → `#window`. For every other slash-bearing name the id is the name with `/` replaced by `-` — so `custom/notification` → `#custom-notification`. A bare name is its own id — `cpu` → `#cpu`.

  4. **Flatten the layout's stylesheet.** Load `$HOME/.config/waybar/style-<layout>.css` into a `Gtk.CssProvider` (GTK **3**) and take `provider.to_string()`.

     **This is verified, not assumed** (confirmed live against the installed GTK 3.24.52 during planning): `to_string()` returns the **fully flattened sheet — the entire `@import` chain inlined**, including `@define-color` declarations pulled in from the rendered `waybar.css` and rules pulled in from `waybar-modules.css`. Unresolved colour references survive **verbatim** in value position, including inside `alpha(@token, 0.9)`. That single call is therefore the whole primitive this gate needs, and it reuses machinery already in this script.

     Before relying on it, assert the property once at gate start: the flattened output of any layout sheet must contain at least one `@define-color` (proving the theme import inlined) and at least one shared-module selector (proving `waybar-modules.css` inlined). If either is missing on the executing machine, the flattening assumption is false — **fall back to a manual recursive `@import` walk** (resolve each `@import url("…")` relative to the importing sheet's own directory, concatenate) and record the fallback in the SUMMARY. Do not silently proceed on a false assumption.

  5. **Assertion 1 — rule reachability.** Every module id from step 3 must appear as a selector in the flattened CSS, matched on a word boundary so `#cpu` cannot be satisfied by a `#cpu-foo` rule. A module listed in a layout's arrays with **no** reachable rule is a FAIL naming the layout and the module. This is the "silently unstyled module" half of the Phase-1 bug.

  6. **Assertion 2 — colour resolution (HARD FAIL).** From the same flattened output, collect (a) every referenced `@<ident>` token and (b) every `<ident>` declared by an `@define-color` statement. Exclude the CSS at-rule keywords from (a): `import`, `define-color`, `media`, `keyframes`, `supports`, `charset`. Any token referenced but never defined is a **hard FAIL**, naming the layout and the token.

     **Why this cannot be folded into the existing guard, and why it is the load-bearing half of D-17** — verified live during planning against the installed GTK 3.24.52: a rule referencing an **undefined** named colour raises **NO `parsing-error` signal** and yields a **NON-EMPTY provider**. The token simply survives into the resolved sheet and the widget renders unstyled. The existing guard's two signals — fatal parse error, and empty provider — are both **structurally blind** to it. That is precisely the Phase-1 battery/backlight failure, and it is why D-17 asks for a *new* assertion rather than a 4th line in an existing array.

  7. Report one `check()` line per layout for each assertion, so a failure names the layout.

**C — the gate must be provably capable of failing.** A gate nobody has seen fail is a gate nobody should trust (`keybind-doctor`'s own header convention; 08-01 Task 1 applied the same rule to `waybar-equivalence-check`). Self-test it: create a throwaway `config-zzztest.jsonc` (including `modules.jsonc`, listing one real module) plus a `style-zzztest.css` whose rule for that module references a colour token that is not one of the 19 palette keys; `./stow.sh`; confirm `theme-doctor` exits **nonzero** and its output names both the layout and the offending token; then delete both files, re-stow, and confirm it is green again. Record the failure output in the SUMMARY.

**D — degrade, never break.** Every new check follows the script's existing guarded-skip idiom (see the `stow`, `git`, and `gi` guards already in this file): missing `python3-gi`, missing `waybar-equivalence-check`, or an unstowed waybar package must produce a `[SKIP]` with a reason, not a `[FAIL]`. `theme-doctor` runs in the unattended container gate, and a gate that fails on a legitimately-absent optional dependency trains people to ignore it.
  </action>

  <verify>
    <automated>bash -n theme-engine/.config/theme-engine/theme-doctor && shellcheck -S error theme-engine/.config/theme-engine/theme-doctor</automated>
    <automated>grep -v '^[[:space:]]*#' theme-engine/.config/theme-engine/theme-doctor | grep -cE '\.config/waybar/style-(full|minimal|floating|vertical)\.css' | grep -qx 0 && echo "OK: hardcoded waybar sheet list is gone"</automated>
    <automated>grep -v '^[[:space:]]*#' theme-engine/.config/theme-engine/theme-doctor | grep -q 'style-\*\.css' && echo "OK: waybar sheets discovered by glob"</automated>
    <automated>grep -q 'waybar-equivalence-check' theme-engine/.config/theme-engine/theme-doctor && grep -q -- '--resolve' theme-engine/.config/theme-engine/theme-doctor && echo "OK: reuses the 08-01 resolver, no second JSONC parser"</automated>
    <automated>grep -q 'define-color' theme-engine/.config/theme-engine/theme-doctor && echo "OK: colour-resolution assertion present"</automated>
    <automated>./stow.sh >/dev/null 2>&1; theme-engine/.config/theme-engine/theme-doctor; echo "exit=$?"</automated>
    <automated>./stow.sh >/dev/null 2>&1; theme-engine/.config/theme-engine/theme-doctor 2>&1 | grep -icE 'vertical' | awk '{ if ($1 < 1) { print "FAIL: vertical layout not covered by the gate"; exit 1 } else print "OK: vertical is under the gate" }'</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n` exits 0 and `shellcheck -S error` reports no errors.
    - **The 4th hardcoded list is dead:** with comment lines stripped, `theme-doctor` contains **zero** per-layout waybar stylesheet paths, and contains a `style-*.css` glob. A 5th layout is covered by the CSS-parse guard automatically, with no edit to this script — the same disk-truth principle 08-02 applied to the two waybar scripts (D-31/D-32).
    - **One parser, not two:** the gate resolves layout configs by invoking `waybar-equivalence-check --resolve` and parsing its JSON. `theme-doctor` contains no JSONC comment-stripper and no `include`-resolution logic of its own.
    - **Assertion 1 fires for real:** for every deployed `config-*.jsonc`, every module in its `modules-*` arrays maps to a widget id that has a reachable CSS rule in the flattened stylesheet. With all four layouts stowed, this passes — including `custom/gaming-mode` (whose rule Task 1 put in the shared sheet) and floating's newly-added `custom/notification` (which reaches floating only via the shared sheet, so this criterion is also a live proof that the D-31 refactor actually delivers).
    - **Assertion 2 fires for real:** every `@<ident>` colour token referenced anywhere in each layout's flattened stylesheet is declared by an `@define-color` in that same flattened output (i.e. it arrived from the matugen-rendered `waybar.css` via the import chain). Zero unresolved tokens across all four layouts.
    - **The gate is provably capable of failing (the criterion that makes the other two mean anything):** with a throwaway 5th layout stowed whose stylesheet references a non-palette colour token, `theme-doctor` exits **nonzero** and its output names the layout and the token. After the throwaway files are removed and re-stowed, `theme-doctor` exits 0 again. Both outputs are recorded in the SUMMARY.
    - **Graceful degradation:** with `waybar-equivalence-check` absent from `~/.config/hypr/scripts/`, the new gate reports `[SKIP]` with a reason and `theme-doctor`'s exit code is unchanged — the unattended container gate stays green (P7 D-34).
    - `theme-doctor` exits 0 on the fully-stowed repo, and its output includes at least one line naming the `vertical` layout — proving the 4th layout is genuinely under the gate rather than merely on disk.
    - `theme-engine/.config/theme-engine/theme-parity` is still green — this plan added no render target (BAR-03 is pure config/CSS composition on the existing `waybar.css` target; the vertical layout costs the theme pipeline exactly nothing).
  </acceptance_criteria>

  <done>D-17's "full module re-test" is a command, not a checklist: `theme-doctor` discovers every waybar layout by glob, resolves each one's real module list through the single existing config resolver, and hard-fails if any module has no reachable CSS rule or references a colour the palette never defines — the exact bug class Phase 1 shipped and nobody saw. It has been watched to fail on purpose before being trusted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.cache/gaming-mode` → `custom/gaming-mode` `exec` → waybar Pango rendering | A host-side, user-writable state file whose value drives a module that waybar renders as **Pango markup** (waybar's `tooltip` and `format` output are markup-parsed). This is the only genuinely untrusted-ish input this plan introduces. |
| resolved waybar config JSON → `theme-doctor`'s gate | Module names, sourced from repo-owned JSONC, are used to build CSS selector searches. |
| `waybar-equivalence-check --resolve` stdout → `theme-doctor` | A subprocess's stdout is parsed as JSON inside a health-check that runs in the unattended container gate. |
| repo stylesheets → GTK3 CssProvider (waybar + theme-doctor) | CSS authored in-repo, deployed via stow, parsed with the session's privileges. No untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-20 | Tampering | `custom/gaming-mode` `exec` → waybar Pango markup | medium | mitigate | The `exec` **never interpolates the state file's bytes into its output.** It compares the file's content against the literal `on` and emits one of exactly TWO hardcoded JSON payloads. A tampered state file can therefore only select between two fixed, repo-authored strings — it cannot inject Pango markup, a format-string placeholder, or a `<span>` into the bar or the tooltip. An unexpected value fails safe to OFF. Asserted as a source criterion in Task 1. |
| T-08-21 | Elevation of Privilege | `custom/gaming-mode` `exec` shell string | medium | mitigate | The `exec` is a **fixed literal** in `modules.jsonc` with no variable expansion of any external value into a command position. No `eval`. The state file's content reaches only the right-hand side of a `[ … = on ]` test, quoted. (Precedent: `custom/updates` in `config-floating.jsonc` already uses an inline `exec` — this is the house pattern, hardened.) |
| T-08-22 | Tampering | `theme-doctor` parsing `waybar-equivalence-check --resolve` stdout | low | mitigate | Parsed with `json.load`, never `eval`. Module names are used only to build a regex/selector **search string**, never a shell command, a path, or a filename. A malformed resolver output produces a `[SKIP]` with a reason, not a crash and not a false PASS. |
| T-08-23 | Denial of Service | `theme-doctor` in the unattended container gate | low | mitigate | Every new check follows the script's existing guarded-skip idiom: absent `python3-gi`, absent `waybar-equivalence-check`, or an unstowed waybar package produce `[SKIP]`, not `[FAIL]`. `Gtk.init()` is never called (CssProvider needs no display), so the gate stays headless-safe. The glob is guarded against the no-match case so an unexpanded pattern is never handed to the parser. P7 D-34's "container gate stays green and unattended" is preserved. |
| T-08-24 | Repudiation | re-snapshotting the equivalence baseline | medium | mitigate | The baseline is the only record of what each layout's effective config *should* be. It is regenerated **only** by `waybar-equivalence-check --snapshot`, never hand-edited, and only after the printed diff has been read and confirmed to contain nothing but the intended change. The reviewed diff is recorded verbatim in the SUMMARY, so a future reader can audit what was accepted and why. A hand-edited baseline is a gate that cannot fail. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | **Not applicable to this plan** — it installs no packages. The phase's only new package (`eww`, AUR) is gated by a blocking human legitimacy checkpoint in plan 08-06. |
</threat_model>

## Artifacts this phase produces

Phase-wide registry (this plan's own artifacts marked **(this plan)**):

**New scripts** (`hypr/.config/hypr/scripts/`)
- `waybar-equivalence-check` — D-34 resolved-config equivalence gate (08-01)
- `waybar-visibility.sh` — the single visibility owner (08-03)
- `waybar-fullscreen-watch.sh` — Hyprland socket2 fullscreen listener (08-04)
- `media-art-resolve.sh`, `media-players.sh`, `media-status.sh` (08-07)
- `media-popup-open.sh` (08-08)

**Modified gates / scripts**
- `waybar-switch.sh`, `waybar-launch.sh` — disk-enumerated layouts (08-02). **Untouched by this plan — that is the point (D-32).**
- `theme-engine/.config/theme-engine/theme-doctor` — waybar stylesheets discovered by **glob** (4th hardcoded list killed); new **D-17 per-module CSS-reachability + colour-resolution gate** (hard-fails on an unresolved `@token`) **(this plan)**

**New config / style files**
- `waybar/.config/waybar/modules.jsonc`, `waybar-modules.css` (08-01)
- `waybar/.config/waybar/bar-common.jsonc` (08-03)
- `waybar/.config/waybar/config-vertical.jsonc` — the 4th layout: `position: left`, `width: 48`, no `output` key **(this plan, D-11..D-16)**
- `waybar/.config/waybar/style-vertical.css` — the column stylesheet; OLED trim with the accent edge rotated to `border-right` **(this plan, D-06/D-14)**
- `eww/.config/eww/eww.yuck`, `eww/.config/eww/eww.scss` (08-06/08-07)
- `matugen/.config/matugen/templates/eww-colors.scss` (08-06)

**New waybar module names**
- `custom/gaming-mode` — defined ONCE in `modules.jsonc`; read-only consumer of `~/.cache/gaming-mode`; `return-type: json`, `interval: 2`, JSON-driven `format-icons` + `class` **(this plan, D-35)**

**New glyph codepoints** (cmap + advance-width verified against the installed `FiraCodeNerdFont-Regular.ttf`)
- `U+F04FE` (`nf-md-controller`) — gaming-mode ON, coloured `@primary` **(this plan)**
- `U+F04FF` (`nf-md-controller_off`) — gaming-mode OFF, coloured `@outline` **(this plan)**

**Module composition changes**
- `custom/notification` added to `config-floating.jsonc` — the D-26 parity fix; **by reference, not by copy** **(this plan, BAR-05)**
- `custom/gaming-mode` added to `modules-right` in full / minimal / floating / vertical, always immediately before `custom/notification` **(this plan)**

**New runtime state (host-side only — never written into the repo)**
- `~/.local/state/theme/waybar-visibility.css`, `~/.cache/waybar-visibility.d/*` (08-03)
- `~/.local/state/theme/eww.scss` (08-06), `~/.cache/eww-media-art/` (08-07)
- **This plan adds none** — the vertical layout and the gaming indicator introduce zero new runtime state; the indicator is a pure reader of a file Phase 7 already owns.

**New matugen `[templates.*]` keys**: `[templates.eww]` (08-06) — **this plan adds no render target**; the vertical layout consumes the existing `waybar.css` target, so `contract.json` stays at its current count and `theme-parity` is unaffected.

**New planning artifacts**
- `.planning/phases/08-waybar-evolution/.waybar-config-baseline/vertical.json` — the 4th layout's baseline; the other three are **re-snapshotted** after a reviewed diff **(this plan)**

**Other phase artifacts**: `[templates.eww]` + `contract.json` 17→18 + `scss-kv` format (08-06) · `$mainMod SHIFT, B` bar-toggle keybind, `waybar-fullscreen-watch.sh` autostart, hypridle idle listener (08-04) · `eww` → `install.sh` AUR_PKGS + `stow.sh` PACKAGES (08-06) · swaync `volume`/`backlight`/`buttons-grid` widgets, `mpris` removed (08-09) · `08-BAR-02-EVIDENCE.md` (08-10)

<verification>
- `theme-engine/.config/theme-engine/theme-doctor` exits 0 on the fully-stowed repo, its output names the `vertical` layout, and it was **watched failing on purpose** (throwaway layout with a non-palette colour token → nonzero exit naming the layout and the token).
- `hypr/.config/hypr/scripts/waybar-equivalence-check waybar/.config/waybar` exits 0 against a baseline re-snapshotted **only after** its diff was read and confirmed to contain nothing but the gaming-mode module, the three array insertions, and floating's bell.
- `theme-engine/.config/theme-engine/theme-parity` is still green — BAR-03 added no render target.
- `waybar -c config-vertical.jsonc -s style-vertical.css --log-level error` starts clean, with no parse error.
- `grep -c 'vertical'` across `waybar-switch.sh` and `waybar-launch.sh` returns 0 — the 4th layout became selectable and launchable with **zero script edits** (D-32's payoff, cashed).
- `git status --porcelain` is clean after a theme switch (the CLEAN-02 invariant `theme-doctor` already asserts).
</verification>

<success_criteria>
- **BAR-03 / ROADMAP criterion 2:** a vertical (left) waybar layout exists, is selectable from the picker, launches at login, and re-themes correctly through a theme switch — with the "full module re-test" discharged as a **rerunnable gate** (`theme-doctor`) plus a human pass under one light and one dark preset, not as a copy-paste of another layout's stylesheet (D-11, D-17).
- **BAR-05 (half):** the `custom/notification` bell is present in every layout, including floating, where it has been missing (D-26). The panel it opens is plan 08-09's job.
- **D-35:** the gaming-mode indicator Phase 7 deferred here exists, reads the state file Phase 7 left as its probe, and can never disagree with the Super-key menu because it derives nothing.
- **D-31, proven:** one module definition, one CSS rule, one edit — reaching four layouts. The bell fix is a one-line array insertion rather than a fourth copy. If that is not what it felt like, the refactor did not work.
- **D-16 holds:** `full` is still what a fresh install and the container gate land on.
</success_criteria>

<verify>
  <human-check>
    1. Press `Super+B`. The picker now lists **four** layouts, and the 4th reads as `Vertical` — a label derived from the filename by 08-02, not a string anyone typed. Select it.
    2. The bar is a narrow column flush against the left edge, translucent, with a thin accent line down its **right** edge. Every module is a single glyph, except the clock (hours stacked over minutes) and cpu/memory/temperature (glyph over a small value). Nothing is clipped by the 48px width; no glyph renders as a tofu box.
    3. Hover the media segment — the now-playing artist/title/album appears in a tooltip. Hover the clock — the calendar appears. Hover cpu/network/battery — the detail is there. Nothing that needs a text label is trying to render one in the column.
    4. Toggle gaming mode from the Super-key menu. The waybar indicator flips within ~2 seconds: controller glyph in the accent colour when ON, the "off" controller in a dim outline colour when OFF, tooltip reading `Gaming Mode: ON` / `Gaming Mode: OFF` — **the same wording the menu entry uses.** They read the same file; confirm they never disagree.
    5. Switch to a **light** preset, then a **dark** preset, staying on the vertical layout. Every module re-themes. Nothing renders unstyled, invisible, or black-on-black. This is D-17's human half — the gate proves the colours *resolve*; your eyes confirm they were the *right* colours.
    6. Switch to Floating. **The notification bell is there now** — click it; the swaync panel opens. It has been missing from this layout since before this phase began.
  </human-check>
</verify>

<output>
Create `.planning/phases/08-waybar-evolution/08-05-SUMMARY.md` when done. Record:
1. The **glyph verification output** — the measured cmap presence and `hmtx` advance ratios for `U+F04FE` / `U+F04FF` (and any substitution, if either was absent).
2. The **reviewed equivalence diff, verbatim**, as it stood before the baseline was re-snapshotted — this is the audit trail for T-08-24, and the next planner will need it.
3. Whether GTK3's `CssProvider.to_string()` flattened the `@import` chain as planning verified, or whether the manual `@import`-walk fallback was needed.
4. The **self-test output showing `theme-doctor` failing on purpose** (the throwaway layout with a non-palette colour token) — a gate nobody has watched fail is a gate nobody should trust.
5. The two resolved spec conflicts and their reasoning: (a) cpu/memory/temperature keep a stacked numeric value because D-12 is a locked decision and UI-SPEC's "no text label, ever" cannot override it; (b) `hyprland/window` is absent from the column because D-12's module set omits it and a window title has no glanceable column form.
6. The `border-bottom` → `border-right` accent-edge rotation for the left-anchored column, and the 20px base font-size (vs. the horizontal bars' 13px), so 08-08 knows what it is composing into.
</output>
