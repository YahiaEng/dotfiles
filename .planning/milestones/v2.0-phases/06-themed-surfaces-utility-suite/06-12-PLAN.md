---
phase: 06-themed-surfaces-utility-suite
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - hypr/.config/hypr/hyprlock.conf
  - matugen/.config/matugen/templates/hyprlock-colors.conf
autonomous: true
gap_closure: true
requirements: [LOCK-01]

must_haves:
  truths:
    - "A fresh install.sh run completes end-to-end on a yay-based system without crashing during package cleanup: orphan-removal and cache-clean use $AUR_HELPER (not a literal paru), so verify_packages, audio/dbus/swayosd service enablement, and VSCodium extension install all still run."
    - "The hyprlock password-field placeholder color is sourced from the theme pipeline via the hyprlock render target (no hardcoded Catppuccin literal hex), so it re-themes under both light and dark presets."
  artifacts:
    - "install.sh cleanup steps using \"$AUR_HELPER\" for orphan-removal and cache-clean"
    - "matugen/.config/matugen/templates/hyprlock-colors.conf defining a stripped-hex placeholder color variable"
    - "hypr/.config/hypr/hyprlock.conf placeholder_text referencing the rendered variable, not a literal hex"
  key_links:
    - "install.sh cleanup -> $AUR_HELPER -R / $AUR_HELPER -Sc (yay-safe indirection)"
    - "hyprlock.conf placeholder_text -> ~/.local/state/theme/hyprlock.conf $on_surface_variant_hex (rendered per theme)"
---

<objective>
Close two remaining single-surface defects: CR-04 (install.sh hardcodes literal `paru` in the orphan-removal and cache-clean cleanup steps, aborting the whole script under `set -euo pipefail` on a yay-only system before services/extensions/verify run) and WR-01 (hyprlock's password placeholder hardcodes a Catppuccin Mocha hex, bypassing the theme pipeline that owns every other color in the file). Both are independent, mechanical, single-file corrections that restore, respectively, the project's fresh-install reproducibility promise on yay and the "colors sourced live from the shared theme pipeline" success criterion for the lock screen.

Purpose: install.sh already detects and uses `$AUR_HELPER` correctly for the actual package install; only the cleanup step reverts to a literal. hyprlock.conf already sources a dedicated render target for every other color; only placeholder_text leaks a literal hex.
Output: install.sh cleanup using the helper variable, and a themed placeholder color rendered through the hyprlock target.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-VERIFICATION.md
@.planning/phases/06-themed-surfaces-utility-suite/06-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use $AUR_HELPER instead of literal paru in install.sh cleanup (CR-04)</name>
  <files>install.sh</files>
  <read_first>
    - install.sh (lines 253-296: `AUR_HELPER` is detected as "paru" or "yay" at lines 255-270 and used correctly for the install at line 281; the orphan-removal at line 292 and cache-clean at line 296 hardcode the literal `paru`; lines 285-289 and 294-295 are explanatory comments that also name `paru`)
  </read_first>
  <action>
    In the cleanup block, change the orphan-removal invocation (line ~292) from `paru -R --noconfirm "${ORPHANS[@]}"` to `"$AUR_HELPER" -R --noconfirm "${ORPHANS[@]}"`, and the cache-clean invocation (line ~296) from `paru -Sc --noconfirm` to `"$AUR_HELPER" -Sc --noconfirm`. Keep the `mapfile`/orphan count-guard and the `--noconfirm` flags exactly as-is. Also update the two adjacent explanatory comments (the orphan-removal comment at ~285-289 and the cache-clean comment at ~294-295) so they refer to `$AUR_HELPER` rather than a literal `paru`, so the comments no longer mislead the next reader on a yay system. Do NOT touch the AUR-helper detection/bootstrap block (lines 253-270) — those literal `paru` references are correct (they name the package being cloned/installed as the fallback helper).
  </action>
  <acceptance_criteria>
    - install.sh contains `"$AUR_HELPER" -R --noconfirm "${ORPHANS[@]}"` and `"$AUR_HELPER" -Sc --noconfirm` in the cleanup block (positive assertion of the fixed indirection).
    - The AUR-helper detection block (the `command -v paru` / `AUR_HELPER="paru"` / paru.git bootstrap) is unchanged.
    - install.sh still passes `bash -n`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n install.sh && grep -Fq '"$AUR_HELPER" -R --noconfirm "${ORPHANS[@]}"' install.sh && grep -Fq '"$AUR_HELPER" -Sc --noconfirm' install.sh && grep -Fq 'AUR_HELPER="paru"' install.sh</automated>
  </verify>
  <done>On a yay-only system install.sh's cleanup runs `yay -R` / `yay -Sc`, so the script no longer aborts under set -e and reaches service enablement, VSCodium extensions, and verify_packages.</done>
</task>

<task type="auto">
  <name>Task 2: Theme the hyprlock placeholder color via the render target (WR-01)</name>
  <files>matugen/.config/matugen/templates/hyprlock-colors.conf, hypr/.config/hypr/hyprlock.conf</files>
  <read_first>
    - matugen/.config/matugen/templates/hyprlock-colors.conf (the 19 `$var = rgba({{colors.KEY.default.hex_stripped}}ff)` lines that render into ~/.local/state/theme/hyprlock.conf; note it emits rgba() forms, which a pango span cannot consume — a bare stripped-hex is needed)
    - hypr/.config/hypr/hyprlock.conf (line 5 `source = ~/.local/state/theme/hyprlock.conf`; line 177 `placeholder_text = <span foreground="##a6adc8">  Enter Password...</span>` — the only hardcoded color in the file; note hyprlock uses `##` to escape a literal `#` in a pango span)
    - .planning/phases/06-themed-surfaces-utility-suite/06-VERIFICATION.md (human_verification #1 — the light/dark placeholder contrast check that remains a human step after this source fix)
  </read_first>
  <action>
    In hyprlock-colors.conf, add one new variable that renders a bare (no rgba wrapper, no leading #) stripped-hex value for the muted placeholder color: `$on_surface_variant_hex = {{colors.on_surface_variant.default.hex_stripped}}`. Place it near the existing `$on_surface_variant` line so the two share an obvious relationship. This renders to e.g. `$on_surface_variant_hex = a6adc8` in the state file that hyprlock.conf sources.

    In hyprlock.conf, change the placeholder_text span so its `foreground` references the rendered variable with hyprlock's `##` escape instead of the literal Catppuccin hex: `placeholder_text = <span foreground="##$on_surface_variant_hex">  Enter Password...</span>`. Keep the rest of the span text and the two leading spaces intact. Change nothing else in the input-field block (the FIX-02 keys check_text/fail_text/ignore_empty_input and every other `$var` color must stay verbatim).

    After editing, run a theme-apply (or the render step) if the environment allows and confirm the rendered ~/.local/state/theme/hyprlock.conf defines `$on_surface_variant_hex`. The visual legibility check under a light theme is already tracked as human_verification #1 in 06-VERIFICATION.md and is not a blocking part of this task.
  </action>
  <acceptance_criteria>
    - hyprlock-colors.conf defines `$on_surface_variant_hex` from `{{colors.on_surface_variant.default.hex_stripped}}` (a bare stripped hex, no rgba wrapper).
    - hyprlock.conf's placeholder_text line references `##$on_surface_variant_hex` in its span foreground (positive assertion — the pipeline variable replaces the hardcoded hex).
    - The FIX-02 hardening keys (check_text, fail_text, ignore_empty_input) and all other `$var` colors in the input-field block are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq '$on_surface_variant_hex = {{colors.on_surface_variant.default.hex_stripped}}' matugen/.config/matugen/templates/hyprlock-colors.conf && grep -Fq 'foreground="##$on_surface_variant_hex"' hypr/.config/hypr/hyprlock.conf && grep -q 'check_text' hypr/.config/hypr/hyprlock.conf && grep -q 'fail_text' hypr/.config/hypr/hyprlock.conf</automated>
  </verify>
  <done>The hyprlock placeholder color is rendered per-theme through the hyprlock target instead of a fixed Catppuccin hex; light/dark contrast is the only remaining (human) verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh AUR helper indirection | `$AUR_HELPER` is set only to the literal "paru" or "yay" by the script's own command -v detection; not user-controlled |
| matugen palette -> hyprlock render target -> pango span | Placeholder hex is rendered from the trusted palette (hex_stripped) into the sourced state file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12-01 | Tampering | install.sh cleanup helper indirection | low | mitigate | Replacing the hardcoded literal with `$AUR_HELPER` (only ever "paru"/"yay" from command -v detection) removes a yay-system crash and restores end-to-end reproducibility integrity |
| T-06-12-02 | Injection | hyprlock placeholder color variable | low | accept | `$on_surface_variant_hex` is rendered by matugen from the trusted palette and consumed inside a pango span in the hyprlock render target; no external input |
| T-06-12-SC | Tampering | package installs | low | accept | install.sh installs AUR/pacman packages, but this plan adds NO new packages — it only fixes the helper-variable indirection; the Phase 6 package legitimacy audit already gated every install |
</threat_model>

<verification>
- install.sh cleanup uses `"$AUR_HELPER" -R` and `"$AUR_HELPER" -Sc`; detection/bootstrap block unchanged; script syntactically valid.
- hyprlock-colors.conf defines the stripped-hex placeholder variable; hyprlock.conf references it in placeholder_text; FIX-02 keys intact.
</verification>

<success_criteria>
- CR-04 closed: install.sh completes on yay (cleanup uses $AUR_HELPER).
- WR-01 closed: hyprlock placeholder color is pipeline-sourced (no hardcoded hex).
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

- `install.sh` cleanup steps use `$AUR_HELPER` for orphan-removal and cache-clean (yay-safe reproducibility)
- `matugen/.config/matugen/templates/hyprlock-colors.conf` new variable: `$on_surface_variant_hex` (stripped-hex placeholder color)
- `hypr/.config/hypr/hyprlock.conf` placeholder_text now references `##$on_surface_variant_hex` (pipeline-sourced, no literal hex)
</artifacts_produced>

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-12-SUMMARY.md` when done
</output>
