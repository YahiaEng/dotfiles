---
phase: 06-themed-surfaces-utility-suite
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - hypr/.config/hypr/scripts/icon-theme-switch.sh
  - hypr/.config/hypr/scripts/font-switch.sh
  - hypr/.config/hypr/config/keybinds.conf
  - hypr/.config/hypr/config/windowrules.conf
  - waybar/.config/waybar/style-full.css
  - waybar/.config/waybar/style-minimal.css
  - waybar/.config/waybar/style-floating.css
  - theme-engine/.config/theme-engine/lib/font.sh
  - theme-engine/.config/theme-engine/contract.json
  - theme-engine/.config/theme-engine/lib/contract.sh
  - theme-engine/.config/theme-engine/theme-doctor
autonomous: true
gap_closure: true
requirements: [UTIL-04, UTIL-05]

must_haves:
  truths:
    - "The user can invoke the icon-theme picker via Super+Shift+Z and the nerd-font switcher via Super+Shift+X — each opens a floating kitty running the fzf picker (fzf has a controlling TTY), matching the wallpaper-switch.sh precedent."
    - "The chosen nerd font propagates to waybar: waybar-font.css (the @import'd render target) is the sole owner of waybar's font-family, so a font switch actually changes waybar's font on all three layouts (full/minimal/floating)."
    - "theme-doctor presence-checks the two theme-invariant font render targets (kitty-font.conf, waybar-font.css) so a broken/absent font render is caught by the health gate; theme-parity remains at 0 failed."
  artifacts:
    - "hypr/.config/hypr/scripts/icon-theme-switch.sh (floating-kitty launcher wrapper, class icon-theme-picker)"
    - "hypr/.config/hypr/scripts/font-switch.sh (floating-kitty launcher wrapper, class font-switcher)"
    - "windowrules.conf float rules for the icon-theme-picker and font-switcher kitty classes"
    - "waybar/style-{full,minimal,floating}.css local * {} blocks with no font-family declaration"
    - "contract.json presence_only_files array listing kitty-font.conf and waybar-font.css"
  key_links:
    - "keybinds.conf Super+Shift+Z/X -> icon-theme-switch.sh/font-switch.sh -> uwsm app -- kitty --class ... -- picker"
    - "waybar/style-*.css @import waybar-font.css -> waybar-font.css * { font-family } (now sole owner)"
    - "contract.sh contract_presence_only_files() -> theme-doctor presence loop -> $STATE_DIR/{kitty-font.conf,waybar-font.css}"
---

<objective>
Close the two verification gaps that block UTIL-04 (icon-theme picker) and UTIL-05 (nerd-font switcher): CR-02 (both picker keybinds launch TTY-dependent fzf scripts with no terminal, so they silently no-op) and CR-01 (waybar's hardcoded `* { font-family }` literal always shadows the @import'd waybar-font.css, so font switching never changes waybar). Also fold in WR-07 — give theme-doctor presence coverage of the two theme-invariant font render targets it is currently blind to.

Purpose: The underlying picker/state-write/render logic for both axes was verified correct; only the invocation wiring (CR-02) and the waybar CSS sink (CR-01) are broken. This plan fixes the wiring so both features become reachable and effective.
Output: Two floating-kitty launcher wrappers, repointed keybinds, matching windowrules, three waybar stylesheets with the shadowing literal removed, and a presence-only contract mechanism for the font fragments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-VERIFICATION.md
@.planning/phases/06-themed-surfaces-utility-suite/06-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the icon-theme and font pickers a controlling TTY via floating-kitty launcher wrappers (CR-02)</name>
  <files>hypr/.config/hypr/scripts/icon-theme-switch.sh, hypr/.config/hypr/scripts/font-switch.sh, hypr/.config/hypr/config/keybinds.conf, hypr/.config/hypr/config/windowrules.conf</files>
  <read_first>
    - hypr/.config/hypr/scripts/wallpaper-switch.sh (the exact wrapper precedent to mirror: `uwsm app -- kitty --class "wallpaper-picker" --title ... -o background_opacity=0.85 -o font_size=11 -- <picker>`)
    - hypr/.config/hypr/config/windowrules.conf (the `windowrule { name = wallpaper-picker; match:class = ^(wallpaper-picker)$; float = on; size = 75% 70%; center = on; animation = popin }` block to replicate)
    - hypr/.config/hypr/config/keybinds.conf (lines 68-71: the four utility chords; Super+Shift+Z and Super+Shift+X currently exec the picker scripts directly)
    - hypr/.config/hypr/scripts/icon-theme-picker.sh (header confirms it is a bare fzf-in-floating-kitty picker expecting a TTY + kitty; it does NOT self-open kitty — do not double-wrap)
    - hypr/.config/hypr/scripts/font-switcher.sh (same: bare fzf picker, no self-wrap)
  </read_first>
  <action>
    Create two new launcher wrapper scripts that mirror wallpaper-switch.sh exactly (do not paste full contents here — replicate that file's shape).

    icon-theme-switch.sh: `uwsm app -- kitty` with `--class "icon-theme-picker"`, `--title "Icon Theme Picker"`, `-o background_opacity=0.85`, `-o font_size=11`, then `-- ~/.config/hypr/scripts/icon-theme-picker.sh`. Include the `#!/usr/bin/env bash` shebang and a short header comment block matching wallpaper-switch.sh's style. Make it executable (chmod +x).

    font-switch.sh: identical shape with `--class "font-switcher"`, `--title "Font Switcher"`, same opacity/font_size, then `-- ~/.config/hypr/scripts/font-switcher.sh`. Executable.

    In keybinds.conf, change the two bind lines (currently ~line 69 and ~line 71) so Super+Shift+Z execs `~/.config/hypr/scripts/icon-theme-switch.sh` and Super+Shift+X execs `~/.config/hypr/scripts/font-switch.sh` (the wrappers), keeping the trailing `# Icon theme picker` / `# Font switcher` comments intact. Leave the documentation comment block (lines 59-67) unchanged.

    In windowrules.conf, append two `windowrule { ... }` blocks after the existing wallpaper-picker block — one `name = icon-theme-picker` matching `match:class = ^(icon-theme-picker)$` and one `name = font-switcher` matching `match:class = ^(font-switcher)$`. Each uses `float = on`, `size = 75% 70%`, `center = on`, `animation = popin` (identical to the wallpaper-picker rule).
  </action>
  <acceptance_criteria>
    - hypr/.config/hypr/scripts/icon-theme-switch.sh and hypr/.config/hypr/scripts/font-switch.sh both exist, are executable, and pass `bash -n`.
    - icon-theme-switch.sh contains `--class "icon-theme-picker"` and ends its kitty invocation with `-- ~/.config/hypr/scripts/icon-theme-picker.sh`; font-switch.sh contains `--class "font-switcher"` and ends with `-- ~/.config/hypr/scripts/font-switcher.sh`.
    - keybinds.conf: the Super+Shift+Z bind targets `~/.config/hypr/scripts/icon-theme-switch.sh` and the Super+Shift+X bind targets `~/.config/hypr/scripts/font-switch.sh` (positive assertion — the bind lines now point at the wrappers, not the raw pickers).
    - windowrules.conf contains a float windowrule matching class `^(icon-theme-picker)$` and one matching `^(font-switcher)$`.
  </acceptance_criteria>
  <verify>
    <automated>test -x hypr/.config/hypr/scripts/icon-theme-switch.sh && test -x hypr/.config/hypr/scripts/font-switch.sh && bash -n hypr/.config/hypr/scripts/icon-theme-switch.sh && bash -n hypr/.config/hypr/scripts/font-switch.sh && grep -q 'icon-theme-picker.sh' hypr/.config/hypr/scripts/icon-theme-switch.sh && grep -q 'font-switcher.sh' hypr/.config/hypr/scripts/font-switch.sh && grep -q 'exec, ~/.config/hypr/scripts/icon-theme-switch.sh' hypr/.config/hypr/config/keybinds.conf && grep -q 'exec, ~/.config/hypr/scripts/font-switch.sh' hypr/.config/hypr/config/keybinds.conf && grep -q 'icon-theme-picker)\$' hypr/.config/hypr/config/windowrules.conf && grep -q 'font-switcher)\$' hypr/.config/hypr/config/windowrules.conf</automated>
  </verify>
  <done>Super+Shift+Z and Super+Shift+X each open a floating kitty running their fzf picker; the pickers have a real TTY and their previews (kitten icat) run inside kitty. UTIL-04/UTIL-05 invocation is reachable.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the waybar font-family literal so waybar-font.css owns the font (CR-01)</name>
  <files>waybar/.config/waybar/style-full.css, waybar/.config/waybar/style-minimal.css, waybar/.config/waybar/style-floating.css, theme-engine/.config/theme-engine/lib/font.sh</files>
  <read_first>
    - waybar/.config/waybar/style-full.css (lines 1-10: two @imports then a local `* {}` block whose font-family shadows the import)
    - waybar/.config/waybar/style-minimal.css (same pattern, line ~5)
    - waybar/.config/waybar/style-floating.css (same pattern, line ~5, plus a different fallback stack)
    - theme-engine/.config/theme-engine/lib/font.sh (lines 46-53: renders waybar-font.css as `* { font-family: "<name>", "Font Awesome 6 Free"; }`, with a comment whose cascade rationale becomes stale once the local literal is gone)
  </read_first>
  <action>
    In each of the three waybar stylesheets (style-full.css, style-minimal.css, style-floating.css), delete ONLY the font-family declaration line inside the local `* {}` block (the line beginning `font-family:` at ~line 5). Do NOT delete it as a comment — remove the line entirely. Keep every other property in that block (font-size, min-height, border, border-radius) and keep both `@import` lines at the top unchanged, so waybar-font.css remains @import'd and becomes the sole `* { font-family }` declaration for waybar.

    In font.sh, correct the now-stale comment (currently ~lines 46-51) that claims the rendered rule "wins over each stylesheet's own hardcoded `* { }` literal (later CSS rule of equal specificity wins)". Reword it to state that waybar-font.css is the SOLE owner of waybar's font-family (the per-stylesheet literal was removed in gap plan 06-10), so ordering is no longer load-bearing. Leave the printf that renders waybar-font.css unchanged — that render is still required.

    <!-- planner-discipline-allow: font-family -->
  </action>
  <acceptance_criteria>
    - `grep -c 'font-family' waybar/.config/waybar/style-full.css` returns 0; same for style-minimal.css and style-floating.css (the local literal is gone and no comment reintroduces it).
    - All three stylesheets still contain `@import url("../../.local/state/theme/waybar-font.css");` on line 2 (positive assertion — the import that now owns the font is preserved).
    - font.sh's waybar-font.css render block no longer describes a competing hardcoded per-stylesheet literal; it describes waybar-font.css as the sole owner (source assertion — do NOT grep font.sh for the font property token, which legitimately still appears in the printf).
    - font.sh still passes `bash -n` and still renders waybar-font.css (the printf line is intact).
  </acceptance_criteria>
  <verify>
    <automated>for f in waybar/.config/waybar/style-full.css waybar/.config/waybar/style-minimal.css waybar/.config/waybar/style-floating.css; do test "$(grep -c 'font-family' "$f")" -eq 0 && grep -q 'waybar-font.css' "$f" || { echo "FAIL $f"; exit 1; }; done && bash -n theme-engine/.config/theme-engine/lib/font.sh && grep -q 'waybar-font.css' theme-engine/.config/theme-engine/lib/font.sh</automated>
  </verify>
  <done>A font switch changes waybar's font on full/minimal/floating layouts because waybar-font.css is the only `* { font-family }` rule; the four-surface propagation (kitty/vscodium/GTK/waybar) is complete.</done>
</task>

<task type="auto">
  <name>Task 3: Add theme-doctor presence coverage for the font render targets without touching the parity gate (WR-07)</name>
  <files>theme-engine/.config/theme-engine/contract.json, theme-engine/.config/theme-engine/lib/contract.sh, theme-engine/.config/theme-engine/theme-doctor</files>
  <read_first>
    - theme-engine/.config/theme-engine/contract.json (the `files` array of 17 color-contract targets + the `state_metadata_files` array — the schema to extend)
    - theme-engine/.config/theme-engine/lib/contract.sh (contract_files/contract_format/contract_extract_names/contract_extract_values — the shared reader both tools use; the format extractors expect color-declaration files, which kitty-font.conf/waybar-font.css are NOT)
    - theme-engine/.config/theme-engine/theme-doctor (lines 52-61: the `contract_files` presence loop that checks each $STATE_DIR file exists)
    - theme-engine/.config/theme-engine/theme-parity (lines 103-112, 179-310: consumes ONLY contract_files + the per-file extractors; would FAIL on waybar-font.css because it has no @define-color / no color values — hence font files must NOT go in the `files` array)
    - theme-engine/.config/theme-engine/lib/font.sh (confirms both fragments are rendered into $STATE_DIR and committed, so a presence check will pass on a rendered state dir)
  </read_first>
  <action>
    Add a NEW top-level array `presence_only_files` to contract.json listing the two theme-invariant font render targets: `kitty-font.conf` and `waybar-font.css`. Do NOT add them to the existing `files` array — that array is consumed by theme-parity's name-set/semantic-value parity, which requires color-declaration content these files do not have (waybar-font.css is a `* {}` rule with no @define-color; kitty-font.conf carries font names, not colors). Keeping them out of `files` leaves theme-parity's currently-green run unchanged.

    In contract.sh, add a small helper `contract_presence_only_files()` that emits `jq -r '(.presence_only_files // [])[]' "$CONTRACT_JSON"` (mirroring contract_files's shape, tolerant of the key being absent). Place it directly after contract_files() and give it a one-line comment noting these are theme-invariant fragments that get presence checks but are excluded from color parity.

    In theme-doctor, after the existing `contract_files` presence loop (the `while IFS= read -r f ... [[ -f "$STATE_DIR/$f" ]]` block ending ~line 61), add a second presence loop over `contract_presence_only_files` doing the same `[[ -f "$STATE_DIR/$f" ]]` + `check "$STATE_DIR/$f exists"` for each returned name. Do not modify theme-parity at all.
  </action>
  <acceptance_criteria>
    - `jq -r '.presence_only_files[]' theme-engine/.config/theme-engine/contract.json` outputs exactly `kitty-font.conf` and `waybar-font.css`.
    - `contract_files` still emits 17 names (font files are NOT in the `files` array).
    - contract.sh defines `contract_presence_only_files` and passes `bash -n`.
    - theme-doctor passes `bash -n` and its presence loop now emits a check line for both `kitty-font.conf` and `waybar-font.css`.
    - A headless theme-parity run still reports `0 failed` (no regression from adding the presence-only mechanism).
  </acceptance_criteria>
  <verify>
    <automated>test "$(jq -r '.presence_only_files | join(",")' theme-engine/.config/theme-engine/contract.json)" = "kitty-font.conf,waybar-font.css" && test "$(jq -r '.files | length' theme-engine/.config/theme-engine/contract.json)" -eq 17 && bash -n theme-engine/.config/theme-engine/lib/contract.sh && grep -q 'contract_presence_only_files' theme-engine/.config/theme-engine/lib/contract.sh && bash -n theme-engine/.config/theme-engine/theme-doctor && grep -q 'contract_presence_only_files' theme-engine/.config/theme-engine/theme-doctor && env -u WAYLAND_DISPLAY -u DBUS_SESSION_BUS_ADDRESS bash theme-engine/.config/theme-engine/theme-parity 2>&1 | grep -qE 'Summary: [0-9]+ passed, 0 failed'</automated>
  </verify>
  <done>theme-doctor presence-checks kitty-font.conf and waybar-font.css so a broken font render is caught by the health gate, while theme-parity's color-contract run is unaffected (still 0 failed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keybind exec -> wrapper -> kitty -> fzf picker | User selects an enumerated icon-theme/font name; the picker re-validates it before any state write (existing Security Domain V5) |
| contract.json / contract.sh -> theme-doctor | Repo-controlled config drives a filesystem existence check only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10-01 | Tampering | icon-theme-switch.sh / font-switch.sh (kitty exec) | low | accept | Wrapper passes only a fixed literal script path + window class to `uwsm app -- kitty`; no user-controlled interpolation, mirrors the audited wallpaper-switch.sh precedent |
| T-06-10-02 | Injection | fzf selection -> gtk.sh/font.sh state write | low | mitigate | Picker scripts already re-validate the selection against actually-installed themes/fonts before writing state; this plan does not alter that path |
| T-06-10-03 | Tampering | contract.json presence_only_files -> theme-doctor | low | accept | List drives only `[[ -f ... ]]` existence checks; no code execution or path interpolation beyond $STATE_DIR |
| T-06-10-SC | Tampering | package installs | low | accept | No npm/pip/cargo/AUR/pacman installs are introduced by this plan |
</threat_model>

<verification>
- Both wrappers exist, are executable, syntactically valid, and target their respective pickers.
- Super+Shift+Z/X binds point at the wrappers; matching float windowrules exist for both new kitty classes.
- All three waybar stylesheets have zero `font-family` occurrences and retain the waybar-font.css @import.
- contract.json carries a `presence_only_files` array; theme-doctor presence-checks both font fragments; theme-parity still reports 0 failed.
</verification>

<success_criteria>
- CR-01 closed: waybar font switching is effective (waybar-font.css is sole owner).
- CR-02 closed: icon-theme and font pickers are reachable via their keybinds inside a floating kitty.
- WR-07 closed: theme-doctor covers the two font render targets with zero theme-parity regression.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

- `hypr/.config/hypr/scripts/icon-theme-switch.sh` — NEW floating-kitty launcher wrapper (kitty class `icon-theme-picker`) running icon-theme-picker.sh
- `hypr/.config/hypr/scripts/font-switch.sh` — NEW floating-kitty launcher wrapper (kitty class `font-switcher`) running font-switcher.sh
- windowrule classes: `icon-theme-picker`, `font-switcher` (new float rules in windowrules.conf)
- keybind retargets: Super+Shift+Z -> icon-theme-switch.sh, Super+Shift+X -> font-switch.sh
- `contract.json` new key: `presence_only_files: [kitty-font.conf, waybar-font.css]`
- `contract.sh` new function: `contract_presence_only_files()`
- theme-doctor: new presence-check loop over the presence-only font fragments
- waybar-font.css is now the sole `* { font-family }` owner for waybar (3 local literals removed)
</artifacts_produced>

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-10-SUMMARY.md` when done
</output>
