---
phase: 06-themed-surfaces-utility-suite
plan: 06
type: execute
wave: 3
depends_on: ['06-01', '06-02']
files_modified:
  - swayosd/.config/swayosd/style.css
  - theme-engine/.config/theme-engine/lib/reload.sh
  - hypr/.config/hypr/config/keybinds.conf
autonomous: true
requirements: [OSD-01, THM-05]
user_setup: []

must_haves:
  truths:
    - "Volume, mic-mute, and caps-lock changes show a themed bottom-center rounded-pill SwayOSD indicator (D-24), colors live from the pipeline via swayosd.css (rendered in 06-02)"
    - "Media keys (XF86AudioRaiseVolume/Lower/Mute/MicMute) route through swayosd-client so the OSD shows on every change; caps-lock OSD works keylessly via the libinput backend service (enabled in 06-01) (D-23)"
    - "reload.sh restarts/reloads SwayOSD on theme switch, headless-guarded (container gate stays green)"
    - "On theme switch, if Zen is running, reload.sh notifies 'Restart Zen to apply theme' and NEVER kills the browser (D-28)"
    - "reload.sh lazily self-heals the Zen userChrome link: resolves the default profile from installs.ini then profiles.ini, validates the path is a real subdir of ~/.zen, symlinks userChrome.css to the rendered zen-userchrome.css, and writes the defensive user.js pref — skipping gracefully (logged, non-fatal) when ~/.zen is absent (D-26)"
    - "ddcutil DDC brightness is included only if a DDC-capable monitor is detected, else descoped with evidence (D-25)"
  artifacts:
    - swayosd/.config/swayosd/style.css (@import swayosd.css + bottom-center pill structure)
    - theme-engine/.config/theme-engine/lib/reload.sh (swayosd reload + zen self-heal/notify blocks)
    - hypr/.config/hypr/config/keybinds.conf (media keys via swayosd-client)
  key_links:
    - "swayosd/style.css @import ../../.local/state/theme/swayosd.css -> live palette"
    - "keybinds XF86Audio* -> swayosd-client (volume/mute) + swayosd-libinput-backend.service -> caps-lock OSD"
    - "reload.sh -> Zen profile resolution (installs.ini/profiles.ini) -> symlink userChrome.css to zen-userchrome.css + user.js pref"
  prohibitions:
    - statement: "reload.sh must NEVER kill or restart the Zen browser process — Zen theming is notify-only (D-28), matching the accepted GTK3 stale-until-closed posture"
      status: resolved
      verification: "the zen block in reload.sh contains no pkill/killall/kill targeting zen; only notify-send + filesystem symlink/user.js work"
    - statement: "Every new reload.sh fan-out addition (swayosd restart, zen self-heal/notify, ddcutil) must sit inside the existing headless guard so the container verify gate stays green with no Wayland/D-Bus session (D-29)"
      status: resolved
      verification: "new reload blocks are reached only after the WAYLAND_DISPLAY/DBUS_SESSION_BUS_ADDRESS early-return guard, each pgrep/pgrep-x gated"
---

<objective>
Wire two new pipeline-themed surfaces that both integrate through reload.sh: SwayOSD (OSD-01 — a bottom-center rounded-pill volume/mute/caps-lock indicator bound to the media keys, D-23/D-24) and Zen browser chrome (THM-05 — lazy profile self-heal + userChrome symlink + notify-only reload, D-26/D-27/D-28/D-29). Both consume templates rendered in 06-02. This plan is the single owner of the reload.sh fan-out extension for the phase, matching reload.sh's "single reload owner" design.

Purpose: SwayOSD needs a stow-package style.css that @imports its rendered colors + a reload hook + media-key wiring. Zen needs the genuinely-new Firefox-family profile-resolution self-heal (no analog in the repo) plus a notify-only reload. Grouping them keeps reload.sh edited once.
Output: swayosd stow style.css, reload.sh swayosd + zen blocks, media-key binds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md
@.planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md
@.planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md
@theme-engine/.config/theme-engine/lib/reload.sh
@swaync/.config/swaync/style.css
@hypr/.config/hypr/config/keybinds.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: SwayOSD stow style.css + media-key wiring + reload hook (OSD-01, D-23/D-24/D-25)</name>
  <files>swayosd/.config/swayosd/style.css, hypr/.config/hypr/config/keybinds.conf</files>
  <read_first>
    - swaync/.config/swaync/style.css (the @import-from-state-dir pattern to mirror for swayosd/style.css — first line imports the rendered colors)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Spacing SwayOSD table: bottom-center anchor, 64px margin, 16px container, 6px progress track kept, 999px pill radius; Color SwayOSD 60/30/10 — background 0.85 alpha pill fill, surface_variant 0.6 progress track, primary progress fill, on_background icon+label)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Pattern 6 ddcutil→swayosd custom-progress brightness wrapper + its D-25 descope trigger; swayosd binaries swayosd-server/swayosd-client/swayosd-libinput-backend confirmed)
    - hypr/.config/hypr/config/keybinds.conf (the Audio controls bindel block lines 112-116 currently calling wpctl directly — reroute through swayosd-client so the OSD shows)
  </read_first>
  <action>
    Create swayosd/.config/swayosd/style.css: first line @import url("../../.local/state/theme/swayosd.css") (the rendered palette from 06-02), then the structural pill CSS per the UI-SPEC (bottom-center, 999px pill radius, 16px container, 6px progress track, background 0.85-alpha fill, surface_variant progress track, primary progress fill, on_background icon/label) referencing the @define-color names from swayosd.css. In keybinds.conf, reroute the Audio controls: XF86AudioRaiseVolume/LowerVolume/Mute → swayosd-client output-volume raise/lower + output-volume mute-toggle; XF86AudioMicMute → swayosd-client input-volume mute-toggle (D-23 — both mute and mic-mute route through swayosd-client; caps-lock is handled keylessly by the libinput backend service enabled in 06-01). For monitor brightness (D-25): probe with `ddcutil detect`; ONLY if a DDC-capable monitor is found, wire the XF86MonBrightness keys through a ddcutil→swayosd-client --custom-progress wrapper (Pattern 6); if `ddcutil detect` finds nothing or errors, DESCOPE brightness-via-DDC entirely (leave the existing brightnessctl binds) and record the descope evidence in the SUMMARY — this is a pre-authorized D-25 fallback, not a gap. Add a headless-guarded swayosd reload block to reload.sh in Task 2. Cite D-23/D-24/D-25.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && [ -f swayosd/.config/swayosd/style.css ] && grep -q '@import url("../../.local/state/theme/swayosd.css")' swayosd/.config/swayosd/style.css && grep -q 'swayosd-client' hypr/.config/hypr/config/keybinds.conf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - swayosd/.config/swayosd/style.css exists and @imports the swayosd.css state-dir target on line 1
    - style.css has bottom-center pill structure referencing swayosd.css @define-color names
    - keybinds.conf XF86Audio Raise/Lower/Mute/MicMute route through swayosd-client
    - Brightness-via-DDC either wired (DDC monitor present) or descoped with evidence noted (D-25)
  </acceptance_criteria>
  <done>Themed bottom-center OSD pill bound to media keys; caps-lock keyless via the backend service; brightness conditional per D-25.</done>
</task>

<task type="auto">
  <name>Task 2: reload.sh swayosd reload + Zen self-heal/notify blocks (THM-05, D-26/D-27/D-28/D-29)</name>
  <files>theme-engine/.config/theme-engine/lib/reload.sh</files>
  <read_first>
    - theme-engine/.config/theme-engine/lib/reload.sh (the headless guard lines 15-36 that MUST wrap every new block; the existing guarded fan-out lines 38-49 pkill -SIGUSR2/pgrep -x + timeout idiom to copy; theme_engine_reload_walker/vscodium show how helper functions are defined inside this file — add theme_engine_reload_zen the same way, called from theme_engine_reload after the guard)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Pitfall 4 Zen userChrome activation: write toolkit.legacyUserProfileCustomizations.stylesheets defensively via a profile-root user.js; Pitfall 5 profiles.ini vs installs.ini: read installs.ini first, fall back to profiles.ini Default=; Security Domain: validate the resolved path is a real subdir of ~/.zen before symlinking)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Copywriting: Zen theme-rendered-browser-running notification "Restart Zen to apply the new theme"; Zen profile-not-resolvable = silent, logged non-fatally)
    - .planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md (reload.sh fan-out extension: swayosd guarded restart of swayosd-libinput-backend.service / CSS loads on next trigger; zen pgrep -x zen-bin + notify-only, never kill)
  </read_first>
  <action>
    Extend theme-engine/.config/theme-engine/lib/reload.sh, all additions INSIDE the existing headless early-return guard (D-29 container gate). (1) Add a guarded swayosd reload block: if swayosd-server is running (pgrep -x), restart the libinput backend service (systemctl --user restart swayosd-libinput-backend.service, timeout-bounded) — swayosd re-reads style.css on next OSD trigger, so no explicit style reload call is needed (verify against installed swayosd behavior). (2) Add a theme_engine_reload_zen helper function (defined in reload.sh like theme_engine_reload_walker) and call it: resolve the default Zen profile by reading ~/.zen/installs.ini first (take the single install-hash section's Default= unconditionally if exactly one exists), falling back to ~/.zen/profiles.ini Default=/Default=1 (Pitfall 5); if ~/.zen is absent, log non-fatally and return 0 silently (D-26 — no false-alarm toast before Zen has ever launched). Validate the resolved profile path is a real existing subdirectory of ~/.zen before use (Security Domain). Then: ensure the profile's chrome/ dir exists, symlink chrome/userChrome.css → ~/.local/state/theme/zen-userchrome.css (idempotent ln -sf, first-time auto-wire per D-26), and write/ensure a profile-root user.js containing the legacy stylesheets pref defensively (Pitfall 4 — harmless on versions that ignore it). If a Zen process is running (pgrep -x zen-bin), notify "Restart Zen to apply the new theme" (D-28) — NEVER kill it. Chrome-colors-only scope (D-27) is already baked into the 06-02 template; this task only wires the symlink + reload. Cite D-26/D-27/D-28/D-29.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && bash -n theme-engine/.config/theme-engine/lib/reload.sh && grep -q 'swayosd' theme-engine/.config/theme-engine/lib/reload.sh && grep -q 'theme_engine_reload_zen' theme-engine/.config/theme-engine/lib/reload.sh && grep -q 'installs.ini' theme-engine/.config/theme-engine/lib/reload.sh && grep -q 'zen-userchrome.css' theme-engine/.config/theme-engine/lib/reload.sh && ! grep -nE '(pkill|killall|kill)[^a-z].*zen' theme-engine/.config/theme-engine/lib/reload.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bash -n reload.sh exits 0
    - reload.sh contains a guarded swayosd reload block (pgrep -x gated, timeout-bounded)
    - reload.sh contains theme_engine_reload_zen resolving installs.ini then profiles.ini, validating the path under ~/.zen, symlinking userChrome.css to zen-userchrome.css, writing the user.js pref, and notifying (not killing) when Zen runs
    - No pkill/killall/kill of any zen process anywhere in reload.sh
    - All new blocks sit after the headless early-return guard
  </acceptance_criteria>
  <done>SwayOSD reloads on switch (headless-safe); Zen self-heals its userChrome link + notifies, never killed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify container gate stays green + parity unaffected (D-29)</name>
  <files>verify/container-run.sh (read), theme-engine/.config/theme-engine/theme-parity (executed)</files>
  <read_first>
    - verify/container-run.sh (the theme-parity headless gate step lines ~204-212 — confirm the new reload blocks never run headless so the gate stays green)
    - theme-engine/.config/theme-engine/theme-parity (D-29: parity validates the rendered state-dir CSS only, including swayosd.css + zen-userchrome.css from 06-02 — no zen symlink involved)
  </read_first>
  <action>
    Run theme-parity headless (no WAYLAND_DISPLAY) and confirm it still passes for all 17 targets including swayosd.css + zen-userchrome.css, proving the reload additions (swayosd restart, zen self-heal) are never invoked without a session (D-29 — the headless guard early-returns). Confirm swayosd/style.css is a pure @import + structural CSS (no runtime palette read). This closes the container-gate-stays-green requirement.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && env -u WAYLAND_DISPLAY -u DBUS_SESSION_BUS_ADDRESS theme-engine/.config/theme-engine/theme-parity >/dev/null 2>&1 && echo PARITY_HEADLESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - theme-parity exits 0 headless (no WAYLAND_DISPLAY/DBUS) with all 17 targets green
    - swayosd.css + zen-userchrome.css render correctly for every palette (from 06-02)
  </acceptance_criteria>
  <done>Container gate green; both new surfaces are contract-parity-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.zen/installs.ini,profiles.ini → filesystem path | parsed Path= value used to construct a symlink target |
| media keys → swayosd-client | hardware key events trigger OSD/volume changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering | Zen profile-path resolution reading installs.ini/profiles.ini | medium | mitigate | Validate the resolved profile path exists and is a real subdirectory of ~/.zen before symlinking (Security Domain V5); single-user desktop, low realistic risk but path is validated not blindly readlinked |
| T-06-11 | Denial of Service | reload fan-out hanging with no session | medium | mitigate | All new blocks sit inside the existing headless guard + pgrep -x + timeout bounds (the INST-03 hang class already fixed once); container gate proves headless green |
</threat_model>

<verification>
- reload.sh bash -n clean; swayosd + theme_engine_reload_zen blocks present, headless-guarded; zero zen kill calls
- swayosd/style.css @imports swayosd.css; media keys route via swayosd-client
- theme-parity green headless for 17 targets (swayosd.css + zen-userchrome.css included)
- Live-session manual: volume/mute/caps-lock show the pill; theme switch notifies to restart Zen and re-themes its chrome after restart, under light + dark
</verification>

<success_criteria>
- Themed SwayOSD pill bound to media keys, keyless caps-lock (OSD-01)
- Zen re-themes on switch via userChrome self-heal, notify-only reload, contract-parity target (THM-05, D-29)
</success_criteria>

## Artifacts this phase produces

- `swayosd/.config/swayosd/style.css` (new stow-package config @importing swayosd.css)
- `theme-engine/.config/theme-engine/lib/reload.sh` additions: guarded swayosd reload block, `theme_engine_reload_zen` function
- keybinds: XF86Audio Raise/Lower/Mute/MicMute rerouted through swayosd-client; optional ddcutil brightness wrapper
- Zen runtime artifacts (created at reload time, not in repo): `~/.zen/<profile>/chrome/userChrome.css` symlink, `~/.zen/<profile>/user.js`

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-06-SUMMARY.md` when done.
</output>
