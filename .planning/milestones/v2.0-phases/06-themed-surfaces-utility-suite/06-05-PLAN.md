---
phase: 06-themed-surfaces-utility-suite
plan: 05
type: execute
wave: 2
depends_on: ['06-01', '06-02']
files_modified:
  - hypr/.config/hypr/scripts/capture-region.sh
  - hypr/.config/hypr/scripts/capture-window.sh
  - hypr/.config/hypr/scripts/capture-full.sh
  - hypr/.config/hypr/scripts/record-toggle.sh
  - hypr/.config/hypr/scripts/gif-export.sh
  - hypr/.config/hypr/config/keybinds.conf
  - hypr/.config/hypr/scripts/screenshot.sh
autonomous: true
requirements: [SHOT-01, SHOT-02, SHOT-03]
user_setup: []

must_haves:
  truths:
    - "Print captures a region, Shift+Print a window, Ctrl+Print full-screen — each freezes the screen (hyprshot -z), pipes into satty, and satty owns save+copy+notify (D-01/D-02/D-05)"
    - "Enter (or Ctrl+C) in satty copies to clipboard + saves to ~/Pictures/Screenshots + exits; default pre-selected tool is arrow (D-02)"
    - "Alt+Print toggles recording: first press opens a pipeline-themed walker --dmenu audio picker (silent / desktop / desktop+mic), selects a region/monitor, and records via gpu-screen-recorder with NVENC; second press stops via SIGINT (D-03/D-05/D-06/D-07)"
    - "The recording-saved notification offers Open / Export GIF; Export GIF runs an ffmpeg palette-pass next to the .mp4 (D-04)"
    - "Recordings save to ~/Videos, screenshots to ~/Pictures/Screenshots, timestamped (D-08)"
    - "The old bare screenshot.sh is deleted and its Super+X/Z/Shift+Print binds removed (D-05 frees Super+X/Z)"
    - "record-toggle.sh exposes a pgrep-able status probe for the future Phase 8 waybar indicator"
  artifacts:
    - hypr/.config/hypr/scripts/capture-region.sh, capture-window.sh, capture-full.sh
    - hypr/.config/hypr/scripts/record-toggle.sh
    - hypr/.config/hypr/scripts/gif-export.sh
    - hypr/.config/hypr/config/keybinds.conf (Print family binds; old screenshot binds removed)
  key_links:
    - "keybinds Print/Shift+Print/Ctrl+Print -> capture-{region,window,full}.sh -> hyprshot -z --raw | satty (reads ~/.config/satty/config.toml symlink from 06-02)"
    - "keybinds Alt+Print -> record-toggle.sh -> walker --dmenu audio picker -> gpu-screen-recorder; SIGINT stop -> notification Open/Export GIF -> gif-export.sh"
  prohibitions:
    - statement: "No audio may be captured without explicit selection — the record-start audio picker (silent/desktop/desktop+mic) runs BEFORE region select every time; silent omits the -a flag entirely (D-06)"
      status: resolved
      verification: "record-toggle.sh routes through the walker --dmenu audio picker before invoking gpu-screen-recorder; the silent branch passes no -a argument"
    - statement: "Raw subprocess stderr (hyprshot/satty/gpu-screen-recorder/ffmpeg) must NOT be piped unsanitized into notify-send — reuse the head -c 200 truncation + control-char strip pattern (RESEARCH Security Domain)"
      status: resolved
      verification: "any notify-send surfacing subprocess error output truncates/sanitizes first"
---

<objective>
Ship the full screenshot suite (SHOT-01/02/03): freeze-capture region/window/full via hyprshot piped into satty for annotate+save+copy+notify (D-01/D-02), screen/region recording via gpu-screen-recorder with an explicit audio picker and NVENC hardware encode (D-03/D-06), and GIF export as a notification action (D-04) — all on the Omarchy-style Print-key family (D-05), replacing the old bare screenshot.sh. Consumes the satty config symlinked by plan 06-02 and the packages declared in 06-01. Owns the keybinds.conf Print-family edits for Wave 2.

Purpose: The repo currently has a bare grim/slurp screenshot.sh with no annotation, freeze, or recording. This delivers the modern Omarchy capture stack.
Output: 3 capture scripts (or one mode-parametrized script), record-toggle.sh, gif-export.sh, Print-family keybinds, screenshot.sh deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md
@.planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md
@.planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md
@hypr/.config/hypr/scripts/screenshot.sh
@hypr/.config/hypr/scripts/theme-switch.sh
@hypr/.config/hypr/config/keybinds.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture scripts (hyprshot -z into satty) + Print-family keybinds (SHOT-01/02, D-01/D-02/D-05)</name>
  <files>hypr/.config/hypr/scripts/capture-region.sh, hypr/.config/hypr/scripts/capture-window.sh, hypr/.config/hypr/scripts/capture-full.sh, hypr/.config/hypr/config/keybinds.conf</files>
  <read_first>
    - hypr/.config/hypr/scripts/screenshot.sh (the current bare grim/slurp wrapper being replaced — note its notify wording/icon so the surface swap stays invisible to the user)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Pattern 4 hyprshot region/window/full into satty; Assumption A1/A2 + Open Question 3 — verify exact hyprshot -m/-z/-r flags and satty early-exit/action-on-enter key spelling against installed `hyprshot --help` / `satty --help` FIRST)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Copywriting: screenshot-captured notification identical wording/icon/timeout to the old screenshot.sh; satty default tool = arrow, palette themed via 06-02 satty.toml)
    - hypr/.config/hypr/config/keybinds.conf (the Screenshots section lines 47-50 to replace; Print binds go here, existing Super+X/Z screenshot binds removed)
  </read_first>
  <action>
    First verify the installed CLI: run `hyprshot --help` and `satty --help` and use the ACTUAL flag names (not the RESEARCH WebSearch guesses) for freeze, raw stdout, mode region|window|output, and satty's copy+save+exit-on-Enter behavior. Create the capture scripts — either three thin wrappers (capture-region.sh, capture-window.sh, capture-full.sh) or one mode-parametrized script mirroring the old screenshot.sh single-arg pattern; the frontmatter lists three files, collapse to one only if you also update the keybinds to pass a mode arg. Each: set -euo pipefail, a timestamp, then pipe hyprshot in freeze+raw mode for the chosen capture mode into satty reading stdin, with satty writing a timestamped PNG under ~/Pictures/Screenshots and copying+saving+exiting on Enter. satty owns save+copy+notify (D-02); if you must add a notify, reuse the old screenshot.sh wording/icon (camera-photo) so the swap is invisible, sanitizing any error output first (head -c 200, strip control chars). Default tool arrow + the annotation palette come from the satty.toml config (06-02). In keybinds.conf, replace the Screenshots section: remove the three old binds (Super+X full, Super+Z area, Super+Shift+Print window — freeing Super+X/Z per D-05), and add Print=region, Shift+Print=window, Ctrl+Print=full (D-05). Ensure ~/Pictures/Screenshots exists (mkdir -p in the script). Cite D-01/D-02/D-05.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && for f in capture-region capture-window capture-full; do [ -f hypr/.config/hypr/scripts/$f.sh ] && bash -n hypr/.config/hypr/scripts/$f.sh; done; grep -q 'hyprshot' hypr/.config/hypr/scripts/capture-region.sh && grep -q 'satty' hypr/.config/hypr/scripts/capture-region.sh && grep -Eq ', Print,' hypr/.config/hypr/config/keybinds.conf && grep -q 'SHIFT, Print' hypr/.config/hypr/config/keybinds.conf && grep -q 'CTRL, Print' hypr/.config/hypr/config/keybinds.conf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Capture script(s) exist, bash -n clean, pipe hyprshot freeze-capture into satty
    - keybinds.conf binds Print region, Shift+Print window, Ctrl+Print full
    - The old Super+X and Super+Z screenshot binds are gone from keybinds.conf
    - satty save+copy+exit flag names verified against satty --help (recorded in a script comment)
  </acceptance_criteria>
  <done>Freeze-capture region/window/full into satty on the Print family; Super+X/Z freed.</done>
</task>

<task type="auto">
  <name>Task 2: record-toggle.sh + gif-export.sh + Alt+Print bind (SHOT-03, D-03/D-04/D-06/D-07/D-08)</name>
  <files>hypr/.config/hypr/scripts/record-toggle.sh, hypr/.config/hypr/scripts/gif-export.sh, hypr/.config/hypr/config/keybinds.conf</files>
  <read_first>
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Pattern 5 Omarchy omarchy-capture-screenrecording structure — slurp monitor/region picker, gpu-screen-recorder NVENC invocation with cfr + cpu fallback, SIGINT stop with a bounded 5s poll then SIGKILL, audio_args: silent omits -a, desktop = default_output, desktop+mic adds default_input; ffmpeg two-pass palettegen/paletteuse GIF)
    - hypr/.config/hypr/scripts/theme-switch.sh (the walker --dmenu exit-code-130 cancel pattern lines 46-59 to reuse verbatim for the audio picker, D-06)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Copywriting: audio picker header, the three exact option labels, Recording Started/Saved notifications with media-record icon, Recording Saved Open + Export GIF actions, GIF Exported notification)
    - hypr/.config/hypr/config/keybinds.conf (add Alt+Print record toggle alongside the Print family from Task 1)
  </read_first>
  <action>
    Create hypr/.config/hypr/scripts/record-toggle.sh: if a gpu-screen-recorder process is running, stop it with SIGINT (bounded 5s poll then SIGKILL, mirroring reload.sh's bounded-poll idiom) and fire the Recording Saved notification carrying Open + Export GIF actions (notify-send action flags; the Export GIF action calls gif-export.sh with the just-saved .mp4) per D-04/D-07. If not running: open the walker --dmenu audio picker (three options silent/desktop/desktop+mic, exact labels + exit-130 cancel reused from theme-switch.sh; NEVER capture audio without explicit selection — silent omits the audio flag entirely, D-06), then a slurp region/monitor selection, then launch gpu-screen-recorder with NVENC hardware encode and cpu fallback writing a timestamped file into ~/Videos (D-08), then fire Recording Started (D-07). Expose a status probe (the pgrep -f gpu-screen-recorder pattern documented in a comment) so Phase 8's waybar indicator can read recording state. Create hypr/.config/hypr/scripts/gif-export.sh: a two-pass ffmpeg palettegen/paletteuse conversion of the given .mp4 to a .gif beside it, then the GIF Exported notification (exact ffmpeg palette flags are Claude's discretion; sanitize any ffmpeg error before notify-send). Add the Alt+Print record-toggle bind to keybinds.conf. Cite D-03/D-04/D-06/D-07/D-08.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && [ -f hypr/.config/hypr/scripts/record-toggle.sh ] && bash -n hypr/.config/hypr/scripts/record-toggle.sh && [ -f hypr/.config/hypr/scripts/gif-export.sh ] && bash -n hypr/.config/hypr/scripts/gif-export.sh && grep -q 'gpu-screen-recorder' hypr/.config/hypr/scripts/record-toggle.sh && grep -q 'walker --dmenu' hypr/.config/hypr/scripts/record-toggle.sh && grep -q 'SIGINT' hypr/.config/hypr/scripts/record-toggle.sh && grep -q 'palettegen' hypr/.config/hypr/scripts/gif-export.sh && grep -q 'ALT, Print' hypr/.config/hypr/config/keybinds.conf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - record-toggle.sh + gif-export.sh exist and are bash -n clean
    - record-toggle.sh invokes gpu-screen-recorder, routes through walker --dmenu audio picker before recording, stops via SIGINT with a bounded poll
    - The silent audio branch passes no audio-capture flag
    - gif-export.sh uses a two-pass palettegen/paletteuse ffmpeg conversion
    - keybinds.conf binds Alt+Print to record-toggle.sh
  </acceptance_criteria>
  <done>Toggle recording with explicit audio selection + NVENC; GIF export as a notification action.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the old screenshot.sh and confirm no dangling references (D-05)</name>
  <files>hypr/.config/hypr/scripts/screenshot.sh (removed)</files>
  <read_first>
    - hypr/.config/hypr/scripts/screenshot.sh (the file being removed)
    - hypr/.config/hypr/config/keybinds.conf (confirm the old screenshot binds were removed in Task 1; grep for any remaining reference to screenshot.sh)
    - hypr/.config/hypr/config/autostart.conf (confirm screenshot.sh is not referenced elsewhere)
  </read_first>
  <action>
    Remove hypr/.config/hypr/scripts/screenshot.sh via `git rm` (D-05/RESEARCH State of the Art: the bare grim/slurp screenshot.sh is fully replaced, do not keep it as a fallback). Grep the hypr package to confirm no remaining reference to screenshot.sh in any keybind or autostart line. This closes the surface swap cleanly.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && [ ! -f hypr/.config/hypr/scripts/screenshot.sh ] && ! grep -rq 'screenshot.sh' hypr/.config/hypr/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - hypr/.config/hypr/scripts/screenshot.sh no longer exists
    - No file under hypr/.config/hypr/ references screenshot.sh
  </acceptance_criteria>
  <done>Old screenshot script and its references gone; capture suite is the sole path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| walker/slurp selection → recorder args | user picker output influences audio/region flags |
| subprocess stderr → notify-send | hyprshot/satty/gsr/ffmpeg error text surfaced to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Information Disclosure | recording audio captured without consent | medium | mitigate | Explicit audio picker (silent/desktop/desktop+mic) runs before every record; silent omits the audio flag entirely (D-06) — no implicit mic capture |
| T-06-09 | Tampering | unsanitized subprocess stderr → notify-send | low | mitigate | Reuse the existing head -c 200 + control-char strip truncation before any notify-send surfacing subprocess error output (RESEARCH Security Domain) |
</threat_model>

<verification>
- All capture/record/gif scripts bash -n clean; hyprshot→satty and gpu-screen-recorder wiring grep-present
- Print/Shift+Print/Ctrl+Print/Alt+Print binds present; old Super+X/Z screenshot binds removed; screenshot.sh deleted
- Live-session manual: Print annotates+saves+copies; Alt+Print records with a chosen audio mode and stops; the saved notification's Export GIF produces a .gif
</verification>

<success_criteria>
- Region/window/full freeze-capture with annotation, save+copy, notification (SHOT-01/02)
- Region/screen recording with explicit audio + NVENC, GIF export as a notification action (SHOT-03)
- Omarchy Print-key family; Super+X/Z freed for utilities
</success_criteria>

## Artifacts this phase produces

- `hypr/.config/hypr/scripts/capture-region.sh`, `capture-window.sh`, `capture-full.sh` (or one mode-parametrized capture script)
- `hypr/.config/hypr/scripts/record-toggle.sh` (+ pgrep status probe convention)
- `hypr/.config/hypr/scripts/gif-export.sh`
- keybinds: Print (region), Shift+Print (window), Ctrl+Print (full), Alt+Print (record toggle)
- Removal: `hypr/.config/hypr/scripts/screenshot.sh` and its old Super+X/Z/Shift+Print binds

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-05-SUMMARY.md` when done.
</output>
