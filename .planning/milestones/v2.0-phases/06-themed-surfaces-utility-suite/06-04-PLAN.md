---
phase: 06-themed-surfaces-utility-suite
plan: 04
type: execute
wave: 2
depends_on: ['06-02']
files_modified:
  - hypr/.config/hypr/hyprlock.conf
autonomous: false
requirements: [LOCK-01]
user_setup: []

must_haves:
  truths:
    - "hyprlock sources ~/.local/state/theme/hyprlock.conf (its own dedicated render target) instead of piggybacking on hyprland.conf (D-30)"
    - "The lock screen shows a themed-initial avatar (styled circle with the user's initial in palette colors, no photo asset, no ~/.face dependency) (D-11/D-12)"
    - "Now-playing (playerctl) shows when media plays and is hidden when nothing plays (D-11)"
    - "Battery + caps-lock indicators and a failed-attempts counter are visible (D-11)"
    - "The FIX-02 hardening (immediate_render, ignore_empty_input, check_text, fail_text) is preserved verbatim (D-14 safety, Phase 4 carry-forward)"
    - "Background stays the blurred current wallpaper ($image) with blur/dim re-tuned for the busier layout (D-13)"
  artifacts:
    - hypr/.config/hypr/hyprlock.conf (source swap + new indicator labels + preserved FIX-02 block)
  key_links:
    - "hyprlock.conf source = ~/.local/state/theme/hyprlock.conf -> matugen-rendered lock palette (from plan 06-02)"
    - "cmd[update:N] label -> playerctl metadata (now-playing) / battery / hyprlock fail counter"
  prohibitions:
    - statement: "The general{} block keys immediate_render + ignore_empty_input and the input-field check_text/fail_text/fail_color/capslock_color keys must NOT be removed or altered — they are the Phase 4 FIX-02 hardening (D-14). LOCK-01 is additive visual work only."
      status: resolved
      verification: "hyprlock.conf still contains immediate_render = true, ignore_empty_input = true, check_text, and fail_text = <i>$FAIL</i>"
---

<objective>
Redesign hyprlock into an info-rich lock screen (LOCK-01, D-11): themed-initial avatar, now-playing, battery + caps-lock indicators, and a failed-attempts counter — decoupled from hyprland.conf onto its own dedicated render target (D-30), colors live from the pipeline, background a re-tuned blur of the current wallpaper (D-13). The Phase 4 FIX-02 input-hardening block is preserved verbatim (D-14). All testing follows the documented lockout-recovery procedure (second TTY logged in) — a launch requirement, not a follow-up.

Purpose: hyprlock currently sources hyprland.conf's palette and shows only time/date/greeting/input. D-30 gives it a dedicated hyprlock.conf target (rendered in 06-02) so lock-specific variables live independently, and D-11 makes it maximalist while the avatar stays a themed initial (personal photo explicitly kept out of the repo).
Output: Redesigned hyprlock.conf with new indicator labels, source swap, FIX-02 preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md
@.planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md
@hypr/.config/hypr/hyprlock.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source swap + info-rich indicator labels, preserving FIX-02 (D-11/D-12/D-13/D-30)</name>
  <files>hypr/.config/hypr/hyprlock.conf</files>
  <read_first>
    - hypr/.config/hypr/hyprlock.conf (full file — line 5 source line to swap; the general{} block lines 18-22 and input-field check_text/fail_text/capslock_color lines 104-118 are the FIX-02 hardening that MUST survive verbatim; the existing Time/Date/Greeting label blocks lines 47-84 are the exact shape to copy for new labels)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (hyprlock new-elements position/size table: avatar 0,280 90x90 circle 2px border; battery+capslock row 0,-190; failed-attempts 0,-220; now-playing 0,40 ellipsized; Typography: avatar initial 40px bold, indicator rows 14px; Color hyprlock 60/30/10 — primary reserved for time/input-outline/avatar border-fill, tertiary=check_color, error=fail_color/failed-attempts)
    - .planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md (hyprlock.conf section: copy the existing label block shape monitor/text/color/font_size/position/halign/valign/shadow for each new indicator; now-playing + failed-attempts need cmd[update:N] polling)
  </read_first>
  <action>
    Edit hypr/.config/hypr/hyprlock.conf. (1) Change line 5 `source = ~/.local/state/theme/hyprland.conf` to `source = ~/.local/state/theme/hyprlock.conf` (D-30 decoupling — the dedicated target rendered in 06-02). (2) Re-tune the background{} blur/dim for the busier layout (D-13) — keep path = $image, adjust blur_passes/brightness slightly so foreground text stays legible over the extra elements. (3) Add new label{} blocks copying the existing Time/Date/Greeting block shape: a themed-initial avatar (D-12 — a styled element rendering `$USER`'s first initial at 40px bold in $primary inside a ~90x90 circle at position 0,280; if hyprlock has no native circle-shape, render the initial as a large glyph/letter label with a background/border via the available label styling — no photo, no ~/.face); a now-playing label at 0,40 using `text = cmd[update:2000] playerctl metadata --format "{{artist}} - {{title}}" 2>/dev/null` ellipsized single line (D-11 — naturally empty/hidden when nothing plays); a battery + caps-lock row at 0,-190 (battery via `cmd[update:30000] cat /sys/class/power_supply/BAT*/capacity` style probe, caps-lock via the input-field's existing capslock_color plus a small status label); a failed-attempts counter at 0,-220 in $error. Use $primary/$secondary/$tertiary/$error variables from the sourced hyprlock.conf per the UI-SPEC color reservations. (4) PRESERVE the entire general{} block (immediate_render, ignore_empty_input) and every input-field FIX-02 key (check_text, fail_text = <i>$FAIL</i>, fail_color, capslock_color) exactly as-is (D-14). Exact element positioning/typography beyond the UI-SPEC table is Claude's discretion; variable naming in the hyprlock target follows hyprland-colors.conf convention.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && grep -q 'source = ~/.local/state/theme/hyprlock.conf' hypr/.config/hypr/hyprlock.conf && grep -q 'immediate_render = true' hypr/.config/hypr/hyprlock.conf && grep -q 'ignore_empty_input = true' hypr/.config/hypr/hyprlock.conf && grep -q 'check_text' hypr/.config/hypr/hyprlock.conf && grep -q 'fail_text = <i>$FAIL</i>' hypr/.config/hypr/hyprlock.conf && grep -q 'playerctl' hypr/.config/hypr/hyprlock.conf && ! grep -q 'source = ~/.local/state/theme/hyprland.conf' hypr/.config/hypr/hyprlock.conf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - hyprlock.conf sources hyprlock.conf (not hyprland.conf)
    - immediate_render = true AND ignore_empty_input = true still present
    - check_text present AND fail_text = <i>$FAIL</i> present (FIX-02 preserved verbatim)
    - A playerctl-driven now-playing label exists
    - New avatar/battery/caps-lock/failed-attempts labels exist (grep for the added label blocks / their cmd probes)
    - The old `source = ...hyprland.conf` line no longer exists
  </acceptance_criteria>
  <done>Info-rich lock screen sourcing its dedicated target; FIX-02 intact; avatar is a themed initial.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Lockout-recovery-guarded live lock test (D-14 launch requirement)</name>
  <files>hypr/.config/hypr/hyprlock.conf (verified live, not edited in this task)</files>
  <action>Gate only — no file changes in this task. Perform the lockout-recovery-guarded live lock test per the how-to-verify steps below, confirming the redesign renders correctly and the FIX-02 input behavior still holds under both light and dark themes. Block until a human approves.</action>
  <what-built>The redesigned info-rich hyprlock (avatar, now-playing, battery, caps-lock, failed-attempts, re-tuned blur), sourcing its dedicated hyprlock.conf render target, with the FIX-02 input hardening preserved.</what-built>
  <how-to-verify>
    MANDATORY safety setup FIRST (D-14, Phase 4 lockout-recovery procedure — see .planning/phases/04-reliability-fixes-tech-debt/ 04-02 artifacts):
    1. Log in on a SECOND TTY (Ctrl+Alt+F3) BEFORE locking, so `pkill hyprlock` is available if the lock misbehaves.
    2. Ensure a theme is applied so ~/.local/state/theme/hyprlock.conf exists (run theme-apply if needed).
    3. From the graphical session, lock with Super+L (or `uwsm app -- hyprlock`).
    4. Confirm: the avatar shows your initial in palette colors; time/date/greeting render; battery + caps-lock indicators show; if media is playing, now-playing appears (and is absent otherwise); the background is a re-tuned blur of the current wallpaper.
    5. Confirm FIX-02 still holds: the FIRST keystroke registers (no dropped-input loop); pressing Enter on an empty field does nothing; a wrong password shows the Checking.../fail text and increments the failed-attempts counter.
    6. Test under BOTH a light theme and a dark theme (re-apply and re-lock) — every element must stay legible.
    If anything wedges, switch to the second TTY and `pkill hyprlock`.
  </how-to-verify>
  <resume-signal>Type "approved" once the lock renders correctly and FIX-02 behavior is confirmed under both light and dark, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard input → hyprlock PAM | lock-screen password path (auth boundary, V2) |
| playerctl/battery probe → lock label | subprocess output rendered on the lock surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Spoofing / Denial | hyprlock auth input path | high | mitigate | LOCK-01 is additive UI only; the FIX-02 hardening (ignore_empty_input, check_text, immediate_render) is preserved verbatim — the PAM/input-field auth logic is never touched (RESEARCH Security Domain V2). Lockout-recovery procedure (second TTY) is a mandatory test gate (Task 2). |
| T-06-07 | Information Disclosure | now-playing / battery cmd output on lock surface | low | accept | Labels render only trusted local subprocess output (playerctl metadata, battery capacity); no external/untrusted input reaches the lock surface |
</threat_model>

<verification>
- hyprlock.conf sources hyprlock.conf; FIX-02 keys grep-present verbatim
- playerctl now-playing + battery/capslock/failed-attempts/avatar elements present
- Human-verified live lock under light + dark with the lockout-recovery procedure (Task 2)
</verification>

<success_criteria>
- Modern info-rich lock screen, colors from the shared pipeline, decoupled onto its own target (LOCK-01/D-30)
- FIX-02 hardening intact; lockout-recovery procedure followed during testing (D-14)
</success_criteria>

## Artifacts this phase produces

- `hypr/.config/hypr/hyprlock.conf` source line swap to `~/.local/state/theme/hyprlock.conf`
- New hyprlock label blocks: themed-initial avatar, now-playing (playerctl cmd), battery + caps-lock row, failed-attempts counter

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-04-SUMMARY.md` when done.
</output>
