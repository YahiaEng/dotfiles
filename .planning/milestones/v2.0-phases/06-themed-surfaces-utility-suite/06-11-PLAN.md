---
phase: 06-themed-surfaces-utility-suite
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - hypr/.config/hypr/scripts/record-toggle.sh
  - hypr/.config/hypr/scripts/capture-region.sh
  - hypr/.config/hypr/scripts/capture-window.sh
  - hypr/.config/hypr/scripts/capture-full.sh
  - hypr/.config/hypr/scripts/color-picker.sh
autonomous: true
gap_closure: true
requirements: [SHOT-03, SHOT-01, SHOT-02, UTIL-02]

must_haves:
  truths:
    - "The user can record a drag-selected screen region to video: record-toggle.sh invokes gpu-screen-recorder with `-w region -region WxH+X+Y` (not the geometry string as a bare `-w` value), so region recordings start successfully. Monitor-target recording remains unaffected."
    - "A screenshot capture whose required tool (hyprshot or satty) is missing produces an explicit error notification instead of a silent set -e abort, so 'tool not installed' is distinguishable from 'user cancelled'."
    - "color-picker.sh exits 0 on its own success path (a picked+copied+notified color) even when ImageMagick is unavailable and no swatch was generated."
  artifacts:
    - "hypr/.config/hypr/scripts/record-toggle.sh region branch using `-w region -region`"
    - "hypr/.config/hypr/scripts/capture-{region,window,full}.sh with a command -v guard for hyprshot and satty"
    - "hypr/.config/hypr/scripts/color-picker.sh success-path cleanup as an if-block (no trailing &&-list)"
  key_links:
    - "record-toggle.sh region:* case -> gpu-screen-recorder -w region -region <geom>"
    - "capture-*.sh command -v hyprshot/satty -> notify-send error + exit 1 (missing tool no longer silent)"
---

<objective>
Close the SHOT-03 region-recording blocker (CR-03) and harden the screenshot/utility scripts against silent-failure classes flagged in the review (WR-06 missing-tool guards, WR-05 color-picker exit-status bug). All are narrow, mechanical script fixes; capture/annotate/GIF-export and monitor recording were verified correct and are untouched except for the added guards.

Purpose: Region recording currently fails to start on every drag-selected capture because the geometry string is passed as gpu-screen-recorder's `-w` value, which its CLI does not accept. The capture scripts also mask a missing hyprshot/satty as an identical-looking silent no-op, and color-picker.sh returns exit 1 on success when ImageMagick is absent.
Output: A corrected region capture flag, tool-presence guards on the three capture scripts, and a corrected color-picker success-path exit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-VERIFICATION.md
@.planning/phases/06-themed-surfaces-utility-suite/06-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the region-capture flag shape for gpu-screen-recorder (CR-03)</name>
  <files>hypr/.config/hypr/scripts/record-toggle.sh</files>
  <read_first>
    - hypr/.config/hypr/scripts/record-toggle.sh (lines 168-192: `start_recording()` builds `capture_args` from the `select_capture_target` output; line ~177 is the `region:*` case that passes the geometry as a bare `-w` value; line ~189 is where `capture_args` is expanded into the gpu-screen-recorder invocation)
  </read_first>
  <action>
    In the `case "$target" in` block inside start_recording(), change the `region:*)` branch so it builds `capture_args=(-w region -region "${target#region:}")` instead of `capture_args=(-w "${target#region:}")`. The literal string `region` is gpu-screen-recorder's accepted `-w` value for region capture, and `-region WxH+X+Y` supplies the geometry (the geometry itself is already `${sw}x${sh}+${sx}+${sy}` from select_capture_target). Leave the `monitor:*)` branch exactly as-is (`-w "${target#monitor:}"` is correct for a monitor name). Do not change the audio_args, the output filename logic, or the start-poll loop.
  </action>
  <acceptance_criteria>
    - record-toggle.sh's region branch expands to `capture_args=(-w region -region "${target#region:}")` (positive assertion of the corrected flag shape).
    - The monitor branch is unchanged (`-w "${target#monitor:}"`).
    - record-toggle.sh still passes `bash -n`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n hypr/.config/hypr/scripts/record-toggle.sh && grep -Fq -- '-w region -region "${target#region:}"' hypr/.config/hypr/scripts/record-toggle.sh && grep -Fq -- '-w "${target#monitor:}"' hypr/.config/hypr/scripts/record-toggle.sh</automated>
  </verify>
  <done>Drag-selected region recordings start successfully because gpu-screen-recorder receives `-w region -region <geom>`; SHOT-03's region-recording path is unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: Guard capture scripts against missing tools and fix color-picker success exit (WR-06, WR-05)</name>
  <files>hypr/.config/hypr/scripts/capture-region.sh, hypr/.config/hypr/scripts/capture-window.sh, hypr/.config/hypr/scripts/capture-full.sh, hypr/.config/hypr/scripts/color-picker.sh</files>
  <read_first>
    - hypr/.config/hypr/scripts/capture-region.sh (line 32 `set -euo pipefail`, line 40 the `hyprshot ... | satty ...` pipeline with no tool-presence guard)
    - hypr/.config/hypr/scripts/capture-window.sh (same shape, pipeline line ~18)
    - hypr/.config/hypr/scripts/capture-full.sh (same shape, pipeline line ~21)
    - hypr/.config/hypr/scripts/color-picker.sh (line 16-19 the existing `command -v hyprpicker` guard pattern to reuse; line 57 the trailing `[[ "$ICON" != "color-picker" ]] && rm -f "$ICON"` that returns exit 1 under set -e when no swatch was made)
  </read_first>
  <action>
    In each of the three capture scripts, add a tool-presence guard immediately after `set -euo pipefail` and before the mkdir/hyprshot pipeline: loop over the two required tools `hyprshot` and `satty`, and for any that `command -v` cannot find, fire `notify-send -a "Screenshot" "Error" "<tool> not installed" -i dialog-error -t 6000` (guarded with `2>/dev/null || true`) and `exit 1`. This mirrors color-picker.sh's existing `command -v hyprpicker` guard and keeps a genuine Escape/cancel silent (the guard only fires when a binary is absent, not on user cancel). Do not otherwise change the capture/annotate/notify logic.

    In color-picker.sh, replace the final trailing-AND-list line (`[[ "$ICON" != "color-picker" ]] && rm -f "$ICON"`) with an explicit `if [[ "$ICON" != "color-picker" ]]; then rm -f "$ICON"; fi` block so the script's final exit status is 0 on its own success path regardless of whether a swatch PNG was generated. This is the same set -e / trailing-AND-list fix class already applied in reload.sh.
  </action>
  <acceptance_criteria>
    - capture-region.sh, capture-window.sh, and capture-full.sh each contain a `command -v` presence check that references both `hyprshot` and `satty` and calls `notify-send` + `exit 1` when a tool is missing.
    - All three capture scripts still pass `bash -n` and still contain their `hyprshot ... | satty ...` pipeline.
    - color-picker.sh's swatch cleanup is an `if [[ "$ICON" != "color-picker" ]]; then` block (no trailing `&&`-list on the final line) and the file passes `bash -n`.
  </acceptance_criteria>
  <verify>
    <automated>for f in hypr/.config/hypr/scripts/capture-region.sh hypr/.config/hypr/scripts/capture-window.sh hypr/.config/hypr/scripts/capture-full.sh; do bash -n "$f" && grep -q 'command -v' "$f" && grep -q 'hyprshot' "$f" && grep -q 'satty' "$f" && grep -q 'not installed' "$f" || { echo "FAIL $f"; exit 1; }; done && bash -n hypr/.config/hypr/scripts/color-picker.sh && grep -q 'if \[\[ "\$ICON" != "color-picker" \]\]; then' hypr/.config/hypr/scripts/color-picker.sh</automated>
  </verify>
  <done>A missing hyprshot/satty produces a clear error notification instead of a silent dead keypress; color-picker.sh returns 0 on success even without ImageMagick.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slurp geometry -> gpu-screen-recorder -region | Geometry string is machine-generated numeric (WxH+X+Y), not free-text user input |
| subprocess stderr -> notify-send | Capture guards emit only fixed strings; color-picker already sanitizes stderr |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11-01 | Injection | record-toggle.sh region geometry -> gpu-screen-recorder `-region` | low | mitigate | Geometry is built internally from slurp's numeric output and passed as a single quoted argv element; no shell re-evaluation |
| T-06-11-02 | Denial of Service (availability) | capture-*.sh missing tool | low | mitigate | Added command -v guards convert a silent set -e abort into an explicit notify-send + exit 1 |
| T-06-11-03 | Information disclosure | subprocess stderr -> notify-send | low | accept | color-picker.sh's existing sanitize() (head -c 200 + control-char strip) already gates stderr; capture guards emit only a fixed "not installed" string |
| T-06-11-SC | Tampering | package installs | low | accept | No npm/pip/cargo/AUR/pacman installs are introduced by this plan |
</threat_model>

<verification>
- record-toggle.sh region branch uses `-w region -region`; monitor branch unchanged; script syntactically valid.
- All three capture scripts guard hyprshot + satty presence and notify on absence.
- color-picker.sh success-path exit is an if-block; script syntactically valid.
</verification>

<success_criteria>
- CR-03 closed: region recordings start (correct gpu-screen-recorder flag shape).
- WR-06 closed: missing capture tools surface an explicit error, not a silent no-op.
- WR-05 closed: color-picker.sh exits 0 on its success path.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

- `record-toggle.sh` region capture now uses `-w region -region <geom>` (SHOT-03)
- `capture-region.sh`, `capture-window.sh`, `capture-full.sh` gain a `command -v` guard for hyprshot and satty (notify + exit 1 on missing tool)
- `color-picker.sh` success-path cleanup converted to an `if` block (exit 0 on success without ImageMagick)
</artifacts_produced>

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-11-SUMMARY.md` when done
</output>
