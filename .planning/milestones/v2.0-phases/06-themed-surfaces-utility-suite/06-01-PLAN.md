---
phase: 06-themed-surfaces-utility-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - stow.sh
autonomous: false
requirements: [SHOT-01, SHOT-02, SHOT-03, OSD-01, UTIL-01, UTIL-02, UTIL-04, UTIL-05]
user_setup: []

must_haves:
  truths:
    - "A fresh install.sh run installs hyprshot, satty, gpu-screen-recorder, swayosd, hyprpicker, wtype, ddcutil, papirus-icon-theme, and the 5 new ttf-*-nerd fonts from the official extra repo (not AUR)"
    - "A fresh install.sh run installs tela-icon-theme, colloid-icon-theme-git, and papirus-folders from AUR after a human legitimacy gate"
    - "swayosd-libinput-backend.service is enabled as a systemd --user unit by install.sh so caps-lock OSD works without a keybind (D-23)"
    - "stow.sh declares the swayosd stow package so its config symlinks on a fresh setup"
    - "verify_packages confirms every newly-added package installed, hard-failing if any is missing"
  artifacts:
    - install.sh (PACMAN_PKGS + AUR_PKGS extended; swayosd service enable in section_core_rice)
    - stow.sh (PACKAGES array extended with swayosd)
  key_links:
    - "install.sh AUR_PKGS -> human package-legitimacy checkpoint gates tela/colloid/papirus-folders before any AUR install runs"
    - "install.sh section_core_rice systemctl --user enable -> swayosd-libinput-backend.service"
  prohibitions:
    - statement: "colloid-icon-theme (plain name, without -git suffix) must NOT be added to AUR_PKGS — that package does not exist on AUR; only colloid-icon-theme-git exists (RESEARCH Package Legitimacy Audit)"
      status: resolved
      verification: "grep AUR_PKGS block: colloid entry ends in -git"
    - statement: "None of the 13 official-repo packages (hyprshot, satty, gpu-screen-recorder, swayosd, hyprpicker, wtype, ddcutil, papirus-icon-theme, 5 nerd fonts) may be placed in AUR_PKGS — they are all official extra repo (RESEARCH correction to CONTEXT.md)"
      status: resolved
      verification: "grep PACMAN_PKGS contains each official-repo package; AUR_PKGS contains only the 3 AUR packages"
---

<objective>
Extend install.sh and stow.sh so every tool this phase's surfaces and utilities depend on is reproducibly installed from a fresh Arch system, and gate the three AUR packages behind a human package-legitimacy checkpoint. This is the reproducibility foundation for the whole phase — the RESEARCH Package Legitimacy Audit corrected CONTEXT.md's assumption: only 3 of 12 new packages are AUR (tela-icon-theme, colloid-icon-theme-git, papirus-folders); the rest are official `extra` repo. Also enable the SwayOSD libinput backend as a systemd user service per D-23.

Purpose: The core project value is "the whole setup reproduces from scratch with one script." Every new binary (hyprshot, satty, gpu-screen-recorder, swayosd, hyprpicker, wtype, ddcutil, icon themes, nerd fonts) must be declared here, not manually installed.
Output: install.sh with extended PACMAN_PKGS/AUR_PKGS + swayosd service enable; stow.sh with the swayosd package declared.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md
@.planning/phases/06-themed-surfaces-utility-suite/06-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: AUR package-legitimacy gate for the three AUR packages</name>
  <files>install.sh (AUR_PKGS block — gated here, wired in Task 2)</files>
  <action>Gate only — no file changes in this task. Confirm the three AUR packages (tela-icon-theme, colloid-icon-theme-git, papirus-folders) are legitimate on their AUR pages per the how-to-verify steps below before Task 2 adds them to install.sh AUR_PKGS. Block until a human approves.</action>
  <what-built>Nothing yet — this is a blocking legitimacy gate that MUST clear before Task 2 adds any AUR package to install.sh. Per project precedent (Phase 4) and the security threat model, all AUR (non-official-repo) packages get a human legitimacy check before install wiring. RESEARCH already pre-verified all three against AUR RPC v5 this session; this checkpoint confirms them.</what-built>
  <how-to-verify>
    Confirm each package on its AUR page before approving:
    1. Visit https://aur.archlinux.org/packages/tela-icon-theme — confirm: maintained (yochananmarqos), non-trivial votes (~21), upstream github.com/vinceliuice/Tela-icon-theme, recently updated.
    2. Visit https://aur.archlinux.org/packages/colloid-icon-theme-git — confirm it EXISTS under this exact name (the plain `colloid-icon-theme` does NOT exist), maintainer/votes present, upstream github.com/vinceliuice/Colloid-icon-theme, Provides colloid-icon-theme.
    3. Visit https://aur.archlinux.org/packages/papirus-folders — confirm maintained (yochananmarqos), ~33 votes, upstream github.com/PapirusDevelopmentTeam/papirus-folders.
    All three are verdict OK in RESEARCH.md Package Legitimacy Audit (no [SLOP]/[SUS]).
  </how-to-verify>
  <resume-signal>Type "approved" once all three AUR pages are confirmed legitimate, or name any package to drop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add all new packages to install.sh + enable swayosd libinput backend (D-03/D-16/D-18/D-23)</name>
  <read_first>
    - install.sh (read the full PACMAN_PKGS array lines 52-152, AUR_PKGS lines 163-182, and section_core_rice's systemctl --user enable block near lines 292-301, and verify_packages at 363-389 — you must extend the existing arrays in place, matching their commented-group style)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Standard Stack + Package Legitimacy Audit — the authoritative official-vs-AUR split and exact package names)
  </read_first>
  <action>
    Extend install.sh PACMAN_PKGS (official extra repo) with a new commented group adding exactly these 13 packages: hyprshot, satty, gpu-screen-recorder, swayosd, hyprpicker, wtype, ddcutil, papirus-icon-theme, ttf-jetbrains-mono-nerd, ttf-cascadia-code-nerd, ttf-hack-nerd, ttf-iosevka-nerd, ttf-meslo-nerd. Extend AUR_PKGS with exactly these 3: tela-icon-theme, colloid-icon-theme-git (note the -git suffix — plain colloid-icon-theme does not exist per RESEARCH), papirus-folders. Do NOT place any of the 13 official packages in AUR_PKGS. In section_core_rice, after the existing dbus-broker enable (~line 299), add a guarded `systemctl --user enable swayosd-libinput-backend.service` (per D-23: enabled via install.sh so caps-lock OSD works without a keybind), wrapped `2>/dev/null || true` following the existing dbus-broker enable idiom. The verify_packages hard-fail table automatically covers the new packages since VERIFY_PKGS is built from PACMAN_PKGS + AUR_PKGS — no verify_packages edit needed. Cite D-03 (gpu-screen-recorder/NVENC), D-16 (papirus/tela/colloid), D-18 (nerd fonts 5-pack), D-23 (swayosd service).
  </action>
  <verify>
    <automated>bash -n install.sh && for p in hyprshot satty gpu-screen-recorder swayosd hyprpicker wtype ddcutil papirus-icon-theme ttf-jetbrains-mono-nerd ttf-cascadia-code-nerd ttf-hack-nerd ttf-iosevka-nerd ttf-meslo-nerd tela-icon-theme colloid-icon-theme-git papirus-folders; do grep -q "$p" install.sh || { echo "MISSING: $p"; exit 1; }; done && grep -q 'swayosd-libinput-backend.service' install.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0 (no syntax error)
    - PACMAN_PKGS section contains all 13 official packages; AUR_PKGS contains exactly the 3 AUR packages
    - AUR_PKGS colloid entry is `colloid-icon-theme-git` (not plain `colloid-icon-theme`)
    - install.sh contains a `systemctl --user enable swayosd-libinput-backend.service` line inside section_core_rice
    - No official-repo package name appears inside the AUR_PKGS array block
  </acceptance_criteria>
  <done>All 16 new packages declared in the correct array; swayosd libinput backend enabled via install.sh (D-23); verify_packages covers them automatically.</done>
</task>

<task type="auto">
  <name>Task 3: Declare swayosd stow package in stow.sh</name>
  <read_first>
    - stow.sh (read the PACKAGES array lines 19-37 and the surrounding stow loop — add the new package name alphabetically/logically in the array)
    - .planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md (swayosd stow package section — swayosd/.config/swayosd/style.css @imports the rendered swayosd.css, mirroring the swaync/wlogout pattern; the actual style.css is created in plan 06-06)
  </read_first>
  <action>
    Add `swayosd` to the stow.sh PACKAGES array (the swayosd stow package dir swayosd/.config/swayosd/ is created by plan 06-06; declaring it here centralizes stow.sh ownership in this plan so no later plan edits stow.sh). Do NOT add a satty package — satty's themed config is a full matugen-rendered file symlinked by commit.sh (plan 06-02), not a stow package (TOML has no @import, so it follows the walker/yazi symlink model, not the swaync @import model). Verify the container gate note: the swayosd package config only @imports a state-dir file, so a fresh headless stow stays green.
  </action>
  <verify>
    <automated>bash -n stow.sh && grep -A25 'PACKAGES=(' stow.sh | grep -qx '    swayosd' && ! grep -A25 'PACKAGES=(' stow.sh | grep -qx '    satty' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n stow.sh` exits 0
    - PACKAGES array contains a `swayosd` entry
    - PACKAGES array does NOT contain a `satty` entry (satty uses the commit.sh symlink model, not stow)
  </acceptance_criteria>
  <done>stow.sh declares the swayosd package; satty correctly excluded (symlink model).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AUR build scripts → local system | AUR PKGBUILDs run arbitrary code at build time (untrusted-by-default) |
| upstream package registry → install.sh | package names resolved against remote repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | AUR installs (tela-icon-theme, colloid-icon-theme-git, papirus-folders) | high | mitigate | Blocking human package-legitimacy checkpoint (Task 1) before any AUR wiring; all three pre-verified against AUR RPC v5 with named maintainers, real upstream repos, non-trivial votes (RESEARCH audit) |
| T-06-01 | Tampering | mistyped/non-existent package name (colloid-icon-theme) | medium | mitigate | RESEARCH name-corrected to colloid-icon-theme-git; verify_packages hard-fails on any missing package post-install |
</threat_model>

<verification>
- `bash -n install.sh && bash -n stow.sh` both exit 0
- All 16 packages grep-present in the correct arrays; swayosd service enable present
- On a real fresh install (out of scope for headless CI): install.sh completes, verify_packages prints [OK] for all new packages
</verification>

<success_criteria>
- Every tool consumed by later plans (06-04..06-09) is declared in install.sh from the official repo (13) or AUR-gated (3)
- swayosd libinput backend enabled via install.sh (D-23)
- stow.sh declares swayosd; container gate stays green
</success_criteria>

## Artifacts this phase produces

This plan creates/modifies these symbols (source-grounding exclusion list):
- `install.sh` PACMAN_PKGS entries: hyprshot, satty, gpu-screen-recorder, swayosd, hyprpicker, wtype, ddcutil, papirus-icon-theme, ttf-jetbrains-mono-nerd, ttf-cascadia-code-nerd, ttf-hack-nerd, ttf-iosevka-nerd, ttf-meslo-nerd
- `install.sh` AUR_PKGS entries: tela-icon-theme, colloid-icon-theme-git, papirus-folders
- `install.sh` new line: `systemctl --user enable swayosd-libinput-backend.service`
- `stow.sh` PACKAGES entry: swayosd

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-01-SUMMARY.md` when done.
</output>
