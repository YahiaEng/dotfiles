---
phase: 06-themed-surfaces-utility-suite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - wlogout/.config/wlogout/layout
  - wlogout/.config/wlogout/style.css
  - wlogout/.config/wlogout/icons
autonomous: true
requirements: [WLOG-01]
user_setup: []

must_haves:
  truths:
    - "wlogout shows a compact horizontal center bar of icon buttons floating over the blurred/dimmed desktop, not the old full-screen tile grid (D-09)"
    - "Each button renders a Nerd Font glyph as its text label (D-10) — no SVG image assets remain"
    - "All six actions (lock, logout, suspend, hibernate, reboot, shutdown) are present with their existing uwsm-audited commands unchanged"
    - "Button labels appear on hover/focus; the desktop is visible behind the bar (HUD, not a page)"
    - "Colors still come live from the existing wlogout.css pipeline target — no new render target added (contract.json unchanged by this plan)"
  artifacts:
    - wlogout/.config/wlogout/layout (glyph text field per button)
    - wlogout/.config/wlogout/style.css (center-bar layout, glyph typography, no background-image rules)
  key_links:
    - "style.css @import ../../.local/state/theme/wlogout.css -> live palette (unchanged first line)"
    - "layout text field -> GTK button label rendering the Nerd Font glyph"
  prohibitions:
    - statement: "No SVG background-image icon rule may remain in style.css and the icons/ asset dir must be removed (D-10: SVG assets deleted, glyphs replace them)"
      status: resolved
      verification: "style.css contains no background-image url rule referencing the icons dir; wlogout/.config/wlogout/icons no longer exists"
    - statement: "The FiraCode Nerd Font family on the wlogout glyph is a hardcoded literal, NOT the UTIL-05 switchable font axis (UI-SPEC Design System / Typography)"
      status: resolved
      verification: "style.css button label font-family is the literal FiraCode Nerd Font, not a state-dir import"
---

<objective>
Redesign wlogout from the current full-screen SVG-icon tile grid into a modern minimal center bar of Nerd-Font-glyph buttons floating over the blurred desktop (WLOG-01, D-09/D-10). Pure template + CSS work on the existing wlogout.css pipeline target — no new render target, no contract.json change, no engine plumbing. Independent of every other Wave 1 plan (touches only the wlogout package).

Purpose: The old bar used stretched low-res SVGs; D-10 swaps them for Nerd Font glyphs (sharp at any scale, light/dark for free, zero icon render targets). D-09 restructures the layout into a HUD center bar.
Output: Redesigned wlogout/layout + style.css; icons/ dir deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md
@.planning/phases/06-themed-surfaces-utility-suite/06-PATTERNS.md
@wlogout/.config/wlogout/layout
@wlogout/.config/wlogout/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap layout button text to Nerd Font glyphs (D-10)</name>
  <files>wlogout/.config/wlogout/layout</files>
  <read_first>
    - wlogout/.config/wlogout/layout (the 6 JSON-per-line button objects — lock/logout/suspend/hibernate/shutdown/reboot; keep every action command and keybind unchanged, only the text field changes)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Typography wlogout icon glyph table mapping each action to a Nerd Font glyph family: nf-fa-lock, nf-fa-sign_out, nf-fa-moon_o, nf-fa-snowflake_o, nf-fa-refresh, nf-fa-power_off)
    - .planning/phases/06-themed-surfaces-utility-suite/06-RESEARCH.md (Pattern 3: layout text field is a real GTK label, so any Unicode codepoint renders; exact codepoints resolved at execution via fc-list, do not trust an unverified codepoint)
  </read_first>
  <action>
    Edit wlogout/.config/wlogout/layout: replace each button's text value (currently the words Lock/Logout/Suspend/Hibernate/Shutdown/Reboot) with the corresponding Nerd Font glyph codepoint per the UI-SPEC mapping (lock=nf-fa-lock, logout=nf-fa-sign_out, suspend=nf-fa-moon_o, hibernate=nf-fa-snowflake_o, reboot=nf-fa-refresh, shutdown=nf-fa-power_off). Resolve the actual codepoint for each glyph family at execution via `fc-list | grep -i nerd` plus the nerdfonts.com cheat-sheet — do NOT paste an unverified codepoint. Keep every label, action, and keybind field exactly as-is (the six actions were Phase-4-audited against uwsm; changing them is out of scope). All six actions stay present (D-09).
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && grep -c '"action"' wlogout/.config/wlogout/layout | grep -qx 6 && grep -q 'hyprshutdown --post-cmd' wlogout/.config/wlogout/layout && python3 -c "import json; [json.loads(l) for l in open('wlogout/.config/wlogout/layout') if l.strip()]" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - layout still contains 6 button objects, each valid JSON per line (python json.loads succeeds for every non-blank line)
    - The hyprshutdown --post-cmd shutdown/reboot actions are unchanged
    - No button text value is a plain ASCII word (each is a glyph codepoint)
  </acceptance_criteria>
  <done>All six buttons render Nerd Font glyphs; actions/keybinds preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite style.css as a center bar with glyph typography, delete icon rules (D-09/D-10)</name>
  <files>wlogout/.config/wlogout/style.css</files>
  <read_first>
    - wlogout/.config/wlogout/style.css (full file — line 1 @import stays; the button base rule, button:hover/:focus transition block, and the per-id background-image + border-hue accent rules are what changes)
    - .planning/phases/06-themed-surfaces-utility-suite/06-UI-SPEC.md (Spacing wlogout bar table: button 72x72px, glyph 28px, button gap 16px md, radius 20px kept as pre-existing exception, padding 10px kept; Color wlogout 60/30/10 map — background scrim rgba(0,0,0,0.6) kept, per-action border-hue accents preserved: primary/secondary/tertiary/error; Typography: label 14px reveal on hover, glyph 28px)
  </read_first>
  <action>
    Rewrite wlogout/.config/wlogout/style.css keeping line 1 (@import url("../../.local/state/theme/wlogout.css")) unchanged. Keep the window full-screen dim scrim (rgba(0,0,0,0.6)) but restructure the button container into a compact horizontal center bar: 72x72px buttons, 16px inter-button gap, buttons centered as a floating row (HUD over the visible desktop). Add `button label { font-family: "FiraCode Nerd Font"; font-size: 28px; }` (hardcoded literal font — NOT the UTIL-05 axis) for the glyph rendering. Keep the existing button:hover/:focus transition block and the per-action border-hue accents (lock=primary, logout=secondary, suspend/reboot=tertiary, hibernate=on_surface_variant, shutdown=error) that already give each action a distinct accent. Reveal the text label on hover/focus (14px). DELETE every per-id SVG background-image rule (the rules that set a background image from the icons directory) — those assets are gone. Keep the pre-existing 20px border-radius and 10px padding on the button rule (UI-SPEC Deviation Request: D-10 scopes this redesign to icon-source swap only, not shape).
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && grep -q '@import url("../../.local/state/theme/wlogout.css")' wlogout/.config/wlogout/style.css && grep -q 'button label' wlogout/.config/wlogout/style.css && grep -vc '^\s*/\*' wlogout/.config/wlogout/style.css >/dev/null && [ "$(grep -c 'background-image' wlogout/.config/wlogout/style.css)" = "0" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - style.css line 1 @import of the wlogout.css state-dir target is unchanged
    - style.css contains a `button label` rule with a 28px glyph font-size and the literal FiraCode Nerd Font family
    - `grep -c 'background-image' style.css` returns 0 (no SVG icon rules remain)
    - Per-action border accents (primary/secondary/tertiary/error) still present
  </acceptance_criteria>
  <done>Center-bar HUD layout with glyph buttons; all SVG background-image rules removed; palette wiring intact.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the obsolete icons/ asset directory (D-10)</name>
  <files>wlogout/.config/wlogout/icons (removed)</files>
  <read_first>
    - wlogout/.config/wlogout/icons (list its contents so you know what is being removed — the SVGs lock.svg/logout.svg/etc. that the deleted style.css rules referenced)
    - stow.sh (confirm wlogout is a stowed package — removing icons/ from the repo removes them from the stow tree; no live symlink to icons/ remains after Task 2 removed the references)
  </read_first>
  <action>
    Remove the wlogout/.config/wlogout/icons/ directory and its SVG contents from the repo via `git rm -r wlogout/.config/wlogout/icons` (D-10: the SVG assets are deleted, glyphs replace them). Confirm no remaining reference to the icons path exists anywhere in the wlogout package after Task 2's rule deletions. This keeps the redesign a clean icon-source swap with zero new render targets for icons.
  </action>
  <verify>
    <automated>cd /home/aorus/dotfiles && test ! -d wlogout/.config/wlogout/icons && ! grep -rq 'icons/' wlogout/.config/wlogout/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - wlogout/.config/wlogout/icons directory no longer exists
    - No file under wlogout/.config/wlogout/ references the icons/ path
  </acceptance_criteria>
  <done>icons/ deleted; wlogout package is glyph-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click/keybind → wlogout action | wlogout action commands run session-power operations (already Phase-4-audited) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Elevation of Privilege | wlogout power-action commands | low | accept | Action commands are unchanged from Phase 4's uwsm audit; this plan touches only visual text/CSS, never the action strings |
| T-06-05 | Tampering | GTK label rendering an arbitrary Unicode glyph | low | accept | Glyph codepoints come from the curated Nerd Font cheat-sheet, hardcoded in the layout; no external input |
</threat_model>

<verification>
- layout is 6 valid JSON objects with unchanged actions/keybinds
- style.css keeps the wlogout.css @import, has a glyph `button label` rule, and zero background-image rules
- icons/ directory removed; no icons/ references remain
- Visual confirmation (manual, live session): Super+Shift+Q shows the center bar with glyphs, hover reveals labels, desktop visible behind — under both a light and a dark theme
</verification>

<success_criteria>
- Modern minimal center bar with sharp Nerd Font glyphs, colors live from the pipeline (WLOG-01)
- No SVG assets; contract.json unchanged (wlogout.css is an existing edited target)
</success_criteria>

## Artifacts this phase produces

- `wlogout/.config/wlogout/layout` (glyph text values)
- `wlogout/.config/wlogout/style.css` (center-bar layout + `button label` glyph rule)
- Removal: `wlogout/.config/wlogout/icons/` directory

<output>
Create `.planning/phases/06-themed-surfaces-utility-suite/06-03-SUMMARY.md` when done.
</output>
