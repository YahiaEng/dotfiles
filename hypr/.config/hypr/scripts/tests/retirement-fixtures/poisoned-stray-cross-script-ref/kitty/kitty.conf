# fixture: poisoned — an unrelated package still namechecks the retired
# surface. The reference is on the `map` line below, NOT in this comment,
# and that is load-bearing: as of quick task 260826-npc the
# cross-package-refs class skips whole-line comments, so a commented poison
# would leave this fixture green and the class would lose its only proof
# that it can still fail. Keep the token on a real config line.
font_family FiraCode Nerd Font
map ctrl+shift+r launch retirement-fixture
