# motion-lint fixture (13-01 Task 4).
# Derives from compliant-hypr.conf, corrupting the animation line's SPEED
# field back to a raw hand-authored number instead of a $motion_speed_*
# token — the Hyprland-side equivalent of CSS's bare `Nms` raw-value
# violation. Never sourced by any live Hyprland config — motion-lint
# self-test fixture only.
animations {
    bezier = motion-standard, 0.2, 0, 0, 1
    bezier = motion-emphasized-decelerate, 0.05, 0.7, 0.1, 1
    animation = windowsIn, 1, 5, motion-emphasized-decelerate, popin 60% # CORRUPTED: was `$motion_speed_standard`
}
