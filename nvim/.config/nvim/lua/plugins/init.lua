-- Placeholder so lazy.nvim's `{ import = "plugins" }` has something to find
-- before any real plugin spec exists. Safe to leave once real specs land
-- alongside it — lazy merges every file under this directory.
return {}
