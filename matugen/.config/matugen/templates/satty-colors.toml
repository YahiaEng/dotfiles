# Auto-generated by matugen — Material You
# Do NOT edit manually — changes will be overwritten
#
# SHOT-02/D-02/D-31: satty annotation config. [general] is static behavior
# (does not vary per-theme); [color-palette] is the pipeline-themed 7-swatch
# annotation row. Keys verified against the live upstream schema
# (github.com/gabm/Satty config.toml, installed version 0.21.1 — official
# extra repo).

[general]
# D-02: default pre-selected annotation tool = arrow
initial-tool = "arrow"
# D-02: exit immediately once a copy or save action fires (covers both the
# Enter action below and satty's own built-in Ctrl+C copy shortcut)
early-exit = ["all"]
# D-02: Enter = copy to clipboard + save to file + exit
actions-on-enter = ["save-to-clipboard", "save-to-file"]

# D-31: curated 7-swatch annotation palette, default selection = primary
# (first entry). RRGGBBAA format (00 = fully transparent, ff = fully
# opaque), per satty's hex_color-crate parsing (requires the leading '#').
[color-palette]
palette = [
  "{{colors.primary.default.hex}}ff",
  "{{colors.secondary.default.hex}}ff",
  "{{colors.tertiary.default.hex}}ff",
  "{{colors.on_surface.default.hex}}ff",
  "{{colors.background.default.hex}}ff",
  "#E53935ff",
  "{{colors.outline.default.hex}}ff",
]
