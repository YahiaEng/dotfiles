# Auto-generated by matugen — Material You
#
# THM-04/D-15: fzf color fragment — shell-sourceable FZF_COLOR_* assignments
# consumed by hypr/.config/hypr/scripts/wallpaper-picker.sh. Slot mapping is
# LOCKED per 05-UI-SPEC.md Color section 3 (60/30/10 split translated to fzf
# color roles):
#   FG          <- on_background       (dominant 60%, unselected row text)
#   BG          <- -1 (literal)        (terminal transparency, kept as-is)
#   BG_PLUS     <- surface_variant     (secondary 30%, selected-row bg)
#   HEADER      <- on_surface_variant  (secondary 30%, header text)
#   FG_PLUS     <- on_primary_container (selected-row text, readable on BG_PLUS)
#   HL/HL_PLUS/POINTER/MARKER/SPINNER <- tertiary (accent 10%, reserved —
#     match highlights, pointer glyph, marker glyph, spinner ONLY; never
#     plain list text, borders, or header body)
#   PROMPT      <- primary             (prompt glyph only)
#   INFO        <- secondary           (match-count info line only)
#   BORDER      <- outline             (frame border only)
#
# The only literal in this file is BG's "-1" — every color slot is a
# matugen keyword. Consumed via `source` with ${VAR:-fallback} defaults so
# a fresh install (before the first theme-apply) degrades gracefully.
FZF_COLOR_FG="{{colors.on_background.default.hex}}"
FZF_COLOR_BG="-1"
FZF_COLOR_BG_PLUS="{{colors.surface_variant.default.hex}}"
FZF_COLOR_FG_PLUS="{{colors.on_primary_container.default.hex}}"
FZF_COLOR_HL="{{colors.tertiary.default.hex}}"
FZF_COLOR_HL_PLUS="{{colors.tertiary.default.hex}}"
FZF_COLOR_INFO="{{colors.secondary.default.hex}}"
FZF_COLOR_PROMPT="{{colors.primary.default.hex}}"
FZF_COLOR_POINTER="{{colors.tertiary.default.hex}}"
FZF_COLOR_MARKER="{{colors.tertiary.default.hex}}"
FZF_COLOR_SPINNER="{{colors.tertiary.default.hex}}"
FZF_COLOR_HEADER="{{colors.on_surface_variant.default.hex}}"
FZF_COLOR_BORDER="{{colors.outline.default.hex}}"
