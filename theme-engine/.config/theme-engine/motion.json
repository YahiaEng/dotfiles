{
  "_comment": "Sole source for every duration and control-point number the theme-engine emits (D-05). Every consumer resolves a semantic name — never a copied/inlined literal. The 'motion-' prefix on emitted Hyprland curve names is a deliberate collision guard against animations.conf's 12 hand-authored beziers (D-04: additive only, nothing rewired this phase).",
  "_comment_13_07_removal": "13-07 removed D-21's temporary A/B curve-comparison toggle (the top-level object that used to sit here, named 'curve' + '_sets') and D-11's marked non-MD3 'x-' prefixed easing extension (the five character curves this project's default vocabulary never adopted) — no D-22 retune ever adopted any of them, because the soak gate that would have produced a retune verdict was waived by explicit operator decision rather than passed (see 13-MOTION-SOAK-VERDICT.md). The six feel-changing Hyprland slots (windows-in/out/move, fade-in/out, workspaces) that used to read the removed per-slot curve variable now reference the toggle's former MD3-default mapping directly as literals in animations.conf. The shipped easing vocabulary below is pure MD3.",
  "_comment_260821_swp_reversal": "quick-260821-swp reinstates, as a genuine user-facing setting, the kind of per-style curve data plan 13-07 removed above as a measuring instrument — this time under a 'styles' table rather than a temporary A/B toggle, and never re-adopting the deleted 'x-' character easings by name. TOKEN-06's 'MD3 is better, spring is too fast' verdict was a tuning-parameter rejection, not a mechanism rejection (a future spring revisit was explicitly left open) — this task does not revisit springs, it revisits SHAPE via bezier-only per-style curves (R-1), so neither prior decision is being overturned, only the axis this repo exposes to the user.",
  "durations": {
    "_comment_260829_micro": "quick task 260829-rhy. M3's own duration scale starts at short1 (50ms), which is coarser than a STAGGER OFFSET needs: with only 50/100/150 available, five styles cannot hold five distinct cascade rhythms. These four sit below short1 on the same naming convention and exist for the stagger-offset channel. 45 rather than 40 for the tightest: floor_ms IS 40 and every duration is clamped UP to it (`if $scaled < $floor then $floor else $scaled end`), so a 40ms token would be indistinguishable from the floor itself and would stop scaling under the accessibility multiplier. extra-long5 extends the other end for `ambient`, a LOOP PERIOD where 1000ms was previously the ceiling and every style sat on it.",
    "micro1": 45,
    "micro2": 60,
    "micro3": 80,
    "micro4": 100,
    "short1": 50,
    "short2": 100,
    "short3": 150,
    "short4": 200,
    "medium1": 250,
    "medium2": 300,
    "medium3": 350,
    "medium4": 400,
    "long1": 450,
    "long2": 500,
    "long3": 550,
    "long4": 600,
    "extra-long1": 700,
    "extra-long2": 800,
    "extra-long3": 900,
    "extra-long4": 1000,
    "extra-long5": 1200
  },
  "_comment_spatial_easings": "spatial-in/spatial-out/spatial-move (R-2) are the ONLY three easing names a style may ever push a control-point Y above 1.0 on — every other name below stays monotonic within [0,1] in every style, forever, so a fade or a colour can never be given overshoot by construction. Seeded here identical to emphasized-decelerate/emphasized-accelerate/standard so this task's Task 1 restructure moves nothing on screen; Task 2 gives each non-md3 style its own points for these three names only.",
  "_comment_multi_segment_easings": "An easing is normally FOUR numbers - one cubic's two control points, with the (1,1) endpoint appended at render time. `emphasized` is the exception: TEN numbers, which render to twelve, which is Qt's BezierSpline layout of SIX numbers per cubic SEGMENT (c1x,c1y,c2x,c2y,endX,endY) and therefore TWO segments. That is Material 3's real emphasized easing and it cannot be expressed as a single cubic. IT IS A QML-ONLY EASING BY NECESSITY: Hyprland's hl.curve takes exactly two control points, so lib/motion.sh SKIPS any easing that is not four numbers when registering Hyprland curves and when writing CSS cubic-bezier(), rather than truncating it into a plausible-looking wrong curve. No hypr_leaves entry in any style may name it - register_hypr_leaf would find no registered curve and would drop the leaf entirely.",
  "easings": {
    "standard": [0.2, 0, 0, 1],
    "standard-decelerate": [0, 0, 0, 1],
    "standard-accelerate": [0.3, 0, 1, 1],
    "emphasized-decelerate": [0.05, 0.7, 0.1, 1],
    "emphasized-accelerate": [0.3, 0, 0.8, 0.15],
    "legacy": [0.4, 0, 0.2, 1],
    "legacy-decelerate": [0, 0, 0.2, 1],
    "legacy-accelerate": [0.4, 0, 1, 1],
    "linear": [1, 1, 1, 1],
    "css-linear": [0, 0, 1, 1],
    "spatial-in": [0.05, 0.7, 0.1, 1],
    "spatial-out": [0.3, 0, 0.8, 0.15],
    "spatial-move": [0.2, 0, 0, 1],
    "emphasized": [0.05, 0, 0.13333333, 0.06, 0.16666667, 0.4, 0.20833333, 0.82, 0.25, 1],
    "effects": [0.2, 0, 0, 1]
  },
  "_comment_semantic_leaf_channels": "semantic's four leaf-* entries are LEAF-ONLY SPEED CHANNELS. A Hyprland leaf normally takes its duration from its own curve name, which welds curve and speed together and stops two leaves sharing a curve from holding different speeds. A leaf entry's optional `speed_key` field names one of these channels instead, breaking that weld. They are named for the DURATION TOKEN they carry, never for a millisecond count, so they stay honest under the accessibility multiplier, which scales them like any other semantic entry. Their `easing` is filler and is never read: a leaf takes its bezier from its own `curve`, and only the channel's DURATION is used. Nothing in the base tables points at them and Motion.qml walks a FIXED _pairNames list, so they reach neither QML nor any existing style. NOTE: every key inside 'semantic' MUST be a {duration,easing} object, never a comment string - lib/motion.sh renders it with `.semantic | to_entries[] | $D[$v.duration]`, which a string value breaks. Added 2026-08-25 for 'zen'.",
  "semantic": {
    "standard": { "duration": "short4", "easing": "standard" },
    "emphasized-in": { "duration": "medium2", "easing": "emphasized-decelerate" },
    "emphasized-out": { "duration": "short3", "easing": "emphasized-accelerate" },
    "standard-slow": { "duration": "medium2", "easing": "standard" },
    "ambient": { "duration": "extra-long4", "easing": "linear" },
    "stagger-offset": { "duration": "short1", "easing": "standard" },
    "spatial-in": { "duration": "medium2", "easing": "spatial-in" },
    "spatial-out": { "duration": "short3", "easing": "spatial-out" },
    "spatial-move": { "duration": "short4", "easing": "spatial-move" },
    "leaf-short3": { "duration": "short3", "easing": "standard" },
    "leaf-short4": { "duration": "short4", "easing": "standard" },
    "leaf-medium1": { "duration": "medium1", "easing": "standard" },
    "leaf-medium2": { "duration": "medium2", "easing": "standard" },
    "leaf-medium4": { "duration": "medium4", "easing": "standard" },
    "leaf-long2": { "duration": "long2", "easing": "standard" },
    "leaf-long4": { "duration": "long4", "easing": "standard" },
    "colour": { "duration": "short4", "easing": "effects" }
  },
  "_comment_260829_border_transition": "quick task 260829-rhy. Hyprland's `border` leaf (the focus-change border colour transition) used to read `ambient` directly in animations.lua. That was a latent coupling smell — `ambient` is a LOOP PERIOD for continuous QML motion (breathing, pulsing), not a one-shot transition duration — and it became a real problem the moment `ambient` was made per-style: hypr-equivalence-check pins the border leaf at the baseline's 10.0ds, so every style with a different ambient failed the gate. Decoupled rather than waived. The border transition keeps the 1000ms the baseline pins and lives in `indicators`, which styles cannot override; `ambient` is now QML-only, which is what it was always for.",
  "indicators": {
    "blink-slow": { "duration_ms": 1000, "easing": "css-linear" },
    "blink-fast": { "duration_ms": 500, "easing": "css-linear" },
    "border-transition": { "duration_ms": 1000, "easing": "linear" },
    "border-rotate": { "duration_ms": 10000, "easing": "linear" }
  },
  "_comment_accessibility": "The reduced-motion/animations-off axis, orthogonal to style (BRIEF: 'off'/'reduced' do NOT become styles). Same {multiplier, animations_enabled} shape the removed 'scales' table carried, so every reader below the resolution point (Motion.qml's ambientDuration divide-back-out logic, the gate's scaled-speed forgiveness) needs no rewrite. 'lively' (1.25x) has no home here — the multiplier axis is absorbed by per-style durations; a legacy 'lively' value migrates to style md3 + accessibility full (a deliberate, stated loss).",
  "accessibility": {
    "full": { "multiplier": 1.0, "animations_enabled": true },
    "reduced": { "multiplier": 0.5, "animations_enabled": true },
    "off": { "multiplier": 1.0, "animations_enabled": false }
  },
  "_comment_hypr_leaves": "One entry per style-varying Hyprland leaf. Fade/border/borderangle leaves are deliberately absent — they keep their hardcoded wiring in animations.lua (a style changes window/workspace ENTRY SHAPE and curve, never a fade's style string or borderangle's loop mechanism). Seeded here identical to today's animations.lua literals, with the spatial curve names substituted for the emphasized-decelerate/emphasized-accelerate/standard names those leaves used before this task.",
  "hypr_leaves": {
    "windows_in": { "curve": "spatial-in", "style": "popin 60%" },
    "windows_out": { "curve": "spatial-out", "style": "popin 60%" },
    "windows_move": { "curve": "spatial-move", "style": "slide" },
    "workspaces": { "curve": "spatial-in", "style": "slide" },
    "special_workspace": { "curve": "spatial-in", "style": "slidevert" },
    "layers": { "curve": "spatial-move", "style": "popin 80%" },
    "layers_in": { "curve": "spatial-in", "style": "popin 80%" },
    "layers_out": { "curve": "spatial-out", "style": "popin 80%" },
    "fade_in": { "curve": "standard-decelerate", "speed_key": "emphasized-in" },
    "fade_out": { "curve": "standard-accelerate", "speed_key": "emphasized-out" },
    "fade_switch": { "curve": "standard" },
    "fade_shadow": { "curve": "standard" },
    "fade_dim": { "curve": "standard" }
  },
  "_comment_styles": "Each style may override a SUBSET of easings/semantic/hypr_leaves keys (schema invariant: override keys must be a subset of the base table's keys — a style changes points behind a name, never adds a name). Overrides are shallow-merged over the base tables at render/gate time — a style-provided key replaces its base entry wholesale, a key the style does not mention keeps the base value. 'style' strings reach Hyprland's own parser directly, so each must match ^[a-z]+( [0-9]{1,3}%)?$.",
  "styles": {
    "md3": {
      "label": "MD3",
      "description": "Decelerate, lands exactly, no overshoot — the M3 reference curves, at the calmer tempo the retired `smooth` style used to carry.",
      "_comment_260829_merge": "quick task 260829-grv: `smooth` was folded into this style and deleted. Measured cause: md3 and smooth were the ONLY two monotonic styles in the set (both peak exactly 1.000 — every other style overshoots), so they shared one whole personality niche and differed ONLY in tempo, md3 running 50-100ms faster on every single channel. That is a speed dial wearing two names, which is precisely what 260821-swp abolished when it replaced the motion-scale duration multiplier with style-as-personality. md3 is the survivor because it is MOTION_DEFAULT_STYLE, the fallback target for any invalid persisted value, and the reference the other styles' derived emphasized pairs are validated against. Operator's call was `keep md3's curves, take smooth's calmer timing`, so `easings` stays empty — these are still the untouched M3 spec curves, which keeps the emphasized derivation's validation anchor intact — and only the durations below moved to smooth's values. NOTE the Hyprland leaves follow for free: a leaf with no `speed_key` uses its own CURVE name as the speed name (register_hypr_leaf's documented default), so windows_in reads `spatial-in`'s 400ms here rather than needing a hypr_leaves block. That is why this style still overrides no leaves at all.",
      "easings": {},
      "semantic": {
        "stagger-offset": { "duration": "short1", "easing": "standard" },
        "ambient": { "duration": "extra-long4", "easing": "linear" },
        "standard": { "duration": "medium1", "easing": "standard" },
        "standard-slow": { "duration": "long2", "easing": "standard" },
        "emphasized-in": { "duration": "medium3", "easing": "emphasized-decelerate" },
        "emphasized-out": { "duration": "short3", "easing": "emphasized-accelerate" },
        "colour": { "duration": "medium1", "easing": "effects" },
        "spatial-in": { "duration": "medium4", "easing": "spatial-in" },
        "spatial-out": { "duration": "short4", "easing": "spatial-out" },
        "spatial-move": { "duration": "medium1", "easing": "spatial-move" }
      },
      "hypr_leaves": {}
    },
    "snappy": {
      "label": "Snappy",
      "description": "Short, fast rise, a barely-there overshoot.",
      "_comment_260828_parity": "quick task 260828-mot: brought to zen's coverage. Before this, this style overrode 1-2 easings, 6 semantic names and 2-4 Hyprland leaves while zen overrode 4/8/13, so switching away from zen dropped nine leaves (windows_move, all three layers_*, all five fade_*) back to the base MD3 table and the style's own character stopped at the window border. spatial-out/spatial-move/effects are overridden here for the same reason. INVARIANT HELD: only spatial-in/out/move may leave [0,1] (the file's own rule) - `effects` drives colour and stays monotonic in every style below, because a colour that overshoots is simply the wrong colour briefly. Hyprland silently keeps the OLD curve for any control-point Y above 2.00; every value here is well under.",
      "_comment_260829_emphasized": "quick task 260829-wvy: the emphasized pair is DERIVED, not chosen. In the base table `emphasized-decelerate` is byte-identical to `spatial-in` and `emphasized-accelerate` to `spatial-out` — the emphasized curves ARE the monotonic form of the spatial ones. Before this, only zen overrode an emphasized easing, so five of six styles shared one curve and the channel carried no style character at all. Each style now takes its OWN spatial pair clamped into [0,1]. The clamp is what keeps the file invariant (only spatial-in/out/move may leave [0,1]) intact, and it is why bouncy's 1.85 crest lands at 1.0 here while staying 1.85 in spatial-in. md3 needs no override: it overrides no spatial easing, so the base pair is already correct for it by construction.",
      "easings": {
        "spatial-in": [0.1, 0.9, 0.4, 1.12],
        "spatial-out": [0.3, 0, 0.8, 1],
        "emphasized-decelerate": [0.1, 0.9, 0.4, 1.0],
        "emphasized-accelerate": [0.3, 0, 0.8, 1],
        "spatial-move": [0.1, 0.9, 0.4, 1.06],
        "standard": [0.05, 0.5, 0.1, 1],
        "effects": [0.05, 0.5, 0.1, 1]
      },
      "semantic": {
        "stagger-offset": { "duration": "micro1", "easing": "standard" },
        "ambient": { "duration": "extra-long1", "easing": "linear" },
        "standard": { "duration": "short3", "easing": "standard" },
        "standard-slow": { "duration": "short4", "easing": "standard" },
        "emphasized-in": { "duration": "short4", "easing": "emphasized-decelerate" },
        "emphasized-out": { "duration": "short2", "easing": "emphasized-accelerate" },
        "colour": { "duration": "short2", "easing": "effects" },
        "spatial-in": { "duration": "short4", "easing": "spatial-in" },
        "spatial-out": { "duration": "short2", "easing": "spatial-out" },
        "spatial-move": { "duration": "short3", "easing": "spatial-move" }
      },
      "hypr_leaves": {
        "windows_in": { "curve": "spatial-in", "speed_key": "leaf-short4", "style": "popin 80%" },
        "windows_out": { "curve": "spatial-out", "speed_key": "leaf-short3", "style": "popin 80%" },
        "windows_move": { "curve": "spatial-move", "speed_key": "leaf-short4", "style": "slide" },
        "workspaces": { "curve": "spatial-move", "speed_key": "leaf-short4", "style": "slide" },
        "special_workspace": { "curve": "spatial-in", "speed_key": "leaf-short4", "style": "slidevert" },
        "layers": { "curve": "spatial-move", "speed_key": "leaf-short4", "style": "popin 80%" },
        "layers_in": { "curve": "spatial-in", "speed_key": "leaf-short4", "style": "popin 80%" },
        "layers_out": { "curve": "spatial-out", "speed_key": "leaf-short3", "style": "popin 80%" },
        "fade_in": { "curve": "standard", "speed_key": "leaf-short4" },
        "fade_out": { "curve": "standard", "speed_key": "leaf-short3" },
        "fade_switch": { "curve": "standard", "speed_key": "leaf-short4" },
        "fade_shadow": { "curve": "standard", "speed_key": "leaf-short4" },
        "fade_dim": { "curve": "standard", "speed_key": "leaf-short4" }
      }
    },
    "bouncy": {
      "label": "Bouncy",
      "description": "Crests past the target, drops back through it, then settles — the snap-back of a bounce.",
      "_comment_260828_parity": "quick task 260828-mot: brought to zen's coverage. Before this, this style overrode 1-2 easings, 6 semantic names and 2-4 Hyprland leaves while zen overrode 4/8/13, so switching away from zen dropped nine leaves (windows_move, all three layers_*, all five fade_*) back to the base MD3 table and the style's own character stopped at the window border. spatial-out/spatial-move/effects are overridden here for the same reason. INVARIANT HELD: only spatial-in/out/move may leave [0,1] (the file's own rule) - `effects` drives colour and stays monotonic in every style below, because a colour that overshoots is simply the wrong colour briefly. Hyprland silently keeps the OLD curve for any control-point Y above 2.00; every value here is well under.",
      "_comment_260829_emphasized": "quick task 260829-wvy: the emphasized pair is DERIVED, not chosen. In the base table `emphasized-decelerate` is byte-identical to `spatial-in` and `emphasized-accelerate` to `spatial-out` — the emphasized curves ARE the monotonic form of the spatial ones. Before this, only zen overrode an emphasized easing, so five of six styles shared one curve and the channel carried no style character at all. Each style now takes its OWN spatial pair clamped into [0,1]. The clamp is what keeps the file invariant (only spatial-in/out/move may leave [0,1]) intact, and it is why bouncy's 1.85 crest lands at 1.0 here while staying 1.85 in spatial-in. md3 needs no override: it overrides no spatial easing, so the base pair is already correct for it by construction.",
      "easings": {
        "spatial-in": [0.4, 1.85, 0.75, 0.74],
        "spatial-out": [0.45, 0, 0.6, 1.15],
        "emphasized-decelerate": [0.4, 1.0, 0.75, 0.74],
        "emphasized-accelerate": [0.45, 0, 0.6, 1],
        "spatial-move": [0.4, 1.5, 0.6, 0.9],
        "effects": [0.3, 0, 0.5, 1]
      },
      "semantic": {
        "stagger-offset": { "duration": "micro3", "easing": "standard" },
        "ambient": { "duration": "extra-long2", "easing": "linear" },
        "standard": { "duration": "short4", "easing": "standard" },
        "standard-slow": { "duration": "long2", "easing": "standard" },
        "emphasized-in": { "duration": "long1", "easing": "emphasized-decelerate" },
        "emphasized-out": { "duration": "short3", "easing": "emphasized-accelerate" },
        "colour": { "duration": "short4", "easing": "effects" },
        "spatial-in": { "duration": "long1", "easing": "spatial-in" },
        "spatial-out": { "duration": "short3", "easing": "spatial-out" },
        "spatial-move": { "duration": "short4", "easing": "spatial-move" }
      },
      "hypr_leaves": {
        "windows_in": { "curve": "spatial-in", "speed_key": "leaf-long2", "style": "popin 0%" },
        "windows_out": { "curve": "spatial-out", "speed_key": "leaf-medium1", "style": "popin 0%" },
        "windows_move": { "curve": "spatial-move", "speed_key": "leaf-medium4", "style": "slide" },
        "workspaces": { "curve": "spatial-move", "speed_key": "leaf-medium4", "style": "slidevert" },
        "special_workspace": { "curve": "spatial-move", "speed_key": "leaf-medium4", "style": "slidevert" },
        "layers": { "curve": "spatial-move", "speed_key": "leaf-medium4", "style": "popin 0%" },
        "layers_in": { "curve": "spatial-in", "speed_key": "leaf-long2", "style": "popin 0%" },
        "layers_out": { "curve": "spatial-out", "speed_key": "leaf-medium1", "style": "popin 0%" },
        "fade_in": { "curve": "standard", "speed_key": "leaf-medium2" },
        "fade_out": { "curve": "standard", "speed_key": "leaf-medium1" },
        "fade_switch": { "curve": "standard", "speed_key": "leaf-medium2" },
        "fade_shadow": { "curve": "standard", "speed_key": "leaf-medium2" },
        "fade_dim": { "curve": "standard", "speed_key": "leaf-medium2" }
      }
    },
    "gravity": {
      "label": "Gravity",
      "description": "Starts slow, gathers speed, lands with a soft thud — motion with mass.",
      "_comment_260829_axis": "quick task 260829-grv, the style added in place of the merged-away `smooth`. Chosen by measuring which axis the set left EMPTY rather than by picking a mood: every pre-existing style front-loads its motion, covering 30-62% of the distance in the first 10% of the time (md3 62%, snappy 51%, smooth 44%, bouncy 41%, zen 32%). Not one of them starts gently. This one covers 0.8%, so it is the first style in the set with weight — things take a moment to get moving, then arrive. The landing is deliberately a thud and not a bounce: peak 1.0135 (+1.4%), just under zen's 1.014 and far below bouncy's 1.097, because mass settles, it does not spring. Getting a perceptible settle at all needed the overshoot pushed LATE in the curve (x2 0.65, not 0.25) — a slow start damps a trailing overshoot so hard that [0.75, 0, 0.25, 1.10] still peaks at only 1.0064.",
      "_comment_260829_emphasized": "Same derived rule as the other styles (260829-wvy): the emphasized pair is this style's own spatial pair clamped into [0,1], since in the base table emphasized-decelerate IS spatial-in and emphasized-accelerate IS spatial-out. The clamp is what holds the file invariant that only spatial-in/out/move may leave [0,1].",
      "easings": {
        "spatial-in": [0.7, 0, 0.65, 1.15],
        "spatial-out": [0.6, 0, 0.9, 0.3],
        "spatial-move": [0.7, 0, 0.6, 1.08],
        "emphasized-decelerate": [0.7, 0, 0.65, 1.0],
        "emphasized-accelerate": [0.6, 0, 0.9, 0.3],
        "standard": [0.65, 0, 0.35, 1],
        "effects": [0.5, 0, 0.4, 1]
      },
      "semantic": {
        "stagger-offset": { "duration": "micro4", "easing": "standard" },
        "ambient": { "duration": "extra-long5", "easing": "linear" },
        "standard": { "duration": "medium2", "easing": "standard" },
        "standard-slow": { "duration": "extra-long1", "easing": "standard" },
        "emphasized-in": { "duration": "long3", "easing": "emphasized-decelerate" },
        "emphasized-out": { "duration": "medium1", "easing": "emphasized-accelerate" },
        "colour": { "duration": "medium2", "easing": "effects" },
        "spatial-in": { "duration": "long3", "easing": "spatial-in" },
        "spatial-out": { "duration": "medium2", "easing": "spatial-out" },
        "spatial-move": { "duration": "long1", "easing": "spatial-move" }
      },
      "hypr_leaves": {
        "windows_in": { "curve": "spatial-in", "speed_key": "leaf-long4", "style": "slidevert" },
        "windows_out": { "curve": "spatial-out", "speed_key": "leaf-medium4", "style": "slidevert" },
        "windows_move": { "curve": "spatial-move", "speed_key": "leaf-long2", "style": "slide" },
        "workspaces": { "curve": "spatial-move", "speed_key": "leaf-long4", "style": "slidevert" },
        "special_workspace": { "curve": "spatial-in", "speed_key": "leaf-long2", "style": "slidevert" },
        "layers": { "curve": "spatial-move", "speed_key": "leaf-long2", "style": "slide" },
        "layers_in": { "curve": "spatial-in", "speed_key": "leaf-long2", "style": "slidevert" },
        "layers_out": { "curve": "spatial-out", "speed_key": "leaf-medium4", "style": "slidevert" },
        "fade_in": { "curve": "standard", "speed_key": "leaf-long2" },
        "fade_out": { "curve": "standard", "speed_key": "leaf-medium4" },
        "fade_switch": { "curve": "standard", "speed_key": "leaf-long2" },
        "fade_shadow": { "curve": "standard", "speed_key": "leaf-long2" },
        "fade_dim": { "curve": "standard", "speed_key": "leaf-long2" }
      }
    },
    "zen": {
      "label": "Zen",
      "description": "Caelestia's Hyprland animations, reproduced exactly \u2014 unhurried, gliding, no overshoot anywhere.",
      "_comment_provenance": "EXACT PORT of caelestia-dots/caelestia hypr/hyprland/animations.lua (read 2026-08-25 via the GitHub contents API). Source, with speeds in deciseconds (confirmed on this host: hyprctl animations reports a 450ms leaf as 4.50), so 5 = 500ms: layersIn 5/emphasizedDecel/slide, layersOut 4/emphasizedAccel/slide, windowsIn 5/emphasizedDecel/no style, windowsOut 3/emphasizedAccel/no style, windowsMove 6/standard/no style, workspaces 5/standard/no style, specialWorkspace 4/specialWorkSwitch/slidefadevert 15%, fade 6/standard, fadeDim 6/standard. In Hyprland's animation tree 'fade' is the PARENT of fadeIn/fadeOut/fadeSwitch/fadeShadow/fadeDim, so Caelestia's single fade=6/standard puts all five at 600ms on the standard curve \u2014 which is why all five are spelled out below rather than left alone.",
      "_comment_curves": "This style overrides NO easings, and that is a measured result rather than an omission: all four of Caelestia's curves are already BYTE-IDENTICAL to this repo's base easings. Caelestia 'standard' {0.2,0},{0,1} == our standard; 'emphasizedDecel' {0.05,0.7},{0.1,1} == our emphasized-decelerate; 'emphasizedAccel' {0.3,0},{0.8,0.15} == our emphasized-accelerate; and 'specialWorkSwitch' is a byte-for-byte duplicate of emphasizedDecel. What makes Caelestia feel like Caelestia is duration and leaf shape, not curvature.",
      "_comment_shell_curves": "Ported from caelestia-dots/shell plugin/src/Caelestia/Config/tokens.hpp (AnimCurves defaults, read 2026-08-25). ONLY the three expressive SPATIAL curves are overridden, because the other five Caelestia shell curves are already BYTE-IDENTICAL to this repo's base easings - standard {0.2,0,0,1}, standardAccel {0.3,0,1,1}, standardDecel {0,0,0,1}, emphasizedDecel {0.05,0.7,0.1,1}, emphasizedAccel {0.3,0,0.8,0.15} all match, verified value by value. THE SPLIT LINES UP EXACTLY WITH THIS REPO'S OWN DOCTRINE: Caelestia's expressive*Spatial curves overshoot and its expressive*Effects curves stay monotonic, which is the same rule quick-260821-swp arrived at independently - spatial names may overshoot, effects names never may. So the overshoot lands only on spatial-in/out/move, exactly where motion-lint CHECK E requires it. spatial-in and spatial-move take expressiveDefaultSpatial (Anim.qml's DEFAULT type, so it is what most of Caelestia's shell actually animates on); spatial-out takes expressiveFastSpatial, since an exit should out-run an entrance.",
      "easings": {
        "spatial-in": [0.38, 1.21, 0.22, 1],
        "spatial-out": [0.42, 1.67, 0.21, 0.9],
        "spatial-move": [0.38, 1.21, 0.22, 1],
        "effects": [0.34, 0.88, 0.34, 1]
      },
      "_comment_no_semantic": "This style overrides NO semantic entries either, and that is the DECOUPLING, deliberately chosen on 2026-08-25 after an earlier revision set semantic.standard to 600ms. That worked but was overreach: semantic.standard is also Motion.standardDuration, so every QML surface bound to it ran at 600ms, and Caelestia's animations.lua says nothing whatsoever about shell-side timing \u2014 its shell keeps its own separate tokens, which this repo has not ported. Each leaf below therefore names a leaf-only `speed_key` channel instead of borrowing a shared semantic name. THE CONSEQUENCE, worth stating outright: zen changes Hyprland's animations ONLY. Every QML surface animates exactly as it does under md3. If the shell should feel Caelestia-like too, that is a separate port of Caelestia's shell tokens, not a change to this style.",
      "_comment_shell_durations": "Ported from the same tokens.hpp (AnimDurationTokens defaults): small 200, normal 400, large 600, extraLarge 1000, expressiveFastSpatial 350, expressiveDefaultSpatial 500, expressiveSlowSpatial 650, expressiveFastEffects 150, expressiveDefaultEffects 200, expressiveSlowEffects 300. Every one of those values already exists in this repo's durations table, so nothing was rounded or invented. Mapping: standard -> Anim.Standard (normal 400); standard-slow -> Anim.StandardLarge (large 600); emphasized-in/-out -> Caelestia's emphasizedDecel/emphasizedAccel tokens at normal 400 / small 200; spatial-in and spatial-move -> expressiveDefaultSpatial 500; spatial-out -> expressiveFastSpatial 350. 'ambient' is left at base because base ALREADY equals Caelestia's extraLarge 1000, and 'stagger-offset' is left at base because Caelestia has no counterpart for it.",
      "_comment_shell_fully_ported": "The two tokens an earlier revision could not carry are now BOTH ported, after the operator ruled that expanding this repo's motion language beats dropping Caelestia values. (1) `emphasized` is Caelestia's real MD3 two-segment spline and is what emphasized-in/-out now use, at Anim.Emphasized's normal 400 / small 200 - Caelestia drives BOTH directions from that one curve rather than from a decel/accel pair, so pointing both at it is the faithful reading. It is QML-only of necessity; see _comment_multi_segment_easings at the top of this file. (2) `colour` is a dedicated colour/effects channel carrying Caelestia's CAnim token, expressiveSlowEffects at 300ms - every ColorAnimation and colour Behavior in the shell binds it now instead of the generic `standard` pair. quick-260821-swp had rejected such a channel as 90 retargets and default-unsafe, and the SAFETY half of that objection is answered structurally rather than by promise: `colour` and its `effects` easing sit on the permanently-monotonic side of the R-2 split, so a colour still cannot overshoot in any style - which matters more than for opacity, because an overshooting colour interpolation extrapolates PAST the target where opacity would clamp.",
      "semantic": {
        "stagger-offset": { "duration": "micro2", "easing": "standard" },
        "ambient": { "duration": "extra-long3", "easing": "linear" },
        "standard": { "duration": "medium4", "easing": "standard" },
        "standard-slow": { "duration": "long4", "easing": "standard" },
        "emphasized-in": { "duration": "medium4", "easing": "emphasized" },
        "emphasized-out": { "duration": "short4", "easing": "emphasized" },
        "colour": { "duration": "medium2", "easing": "effects" },
        "spatial-in": { "duration": "long2", "easing": "spatial-in" },
        "spatial-out": { "duration": "medium3", "easing": "spatial-out" },
        "spatial-move": { "duration": "long2", "easing": "spatial-move" }
      },
      "_comment_hypr_unaffected": "These shell tokens do NOT disturb the compositor half. Every one of zen's thirteen hypr_leaves below names its own leaf-* `speed_key` and one of standard/emphasized-decelerate/emphasized-accelerate as its curve, so not one of them reads a semantic duration or a spatial easing changed above. The Hyprland side stays the exact Caelestia port it already was - re-verified live across all twelve leaves after this change.",
      "_comment_leaves": "windows_in/out/move, workspaces and the five fade leaves carry a curve but NO style key, reproducing Caelestia exactly: it passes no style for those leaves, so Hyprland's own default applies. register_hypr_leaf only sets args.style when entry.style ~= nil, and a style's leaf entry REPLACES the base entry wholesale, so omitting the key genuinely drops base's 'popin 60%'/'slide' rather than inheriting it. The parent 'layers' leaf has no Caelestia counterpart (its config declares layersIn/layersOut/fadeLayers only); it mirrors layersIn so the inheritance slot stays declared, as animations.lua's own comment requires.",
      "hypr_leaves": {
        "windows_in": { "curve": "emphasized-decelerate", "speed_key": "leaf-long2" },
        "windows_out": { "curve": "emphasized-accelerate", "speed_key": "leaf-medium2" },
        "windows_move": { "curve": "standard", "speed_key": "leaf-long4" },
        "workspaces": { "curve": "standard", "speed_key": "leaf-long2" },
        "special_workspace": { "curve": "emphasized-decelerate", "speed_key": "leaf-medium4", "style": "slidefadevert 15%" },
        "layers": { "curve": "emphasized-decelerate", "speed_key": "leaf-long2", "style": "slide" },
        "layers_in": { "curve": "emphasized-decelerate", "speed_key": "leaf-long2", "style": "slide" },
        "layers_out": { "curve": "emphasized-accelerate", "speed_key": "leaf-medium4", "style": "slide" },
        "fade_in": { "curve": "standard", "speed_key": "leaf-long4" },
        "fade_out": { "curve": "standard", "speed_key": "leaf-long4" },
        "fade_switch": { "curve": "standard", "speed_key": "leaf-long4" },
        "fade_shadow": { "curve": "standard", "speed_key": "leaf-long4" },
        "fade_dim": { "curve": "standard", "speed_key": "leaf-long4" }
      }
    }
  },
  "floor_ms": 40
}
